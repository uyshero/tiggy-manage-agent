var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var _a;
(function polyfill() {
  const relList = document.createElement("link").relList;
  if (relList && relList.supports && relList.supports("modulepreload")) {
    return;
  }
  for (const link2 of document.querySelectorAll('link[rel="modulepreload"]')) {
    processPreload(link2);
  }
  new MutationObserver((mutations) => {
    for (const mutation of mutations) {
      if (mutation.type !== "childList") {
        continue;
      }
      for (const node2 of mutation.addedNodes) {
        if (node2.tagName === "LINK" && node2.rel === "modulepreload")
          processPreload(node2);
      }
    }
  }).observe(document, { childList: true, subtree: true });
  function getFetchOpts(link2) {
    const fetchOpts = {};
    if (link2.integrity) fetchOpts.integrity = link2.integrity;
    if (link2.referrerPolicy) fetchOpts.referrerPolicy = link2.referrerPolicy;
    if (link2.crossOrigin === "use-credentials")
      fetchOpts.credentials = "include";
    else if (link2.crossOrigin === "anonymous") fetchOpts.credentials = "omit";
    else fetchOpts.credentials = "same-origin";
    return fetchOpts;
  }
  function processPreload(link2) {
    if (link2.ep)
      return;
    link2.ep = true;
    const fetchOpts = getFetchOpts(link2);
    fetch(link2.href, fetchOpts);
  }
})();
function getDefaultExportFromCjs(x) {
  return x && x.__esModule && Object.prototype.hasOwnProperty.call(x, "default") ? x["default"] : x;
}
var jsxRuntime = { exports: {} };
var reactJsxRuntime_production = {};
/**
 * @license React
 * react-jsx-runtime.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var hasRequiredReactJsxRuntime_production;
function requireReactJsxRuntime_production() {
  if (hasRequiredReactJsxRuntime_production) return reactJsxRuntime_production;
  hasRequiredReactJsxRuntime_production = 1;
  var REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment");
  function jsxProd(type, config, maybeKey) {
    var key = null;
    void 0 !== maybeKey && (key = "" + maybeKey);
    void 0 !== config.key && (key = "" + config.key);
    if ("key" in config) {
      maybeKey = {};
      for (var propName in config)
        "key" !== propName && (maybeKey[propName] = config[propName]);
    } else maybeKey = config;
    config = maybeKey.ref;
    return {
      $$typeof: REACT_ELEMENT_TYPE,
      type,
      key,
      ref: void 0 !== config ? config : null,
      props: maybeKey
    };
  }
  reactJsxRuntime_production.Fragment = REACT_FRAGMENT_TYPE;
  reactJsxRuntime_production.jsx = jsxProd;
  reactJsxRuntime_production.jsxs = jsxProd;
  return reactJsxRuntime_production;
}
var hasRequiredJsxRuntime;
function requireJsxRuntime() {
  if (hasRequiredJsxRuntime) return jsxRuntime.exports;
  hasRequiredJsxRuntime = 1;
  {
    jsxRuntime.exports = requireReactJsxRuntime_production();
  }
  return jsxRuntime.exports;
}
var jsxRuntimeExports = requireJsxRuntime();
var react = { exports: {} };
var react_production = {};
/**
 * @license React
 * react.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var hasRequiredReact_production;
function requireReact_production() {
  if (hasRequiredReact_production) return react_production;
  hasRequiredReact_production = 1;
  var REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"), REACT_MEMO_TYPE = Symbol.for("react.memo"), REACT_LAZY_TYPE = Symbol.for("react.lazy"), REACT_ACTIVITY_TYPE = Symbol.for("react.activity"), MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
  function getIteratorFn(maybeIterable) {
    if (null === maybeIterable || "object" !== typeof maybeIterable) return null;
    maybeIterable = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable["@@iterator"];
    return "function" === typeof maybeIterable ? maybeIterable : null;
  }
  var ReactNoopUpdateQueue = {
    isMounted: function() {
      return false;
    },
    enqueueForceUpdate: function() {
    },
    enqueueReplaceState: function() {
    },
    enqueueSetState: function() {
    }
  }, assign = Object.assign, emptyObject = {};
  function Component(props, context, updater) {
    this.props = props;
    this.context = context;
    this.refs = emptyObject;
    this.updater = updater || ReactNoopUpdateQueue;
  }
  Component.prototype.isReactComponent = {};
  Component.prototype.setState = function(partialState, callback) {
    if ("object" !== typeof partialState && "function" !== typeof partialState && null != partialState)
      throw Error(
        "takes an object of state variables to update or a function which returns an object of state variables."
      );
    this.updater.enqueueSetState(this, partialState, callback, "setState");
  };
  Component.prototype.forceUpdate = function(callback) {
    this.updater.enqueueForceUpdate(this, callback, "forceUpdate");
  };
  function ComponentDummy() {
  }
  ComponentDummy.prototype = Component.prototype;
  function PureComponent(props, context, updater) {
    this.props = props;
    this.context = context;
    this.refs = emptyObject;
    this.updater = updater || ReactNoopUpdateQueue;
  }
  var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();
  pureComponentPrototype.constructor = PureComponent;
  assign(pureComponentPrototype, Component.prototype);
  pureComponentPrototype.isPureReactComponent = true;
  var isArrayImpl = Array.isArray;
  function noop2() {
  }
  var ReactSharedInternals = { H: null, A: null, T: null, S: null }, hasOwnProperty2 = Object.prototype.hasOwnProperty;
  function ReactElement(type, key, props) {
    var refProp = props.ref;
    return {
      $$typeof: REACT_ELEMENT_TYPE,
      type,
      key,
      ref: void 0 !== refProp ? refProp : null,
      props
    };
  }
  function cloneAndReplaceKey(oldElement, newKey) {
    return ReactElement(oldElement.type, newKey, oldElement.props);
  }
  function isValidElement(object) {
    return "object" === typeof object && null !== object && object.$$typeof === REACT_ELEMENT_TYPE;
  }
  function escape(key) {
    var escaperLookup = { "=": "=0", ":": "=2" };
    return "$" + key.replace(/[=:]/g, function(match) {
      return escaperLookup[match];
    });
  }
  var userProvidedKeyEscapeRegex = /\/+/g;
  function getElementKey(element2, index2) {
    return "object" === typeof element2 && null !== element2 && null != element2.key ? escape("" + element2.key) : index2.toString(36);
  }
  function resolveThenable(thenable) {
    switch (thenable.status) {
      case "fulfilled":
        return thenable.value;
      case "rejected":
        throw thenable.reason;
      default:
        switch ("string" === typeof thenable.status ? thenable.then(noop2, noop2) : (thenable.status = "pending", thenable.then(
          function(fulfilledValue) {
            "pending" === thenable.status && (thenable.status = "fulfilled", thenable.value = fulfilledValue);
          },
          function(error) {
            "pending" === thenable.status && (thenable.status = "rejected", thenable.reason = error);
          }
        )), thenable.status) {
          case "fulfilled":
            return thenable.value;
          case "rejected":
            throw thenable.reason;
        }
    }
    throw thenable;
  }
  function mapIntoArray(children, array, escapedPrefix, nameSoFar, callback) {
    var type = typeof children;
    if ("undefined" === type || "boolean" === type) children = null;
    var invokeCallback = false;
    if (null === children) invokeCallback = true;
    else
      switch (type) {
        case "bigint":
        case "string":
        case "number":
          invokeCallback = true;
          break;
        case "object":
          switch (children.$$typeof) {
            case REACT_ELEMENT_TYPE:
            case REACT_PORTAL_TYPE:
              invokeCallback = true;
              break;
            case REACT_LAZY_TYPE:
              return invokeCallback = children._init, mapIntoArray(
                invokeCallback(children._payload),
                array,
                escapedPrefix,
                nameSoFar,
                callback
              );
          }
      }
    if (invokeCallback)
      return callback = callback(children), invokeCallback = "" === nameSoFar ? "." + getElementKey(children, 0) : nameSoFar, isArrayImpl(callback) ? (escapedPrefix = "", null != invokeCallback && (escapedPrefix = invokeCallback.replace(userProvidedKeyEscapeRegex, "$&/") + "/"), mapIntoArray(callback, array, escapedPrefix, "", function(c) {
        return c;
      })) : null != callback && (isValidElement(callback) && (callback = cloneAndReplaceKey(
        callback,
        escapedPrefix + (null == callback.key || children && children.key === callback.key ? "" : ("" + callback.key).replace(
          userProvidedKeyEscapeRegex,
          "$&/"
        ) + "/") + invokeCallback
      )), array.push(callback)), 1;
    invokeCallback = 0;
    var nextNamePrefix = "" === nameSoFar ? "." : nameSoFar + ":";
    if (isArrayImpl(children))
      for (var i = 0; i < children.length; i++)
        nameSoFar = children[i], type = nextNamePrefix + getElementKey(nameSoFar, i), invokeCallback += mapIntoArray(
          nameSoFar,
          array,
          escapedPrefix,
          type,
          callback
        );
    else if (i = getIteratorFn(children), "function" === typeof i)
      for (children = i.call(children), i = 0; !(nameSoFar = children.next()).done; )
        nameSoFar = nameSoFar.value, type = nextNamePrefix + getElementKey(nameSoFar, i++), invokeCallback += mapIntoArray(
          nameSoFar,
          array,
          escapedPrefix,
          type,
          callback
        );
    else if ("object" === type) {
      if ("function" === typeof children.then)
        return mapIntoArray(
          resolveThenable(children),
          array,
          escapedPrefix,
          nameSoFar,
          callback
        );
      array = String(children);
      throw Error(
        "Objects are not valid as a React child (found: " + ("[object Object]" === array ? "object with keys {" + Object.keys(children).join(", ") + "}" : array) + "). If you meant to render a collection of children, use an array instead."
      );
    }
    return invokeCallback;
  }
  function mapChildren(children, func, context) {
    if (null == children) return children;
    var result = [], count = 0;
    mapIntoArray(children, result, "", "", function(child) {
      return func.call(context, child, count++);
    });
    return result;
  }
  function lazyInitializer(payload2) {
    if (-1 === payload2._status) {
      var ctor = payload2._result;
      ctor = ctor();
      ctor.then(
        function(moduleObject) {
          if (0 === payload2._status || -1 === payload2._status)
            payload2._status = 1, payload2._result = moduleObject;
        },
        function(error) {
          if (0 === payload2._status || -1 === payload2._status)
            payload2._status = 2, payload2._result = error;
        }
      );
      -1 === payload2._status && (payload2._status = 0, payload2._result = ctor);
    }
    if (1 === payload2._status) return payload2._result.default;
    throw payload2._result;
  }
  var reportGlobalError = "function" === typeof reportError ? reportError : function(error) {
    if ("object" === typeof window && "function" === typeof window.ErrorEvent) {
      var event = new window.ErrorEvent("error", {
        bubbles: true,
        cancelable: true,
        message: "object" === typeof error && null !== error && "string" === typeof error.message ? String(error.message) : String(error),
        error
      });
      if (!window.dispatchEvent(event)) return;
    } else if ("object" === typeof process && "function" === typeof process.emit) {
      process.emit("uncaughtException", error);
      return;
    }
    console.error(error);
  }, Children = {
    map: mapChildren,
    forEach: function(children, forEachFunc, forEachContext) {
      mapChildren(
        children,
        function() {
          forEachFunc.apply(this, arguments);
        },
        forEachContext
      );
    },
    count: function(children) {
      var n = 0;
      mapChildren(children, function() {
        n++;
      });
      return n;
    },
    toArray: function(children) {
      return mapChildren(children, function(child) {
        return child;
      }) || [];
    },
    only: function(children) {
      if (!isValidElement(children))
        throw Error(
          "React.Children.only expected to receive a single React element child."
        );
      return children;
    }
  };
  react_production.Activity = REACT_ACTIVITY_TYPE;
  react_production.Children = Children;
  react_production.Component = Component;
  react_production.Fragment = REACT_FRAGMENT_TYPE;
  react_production.Profiler = REACT_PROFILER_TYPE;
  react_production.PureComponent = PureComponent;
  react_production.StrictMode = REACT_STRICT_MODE_TYPE;
  react_production.Suspense = REACT_SUSPENSE_TYPE;
  react_production.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE = ReactSharedInternals;
  react_production.__COMPILER_RUNTIME = {
    __proto__: null,
    c: function(size) {
      return ReactSharedInternals.H.useMemoCache(size);
    }
  };
  react_production.cache = function(fn) {
    return function() {
      return fn.apply(null, arguments);
    };
  };
  react_production.cacheSignal = function() {
    return null;
  };
  react_production.cloneElement = function(element2, config, children) {
    if (null === element2 || void 0 === element2)
      throw Error(
        "The argument must be a React element, but you passed " + element2 + "."
      );
    var props = assign({}, element2.props), key = element2.key;
    if (null != config)
      for (propName in void 0 !== config.key && (key = "" + config.key), config)
        !hasOwnProperty2.call(config, propName) || "key" === propName || "__self" === propName || "__source" === propName || "ref" === propName && void 0 === config.ref || (props[propName] = config[propName]);
    var propName = arguments.length - 2;
    if (1 === propName) props.children = children;
    else if (1 < propName) {
      for (var childArray = Array(propName), i = 0; i < propName; i++)
        childArray[i] = arguments[i + 2];
      props.children = childArray;
    }
    return ReactElement(element2.type, key, props);
  };
  react_production.createContext = function(defaultValue) {
    defaultValue = {
      $$typeof: REACT_CONTEXT_TYPE,
      _currentValue: defaultValue,
      _currentValue2: defaultValue,
      _threadCount: 0,
      Provider: null,
      Consumer: null
    };
    defaultValue.Provider = defaultValue;
    defaultValue.Consumer = {
      $$typeof: REACT_CONSUMER_TYPE,
      _context: defaultValue
    };
    return defaultValue;
  };
  react_production.createElement = function(type, config, children) {
    var propName, props = {}, key = null;
    if (null != config)
      for (propName in void 0 !== config.key && (key = "" + config.key), config)
        hasOwnProperty2.call(config, propName) && "key" !== propName && "__self" !== propName && "__source" !== propName && (props[propName] = config[propName]);
    var childrenLength = arguments.length - 2;
    if (1 === childrenLength) props.children = children;
    else if (1 < childrenLength) {
      for (var childArray = Array(childrenLength), i = 0; i < childrenLength; i++)
        childArray[i] = arguments[i + 2];
      props.children = childArray;
    }
    if (type && type.defaultProps)
      for (propName in childrenLength = type.defaultProps, childrenLength)
        void 0 === props[propName] && (props[propName] = childrenLength[propName]);
    return ReactElement(type, key, props);
  };
  react_production.createRef = function() {
    return { current: null };
  };
  react_production.forwardRef = function(render) {
    return { $$typeof: REACT_FORWARD_REF_TYPE, render };
  };
  react_production.isValidElement = isValidElement;
  react_production.lazy = function(ctor) {
    return {
      $$typeof: REACT_LAZY_TYPE,
      _payload: { _status: -1, _result: ctor },
      _init: lazyInitializer
    };
  };
  react_production.memo = function(type, compare) {
    return {
      $$typeof: REACT_MEMO_TYPE,
      type,
      compare: void 0 === compare ? null : compare
    };
  };
  react_production.startTransition = function(scope) {
    var prevTransition = ReactSharedInternals.T, currentTransition = {};
    ReactSharedInternals.T = currentTransition;
    try {
      var returnValue = scope(), onStartTransitionFinish = ReactSharedInternals.S;
      null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
      "object" === typeof returnValue && null !== returnValue && "function" === typeof returnValue.then && returnValue.then(noop2, reportGlobalError);
    } catch (error) {
      reportGlobalError(error);
    } finally {
      null !== prevTransition && null !== currentTransition.types && (prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition;
    }
  };
  react_production.unstable_useCacheRefresh = function() {
    return ReactSharedInternals.H.useCacheRefresh();
  };
  react_production.use = function(usable) {
    return ReactSharedInternals.H.use(usable);
  };
  react_production.useActionState = function(action, initialState, permalink) {
    return ReactSharedInternals.H.useActionState(action, initialState, permalink);
  };
  react_production.useCallback = function(callback, deps) {
    return ReactSharedInternals.H.useCallback(callback, deps);
  };
  react_production.useContext = function(Context) {
    return ReactSharedInternals.H.useContext(Context);
  };
  react_production.useDebugValue = function() {
  };
  react_production.useDeferredValue = function(value, initialValue) {
    return ReactSharedInternals.H.useDeferredValue(value, initialValue);
  };
  react_production.useEffect = function(create2, deps) {
    return ReactSharedInternals.H.useEffect(create2, deps);
  };
  react_production.useEffectEvent = function(callback) {
    return ReactSharedInternals.H.useEffectEvent(callback);
  };
  react_production.useId = function() {
    return ReactSharedInternals.H.useId();
  };
  react_production.useImperativeHandle = function(ref, create2, deps) {
    return ReactSharedInternals.H.useImperativeHandle(ref, create2, deps);
  };
  react_production.useInsertionEffect = function(create2, deps) {
    return ReactSharedInternals.H.useInsertionEffect(create2, deps);
  };
  react_production.useLayoutEffect = function(create2, deps) {
    return ReactSharedInternals.H.useLayoutEffect(create2, deps);
  };
  react_production.useMemo = function(create2, deps) {
    return ReactSharedInternals.H.useMemo(create2, deps);
  };
  react_production.useOptimistic = function(passthrough, reducer) {
    return ReactSharedInternals.H.useOptimistic(passthrough, reducer);
  };
  react_production.useReducer = function(reducer, initialArg, init) {
    return ReactSharedInternals.H.useReducer(reducer, initialArg, init);
  };
  react_production.useRef = function(initialValue) {
    return ReactSharedInternals.H.useRef(initialValue);
  };
  react_production.useState = function(initialState) {
    return ReactSharedInternals.H.useState(initialState);
  };
  react_production.useSyncExternalStore = function(subscribe, getSnapshot, getServerSnapshot) {
    return ReactSharedInternals.H.useSyncExternalStore(
      subscribe,
      getSnapshot,
      getServerSnapshot
    );
  };
  react_production.useTransition = function() {
    return ReactSharedInternals.H.useTransition();
  };
  react_production.version = "19.2.7";
  return react_production;
}
var hasRequiredReact;
function requireReact() {
  if (hasRequiredReact) return react.exports;
  hasRequiredReact = 1;
  {
    react.exports = requireReact_production();
  }
  return react.exports;
}
var reactExports = requireReact();
const React = /* @__PURE__ */ getDefaultExportFromCjs(reactExports);
var reactDom = { exports: {} };
var reactDom_production = {};
/**
 * @license React
 * react-dom.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var hasRequiredReactDom_production;
function requireReactDom_production() {
  if (hasRequiredReactDom_production) return reactDom_production;
  hasRequiredReactDom_production = 1;
  var React2 = requireReact();
  function formatProdErrorMessage(code2) {
    var url = "https://react.dev/errors/" + code2;
    if (1 < arguments.length) {
      url += "?args[]=" + encodeURIComponent(arguments[1]);
      for (var i = 2; i < arguments.length; i++)
        url += "&args[]=" + encodeURIComponent(arguments[i]);
    }
    return "Minified React error #" + code2 + "; visit " + url + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
  }
  function noop2() {
  }
  var Internals = {
    d: {
      f: noop2,
      r: function() {
        throw Error(formatProdErrorMessage(522));
      },
      D: noop2,
      C: noop2,
      L: noop2,
      m: noop2,
      X: noop2,
      S: noop2,
      M: noop2
    },
    p: 0,
    findDOMNode: null
  }, REACT_PORTAL_TYPE = Symbol.for("react.portal");
  function createPortal$1(children, containerInfo, implementation) {
    var key = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : null;
    return {
      $$typeof: REACT_PORTAL_TYPE,
      key: null == key ? null : "" + key,
      children,
      containerInfo,
      implementation
    };
  }
  var ReactSharedInternals = React2.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
  function getCrossOriginStringAs(as, input) {
    if ("font" === as) return "";
    if ("string" === typeof input)
      return "use-credentials" === input ? input : "";
  }
  reactDom_production.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE = Internals;
  reactDom_production.createPortal = function(children, container) {
    var key = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : null;
    if (!container || 1 !== container.nodeType && 9 !== container.nodeType && 11 !== container.nodeType)
      throw Error(formatProdErrorMessage(299));
    return createPortal$1(children, container, null, key);
  };
  reactDom_production.flushSync = function(fn) {
    var previousTransition = ReactSharedInternals.T, previousUpdatePriority = Internals.p;
    try {
      if (ReactSharedInternals.T = null, Internals.p = 2, fn) return fn();
    } finally {
      ReactSharedInternals.T = previousTransition, Internals.p = previousUpdatePriority, Internals.d.f();
    }
  };
  reactDom_production.preconnect = function(href, options) {
    "string" === typeof href && (options ? (options = options.crossOrigin, options = "string" === typeof options ? "use-credentials" === options ? options : "" : void 0) : options = null, Internals.d.C(href, options));
  };
  reactDom_production.prefetchDNS = function(href) {
    "string" === typeof href && Internals.d.D(href);
  };
  reactDom_production.preinit = function(href, options) {
    if ("string" === typeof href && options && "string" === typeof options.as) {
      var as = options.as, crossOrigin = getCrossOriginStringAs(as, options.crossOrigin), integrity = "string" === typeof options.integrity ? options.integrity : void 0, fetchPriority = "string" === typeof options.fetchPriority ? options.fetchPriority : void 0;
      "style" === as ? Internals.d.S(
        href,
        "string" === typeof options.precedence ? options.precedence : void 0,
        {
          crossOrigin,
          integrity,
          fetchPriority
        }
      ) : "script" === as && Internals.d.X(href, {
        crossOrigin,
        integrity,
        fetchPriority,
        nonce: "string" === typeof options.nonce ? options.nonce : void 0
      });
    }
  };
  reactDom_production.preinitModule = function(href, options) {
    if ("string" === typeof href)
      if ("object" === typeof options && null !== options) {
        if (null == options.as || "script" === options.as) {
          var crossOrigin = getCrossOriginStringAs(
            options.as,
            options.crossOrigin
          );
          Internals.d.M(href, {
            crossOrigin,
            integrity: "string" === typeof options.integrity ? options.integrity : void 0,
            nonce: "string" === typeof options.nonce ? options.nonce : void 0
          });
        }
      } else null == options && Internals.d.M(href);
  };
  reactDom_production.preload = function(href, options) {
    if ("string" === typeof href && "object" === typeof options && null !== options && "string" === typeof options.as) {
      var as = options.as, crossOrigin = getCrossOriginStringAs(as, options.crossOrigin);
      Internals.d.L(href, as, {
        crossOrigin,
        integrity: "string" === typeof options.integrity ? options.integrity : void 0,
        nonce: "string" === typeof options.nonce ? options.nonce : void 0,
        type: "string" === typeof options.type ? options.type : void 0,
        fetchPriority: "string" === typeof options.fetchPriority ? options.fetchPriority : void 0,
        referrerPolicy: "string" === typeof options.referrerPolicy ? options.referrerPolicy : void 0,
        imageSrcSet: "string" === typeof options.imageSrcSet ? options.imageSrcSet : void 0,
        imageSizes: "string" === typeof options.imageSizes ? options.imageSizes : void 0,
        media: "string" === typeof options.media ? options.media : void 0
      });
    }
  };
  reactDom_production.preloadModule = function(href, options) {
    if ("string" === typeof href)
      if (options) {
        var crossOrigin = getCrossOriginStringAs(options.as, options.crossOrigin);
        Internals.d.m(href, {
          as: "string" === typeof options.as && "script" !== options.as ? options.as : void 0,
          crossOrigin,
          integrity: "string" === typeof options.integrity ? options.integrity : void 0
        });
      } else Internals.d.m(href);
  };
  reactDom_production.requestFormReset = function(form) {
    Internals.d.r(form);
  };
  reactDom_production.unstable_batchedUpdates = function(fn, a) {
    return fn(a);
  };
  reactDom_production.useFormState = function(action, initialState, permalink) {
    return ReactSharedInternals.H.useFormState(action, initialState, permalink);
  };
  reactDom_production.useFormStatus = function() {
    return ReactSharedInternals.H.useHostTransitionStatus();
  };
  reactDom_production.version = "19.2.7";
  return reactDom_production;
}
var hasRequiredReactDom;
function requireReactDom() {
  if (hasRequiredReactDom) return reactDom.exports;
  hasRequiredReactDom = 1;
  function checkDCE() {
    if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === "undefined" || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== "function") {
      return;
    }
    try {
      __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
    } catch (err) {
      console.error(err);
    }
  }
  {
    checkDCE();
    reactDom.exports = requireReactDom_production();
  }
  return reactDom.exports;
}
var reactDomExports = requireReactDom();
var client = { exports: {} };
var reactDomClient_production = {};
var scheduler = { exports: {} };
var scheduler_production = {};
/**
 * @license React
 * scheduler.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var hasRequiredScheduler_production;
function requireScheduler_production() {
  if (hasRequiredScheduler_production) return scheduler_production;
  hasRequiredScheduler_production = 1;
  (function(exports) {
    function push2(heap, node2) {
      var index2 = heap.length;
      heap.push(node2);
      a: for (; 0 < index2; ) {
        var parentIndex = index2 - 1 >>> 1, parent = heap[parentIndex];
        if (0 < compare(parent, node2))
          heap[parentIndex] = node2, heap[index2] = parent, index2 = parentIndex;
        else break a;
      }
    }
    function peek(heap) {
      return 0 === heap.length ? null : heap[0];
    }
    function pop(heap) {
      if (0 === heap.length) return null;
      var first = heap[0], last = heap.pop();
      if (last !== first) {
        heap[0] = last;
        a: for (var index2 = 0, length = heap.length, halfLength = length >>> 1; index2 < halfLength; ) {
          var leftIndex = 2 * (index2 + 1) - 1, left = heap[leftIndex], rightIndex = leftIndex + 1, right = heap[rightIndex];
          if (0 > compare(left, last))
            rightIndex < length && 0 > compare(right, left) ? (heap[index2] = right, heap[rightIndex] = last, index2 = rightIndex) : (heap[index2] = left, heap[leftIndex] = last, index2 = leftIndex);
          else if (rightIndex < length && 0 > compare(right, last))
            heap[index2] = right, heap[rightIndex] = last, index2 = rightIndex;
          else break a;
        }
      }
      return first;
    }
    function compare(a, b) {
      var diff = a.sortIndex - b.sortIndex;
      return 0 !== diff ? diff : a.id - b.id;
    }
    exports.unstable_now = void 0;
    if ("object" === typeof performance && "function" === typeof performance.now) {
      var localPerformance = performance;
      exports.unstable_now = function() {
        return localPerformance.now();
      };
    } else {
      var localDate = Date, initialTime = localDate.now();
      exports.unstable_now = function() {
        return localDate.now() - initialTime;
      };
    }
    var taskQueue = [], timerQueue = [], taskIdCounter = 1, currentTask = null, currentPriorityLevel = 3, isPerformingWork = false, isHostCallbackScheduled = false, isHostTimeoutScheduled = false, needsPaint = false, localSetTimeout = "function" === typeof setTimeout ? setTimeout : null, localClearTimeout = "function" === typeof clearTimeout ? clearTimeout : null, localSetImmediate = "undefined" !== typeof setImmediate ? setImmediate : null;
    function advanceTimers(currentTime) {
      for (var timer = peek(timerQueue); null !== timer; ) {
        if (null === timer.callback) pop(timerQueue);
        else if (timer.startTime <= currentTime)
          pop(timerQueue), timer.sortIndex = timer.expirationTime, push2(taskQueue, timer);
        else break;
        timer = peek(timerQueue);
      }
    }
    function handleTimeout(currentTime) {
      isHostTimeoutScheduled = false;
      advanceTimers(currentTime);
      if (!isHostCallbackScheduled)
        if (null !== peek(taskQueue))
          isHostCallbackScheduled = true, isMessageLoopRunning || (isMessageLoopRunning = true, schedulePerformWorkUntilDeadline());
        else {
          var firstTimer = peek(timerQueue);
          null !== firstTimer && requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
        }
    }
    var isMessageLoopRunning = false, taskTimeoutID = -1, frameInterval = 5, startTime = -1;
    function shouldYieldToHost() {
      return needsPaint ? true : exports.unstable_now() - startTime < frameInterval ? false : true;
    }
    function performWorkUntilDeadline() {
      needsPaint = false;
      if (isMessageLoopRunning) {
        var currentTime = exports.unstable_now();
        startTime = currentTime;
        var hasMoreWork = true;
        try {
          a: {
            isHostCallbackScheduled = false;
            isHostTimeoutScheduled && (isHostTimeoutScheduled = false, localClearTimeout(taskTimeoutID), taskTimeoutID = -1);
            isPerformingWork = true;
            var previousPriorityLevel = currentPriorityLevel;
            try {
              b: {
                advanceTimers(currentTime);
                for (currentTask = peek(taskQueue); null !== currentTask && !(currentTask.expirationTime > currentTime && shouldYieldToHost()); ) {
                  var callback = currentTask.callback;
                  if ("function" === typeof callback) {
                    currentTask.callback = null;
                    currentPriorityLevel = currentTask.priorityLevel;
                    var continuationCallback = callback(
                      currentTask.expirationTime <= currentTime
                    );
                    currentTime = exports.unstable_now();
                    if ("function" === typeof continuationCallback) {
                      currentTask.callback = continuationCallback;
                      advanceTimers(currentTime);
                      hasMoreWork = true;
                      break b;
                    }
                    currentTask === peek(taskQueue) && pop(taskQueue);
                    advanceTimers(currentTime);
                  } else pop(taskQueue);
                  currentTask = peek(taskQueue);
                }
                if (null !== currentTask) hasMoreWork = true;
                else {
                  var firstTimer = peek(timerQueue);
                  null !== firstTimer && requestHostTimeout(
                    handleTimeout,
                    firstTimer.startTime - currentTime
                  );
                  hasMoreWork = false;
                }
              }
              break a;
            } finally {
              currentTask = null, currentPriorityLevel = previousPriorityLevel, isPerformingWork = false;
            }
            hasMoreWork = void 0;
          }
        } finally {
          hasMoreWork ? schedulePerformWorkUntilDeadline() : isMessageLoopRunning = false;
        }
      }
    }
    var schedulePerformWorkUntilDeadline;
    if ("function" === typeof localSetImmediate)
      schedulePerformWorkUntilDeadline = function() {
        localSetImmediate(performWorkUntilDeadline);
      };
    else if ("undefined" !== typeof MessageChannel) {
      var channel = new MessageChannel(), port = channel.port2;
      channel.port1.onmessage = performWorkUntilDeadline;
      schedulePerformWorkUntilDeadline = function() {
        port.postMessage(null);
      };
    } else
      schedulePerformWorkUntilDeadline = function() {
        localSetTimeout(performWorkUntilDeadline, 0);
      };
    function requestHostTimeout(callback, ms) {
      taskTimeoutID = localSetTimeout(function() {
        callback(exports.unstable_now());
      }, ms);
    }
    exports.unstable_IdlePriority = 5;
    exports.unstable_ImmediatePriority = 1;
    exports.unstable_LowPriority = 4;
    exports.unstable_NormalPriority = 3;
    exports.unstable_Profiling = null;
    exports.unstable_UserBlockingPriority = 2;
    exports.unstable_cancelCallback = function(task) {
      task.callback = null;
    };
    exports.unstable_forceFrameRate = function(fps) {
      0 > fps || 125 < fps ? console.error(
        "forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported"
      ) : frameInterval = 0 < fps ? Math.floor(1e3 / fps) : 5;
    };
    exports.unstable_getCurrentPriorityLevel = function() {
      return currentPriorityLevel;
    };
    exports.unstable_next = function(eventHandler) {
      switch (currentPriorityLevel) {
        case 1:
        case 2:
        case 3:
          var priorityLevel = 3;
          break;
        default:
          priorityLevel = currentPriorityLevel;
      }
      var previousPriorityLevel = currentPriorityLevel;
      currentPriorityLevel = priorityLevel;
      try {
        return eventHandler();
      } finally {
        currentPriorityLevel = previousPriorityLevel;
      }
    };
    exports.unstable_requestPaint = function() {
      needsPaint = true;
    };
    exports.unstable_runWithPriority = function(priorityLevel, eventHandler) {
      switch (priorityLevel) {
        case 1:
        case 2:
        case 3:
        case 4:
        case 5:
          break;
        default:
          priorityLevel = 3;
      }
      var previousPriorityLevel = currentPriorityLevel;
      currentPriorityLevel = priorityLevel;
      try {
        return eventHandler();
      } finally {
        currentPriorityLevel = previousPriorityLevel;
      }
    };
    exports.unstable_scheduleCallback = function(priorityLevel, callback, options) {
      var currentTime = exports.unstable_now();
      "object" === typeof options && null !== options ? (options = options.delay, options = "number" === typeof options && 0 < options ? currentTime + options : currentTime) : options = currentTime;
      switch (priorityLevel) {
        case 1:
          var timeout = -1;
          break;
        case 2:
          timeout = 250;
          break;
        case 5:
          timeout = 1073741823;
          break;
        case 4:
          timeout = 1e4;
          break;
        default:
          timeout = 5e3;
      }
      timeout = options + timeout;
      priorityLevel = {
        id: taskIdCounter++,
        callback,
        priorityLevel,
        startTime: options,
        expirationTime: timeout,
        sortIndex: -1
      };
      options > currentTime ? (priorityLevel.sortIndex = options, push2(timerQueue, priorityLevel), null === peek(taskQueue) && priorityLevel === peek(timerQueue) && (isHostTimeoutScheduled ? (localClearTimeout(taskTimeoutID), taskTimeoutID = -1) : isHostTimeoutScheduled = true, requestHostTimeout(handleTimeout, options - currentTime))) : (priorityLevel.sortIndex = timeout, push2(taskQueue, priorityLevel), isHostCallbackScheduled || isPerformingWork || (isHostCallbackScheduled = true, isMessageLoopRunning || (isMessageLoopRunning = true, schedulePerformWorkUntilDeadline())));
      return priorityLevel;
    };
    exports.unstable_shouldYield = shouldYieldToHost;
    exports.unstable_wrapCallback = function(callback) {
      var parentPriorityLevel = currentPriorityLevel;
      return function() {
        var previousPriorityLevel = currentPriorityLevel;
        currentPriorityLevel = parentPriorityLevel;
        try {
          return callback.apply(this, arguments);
        } finally {
          currentPriorityLevel = previousPriorityLevel;
        }
      };
    };
  })(scheduler_production);
  return scheduler_production;
}
var hasRequiredScheduler;
function requireScheduler() {
  if (hasRequiredScheduler) return scheduler.exports;
  hasRequiredScheduler = 1;
  {
    scheduler.exports = requireScheduler_production();
  }
  return scheduler.exports;
}
/**
 * @license React
 * react-dom-client.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var hasRequiredReactDomClient_production;
function requireReactDomClient_production() {
  if (hasRequiredReactDomClient_production) return reactDomClient_production;
  hasRequiredReactDomClient_production = 1;
  var Scheduler = requireScheduler(), React2 = requireReact(), ReactDOM = requireReactDom();
  function formatProdErrorMessage(code2) {
    var url = "https://react.dev/errors/" + code2;
    if (1 < arguments.length) {
      url += "?args[]=" + encodeURIComponent(arguments[1]);
      for (var i = 2; i < arguments.length; i++)
        url += "&args[]=" + encodeURIComponent(arguments[i]);
    }
    return "Minified React error #" + code2 + "; visit " + url + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
  }
  function isValidContainer(node2) {
    return !(!node2 || 1 !== node2.nodeType && 9 !== node2.nodeType && 11 !== node2.nodeType);
  }
  function getNearestMountedFiber(fiber) {
    var node2 = fiber, nearestMounted = fiber;
    if (fiber.alternate) for (; node2.return; ) node2 = node2.return;
    else {
      fiber = node2;
      do
        node2 = fiber, 0 !== (node2.flags & 4098) && (nearestMounted = node2.return), fiber = node2.return;
      while (fiber);
    }
    return 3 === node2.tag ? nearestMounted : null;
  }
  function getSuspenseInstanceFromFiber(fiber) {
    if (13 === fiber.tag) {
      var suspenseState = fiber.memoizedState;
      null === suspenseState && (fiber = fiber.alternate, null !== fiber && (suspenseState = fiber.memoizedState));
      if (null !== suspenseState) return suspenseState.dehydrated;
    }
    return null;
  }
  function getActivityInstanceFromFiber(fiber) {
    if (31 === fiber.tag) {
      var activityState = fiber.memoizedState;
      null === activityState && (fiber = fiber.alternate, null !== fiber && (activityState = fiber.memoizedState));
      if (null !== activityState) return activityState.dehydrated;
    }
    return null;
  }
  function assertIsMounted(fiber) {
    if (getNearestMountedFiber(fiber) !== fiber)
      throw Error(formatProdErrorMessage(188));
  }
  function findCurrentFiberUsingSlowPath(fiber) {
    var alternate = fiber.alternate;
    if (!alternate) {
      alternate = getNearestMountedFiber(fiber);
      if (null === alternate) throw Error(formatProdErrorMessage(188));
      return alternate !== fiber ? null : fiber;
    }
    for (var a = fiber, b = alternate; ; ) {
      var parentA = a.return;
      if (null === parentA) break;
      var parentB = parentA.alternate;
      if (null === parentB) {
        b = parentA.return;
        if (null !== b) {
          a = b;
          continue;
        }
        break;
      }
      if (parentA.child === parentB.child) {
        for (parentB = parentA.child; parentB; ) {
          if (parentB === a) return assertIsMounted(parentA), fiber;
          if (parentB === b) return assertIsMounted(parentA), alternate;
          parentB = parentB.sibling;
        }
        throw Error(formatProdErrorMessage(188));
      }
      if (a.return !== b.return) a = parentA, b = parentB;
      else {
        for (var didFindChild = false, child$0 = parentA.child; child$0; ) {
          if (child$0 === a) {
            didFindChild = true;
            a = parentA;
            b = parentB;
            break;
          }
          if (child$0 === b) {
            didFindChild = true;
            b = parentA;
            a = parentB;
            break;
          }
          child$0 = child$0.sibling;
        }
        if (!didFindChild) {
          for (child$0 = parentB.child; child$0; ) {
            if (child$0 === a) {
              didFindChild = true;
              a = parentB;
              b = parentA;
              break;
            }
            if (child$0 === b) {
              didFindChild = true;
              b = parentB;
              a = parentA;
              break;
            }
            child$0 = child$0.sibling;
          }
          if (!didFindChild) throw Error(formatProdErrorMessage(189));
        }
      }
      if (a.alternate !== b) throw Error(formatProdErrorMessage(190));
    }
    if (3 !== a.tag) throw Error(formatProdErrorMessage(188));
    return a.stateNode.current === a ? fiber : alternate;
  }
  function findCurrentHostFiberImpl(node2) {
    var tag = node2.tag;
    if (5 === tag || 26 === tag || 27 === tag || 6 === tag) return node2;
    for (node2 = node2.child; null !== node2; ) {
      tag = findCurrentHostFiberImpl(node2);
      if (null !== tag) return tag;
      node2 = node2.sibling;
    }
    return null;
  }
  var assign = Object.assign, REACT_LEGACY_ELEMENT_TYPE = Symbol.for("react.element"), REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"), REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list"), REACT_MEMO_TYPE = Symbol.for("react.memo"), REACT_LAZY_TYPE = Symbol.for("react.lazy");
  var REACT_ACTIVITY_TYPE = Symbol.for("react.activity");
  var REACT_MEMO_CACHE_SENTINEL = Symbol.for("react.memo_cache_sentinel");
  var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
  function getIteratorFn(maybeIterable) {
    if (null === maybeIterable || "object" !== typeof maybeIterable) return null;
    maybeIterable = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable["@@iterator"];
    return "function" === typeof maybeIterable ? maybeIterable : null;
  }
  var REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference");
  function getComponentNameFromType(type) {
    if (null == type) return null;
    if ("function" === typeof type)
      return type.$$typeof === REACT_CLIENT_REFERENCE ? null : type.displayName || type.name || null;
    if ("string" === typeof type) return type;
    switch (type) {
      case REACT_FRAGMENT_TYPE:
        return "Fragment";
      case REACT_PROFILER_TYPE:
        return "Profiler";
      case REACT_STRICT_MODE_TYPE:
        return "StrictMode";
      case REACT_SUSPENSE_TYPE:
        return "Suspense";
      case REACT_SUSPENSE_LIST_TYPE:
        return "SuspenseList";
      case REACT_ACTIVITY_TYPE:
        return "Activity";
    }
    if ("object" === typeof type)
      switch (type.$$typeof) {
        case REACT_PORTAL_TYPE:
          return "Portal";
        case REACT_CONTEXT_TYPE:
          return type.displayName || "Context";
        case REACT_CONSUMER_TYPE:
          return (type._context.displayName || "Context") + ".Consumer";
        case REACT_FORWARD_REF_TYPE:
          var innerType = type.render;
          type = type.displayName;
          type || (type = innerType.displayName || innerType.name || "", type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef");
          return type;
        case REACT_MEMO_TYPE:
          return innerType = type.displayName || null, null !== innerType ? innerType : getComponentNameFromType(type.type) || "Memo";
        case REACT_LAZY_TYPE:
          innerType = type._payload;
          type = type._init;
          try {
            return getComponentNameFromType(type(innerType));
          } catch (x) {
          }
      }
    return null;
  }
  var isArrayImpl = Array.isArray, ReactSharedInternals = React2.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, ReactDOMSharedInternals = ReactDOM.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, sharedNotPendingObject = {
    pending: false,
    data: null,
    method: null,
    action: null
  }, valueStack = [], index2 = -1;
  function createCursor(defaultValue) {
    return { current: defaultValue };
  }
  function pop(cursor) {
    0 > index2 || (cursor.current = valueStack[index2], valueStack[index2] = null, index2--);
  }
  function push2(cursor, value) {
    index2++;
    valueStack[index2] = cursor.current;
    cursor.current = value;
  }
  var contextStackCursor = createCursor(null), contextFiberStackCursor = createCursor(null), rootInstanceStackCursor = createCursor(null), hostTransitionProviderCursor = createCursor(null);
  function pushHostContainer(fiber, nextRootInstance) {
    push2(rootInstanceStackCursor, nextRootInstance);
    push2(contextFiberStackCursor, fiber);
    push2(contextStackCursor, null);
    switch (nextRootInstance.nodeType) {
      case 9:
      case 11:
        fiber = (fiber = nextRootInstance.documentElement) ? (fiber = fiber.namespaceURI) ? getOwnHostContext(fiber) : 0 : 0;
        break;
      default:
        if (fiber = nextRootInstance.tagName, nextRootInstance = nextRootInstance.namespaceURI)
          nextRootInstance = getOwnHostContext(nextRootInstance), fiber = getChildHostContextProd(nextRootInstance, fiber);
        else
          switch (fiber) {
            case "svg":
              fiber = 1;
              break;
            case "math":
              fiber = 2;
              break;
            default:
              fiber = 0;
          }
    }
    pop(contextStackCursor);
    push2(contextStackCursor, fiber);
  }
  function popHostContainer() {
    pop(contextStackCursor);
    pop(contextFiberStackCursor);
    pop(rootInstanceStackCursor);
  }
  function pushHostContext(fiber) {
    null !== fiber.memoizedState && push2(hostTransitionProviderCursor, fiber);
    var context = contextStackCursor.current;
    var JSCompiler_inline_result = getChildHostContextProd(context, fiber.type);
    context !== JSCompiler_inline_result && (push2(contextFiberStackCursor, fiber), push2(contextStackCursor, JSCompiler_inline_result));
  }
  function popHostContext(fiber) {
    contextFiberStackCursor.current === fiber && (pop(contextStackCursor), pop(contextFiberStackCursor));
    hostTransitionProviderCursor.current === fiber && (pop(hostTransitionProviderCursor), HostTransitionContext._currentValue = sharedNotPendingObject);
  }
  var prefix, suffix;
  function describeBuiltInComponentFrame(name2) {
    if (void 0 === prefix)
      try {
        throw Error();
      } catch (x) {
        var match = x.stack.trim().match(/\n( *(at )?)/);
        prefix = match && match[1] || "";
        suffix = -1 < x.stack.indexOf("\n    at") ? " (<anonymous>)" : -1 < x.stack.indexOf("@") ? "@unknown:0:0" : "";
      }
    return "\n" + prefix + name2 + suffix;
  }
  var reentry = false;
  function describeNativeComponentFrame(fn, construct) {
    if (!fn || reentry) return "";
    reentry = true;
    var previousPrepareStackTrace = Error.prepareStackTrace;
    Error.prepareStackTrace = void 0;
    try {
      var RunInRootFrame = {
        DetermineComponentFrameRoot: function() {
          try {
            if (construct) {
              var Fake = function() {
                throw Error();
              };
              Object.defineProperty(Fake.prototype, "props", {
                set: function() {
                  throw Error();
                }
              });
              if ("object" === typeof Reflect && Reflect.construct) {
                try {
                  Reflect.construct(Fake, []);
                } catch (x) {
                  var control = x;
                }
                Reflect.construct(fn, [], Fake);
              } else {
                try {
                  Fake.call();
                } catch (x$1) {
                  control = x$1;
                }
                fn.call(Fake.prototype);
              }
            } else {
              try {
                throw Error();
              } catch (x$2) {
                control = x$2;
              }
              (Fake = fn()) && "function" === typeof Fake.catch && Fake.catch(function() {
              });
            }
          } catch (sample) {
            if (sample && control && "string" === typeof sample.stack)
              return [sample.stack, control.stack];
          }
          return [null, null];
        }
      };
      RunInRootFrame.DetermineComponentFrameRoot.displayName = "DetermineComponentFrameRoot";
      var namePropDescriptor = Object.getOwnPropertyDescriptor(
        RunInRootFrame.DetermineComponentFrameRoot,
        "name"
      );
      namePropDescriptor && namePropDescriptor.configurable && Object.defineProperty(
        RunInRootFrame.DetermineComponentFrameRoot,
        "name",
        { value: "DetermineComponentFrameRoot" }
      );
      var _RunInRootFrame$Deter = RunInRootFrame.DetermineComponentFrameRoot(), sampleStack = _RunInRootFrame$Deter[0], controlStack = _RunInRootFrame$Deter[1];
      if (sampleStack && controlStack) {
        var sampleLines = sampleStack.split("\n"), controlLines = controlStack.split("\n");
        for (namePropDescriptor = RunInRootFrame = 0; RunInRootFrame < sampleLines.length && !sampleLines[RunInRootFrame].includes("DetermineComponentFrameRoot"); )
          RunInRootFrame++;
        for (; namePropDescriptor < controlLines.length && !controlLines[namePropDescriptor].includes(
          "DetermineComponentFrameRoot"
        ); )
          namePropDescriptor++;
        if (RunInRootFrame === sampleLines.length || namePropDescriptor === controlLines.length)
          for (RunInRootFrame = sampleLines.length - 1, namePropDescriptor = controlLines.length - 1; 1 <= RunInRootFrame && 0 <= namePropDescriptor && sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor]; )
            namePropDescriptor--;
        for (; 1 <= RunInRootFrame && 0 <= namePropDescriptor; RunInRootFrame--, namePropDescriptor--)
          if (sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor]) {
            if (1 !== RunInRootFrame || 1 !== namePropDescriptor) {
              do
                if (RunInRootFrame--, namePropDescriptor--, 0 > namePropDescriptor || sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor]) {
                  var frame = "\n" + sampleLines[RunInRootFrame].replace(" at new ", " at ");
                  fn.displayName && frame.includes("<anonymous>") && (frame = frame.replace("<anonymous>", fn.displayName));
                  return frame;
                }
              while (1 <= RunInRootFrame && 0 <= namePropDescriptor);
            }
            break;
          }
      }
    } finally {
      reentry = false, Error.prepareStackTrace = previousPrepareStackTrace;
    }
    return (previousPrepareStackTrace = fn ? fn.displayName || fn.name : "") ? describeBuiltInComponentFrame(previousPrepareStackTrace) : "";
  }
  function describeFiber(fiber, childFiber) {
    switch (fiber.tag) {
      case 26:
      case 27:
      case 5:
        return describeBuiltInComponentFrame(fiber.type);
      case 16:
        return describeBuiltInComponentFrame("Lazy");
      case 13:
        return fiber.child !== childFiber && null !== childFiber ? describeBuiltInComponentFrame("Suspense Fallback") : describeBuiltInComponentFrame("Suspense");
      case 19:
        return describeBuiltInComponentFrame("SuspenseList");
      case 0:
      case 15:
        return describeNativeComponentFrame(fiber.type, false);
      case 11:
        return describeNativeComponentFrame(fiber.type.render, false);
      case 1:
        return describeNativeComponentFrame(fiber.type, true);
      case 31:
        return describeBuiltInComponentFrame("Activity");
      default:
        return "";
    }
  }
  function getStackByFiberInDevAndProd(workInProgress2) {
    try {
      var info = "", previous2 = null;
      do
        info += describeFiber(workInProgress2, previous2), previous2 = workInProgress2, workInProgress2 = workInProgress2.return;
      while (workInProgress2);
      return info;
    } catch (x) {
      return "\nError generating stack: " + x.message + "\n" + x.stack;
    }
  }
  var hasOwnProperty2 = Object.prototype.hasOwnProperty, scheduleCallback$3 = Scheduler.unstable_scheduleCallback, cancelCallback$1 = Scheduler.unstable_cancelCallback, shouldYield = Scheduler.unstable_shouldYield, requestPaint = Scheduler.unstable_requestPaint, now = Scheduler.unstable_now, getCurrentPriorityLevel = Scheduler.unstable_getCurrentPriorityLevel, ImmediatePriority = Scheduler.unstable_ImmediatePriority, UserBlockingPriority = Scheduler.unstable_UserBlockingPriority, NormalPriority$1 = Scheduler.unstable_NormalPriority, LowPriority = Scheduler.unstable_LowPriority, IdlePriority = Scheduler.unstable_IdlePriority, log$1 = Scheduler.log, unstable_setDisableYieldValue = Scheduler.unstable_setDisableYieldValue, rendererID = null, injectedHook = null;
  function setIsStrictModeForDevtools(newIsStrictMode) {
    "function" === typeof log$1 && unstable_setDisableYieldValue(newIsStrictMode);
    if (injectedHook && "function" === typeof injectedHook.setStrictMode)
      try {
        injectedHook.setStrictMode(rendererID, newIsStrictMode);
      } catch (err) {
      }
  }
  var clz32 = Math.clz32 ? Math.clz32 : clz32Fallback, log = Math.log, LN2 = Math.LN2;
  function clz32Fallback(x) {
    x >>>= 0;
    return 0 === x ? 32 : 31 - (log(x) / LN2 | 0) | 0;
  }
  var nextTransitionUpdateLane = 256, nextTransitionDeferredLane = 262144, nextRetryLane = 4194304;
  function getHighestPriorityLanes(lanes) {
    var pendingSyncLanes = lanes & 42;
    if (0 !== pendingSyncLanes) return pendingSyncLanes;
    switch (lanes & -lanes) {
      case 1:
        return 1;
      case 2:
        return 2;
      case 4:
        return 4;
      case 8:
        return 8;
      case 16:
        return 16;
      case 32:
        return 32;
      case 64:
        return 64;
      case 128:
        return 128;
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
        return lanes & 261888;
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
        return lanes & 3932160;
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        return lanes & 62914560;
      case 67108864:
        return 67108864;
      case 134217728:
        return 134217728;
      case 268435456:
        return 268435456;
      case 536870912:
        return 536870912;
      case 1073741824:
        return 0;
      default:
        return lanes;
    }
  }
  function getNextLanes(root3, wipLanes, rootHasPendingCommit) {
    var pendingLanes = root3.pendingLanes;
    if (0 === pendingLanes) return 0;
    var nextLanes = 0, suspendedLanes = root3.suspendedLanes, pingedLanes = root3.pingedLanes;
    root3 = root3.warmLanes;
    var nonIdlePendingLanes = pendingLanes & 134217727;
    0 !== nonIdlePendingLanes ? (pendingLanes = nonIdlePendingLanes & ~suspendedLanes, 0 !== pendingLanes ? nextLanes = getHighestPriorityLanes(pendingLanes) : (pingedLanes &= nonIdlePendingLanes, 0 !== pingedLanes ? nextLanes = getHighestPriorityLanes(pingedLanes) : rootHasPendingCommit || (rootHasPendingCommit = nonIdlePendingLanes & ~root3, 0 !== rootHasPendingCommit && (nextLanes = getHighestPriorityLanes(rootHasPendingCommit))))) : (nonIdlePendingLanes = pendingLanes & ~suspendedLanes, 0 !== nonIdlePendingLanes ? nextLanes = getHighestPriorityLanes(nonIdlePendingLanes) : 0 !== pingedLanes ? nextLanes = getHighestPriorityLanes(pingedLanes) : rootHasPendingCommit || (rootHasPendingCommit = pendingLanes & ~root3, 0 !== rootHasPendingCommit && (nextLanes = getHighestPriorityLanes(rootHasPendingCommit))));
    return 0 === nextLanes ? 0 : 0 !== wipLanes && wipLanes !== nextLanes && 0 === (wipLanes & suspendedLanes) && (suspendedLanes = nextLanes & -nextLanes, rootHasPendingCommit = wipLanes & -wipLanes, suspendedLanes >= rootHasPendingCommit || 32 === suspendedLanes && 0 !== (rootHasPendingCommit & 4194048)) ? wipLanes : nextLanes;
  }
  function checkIfRootIsPrerendering(root3, renderLanes2) {
    return 0 === (root3.pendingLanes & ~(root3.suspendedLanes & ~root3.pingedLanes) & renderLanes2);
  }
  function computeExpirationTime(lane, currentTime) {
    switch (lane) {
      case 1:
      case 2:
      case 4:
      case 8:
      case 64:
        return currentTime + 250;
      case 16:
      case 32:
      case 128:
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
        return currentTime + 5e3;
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        return -1;
      case 67108864:
      case 134217728:
      case 268435456:
      case 536870912:
      case 1073741824:
        return -1;
      default:
        return -1;
    }
  }
  function claimNextRetryLane() {
    var lane = nextRetryLane;
    nextRetryLane <<= 1;
    0 === (nextRetryLane & 62914560) && (nextRetryLane = 4194304);
    return lane;
  }
  function createLaneMap(initial) {
    for (var laneMap = [], i = 0; 31 > i; i++) laneMap.push(initial);
    return laneMap;
  }
  function markRootUpdated$1(root3, updateLane) {
    root3.pendingLanes |= updateLane;
    268435456 !== updateLane && (root3.suspendedLanes = 0, root3.pingedLanes = 0, root3.warmLanes = 0);
  }
  function markRootFinished(root3, finishedLanes, remainingLanes, spawnedLane, updatedLanes, suspendedRetryLanes) {
    var previouslyPendingLanes = root3.pendingLanes;
    root3.pendingLanes = remainingLanes;
    root3.suspendedLanes = 0;
    root3.pingedLanes = 0;
    root3.warmLanes = 0;
    root3.expiredLanes &= remainingLanes;
    root3.entangledLanes &= remainingLanes;
    root3.errorRecoveryDisabledLanes &= remainingLanes;
    root3.shellSuspendCounter = 0;
    var entanglements = root3.entanglements, expirationTimes = root3.expirationTimes, hiddenUpdates = root3.hiddenUpdates;
    for (remainingLanes = previouslyPendingLanes & ~remainingLanes; 0 < remainingLanes; ) {
      var index$7 = 31 - clz32(remainingLanes), lane = 1 << index$7;
      entanglements[index$7] = 0;
      expirationTimes[index$7] = -1;
      var hiddenUpdatesForLane = hiddenUpdates[index$7];
      if (null !== hiddenUpdatesForLane)
        for (hiddenUpdates[index$7] = null, index$7 = 0; index$7 < hiddenUpdatesForLane.length; index$7++) {
          var update = hiddenUpdatesForLane[index$7];
          null !== update && (update.lane &= -536870913);
        }
      remainingLanes &= ~lane;
    }
    0 !== spawnedLane && markSpawnedDeferredLane(root3, spawnedLane, 0);
    0 !== suspendedRetryLanes && 0 === updatedLanes && 0 !== root3.tag && (root3.suspendedLanes |= suspendedRetryLanes & ~(previouslyPendingLanes & ~finishedLanes));
  }
  function markSpawnedDeferredLane(root3, spawnedLane, entangledLanes) {
    root3.pendingLanes |= spawnedLane;
    root3.suspendedLanes &= ~spawnedLane;
    var spawnedLaneIndex = 31 - clz32(spawnedLane);
    root3.entangledLanes |= spawnedLane;
    root3.entanglements[spawnedLaneIndex] = root3.entanglements[spawnedLaneIndex] | 1073741824 | entangledLanes & 261930;
  }
  function markRootEntangled(root3, entangledLanes) {
    var rootEntangledLanes = root3.entangledLanes |= entangledLanes;
    for (root3 = root3.entanglements; rootEntangledLanes; ) {
      var index$8 = 31 - clz32(rootEntangledLanes), lane = 1 << index$8;
      lane & entangledLanes | root3[index$8] & entangledLanes && (root3[index$8] |= entangledLanes);
      rootEntangledLanes &= ~lane;
    }
  }
  function getBumpedLaneForHydration(root3, renderLanes2) {
    var renderLane = renderLanes2 & -renderLanes2;
    renderLane = 0 !== (renderLane & 42) ? 1 : getBumpedLaneForHydrationByLane(renderLane);
    return 0 !== (renderLane & (root3.suspendedLanes | renderLanes2)) ? 0 : renderLane;
  }
  function getBumpedLaneForHydrationByLane(lane) {
    switch (lane) {
      case 2:
        lane = 1;
        break;
      case 8:
        lane = 4;
        break;
      case 32:
        lane = 16;
        break;
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        lane = 128;
        break;
      case 268435456:
        lane = 134217728;
        break;
      default:
        lane = 0;
    }
    return lane;
  }
  function lanesToEventPriority(lanes) {
    lanes &= -lanes;
    return 2 < lanes ? 8 < lanes ? 0 !== (lanes & 134217727) ? 32 : 268435456 : 8 : 2;
  }
  function resolveUpdatePriority() {
    var updatePriority = ReactDOMSharedInternals.p;
    if (0 !== updatePriority) return updatePriority;
    updatePriority = window.event;
    return void 0 === updatePriority ? 32 : getEventPriority(updatePriority.type);
  }
  function runWithPriority(priority, fn) {
    var previousPriority = ReactDOMSharedInternals.p;
    try {
      return ReactDOMSharedInternals.p = priority, fn();
    } finally {
      ReactDOMSharedInternals.p = previousPriority;
    }
  }
  var randomKey = Math.random().toString(36).slice(2), internalInstanceKey = "__reactFiber$" + randomKey, internalPropsKey = "__reactProps$" + randomKey, internalContainerInstanceKey = "__reactContainer$" + randomKey, internalEventHandlersKey = "__reactEvents$" + randomKey, internalEventHandlerListenersKey = "__reactListeners$" + randomKey, internalEventHandlesSetKey = "__reactHandles$" + randomKey, internalRootNodeResourcesKey = "__reactResources$" + randomKey, internalHoistableMarker = "__reactMarker$" + randomKey;
  function detachDeletedInstance(node2) {
    delete node2[internalInstanceKey];
    delete node2[internalPropsKey];
    delete node2[internalEventHandlersKey];
    delete node2[internalEventHandlerListenersKey];
    delete node2[internalEventHandlesSetKey];
  }
  function getClosestInstanceFromNode(targetNode) {
    var targetInst = targetNode[internalInstanceKey];
    if (targetInst) return targetInst;
    for (var parentNode = targetNode.parentNode; parentNode; ) {
      if (targetInst = parentNode[internalContainerInstanceKey] || parentNode[internalInstanceKey]) {
        parentNode = targetInst.alternate;
        if (null !== targetInst.child || null !== parentNode && null !== parentNode.child)
          for (targetNode = getParentHydrationBoundary(targetNode); null !== targetNode; ) {
            if (parentNode = targetNode[internalInstanceKey]) return parentNode;
            targetNode = getParentHydrationBoundary(targetNode);
          }
        return targetInst;
      }
      targetNode = parentNode;
      parentNode = targetNode.parentNode;
    }
    return null;
  }
  function getInstanceFromNode(node2) {
    if (node2 = node2[internalInstanceKey] || node2[internalContainerInstanceKey]) {
      var tag = node2.tag;
      if (5 === tag || 6 === tag || 13 === tag || 31 === tag || 26 === tag || 27 === tag || 3 === tag)
        return node2;
    }
    return null;
  }
  function getNodeFromInstance(inst) {
    var tag = inst.tag;
    if (5 === tag || 26 === tag || 27 === tag || 6 === tag) return inst.stateNode;
    throw Error(formatProdErrorMessage(33));
  }
  function getResourcesFromRoot(root3) {
    var resources = root3[internalRootNodeResourcesKey];
    resources || (resources = root3[internalRootNodeResourcesKey] = { hoistableStyles: /* @__PURE__ */ new Map(), hoistableScripts: /* @__PURE__ */ new Map() });
    return resources;
  }
  function markNodeAsHoistable(node2) {
    node2[internalHoistableMarker] = true;
  }
  var allNativeEvents = /* @__PURE__ */ new Set(), registrationNameDependencies = {};
  function registerTwoPhaseEvent(registrationName, dependencies) {
    registerDirectEvent(registrationName, dependencies);
    registerDirectEvent(registrationName + "Capture", dependencies);
  }
  function registerDirectEvent(registrationName, dependencies) {
    registrationNameDependencies[registrationName] = dependencies;
    for (registrationName = 0; registrationName < dependencies.length; registrationName++)
      allNativeEvents.add(dependencies[registrationName]);
  }
  var VALID_ATTRIBUTE_NAME_REGEX = RegExp(
    "^[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"
  ), illegalAttributeNameCache = {}, validatedAttributeNameCache = {};
  function isAttributeNameSafe(attributeName) {
    if (hasOwnProperty2.call(validatedAttributeNameCache, attributeName))
      return true;
    if (hasOwnProperty2.call(illegalAttributeNameCache, attributeName)) return false;
    if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName))
      return validatedAttributeNameCache[attributeName] = true;
    illegalAttributeNameCache[attributeName] = true;
    return false;
  }
  function setValueForAttribute(node2, name2, value) {
    if (isAttributeNameSafe(name2))
      if (null === value) node2.removeAttribute(name2);
      else {
        switch (typeof value) {
          case "undefined":
          case "function":
          case "symbol":
            node2.removeAttribute(name2);
            return;
          case "boolean":
            var prefix$10 = name2.toLowerCase().slice(0, 5);
            if ("data-" !== prefix$10 && "aria-" !== prefix$10) {
              node2.removeAttribute(name2);
              return;
            }
        }
        node2.setAttribute(name2, "" + value);
      }
  }
  function setValueForKnownAttribute(node2, name2, value) {
    if (null === value) node2.removeAttribute(name2);
    else {
      switch (typeof value) {
        case "undefined":
        case "function":
        case "symbol":
        case "boolean":
          node2.removeAttribute(name2);
          return;
      }
      node2.setAttribute(name2, "" + value);
    }
  }
  function setValueForNamespacedAttribute(node2, namespace, name2, value) {
    if (null === value) node2.removeAttribute(name2);
    else {
      switch (typeof value) {
        case "undefined":
        case "function":
        case "symbol":
        case "boolean":
          node2.removeAttribute(name2);
          return;
      }
      node2.setAttributeNS(namespace, name2, "" + value);
    }
  }
  function getToStringValue(value) {
    switch (typeof value) {
      case "bigint":
      case "boolean":
      case "number":
      case "string":
      case "undefined":
        return value;
      case "object":
        return value;
      default:
        return "";
    }
  }
  function isCheckable(elem) {
    var type = elem.type;
    return (elem = elem.nodeName) && "input" === elem.toLowerCase() && ("checkbox" === type || "radio" === type);
  }
  function trackValueOnNode(node2, valueField, currentValue) {
    var descriptor = Object.getOwnPropertyDescriptor(
      node2.constructor.prototype,
      valueField
    );
    if (!node2.hasOwnProperty(valueField) && "undefined" !== typeof descriptor && "function" === typeof descriptor.get && "function" === typeof descriptor.set) {
      var get = descriptor.get, set = descriptor.set;
      Object.defineProperty(node2, valueField, {
        configurable: true,
        get: function() {
          return get.call(this);
        },
        set: function(value) {
          currentValue = "" + value;
          set.call(this, value);
        }
      });
      Object.defineProperty(node2, valueField, {
        enumerable: descriptor.enumerable
      });
      return {
        getValue: function() {
          return currentValue;
        },
        setValue: function(value) {
          currentValue = "" + value;
        },
        stopTracking: function() {
          node2._valueTracker = null;
          delete node2[valueField];
        }
      };
    }
  }
  function track(node2) {
    if (!node2._valueTracker) {
      var valueField = isCheckable(node2) ? "checked" : "value";
      node2._valueTracker = trackValueOnNode(
        node2,
        valueField,
        "" + node2[valueField]
      );
    }
  }
  function updateValueIfChanged(node2) {
    if (!node2) return false;
    var tracker = node2._valueTracker;
    if (!tracker) return true;
    var lastValue = tracker.getValue();
    var value = "";
    node2 && (value = isCheckable(node2) ? node2.checked ? "true" : "false" : node2.value);
    node2 = value;
    return node2 !== lastValue ? (tracker.setValue(node2), true) : false;
  }
  function getActiveElement(doc) {
    doc = doc || ("undefined" !== typeof document ? document : void 0);
    if ("undefined" === typeof doc) return null;
    try {
      return doc.activeElement || doc.body;
    } catch (e) {
      return doc.body;
    }
  }
  var escapeSelectorAttributeValueInsideDoubleQuotesRegex = /[\n"\\]/g;
  function escapeSelectorAttributeValueInsideDoubleQuotes(value) {
    return value.replace(
      escapeSelectorAttributeValueInsideDoubleQuotesRegex,
      function(ch) {
        return "\\" + ch.charCodeAt(0).toString(16) + " ";
      }
    );
  }
  function updateInput(element2, value, defaultValue, lastDefaultValue, checked, defaultChecked, type, name2) {
    element2.name = "";
    null != type && "function" !== typeof type && "symbol" !== typeof type && "boolean" !== typeof type ? element2.type = type : element2.removeAttribute("type");
    if (null != value)
      if ("number" === type) {
        if (0 === value && "" === element2.value || element2.value != value)
          element2.value = "" + getToStringValue(value);
      } else
        element2.value !== "" + getToStringValue(value) && (element2.value = "" + getToStringValue(value));
    else
      "submit" !== type && "reset" !== type || element2.removeAttribute("value");
    null != value ? setDefaultValue(element2, type, getToStringValue(value)) : null != defaultValue ? setDefaultValue(element2, type, getToStringValue(defaultValue)) : null != lastDefaultValue && element2.removeAttribute("value");
    null == checked && null != defaultChecked && (element2.defaultChecked = !!defaultChecked);
    null != checked && (element2.checked = checked && "function" !== typeof checked && "symbol" !== typeof checked);
    null != name2 && "function" !== typeof name2 && "symbol" !== typeof name2 && "boolean" !== typeof name2 ? element2.name = "" + getToStringValue(name2) : element2.removeAttribute("name");
  }
  function initInput(element2, value, defaultValue, checked, defaultChecked, type, name2, isHydrating2) {
    null != type && "function" !== typeof type && "symbol" !== typeof type && "boolean" !== typeof type && (element2.type = type);
    if (null != value || null != defaultValue) {
      if (!("submit" !== type && "reset" !== type || void 0 !== value && null !== value)) {
        track(element2);
        return;
      }
      defaultValue = null != defaultValue ? "" + getToStringValue(defaultValue) : "";
      value = null != value ? "" + getToStringValue(value) : defaultValue;
      isHydrating2 || value === element2.value || (element2.value = value);
      element2.defaultValue = value;
    }
    checked = null != checked ? checked : defaultChecked;
    checked = "function" !== typeof checked && "symbol" !== typeof checked && !!checked;
    element2.checked = isHydrating2 ? element2.checked : !!checked;
    element2.defaultChecked = !!checked;
    null != name2 && "function" !== typeof name2 && "symbol" !== typeof name2 && "boolean" !== typeof name2 && (element2.name = name2);
    track(element2);
  }
  function setDefaultValue(node2, type, value) {
    "number" === type && getActiveElement(node2.ownerDocument) === node2 || node2.defaultValue === "" + value || (node2.defaultValue = "" + value);
  }
  function updateOptions(node2, multiple, propValue, setDefaultSelected) {
    node2 = node2.options;
    if (multiple) {
      multiple = {};
      for (var i = 0; i < propValue.length; i++)
        multiple["$" + propValue[i]] = true;
      for (propValue = 0; propValue < node2.length; propValue++)
        i = multiple.hasOwnProperty("$" + node2[propValue].value), node2[propValue].selected !== i && (node2[propValue].selected = i), i && setDefaultSelected && (node2[propValue].defaultSelected = true);
    } else {
      propValue = "" + getToStringValue(propValue);
      multiple = null;
      for (i = 0; i < node2.length; i++) {
        if (node2[i].value === propValue) {
          node2[i].selected = true;
          setDefaultSelected && (node2[i].defaultSelected = true);
          return;
        }
        null !== multiple || node2[i].disabled || (multiple = node2[i]);
      }
      null !== multiple && (multiple.selected = true);
    }
  }
  function updateTextarea(element2, value, defaultValue) {
    if (null != value && (value = "" + getToStringValue(value), value !== element2.value && (element2.value = value), null == defaultValue)) {
      element2.defaultValue !== value && (element2.defaultValue = value);
      return;
    }
    element2.defaultValue = null != defaultValue ? "" + getToStringValue(defaultValue) : "";
  }
  function initTextarea(element2, value, defaultValue, children) {
    if (null == value) {
      if (null != children) {
        if (null != defaultValue) throw Error(formatProdErrorMessage(92));
        if (isArrayImpl(children)) {
          if (1 < children.length) throw Error(formatProdErrorMessage(93));
          children = children[0];
        }
        defaultValue = children;
      }
      null == defaultValue && (defaultValue = "");
      value = defaultValue;
    }
    defaultValue = getToStringValue(value);
    element2.defaultValue = defaultValue;
    children = element2.textContent;
    children === defaultValue && "" !== children && null !== children && (element2.value = children);
    track(element2);
  }
  function setTextContent(node2, text2) {
    if (text2) {
      var firstChild = node2.firstChild;
      if (firstChild && firstChild === node2.lastChild && 3 === firstChild.nodeType) {
        firstChild.nodeValue = text2;
        return;
      }
    }
    node2.textContent = text2;
  }
  var unitlessNumbers = new Set(
    "animationIterationCount aspectRatio borderImageOutset borderImageSlice borderImageWidth boxFlex boxFlexGroup boxOrdinalGroup columnCount columns flex flexGrow flexPositive flexShrink flexNegative flexOrder gridArea gridRow gridRowEnd gridRowSpan gridRowStart gridColumn gridColumnEnd gridColumnSpan gridColumnStart fontWeight lineClamp lineHeight opacity order orphans scale tabSize widows zIndex zoom fillOpacity floodOpacity stopOpacity strokeDasharray strokeDashoffset strokeMiterlimit strokeOpacity strokeWidth MozAnimationIterationCount MozBoxFlex MozBoxFlexGroup MozLineClamp msAnimationIterationCount msFlex msZoom msFlexGrow msFlexNegative msFlexOrder msFlexPositive msFlexShrink msGridColumn msGridColumnSpan msGridRow msGridRowSpan WebkitAnimationIterationCount WebkitBoxFlex WebKitBoxFlexGroup WebkitBoxOrdinalGroup WebkitColumnCount WebkitColumns WebkitFlex WebkitFlexGrow WebkitFlexPositive WebkitFlexShrink WebkitLineClamp".split(
      " "
    )
  );
  function setValueForStyle(style2, styleName, value) {
    var isCustomProperty = 0 === styleName.indexOf("--");
    null == value || "boolean" === typeof value || "" === value ? isCustomProperty ? style2.setProperty(styleName, "") : "float" === styleName ? style2.cssFloat = "" : style2[styleName] = "" : isCustomProperty ? style2.setProperty(styleName, value) : "number" !== typeof value || 0 === value || unitlessNumbers.has(styleName) ? "float" === styleName ? style2.cssFloat = value : style2[styleName] = ("" + value).trim() : style2[styleName] = value + "px";
  }
  function setValueForStyles(node2, styles, prevStyles) {
    if (null != styles && "object" !== typeof styles)
      throw Error(formatProdErrorMessage(62));
    node2 = node2.style;
    if (null != prevStyles) {
      for (var styleName in prevStyles)
        !prevStyles.hasOwnProperty(styleName) || null != styles && styles.hasOwnProperty(styleName) || (0 === styleName.indexOf("--") ? node2.setProperty(styleName, "") : "float" === styleName ? node2.cssFloat = "" : node2[styleName] = "");
      for (var styleName$16 in styles)
        styleName = styles[styleName$16], styles.hasOwnProperty(styleName$16) && prevStyles[styleName$16] !== styleName && setValueForStyle(node2, styleName$16, styleName);
    } else
      for (var styleName$17 in styles)
        styles.hasOwnProperty(styleName$17) && setValueForStyle(node2, styleName$17, styles[styleName$17]);
  }
  function isCustomElement(tagName) {
    if (-1 === tagName.indexOf("-")) return false;
    switch (tagName) {
      case "annotation-xml":
      case "color-profile":
      case "font-face":
      case "font-face-src":
      case "font-face-uri":
      case "font-face-format":
      case "font-face-name":
      case "missing-glyph":
        return false;
      default:
        return true;
    }
  }
  var aliases = /* @__PURE__ */ new Map([
    ["acceptCharset", "accept-charset"],
    ["htmlFor", "for"],
    ["httpEquiv", "http-equiv"],
    ["crossOrigin", "crossorigin"],
    ["accentHeight", "accent-height"],
    ["alignmentBaseline", "alignment-baseline"],
    ["arabicForm", "arabic-form"],
    ["baselineShift", "baseline-shift"],
    ["capHeight", "cap-height"],
    ["clipPath", "clip-path"],
    ["clipRule", "clip-rule"],
    ["colorInterpolation", "color-interpolation"],
    ["colorInterpolationFilters", "color-interpolation-filters"],
    ["colorProfile", "color-profile"],
    ["colorRendering", "color-rendering"],
    ["dominantBaseline", "dominant-baseline"],
    ["enableBackground", "enable-background"],
    ["fillOpacity", "fill-opacity"],
    ["fillRule", "fill-rule"],
    ["floodColor", "flood-color"],
    ["floodOpacity", "flood-opacity"],
    ["fontFamily", "font-family"],
    ["fontSize", "font-size"],
    ["fontSizeAdjust", "font-size-adjust"],
    ["fontStretch", "font-stretch"],
    ["fontStyle", "font-style"],
    ["fontVariant", "font-variant"],
    ["fontWeight", "font-weight"],
    ["glyphName", "glyph-name"],
    ["glyphOrientationHorizontal", "glyph-orientation-horizontal"],
    ["glyphOrientationVertical", "glyph-orientation-vertical"],
    ["horizAdvX", "horiz-adv-x"],
    ["horizOriginX", "horiz-origin-x"],
    ["imageRendering", "image-rendering"],
    ["letterSpacing", "letter-spacing"],
    ["lightingColor", "lighting-color"],
    ["markerEnd", "marker-end"],
    ["markerMid", "marker-mid"],
    ["markerStart", "marker-start"],
    ["overlinePosition", "overline-position"],
    ["overlineThickness", "overline-thickness"],
    ["paintOrder", "paint-order"],
    ["panose-1", "panose-1"],
    ["pointerEvents", "pointer-events"],
    ["renderingIntent", "rendering-intent"],
    ["shapeRendering", "shape-rendering"],
    ["stopColor", "stop-color"],
    ["stopOpacity", "stop-opacity"],
    ["strikethroughPosition", "strikethrough-position"],
    ["strikethroughThickness", "strikethrough-thickness"],
    ["strokeDasharray", "stroke-dasharray"],
    ["strokeDashoffset", "stroke-dashoffset"],
    ["strokeLinecap", "stroke-linecap"],
    ["strokeLinejoin", "stroke-linejoin"],
    ["strokeMiterlimit", "stroke-miterlimit"],
    ["strokeOpacity", "stroke-opacity"],
    ["strokeWidth", "stroke-width"],
    ["textAnchor", "text-anchor"],
    ["textDecoration", "text-decoration"],
    ["textRendering", "text-rendering"],
    ["transformOrigin", "transform-origin"],
    ["underlinePosition", "underline-position"],
    ["underlineThickness", "underline-thickness"],
    ["unicodeBidi", "unicode-bidi"],
    ["unicodeRange", "unicode-range"],
    ["unitsPerEm", "units-per-em"],
    ["vAlphabetic", "v-alphabetic"],
    ["vHanging", "v-hanging"],
    ["vIdeographic", "v-ideographic"],
    ["vMathematical", "v-mathematical"],
    ["vectorEffect", "vector-effect"],
    ["vertAdvY", "vert-adv-y"],
    ["vertOriginX", "vert-origin-x"],
    ["vertOriginY", "vert-origin-y"],
    ["wordSpacing", "word-spacing"],
    ["writingMode", "writing-mode"],
    ["xmlnsXlink", "xmlns:xlink"],
    ["xHeight", "x-height"]
  ]), isJavaScriptProtocol = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i;
  function sanitizeURL(url) {
    return isJavaScriptProtocol.test("" + url) ? "javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')" : url;
  }
  function noop$1() {
  }
  var currentReplayingEvent = null;
  function getEventTarget(nativeEvent) {
    nativeEvent = nativeEvent.target || nativeEvent.srcElement || window;
    nativeEvent.correspondingUseElement && (nativeEvent = nativeEvent.correspondingUseElement);
    return 3 === nativeEvent.nodeType ? nativeEvent.parentNode : nativeEvent;
  }
  var restoreTarget = null, restoreQueue = null;
  function restoreStateOfTarget(target) {
    var internalInstance = getInstanceFromNode(target);
    if (internalInstance && (target = internalInstance.stateNode)) {
      var props = target[internalPropsKey] || null;
      a: switch (target = internalInstance.stateNode, internalInstance.type) {
        case "input":
          updateInput(
            target,
            props.value,
            props.defaultValue,
            props.defaultValue,
            props.checked,
            props.defaultChecked,
            props.type,
            props.name
          );
          internalInstance = props.name;
          if ("radio" === props.type && null != internalInstance) {
            for (props = target; props.parentNode; ) props = props.parentNode;
            props = props.querySelectorAll(
              'input[name="' + escapeSelectorAttributeValueInsideDoubleQuotes(
                "" + internalInstance
              ) + '"][type="radio"]'
            );
            for (internalInstance = 0; internalInstance < props.length; internalInstance++) {
              var otherNode = props[internalInstance];
              if (otherNode !== target && otherNode.form === target.form) {
                var otherProps = otherNode[internalPropsKey] || null;
                if (!otherProps) throw Error(formatProdErrorMessage(90));
                updateInput(
                  otherNode,
                  otherProps.value,
                  otherProps.defaultValue,
                  otherProps.defaultValue,
                  otherProps.checked,
                  otherProps.defaultChecked,
                  otherProps.type,
                  otherProps.name
                );
              }
            }
            for (internalInstance = 0; internalInstance < props.length; internalInstance++)
              otherNode = props[internalInstance], otherNode.form === target.form && updateValueIfChanged(otherNode);
          }
          break a;
        case "textarea":
          updateTextarea(target, props.value, props.defaultValue);
          break a;
        case "select":
          internalInstance = props.value, null != internalInstance && updateOptions(target, !!props.multiple, internalInstance, false);
      }
    }
  }
  var isInsideEventHandler = false;
  function batchedUpdates$1(fn, a, b) {
    if (isInsideEventHandler) return fn(a, b);
    isInsideEventHandler = true;
    try {
      var JSCompiler_inline_result = fn(a);
      return JSCompiler_inline_result;
    } finally {
      if (isInsideEventHandler = false, null !== restoreTarget || null !== restoreQueue) {
        if (flushSyncWork$1(), restoreTarget && (a = restoreTarget, fn = restoreQueue, restoreQueue = restoreTarget = null, restoreStateOfTarget(a), fn))
          for (a = 0; a < fn.length; a++) restoreStateOfTarget(fn[a]);
      }
    }
  }
  function getListener(inst, registrationName) {
    var stateNode = inst.stateNode;
    if (null === stateNode) return null;
    var props = stateNode[internalPropsKey] || null;
    if (null === props) return null;
    stateNode = props[registrationName];
    a: switch (registrationName) {
      case "onClick":
      case "onClickCapture":
      case "onDoubleClick":
      case "onDoubleClickCapture":
      case "onMouseDown":
      case "onMouseDownCapture":
      case "onMouseMove":
      case "onMouseMoveCapture":
      case "onMouseUp":
      case "onMouseUpCapture":
      case "onMouseEnter":
        (props = !props.disabled) || (inst = inst.type, props = !("button" === inst || "input" === inst || "select" === inst || "textarea" === inst));
        inst = !props;
        break a;
      default:
        inst = false;
    }
    if (inst) return null;
    if (stateNode && "function" !== typeof stateNode)
      throw Error(
        formatProdErrorMessage(231, registrationName, typeof stateNode)
      );
    return stateNode;
  }
  var canUseDOM = !("undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement), passiveBrowserEventsSupported = false;
  if (canUseDOM)
    try {
      var options = {};
      Object.defineProperty(options, "passive", {
        get: function() {
          passiveBrowserEventsSupported = true;
        }
      });
      window.addEventListener("test", options, options);
      window.removeEventListener("test", options, options);
    } catch (e) {
      passiveBrowserEventsSupported = false;
    }
  var root2 = null, startText = null, fallbackText = null;
  function getData() {
    if (fallbackText) return fallbackText;
    var start, startValue = startText, startLength = startValue.length, end, endValue = "value" in root2 ? root2.value : root2.textContent, endLength = endValue.length;
    for (start = 0; start < startLength && startValue[start] === endValue[start]; start++) ;
    var minEnd = startLength - start;
    for (end = 1; end <= minEnd && startValue[startLength - end] === endValue[endLength - end]; end++) ;
    return fallbackText = endValue.slice(start, 1 < end ? 1 - end : void 0);
  }
  function getEventCharCode(nativeEvent) {
    var keyCode = nativeEvent.keyCode;
    "charCode" in nativeEvent ? (nativeEvent = nativeEvent.charCode, 0 === nativeEvent && 13 === keyCode && (nativeEvent = 13)) : nativeEvent = keyCode;
    10 === nativeEvent && (nativeEvent = 13);
    return 32 <= nativeEvent || 13 === nativeEvent ? nativeEvent : 0;
  }
  function functionThatReturnsTrue() {
    return true;
  }
  function functionThatReturnsFalse() {
    return false;
  }
  function createSyntheticEvent(Interface) {
    function SyntheticBaseEvent(reactName, reactEventType, targetInst, nativeEvent, nativeEventTarget) {
      this._reactName = reactName;
      this._targetInst = targetInst;
      this.type = reactEventType;
      this.nativeEvent = nativeEvent;
      this.target = nativeEventTarget;
      this.currentTarget = null;
      for (var propName in Interface)
        Interface.hasOwnProperty(propName) && (reactName = Interface[propName], this[propName] = reactName ? reactName(nativeEvent) : nativeEvent[propName]);
      this.isDefaultPrevented = (null != nativeEvent.defaultPrevented ? nativeEvent.defaultPrevented : false === nativeEvent.returnValue) ? functionThatReturnsTrue : functionThatReturnsFalse;
      this.isPropagationStopped = functionThatReturnsFalse;
      return this;
    }
    assign(SyntheticBaseEvent.prototype, {
      preventDefault: function() {
        this.defaultPrevented = true;
        var event = this.nativeEvent;
        event && (event.preventDefault ? event.preventDefault() : "unknown" !== typeof event.returnValue && (event.returnValue = false), this.isDefaultPrevented = functionThatReturnsTrue);
      },
      stopPropagation: function() {
        var event = this.nativeEvent;
        event && (event.stopPropagation ? event.stopPropagation() : "unknown" !== typeof event.cancelBubble && (event.cancelBubble = true), this.isPropagationStopped = functionThatReturnsTrue);
      },
      persist: function() {
      },
      isPersistent: functionThatReturnsTrue
    });
    return SyntheticBaseEvent;
  }
  var EventInterface = {
    eventPhase: 0,
    bubbles: 0,
    cancelable: 0,
    timeStamp: function(event) {
      return event.timeStamp || Date.now();
    },
    defaultPrevented: 0,
    isTrusted: 0
  }, SyntheticEvent = createSyntheticEvent(EventInterface), UIEventInterface = assign({}, EventInterface, { view: 0, detail: 0 }), SyntheticUIEvent = createSyntheticEvent(UIEventInterface), lastMovementX, lastMovementY, lastMouseEvent, MouseEventInterface = assign({}, UIEventInterface, {
    screenX: 0,
    screenY: 0,
    clientX: 0,
    clientY: 0,
    pageX: 0,
    pageY: 0,
    ctrlKey: 0,
    shiftKey: 0,
    altKey: 0,
    metaKey: 0,
    getModifierState: getEventModifierState,
    button: 0,
    buttons: 0,
    relatedTarget: function(event) {
      return void 0 === event.relatedTarget ? event.fromElement === event.srcElement ? event.toElement : event.fromElement : event.relatedTarget;
    },
    movementX: function(event) {
      if ("movementX" in event) return event.movementX;
      event !== lastMouseEvent && (lastMouseEvent && "mousemove" === event.type ? (lastMovementX = event.screenX - lastMouseEvent.screenX, lastMovementY = event.screenY - lastMouseEvent.screenY) : lastMovementY = lastMovementX = 0, lastMouseEvent = event);
      return lastMovementX;
    },
    movementY: function(event) {
      return "movementY" in event ? event.movementY : lastMovementY;
    }
  }), SyntheticMouseEvent = createSyntheticEvent(MouseEventInterface), DragEventInterface = assign({}, MouseEventInterface, { dataTransfer: 0 }), SyntheticDragEvent = createSyntheticEvent(DragEventInterface), FocusEventInterface = assign({}, UIEventInterface, { relatedTarget: 0 }), SyntheticFocusEvent = createSyntheticEvent(FocusEventInterface), AnimationEventInterface = assign({}, EventInterface, {
    animationName: 0,
    elapsedTime: 0,
    pseudoElement: 0
  }), SyntheticAnimationEvent = createSyntheticEvent(AnimationEventInterface), ClipboardEventInterface = assign({}, EventInterface, {
    clipboardData: function(event) {
      return "clipboardData" in event ? event.clipboardData : window.clipboardData;
    }
  }), SyntheticClipboardEvent = createSyntheticEvent(ClipboardEventInterface), CompositionEventInterface = assign({}, EventInterface, { data: 0 }), SyntheticCompositionEvent = createSyntheticEvent(CompositionEventInterface), normalizeKey = {
    Esc: "Escape",
    Spacebar: " ",
    Left: "ArrowLeft",
    Up: "ArrowUp",
    Right: "ArrowRight",
    Down: "ArrowDown",
    Del: "Delete",
    Win: "OS",
    Menu: "ContextMenu",
    Apps: "ContextMenu",
    Scroll: "ScrollLock",
    MozPrintableKey: "Unidentified"
  }, translateToKey = {
    8: "Backspace",
    9: "Tab",
    12: "Clear",
    13: "Enter",
    16: "Shift",
    17: "Control",
    18: "Alt",
    19: "Pause",
    20: "CapsLock",
    27: "Escape",
    32: " ",
    33: "PageUp",
    34: "PageDown",
    35: "End",
    36: "Home",
    37: "ArrowLeft",
    38: "ArrowUp",
    39: "ArrowRight",
    40: "ArrowDown",
    45: "Insert",
    46: "Delete",
    112: "F1",
    113: "F2",
    114: "F3",
    115: "F4",
    116: "F5",
    117: "F6",
    118: "F7",
    119: "F8",
    120: "F9",
    121: "F10",
    122: "F11",
    123: "F12",
    144: "NumLock",
    145: "ScrollLock",
    224: "Meta"
  }, modifierKeyToProp = {
    Alt: "altKey",
    Control: "ctrlKey",
    Meta: "metaKey",
    Shift: "shiftKey"
  };
  function modifierStateGetter(keyArg) {
    var nativeEvent = this.nativeEvent;
    return nativeEvent.getModifierState ? nativeEvent.getModifierState(keyArg) : (keyArg = modifierKeyToProp[keyArg]) ? !!nativeEvent[keyArg] : false;
  }
  function getEventModifierState() {
    return modifierStateGetter;
  }
  var KeyboardEventInterface = assign({}, UIEventInterface, {
    key: function(nativeEvent) {
      if (nativeEvent.key) {
        var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
        if ("Unidentified" !== key) return key;
      }
      return "keypress" === nativeEvent.type ? (nativeEvent = getEventCharCode(nativeEvent), 13 === nativeEvent ? "Enter" : String.fromCharCode(nativeEvent)) : "keydown" === nativeEvent.type || "keyup" === nativeEvent.type ? translateToKey[nativeEvent.keyCode] || "Unidentified" : "";
    },
    code: 0,
    location: 0,
    ctrlKey: 0,
    shiftKey: 0,
    altKey: 0,
    metaKey: 0,
    repeat: 0,
    locale: 0,
    getModifierState: getEventModifierState,
    charCode: function(event) {
      return "keypress" === event.type ? getEventCharCode(event) : 0;
    },
    keyCode: function(event) {
      return "keydown" === event.type || "keyup" === event.type ? event.keyCode : 0;
    },
    which: function(event) {
      return "keypress" === event.type ? getEventCharCode(event) : "keydown" === event.type || "keyup" === event.type ? event.keyCode : 0;
    }
  }), SyntheticKeyboardEvent = createSyntheticEvent(KeyboardEventInterface), PointerEventInterface = assign({}, MouseEventInterface, {
    pointerId: 0,
    width: 0,
    height: 0,
    pressure: 0,
    tangentialPressure: 0,
    tiltX: 0,
    tiltY: 0,
    twist: 0,
    pointerType: 0,
    isPrimary: 0
  }), SyntheticPointerEvent = createSyntheticEvent(PointerEventInterface), TouchEventInterface = assign({}, UIEventInterface, {
    touches: 0,
    targetTouches: 0,
    changedTouches: 0,
    altKey: 0,
    metaKey: 0,
    ctrlKey: 0,
    shiftKey: 0,
    getModifierState: getEventModifierState
  }), SyntheticTouchEvent = createSyntheticEvent(TouchEventInterface), TransitionEventInterface = assign({}, EventInterface, {
    propertyName: 0,
    elapsedTime: 0,
    pseudoElement: 0
  }), SyntheticTransitionEvent = createSyntheticEvent(TransitionEventInterface), WheelEventInterface = assign({}, MouseEventInterface, {
    deltaX: function(event) {
      return "deltaX" in event ? event.deltaX : "wheelDeltaX" in event ? -event.wheelDeltaX : 0;
    },
    deltaY: function(event) {
      return "deltaY" in event ? event.deltaY : "wheelDeltaY" in event ? -event.wheelDeltaY : "wheelDelta" in event ? -event.wheelDelta : 0;
    },
    deltaZ: 0,
    deltaMode: 0
  }), SyntheticWheelEvent = createSyntheticEvent(WheelEventInterface), ToggleEventInterface = assign({}, EventInterface, {
    newState: 0,
    oldState: 0
  }), SyntheticToggleEvent = createSyntheticEvent(ToggleEventInterface), END_KEYCODES = [9, 13, 27, 32], canUseCompositionEvent = canUseDOM && "CompositionEvent" in window, documentMode = null;
  canUseDOM && "documentMode" in document && (documentMode = document.documentMode);
  var canUseTextInputEvent = canUseDOM && "TextEvent" in window && !documentMode, useFallbackCompositionData = canUseDOM && (!canUseCompositionEvent || documentMode && 8 < documentMode && 11 >= documentMode), SPACEBAR_CHAR = String.fromCharCode(32), hasSpaceKeypress = false;
  function isFallbackCompositionEnd(domEventName, nativeEvent) {
    switch (domEventName) {
      case "keyup":
        return -1 !== END_KEYCODES.indexOf(nativeEvent.keyCode);
      case "keydown":
        return 229 !== nativeEvent.keyCode;
      case "keypress":
      case "mousedown":
      case "focusout":
        return true;
      default:
        return false;
    }
  }
  function getDataFromCustomEvent(nativeEvent) {
    nativeEvent = nativeEvent.detail;
    return "object" === typeof nativeEvent && "data" in nativeEvent ? nativeEvent.data : null;
  }
  var isComposing = false;
  function getNativeBeforeInputChars(domEventName, nativeEvent) {
    switch (domEventName) {
      case "compositionend":
        return getDataFromCustomEvent(nativeEvent);
      case "keypress":
        if (32 !== nativeEvent.which) return null;
        hasSpaceKeypress = true;
        return SPACEBAR_CHAR;
      case "textInput":
        return domEventName = nativeEvent.data, domEventName === SPACEBAR_CHAR && hasSpaceKeypress ? null : domEventName;
      default:
        return null;
    }
  }
  function getFallbackBeforeInputChars(domEventName, nativeEvent) {
    if (isComposing)
      return "compositionend" === domEventName || !canUseCompositionEvent && isFallbackCompositionEnd(domEventName, nativeEvent) ? (domEventName = getData(), fallbackText = startText = root2 = null, isComposing = false, domEventName) : null;
    switch (domEventName) {
      case "paste":
        return null;
      case "keypress":
        if (!(nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) || nativeEvent.ctrlKey && nativeEvent.altKey) {
          if (nativeEvent.char && 1 < nativeEvent.char.length)
            return nativeEvent.char;
          if (nativeEvent.which) return String.fromCharCode(nativeEvent.which);
        }
        return null;
      case "compositionend":
        return useFallbackCompositionData && "ko" !== nativeEvent.locale ? null : nativeEvent.data;
      default:
        return null;
    }
  }
  var supportedInputTypes = {
    color: true,
    date: true,
    datetime: true,
    "datetime-local": true,
    email: true,
    month: true,
    number: true,
    password: true,
    range: true,
    search: true,
    tel: true,
    text: true,
    time: true,
    url: true,
    week: true
  };
  function isTextInputElement(elem) {
    var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
    return "input" === nodeName ? !!supportedInputTypes[elem.type] : "textarea" === nodeName ? true : false;
  }
  function createAndAccumulateChangeEvent(dispatchQueue, inst, nativeEvent, target) {
    restoreTarget ? restoreQueue ? restoreQueue.push(target) : restoreQueue = [target] : restoreTarget = target;
    inst = accumulateTwoPhaseListeners(inst, "onChange");
    0 < inst.length && (nativeEvent = new SyntheticEvent(
      "onChange",
      "change",
      null,
      nativeEvent,
      target
    ), dispatchQueue.push({ event: nativeEvent, listeners: inst }));
  }
  var activeElement$1 = null, activeElementInst$1 = null;
  function runEventInBatch(dispatchQueue) {
    processDispatchQueue(dispatchQueue, 0);
  }
  function getInstIfValueChanged(targetInst) {
    var targetNode = getNodeFromInstance(targetInst);
    if (updateValueIfChanged(targetNode)) return targetInst;
  }
  function getTargetInstForChangeEvent(domEventName, targetInst) {
    if ("change" === domEventName) return targetInst;
  }
  var isInputEventSupported = false;
  if (canUseDOM) {
    var JSCompiler_inline_result$jscomp$286;
    if (canUseDOM) {
      var isSupported$jscomp$inline_427 = "oninput" in document;
      if (!isSupported$jscomp$inline_427) {
        var element$jscomp$inline_428 = document.createElement("div");
        element$jscomp$inline_428.setAttribute("oninput", "return;");
        isSupported$jscomp$inline_427 = "function" === typeof element$jscomp$inline_428.oninput;
      }
      JSCompiler_inline_result$jscomp$286 = isSupported$jscomp$inline_427;
    } else JSCompiler_inline_result$jscomp$286 = false;
    isInputEventSupported = JSCompiler_inline_result$jscomp$286 && (!document.documentMode || 9 < document.documentMode);
  }
  function stopWatchingForValueChange() {
    activeElement$1 && (activeElement$1.detachEvent("onpropertychange", handlePropertyChange), activeElementInst$1 = activeElement$1 = null);
  }
  function handlePropertyChange(nativeEvent) {
    if ("value" === nativeEvent.propertyName && getInstIfValueChanged(activeElementInst$1)) {
      var dispatchQueue = [];
      createAndAccumulateChangeEvent(
        dispatchQueue,
        activeElementInst$1,
        nativeEvent,
        getEventTarget(nativeEvent)
      );
      batchedUpdates$1(runEventInBatch, dispatchQueue);
    }
  }
  function handleEventsForInputEventPolyfill(domEventName, target, targetInst) {
    "focusin" === domEventName ? (stopWatchingForValueChange(), activeElement$1 = target, activeElementInst$1 = targetInst, activeElement$1.attachEvent("onpropertychange", handlePropertyChange)) : "focusout" === domEventName && stopWatchingForValueChange();
  }
  function getTargetInstForInputEventPolyfill(domEventName) {
    if ("selectionchange" === domEventName || "keyup" === domEventName || "keydown" === domEventName)
      return getInstIfValueChanged(activeElementInst$1);
  }
  function getTargetInstForClickEvent(domEventName, targetInst) {
    if ("click" === domEventName) return getInstIfValueChanged(targetInst);
  }
  function getTargetInstForInputOrChangeEvent(domEventName, targetInst) {
    if ("input" === domEventName || "change" === domEventName)
      return getInstIfValueChanged(targetInst);
  }
  function is(x, y) {
    return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
  }
  var objectIs = "function" === typeof Object.is ? Object.is : is;
  function shallowEqual(objA, objB) {
    if (objectIs(objA, objB)) return true;
    if ("object" !== typeof objA || null === objA || "object" !== typeof objB || null === objB)
      return false;
    var keysA = Object.keys(objA), keysB = Object.keys(objB);
    if (keysA.length !== keysB.length) return false;
    for (keysB = 0; keysB < keysA.length; keysB++) {
      var currentKey = keysA[keysB];
      if (!hasOwnProperty2.call(objB, currentKey) || !objectIs(objA[currentKey], objB[currentKey]))
        return false;
    }
    return true;
  }
  function getLeafNode(node2) {
    for (; node2 && node2.firstChild; ) node2 = node2.firstChild;
    return node2;
  }
  function getNodeForCharacterOffset(root3, offset) {
    var node2 = getLeafNode(root3);
    root3 = 0;
    for (var nodeEnd; node2; ) {
      if (3 === node2.nodeType) {
        nodeEnd = root3 + node2.textContent.length;
        if (root3 <= offset && nodeEnd >= offset)
          return { node: node2, offset: offset - root3 };
        root3 = nodeEnd;
      }
      a: {
        for (; node2; ) {
          if (node2.nextSibling) {
            node2 = node2.nextSibling;
            break a;
          }
          node2 = node2.parentNode;
        }
        node2 = void 0;
      }
      node2 = getLeafNode(node2);
    }
  }
  function containsNode(outerNode, innerNode) {
    return outerNode && innerNode ? outerNode === innerNode ? true : outerNode && 3 === outerNode.nodeType ? false : innerNode && 3 === innerNode.nodeType ? containsNode(outerNode, innerNode.parentNode) : "contains" in outerNode ? outerNode.contains(innerNode) : outerNode.compareDocumentPosition ? !!(outerNode.compareDocumentPosition(innerNode) & 16) : false : false;
  }
  function getActiveElementDeep(containerInfo) {
    containerInfo = null != containerInfo && null != containerInfo.ownerDocument && null != containerInfo.ownerDocument.defaultView ? containerInfo.ownerDocument.defaultView : window;
    for (var element2 = getActiveElement(containerInfo.document); element2 instanceof containerInfo.HTMLIFrameElement; ) {
      try {
        var JSCompiler_inline_result = "string" === typeof element2.contentWindow.location.href;
      } catch (err) {
        JSCompiler_inline_result = false;
      }
      if (JSCompiler_inline_result) containerInfo = element2.contentWindow;
      else break;
      element2 = getActiveElement(containerInfo.document);
    }
    return element2;
  }
  function hasSelectionCapabilities(elem) {
    var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
    return nodeName && ("input" === nodeName && ("text" === elem.type || "search" === elem.type || "tel" === elem.type || "url" === elem.type || "password" === elem.type) || "textarea" === nodeName || "true" === elem.contentEditable);
  }
  var skipSelectionChangeEvent = canUseDOM && "documentMode" in document && 11 >= document.documentMode, activeElement = null, activeElementInst = null, lastSelection = null, mouseDown = false;
  function constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget) {
    var doc = nativeEventTarget.window === nativeEventTarget ? nativeEventTarget.document : 9 === nativeEventTarget.nodeType ? nativeEventTarget : nativeEventTarget.ownerDocument;
    mouseDown || null == activeElement || activeElement !== getActiveElement(doc) || (doc = activeElement, "selectionStart" in doc && hasSelectionCapabilities(doc) ? doc = { start: doc.selectionStart, end: doc.selectionEnd } : (doc = (doc.ownerDocument && doc.ownerDocument.defaultView || window).getSelection(), doc = {
      anchorNode: doc.anchorNode,
      anchorOffset: doc.anchorOffset,
      focusNode: doc.focusNode,
      focusOffset: doc.focusOffset
    }), lastSelection && shallowEqual(lastSelection, doc) || (lastSelection = doc, doc = accumulateTwoPhaseListeners(activeElementInst, "onSelect"), 0 < doc.length && (nativeEvent = new SyntheticEvent(
      "onSelect",
      "select",
      null,
      nativeEvent,
      nativeEventTarget
    ), dispatchQueue.push({ event: nativeEvent, listeners: doc }), nativeEvent.target = activeElement)));
  }
  function makePrefixMap(styleProp, eventName) {
    var prefixes = {};
    prefixes[styleProp.toLowerCase()] = eventName.toLowerCase();
    prefixes["Webkit" + styleProp] = "webkit" + eventName;
    prefixes["Moz" + styleProp] = "moz" + eventName;
    return prefixes;
  }
  var vendorPrefixes = {
    animationend: makePrefixMap("Animation", "AnimationEnd"),
    animationiteration: makePrefixMap("Animation", "AnimationIteration"),
    animationstart: makePrefixMap("Animation", "AnimationStart"),
    transitionrun: makePrefixMap("Transition", "TransitionRun"),
    transitionstart: makePrefixMap("Transition", "TransitionStart"),
    transitioncancel: makePrefixMap("Transition", "TransitionCancel"),
    transitionend: makePrefixMap("Transition", "TransitionEnd")
  }, prefixedEventNames = {}, style = {};
  canUseDOM && (style = document.createElement("div").style, "AnimationEvent" in window || (delete vendorPrefixes.animationend.animation, delete vendorPrefixes.animationiteration.animation, delete vendorPrefixes.animationstart.animation), "TransitionEvent" in window || delete vendorPrefixes.transitionend.transition);
  function getVendorPrefixedEventName(eventName) {
    if (prefixedEventNames[eventName]) return prefixedEventNames[eventName];
    if (!vendorPrefixes[eventName]) return eventName;
    var prefixMap = vendorPrefixes[eventName], styleProp;
    for (styleProp in prefixMap)
      if (prefixMap.hasOwnProperty(styleProp) && styleProp in style)
        return prefixedEventNames[eventName] = prefixMap[styleProp];
    return eventName;
  }
  var ANIMATION_END = getVendorPrefixedEventName("animationend"), ANIMATION_ITERATION = getVendorPrefixedEventName("animationiteration"), ANIMATION_START = getVendorPrefixedEventName("animationstart"), TRANSITION_RUN = getVendorPrefixedEventName("transitionrun"), TRANSITION_START = getVendorPrefixedEventName("transitionstart"), TRANSITION_CANCEL = getVendorPrefixedEventName("transitioncancel"), TRANSITION_END = getVendorPrefixedEventName("transitionend"), topLevelEventsToReactNames = /* @__PURE__ */ new Map(), simpleEventPluginEvents = "abort auxClick beforeToggle cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(
    " "
  );
  simpleEventPluginEvents.push("scrollEnd");
  function registerSimpleEvent(domEventName, reactName) {
    topLevelEventsToReactNames.set(domEventName, reactName);
    registerTwoPhaseEvent(reactName, [domEventName]);
  }
  var reportGlobalError = "function" === typeof reportError ? reportError : function(error) {
    if ("object" === typeof window && "function" === typeof window.ErrorEvent) {
      var event = new window.ErrorEvent("error", {
        bubbles: true,
        cancelable: true,
        message: "object" === typeof error && null !== error && "string" === typeof error.message ? String(error.message) : String(error),
        error
      });
      if (!window.dispatchEvent(event)) return;
    } else if ("object" === typeof process && "function" === typeof process.emit) {
      process.emit("uncaughtException", error);
      return;
    }
    console.error(error);
  }, concurrentQueues = [], concurrentQueuesIndex = 0, concurrentlyUpdatedLanes = 0;
  function finishQueueingConcurrentUpdates() {
    for (var endIndex = concurrentQueuesIndex, i = concurrentlyUpdatedLanes = concurrentQueuesIndex = 0; i < endIndex; ) {
      var fiber = concurrentQueues[i];
      concurrentQueues[i++] = null;
      var queue = concurrentQueues[i];
      concurrentQueues[i++] = null;
      var update = concurrentQueues[i];
      concurrentQueues[i++] = null;
      var lane = concurrentQueues[i];
      concurrentQueues[i++] = null;
      if (null !== queue && null !== update) {
        var pending = queue.pending;
        null === pending ? update.next = update : (update.next = pending.next, pending.next = update);
        queue.pending = update;
      }
      0 !== lane && markUpdateLaneFromFiberToRoot(fiber, update, lane);
    }
  }
  function enqueueUpdate$1(fiber, queue, update, lane) {
    concurrentQueues[concurrentQueuesIndex++] = fiber;
    concurrentQueues[concurrentQueuesIndex++] = queue;
    concurrentQueues[concurrentQueuesIndex++] = update;
    concurrentQueues[concurrentQueuesIndex++] = lane;
    concurrentlyUpdatedLanes |= lane;
    fiber.lanes |= lane;
    fiber = fiber.alternate;
    null !== fiber && (fiber.lanes |= lane);
  }
  function enqueueConcurrentHookUpdate(fiber, queue, update, lane) {
    enqueueUpdate$1(fiber, queue, update, lane);
    return getRootForUpdatedFiber(fiber);
  }
  function enqueueConcurrentRenderForLane(fiber, lane) {
    enqueueUpdate$1(fiber, null, null, lane);
    return getRootForUpdatedFiber(fiber);
  }
  function markUpdateLaneFromFiberToRoot(sourceFiber, update, lane) {
    sourceFiber.lanes |= lane;
    var alternate = sourceFiber.alternate;
    null !== alternate && (alternate.lanes |= lane);
    for (var isHidden = false, parent = sourceFiber.return; null !== parent; )
      parent.childLanes |= lane, alternate = parent.alternate, null !== alternate && (alternate.childLanes |= lane), 22 === parent.tag && (sourceFiber = parent.stateNode, null === sourceFiber || sourceFiber._visibility & 1 || (isHidden = true)), sourceFiber = parent, parent = parent.return;
    return 3 === sourceFiber.tag ? (parent = sourceFiber.stateNode, isHidden && null !== update && (isHidden = 31 - clz32(lane), sourceFiber = parent.hiddenUpdates, alternate = sourceFiber[isHidden], null === alternate ? sourceFiber[isHidden] = [update] : alternate.push(update), update.lane = lane | 536870912), parent) : null;
  }
  function getRootForUpdatedFiber(sourceFiber) {
    if (50 < nestedUpdateCount)
      throw nestedUpdateCount = 0, rootWithNestedUpdates = null, Error(formatProdErrorMessage(185));
    for (var parent = sourceFiber.return; null !== parent; )
      sourceFiber = parent, parent = sourceFiber.return;
    return 3 === sourceFiber.tag ? sourceFiber.stateNode : null;
  }
  var emptyContextObject = {};
  function FiberNode(tag, pendingProps, key, mode) {
    this.tag = tag;
    this.key = key;
    this.sibling = this.child = this.return = this.stateNode = this.type = this.elementType = null;
    this.index = 0;
    this.refCleanup = this.ref = null;
    this.pendingProps = pendingProps;
    this.dependencies = this.memoizedState = this.updateQueue = this.memoizedProps = null;
    this.mode = mode;
    this.subtreeFlags = this.flags = 0;
    this.deletions = null;
    this.childLanes = this.lanes = 0;
    this.alternate = null;
  }
  function createFiberImplClass(tag, pendingProps, key, mode) {
    return new FiberNode(tag, pendingProps, key, mode);
  }
  function shouldConstruct(Component) {
    Component = Component.prototype;
    return !(!Component || !Component.isReactComponent);
  }
  function createWorkInProgress(current, pendingProps) {
    var workInProgress2 = current.alternate;
    null === workInProgress2 ? (workInProgress2 = createFiberImplClass(
      current.tag,
      pendingProps,
      current.key,
      current.mode
    ), workInProgress2.elementType = current.elementType, workInProgress2.type = current.type, workInProgress2.stateNode = current.stateNode, workInProgress2.alternate = current, current.alternate = workInProgress2) : (workInProgress2.pendingProps = pendingProps, workInProgress2.type = current.type, workInProgress2.flags = 0, workInProgress2.subtreeFlags = 0, workInProgress2.deletions = null);
    workInProgress2.flags = current.flags & 65011712;
    workInProgress2.childLanes = current.childLanes;
    workInProgress2.lanes = current.lanes;
    workInProgress2.child = current.child;
    workInProgress2.memoizedProps = current.memoizedProps;
    workInProgress2.memoizedState = current.memoizedState;
    workInProgress2.updateQueue = current.updateQueue;
    pendingProps = current.dependencies;
    workInProgress2.dependencies = null === pendingProps ? null : { lanes: pendingProps.lanes, firstContext: pendingProps.firstContext };
    workInProgress2.sibling = current.sibling;
    workInProgress2.index = current.index;
    workInProgress2.ref = current.ref;
    workInProgress2.refCleanup = current.refCleanup;
    return workInProgress2;
  }
  function resetWorkInProgress(workInProgress2, renderLanes2) {
    workInProgress2.flags &= 65011714;
    var current = workInProgress2.alternate;
    null === current ? (workInProgress2.childLanes = 0, workInProgress2.lanes = renderLanes2, workInProgress2.child = null, workInProgress2.subtreeFlags = 0, workInProgress2.memoizedProps = null, workInProgress2.memoizedState = null, workInProgress2.updateQueue = null, workInProgress2.dependencies = null, workInProgress2.stateNode = null) : (workInProgress2.childLanes = current.childLanes, workInProgress2.lanes = current.lanes, workInProgress2.child = current.child, workInProgress2.subtreeFlags = 0, workInProgress2.deletions = null, workInProgress2.memoizedProps = current.memoizedProps, workInProgress2.memoizedState = current.memoizedState, workInProgress2.updateQueue = current.updateQueue, workInProgress2.type = current.type, renderLanes2 = current.dependencies, workInProgress2.dependencies = null === renderLanes2 ? null : {
      lanes: renderLanes2.lanes,
      firstContext: renderLanes2.firstContext
    });
    return workInProgress2;
  }
  function createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, lanes) {
    var fiberTag = 0;
    owner = type;
    if ("function" === typeof type) shouldConstruct(type) && (fiberTag = 1);
    else if ("string" === typeof type)
      fiberTag = isHostHoistableType(
        type,
        pendingProps,
        contextStackCursor.current
      ) ? 26 : "html" === type || "head" === type || "body" === type ? 27 : 5;
    else
      a: switch (type) {
        case REACT_ACTIVITY_TYPE:
          return type = createFiberImplClass(31, pendingProps, key, mode), type.elementType = REACT_ACTIVITY_TYPE, type.lanes = lanes, type;
        case REACT_FRAGMENT_TYPE:
          return createFiberFromFragment(pendingProps.children, mode, lanes, key);
        case REACT_STRICT_MODE_TYPE:
          fiberTag = 8;
          mode |= 24;
          break;
        case REACT_PROFILER_TYPE:
          return type = createFiberImplClass(12, pendingProps, key, mode | 2), type.elementType = REACT_PROFILER_TYPE, type.lanes = lanes, type;
        case REACT_SUSPENSE_TYPE:
          return type = createFiberImplClass(13, pendingProps, key, mode), type.elementType = REACT_SUSPENSE_TYPE, type.lanes = lanes, type;
        case REACT_SUSPENSE_LIST_TYPE:
          return type = createFiberImplClass(19, pendingProps, key, mode), type.elementType = REACT_SUSPENSE_LIST_TYPE, type.lanes = lanes, type;
        default:
          if ("object" === typeof type && null !== type)
            switch (type.$$typeof) {
              case REACT_CONTEXT_TYPE:
                fiberTag = 10;
                break a;
              case REACT_CONSUMER_TYPE:
                fiberTag = 9;
                break a;
              case REACT_FORWARD_REF_TYPE:
                fiberTag = 11;
                break a;
              case REACT_MEMO_TYPE:
                fiberTag = 14;
                break a;
              case REACT_LAZY_TYPE:
                fiberTag = 16;
                owner = null;
                break a;
            }
          fiberTag = 29;
          pendingProps = Error(
            formatProdErrorMessage(130, null === type ? "null" : typeof type, "")
          );
          owner = null;
      }
    key = createFiberImplClass(fiberTag, pendingProps, key, mode);
    key.elementType = type;
    key.type = owner;
    key.lanes = lanes;
    return key;
  }
  function createFiberFromFragment(elements, mode, lanes, key) {
    elements = createFiberImplClass(7, elements, key, mode);
    elements.lanes = lanes;
    return elements;
  }
  function createFiberFromText(content2, mode, lanes) {
    content2 = createFiberImplClass(6, content2, null, mode);
    content2.lanes = lanes;
    return content2;
  }
  function createFiberFromDehydratedFragment(dehydratedNode) {
    var fiber = createFiberImplClass(18, null, null, 0);
    fiber.stateNode = dehydratedNode;
    return fiber;
  }
  function createFiberFromPortal(portal, mode, lanes) {
    mode = createFiberImplClass(
      4,
      null !== portal.children ? portal.children : [],
      portal.key,
      mode
    );
    mode.lanes = lanes;
    mode.stateNode = {
      containerInfo: portal.containerInfo,
      pendingChildren: null,
      implementation: portal.implementation
    };
    return mode;
  }
  var CapturedStacks = /* @__PURE__ */ new WeakMap();
  function createCapturedValueAtFiber(value, source) {
    if ("object" === typeof value && null !== value) {
      var existing = CapturedStacks.get(value);
      if (void 0 !== existing) return existing;
      source = {
        value,
        source,
        stack: getStackByFiberInDevAndProd(source)
      };
      CapturedStacks.set(value, source);
      return source;
    }
    return {
      value,
      source,
      stack: getStackByFiberInDevAndProd(source)
    };
  }
  var forkStack = [], forkStackIndex = 0, treeForkProvider = null, treeForkCount = 0, idStack = [], idStackIndex = 0, treeContextProvider = null, treeContextId = 1, treeContextOverflow = "";
  function pushTreeFork(workInProgress2, totalChildren) {
    forkStack[forkStackIndex++] = treeForkCount;
    forkStack[forkStackIndex++] = treeForkProvider;
    treeForkProvider = workInProgress2;
    treeForkCount = totalChildren;
  }
  function pushTreeId(workInProgress2, totalChildren, index3) {
    idStack[idStackIndex++] = treeContextId;
    idStack[idStackIndex++] = treeContextOverflow;
    idStack[idStackIndex++] = treeContextProvider;
    treeContextProvider = workInProgress2;
    var baseIdWithLeadingBit = treeContextId;
    workInProgress2 = treeContextOverflow;
    var baseLength = 32 - clz32(baseIdWithLeadingBit) - 1;
    baseIdWithLeadingBit &= ~(1 << baseLength);
    index3 += 1;
    var length = 32 - clz32(totalChildren) + baseLength;
    if (30 < length) {
      var numberOfOverflowBits = baseLength - baseLength % 5;
      length = (baseIdWithLeadingBit & (1 << numberOfOverflowBits) - 1).toString(32);
      baseIdWithLeadingBit >>= numberOfOverflowBits;
      baseLength -= numberOfOverflowBits;
      treeContextId = 1 << 32 - clz32(totalChildren) + baseLength | index3 << baseLength | baseIdWithLeadingBit;
      treeContextOverflow = length + workInProgress2;
    } else
      treeContextId = 1 << length | index3 << baseLength | baseIdWithLeadingBit, treeContextOverflow = workInProgress2;
  }
  function pushMaterializedTreeId(workInProgress2) {
    null !== workInProgress2.return && (pushTreeFork(workInProgress2, 1), pushTreeId(workInProgress2, 1, 0));
  }
  function popTreeContext(workInProgress2) {
    for (; workInProgress2 === treeForkProvider; )
      treeForkProvider = forkStack[--forkStackIndex], forkStack[forkStackIndex] = null, treeForkCount = forkStack[--forkStackIndex], forkStack[forkStackIndex] = null;
    for (; workInProgress2 === treeContextProvider; )
      treeContextProvider = idStack[--idStackIndex], idStack[idStackIndex] = null, treeContextOverflow = idStack[--idStackIndex], idStack[idStackIndex] = null, treeContextId = idStack[--idStackIndex], idStack[idStackIndex] = null;
  }
  function restoreSuspendedTreeContext(workInProgress2, suspendedContext) {
    idStack[idStackIndex++] = treeContextId;
    idStack[idStackIndex++] = treeContextOverflow;
    idStack[idStackIndex++] = treeContextProvider;
    treeContextId = suspendedContext.id;
    treeContextOverflow = suspendedContext.overflow;
    treeContextProvider = workInProgress2;
  }
  var hydrationParentFiber = null, nextHydratableInstance = null, isHydrating = false, hydrationErrors = null, rootOrSingletonContext = false, HydrationMismatchException = Error(formatProdErrorMessage(519));
  function throwOnHydrationMismatch(fiber) {
    var error = Error(
      formatProdErrorMessage(
        418,
        1 < arguments.length && void 0 !== arguments[1] && arguments[1] ? "text" : "HTML",
        ""
      )
    );
    queueHydrationError(createCapturedValueAtFiber(error, fiber));
    throw HydrationMismatchException;
  }
  function prepareToHydrateHostInstance(fiber) {
    var instance = fiber.stateNode, type = fiber.type, props = fiber.memoizedProps;
    instance[internalInstanceKey] = fiber;
    instance[internalPropsKey] = props;
    switch (type) {
      case "dialog":
        listenToNonDelegatedEvent("cancel", instance);
        listenToNonDelegatedEvent("close", instance);
        break;
      case "iframe":
      case "object":
      case "embed":
        listenToNonDelegatedEvent("load", instance);
        break;
      case "video":
      case "audio":
        for (type = 0; type < mediaEventTypes.length; type++)
          listenToNonDelegatedEvent(mediaEventTypes[type], instance);
        break;
      case "source":
        listenToNonDelegatedEvent("error", instance);
        break;
      case "img":
      case "image":
      case "link":
        listenToNonDelegatedEvent("error", instance);
        listenToNonDelegatedEvent("load", instance);
        break;
      case "details":
        listenToNonDelegatedEvent("toggle", instance);
        break;
      case "input":
        listenToNonDelegatedEvent("invalid", instance);
        initInput(
          instance,
          props.value,
          props.defaultValue,
          props.checked,
          props.defaultChecked,
          props.type,
          props.name,
          true
        );
        break;
      case "select":
        listenToNonDelegatedEvent("invalid", instance);
        break;
      case "textarea":
        listenToNonDelegatedEvent("invalid", instance), initTextarea(instance, props.value, props.defaultValue, props.children);
    }
    type = props.children;
    "string" !== typeof type && "number" !== typeof type && "bigint" !== typeof type || instance.textContent === "" + type || true === props.suppressHydrationWarning || checkForUnmatchedText(instance.textContent, type) ? (null != props.popover && (listenToNonDelegatedEvent("beforetoggle", instance), listenToNonDelegatedEvent("toggle", instance)), null != props.onScroll && listenToNonDelegatedEvent("scroll", instance), null != props.onScrollEnd && listenToNonDelegatedEvent("scrollend", instance), null != props.onClick && (instance.onclick = noop$1), instance = true) : instance = false;
    instance || throwOnHydrationMismatch(fiber, true);
  }
  function popToNextHostParent(fiber) {
    for (hydrationParentFiber = fiber.return; hydrationParentFiber; )
      switch (hydrationParentFiber.tag) {
        case 5:
        case 31:
        case 13:
          rootOrSingletonContext = false;
          return;
        case 27:
        case 3:
          rootOrSingletonContext = true;
          return;
        default:
          hydrationParentFiber = hydrationParentFiber.return;
      }
  }
  function popHydrationState(fiber) {
    if (fiber !== hydrationParentFiber) return false;
    if (!isHydrating) return popToNextHostParent(fiber), isHydrating = true, false;
    var tag = fiber.tag, JSCompiler_temp;
    if (JSCompiler_temp = 3 !== tag && 27 !== tag) {
      if (JSCompiler_temp = 5 === tag)
        JSCompiler_temp = fiber.type, JSCompiler_temp = !("form" !== JSCompiler_temp && "button" !== JSCompiler_temp) || shouldSetTextContent(fiber.type, fiber.memoizedProps);
      JSCompiler_temp = !JSCompiler_temp;
    }
    JSCompiler_temp && nextHydratableInstance && throwOnHydrationMismatch(fiber);
    popToNextHostParent(fiber);
    if (13 === tag) {
      fiber = fiber.memoizedState;
      fiber = null !== fiber ? fiber.dehydrated : null;
      if (!fiber) throw Error(formatProdErrorMessage(317));
      nextHydratableInstance = getNextHydratableInstanceAfterHydrationBoundary(fiber);
    } else if (31 === tag) {
      fiber = fiber.memoizedState;
      fiber = null !== fiber ? fiber.dehydrated : null;
      if (!fiber) throw Error(formatProdErrorMessage(317));
      nextHydratableInstance = getNextHydratableInstanceAfterHydrationBoundary(fiber);
    } else
      27 === tag ? (tag = nextHydratableInstance, isSingletonScope(fiber.type) ? (fiber = previousHydratableOnEnteringScopedSingleton, previousHydratableOnEnteringScopedSingleton = null, nextHydratableInstance = fiber) : nextHydratableInstance = tag) : nextHydratableInstance = hydrationParentFiber ? getNextHydratable(fiber.stateNode.nextSibling) : null;
    return true;
  }
  function resetHydrationState() {
    nextHydratableInstance = hydrationParentFiber = null;
    isHydrating = false;
  }
  function upgradeHydrationErrorsToRecoverable() {
    var queuedErrors = hydrationErrors;
    null !== queuedErrors && (null === workInProgressRootRecoverableErrors ? workInProgressRootRecoverableErrors = queuedErrors : workInProgressRootRecoverableErrors.push.apply(
      workInProgressRootRecoverableErrors,
      queuedErrors
    ), hydrationErrors = null);
    return queuedErrors;
  }
  function queueHydrationError(error) {
    null === hydrationErrors ? hydrationErrors = [error] : hydrationErrors.push(error);
  }
  var valueCursor = createCursor(null), currentlyRenderingFiber$1 = null, lastContextDependency = null;
  function pushProvider(providerFiber, context, nextValue) {
    push2(valueCursor, context._currentValue);
    context._currentValue = nextValue;
  }
  function popProvider(context) {
    context._currentValue = valueCursor.current;
    pop(valueCursor);
  }
  function scheduleContextWorkOnParentPath(parent, renderLanes2, propagationRoot) {
    for (; null !== parent; ) {
      var alternate = parent.alternate;
      (parent.childLanes & renderLanes2) !== renderLanes2 ? (parent.childLanes |= renderLanes2, null !== alternate && (alternate.childLanes |= renderLanes2)) : null !== alternate && (alternate.childLanes & renderLanes2) !== renderLanes2 && (alternate.childLanes |= renderLanes2);
      if (parent === propagationRoot) break;
      parent = parent.return;
    }
  }
  function propagateContextChanges(workInProgress2, contexts, renderLanes2, forcePropagateEntireTree) {
    var fiber = workInProgress2.child;
    null !== fiber && (fiber.return = workInProgress2);
    for (; null !== fiber; ) {
      var list2 = fiber.dependencies;
      if (null !== list2) {
        var nextFiber = fiber.child;
        list2 = list2.firstContext;
        a: for (; null !== list2; ) {
          var dependency = list2;
          list2 = fiber;
          for (var i = 0; i < contexts.length; i++)
            if (dependency.context === contexts[i]) {
              list2.lanes |= renderLanes2;
              dependency = list2.alternate;
              null !== dependency && (dependency.lanes |= renderLanes2);
              scheduleContextWorkOnParentPath(
                list2.return,
                renderLanes2,
                workInProgress2
              );
              forcePropagateEntireTree || (nextFiber = null);
              break a;
            }
          list2 = dependency.next;
        }
      } else if (18 === fiber.tag) {
        nextFiber = fiber.return;
        if (null === nextFiber) throw Error(formatProdErrorMessage(341));
        nextFiber.lanes |= renderLanes2;
        list2 = nextFiber.alternate;
        null !== list2 && (list2.lanes |= renderLanes2);
        scheduleContextWorkOnParentPath(nextFiber, renderLanes2, workInProgress2);
        nextFiber = null;
      } else nextFiber = fiber.child;
      if (null !== nextFiber) nextFiber.return = fiber;
      else
        for (nextFiber = fiber; null !== nextFiber; ) {
          if (nextFiber === workInProgress2) {
            nextFiber = null;
            break;
          }
          fiber = nextFiber.sibling;
          if (null !== fiber) {
            fiber.return = nextFiber.return;
            nextFiber = fiber;
            break;
          }
          nextFiber = nextFiber.return;
        }
      fiber = nextFiber;
    }
  }
  function propagateParentContextChanges(current, workInProgress2, renderLanes2, forcePropagateEntireTree) {
    current = null;
    for (var parent = workInProgress2, isInsidePropagationBailout = false; null !== parent; ) {
      if (!isInsidePropagationBailout) {
        if (0 !== (parent.flags & 524288)) isInsidePropagationBailout = true;
        else if (0 !== (parent.flags & 262144)) break;
      }
      if (10 === parent.tag) {
        var currentParent = parent.alternate;
        if (null === currentParent) throw Error(formatProdErrorMessage(387));
        currentParent = currentParent.memoizedProps;
        if (null !== currentParent) {
          var context = parent.type;
          objectIs(parent.pendingProps.value, currentParent.value) || (null !== current ? current.push(context) : current = [context]);
        }
      } else if (parent === hostTransitionProviderCursor.current) {
        currentParent = parent.alternate;
        if (null === currentParent) throw Error(formatProdErrorMessage(387));
        currentParent.memoizedState.memoizedState !== parent.memoizedState.memoizedState && (null !== current ? current.push(HostTransitionContext) : current = [HostTransitionContext]);
      }
      parent = parent.return;
    }
    null !== current && propagateContextChanges(
      workInProgress2,
      current,
      renderLanes2,
      forcePropagateEntireTree
    );
    workInProgress2.flags |= 262144;
  }
  function checkIfContextChanged(currentDependencies) {
    for (currentDependencies = currentDependencies.firstContext; null !== currentDependencies; ) {
      if (!objectIs(
        currentDependencies.context._currentValue,
        currentDependencies.memoizedValue
      ))
        return true;
      currentDependencies = currentDependencies.next;
    }
    return false;
  }
  function prepareToReadContext(workInProgress2) {
    currentlyRenderingFiber$1 = workInProgress2;
    lastContextDependency = null;
    workInProgress2 = workInProgress2.dependencies;
    null !== workInProgress2 && (workInProgress2.firstContext = null);
  }
  function readContext(context) {
    return readContextForConsumer(currentlyRenderingFiber$1, context);
  }
  function readContextDuringReconciliation(consumer, context) {
    null === currentlyRenderingFiber$1 && prepareToReadContext(consumer);
    return readContextForConsumer(consumer, context);
  }
  function readContextForConsumer(consumer, context) {
    var value = context._currentValue;
    context = { context, memoizedValue: value, next: null };
    if (null === lastContextDependency) {
      if (null === consumer) throw Error(formatProdErrorMessage(308));
      lastContextDependency = context;
      consumer.dependencies = { lanes: 0, firstContext: context };
      consumer.flags |= 524288;
    } else lastContextDependency = lastContextDependency.next = context;
    return value;
  }
  var AbortControllerLocal = "undefined" !== typeof AbortController ? AbortController : function() {
    var listeners = [], signal = this.signal = {
      aborted: false,
      addEventListener: function(type, listener) {
        listeners.push(listener);
      }
    };
    this.abort = function() {
      signal.aborted = true;
      listeners.forEach(function(listener) {
        return listener();
      });
    };
  }, scheduleCallback$2 = Scheduler.unstable_scheduleCallback, NormalPriority = Scheduler.unstable_NormalPriority, CacheContext = {
    $$typeof: REACT_CONTEXT_TYPE,
    Consumer: null,
    Provider: null,
    _currentValue: null,
    _currentValue2: null,
    _threadCount: 0
  };
  function createCache() {
    return {
      controller: new AbortControllerLocal(),
      data: /* @__PURE__ */ new Map(),
      refCount: 0
    };
  }
  function releaseCache(cache) {
    cache.refCount--;
    0 === cache.refCount && scheduleCallback$2(NormalPriority, function() {
      cache.controller.abort();
    });
  }
  var currentEntangledListeners = null, currentEntangledPendingCount = 0, currentEntangledLane = 0, currentEntangledActionThenable = null;
  function entangleAsyncAction(transition, thenable) {
    if (null === currentEntangledListeners) {
      var entangledListeners = currentEntangledListeners = [];
      currentEntangledPendingCount = 0;
      currentEntangledLane = requestTransitionLane();
      currentEntangledActionThenable = {
        status: "pending",
        value: void 0,
        then: function(resolve) {
          entangledListeners.push(resolve);
        }
      };
    }
    currentEntangledPendingCount++;
    thenable.then(pingEngtangledActionScope, pingEngtangledActionScope);
    return thenable;
  }
  function pingEngtangledActionScope() {
    if (0 === --currentEntangledPendingCount && null !== currentEntangledListeners) {
      null !== currentEntangledActionThenable && (currentEntangledActionThenable.status = "fulfilled");
      var listeners = currentEntangledListeners;
      currentEntangledListeners = null;
      currentEntangledLane = 0;
      currentEntangledActionThenable = null;
      for (var i = 0; i < listeners.length; i++) (0, listeners[i])();
    }
  }
  function chainThenableValue(thenable, result) {
    var listeners = [], thenableWithOverride = {
      status: "pending",
      value: null,
      reason: null,
      then: function(resolve) {
        listeners.push(resolve);
      }
    };
    thenable.then(
      function() {
        thenableWithOverride.status = "fulfilled";
        thenableWithOverride.value = result;
        for (var i = 0; i < listeners.length; i++) (0, listeners[i])(result);
      },
      function(error) {
        thenableWithOverride.status = "rejected";
        thenableWithOverride.reason = error;
        for (error = 0; error < listeners.length; error++)
          (0, listeners[error])(void 0);
      }
    );
    return thenableWithOverride;
  }
  var prevOnStartTransitionFinish = ReactSharedInternals.S;
  ReactSharedInternals.S = function(transition, returnValue) {
    globalMostRecentTransitionTime = now();
    "object" === typeof returnValue && null !== returnValue && "function" === typeof returnValue.then && entangleAsyncAction(transition, returnValue);
    null !== prevOnStartTransitionFinish && prevOnStartTransitionFinish(transition, returnValue);
  };
  var resumedCache = createCursor(null);
  function peekCacheFromPool() {
    var cacheResumedFromPreviousRender = resumedCache.current;
    return null !== cacheResumedFromPreviousRender ? cacheResumedFromPreviousRender : workInProgressRoot.pooledCache;
  }
  function pushTransition(offscreenWorkInProgress, prevCachePool) {
    null === prevCachePool ? push2(resumedCache, resumedCache.current) : push2(resumedCache, prevCachePool.pool);
  }
  function getSuspendedCache() {
    var cacheFromPool = peekCacheFromPool();
    return null === cacheFromPool ? null : { parent: CacheContext._currentValue, pool: cacheFromPool };
  }
  var SuspenseException = Error(formatProdErrorMessage(460)), SuspenseyCommitException = Error(formatProdErrorMessage(474)), SuspenseActionException = Error(formatProdErrorMessage(542)), noopSuspenseyCommitThenable = { then: function() {
  } };
  function isThenableResolved(thenable) {
    thenable = thenable.status;
    return "fulfilled" === thenable || "rejected" === thenable;
  }
  function trackUsedThenable(thenableState2, thenable, index3) {
    index3 = thenableState2[index3];
    void 0 === index3 ? thenableState2.push(thenable) : index3 !== thenable && (thenable.then(noop$1, noop$1), thenable = index3);
    switch (thenable.status) {
      case "fulfilled":
        return thenable.value;
      case "rejected":
        throw thenableState2 = thenable.reason, checkIfUseWrappedInAsyncCatch(thenableState2), thenableState2;
      default:
        if ("string" === typeof thenable.status) thenable.then(noop$1, noop$1);
        else {
          thenableState2 = workInProgressRoot;
          if (null !== thenableState2 && 100 < thenableState2.shellSuspendCounter)
            throw Error(formatProdErrorMessage(482));
          thenableState2 = thenable;
          thenableState2.status = "pending";
          thenableState2.then(
            function(fulfilledValue) {
              if ("pending" === thenable.status) {
                var fulfilledThenable = thenable;
                fulfilledThenable.status = "fulfilled";
                fulfilledThenable.value = fulfilledValue;
              }
            },
            function(error) {
              if ("pending" === thenable.status) {
                var rejectedThenable = thenable;
                rejectedThenable.status = "rejected";
                rejectedThenable.reason = error;
              }
            }
          );
        }
        switch (thenable.status) {
          case "fulfilled":
            return thenable.value;
          case "rejected":
            throw thenableState2 = thenable.reason, checkIfUseWrappedInAsyncCatch(thenableState2), thenableState2;
        }
        suspendedThenable = thenable;
        throw SuspenseException;
    }
  }
  function resolveLazy(lazyType) {
    try {
      var init = lazyType._init;
      return init(lazyType._payload);
    } catch (x) {
      if (null !== x && "object" === typeof x && "function" === typeof x.then)
        throw suspendedThenable = x, SuspenseException;
      throw x;
    }
  }
  var suspendedThenable = null;
  function getSuspendedThenable() {
    if (null === suspendedThenable) throw Error(formatProdErrorMessage(459));
    var thenable = suspendedThenable;
    suspendedThenable = null;
    return thenable;
  }
  function checkIfUseWrappedInAsyncCatch(rejectedReason) {
    if (rejectedReason === SuspenseException || rejectedReason === SuspenseActionException)
      throw Error(formatProdErrorMessage(483));
  }
  var thenableState$1 = null, thenableIndexCounter$1 = 0;
  function unwrapThenable(thenable) {
    var index3 = thenableIndexCounter$1;
    thenableIndexCounter$1 += 1;
    null === thenableState$1 && (thenableState$1 = []);
    return trackUsedThenable(thenableState$1, thenable, index3);
  }
  function coerceRef(workInProgress2, element2) {
    element2 = element2.props.ref;
    workInProgress2.ref = void 0 !== element2 ? element2 : null;
  }
  function throwOnInvalidObjectTypeImpl(returnFiber, newChild) {
    if (newChild.$$typeof === REACT_LEGACY_ELEMENT_TYPE)
      throw Error(formatProdErrorMessage(525));
    returnFiber = Object.prototype.toString.call(newChild);
    throw Error(
      formatProdErrorMessage(
        31,
        "[object Object]" === returnFiber ? "object with keys {" + Object.keys(newChild).join(", ") + "}" : returnFiber
      )
    );
  }
  function createChildReconciler(shouldTrackSideEffects) {
    function deleteChild(returnFiber, childToDelete) {
      if (shouldTrackSideEffects) {
        var deletions = returnFiber.deletions;
        null === deletions ? (returnFiber.deletions = [childToDelete], returnFiber.flags |= 16) : deletions.push(childToDelete);
      }
    }
    function deleteRemainingChildren(returnFiber, currentFirstChild) {
      if (!shouldTrackSideEffects) return null;
      for (; null !== currentFirstChild; )
        deleteChild(returnFiber, currentFirstChild), currentFirstChild = currentFirstChild.sibling;
      return null;
    }
    function mapRemainingChildren(currentFirstChild) {
      for (var existingChildren = /* @__PURE__ */ new Map(); null !== currentFirstChild; )
        null !== currentFirstChild.key ? existingChildren.set(currentFirstChild.key, currentFirstChild) : existingChildren.set(currentFirstChild.index, currentFirstChild), currentFirstChild = currentFirstChild.sibling;
      return existingChildren;
    }
    function useFiber(fiber, pendingProps) {
      fiber = createWorkInProgress(fiber, pendingProps);
      fiber.index = 0;
      fiber.sibling = null;
      return fiber;
    }
    function placeChild(newFiber, lastPlacedIndex, newIndex) {
      newFiber.index = newIndex;
      if (!shouldTrackSideEffects)
        return newFiber.flags |= 1048576, lastPlacedIndex;
      newIndex = newFiber.alternate;
      if (null !== newIndex)
        return newIndex = newIndex.index, newIndex < lastPlacedIndex ? (newFiber.flags |= 67108866, lastPlacedIndex) : newIndex;
      newFiber.flags |= 67108866;
      return lastPlacedIndex;
    }
    function placeSingleChild(newFiber) {
      shouldTrackSideEffects && null === newFiber.alternate && (newFiber.flags |= 67108866);
      return newFiber;
    }
    function updateTextNode(returnFiber, current, textContent, lanes) {
      if (null === current || 6 !== current.tag)
        return current = createFiberFromText(textContent, returnFiber.mode, lanes), current.return = returnFiber, current;
      current = useFiber(current, textContent);
      current.return = returnFiber;
      return current;
    }
    function updateElement(returnFiber, current, element2, lanes) {
      var elementType = element2.type;
      if (elementType === REACT_FRAGMENT_TYPE)
        return updateFragment(
          returnFiber,
          current,
          element2.props.children,
          lanes,
          element2.key
        );
      if (null !== current && (current.elementType === elementType || "object" === typeof elementType && null !== elementType && elementType.$$typeof === REACT_LAZY_TYPE && resolveLazy(elementType) === current.type))
        return current = useFiber(current, element2.props), coerceRef(current, element2), current.return = returnFiber, current;
      current = createFiberFromTypeAndProps(
        element2.type,
        element2.key,
        element2.props,
        null,
        returnFiber.mode,
        lanes
      );
      coerceRef(current, element2);
      current.return = returnFiber;
      return current;
    }
    function updatePortal(returnFiber, current, portal, lanes) {
      if (null === current || 4 !== current.tag || current.stateNode.containerInfo !== portal.containerInfo || current.stateNode.implementation !== portal.implementation)
        return current = createFiberFromPortal(portal, returnFiber.mode, lanes), current.return = returnFiber, current;
      current = useFiber(current, portal.children || []);
      current.return = returnFiber;
      return current;
    }
    function updateFragment(returnFiber, current, fragment, lanes, key) {
      if (null === current || 7 !== current.tag)
        return current = createFiberFromFragment(
          fragment,
          returnFiber.mode,
          lanes,
          key
        ), current.return = returnFiber, current;
      current = useFiber(current, fragment);
      current.return = returnFiber;
      return current;
    }
    function createChild(returnFiber, newChild, lanes) {
      if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild)
        return newChild = createFiberFromText(
          "" + newChild,
          returnFiber.mode,
          lanes
        ), newChild.return = returnFiber, newChild;
      if ("object" === typeof newChild && null !== newChild) {
        switch (newChild.$$typeof) {
          case REACT_ELEMENT_TYPE:
            return lanes = createFiberFromTypeAndProps(
              newChild.type,
              newChild.key,
              newChild.props,
              null,
              returnFiber.mode,
              lanes
            ), coerceRef(lanes, newChild), lanes.return = returnFiber, lanes;
          case REACT_PORTAL_TYPE:
            return newChild = createFiberFromPortal(
              newChild,
              returnFiber.mode,
              lanes
            ), newChild.return = returnFiber, newChild;
          case REACT_LAZY_TYPE:
            return newChild = resolveLazy(newChild), createChild(returnFiber, newChild, lanes);
        }
        if (isArrayImpl(newChild) || getIteratorFn(newChild))
          return newChild = createFiberFromFragment(
            newChild,
            returnFiber.mode,
            lanes,
            null
          ), newChild.return = returnFiber, newChild;
        if ("function" === typeof newChild.then)
          return createChild(returnFiber, unwrapThenable(newChild), lanes);
        if (newChild.$$typeof === REACT_CONTEXT_TYPE)
          return createChild(
            returnFiber,
            readContextDuringReconciliation(returnFiber, newChild),
            lanes
          );
        throwOnInvalidObjectTypeImpl(returnFiber, newChild);
      }
      return null;
    }
    function updateSlot(returnFiber, oldFiber, newChild, lanes) {
      var key = null !== oldFiber ? oldFiber.key : null;
      if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild)
        return null !== key ? null : updateTextNode(returnFiber, oldFiber, "" + newChild, lanes);
      if ("object" === typeof newChild && null !== newChild) {
        switch (newChild.$$typeof) {
          case REACT_ELEMENT_TYPE:
            return newChild.key === key ? updateElement(returnFiber, oldFiber, newChild, lanes) : null;
          case REACT_PORTAL_TYPE:
            return newChild.key === key ? updatePortal(returnFiber, oldFiber, newChild, lanes) : null;
          case REACT_LAZY_TYPE:
            return newChild = resolveLazy(newChild), updateSlot(returnFiber, oldFiber, newChild, lanes);
        }
        if (isArrayImpl(newChild) || getIteratorFn(newChild))
          return null !== key ? null : updateFragment(returnFiber, oldFiber, newChild, lanes, null);
        if ("function" === typeof newChild.then)
          return updateSlot(
            returnFiber,
            oldFiber,
            unwrapThenable(newChild),
            lanes
          );
        if (newChild.$$typeof === REACT_CONTEXT_TYPE)
          return updateSlot(
            returnFiber,
            oldFiber,
            readContextDuringReconciliation(returnFiber, newChild),
            lanes
          );
        throwOnInvalidObjectTypeImpl(returnFiber, newChild);
      }
      return null;
    }
    function updateFromMap(existingChildren, returnFiber, newIdx, newChild, lanes) {
      if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild)
        return existingChildren = existingChildren.get(newIdx) || null, updateTextNode(returnFiber, existingChildren, "" + newChild, lanes);
      if ("object" === typeof newChild && null !== newChild) {
        switch (newChild.$$typeof) {
          case REACT_ELEMENT_TYPE:
            return existingChildren = existingChildren.get(
              null === newChild.key ? newIdx : newChild.key
            ) || null, updateElement(returnFiber, existingChildren, newChild, lanes);
          case REACT_PORTAL_TYPE:
            return existingChildren = existingChildren.get(
              null === newChild.key ? newIdx : newChild.key
            ) || null, updatePortal(returnFiber, existingChildren, newChild, lanes);
          case REACT_LAZY_TYPE:
            return newChild = resolveLazy(newChild), updateFromMap(
              existingChildren,
              returnFiber,
              newIdx,
              newChild,
              lanes
            );
        }
        if (isArrayImpl(newChild) || getIteratorFn(newChild))
          return existingChildren = existingChildren.get(newIdx) || null, updateFragment(returnFiber, existingChildren, newChild, lanes, null);
        if ("function" === typeof newChild.then)
          return updateFromMap(
            existingChildren,
            returnFiber,
            newIdx,
            unwrapThenable(newChild),
            lanes
          );
        if (newChild.$$typeof === REACT_CONTEXT_TYPE)
          return updateFromMap(
            existingChildren,
            returnFiber,
            newIdx,
            readContextDuringReconciliation(returnFiber, newChild),
            lanes
          );
        throwOnInvalidObjectTypeImpl(returnFiber, newChild);
      }
      return null;
    }
    function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, lanes) {
      for (var resultingFirstChild = null, previousNewFiber = null, oldFiber = currentFirstChild, newIdx = currentFirstChild = 0, nextOldFiber = null; null !== oldFiber && newIdx < newChildren.length; newIdx++) {
        oldFiber.index > newIdx ? (nextOldFiber = oldFiber, oldFiber = null) : nextOldFiber = oldFiber.sibling;
        var newFiber = updateSlot(
          returnFiber,
          oldFiber,
          newChildren[newIdx],
          lanes
        );
        if (null === newFiber) {
          null === oldFiber && (oldFiber = nextOldFiber);
          break;
        }
        shouldTrackSideEffects && oldFiber && null === newFiber.alternate && deleteChild(returnFiber, oldFiber);
        currentFirstChild = placeChild(newFiber, currentFirstChild, newIdx);
        null === previousNewFiber ? resultingFirstChild = newFiber : previousNewFiber.sibling = newFiber;
        previousNewFiber = newFiber;
        oldFiber = nextOldFiber;
      }
      if (newIdx === newChildren.length)
        return deleteRemainingChildren(returnFiber, oldFiber), isHydrating && pushTreeFork(returnFiber, newIdx), resultingFirstChild;
      if (null === oldFiber) {
        for (; newIdx < newChildren.length; newIdx++)
          oldFiber = createChild(returnFiber, newChildren[newIdx], lanes), null !== oldFiber && (currentFirstChild = placeChild(
            oldFiber,
            currentFirstChild,
            newIdx
          ), null === previousNewFiber ? resultingFirstChild = oldFiber : previousNewFiber.sibling = oldFiber, previousNewFiber = oldFiber);
        isHydrating && pushTreeFork(returnFiber, newIdx);
        return resultingFirstChild;
      }
      for (oldFiber = mapRemainingChildren(oldFiber); newIdx < newChildren.length; newIdx++)
        nextOldFiber = updateFromMap(
          oldFiber,
          returnFiber,
          newIdx,
          newChildren[newIdx],
          lanes
        ), null !== nextOldFiber && (shouldTrackSideEffects && null !== nextOldFiber.alternate && oldFiber.delete(
          null === nextOldFiber.key ? newIdx : nextOldFiber.key
        ), currentFirstChild = placeChild(
          nextOldFiber,
          currentFirstChild,
          newIdx
        ), null === previousNewFiber ? resultingFirstChild = nextOldFiber : previousNewFiber.sibling = nextOldFiber, previousNewFiber = nextOldFiber);
      shouldTrackSideEffects && oldFiber.forEach(function(child) {
        return deleteChild(returnFiber, child);
      });
      isHydrating && pushTreeFork(returnFiber, newIdx);
      return resultingFirstChild;
    }
    function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildren, lanes) {
      if (null == newChildren) throw Error(formatProdErrorMessage(151));
      for (var resultingFirstChild = null, previousNewFiber = null, oldFiber = currentFirstChild, newIdx = currentFirstChild = 0, nextOldFiber = null, step = newChildren.next(); null !== oldFiber && !step.done; newIdx++, step = newChildren.next()) {
        oldFiber.index > newIdx ? (nextOldFiber = oldFiber, oldFiber = null) : nextOldFiber = oldFiber.sibling;
        var newFiber = updateSlot(returnFiber, oldFiber, step.value, lanes);
        if (null === newFiber) {
          null === oldFiber && (oldFiber = nextOldFiber);
          break;
        }
        shouldTrackSideEffects && oldFiber && null === newFiber.alternate && deleteChild(returnFiber, oldFiber);
        currentFirstChild = placeChild(newFiber, currentFirstChild, newIdx);
        null === previousNewFiber ? resultingFirstChild = newFiber : previousNewFiber.sibling = newFiber;
        previousNewFiber = newFiber;
        oldFiber = nextOldFiber;
      }
      if (step.done)
        return deleteRemainingChildren(returnFiber, oldFiber), isHydrating && pushTreeFork(returnFiber, newIdx), resultingFirstChild;
      if (null === oldFiber) {
        for (; !step.done; newIdx++, step = newChildren.next())
          step = createChild(returnFiber, step.value, lanes), null !== step && (currentFirstChild = placeChild(step, currentFirstChild, newIdx), null === previousNewFiber ? resultingFirstChild = step : previousNewFiber.sibling = step, previousNewFiber = step);
        isHydrating && pushTreeFork(returnFiber, newIdx);
        return resultingFirstChild;
      }
      for (oldFiber = mapRemainingChildren(oldFiber); !step.done; newIdx++, step = newChildren.next())
        step = updateFromMap(oldFiber, returnFiber, newIdx, step.value, lanes), null !== step && (shouldTrackSideEffects && null !== step.alternate && oldFiber.delete(null === step.key ? newIdx : step.key), currentFirstChild = placeChild(step, currentFirstChild, newIdx), null === previousNewFiber ? resultingFirstChild = step : previousNewFiber.sibling = step, previousNewFiber = step);
      shouldTrackSideEffects && oldFiber.forEach(function(child) {
        return deleteChild(returnFiber, child);
      });
      isHydrating && pushTreeFork(returnFiber, newIdx);
      return resultingFirstChild;
    }
    function reconcileChildFibersImpl(returnFiber, currentFirstChild, newChild, lanes) {
      "object" === typeof newChild && null !== newChild && newChild.type === REACT_FRAGMENT_TYPE && null === newChild.key && (newChild = newChild.props.children);
      if ("object" === typeof newChild && null !== newChild) {
        switch (newChild.$$typeof) {
          case REACT_ELEMENT_TYPE:
            a: {
              for (var key = newChild.key; null !== currentFirstChild; ) {
                if (currentFirstChild.key === key) {
                  key = newChild.type;
                  if (key === REACT_FRAGMENT_TYPE) {
                    if (7 === currentFirstChild.tag) {
                      deleteRemainingChildren(
                        returnFiber,
                        currentFirstChild.sibling
                      );
                      lanes = useFiber(
                        currentFirstChild,
                        newChild.props.children
                      );
                      lanes.return = returnFiber;
                      returnFiber = lanes;
                      break a;
                    }
                  } else if (currentFirstChild.elementType === key || "object" === typeof key && null !== key && key.$$typeof === REACT_LAZY_TYPE && resolveLazy(key) === currentFirstChild.type) {
                    deleteRemainingChildren(
                      returnFiber,
                      currentFirstChild.sibling
                    );
                    lanes = useFiber(currentFirstChild, newChild.props);
                    coerceRef(lanes, newChild);
                    lanes.return = returnFiber;
                    returnFiber = lanes;
                    break a;
                  }
                  deleteRemainingChildren(returnFiber, currentFirstChild);
                  break;
                } else deleteChild(returnFiber, currentFirstChild);
                currentFirstChild = currentFirstChild.sibling;
              }
              newChild.type === REACT_FRAGMENT_TYPE ? (lanes = createFiberFromFragment(
                newChild.props.children,
                returnFiber.mode,
                lanes,
                newChild.key
              ), lanes.return = returnFiber, returnFiber = lanes) : (lanes = createFiberFromTypeAndProps(
                newChild.type,
                newChild.key,
                newChild.props,
                null,
                returnFiber.mode,
                lanes
              ), coerceRef(lanes, newChild), lanes.return = returnFiber, returnFiber = lanes);
            }
            return placeSingleChild(returnFiber);
          case REACT_PORTAL_TYPE:
            a: {
              for (key = newChild.key; null !== currentFirstChild; ) {
                if (currentFirstChild.key === key)
                  if (4 === currentFirstChild.tag && currentFirstChild.stateNode.containerInfo === newChild.containerInfo && currentFirstChild.stateNode.implementation === newChild.implementation) {
                    deleteRemainingChildren(
                      returnFiber,
                      currentFirstChild.sibling
                    );
                    lanes = useFiber(currentFirstChild, newChild.children || []);
                    lanes.return = returnFiber;
                    returnFiber = lanes;
                    break a;
                  } else {
                    deleteRemainingChildren(returnFiber, currentFirstChild);
                    break;
                  }
                else deleteChild(returnFiber, currentFirstChild);
                currentFirstChild = currentFirstChild.sibling;
              }
              lanes = createFiberFromPortal(newChild, returnFiber.mode, lanes);
              lanes.return = returnFiber;
              returnFiber = lanes;
            }
            return placeSingleChild(returnFiber);
          case REACT_LAZY_TYPE:
            return newChild = resolveLazy(newChild), reconcileChildFibersImpl(
              returnFiber,
              currentFirstChild,
              newChild,
              lanes
            );
        }
        if (isArrayImpl(newChild))
          return reconcileChildrenArray(
            returnFiber,
            currentFirstChild,
            newChild,
            lanes
          );
        if (getIteratorFn(newChild)) {
          key = getIteratorFn(newChild);
          if ("function" !== typeof key) throw Error(formatProdErrorMessage(150));
          newChild = key.call(newChild);
          return reconcileChildrenIterator(
            returnFiber,
            currentFirstChild,
            newChild,
            lanes
          );
        }
        if ("function" === typeof newChild.then)
          return reconcileChildFibersImpl(
            returnFiber,
            currentFirstChild,
            unwrapThenable(newChild),
            lanes
          );
        if (newChild.$$typeof === REACT_CONTEXT_TYPE)
          return reconcileChildFibersImpl(
            returnFiber,
            currentFirstChild,
            readContextDuringReconciliation(returnFiber, newChild),
            lanes
          );
        throwOnInvalidObjectTypeImpl(returnFiber, newChild);
      }
      return "string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild ? (newChild = "" + newChild, null !== currentFirstChild && 6 === currentFirstChild.tag ? (deleteRemainingChildren(returnFiber, currentFirstChild.sibling), lanes = useFiber(currentFirstChild, newChild), lanes.return = returnFiber, returnFiber = lanes) : (deleteRemainingChildren(returnFiber, currentFirstChild), lanes = createFiberFromText(newChild, returnFiber.mode, lanes), lanes.return = returnFiber, returnFiber = lanes), placeSingleChild(returnFiber)) : deleteRemainingChildren(returnFiber, currentFirstChild);
    }
    return function(returnFiber, currentFirstChild, newChild, lanes) {
      try {
        thenableIndexCounter$1 = 0;
        var firstChildFiber = reconcileChildFibersImpl(
          returnFiber,
          currentFirstChild,
          newChild,
          lanes
        );
        thenableState$1 = null;
        return firstChildFiber;
      } catch (x) {
        if (x === SuspenseException || x === SuspenseActionException) throw x;
        var fiber = createFiberImplClass(29, x, null, returnFiber.mode);
        fiber.lanes = lanes;
        fiber.return = returnFiber;
        return fiber;
      } finally {
      }
    };
  }
  var reconcileChildFibers = createChildReconciler(true), mountChildFibers = createChildReconciler(false), hasForceUpdate = false;
  function initializeUpdateQueue(fiber) {
    fiber.updateQueue = {
      baseState: fiber.memoizedState,
      firstBaseUpdate: null,
      lastBaseUpdate: null,
      shared: { pending: null, lanes: 0, hiddenCallbacks: null },
      callbacks: null
    };
  }
  function cloneUpdateQueue(current, workInProgress2) {
    current = current.updateQueue;
    workInProgress2.updateQueue === current && (workInProgress2.updateQueue = {
      baseState: current.baseState,
      firstBaseUpdate: current.firstBaseUpdate,
      lastBaseUpdate: current.lastBaseUpdate,
      shared: current.shared,
      callbacks: null
    });
  }
  function createUpdate(lane) {
    return { lane, tag: 0, payload: null, callback: null, next: null };
  }
  function enqueueUpdate(fiber, update, lane) {
    var updateQueue = fiber.updateQueue;
    if (null === updateQueue) return null;
    updateQueue = updateQueue.shared;
    if (0 !== (executionContext & 2)) {
      var pending = updateQueue.pending;
      null === pending ? update.next = update : (update.next = pending.next, pending.next = update);
      updateQueue.pending = update;
      update = getRootForUpdatedFiber(fiber);
      markUpdateLaneFromFiberToRoot(fiber, null, lane);
      return update;
    }
    enqueueUpdate$1(fiber, updateQueue, update, lane);
    return getRootForUpdatedFiber(fiber);
  }
  function entangleTransitions(root3, fiber, lane) {
    fiber = fiber.updateQueue;
    if (null !== fiber && (fiber = fiber.shared, 0 !== (lane & 4194048))) {
      var queueLanes = fiber.lanes;
      queueLanes &= root3.pendingLanes;
      lane |= queueLanes;
      fiber.lanes = lane;
      markRootEntangled(root3, lane);
    }
  }
  function enqueueCapturedUpdate(workInProgress2, capturedUpdate) {
    var queue = workInProgress2.updateQueue, current = workInProgress2.alternate;
    if (null !== current && (current = current.updateQueue, queue === current)) {
      var newFirst = null, newLast = null;
      queue = queue.firstBaseUpdate;
      if (null !== queue) {
        do {
          var clone = {
            lane: queue.lane,
            tag: queue.tag,
            payload: queue.payload,
            callback: null,
            next: null
          };
          null === newLast ? newFirst = newLast = clone : newLast = newLast.next = clone;
          queue = queue.next;
        } while (null !== queue);
        null === newLast ? newFirst = newLast = capturedUpdate : newLast = newLast.next = capturedUpdate;
      } else newFirst = newLast = capturedUpdate;
      queue = {
        baseState: current.baseState,
        firstBaseUpdate: newFirst,
        lastBaseUpdate: newLast,
        shared: current.shared,
        callbacks: current.callbacks
      };
      workInProgress2.updateQueue = queue;
      return;
    }
    workInProgress2 = queue.lastBaseUpdate;
    null === workInProgress2 ? queue.firstBaseUpdate = capturedUpdate : workInProgress2.next = capturedUpdate;
    queue.lastBaseUpdate = capturedUpdate;
  }
  var didReadFromEntangledAsyncAction = false;
  function suspendIfUpdateReadFromEntangledAsyncAction() {
    if (didReadFromEntangledAsyncAction) {
      var entangledActionThenable = currentEntangledActionThenable;
      if (null !== entangledActionThenable) throw entangledActionThenable;
    }
  }
  function processUpdateQueue(workInProgress$jscomp$0, props, instance$jscomp$0, renderLanes2) {
    didReadFromEntangledAsyncAction = false;
    var queue = workInProgress$jscomp$0.updateQueue;
    hasForceUpdate = false;
    var firstBaseUpdate = queue.firstBaseUpdate, lastBaseUpdate = queue.lastBaseUpdate, pendingQueue = queue.shared.pending;
    if (null !== pendingQueue) {
      queue.shared.pending = null;
      var lastPendingUpdate = pendingQueue, firstPendingUpdate = lastPendingUpdate.next;
      lastPendingUpdate.next = null;
      null === lastBaseUpdate ? firstBaseUpdate = firstPendingUpdate : lastBaseUpdate.next = firstPendingUpdate;
      lastBaseUpdate = lastPendingUpdate;
      var current = workInProgress$jscomp$0.alternate;
      null !== current && (current = current.updateQueue, pendingQueue = current.lastBaseUpdate, pendingQueue !== lastBaseUpdate && (null === pendingQueue ? current.firstBaseUpdate = firstPendingUpdate : pendingQueue.next = firstPendingUpdate, current.lastBaseUpdate = lastPendingUpdate));
    }
    if (null !== firstBaseUpdate) {
      var newState = queue.baseState;
      lastBaseUpdate = 0;
      current = firstPendingUpdate = lastPendingUpdate = null;
      pendingQueue = firstBaseUpdate;
      do {
        var updateLane = pendingQueue.lane & -536870913, isHiddenUpdate = updateLane !== pendingQueue.lane;
        if (isHiddenUpdate ? (workInProgressRootRenderLanes & updateLane) === updateLane : (renderLanes2 & updateLane) === updateLane) {
          0 !== updateLane && updateLane === currentEntangledLane && (didReadFromEntangledAsyncAction = true);
          null !== current && (current = current.next = {
            lane: 0,
            tag: pendingQueue.tag,
            payload: pendingQueue.payload,
            callback: null,
            next: null
          });
          a: {
            var workInProgress2 = workInProgress$jscomp$0, update = pendingQueue;
            updateLane = props;
            var instance = instance$jscomp$0;
            switch (update.tag) {
              case 1:
                workInProgress2 = update.payload;
                if ("function" === typeof workInProgress2) {
                  newState = workInProgress2.call(instance, newState, updateLane);
                  break a;
                }
                newState = workInProgress2;
                break a;
              case 3:
                workInProgress2.flags = workInProgress2.flags & -65537 | 128;
              case 0:
                workInProgress2 = update.payload;
                updateLane = "function" === typeof workInProgress2 ? workInProgress2.call(instance, newState, updateLane) : workInProgress2;
                if (null === updateLane || void 0 === updateLane) break a;
                newState = assign({}, newState, updateLane);
                break a;
              case 2:
                hasForceUpdate = true;
            }
          }
          updateLane = pendingQueue.callback;
          null !== updateLane && (workInProgress$jscomp$0.flags |= 64, isHiddenUpdate && (workInProgress$jscomp$0.flags |= 8192), isHiddenUpdate = queue.callbacks, null === isHiddenUpdate ? queue.callbacks = [updateLane] : isHiddenUpdate.push(updateLane));
        } else
          isHiddenUpdate = {
            lane: updateLane,
            tag: pendingQueue.tag,
            payload: pendingQueue.payload,
            callback: pendingQueue.callback,
            next: null
          }, null === current ? (firstPendingUpdate = current = isHiddenUpdate, lastPendingUpdate = newState) : current = current.next = isHiddenUpdate, lastBaseUpdate |= updateLane;
        pendingQueue = pendingQueue.next;
        if (null === pendingQueue)
          if (pendingQueue = queue.shared.pending, null === pendingQueue)
            break;
          else
            isHiddenUpdate = pendingQueue, pendingQueue = isHiddenUpdate.next, isHiddenUpdate.next = null, queue.lastBaseUpdate = isHiddenUpdate, queue.shared.pending = null;
      } while (1);
      null === current && (lastPendingUpdate = newState);
      queue.baseState = lastPendingUpdate;
      queue.firstBaseUpdate = firstPendingUpdate;
      queue.lastBaseUpdate = current;
      null === firstBaseUpdate && (queue.shared.lanes = 0);
      workInProgressRootSkippedLanes |= lastBaseUpdate;
      workInProgress$jscomp$0.lanes = lastBaseUpdate;
      workInProgress$jscomp$0.memoizedState = newState;
    }
  }
  function callCallback(callback, context) {
    if ("function" !== typeof callback)
      throw Error(formatProdErrorMessage(191, callback));
    callback.call(context);
  }
  function commitCallbacks(updateQueue, context) {
    var callbacks = updateQueue.callbacks;
    if (null !== callbacks)
      for (updateQueue.callbacks = null, updateQueue = 0; updateQueue < callbacks.length; updateQueue++)
        callCallback(callbacks[updateQueue], context);
  }
  var currentTreeHiddenStackCursor = createCursor(null), prevEntangledRenderLanesCursor = createCursor(0);
  function pushHiddenContext(fiber, context) {
    fiber = entangledRenderLanes;
    push2(prevEntangledRenderLanesCursor, fiber);
    push2(currentTreeHiddenStackCursor, context);
    entangledRenderLanes = fiber | context.baseLanes;
  }
  function reuseHiddenContextOnStack() {
    push2(prevEntangledRenderLanesCursor, entangledRenderLanes);
    push2(currentTreeHiddenStackCursor, currentTreeHiddenStackCursor.current);
  }
  function popHiddenContext() {
    entangledRenderLanes = prevEntangledRenderLanesCursor.current;
    pop(currentTreeHiddenStackCursor);
    pop(prevEntangledRenderLanesCursor);
  }
  var suspenseHandlerStackCursor = createCursor(null), shellBoundary = null;
  function pushPrimaryTreeSuspenseHandler(handler) {
    var current = handler.alternate;
    push2(suspenseStackCursor, suspenseStackCursor.current & 1);
    push2(suspenseHandlerStackCursor, handler);
    null === shellBoundary && (null === current || null !== currentTreeHiddenStackCursor.current ? shellBoundary = handler : null !== current.memoizedState && (shellBoundary = handler));
  }
  function pushDehydratedActivitySuspenseHandler(fiber) {
    push2(suspenseStackCursor, suspenseStackCursor.current);
    push2(suspenseHandlerStackCursor, fiber);
    null === shellBoundary && (shellBoundary = fiber);
  }
  function pushOffscreenSuspenseHandler(fiber) {
    22 === fiber.tag ? (push2(suspenseStackCursor, suspenseStackCursor.current), push2(suspenseHandlerStackCursor, fiber), null === shellBoundary && (shellBoundary = fiber)) : reuseSuspenseHandlerOnStack();
  }
  function reuseSuspenseHandlerOnStack() {
    push2(suspenseStackCursor, suspenseStackCursor.current);
    push2(suspenseHandlerStackCursor, suspenseHandlerStackCursor.current);
  }
  function popSuspenseHandler(fiber) {
    pop(suspenseHandlerStackCursor);
    shellBoundary === fiber && (shellBoundary = null);
    pop(suspenseStackCursor);
  }
  var suspenseStackCursor = createCursor(0);
  function findFirstSuspended(row) {
    for (var node2 = row; null !== node2; ) {
      if (13 === node2.tag) {
        var state = node2.memoizedState;
        if (null !== state && (state = state.dehydrated, null === state || isSuspenseInstancePending(state) || isSuspenseInstanceFallback(state)))
          return node2;
      } else if (19 === node2.tag && ("forwards" === node2.memoizedProps.revealOrder || "backwards" === node2.memoizedProps.revealOrder || "unstable_legacy-backwards" === node2.memoizedProps.revealOrder || "together" === node2.memoizedProps.revealOrder)) {
        if (0 !== (node2.flags & 128)) return node2;
      } else if (null !== node2.child) {
        node2.child.return = node2;
        node2 = node2.child;
        continue;
      }
      if (node2 === row) break;
      for (; null === node2.sibling; ) {
        if (null === node2.return || node2.return === row) return null;
        node2 = node2.return;
      }
      node2.sibling.return = node2.return;
      node2 = node2.sibling;
    }
    return null;
  }
  var renderLanes = 0, currentlyRenderingFiber = null, currentHook = null, workInProgressHook = null, didScheduleRenderPhaseUpdate = false, didScheduleRenderPhaseUpdateDuringThisPass = false, shouldDoubleInvokeUserFnsInHooksDEV = false, localIdCounter = 0, thenableIndexCounter = 0, thenableState = null, globalClientIdCounter = 0;
  function throwInvalidHookError() {
    throw Error(formatProdErrorMessage(321));
  }
  function areHookInputsEqual(nextDeps, prevDeps) {
    if (null === prevDeps) return false;
    for (var i = 0; i < prevDeps.length && i < nextDeps.length; i++)
      if (!objectIs(nextDeps[i], prevDeps[i])) return false;
    return true;
  }
  function renderWithHooks(current, workInProgress2, Component, props, secondArg, nextRenderLanes) {
    renderLanes = nextRenderLanes;
    currentlyRenderingFiber = workInProgress2;
    workInProgress2.memoizedState = null;
    workInProgress2.updateQueue = null;
    workInProgress2.lanes = 0;
    ReactSharedInternals.H = null === current || null === current.memoizedState ? HooksDispatcherOnMount : HooksDispatcherOnUpdate;
    shouldDoubleInvokeUserFnsInHooksDEV = false;
    nextRenderLanes = Component(props, secondArg);
    shouldDoubleInvokeUserFnsInHooksDEV = false;
    didScheduleRenderPhaseUpdateDuringThisPass && (nextRenderLanes = renderWithHooksAgain(
      workInProgress2,
      Component,
      props,
      secondArg
    ));
    finishRenderingHooks(current);
    return nextRenderLanes;
  }
  function finishRenderingHooks(current) {
    ReactSharedInternals.H = ContextOnlyDispatcher;
    var didRenderTooFewHooks = null !== currentHook && null !== currentHook.next;
    renderLanes = 0;
    workInProgressHook = currentHook = currentlyRenderingFiber = null;
    didScheduleRenderPhaseUpdate = false;
    thenableIndexCounter = 0;
    thenableState = null;
    if (didRenderTooFewHooks) throw Error(formatProdErrorMessage(300));
    null === current || didReceiveUpdate || (current = current.dependencies, null !== current && checkIfContextChanged(current) && (didReceiveUpdate = true));
  }
  function renderWithHooksAgain(workInProgress2, Component, props, secondArg) {
    currentlyRenderingFiber = workInProgress2;
    var numberOfReRenders = 0;
    do {
      didScheduleRenderPhaseUpdateDuringThisPass && (thenableState = null);
      thenableIndexCounter = 0;
      didScheduleRenderPhaseUpdateDuringThisPass = false;
      if (25 <= numberOfReRenders) throw Error(formatProdErrorMessage(301));
      numberOfReRenders += 1;
      workInProgressHook = currentHook = null;
      if (null != workInProgress2.updateQueue) {
        var children = workInProgress2.updateQueue;
        children.lastEffect = null;
        children.events = null;
        children.stores = null;
        null != children.memoCache && (children.memoCache.index = 0);
      }
      ReactSharedInternals.H = HooksDispatcherOnRerender;
      children = Component(props, secondArg);
    } while (didScheduleRenderPhaseUpdateDuringThisPass);
    return children;
  }
  function TransitionAwareHostComponent() {
    var dispatcher = ReactSharedInternals.H, maybeThenable = dispatcher.useState()[0];
    maybeThenable = "function" === typeof maybeThenable.then ? useThenable(maybeThenable) : maybeThenable;
    dispatcher = dispatcher.useState()[0];
    (null !== currentHook ? currentHook.memoizedState : null) !== dispatcher && (currentlyRenderingFiber.flags |= 1024);
    return maybeThenable;
  }
  function checkDidRenderIdHook() {
    var didRenderIdHook = 0 !== localIdCounter;
    localIdCounter = 0;
    return didRenderIdHook;
  }
  function bailoutHooks(current, workInProgress2, lanes) {
    workInProgress2.updateQueue = current.updateQueue;
    workInProgress2.flags &= -2053;
    current.lanes &= ~lanes;
  }
  function resetHooksOnUnwind(workInProgress2) {
    if (didScheduleRenderPhaseUpdate) {
      for (workInProgress2 = workInProgress2.memoizedState; null !== workInProgress2; ) {
        var queue = workInProgress2.queue;
        null !== queue && (queue.pending = null);
        workInProgress2 = workInProgress2.next;
      }
      didScheduleRenderPhaseUpdate = false;
    }
    renderLanes = 0;
    workInProgressHook = currentHook = currentlyRenderingFiber = null;
    didScheduleRenderPhaseUpdateDuringThisPass = false;
    thenableIndexCounter = localIdCounter = 0;
    thenableState = null;
  }
  function mountWorkInProgressHook() {
    var hook = {
      memoizedState: null,
      baseState: null,
      baseQueue: null,
      queue: null,
      next: null
    };
    null === workInProgressHook ? currentlyRenderingFiber.memoizedState = workInProgressHook = hook : workInProgressHook = workInProgressHook.next = hook;
    return workInProgressHook;
  }
  function updateWorkInProgressHook() {
    if (null === currentHook) {
      var nextCurrentHook = currentlyRenderingFiber.alternate;
      nextCurrentHook = null !== nextCurrentHook ? nextCurrentHook.memoizedState : null;
    } else nextCurrentHook = currentHook.next;
    var nextWorkInProgressHook = null === workInProgressHook ? currentlyRenderingFiber.memoizedState : workInProgressHook.next;
    if (null !== nextWorkInProgressHook)
      workInProgressHook = nextWorkInProgressHook, currentHook = nextCurrentHook;
    else {
      if (null === nextCurrentHook) {
        if (null === currentlyRenderingFiber.alternate)
          throw Error(formatProdErrorMessage(467));
        throw Error(formatProdErrorMessage(310));
      }
      currentHook = nextCurrentHook;
      nextCurrentHook = {
        memoizedState: currentHook.memoizedState,
        baseState: currentHook.baseState,
        baseQueue: currentHook.baseQueue,
        queue: currentHook.queue,
        next: null
      };
      null === workInProgressHook ? currentlyRenderingFiber.memoizedState = workInProgressHook = nextCurrentHook : workInProgressHook = workInProgressHook.next = nextCurrentHook;
    }
    return workInProgressHook;
  }
  function createFunctionComponentUpdateQueue() {
    return { lastEffect: null, events: null, stores: null, memoCache: null };
  }
  function useThenable(thenable) {
    var index3 = thenableIndexCounter;
    thenableIndexCounter += 1;
    null === thenableState && (thenableState = []);
    thenable = trackUsedThenable(thenableState, thenable, index3);
    index3 = currentlyRenderingFiber;
    null === (null === workInProgressHook ? index3.memoizedState : workInProgressHook.next) && (index3 = index3.alternate, ReactSharedInternals.H = null === index3 || null === index3.memoizedState ? HooksDispatcherOnMount : HooksDispatcherOnUpdate);
    return thenable;
  }
  function use(usable) {
    if (null !== usable && "object" === typeof usable) {
      if ("function" === typeof usable.then) return useThenable(usable);
      if (usable.$$typeof === REACT_CONTEXT_TYPE) return readContext(usable);
    }
    throw Error(formatProdErrorMessage(438, String(usable)));
  }
  function useMemoCache(size) {
    var memoCache = null, updateQueue = currentlyRenderingFiber.updateQueue;
    null !== updateQueue && (memoCache = updateQueue.memoCache);
    if (null == memoCache) {
      var current = currentlyRenderingFiber.alternate;
      null !== current && (current = current.updateQueue, null !== current && (current = current.memoCache, null != current && (memoCache = {
        data: current.data.map(function(array) {
          return array.slice();
        }),
        index: 0
      })));
    }
    null == memoCache && (memoCache = { data: [], index: 0 });
    null === updateQueue && (updateQueue = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = updateQueue);
    updateQueue.memoCache = memoCache;
    updateQueue = memoCache.data[memoCache.index];
    if (void 0 === updateQueue)
      for (updateQueue = memoCache.data[memoCache.index] = Array(size), current = 0; current < size; current++)
        updateQueue[current] = REACT_MEMO_CACHE_SENTINEL;
    memoCache.index++;
    return updateQueue;
  }
  function basicStateReducer(state, action) {
    return "function" === typeof action ? action(state) : action;
  }
  function updateReducer(reducer) {
    var hook = updateWorkInProgressHook();
    return updateReducerImpl(hook, currentHook, reducer);
  }
  function updateReducerImpl(hook, current, reducer) {
    var queue = hook.queue;
    if (null === queue) throw Error(formatProdErrorMessage(311));
    queue.lastRenderedReducer = reducer;
    var baseQueue = hook.baseQueue, pendingQueue = queue.pending;
    if (null !== pendingQueue) {
      if (null !== baseQueue) {
        var baseFirst = baseQueue.next;
        baseQueue.next = pendingQueue.next;
        pendingQueue.next = baseFirst;
      }
      current.baseQueue = baseQueue = pendingQueue;
      queue.pending = null;
    }
    pendingQueue = hook.baseState;
    if (null === baseQueue) hook.memoizedState = pendingQueue;
    else {
      current = baseQueue.next;
      var newBaseQueueFirst = baseFirst = null, newBaseQueueLast = null, update = current, didReadFromEntangledAsyncAction$60 = false;
      do {
        var updateLane = update.lane & -536870913;
        if (updateLane !== update.lane ? (workInProgressRootRenderLanes & updateLane) === updateLane : (renderLanes & updateLane) === updateLane) {
          var revertLane = update.revertLane;
          if (0 === revertLane)
            null !== newBaseQueueLast && (newBaseQueueLast = newBaseQueueLast.next = {
              lane: 0,
              revertLane: 0,
              gesture: null,
              action: update.action,
              hasEagerState: update.hasEagerState,
              eagerState: update.eagerState,
              next: null
            }), updateLane === currentEntangledLane && (didReadFromEntangledAsyncAction$60 = true);
          else if ((renderLanes & revertLane) === revertLane) {
            update = update.next;
            revertLane === currentEntangledLane && (didReadFromEntangledAsyncAction$60 = true);
            continue;
          } else
            updateLane = {
              lane: 0,
              revertLane: update.revertLane,
              gesture: null,
              action: update.action,
              hasEagerState: update.hasEagerState,
              eagerState: update.eagerState,
              next: null
            }, null === newBaseQueueLast ? (newBaseQueueFirst = newBaseQueueLast = updateLane, baseFirst = pendingQueue) : newBaseQueueLast = newBaseQueueLast.next = updateLane, currentlyRenderingFiber.lanes |= revertLane, workInProgressRootSkippedLanes |= revertLane;
          updateLane = update.action;
          shouldDoubleInvokeUserFnsInHooksDEV && reducer(pendingQueue, updateLane);
          pendingQueue = update.hasEagerState ? update.eagerState : reducer(pendingQueue, updateLane);
        } else
          revertLane = {
            lane: updateLane,
            revertLane: update.revertLane,
            gesture: update.gesture,
            action: update.action,
            hasEagerState: update.hasEagerState,
            eagerState: update.eagerState,
            next: null
          }, null === newBaseQueueLast ? (newBaseQueueFirst = newBaseQueueLast = revertLane, baseFirst = pendingQueue) : newBaseQueueLast = newBaseQueueLast.next = revertLane, currentlyRenderingFiber.lanes |= updateLane, workInProgressRootSkippedLanes |= updateLane;
        update = update.next;
      } while (null !== update && update !== current);
      null === newBaseQueueLast ? baseFirst = pendingQueue : newBaseQueueLast.next = newBaseQueueFirst;
      if (!objectIs(pendingQueue, hook.memoizedState) && (didReceiveUpdate = true, didReadFromEntangledAsyncAction$60 && (reducer = currentEntangledActionThenable, null !== reducer)))
        throw reducer;
      hook.memoizedState = pendingQueue;
      hook.baseState = baseFirst;
      hook.baseQueue = newBaseQueueLast;
      queue.lastRenderedState = pendingQueue;
    }
    null === baseQueue && (queue.lanes = 0);
    return [hook.memoizedState, queue.dispatch];
  }
  function rerenderReducer(reducer) {
    var hook = updateWorkInProgressHook(), queue = hook.queue;
    if (null === queue) throw Error(formatProdErrorMessage(311));
    queue.lastRenderedReducer = reducer;
    var dispatch = queue.dispatch, lastRenderPhaseUpdate = queue.pending, newState = hook.memoizedState;
    if (null !== lastRenderPhaseUpdate) {
      queue.pending = null;
      var update = lastRenderPhaseUpdate = lastRenderPhaseUpdate.next;
      do
        newState = reducer(newState, update.action), update = update.next;
      while (update !== lastRenderPhaseUpdate);
      objectIs(newState, hook.memoizedState) || (didReceiveUpdate = true);
      hook.memoizedState = newState;
      null === hook.baseQueue && (hook.baseState = newState);
      queue.lastRenderedState = newState;
    }
    return [newState, dispatch];
  }
  function updateSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
    var fiber = currentlyRenderingFiber, hook = updateWorkInProgressHook(), isHydrating$jscomp$0 = isHydrating;
    if (isHydrating$jscomp$0) {
      if (void 0 === getServerSnapshot) throw Error(formatProdErrorMessage(407));
      getServerSnapshot = getServerSnapshot();
    } else getServerSnapshot = getSnapshot();
    var snapshotChanged = !objectIs(
      (currentHook || hook).memoizedState,
      getServerSnapshot
    );
    snapshotChanged && (hook.memoizedState = getServerSnapshot, didReceiveUpdate = true);
    hook = hook.queue;
    updateEffect(subscribeToStore.bind(null, fiber, hook, subscribe), [
      subscribe
    ]);
    if (hook.getSnapshot !== getSnapshot || snapshotChanged || null !== workInProgressHook && workInProgressHook.memoizedState.tag & 1) {
      fiber.flags |= 2048;
      pushSimpleEffect(
        9,
        { destroy: void 0 },
        updateStoreInstance.bind(
          null,
          fiber,
          hook,
          getServerSnapshot,
          getSnapshot
        ),
        null
      );
      if (null === workInProgressRoot) throw Error(formatProdErrorMessage(349));
      isHydrating$jscomp$0 || 0 !== (renderLanes & 127) || pushStoreConsistencyCheck(fiber, getSnapshot, getServerSnapshot);
    }
    return getServerSnapshot;
  }
  function pushStoreConsistencyCheck(fiber, getSnapshot, renderedSnapshot) {
    fiber.flags |= 16384;
    fiber = { getSnapshot, value: renderedSnapshot };
    getSnapshot = currentlyRenderingFiber.updateQueue;
    null === getSnapshot ? (getSnapshot = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = getSnapshot, getSnapshot.stores = [fiber]) : (renderedSnapshot = getSnapshot.stores, null === renderedSnapshot ? getSnapshot.stores = [fiber] : renderedSnapshot.push(fiber));
  }
  function updateStoreInstance(fiber, inst, nextSnapshot, getSnapshot) {
    inst.value = nextSnapshot;
    inst.getSnapshot = getSnapshot;
    checkIfSnapshotChanged(inst) && forceStoreRerender(fiber);
  }
  function subscribeToStore(fiber, inst, subscribe) {
    return subscribe(function() {
      checkIfSnapshotChanged(inst) && forceStoreRerender(fiber);
    });
  }
  function checkIfSnapshotChanged(inst) {
    var latestGetSnapshot = inst.getSnapshot;
    inst = inst.value;
    try {
      var nextValue = latestGetSnapshot();
      return !objectIs(inst, nextValue);
    } catch (error) {
      return true;
    }
  }
  function forceStoreRerender(fiber) {
    var root3 = enqueueConcurrentRenderForLane(fiber, 2);
    null !== root3 && scheduleUpdateOnFiber(root3, fiber, 2);
  }
  function mountStateImpl(initialState) {
    var hook = mountWorkInProgressHook();
    if ("function" === typeof initialState) {
      var initialStateInitializer = initialState;
      initialState = initialStateInitializer();
      if (shouldDoubleInvokeUserFnsInHooksDEV) {
        setIsStrictModeForDevtools(true);
        try {
          initialStateInitializer();
        } finally {
          setIsStrictModeForDevtools(false);
        }
      }
    }
    hook.memoizedState = hook.baseState = initialState;
    hook.queue = {
      pending: null,
      lanes: 0,
      dispatch: null,
      lastRenderedReducer: basicStateReducer,
      lastRenderedState: initialState
    };
    return hook;
  }
  function updateOptimisticImpl(hook, current, passthrough, reducer) {
    hook.baseState = passthrough;
    return updateReducerImpl(
      hook,
      currentHook,
      "function" === typeof reducer ? reducer : basicStateReducer
    );
  }
  function dispatchActionState(fiber, actionQueue, setPendingState, setState, payload2) {
    if (isRenderPhaseUpdate(fiber)) throw Error(formatProdErrorMessage(485));
    fiber = actionQueue.action;
    if (null !== fiber) {
      var actionNode = {
        payload: payload2,
        action: fiber,
        next: null,
        isTransition: true,
        status: "pending",
        value: null,
        reason: null,
        listeners: [],
        then: function(listener) {
          actionNode.listeners.push(listener);
        }
      };
      null !== ReactSharedInternals.T ? setPendingState(true) : actionNode.isTransition = false;
      setState(actionNode);
      setPendingState = actionQueue.pending;
      null === setPendingState ? (actionNode.next = actionQueue.pending = actionNode, runActionStateAction(actionQueue, actionNode)) : (actionNode.next = setPendingState.next, actionQueue.pending = setPendingState.next = actionNode);
    }
  }
  function runActionStateAction(actionQueue, node2) {
    var action = node2.action, payload2 = node2.payload, prevState = actionQueue.state;
    if (node2.isTransition) {
      var prevTransition = ReactSharedInternals.T, currentTransition = {};
      ReactSharedInternals.T = currentTransition;
      try {
        var returnValue = action(prevState, payload2), onStartTransitionFinish = ReactSharedInternals.S;
        null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
        handleActionReturnValue(actionQueue, node2, returnValue);
      } catch (error) {
        onActionError(actionQueue, node2, error);
      } finally {
        null !== prevTransition && null !== currentTransition.types && (prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition;
      }
    } else
      try {
        prevTransition = action(prevState, payload2), handleActionReturnValue(actionQueue, node2, prevTransition);
      } catch (error$66) {
        onActionError(actionQueue, node2, error$66);
      }
  }
  function handleActionReturnValue(actionQueue, node2, returnValue) {
    null !== returnValue && "object" === typeof returnValue && "function" === typeof returnValue.then ? returnValue.then(
      function(nextState) {
        onActionSuccess(actionQueue, node2, nextState);
      },
      function(error) {
        return onActionError(actionQueue, node2, error);
      }
    ) : onActionSuccess(actionQueue, node2, returnValue);
  }
  function onActionSuccess(actionQueue, actionNode, nextState) {
    actionNode.status = "fulfilled";
    actionNode.value = nextState;
    notifyActionListeners(actionNode);
    actionQueue.state = nextState;
    actionNode = actionQueue.pending;
    null !== actionNode && (nextState = actionNode.next, nextState === actionNode ? actionQueue.pending = null : (nextState = nextState.next, actionNode.next = nextState, runActionStateAction(actionQueue, nextState)));
  }
  function onActionError(actionQueue, actionNode, error) {
    var last = actionQueue.pending;
    actionQueue.pending = null;
    if (null !== last) {
      last = last.next;
      do
        actionNode.status = "rejected", actionNode.reason = error, notifyActionListeners(actionNode), actionNode = actionNode.next;
      while (actionNode !== last);
    }
    actionQueue.action = null;
  }
  function notifyActionListeners(actionNode) {
    actionNode = actionNode.listeners;
    for (var i = 0; i < actionNode.length; i++) (0, actionNode[i])();
  }
  function actionStateReducer(oldState, newState) {
    return newState;
  }
  function mountActionState(action, initialStateProp) {
    if (isHydrating) {
      var ssrFormState = workInProgressRoot.formState;
      if (null !== ssrFormState) {
        a: {
          var JSCompiler_inline_result = currentlyRenderingFiber;
          if (isHydrating) {
            if (nextHydratableInstance) {
              b: {
                var JSCompiler_inline_result$jscomp$0 = nextHydratableInstance;
                for (var inRootOrSingleton = rootOrSingletonContext; 8 !== JSCompiler_inline_result$jscomp$0.nodeType; ) {
                  if (!inRootOrSingleton) {
                    JSCompiler_inline_result$jscomp$0 = null;
                    break b;
                  }
                  JSCompiler_inline_result$jscomp$0 = getNextHydratable(
                    JSCompiler_inline_result$jscomp$0.nextSibling
                  );
                  if (null === JSCompiler_inline_result$jscomp$0) {
                    JSCompiler_inline_result$jscomp$0 = null;
                    break b;
                  }
                }
                inRootOrSingleton = JSCompiler_inline_result$jscomp$0.data;
                JSCompiler_inline_result$jscomp$0 = "F!" === inRootOrSingleton || "F" === inRootOrSingleton ? JSCompiler_inline_result$jscomp$0 : null;
              }
              if (JSCompiler_inline_result$jscomp$0) {
                nextHydratableInstance = getNextHydratable(
                  JSCompiler_inline_result$jscomp$0.nextSibling
                );
                JSCompiler_inline_result = "F!" === JSCompiler_inline_result$jscomp$0.data;
                break a;
              }
            }
            throwOnHydrationMismatch(JSCompiler_inline_result);
          }
          JSCompiler_inline_result = false;
        }
        JSCompiler_inline_result && (initialStateProp = ssrFormState[0]);
      }
    }
    ssrFormState = mountWorkInProgressHook();
    ssrFormState.memoizedState = ssrFormState.baseState = initialStateProp;
    JSCompiler_inline_result = {
      pending: null,
      lanes: 0,
      dispatch: null,
      lastRenderedReducer: actionStateReducer,
      lastRenderedState: initialStateProp
    };
    ssrFormState.queue = JSCompiler_inline_result;
    ssrFormState = dispatchSetState.bind(
      null,
      currentlyRenderingFiber,
      JSCompiler_inline_result
    );
    JSCompiler_inline_result.dispatch = ssrFormState;
    JSCompiler_inline_result = mountStateImpl(false);
    inRootOrSingleton = dispatchOptimisticSetState.bind(
      null,
      currentlyRenderingFiber,
      false,
      JSCompiler_inline_result.queue
    );
    JSCompiler_inline_result = mountWorkInProgressHook();
    JSCompiler_inline_result$jscomp$0 = {
      state: initialStateProp,
      dispatch: null,
      action,
      pending: null
    };
    JSCompiler_inline_result.queue = JSCompiler_inline_result$jscomp$0;
    ssrFormState = dispatchActionState.bind(
      null,
      currentlyRenderingFiber,
      JSCompiler_inline_result$jscomp$0,
      inRootOrSingleton,
      ssrFormState
    );
    JSCompiler_inline_result$jscomp$0.dispatch = ssrFormState;
    JSCompiler_inline_result.memoizedState = action;
    return [initialStateProp, ssrFormState, false];
  }
  function updateActionState(action) {
    var stateHook = updateWorkInProgressHook();
    return updateActionStateImpl(stateHook, currentHook, action);
  }
  function updateActionStateImpl(stateHook, currentStateHook, action) {
    currentStateHook = updateReducerImpl(
      stateHook,
      currentStateHook,
      actionStateReducer
    )[0];
    stateHook = updateReducer(basicStateReducer)[0];
    if ("object" === typeof currentStateHook && null !== currentStateHook && "function" === typeof currentStateHook.then)
      try {
        var state = useThenable(currentStateHook);
      } catch (x) {
        if (x === SuspenseException) throw SuspenseActionException;
        throw x;
      }
    else state = currentStateHook;
    currentStateHook = updateWorkInProgressHook();
    var actionQueue = currentStateHook.queue, dispatch = actionQueue.dispatch;
    action !== currentStateHook.memoizedState && (currentlyRenderingFiber.flags |= 2048, pushSimpleEffect(
      9,
      { destroy: void 0 },
      actionStateActionEffect.bind(null, actionQueue, action),
      null
    ));
    return [state, dispatch, stateHook];
  }
  function actionStateActionEffect(actionQueue, action) {
    actionQueue.action = action;
  }
  function rerenderActionState(action) {
    var stateHook = updateWorkInProgressHook(), currentStateHook = currentHook;
    if (null !== currentStateHook)
      return updateActionStateImpl(stateHook, currentStateHook, action);
    updateWorkInProgressHook();
    stateHook = stateHook.memoizedState;
    currentStateHook = updateWorkInProgressHook();
    var dispatch = currentStateHook.queue.dispatch;
    currentStateHook.memoizedState = action;
    return [stateHook, dispatch, false];
  }
  function pushSimpleEffect(tag, inst, create2, deps) {
    tag = { tag, create: create2, deps, inst, next: null };
    inst = currentlyRenderingFiber.updateQueue;
    null === inst && (inst = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = inst);
    create2 = inst.lastEffect;
    null === create2 ? inst.lastEffect = tag.next = tag : (deps = create2.next, create2.next = tag, tag.next = deps, inst.lastEffect = tag);
    return tag;
  }
  function updateRef() {
    return updateWorkInProgressHook().memoizedState;
  }
  function mountEffectImpl(fiberFlags, hookFlags, create2, deps) {
    var hook = mountWorkInProgressHook();
    currentlyRenderingFiber.flags |= fiberFlags;
    hook.memoizedState = pushSimpleEffect(
      1 | hookFlags,
      { destroy: void 0 },
      create2,
      void 0 === deps ? null : deps
    );
  }
  function updateEffectImpl(fiberFlags, hookFlags, create2, deps) {
    var hook = updateWorkInProgressHook();
    deps = void 0 === deps ? null : deps;
    var inst = hook.memoizedState.inst;
    null !== currentHook && null !== deps && areHookInputsEqual(deps, currentHook.memoizedState.deps) ? hook.memoizedState = pushSimpleEffect(hookFlags, inst, create2, deps) : (currentlyRenderingFiber.flags |= fiberFlags, hook.memoizedState = pushSimpleEffect(
      1 | hookFlags,
      inst,
      create2,
      deps
    ));
  }
  function mountEffect(create2, deps) {
    mountEffectImpl(8390656, 8, create2, deps);
  }
  function updateEffect(create2, deps) {
    updateEffectImpl(2048, 8, create2, deps);
  }
  function useEffectEventImpl(payload2) {
    currentlyRenderingFiber.flags |= 4;
    var componentUpdateQueue = currentlyRenderingFiber.updateQueue;
    if (null === componentUpdateQueue)
      componentUpdateQueue = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = componentUpdateQueue, componentUpdateQueue.events = [payload2];
    else {
      var events2 = componentUpdateQueue.events;
      null === events2 ? componentUpdateQueue.events = [payload2] : events2.push(payload2);
    }
  }
  function updateEvent(callback) {
    var ref = updateWorkInProgressHook().memoizedState;
    useEffectEventImpl({ ref, nextImpl: callback });
    return function() {
      if (0 !== (executionContext & 2)) throw Error(formatProdErrorMessage(440));
      return ref.impl.apply(void 0, arguments);
    };
  }
  function updateInsertionEffect(create2, deps) {
    return updateEffectImpl(4, 2, create2, deps);
  }
  function updateLayoutEffect(create2, deps) {
    return updateEffectImpl(4, 4, create2, deps);
  }
  function imperativeHandleEffect(create2, ref) {
    if ("function" === typeof ref) {
      create2 = create2();
      var refCleanup = ref(create2);
      return function() {
        "function" === typeof refCleanup ? refCleanup() : ref(null);
      };
    }
    if (null !== ref && void 0 !== ref)
      return create2 = create2(), ref.current = create2, function() {
        ref.current = null;
      };
  }
  function updateImperativeHandle(ref, create2, deps) {
    deps = null !== deps && void 0 !== deps ? deps.concat([ref]) : null;
    updateEffectImpl(4, 4, imperativeHandleEffect.bind(null, create2, ref), deps);
  }
  function mountDebugValue() {
  }
  function updateCallback(callback, deps) {
    var hook = updateWorkInProgressHook();
    deps = void 0 === deps ? null : deps;
    var prevState = hook.memoizedState;
    if (null !== deps && areHookInputsEqual(deps, prevState[1]))
      return prevState[0];
    hook.memoizedState = [callback, deps];
    return callback;
  }
  function updateMemo(nextCreate, deps) {
    var hook = updateWorkInProgressHook();
    deps = void 0 === deps ? null : deps;
    var prevState = hook.memoizedState;
    if (null !== deps && areHookInputsEqual(deps, prevState[1]))
      return prevState[0];
    prevState = nextCreate();
    if (shouldDoubleInvokeUserFnsInHooksDEV) {
      setIsStrictModeForDevtools(true);
      try {
        nextCreate();
      } finally {
        setIsStrictModeForDevtools(false);
      }
    }
    hook.memoizedState = [prevState, deps];
    return prevState;
  }
  function mountDeferredValueImpl(hook, value, initialValue) {
    if (void 0 === initialValue || 0 !== (renderLanes & 1073741824) && 0 === (workInProgressRootRenderLanes & 261930))
      return hook.memoizedState = value;
    hook.memoizedState = initialValue;
    hook = requestDeferredLane();
    currentlyRenderingFiber.lanes |= hook;
    workInProgressRootSkippedLanes |= hook;
    return initialValue;
  }
  function updateDeferredValueImpl(hook, prevValue, value, initialValue) {
    if (objectIs(value, prevValue)) return value;
    if (null !== currentTreeHiddenStackCursor.current)
      return hook = mountDeferredValueImpl(hook, value, initialValue), objectIs(hook, prevValue) || (didReceiveUpdate = true), hook;
    if (0 === (renderLanes & 42) || 0 !== (renderLanes & 1073741824) && 0 === (workInProgressRootRenderLanes & 261930))
      return didReceiveUpdate = true, hook.memoizedState = value;
    hook = requestDeferredLane();
    currentlyRenderingFiber.lanes |= hook;
    workInProgressRootSkippedLanes |= hook;
    return prevValue;
  }
  function startTransition(fiber, queue, pendingState, finishedState, callback) {
    var previousPriority = ReactDOMSharedInternals.p;
    ReactDOMSharedInternals.p = 0 !== previousPriority && 8 > previousPriority ? previousPriority : 8;
    var prevTransition = ReactSharedInternals.T, currentTransition = {};
    ReactSharedInternals.T = currentTransition;
    dispatchOptimisticSetState(fiber, false, queue, pendingState);
    try {
      var returnValue = callback(), onStartTransitionFinish = ReactSharedInternals.S;
      null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
      if (null !== returnValue && "object" === typeof returnValue && "function" === typeof returnValue.then) {
        var thenableForFinishedState = chainThenableValue(
          returnValue,
          finishedState
        );
        dispatchSetStateInternal(
          fiber,
          queue,
          thenableForFinishedState,
          requestUpdateLane(fiber)
        );
      } else
        dispatchSetStateInternal(
          fiber,
          queue,
          finishedState,
          requestUpdateLane(fiber)
        );
    } catch (error) {
      dispatchSetStateInternal(
        fiber,
        queue,
        { then: function() {
        }, status: "rejected", reason: error },
        requestUpdateLane()
      );
    } finally {
      ReactDOMSharedInternals.p = previousPriority, null !== prevTransition && null !== currentTransition.types && (prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition;
    }
  }
  function noop2() {
  }
  function startHostTransition(formFiber, pendingState, action, formData) {
    if (5 !== formFiber.tag) throw Error(formatProdErrorMessage(476));
    var queue = ensureFormComponentIsStateful(formFiber).queue;
    startTransition(
      formFiber,
      queue,
      pendingState,
      sharedNotPendingObject,
      null === action ? noop2 : function() {
        requestFormReset$1(formFiber);
        return action(formData);
      }
    );
  }
  function ensureFormComponentIsStateful(formFiber) {
    var existingStateHook = formFiber.memoizedState;
    if (null !== existingStateHook) return existingStateHook;
    existingStateHook = {
      memoizedState: sharedNotPendingObject,
      baseState: sharedNotPendingObject,
      baseQueue: null,
      queue: {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: basicStateReducer,
        lastRenderedState: sharedNotPendingObject
      },
      next: null
    };
    var initialResetState = {};
    existingStateHook.next = {
      memoizedState: initialResetState,
      baseState: initialResetState,
      baseQueue: null,
      queue: {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: basicStateReducer,
        lastRenderedState: initialResetState
      },
      next: null
    };
    formFiber.memoizedState = existingStateHook;
    formFiber = formFiber.alternate;
    null !== formFiber && (formFiber.memoizedState = existingStateHook);
    return existingStateHook;
  }
  function requestFormReset$1(formFiber) {
    var stateHook = ensureFormComponentIsStateful(formFiber);
    null === stateHook.next && (stateHook = formFiber.alternate.memoizedState);
    dispatchSetStateInternal(
      formFiber,
      stateHook.next.queue,
      {},
      requestUpdateLane()
    );
  }
  function useHostTransitionStatus() {
    return readContext(HostTransitionContext);
  }
  function updateId() {
    return updateWorkInProgressHook().memoizedState;
  }
  function updateRefresh() {
    return updateWorkInProgressHook().memoizedState;
  }
  function refreshCache(fiber) {
    for (var provider = fiber.return; null !== provider; ) {
      switch (provider.tag) {
        case 24:
        case 3:
          var lane = requestUpdateLane();
          fiber = createUpdate(lane);
          var root$69 = enqueueUpdate(provider, fiber, lane);
          null !== root$69 && (scheduleUpdateOnFiber(root$69, provider, lane), entangleTransitions(root$69, provider, lane));
          provider = { cache: createCache() };
          fiber.payload = provider;
          return;
      }
      provider = provider.return;
    }
  }
  function dispatchReducerAction(fiber, queue, action) {
    var lane = requestUpdateLane();
    action = {
      lane,
      revertLane: 0,
      gesture: null,
      action,
      hasEagerState: false,
      eagerState: null,
      next: null
    };
    isRenderPhaseUpdate(fiber) ? enqueueRenderPhaseUpdate(queue, action) : (action = enqueueConcurrentHookUpdate(fiber, queue, action, lane), null !== action && (scheduleUpdateOnFiber(action, fiber, lane), entangleTransitionUpdate(action, queue, lane)));
  }
  function dispatchSetState(fiber, queue, action) {
    var lane = requestUpdateLane();
    dispatchSetStateInternal(fiber, queue, action, lane);
  }
  function dispatchSetStateInternal(fiber, queue, action, lane) {
    var update = {
      lane,
      revertLane: 0,
      gesture: null,
      action,
      hasEagerState: false,
      eagerState: null,
      next: null
    };
    if (isRenderPhaseUpdate(fiber)) enqueueRenderPhaseUpdate(queue, update);
    else {
      var alternate = fiber.alternate;
      if (0 === fiber.lanes && (null === alternate || 0 === alternate.lanes) && (alternate = queue.lastRenderedReducer, null !== alternate))
        try {
          var currentState = queue.lastRenderedState, eagerState = alternate(currentState, action);
          update.hasEagerState = true;
          update.eagerState = eagerState;
          if (objectIs(eagerState, currentState))
            return enqueueUpdate$1(fiber, queue, update, 0), null === workInProgressRoot && finishQueueingConcurrentUpdates(), false;
        } catch (error) {
        } finally {
        }
      action = enqueueConcurrentHookUpdate(fiber, queue, update, lane);
      if (null !== action)
        return scheduleUpdateOnFiber(action, fiber, lane), entangleTransitionUpdate(action, queue, lane), true;
    }
    return false;
  }
  function dispatchOptimisticSetState(fiber, throwIfDuringRender, queue, action) {
    action = {
      lane: 2,
      revertLane: requestTransitionLane(),
      gesture: null,
      action,
      hasEagerState: false,
      eagerState: null,
      next: null
    };
    if (isRenderPhaseUpdate(fiber)) {
      if (throwIfDuringRender) throw Error(formatProdErrorMessage(479));
    } else
      throwIfDuringRender = enqueueConcurrentHookUpdate(
        fiber,
        queue,
        action,
        2
      ), null !== throwIfDuringRender && scheduleUpdateOnFiber(throwIfDuringRender, fiber, 2);
  }
  function isRenderPhaseUpdate(fiber) {
    var alternate = fiber.alternate;
    return fiber === currentlyRenderingFiber || null !== alternate && alternate === currentlyRenderingFiber;
  }
  function enqueueRenderPhaseUpdate(queue, update) {
    didScheduleRenderPhaseUpdateDuringThisPass = didScheduleRenderPhaseUpdate = true;
    var pending = queue.pending;
    null === pending ? update.next = update : (update.next = pending.next, pending.next = update);
    queue.pending = update;
  }
  function entangleTransitionUpdate(root3, queue, lane) {
    if (0 !== (lane & 4194048)) {
      var queueLanes = queue.lanes;
      queueLanes &= root3.pendingLanes;
      lane |= queueLanes;
      queue.lanes = lane;
      markRootEntangled(root3, lane);
    }
  }
  var ContextOnlyDispatcher = {
    readContext,
    use,
    useCallback: throwInvalidHookError,
    useContext: throwInvalidHookError,
    useEffect: throwInvalidHookError,
    useImperativeHandle: throwInvalidHookError,
    useLayoutEffect: throwInvalidHookError,
    useInsertionEffect: throwInvalidHookError,
    useMemo: throwInvalidHookError,
    useReducer: throwInvalidHookError,
    useRef: throwInvalidHookError,
    useState: throwInvalidHookError,
    useDebugValue: throwInvalidHookError,
    useDeferredValue: throwInvalidHookError,
    useTransition: throwInvalidHookError,
    useSyncExternalStore: throwInvalidHookError,
    useId: throwInvalidHookError,
    useHostTransitionStatus: throwInvalidHookError,
    useFormState: throwInvalidHookError,
    useActionState: throwInvalidHookError,
    useOptimistic: throwInvalidHookError,
    useMemoCache: throwInvalidHookError,
    useCacheRefresh: throwInvalidHookError
  };
  ContextOnlyDispatcher.useEffectEvent = throwInvalidHookError;
  var HooksDispatcherOnMount = {
    readContext,
    use,
    useCallback: function(callback, deps) {
      mountWorkInProgressHook().memoizedState = [
        callback,
        void 0 === deps ? null : deps
      ];
      return callback;
    },
    useContext: readContext,
    useEffect: mountEffect,
    useImperativeHandle: function(ref, create2, deps) {
      deps = null !== deps && void 0 !== deps ? deps.concat([ref]) : null;
      mountEffectImpl(
        4194308,
        4,
        imperativeHandleEffect.bind(null, create2, ref),
        deps
      );
    },
    useLayoutEffect: function(create2, deps) {
      return mountEffectImpl(4194308, 4, create2, deps);
    },
    useInsertionEffect: function(create2, deps) {
      mountEffectImpl(4, 2, create2, deps);
    },
    useMemo: function(nextCreate, deps) {
      var hook = mountWorkInProgressHook();
      deps = void 0 === deps ? null : deps;
      var nextValue = nextCreate();
      if (shouldDoubleInvokeUserFnsInHooksDEV) {
        setIsStrictModeForDevtools(true);
        try {
          nextCreate();
        } finally {
          setIsStrictModeForDevtools(false);
        }
      }
      hook.memoizedState = [nextValue, deps];
      return nextValue;
    },
    useReducer: function(reducer, initialArg, init) {
      var hook = mountWorkInProgressHook();
      if (void 0 !== init) {
        var initialState = init(initialArg);
        if (shouldDoubleInvokeUserFnsInHooksDEV) {
          setIsStrictModeForDevtools(true);
          try {
            init(initialArg);
          } finally {
            setIsStrictModeForDevtools(false);
          }
        }
      } else initialState = initialArg;
      hook.memoizedState = hook.baseState = initialState;
      reducer = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: reducer,
        lastRenderedState: initialState
      };
      hook.queue = reducer;
      reducer = reducer.dispatch = dispatchReducerAction.bind(
        null,
        currentlyRenderingFiber,
        reducer
      );
      return [hook.memoizedState, reducer];
    },
    useRef: function(initialValue) {
      var hook = mountWorkInProgressHook();
      initialValue = { current: initialValue };
      return hook.memoizedState = initialValue;
    },
    useState: function(initialState) {
      initialState = mountStateImpl(initialState);
      var queue = initialState.queue, dispatch = dispatchSetState.bind(null, currentlyRenderingFiber, queue);
      queue.dispatch = dispatch;
      return [initialState.memoizedState, dispatch];
    },
    useDebugValue: mountDebugValue,
    useDeferredValue: function(value, initialValue) {
      var hook = mountWorkInProgressHook();
      return mountDeferredValueImpl(hook, value, initialValue);
    },
    useTransition: function() {
      var stateHook = mountStateImpl(false);
      stateHook = startTransition.bind(
        null,
        currentlyRenderingFiber,
        stateHook.queue,
        true,
        false
      );
      mountWorkInProgressHook().memoizedState = stateHook;
      return [false, stateHook];
    },
    useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
      var fiber = currentlyRenderingFiber, hook = mountWorkInProgressHook();
      if (isHydrating) {
        if (void 0 === getServerSnapshot)
          throw Error(formatProdErrorMessage(407));
        getServerSnapshot = getServerSnapshot();
      } else {
        getServerSnapshot = getSnapshot();
        if (null === workInProgressRoot)
          throw Error(formatProdErrorMessage(349));
        0 !== (workInProgressRootRenderLanes & 127) || pushStoreConsistencyCheck(fiber, getSnapshot, getServerSnapshot);
      }
      hook.memoizedState = getServerSnapshot;
      var inst = { value: getServerSnapshot, getSnapshot };
      hook.queue = inst;
      mountEffect(subscribeToStore.bind(null, fiber, inst, subscribe), [
        subscribe
      ]);
      fiber.flags |= 2048;
      pushSimpleEffect(
        9,
        { destroy: void 0 },
        updateStoreInstance.bind(
          null,
          fiber,
          inst,
          getServerSnapshot,
          getSnapshot
        ),
        null
      );
      return getServerSnapshot;
    },
    useId: function() {
      var hook = mountWorkInProgressHook(), identifierPrefix = workInProgressRoot.identifierPrefix;
      if (isHydrating) {
        var JSCompiler_inline_result = treeContextOverflow;
        var idWithLeadingBit = treeContextId;
        JSCompiler_inline_result = (idWithLeadingBit & ~(1 << 32 - clz32(idWithLeadingBit) - 1)).toString(32) + JSCompiler_inline_result;
        identifierPrefix = "_" + identifierPrefix + "R_" + JSCompiler_inline_result;
        JSCompiler_inline_result = localIdCounter++;
        0 < JSCompiler_inline_result && (identifierPrefix += "H" + JSCompiler_inline_result.toString(32));
        identifierPrefix += "_";
      } else
        JSCompiler_inline_result = globalClientIdCounter++, identifierPrefix = "_" + identifierPrefix + "r_" + JSCompiler_inline_result.toString(32) + "_";
      return hook.memoizedState = identifierPrefix;
    },
    useHostTransitionStatus,
    useFormState: mountActionState,
    useActionState: mountActionState,
    useOptimistic: function(passthrough) {
      var hook = mountWorkInProgressHook();
      hook.memoizedState = hook.baseState = passthrough;
      var queue = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: null,
        lastRenderedState: null
      };
      hook.queue = queue;
      hook = dispatchOptimisticSetState.bind(
        null,
        currentlyRenderingFiber,
        true,
        queue
      );
      queue.dispatch = hook;
      return [passthrough, hook];
    },
    useMemoCache,
    useCacheRefresh: function() {
      return mountWorkInProgressHook().memoizedState = refreshCache.bind(
        null,
        currentlyRenderingFiber
      );
    },
    useEffectEvent: function(callback) {
      var hook = mountWorkInProgressHook(), ref = { impl: callback };
      hook.memoizedState = ref;
      return function() {
        if (0 !== (executionContext & 2))
          throw Error(formatProdErrorMessage(440));
        return ref.impl.apply(void 0, arguments);
      };
    }
  }, HooksDispatcherOnUpdate = {
    readContext,
    use,
    useCallback: updateCallback,
    useContext: readContext,
    useEffect: updateEffect,
    useImperativeHandle: updateImperativeHandle,
    useInsertionEffect: updateInsertionEffect,
    useLayoutEffect: updateLayoutEffect,
    useMemo: updateMemo,
    useReducer: updateReducer,
    useRef: updateRef,
    useState: function() {
      return updateReducer(basicStateReducer);
    },
    useDebugValue: mountDebugValue,
    useDeferredValue: function(value, initialValue) {
      var hook = updateWorkInProgressHook();
      return updateDeferredValueImpl(
        hook,
        currentHook.memoizedState,
        value,
        initialValue
      );
    },
    useTransition: function() {
      var booleanOrThenable = updateReducer(basicStateReducer)[0], start = updateWorkInProgressHook().memoizedState;
      return [
        "boolean" === typeof booleanOrThenable ? booleanOrThenable : useThenable(booleanOrThenable),
        start
      ];
    },
    useSyncExternalStore: updateSyncExternalStore,
    useId: updateId,
    useHostTransitionStatus,
    useFormState: updateActionState,
    useActionState: updateActionState,
    useOptimistic: function(passthrough, reducer) {
      var hook = updateWorkInProgressHook();
      return updateOptimisticImpl(hook, currentHook, passthrough, reducer);
    },
    useMemoCache,
    useCacheRefresh: updateRefresh
  };
  HooksDispatcherOnUpdate.useEffectEvent = updateEvent;
  var HooksDispatcherOnRerender = {
    readContext,
    use,
    useCallback: updateCallback,
    useContext: readContext,
    useEffect: updateEffect,
    useImperativeHandle: updateImperativeHandle,
    useInsertionEffect: updateInsertionEffect,
    useLayoutEffect: updateLayoutEffect,
    useMemo: updateMemo,
    useReducer: rerenderReducer,
    useRef: updateRef,
    useState: function() {
      return rerenderReducer(basicStateReducer);
    },
    useDebugValue: mountDebugValue,
    useDeferredValue: function(value, initialValue) {
      var hook = updateWorkInProgressHook();
      return null === currentHook ? mountDeferredValueImpl(hook, value, initialValue) : updateDeferredValueImpl(
        hook,
        currentHook.memoizedState,
        value,
        initialValue
      );
    },
    useTransition: function() {
      var booleanOrThenable = rerenderReducer(basicStateReducer)[0], start = updateWorkInProgressHook().memoizedState;
      return [
        "boolean" === typeof booleanOrThenable ? booleanOrThenable : useThenable(booleanOrThenable),
        start
      ];
    },
    useSyncExternalStore: updateSyncExternalStore,
    useId: updateId,
    useHostTransitionStatus,
    useFormState: rerenderActionState,
    useActionState: rerenderActionState,
    useOptimistic: function(passthrough, reducer) {
      var hook = updateWorkInProgressHook();
      if (null !== currentHook)
        return updateOptimisticImpl(hook, currentHook, passthrough, reducer);
      hook.baseState = passthrough;
      return [passthrough, hook.queue.dispatch];
    },
    useMemoCache,
    useCacheRefresh: updateRefresh
  };
  HooksDispatcherOnRerender.useEffectEvent = updateEvent;
  function applyDerivedStateFromProps(workInProgress2, ctor, getDerivedStateFromProps, nextProps) {
    ctor = workInProgress2.memoizedState;
    getDerivedStateFromProps = getDerivedStateFromProps(nextProps, ctor);
    getDerivedStateFromProps = null === getDerivedStateFromProps || void 0 === getDerivedStateFromProps ? ctor : assign({}, ctor, getDerivedStateFromProps);
    workInProgress2.memoizedState = getDerivedStateFromProps;
    0 === workInProgress2.lanes && (workInProgress2.updateQueue.baseState = getDerivedStateFromProps);
  }
  var classComponentUpdater = {
    enqueueSetState: function(inst, payload2, callback) {
      inst = inst._reactInternals;
      var lane = requestUpdateLane(), update = createUpdate(lane);
      update.payload = payload2;
      void 0 !== callback && null !== callback && (update.callback = callback);
      payload2 = enqueueUpdate(inst, update, lane);
      null !== payload2 && (scheduleUpdateOnFiber(payload2, inst, lane), entangleTransitions(payload2, inst, lane));
    },
    enqueueReplaceState: function(inst, payload2, callback) {
      inst = inst._reactInternals;
      var lane = requestUpdateLane(), update = createUpdate(lane);
      update.tag = 1;
      update.payload = payload2;
      void 0 !== callback && null !== callback && (update.callback = callback);
      payload2 = enqueueUpdate(inst, update, lane);
      null !== payload2 && (scheduleUpdateOnFiber(payload2, inst, lane), entangleTransitions(payload2, inst, lane));
    },
    enqueueForceUpdate: function(inst, callback) {
      inst = inst._reactInternals;
      var lane = requestUpdateLane(), update = createUpdate(lane);
      update.tag = 2;
      void 0 !== callback && null !== callback && (update.callback = callback);
      callback = enqueueUpdate(inst, update, lane);
      null !== callback && (scheduleUpdateOnFiber(callback, inst, lane), entangleTransitions(callback, inst, lane));
    }
  };
  function checkShouldComponentUpdate(workInProgress2, ctor, oldProps, newProps, oldState, newState, nextContext) {
    workInProgress2 = workInProgress2.stateNode;
    return "function" === typeof workInProgress2.shouldComponentUpdate ? workInProgress2.shouldComponentUpdate(newProps, newState, nextContext) : ctor.prototype && ctor.prototype.isPureReactComponent ? !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState) : true;
  }
  function callComponentWillReceiveProps(workInProgress2, instance, newProps, nextContext) {
    workInProgress2 = instance.state;
    "function" === typeof instance.componentWillReceiveProps && instance.componentWillReceiveProps(newProps, nextContext);
    "function" === typeof instance.UNSAFE_componentWillReceiveProps && instance.UNSAFE_componentWillReceiveProps(newProps, nextContext);
    instance.state !== workInProgress2 && classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
  }
  function resolveClassComponentProps(Component, baseProps) {
    var newProps = baseProps;
    if ("ref" in baseProps) {
      newProps = {};
      for (var propName in baseProps)
        "ref" !== propName && (newProps[propName] = baseProps[propName]);
    }
    if (Component = Component.defaultProps) {
      newProps === baseProps && (newProps = assign({}, newProps));
      for (var propName$73 in Component)
        void 0 === newProps[propName$73] && (newProps[propName$73] = Component[propName$73]);
    }
    return newProps;
  }
  function defaultOnUncaughtError(error) {
    reportGlobalError(error);
  }
  function defaultOnCaughtError(error) {
    console.error(error);
  }
  function defaultOnRecoverableError(error) {
    reportGlobalError(error);
  }
  function logUncaughtError(root3, errorInfo) {
    try {
      var onUncaughtError = root3.onUncaughtError;
      onUncaughtError(errorInfo.value, { componentStack: errorInfo.stack });
    } catch (e$74) {
      setTimeout(function() {
        throw e$74;
      });
    }
  }
  function logCaughtError(root3, boundary, errorInfo) {
    try {
      var onCaughtError = root3.onCaughtError;
      onCaughtError(errorInfo.value, {
        componentStack: errorInfo.stack,
        errorBoundary: 1 === boundary.tag ? boundary.stateNode : null
      });
    } catch (e$75) {
      setTimeout(function() {
        throw e$75;
      });
    }
  }
  function createRootErrorUpdate(root3, errorInfo, lane) {
    lane = createUpdate(lane);
    lane.tag = 3;
    lane.payload = { element: null };
    lane.callback = function() {
      logUncaughtError(root3, errorInfo);
    };
    return lane;
  }
  function createClassErrorUpdate(lane) {
    lane = createUpdate(lane);
    lane.tag = 3;
    return lane;
  }
  function initializeClassErrorUpdate(update, root3, fiber, errorInfo) {
    var getDerivedStateFromError = fiber.type.getDerivedStateFromError;
    if ("function" === typeof getDerivedStateFromError) {
      var error = errorInfo.value;
      update.payload = function() {
        return getDerivedStateFromError(error);
      };
      update.callback = function() {
        logCaughtError(root3, fiber, errorInfo);
      };
    }
    var inst = fiber.stateNode;
    null !== inst && "function" === typeof inst.componentDidCatch && (update.callback = function() {
      logCaughtError(root3, fiber, errorInfo);
      "function" !== typeof getDerivedStateFromError && (null === legacyErrorBoundariesThatAlreadyFailed ? legacyErrorBoundariesThatAlreadyFailed = /* @__PURE__ */ new Set([this]) : legacyErrorBoundariesThatAlreadyFailed.add(this));
      var stack = errorInfo.stack;
      this.componentDidCatch(errorInfo.value, {
        componentStack: null !== stack ? stack : ""
      });
    });
  }
  function throwException(root3, returnFiber, sourceFiber, value, rootRenderLanes) {
    sourceFiber.flags |= 32768;
    if (null !== value && "object" === typeof value && "function" === typeof value.then) {
      returnFiber = sourceFiber.alternate;
      null !== returnFiber && propagateParentContextChanges(
        returnFiber,
        sourceFiber,
        rootRenderLanes,
        true
      );
      sourceFiber = suspenseHandlerStackCursor.current;
      if (null !== sourceFiber) {
        switch (sourceFiber.tag) {
          case 31:
          case 13:
            return null === shellBoundary ? renderDidSuspendDelayIfPossible() : null === sourceFiber.alternate && 0 === workInProgressRootExitStatus && (workInProgressRootExitStatus = 3), sourceFiber.flags &= -257, sourceFiber.flags |= 65536, sourceFiber.lanes = rootRenderLanes, value === noopSuspenseyCommitThenable ? sourceFiber.flags |= 16384 : (returnFiber = sourceFiber.updateQueue, null === returnFiber ? sourceFiber.updateQueue = /* @__PURE__ */ new Set([value]) : returnFiber.add(value), attachPingListener(root3, value, rootRenderLanes)), false;
          case 22:
            return sourceFiber.flags |= 65536, value === noopSuspenseyCommitThenable ? sourceFiber.flags |= 16384 : (returnFiber = sourceFiber.updateQueue, null === returnFiber ? (returnFiber = {
              transitions: null,
              markerInstances: null,
              retryQueue: /* @__PURE__ */ new Set([value])
            }, sourceFiber.updateQueue = returnFiber) : (sourceFiber = returnFiber.retryQueue, null === sourceFiber ? returnFiber.retryQueue = /* @__PURE__ */ new Set([value]) : sourceFiber.add(value)), attachPingListener(root3, value, rootRenderLanes)), false;
        }
        throw Error(formatProdErrorMessage(435, sourceFiber.tag));
      }
      attachPingListener(root3, value, rootRenderLanes);
      renderDidSuspendDelayIfPossible();
      return false;
    }
    if (isHydrating)
      return returnFiber = suspenseHandlerStackCursor.current, null !== returnFiber ? (0 === (returnFiber.flags & 65536) && (returnFiber.flags |= 256), returnFiber.flags |= 65536, returnFiber.lanes = rootRenderLanes, value !== HydrationMismatchException && (root3 = Error(formatProdErrorMessage(422), { cause: value }), queueHydrationError(createCapturedValueAtFiber(root3, sourceFiber)))) : (value !== HydrationMismatchException && (returnFiber = Error(formatProdErrorMessage(423), {
        cause: value
      }), queueHydrationError(
        createCapturedValueAtFiber(returnFiber, sourceFiber)
      )), root3 = root3.current.alternate, root3.flags |= 65536, rootRenderLanes &= -rootRenderLanes, root3.lanes |= rootRenderLanes, value = createCapturedValueAtFiber(value, sourceFiber), rootRenderLanes = createRootErrorUpdate(
        root3.stateNode,
        value,
        rootRenderLanes
      ), enqueueCapturedUpdate(root3, rootRenderLanes), 4 !== workInProgressRootExitStatus && (workInProgressRootExitStatus = 2)), false;
    var wrapperError = Error(formatProdErrorMessage(520), { cause: value });
    wrapperError = createCapturedValueAtFiber(wrapperError, sourceFiber);
    null === workInProgressRootConcurrentErrors ? workInProgressRootConcurrentErrors = [wrapperError] : workInProgressRootConcurrentErrors.push(wrapperError);
    4 !== workInProgressRootExitStatus && (workInProgressRootExitStatus = 2);
    if (null === returnFiber) return true;
    value = createCapturedValueAtFiber(value, sourceFiber);
    sourceFiber = returnFiber;
    do {
      switch (sourceFiber.tag) {
        case 3:
          return sourceFiber.flags |= 65536, root3 = rootRenderLanes & -rootRenderLanes, sourceFiber.lanes |= root3, root3 = createRootErrorUpdate(sourceFiber.stateNode, value, root3), enqueueCapturedUpdate(sourceFiber, root3), false;
        case 1:
          if (returnFiber = sourceFiber.type, wrapperError = sourceFiber.stateNode, 0 === (sourceFiber.flags & 128) && ("function" === typeof returnFiber.getDerivedStateFromError || null !== wrapperError && "function" === typeof wrapperError.componentDidCatch && (null === legacyErrorBoundariesThatAlreadyFailed || !legacyErrorBoundariesThatAlreadyFailed.has(wrapperError))))
            return sourceFiber.flags |= 65536, rootRenderLanes &= -rootRenderLanes, sourceFiber.lanes |= rootRenderLanes, rootRenderLanes = createClassErrorUpdate(rootRenderLanes), initializeClassErrorUpdate(
              rootRenderLanes,
              root3,
              sourceFiber,
              value
            ), enqueueCapturedUpdate(sourceFiber, rootRenderLanes), false;
      }
      sourceFiber = sourceFiber.return;
    } while (null !== sourceFiber);
    return false;
  }
  var SelectiveHydrationException = Error(formatProdErrorMessage(461)), didReceiveUpdate = false;
  function reconcileChildren(current, workInProgress2, nextChildren, renderLanes2) {
    workInProgress2.child = null === current ? mountChildFibers(workInProgress2, null, nextChildren, renderLanes2) : reconcileChildFibers(
      workInProgress2,
      current.child,
      nextChildren,
      renderLanes2
    );
  }
  function updateForwardRef(current, workInProgress2, Component, nextProps, renderLanes2) {
    Component = Component.render;
    var ref = workInProgress2.ref;
    if ("ref" in nextProps) {
      var propsWithoutRef = {};
      for (var key in nextProps)
        "ref" !== key && (propsWithoutRef[key] = nextProps[key]);
    } else propsWithoutRef = nextProps;
    prepareToReadContext(workInProgress2);
    nextProps = renderWithHooks(
      current,
      workInProgress2,
      Component,
      propsWithoutRef,
      ref,
      renderLanes2
    );
    key = checkDidRenderIdHook();
    if (null !== current && !didReceiveUpdate)
      return bailoutHooks(current, workInProgress2, renderLanes2), bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
    isHydrating && key && pushMaterializedTreeId(workInProgress2);
    workInProgress2.flags |= 1;
    reconcileChildren(current, workInProgress2, nextProps, renderLanes2);
    return workInProgress2.child;
  }
  function updateMemoComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
    if (null === current) {
      var type = Component.type;
      if ("function" === typeof type && !shouldConstruct(type) && void 0 === type.defaultProps && null === Component.compare)
        return workInProgress2.tag = 15, workInProgress2.type = type, updateSimpleMemoComponent(
          current,
          workInProgress2,
          type,
          nextProps,
          renderLanes2
        );
      current = createFiberFromTypeAndProps(
        Component.type,
        null,
        nextProps,
        workInProgress2,
        workInProgress2.mode,
        renderLanes2
      );
      current.ref = workInProgress2.ref;
      current.return = workInProgress2;
      return workInProgress2.child = current;
    }
    type = current.child;
    if (!checkScheduledUpdateOrContext(current, renderLanes2)) {
      var prevProps = type.memoizedProps;
      Component = Component.compare;
      Component = null !== Component ? Component : shallowEqual;
      if (Component(prevProps, nextProps) && current.ref === workInProgress2.ref)
        return bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
    }
    workInProgress2.flags |= 1;
    current = createWorkInProgress(type, nextProps);
    current.ref = workInProgress2.ref;
    current.return = workInProgress2;
    return workInProgress2.child = current;
  }
  function updateSimpleMemoComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
    if (null !== current) {
      var prevProps = current.memoizedProps;
      if (shallowEqual(prevProps, nextProps) && current.ref === workInProgress2.ref)
        if (didReceiveUpdate = false, workInProgress2.pendingProps = nextProps = prevProps, checkScheduledUpdateOrContext(current, renderLanes2))
          0 !== (current.flags & 131072) && (didReceiveUpdate = true);
        else
          return workInProgress2.lanes = current.lanes, bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
    }
    return updateFunctionComponent(
      current,
      workInProgress2,
      Component,
      nextProps,
      renderLanes2
    );
  }
  function updateOffscreenComponent(current, workInProgress2, renderLanes2, nextProps) {
    var nextChildren = nextProps.children, prevState = null !== current ? current.memoizedState : null;
    null === current && null === workInProgress2.stateNode && (workInProgress2.stateNode = {
      _visibility: 1,
      _pendingMarkers: null,
      _retryCache: null,
      _transitions: null
    });
    if ("hidden" === nextProps.mode) {
      if (0 !== (workInProgress2.flags & 128)) {
        prevState = null !== prevState ? prevState.baseLanes | renderLanes2 : renderLanes2;
        if (null !== current) {
          nextProps = workInProgress2.child = current.child;
          for (nextChildren = 0; null !== nextProps; )
            nextChildren = nextChildren | nextProps.lanes | nextProps.childLanes, nextProps = nextProps.sibling;
          nextProps = nextChildren & ~prevState;
        } else nextProps = 0, workInProgress2.child = null;
        return deferHiddenOffscreenComponent(
          current,
          workInProgress2,
          prevState,
          renderLanes2,
          nextProps
        );
      }
      if (0 !== (renderLanes2 & 536870912))
        workInProgress2.memoizedState = { baseLanes: 0, cachePool: null }, null !== current && pushTransition(
          workInProgress2,
          null !== prevState ? prevState.cachePool : null
        ), null !== prevState ? pushHiddenContext(workInProgress2, prevState) : reuseHiddenContextOnStack(), pushOffscreenSuspenseHandler(workInProgress2);
      else
        return nextProps = workInProgress2.lanes = 536870912, deferHiddenOffscreenComponent(
          current,
          workInProgress2,
          null !== prevState ? prevState.baseLanes | renderLanes2 : renderLanes2,
          renderLanes2,
          nextProps
        );
    } else
      null !== prevState ? (pushTransition(workInProgress2, prevState.cachePool), pushHiddenContext(workInProgress2, prevState), reuseSuspenseHandlerOnStack(), workInProgress2.memoizedState = null) : (null !== current && pushTransition(workInProgress2, null), reuseHiddenContextOnStack(), reuseSuspenseHandlerOnStack());
    reconcileChildren(current, workInProgress2, nextChildren, renderLanes2);
    return workInProgress2.child;
  }
  function bailoutOffscreenComponent(current, workInProgress2) {
    null !== current && 22 === current.tag || null !== workInProgress2.stateNode || (workInProgress2.stateNode = {
      _visibility: 1,
      _pendingMarkers: null,
      _retryCache: null,
      _transitions: null
    });
    return workInProgress2.sibling;
  }
  function deferHiddenOffscreenComponent(current, workInProgress2, nextBaseLanes, renderLanes2, remainingChildLanes) {
    var JSCompiler_inline_result = peekCacheFromPool();
    JSCompiler_inline_result = null === JSCompiler_inline_result ? null : { parent: CacheContext._currentValue, pool: JSCompiler_inline_result };
    workInProgress2.memoizedState = {
      baseLanes: nextBaseLanes,
      cachePool: JSCompiler_inline_result
    };
    null !== current && pushTransition(workInProgress2, null);
    reuseHiddenContextOnStack();
    pushOffscreenSuspenseHandler(workInProgress2);
    null !== current && propagateParentContextChanges(current, workInProgress2, renderLanes2, true);
    workInProgress2.childLanes = remainingChildLanes;
    return null;
  }
  function mountActivityChildren(workInProgress2, nextProps) {
    nextProps = mountWorkInProgressOffscreenFiber(
      { mode: nextProps.mode, children: nextProps.children },
      workInProgress2.mode
    );
    nextProps.ref = workInProgress2.ref;
    workInProgress2.child = nextProps;
    nextProps.return = workInProgress2;
    return nextProps;
  }
  function retryActivityComponentWithoutHydrating(current, workInProgress2, renderLanes2) {
    reconcileChildFibers(workInProgress2, current.child, null, renderLanes2);
    current = mountActivityChildren(workInProgress2, workInProgress2.pendingProps);
    current.flags |= 2;
    popSuspenseHandler(workInProgress2);
    workInProgress2.memoizedState = null;
    return current;
  }
  function updateActivityComponent(current, workInProgress2, renderLanes2) {
    var nextProps = workInProgress2.pendingProps, didSuspend = 0 !== (workInProgress2.flags & 128);
    workInProgress2.flags &= -129;
    if (null === current) {
      if (isHydrating) {
        if ("hidden" === nextProps.mode)
          return current = mountActivityChildren(workInProgress2, nextProps), workInProgress2.lanes = 536870912, bailoutOffscreenComponent(null, current);
        pushDehydratedActivitySuspenseHandler(workInProgress2);
        (current = nextHydratableInstance) ? (current = canHydrateHydrationBoundary(
          current,
          rootOrSingletonContext
        ), current = null !== current && "&" === current.data ? current : null, null !== current && (workInProgress2.memoizedState = {
          dehydrated: current,
          treeContext: null !== treeContextProvider ? { id: treeContextId, overflow: treeContextOverflow } : null,
          retryLane: 536870912,
          hydrationErrors: null
        }, renderLanes2 = createFiberFromDehydratedFragment(current), renderLanes2.return = workInProgress2, workInProgress2.child = renderLanes2, hydrationParentFiber = workInProgress2, nextHydratableInstance = null)) : current = null;
        if (null === current) throw throwOnHydrationMismatch(workInProgress2);
        workInProgress2.lanes = 536870912;
        return null;
      }
      return mountActivityChildren(workInProgress2, nextProps);
    }
    var prevState = current.memoizedState;
    if (null !== prevState) {
      var dehydrated = prevState.dehydrated;
      pushDehydratedActivitySuspenseHandler(workInProgress2);
      if (didSuspend)
        if (workInProgress2.flags & 256)
          workInProgress2.flags &= -257, workInProgress2 = retryActivityComponentWithoutHydrating(
            current,
            workInProgress2,
            renderLanes2
          );
        else if (null !== workInProgress2.memoizedState)
          workInProgress2.child = current.child, workInProgress2.flags |= 128, workInProgress2 = null;
        else throw Error(formatProdErrorMessage(558));
      else if (didReceiveUpdate || propagateParentContextChanges(current, workInProgress2, renderLanes2, false), didSuspend = 0 !== (renderLanes2 & current.childLanes), didReceiveUpdate || didSuspend) {
        nextProps = workInProgressRoot;
        if (null !== nextProps && (dehydrated = getBumpedLaneForHydration(nextProps, renderLanes2), 0 !== dehydrated && dehydrated !== prevState.retryLane))
          throw prevState.retryLane = dehydrated, enqueueConcurrentRenderForLane(current, dehydrated), scheduleUpdateOnFiber(nextProps, current, dehydrated), SelectiveHydrationException;
        renderDidSuspendDelayIfPossible();
        workInProgress2 = retryActivityComponentWithoutHydrating(
          current,
          workInProgress2,
          renderLanes2
        );
      } else
        current = prevState.treeContext, nextHydratableInstance = getNextHydratable(dehydrated.nextSibling), hydrationParentFiber = workInProgress2, isHydrating = true, hydrationErrors = null, rootOrSingletonContext = false, null !== current && restoreSuspendedTreeContext(workInProgress2, current), workInProgress2 = mountActivityChildren(workInProgress2, nextProps), workInProgress2.flags |= 4096;
      return workInProgress2;
    }
    current = createWorkInProgress(current.child, {
      mode: nextProps.mode,
      children: nextProps.children
    });
    current.ref = workInProgress2.ref;
    workInProgress2.child = current;
    current.return = workInProgress2;
    return current;
  }
  function markRef(current, workInProgress2) {
    var ref = workInProgress2.ref;
    if (null === ref)
      null !== current && null !== current.ref && (workInProgress2.flags |= 4194816);
    else {
      if ("function" !== typeof ref && "object" !== typeof ref)
        throw Error(formatProdErrorMessage(284));
      if (null === current || current.ref !== ref)
        workInProgress2.flags |= 4194816;
    }
  }
  function updateFunctionComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
    prepareToReadContext(workInProgress2);
    Component = renderWithHooks(
      current,
      workInProgress2,
      Component,
      nextProps,
      void 0,
      renderLanes2
    );
    nextProps = checkDidRenderIdHook();
    if (null !== current && !didReceiveUpdate)
      return bailoutHooks(current, workInProgress2, renderLanes2), bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
    isHydrating && nextProps && pushMaterializedTreeId(workInProgress2);
    workInProgress2.flags |= 1;
    reconcileChildren(current, workInProgress2, Component, renderLanes2);
    return workInProgress2.child;
  }
  function replayFunctionComponent(current, workInProgress2, nextProps, Component, secondArg, renderLanes2) {
    prepareToReadContext(workInProgress2);
    workInProgress2.updateQueue = null;
    nextProps = renderWithHooksAgain(
      workInProgress2,
      Component,
      nextProps,
      secondArg
    );
    finishRenderingHooks(current);
    Component = checkDidRenderIdHook();
    if (null !== current && !didReceiveUpdate)
      return bailoutHooks(current, workInProgress2, renderLanes2), bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
    isHydrating && Component && pushMaterializedTreeId(workInProgress2);
    workInProgress2.flags |= 1;
    reconcileChildren(current, workInProgress2, nextProps, renderLanes2);
    return workInProgress2.child;
  }
  function updateClassComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
    prepareToReadContext(workInProgress2);
    if (null === workInProgress2.stateNode) {
      var context = emptyContextObject, contextType = Component.contextType;
      "object" === typeof contextType && null !== contextType && (context = readContext(contextType));
      context = new Component(nextProps, context);
      workInProgress2.memoizedState = null !== context.state && void 0 !== context.state ? context.state : null;
      context.updater = classComponentUpdater;
      workInProgress2.stateNode = context;
      context._reactInternals = workInProgress2;
      context = workInProgress2.stateNode;
      context.props = nextProps;
      context.state = workInProgress2.memoizedState;
      context.refs = {};
      initializeUpdateQueue(workInProgress2);
      contextType = Component.contextType;
      context.context = "object" === typeof contextType && null !== contextType ? readContext(contextType) : emptyContextObject;
      context.state = workInProgress2.memoizedState;
      contextType = Component.getDerivedStateFromProps;
      "function" === typeof contextType && (applyDerivedStateFromProps(
        workInProgress2,
        Component,
        contextType,
        nextProps
      ), context.state = workInProgress2.memoizedState);
      "function" === typeof Component.getDerivedStateFromProps || "function" === typeof context.getSnapshotBeforeUpdate || "function" !== typeof context.UNSAFE_componentWillMount && "function" !== typeof context.componentWillMount || (contextType = context.state, "function" === typeof context.componentWillMount && context.componentWillMount(), "function" === typeof context.UNSAFE_componentWillMount && context.UNSAFE_componentWillMount(), contextType !== context.state && classComponentUpdater.enqueueReplaceState(context, context.state, null), processUpdateQueue(workInProgress2, nextProps, context, renderLanes2), suspendIfUpdateReadFromEntangledAsyncAction(), context.state = workInProgress2.memoizedState);
      "function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308);
      nextProps = true;
    } else if (null === current) {
      context = workInProgress2.stateNode;
      var unresolvedOldProps = workInProgress2.memoizedProps, oldProps = resolveClassComponentProps(Component, unresolvedOldProps);
      context.props = oldProps;
      var oldContext = context.context, contextType$jscomp$0 = Component.contextType;
      contextType = emptyContextObject;
      "object" === typeof contextType$jscomp$0 && null !== contextType$jscomp$0 && (contextType = readContext(contextType$jscomp$0));
      var getDerivedStateFromProps = Component.getDerivedStateFromProps;
      contextType$jscomp$0 = "function" === typeof getDerivedStateFromProps || "function" === typeof context.getSnapshotBeforeUpdate;
      unresolvedOldProps = workInProgress2.pendingProps !== unresolvedOldProps;
      contextType$jscomp$0 || "function" !== typeof context.UNSAFE_componentWillReceiveProps && "function" !== typeof context.componentWillReceiveProps || (unresolvedOldProps || oldContext !== contextType) && callComponentWillReceiveProps(
        workInProgress2,
        context,
        nextProps,
        contextType
      );
      hasForceUpdate = false;
      var oldState = workInProgress2.memoizedState;
      context.state = oldState;
      processUpdateQueue(workInProgress2, nextProps, context, renderLanes2);
      suspendIfUpdateReadFromEntangledAsyncAction();
      oldContext = workInProgress2.memoizedState;
      unresolvedOldProps || oldState !== oldContext || hasForceUpdate ? ("function" === typeof getDerivedStateFromProps && (applyDerivedStateFromProps(
        workInProgress2,
        Component,
        getDerivedStateFromProps,
        nextProps
      ), oldContext = workInProgress2.memoizedState), (oldProps = hasForceUpdate || checkShouldComponentUpdate(
        workInProgress2,
        Component,
        oldProps,
        nextProps,
        oldState,
        oldContext,
        contextType
      )) ? (contextType$jscomp$0 || "function" !== typeof context.UNSAFE_componentWillMount && "function" !== typeof context.componentWillMount || ("function" === typeof context.componentWillMount && context.componentWillMount(), "function" === typeof context.UNSAFE_componentWillMount && context.UNSAFE_componentWillMount()), "function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308)) : ("function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308), workInProgress2.memoizedProps = nextProps, workInProgress2.memoizedState = oldContext), context.props = nextProps, context.state = oldContext, context.context = contextType, nextProps = oldProps) : ("function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308), nextProps = false);
    } else {
      context = workInProgress2.stateNode;
      cloneUpdateQueue(current, workInProgress2);
      contextType = workInProgress2.memoizedProps;
      contextType$jscomp$0 = resolveClassComponentProps(Component, contextType);
      context.props = contextType$jscomp$0;
      getDerivedStateFromProps = workInProgress2.pendingProps;
      oldState = context.context;
      oldContext = Component.contextType;
      oldProps = emptyContextObject;
      "object" === typeof oldContext && null !== oldContext && (oldProps = readContext(oldContext));
      unresolvedOldProps = Component.getDerivedStateFromProps;
      (oldContext = "function" === typeof unresolvedOldProps || "function" === typeof context.getSnapshotBeforeUpdate) || "function" !== typeof context.UNSAFE_componentWillReceiveProps && "function" !== typeof context.componentWillReceiveProps || (contextType !== getDerivedStateFromProps || oldState !== oldProps) && callComponentWillReceiveProps(
        workInProgress2,
        context,
        nextProps,
        oldProps
      );
      hasForceUpdate = false;
      oldState = workInProgress2.memoizedState;
      context.state = oldState;
      processUpdateQueue(workInProgress2, nextProps, context, renderLanes2);
      suspendIfUpdateReadFromEntangledAsyncAction();
      var newState = workInProgress2.memoizedState;
      contextType !== getDerivedStateFromProps || oldState !== newState || hasForceUpdate || null !== current && null !== current.dependencies && checkIfContextChanged(current.dependencies) ? ("function" === typeof unresolvedOldProps && (applyDerivedStateFromProps(
        workInProgress2,
        Component,
        unresolvedOldProps,
        nextProps
      ), newState = workInProgress2.memoizedState), (contextType$jscomp$0 = hasForceUpdate || checkShouldComponentUpdate(
        workInProgress2,
        Component,
        contextType$jscomp$0,
        nextProps,
        oldState,
        newState,
        oldProps
      ) || null !== current && null !== current.dependencies && checkIfContextChanged(current.dependencies)) ? (oldContext || "function" !== typeof context.UNSAFE_componentWillUpdate && "function" !== typeof context.componentWillUpdate || ("function" === typeof context.componentWillUpdate && context.componentWillUpdate(nextProps, newState, oldProps), "function" === typeof context.UNSAFE_componentWillUpdate && context.UNSAFE_componentWillUpdate(
        nextProps,
        newState,
        oldProps
      )), "function" === typeof context.componentDidUpdate && (workInProgress2.flags |= 4), "function" === typeof context.getSnapshotBeforeUpdate && (workInProgress2.flags |= 1024)) : ("function" !== typeof context.componentDidUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 4), "function" !== typeof context.getSnapshotBeforeUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 1024), workInProgress2.memoizedProps = nextProps, workInProgress2.memoizedState = newState), context.props = nextProps, context.state = newState, context.context = oldProps, nextProps = contextType$jscomp$0) : ("function" !== typeof context.componentDidUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 4), "function" !== typeof context.getSnapshotBeforeUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 1024), nextProps = false);
    }
    context = nextProps;
    markRef(current, workInProgress2);
    nextProps = 0 !== (workInProgress2.flags & 128);
    context || nextProps ? (context = workInProgress2.stateNode, Component = nextProps && "function" !== typeof Component.getDerivedStateFromError ? null : context.render(), workInProgress2.flags |= 1, null !== current && nextProps ? (workInProgress2.child = reconcileChildFibers(
      workInProgress2,
      current.child,
      null,
      renderLanes2
    ), workInProgress2.child = reconcileChildFibers(
      workInProgress2,
      null,
      Component,
      renderLanes2
    )) : reconcileChildren(current, workInProgress2, Component, renderLanes2), workInProgress2.memoizedState = context.state, current = workInProgress2.child) : current = bailoutOnAlreadyFinishedWork(
      current,
      workInProgress2,
      renderLanes2
    );
    return current;
  }
  function mountHostRootWithoutHydrating(current, workInProgress2, nextChildren, renderLanes2) {
    resetHydrationState();
    workInProgress2.flags |= 256;
    reconcileChildren(current, workInProgress2, nextChildren, renderLanes2);
    return workInProgress2.child;
  }
  var SUSPENDED_MARKER = {
    dehydrated: null,
    treeContext: null,
    retryLane: 0,
    hydrationErrors: null
  };
  function mountSuspenseOffscreenState(renderLanes2) {
    return { baseLanes: renderLanes2, cachePool: getSuspendedCache() };
  }
  function getRemainingWorkInPrimaryTree(current, primaryTreeDidDefer, renderLanes2) {
    current = null !== current ? current.childLanes & ~renderLanes2 : 0;
    primaryTreeDidDefer && (current |= workInProgressDeferredLane);
    return current;
  }
  function updateSuspenseComponent(current, workInProgress2, renderLanes2) {
    var nextProps = workInProgress2.pendingProps, showFallback = false, didSuspend = 0 !== (workInProgress2.flags & 128), JSCompiler_temp;
    (JSCompiler_temp = didSuspend) || (JSCompiler_temp = null !== current && null === current.memoizedState ? false : 0 !== (suspenseStackCursor.current & 2));
    JSCompiler_temp && (showFallback = true, workInProgress2.flags &= -129);
    JSCompiler_temp = 0 !== (workInProgress2.flags & 32);
    workInProgress2.flags &= -33;
    if (null === current) {
      if (isHydrating) {
        showFallback ? pushPrimaryTreeSuspenseHandler(workInProgress2) : reuseSuspenseHandlerOnStack();
        (current = nextHydratableInstance) ? (current = canHydrateHydrationBoundary(
          current,
          rootOrSingletonContext
        ), current = null !== current && "&" !== current.data ? current : null, null !== current && (workInProgress2.memoizedState = {
          dehydrated: current,
          treeContext: null !== treeContextProvider ? { id: treeContextId, overflow: treeContextOverflow } : null,
          retryLane: 536870912,
          hydrationErrors: null
        }, renderLanes2 = createFiberFromDehydratedFragment(current), renderLanes2.return = workInProgress2, workInProgress2.child = renderLanes2, hydrationParentFiber = workInProgress2, nextHydratableInstance = null)) : current = null;
        if (null === current) throw throwOnHydrationMismatch(workInProgress2);
        isSuspenseInstanceFallback(current) ? workInProgress2.lanes = 32 : workInProgress2.lanes = 536870912;
        return null;
      }
      var nextPrimaryChildren = nextProps.children;
      nextProps = nextProps.fallback;
      if (showFallback)
        return reuseSuspenseHandlerOnStack(), showFallback = workInProgress2.mode, nextPrimaryChildren = mountWorkInProgressOffscreenFiber(
          { mode: "hidden", children: nextPrimaryChildren },
          showFallback
        ), nextProps = createFiberFromFragment(
          nextProps,
          showFallback,
          renderLanes2,
          null
        ), nextPrimaryChildren.return = workInProgress2, nextProps.return = workInProgress2, nextPrimaryChildren.sibling = nextProps, workInProgress2.child = nextPrimaryChildren, nextProps = workInProgress2.child, nextProps.memoizedState = mountSuspenseOffscreenState(renderLanes2), nextProps.childLanes = getRemainingWorkInPrimaryTree(
          current,
          JSCompiler_temp,
          renderLanes2
        ), workInProgress2.memoizedState = SUSPENDED_MARKER, bailoutOffscreenComponent(null, nextProps);
      pushPrimaryTreeSuspenseHandler(workInProgress2);
      return mountSuspensePrimaryChildren(workInProgress2, nextPrimaryChildren);
    }
    var prevState = current.memoizedState;
    if (null !== prevState && (nextPrimaryChildren = prevState.dehydrated, null !== nextPrimaryChildren)) {
      if (didSuspend)
        workInProgress2.flags & 256 ? (pushPrimaryTreeSuspenseHandler(workInProgress2), workInProgress2.flags &= -257, workInProgress2 = retrySuspenseComponentWithoutHydrating(
          current,
          workInProgress2,
          renderLanes2
        )) : null !== workInProgress2.memoizedState ? (reuseSuspenseHandlerOnStack(), workInProgress2.child = current.child, workInProgress2.flags |= 128, workInProgress2 = null) : (reuseSuspenseHandlerOnStack(), nextPrimaryChildren = nextProps.fallback, showFallback = workInProgress2.mode, nextProps = mountWorkInProgressOffscreenFiber(
          { mode: "visible", children: nextProps.children },
          showFallback
        ), nextPrimaryChildren = createFiberFromFragment(
          nextPrimaryChildren,
          showFallback,
          renderLanes2,
          null
        ), nextPrimaryChildren.flags |= 2, nextProps.return = workInProgress2, nextPrimaryChildren.return = workInProgress2, nextProps.sibling = nextPrimaryChildren, workInProgress2.child = nextProps, reconcileChildFibers(
          workInProgress2,
          current.child,
          null,
          renderLanes2
        ), nextProps = workInProgress2.child, nextProps.memoizedState = mountSuspenseOffscreenState(renderLanes2), nextProps.childLanes = getRemainingWorkInPrimaryTree(
          current,
          JSCompiler_temp,
          renderLanes2
        ), workInProgress2.memoizedState = SUSPENDED_MARKER, workInProgress2 = bailoutOffscreenComponent(null, nextProps));
      else if (pushPrimaryTreeSuspenseHandler(workInProgress2), isSuspenseInstanceFallback(nextPrimaryChildren)) {
        JSCompiler_temp = nextPrimaryChildren.nextSibling && nextPrimaryChildren.nextSibling.dataset;
        if (JSCompiler_temp) var digest = JSCompiler_temp.dgst;
        JSCompiler_temp = digest;
        nextProps = Error(formatProdErrorMessage(419));
        nextProps.stack = "";
        nextProps.digest = JSCompiler_temp;
        queueHydrationError({ value: nextProps, source: null, stack: null });
        workInProgress2 = retrySuspenseComponentWithoutHydrating(
          current,
          workInProgress2,
          renderLanes2
        );
      } else if (didReceiveUpdate || propagateParentContextChanges(current, workInProgress2, renderLanes2, false), JSCompiler_temp = 0 !== (renderLanes2 & current.childLanes), didReceiveUpdate || JSCompiler_temp) {
        JSCompiler_temp = workInProgressRoot;
        if (null !== JSCompiler_temp && (nextProps = getBumpedLaneForHydration(JSCompiler_temp, renderLanes2), 0 !== nextProps && nextProps !== prevState.retryLane))
          throw prevState.retryLane = nextProps, enqueueConcurrentRenderForLane(current, nextProps), scheduleUpdateOnFiber(JSCompiler_temp, current, nextProps), SelectiveHydrationException;
        isSuspenseInstancePending(nextPrimaryChildren) || renderDidSuspendDelayIfPossible();
        workInProgress2 = retrySuspenseComponentWithoutHydrating(
          current,
          workInProgress2,
          renderLanes2
        );
      } else
        isSuspenseInstancePending(nextPrimaryChildren) ? (workInProgress2.flags |= 192, workInProgress2.child = current.child, workInProgress2 = null) : (current = prevState.treeContext, nextHydratableInstance = getNextHydratable(
          nextPrimaryChildren.nextSibling
        ), hydrationParentFiber = workInProgress2, isHydrating = true, hydrationErrors = null, rootOrSingletonContext = false, null !== current && restoreSuspendedTreeContext(workInProgress2, current), workInProgress2 = mountSuspensePrimaryChildren(
          workInProgress2,
          nextProps.children
        ), workInProgress2.flags |= 4096);
      return workInProgress2;
    }
    if (showFallback)
      return reuseSuspenseHandlerOnStack(), nextPrimaryChildren = nextProps.fallback, showFallback = workInProgress2.mode, prevState = current.child, digest = prevState.sibling, nextProps = createWorkInProgress(prevState, {
        mode: "hidden",
        children: nextProps.children
      }), nextProps.subtreeFlags = prevState.subtreeFlags & 65011712, null !== digest ? nextPrimaryChildren = createWorkInProgress(
        digest,
        nextPrimaryChildren
      ) : (nextPrimaryChildren = createFiberFromFragment(
        nextPrimaryChildren,
        showFallback,
        renderLanes2,
        null
      ), nextPrimaryChildren.flags |= 2), nextPrimaryChildren.return = workInProgress2, nextProps.return = workInProgress2, nextProps.sibling = nextPrimaryChildren, workInProgress2.child = nextProps, bailoutOffscreenComponent(null, nextProps), nextProps = workInProgress2.child, nextPrimaryChildren = current.child.memoizedState, null === nextPrimaryChildren ? nextPrimaryChildren = mountSuspenseOffscreenState(renderLanes2) : (showFallback = nextPrimaryChildren.cachePool, null !== showFallback ? (prevState = CacheContext._currentValue, showFallback = showFallback.parent !== prevState ? { parent: prevState, pool: prevState } : showFallback) : showFallback = getSuspendedCache(), nextPrimaryChildren = {
        baseLanes: nextPrimaryChildren.baseLanes | renderLanes2,
        cachePool: showFallback
      }), nextProps.memoizedState = nextPrimaryChildren, nextProps.childLanes = getRemainingWorkInPrimaryTree(
        current,
        JSCompiler_temp,
        renderLanes2
      ), workInProgress2.memoizedState = SUSPENDED_MARKER, bailoutOffscreenComponent(current.child, nextProps);
    pushPrimaryTreeSuspenseHandler(workInProgress2);
    renderLanes2 = current.child;
    current = renderLanes2.sibling;
    renderLanes2 = createWorkInProgress(renderLanes2, {
      mode: "visible",
      children: nextProps.children
    });
    renderLanes2.return = workInProgress2;
    renderLanes2.sibling = null;
    null !== current && (JSCompiler_temp = workInProgress2.deletions, null === JSCompiler_temp ? (workInProgress2.deletions = [current], workInProgress2.flags |= 16) : JSCompiler_temp.push(current));
    workInProgress2.child = renderLanes2;
    workInProgress2.memoizedState = null;
    return renderLanes2;
  }
  function mountSuspensePrimaryChildren(workInProgress2, primaryChildren) {
    primaryChildren = mountWorkInProgressOffscreenFiber(
      { mode: "visible", children: primaryChildren },
      workInProgress2.mode
    );
    primaryChildren.return = workInProgress2;
    return workInProgress2.child = primaryChildren;
  }
  function mountWorkInProgressOffscreenFiber(offscreenProps, mode) {
    offscreenProps = createFiberImplClass(22, offscreenProps, null, mode);
    offscreenProps.lanes = 0;
    return offscreenProps;
  }
  function retrySuspenseComponentWithoutHydrating(current, workInProgress2, renderLanes2) {
    reconcileChildFibers(workInProgress2, current.child, null, renderLanes2);
    current = mountSuspensePrimaryChildren(
      workInProgress2,
      workInProgress2.pendingProps.children
    );
    current.flags |= 2;
    workInProgress2.memoizedState = null;
    return current;
  }
  function scheduleSuspenseWorkOnFiber(fiber, renderLanes2, propagationRoot) {
    fiber.lanes |= renderLanes2;
    var alternate = fiber.alternate;
    null !== alternate && (alternate.lanes |= renderLanes2);
    scheduleContextWorkOnParentPath(fiber.return, renderLanes2, propagationRoot);
  }
  function initSuspenseListRenderState(workInProgress2, isBackwards, tail, lastContentRow, tailMode, treeForkCount2) {
    var renderState = workInProgress2.memoizedState;
    null === renderState ? workInProgress2.memoizedState = {
      isBackwards,
      rendering: null,
      renderingStartTime: 0,
      last: lastContentRow,
      tail,
      tailMode,
      treeForkCount: treeForkCount2
    } : (renderState.isBackwards = isBackwards, renderState.rendering = null, renderState.renderingStartTime = 0, renderState.last = lastContentRow, renderState.tail = tail, renderState.tailMode = tailMode, renderState.treeForkCount = treeForkCount2);
  }
  function updateSuspenseListComponent(current, workInProgress2, renderLanes2) {
    var nextProps = workInProgress2.pendingProps, revealOrder = nextProps.revealOrder, tailMode = nextProps.tail;
    nextProps = nextProps.children;
    var suspenseContext = suspenseStackCursor.current, shouldForceFallback = 0 !== (suspenseContext & 2);
    shouldForceFallback ? (suspenseContext = suspenseContext & 1 | 2, workInProgress2.flags |= 128) : suspenseContext &= 1;
    push2(suspenseStackCursor, suspenseContext);
    reconcileChildren(current, workInProgress2, nextProps, renderLanes2);
    nextProps = isHydrating ? treeForkCount : 0;
    if (!shouldForceFallback && null !== current && 0 !== (current.flags & 128))
      a: for (current = workInProgress2.child; null !== current; ) {
        if (13 === current.tag)
          null !== current.memoizedState && scheduleSuspenseWorkOnFiber(current, renderLanes2, workInProgress2);
        else if (19 === current.tag)
          scheduleSuspenseWorkOnFiber(current, renderLanes2, workInProgress2);
        else if (null !== current.child) {
          current.child.return = current;
          current = current.child;
          continue;
        }
        if (current === workInProgress2) break a;
        for (; null === current.sibling; ) {
          if (null === current.return || current.return === workInProgress2)
            break a;
          current = current.return;
        }
        current.sibling.return = current.return;
        current = current.sibling;
      }
    switch (revealOrder) {
      case "forwards":
        renderLanes2 = workInProgress2.child;
        for (revealOrder = null; null !== renderLanes2; )
          current = renderLanes2.alternate, null !== current && null === findFirstSuspended(current) && (revealOrder = renderLanes2), renderLanes2 = renderLanes2.sibling;
        renderLanes2 = revealOrder;
        null === renderLanes2 ? (revealOrder = workInProgress2.child, workInProgress2.child = null) : (revealOrder = renderLanes2.sibling, renderLanes2.sibling = null);
        initSuspenseListRenderState(
          workInProgress2,
          false,
          revealOrder,
          renderLanes2,
          tailMode,
          nextProps
        );
        break;
      case "backwards":
      case "unstable_legacy-backwards":
        renderLanes2 = null;
        revealOrder = workInProgress2.child;
        for (workInProgress2.child = null; null !== revealOrder; ) {
          current = revealOrder.alternate;
          if (null !== current && null === findFirstSuspended(current)) {
            workInProgress2.child = revealOrder;
            break;
          }
          current = revealOrder.sibling;
          revealOrder.sibling = renderLanes2;
          renderLanes2 = revealOrder;
          revealOrder = current;
        }
        initSuspenseListRenderState(
          workInProgress2,
          true,
          renderLanes2,
          null,
          tailMode,
          nextProps
        );
        break;
      case "together":
        initSuspenseListRenderState(
          workInProgress2,
          false,
          null,
          null,
          void 0,
          nextProps
        );
        break;
      default:
        workInProgress2.memoizedState = null;
    }
    return workInProgress2.child;
  }
  function bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2) {
    null !== current && (workInProgress2.dependencies = current.dependencies);
    workInProgressRootSkippedLanes |= workInProgress2.lanes;
    if (0 === (renderLanes2 & workInProgress2.childLanes))
      if (null !== current) {
        if (propagateParentContextChanges(
          current,
          workInProgress2,
          renderLanes2,
          false
        ), 0 === (renderLanes2 & workInProgress2.childLanes))
          return null;
      } else return null;
    if (null !== current && workInProgress2.child !== current.child)
      throw Error(formatProdErrorMessage(153));
    if (null !== workInProgress2.child) {
      current = workInProgress2.child;
      renderLanes2 = createWorkInProgress(current, current.pendingProps);
      workInProgress2.child = renderLanes2;
      for (renderLanes2.return = workInProgress2; null !== current.sibling; )
        current = current.sibling, renderLanes2 = renderLanes2.sibling = createWorkInProgress(current, current.pendingProps), renderLanes2.return = workInProgress2;
      renderLanes2.sibling = null;
    }
    return workInProgress2.child;
  }
  function checkScheduledUpdateOrContext(current, renderLanes2) {
    if (0 !== (current.lanes & renderLanes2)) return true;
    current = current.dependencies;
    return null !== current && checkIfContextChanged(current) ? true : false;
  }
  function attemptEarlyBailoutIfNoScheduledUpdate(current, workInProgress2, renderLanes2) {
    switch (workInProgress2.tag) {
      case 3:
        pushHostContainer(workInProgress2, workInProgress2.stateNode.containerInfo);
        pushProvider(workInProgress2, CacheContext, current.memoizedState.cache);
        resetHydrationState();
        break;
      case 27:
      case 5:
        pushHostContext(workInProgress2);
        break;
      case 4:
        pushHostContainer(workInProgress2, workInProgress2.stateNode.containerInfo);
        break;
      case 10:
        pushProvider(
          workInProgress2,
          workInProgress2.type,
          workInProgress2.memoizedProps.value
        );
        break;
      case 31:
        if (null !== workInProgress2.memoizedState)
          return workInProgress2.flags |= 128, pushDehydratedActivitySuspenseHandler(workInProgress2), null;
        break;
      case 13:
        var state$102 = workInProgress2.memoizedState;
        if (null !== state$102) {
          if (null !== state$102.dehydrated)
            return pushPrimaryTreeSuspenseHandler(workInProgress2), workInProgress2.flags |= 128, null;
          if (0 !== (renderLanes2 & workInProgress2.child.childLanes))
            return updateSuspenseComponent(current, workInProgress2, renderLanes2);
          pushPrimaryTreeSuspenseHandler(workInProgress2);
          current = bailoutOnAlreadyFinishedWork(
            current,
            workInProgress2,
            renderLanes2
          );
          return null !== current ? current.sibling : null;
        }
        pushPrimaryTreeSuspenseHandler(workInProgress2);
        break;
      case 19:
        var didSuspendBefore = 0 !== (current.flags & 128);
        state$102 = 0 !== (renderLanes2 & workInProgress2.childLanes);
        state$102 || (propagateParentContextChanges(
          current,
          workInProgress2,
          renderLanes2,
          false
        ), state$102 = 0 !== (renderLanes2 & workInProgress2.childLanes));
        if (didSuspendBefore) {
          if (state$102)
            return updateSuspenseListComponent(
              current,
              workInProgress2,
              renderLanes2
            );
          workInProgress2.flags |= 128;
        }
        didSuspendBefore = workInProgress2.memoizedState;
        null !== didSuspendBefore && (didSuspendBefore.rendering = null, didSuspendBefore.tail = null, didSuspendBefore.lastEffect = null);
        push2(suspenseStackCursor, suspenseStackCursor.current);
        if (state$102) break;
        else return null;
      case 22:
        return workInProgress2.lanes = 0, updateOffscreenComponent(
          current,
          workInProgress2,
          renderLanes2,
          workInProgress2.pendingProps
        );
      case 24:
        pushProvider(workInProgress2, CacheContext, current.memoizedState.cache);
    }
    return bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
  }
  function beginWork(current, workInProgress2, renderLanes2) {
    if (null !== current)
      if (current.memoizedProps !== workInProgress2.pendingProps)
        didReceiveUpdate = true;
      else {
        if (!checkScheduledUpdateOrContext(current, renderLanes2) && 0 === (workInProgress2.flags & 128))
          return didReceiveUpdate = false, attemptEarlyBailoutIfNoScheduledUpdate(
            current,
            workInProgress2,
            renderLanes2
          );
        didReceiveUpdate = 0 !== (current.flags & 131072) ? true : false;
      }
    else
      didReceiveUpdate = false, isHydrating && 0 !== (workInProgress2.flags & 1048576) && pushTreeId(workInProgress2, treeForkCount, workInProgress2.index);
    workInProgress2.lanes = 0;
    switch (workInProgress2.tag) {
      case 16:
        a: {
          var props = workInProgress2.pendingProps;
          current = resolveLazy(workInProgress2.elementType);
          workInProgress2.type = current;
          if ("function" === typeof current)
            shouldConstruct(current) ? (props = resolveClassComponentProps(current, props), workInProgress2.tag = 1, workInProgress2 = updateClassComponent(
              null,
              workInProgress2,
              current,
              props,
              renderLanes2
            )) : (workInProgress2.tag = 0, workInProgress2 = updateFunctionComponent(
              null,
              workInProgress2,
              current,
              props,
              renderLanes2
            ));
          else {
            if (void 0 !== current && null !== current) {
              var $$typeof = current.$$typeof;
              if ($$typeof === REACT_FORWARD_REF_TYPE) {
                workInProgress2.tag = 11;
                workInProgress2 = updateForwardRef(
                  null,
                  workInProgress2,
                  current,
                  props,
                  renderLanes2
                );
                break a;
              } else if ($$typeof === REACT_MEMO_TYPE) {
                workInProgress2.tag = 14;
                workInProgress2 = updateMemoComponent(
                  null,
                  workInProgress2,
                  current,
                  props,
                  renderLanes2
                );
                break a;
              }
            }
            workInProgress2 = getComponentNameFromType(current) || current;
            throw Error(formatProdErrorMessage(306, workInProgress2, ""));
          }
        }
        return workInProgress2;
      case 0:
        return updateFunctionComponent(
          current,
          workInProgress2,
          workInProgress2.type,
          workInProgress2.pendingProps,
          renderLanes2
        );
      case 1:
        return props = workInProgress2.type, $$typeof = resolveClassComponentProps(
          props,
          workInProgress2.pendingProps
        ), updateClassComponent(
          current,
          workInProgress2,
          props,
          $$typeof,
          renderLanes2
        );
      case 3:
        a: {
          pushHostContainer(
            workInProgress2,
            workInProgress2.stateNode.containerInfo
          );
          if (null === current) throw Error(formatProdErrorMessage(387));
          props = workInProgress2.pendingProps;
          var prevState = workInProgress2.memoizedState;
          $$typeof = prevState.element;
          cloneUpdateQueue(current, workInProgress2);
          processUpdateQueue(workInProgress2, props, null, renderLanes2);
          var nextState = workInProgress2.memoizedState;
          props = nextState.cache;
          pushProvider(workInProgress2, CacheContext, props);
          props !== prevState.cache && propagateContextChanges(
            workInProgress2,
            [CacheContext],
            renderLanes2,
            true
          );
          suspendIfUpdateReadFromEntangledAsyncAction();
          props = nextState.element;
          if (prevState.isDehydrated)
            if (prevState = {
              element: props,
              isDehydrated: false,
              cache: nextState.cache
            }, workInProgress2.updateQueue.baseState = prevState, workInProgress2.memoizedState = prevState, workInProgress2.flags & 256) {
              workInProgress2 = mountHostRootWithoutHydrating(
                current,
                workInProgress2,
                props,
                renderLanes2
              );
              break a;
            } else if (props !== $$typeof) {
              $$typeof = createCapturedValueAtFiber(
                Error(formatProdErrorMessage(424)),
                workInProgress2
              );
              queueHydrationError($$typeof);
              workInProgress2 = mountHostRootWithoutHydrating(
                current,
                workInProgress2,
                props,
                renderLanes2
              );
              break a;
            } else {
              current = workInProgress2.stateNode.containerInfo;
              switch (current.nodeType) {
                case 9:
                  current = current.body;
                  break;
                default:
                  current = "HTML" === current.nodeName ? current.ownerDocument.body : current;
              }
              nextHydratableInstance = getNextHydratable(current.firstChild);
              hydrationParentFiber = workInProgress2;
              isHydrating = true;
              hydrationErrors = null;
              rootOrSingletonContext = true;
              renderLanes2 = mountChildFibers(
                workInProgress2,
                null,
                props,
                renderLanes2
              );
              for (workInProgress2.child = renderLanes2; renderLanes2; )
                renderLanes2.flags = renderLanes2.flags & -3 | 4096, renderLanes2 = renderLanes2.sibling;
            }
          else {
            resetHydrationState();
            if (props === $$typeof) {
              workInProgress2 = bailoutOnAlreadyFinishedWork(
                current,
                workInProgress2,
                renderLanes2
              );
              break a;
            }
            reconcileChildren(current, workInProgress2, props, renderLanes2);
          }
          workInProgress2 = workInProgress2.child;
        }
        return workInProgress2;
      case 26:
        return markRef(current, workInProgress2), null === current ? (renderLanes2 = getResource(
          workInProgress2.type,
          null,
          workInProgress2.pendingProps,
          null
        )) ? workInProgress2.memoizedState = renderLanes2 : isHydrating || (renderLanes2 = workInProgress2.type, current = workInProgress2.pendingProps, props = getOwnerDocumentFromRootContainer(
          rootInstanceStackCursor.current
        ).createElement(renderLanes2), props[internalInstanceKey] = workInProgress2, props[internalPropsKey] = current, setInitialProperties(props, renderLanes2, current), markNodeAsHoistable(props), workInProgress2.stateNode = props) : workInProgress2.memoizedState = getResource(
          workInProgress2.type,
          current.memoizedProps,
          workInProgress2.pendingProps,
          current.memoizedState
        ), null;
      case 27:
        return pushHostContext(workInProgress2), null === current && isHydrating && (props = workInProgress2.stateNode = resolveSingletonInstance(
          workInProgress2.type,
          workInProgress2.pendingProps,
          rootInstanceStackCursor.current
        ), hydrationParentFiber = workInProgress2, rootOrSingletonContext = true, $$typeof = nextHydratableInstance, isSingletonScope(workInProgress2.type) ? (previousHydratableOnEnteringScopedSingleton = $$typeof, nextHydratableInstance = getNextHydratable(props.firstChild)) : nextHydratableInstance = $$typeof), reconcileChildren(
          current,
          workInProgress2,
          workInProgress2.pendingProps.children,
          renderLanes2
        ), markRef(current, workInProgress2), null === current && (workInProgress2.flags |= 4194304), workInProgress2.child;
      case 5:
        if (null === current && isHydrating) {
          if ($$typeof = props = nextHydratableInstance)
            props = canHydrateInstance(
              props,
              workInProgress2.type,
              workInProgress2.pendingProps,
              rootOrSingletonContext
            ), null !== props ? (workInProgress2.stateNode = props, hydrationParentFiber = workInProgress2, nextHydratableInstance = getNextHydratable(props.firstChild), rootOrSingletonContext = false, $$typeof = true) : $$typeof = false;
          $$typeof || throwOnHydrationMismatch(workInProgress2);
        }
        pushHostContext(workInProgress2);
        $$typeof = workInProgress2.type;
        prevState = workInProgress2.pendingProps;
        nextState = null !== current ? current.memoizedProps : null;
        props = prevState.children;
        shouldSetTextContent($$typeof, prevState) ? props = null : null !== nextState && shouldSetTextContent($$typeof, nextState) && (workInProgress2.flags |= 32);
        null !== workInProgress2.memoizedState && ($$typeof = renderWithHooks(
          current,
          workInProgress2,
          TransitionAwareHostComponent,
          null,
          null,
          renderLanes2
        ), HostTransitionContext._currentValue = $$typeof);
        markRef(current, workInProgress2);
        reconcileChildren(current, workInProgress2, props, renderLanes2);
        return workInProgress2.child;
      case 6:
        if (null === current && isHydrating) {
          if (current = renderLanes2 = nextHydratableInstance)
            renderLanes2 = canHydrateTextInstance(
              renderLanes2,
              workInProgress2.pendingProps,
              rootOrSingletonContext
            ), null !== renderLanes2 ? (workInProgress2.stateNode = renderLanes2, hydrationParentFiber = workInProgress2, nextHydratableInstance = null, current = true) : current = false;
          current || throwOnHydrationMismatch(workInProgress2);
        }
        return null;
      case 13:
        return updateSuspenseComponent(current, workInProgress2, renderLanes2);
      case 4:
        return pushHostContainer(
          workInProgress2,
          workInProgress2.stateNode.containerInfo
        ), props = workInProgress2.pendingProps, null === current ? workInProgress2.child = reconcileChildFibers(
          workInProgress2,
          null,
          props,
          renderLanes2
        ) : reconcileChildren(current, workInProgress2, props, renderLanes2), workInProgress2.child;
      case 11:
        return updateForwardRef(
          current,
          workInProgress2,
          workInProgress2.type,
          workInProgress2.pendingProps,
          renderLanes2
        );
      case 7:
        return reconcileChildren(
          current,
          workInProgress2,
          workInProgress2.pendingProps,
          renderLanes2
        ), workInProgress2.child;
      case 8:
        return reconcileChildren(
          current,
          workInProgress2,
          workInProgress2.pendingProps.children,
          renderLanes2
        ), workInProgress2.child;
      case 12:
        return reconcileChildren(
          current,
          workInProgress2,
          workInProgress2.pendingProps.children,
          renderLanes2
        ), workInProgress2.child;
      case 10:
        return props = workInProgress2.pendingProps, pushProvider(workInProgress2, workInProgress2.type, props.value), reconcileChildren(current, workInProgress2, props.children, renderLanes2), workInProgress2.child;
      case 9:
        return $$typeof = workInProgress2.type._context, props = workInProgress2.pendingProps.children, prepareToReadContext(workInProgress2), $$typeof = readContext($$typeof), props = props($$typeof), workInProgress2.flags |= 1, reconcileChildren(current, workInProgress2, props, renderLanes2), workInProgress2.child;
      case 14:
        return updateMemoComponent(
          current,
          workInProgress2,
          workInProgress2.type,
          workInProgress2.pendingProps,
          renderLanes2
        );
      case 15:
        return updateSimpleMemoComponent(
          current,
          workInProgress2,
          workInProgress2.type,
          workInProgress2.pendingProps,
          renderLanes2
        );
      case 19:
        return updateSuspenseListComponent(current, workInProgress2, renderLanes2);
      case 31:
        return updateActivityComponent(current, workInProgress2, renderLanes2);
      case 22:
        return updateOffscreenComponent(
          current,
          workInProgress2,
          renderLanes2,
          workInProgress2.pendingProps
        );
      case 24:
        return prepareToReadContext(workInProgress2), props = readContext(CacheContext), null === current ? ($$typeof = peekCacheFromPool(), null === $$typeof && ($$typeof = workInProgressRoot, prevState = createCache(), $$typeof.pooledCache = prevState, prevState.refCount++, null !== prevState && ($$typeof.pooledCacheLanes |= renderLanes2), $$typeof = prevState), workInProgress2.memoizedState = { parent: props, cache: $$typeof }, initializeUpdateQueue(workInProgress2), pushProvider(workInProgress2, CacheContext, $$typeof)) : (0 !== (current.lanes & renderLanes2) && (cloneUpdateQueue(current, workInProgress2), processUpdateQueue(workInProgress2, null, null, renderLanes2), suspendIfUpdateReadFromEntangledAsyncAction()), $$typeof = current.memoizedState, prevState = workInProgress2.memoizedState, $$typeof.parent !== props ? ($$typeof = { parent: props, cache: props }, workInProgress2.memoizedState = $$typeof, 0 === workInProgress2.lanes && (workInProgress2.memoizedState = workInProgress2.updateQueue.baseState = $$typeof), pushProvider(workInProgress2, CacheContext, props)) : (props = prevState.cache, pushProvider(workInProgress2, CacheContext, props), props !== $$typeof.cache && propagateContextChanges(
          workInProgress2,
          [CacheContext],
          renderLanes2,
          true
        ))), reconcileChildren(
          current,
          workInProgress2,
          workInProgress2.pendingProps.children,
          renderLanes2
        ), workInProgress2.child;
      case 29:
        throw workInProgress2.pendingProps;
    }
    throw Error(formatProdErrorMessage(156, workInProgress2.tag));
  }
  function markUpdate(workInProgress2) {
    workInProgress2.flags |= 4;
  }
  function preloadInstanceAndSuspendIfNeeded(workInProgress2, type, oldProps, newProps, renderLanes2) {
    if (type = 0 !== (workInProgress2.mode & 32)) type = false;
    if (type) {
      if (workInProgress2.flags |= 16777216, (renderLanes2 & 335544128) === renderLanes2)
        if (workInProgress2.stateNode.complete) workInProgress2.flags |= 8192;
        else if (shouldRemainOnPreviousScreen()) workInProgress2.flags |= 8192;
        else
          throw suspendedThenable = noopSuspenseyCommitThenable, SuspenseyCommitException;
    } else workInProgress2.flags &= -16777217;
  }
  function preloadResourceAndSuspendIfNeeded(workInProgress2, resource) {
    if ("stylesheet" !== resource.type || 0 !== (resource.state.loading & 4))
      workInProgress2.flags &= -16777217;
    else if (workInProgress2.flags |= 16777216, !preloadResource(resource))
      if (shouldRemainOnPreviousScreen()) workInProgress2.flags |= 8192;
      else
        throw suspendedThenable = noopSuspenseyCommitThenable, SuspenseyCommitException;
  }
  function scheduleRetryEffect(workInProgress2, retryQueue) {
    null !== retryQueue && (workInProgress2.flags |= 4);
    workInProgress2.flags & 16384 && (retryQueue = 22 !== workInProgress2.tag ? claimNextRetryLane() : 536870912, workInProgress2.lanes |= retryQueue, workInProgressSuspendedRetryLanes |= retryQueue);
  }
  function cutOffTailIfNeeded(renderState, hasRenderedATailFallback) {
    if (!isHydrating)
      switch (renderState.tailMode) {
        case "hidden":
          hasRenderedATailFallback = renderState.tail;
          for (var lastTailNode = null; null !== hasRenderedATailFallback; )
            null !== hasRenderedATailFallback.alternate && (lastTailNode = hasRenderedATailFallback), hasRenderedATailFallback = hasRenderedATailFallback.sibling;
          null === lastTailNode ? renderState.tail = null : lastTailNode.sibling = null;
          break;
        case "collapsed":
          lastTailNode = renderState.tail;
          for (var lastTailNode$106 = null; null !== lastTailNode; )
            null !== lastTailNode.alternate && (lastTailNode$106 = lastTailNode), lastTailNode = lastTailNode.sibling;
          null === lastTailNode$106 ? hasRenderedATailFallback || null === renderState.tail ? renderState.tail = null : renderState.tail.sibling = null : lastTailNode$106.sibling = null;
      }
  }
  function bubbleProperties(completedWork) {
    var didBailout = null !== completedWork.alternate && completedWork.alternate.child === completedWork.child, newChildLanes = 0, subtreeFlags = 0;
    if (didBailout)
      for (var child$107 = completedWork.child; null !== child$107; )
        newChildLanes |= child$107.lanes | child$107.childLanes, subtreeFlags |= child$107.subtreeFlags & 65011712, subtreeFlags |= child$107.flags & 65011712, child$107.return = completedWork, child$107 = child$107.sibling;
    else
      for (child$107 = completedWork.child; null !== child$107; )
        newChildLanes |= child$107.lanes | child$107.childLanes, subtreeFlags |= child$107.subtreeFlags, subtreeFlags |= child$107.flags, child$107.return = completedWork, child$107 = child$107.sibling;
    completedWork.subtreeFlags |= subtreeFlags;
    completedWork.childLanes = newChildLanes;
    return didBailout;
  }
  function completeWork(current, workInProgress2, renderLanes2) {
    var newProps = workInProgress2.pendingProps;
    popTreeContext(workInProgress2);
    switch (workInProgress2.tag) {
      case 16:
      case 15:
      case 0:
      case 11:
      case 7:
      case 8:
      case 12:
      case 9:
      case 14:
        return bubbleProperties(workInProgress2), null;
      case 1:
        return bubbleProperties(workInProgress2), null;
      case 3:
        renderLanes2 = workInProgress2.stateNode;
        newProps = null;
        null !== current && (newProps = current.memoizedState.cache);
        workInProgress2.memoizedState.cache !== newProps && (workInProgress2.flags |= 2048);
        popProvider(CacheContext);
        popHostContainer();
        renderLanes2.pendingContext && (renderLanes2.context = renderLanes2.pendingContext, renderLanes2.pendingContext = null);
        if (null === current || null === current.child)
          popHydrationState(workInProgress2) ? markUpdate(workInProgress2) : null === current || current.memoizedState.isDehydrated && 0 === (workInProgress2.flags & 256) || (workInProgress2.flags |= 1024, upgradeHydrationErrorsToRecoverable());
        bubbleProperties(workInProgress2);
        return null;
      case 26:
        var type = workInProgress2.type, nextResource = workInProgress2.memoizedState;
        null === current ? (markUpdate(workInProgress2), null !== nextResource ? (bubbleProperties(workInProgress2), preloadResourceAndSuspendIfNeeded(workInProgress2, nextResource)) : (bubbleProperties(workInProgress2), preloadInstanceAndSuspendIfNeeded(
          workInProgress2,
          type,
          null,
          newProps,
          renderLanes2
        ))) : nextResource ? nextResource !== current.memoizedState ? (markUpdate(workInProgress2), bubbleProperties(workInProgress2), preloadResourceAndSuspendIfNeeded(workInProgress2, nextResource)) : (bubbleProperties(workInProgress2), workInProgress2.flags &= -16777217) : (current = current.memoizedProps, current !== newProps && markUpdate(workInProgress2), bubbleProperties(workInProgress2), preloadInstanceAndSuspendIfNeeded(
          workInProgress2,
          type,
          current,
          newProps,
          renderLanes2
        ));
        return null;
      case 27:
        popHostContext(workInProgress2);
        renderLanes2 = rootInstanceStackCursor.current;
        type = workInProgress2.type;
        if (null !== current && null != workInProgress2.stateNode)
          current.memoizedProps !== newProps && markUpdate(workInProgress2);
        else {
          if (!newProps) {
            if (null === workInProgress2.stateNode)
              throw Error(formatProdErrorMessage(166));
            bubbleProperties(workInProgress2);
            return null;
          }
          current = contextStackCursor.current;
          popHydrationState(workInProgress2) ? prepareToHydrateHostInstance(workInProgress2) : (current = resolveSingletonInstance(type, newProps, renderLanes2), workInProgress2.stateNode = current, markUpdate(workInProgress2));
        }
        bubbleProperties(workInProgress2);
        return null;
      case 5:
        popHostContext(workInProgress2);
        type = workInProgress2.type;
        if (null !== current && null != workInProgress2.stateNode)
          current.memoizedProps !== newProps && markUpdate(workInProgress2);
        else {
          if (!newProps) {
            if (null === workInProgress2.stateNode)
              throw Error(formatProdErrorMessage(166));
            bubbleProperties(workInProgress2);
            return null;
          }
          nextResource = contextStackCursor.current;
          if (popHydrationState(workInProgress2))
            prepareToHydrateHostInstance(workInProgress2);
          else {
            var ownerDocument = getOwnerDocumentFromRootContainer(
              rootInstanceStackCursor.current
            );
            switch (nextResource) {
              case 1:
                nextResource = ownerDocument.createElementNS(
                  "http://www.w3.org/2000/svg",
                  type
                );
                break;
              case 2:
                nextResource = ownerDocument.createElementNS(
                  "http://www.w3.org/1998/Math/MathML",
                  type
                );
                break;
              default:
                switch (type) {
                  case "svg":
                    nextResource = ownerDocument.createElementNS(
                      "http://www.w3.org/2000/svg",
                      type
                    );
                    break;
                  case "math":
                    nextResource = ownerDocument.createElementNS(
                      "http://www.w3.org/1998/Math/MathML",
                      type
                    );
                    break;
                  case "script":
                    nextResource = ownerDocument.createElement("div");
                    nextResource.innerHTML = "<script><\/script>";
                    nextResource = nextResource.removeChild(
                      nextResource.firstChild
                    );
                    break;
                  case "select":
                    nextResource = "string" === typeof newProps.is ? ownerDocument.createElement("select", {
                      is: newProps.is
                    }) : ownerDocument.createElement("select");
                    newProps.multiple ? nextResource.multiple = true : newProps.size && (nextResource.size = newProps.size);
                    break;
                  default:
                    nextResource = "string" === typeof newProps.is ? ownerDocument.createElement(type, { is: newProps.is }) : ownerDocument.createElement(type);
                }
            }
            nextResource[internalInstanceKey] = workInProgress2;
            nextResource[internalPropsKey] = newProps;
            a: for (ownerDocument = workInProgress2.child; null !== ownerDocument; ) {
              if (5 === ownerDocument.tag || 6 === ownerDocument.tag)
                nextResource.appendChild(ownerDocument.stateNode);
              else if (4 !== ownerDocument.tag && 27 !== ownerDocument.tag && null !== ownerDocument.child) {
                ownerDocument.child.return = ownerDocument;
                ownerDocument = ownerDocument.child;
                continue;
              }
              if (ownerDocument === workInProgress2) break a;
              for (; null === ownerDocument.sibling; ) {
                if (null === ownerDocument.return || ownerDocument.return === workInProgress2)
                  break a;
                ownerDocument = ownerDocument.return;
              }
              ownerDocument.sibling.return = ownerDocument.return;
              ownerDocument = ownerDocument.sibling;
            }
            workInProgress2.stateNode = nextResource;
            a: switch (setInitialProperties(nextResource, type, newProps), type) {
              case "button":
              case "input":
              case "select":
              case "textarea":
                newProps = !!newProps.autoFocus;
                break a;
              case "img":
                newProps = true;
                break a;
              default:
                newProps = false;
            }
            newProps && markUpdate(workInProgress2);
          }
        }
        bubbleProperties(workInProgress2);
        preloadInstanceAndSuspendIfNeeded(
          workInProgress2,
          workInProgress2.type,
          null === current ? null : current.memoizedProps,
          workInProgress2.pendingProps,
          renderLanes2
        );
        return null;
      case 6:
        if (current && null != workInProgress2.stateNode)
          current.memoizedProps !== newProps && markUpdate(workInProgress2);
        else {
          if ("string" !== typeof newProps && null === workInProgress2.stateNode)
            throw Error(formatProdErrorMessage(166));
          current = rootInstanceStackCursor.current;
          if (popHydrationState(workInProgress2)) {
            current = workInProgress2.stateNode;
            renderLanes2 = workInProgress2.memoizedProps;
            newProps = null;
            type = hydrationParentFiber;
            if (null !== type)
              switch (type.tag) {
                case 27:
                case 5:
                  newProps = type.memoizedProps;
              }
            current[internalInstanceKey] = workInProgress2;
            current = current.nodeValue === renderLanes2 || null !== newProps && true === newProps.suppressHydrationWarning || checkForUnmatchedText(current.nodeValue, renderLanes2) ? true : false;
            current || throwOnHydrationMismatch(workInProgress2, true);
          } else
            current = getOwnerDocumentFromRootContainer(current).createTextNode(
              newProps
            ), current[internalInstanceKey] = workInProgress2, workInProgress2.stateNode = current;
        }
        bubbleProperties(workInProgress2);
        return null;
      case 31:
        renderLanes2 = workInProgress2.memoizedState;
        if (null === current || null !== current.memoizedState) {
          newProps = popHydrationState(workInProgress2);
          if (null !== renderLanes2) {
            if (null === current) {
              if (!newProps) throw Error(formatProdErrorMessage(318));
              current = workInProgress2.memoizedState;
              current = null !== current ? current.dehydrated : null;
              if (!current) throw Error(formatProdErrorMessage(557));
              current[internalInstanceKey] = workInProgress2;
            } else
              resetHydrationState(), 0 === (workInProgress2.flags & 128) && (workInProgress2.memoizedState = null), workInProgress2.flags |= 4;
            bubbleProperties(workInProgress2);
            current = false;
          } else
            renderLanes2 = upgradeHydrationErrorsToRecoverable(), null !== current && null !== current.memoizedState && (current.memoizedState.hydrationErrors = renderLanes2), current = true;
          if (!current) {
            if (workInProgress2.flags & 256)
              return popSuspenseHandler(workInProgress2), workInProgress2;
            popSuspenseHandler(workInProgress2);
            return null;
          }
          if (0 !== (workInProgress2.flags & 128))
            throw Error(formatProdErrorMessage(558));
        }
        bubbleProperties(workInProgress2);
        return null;
      case 13:
        newProps = workInProgress2.memoizedState;
        if (null === current || null !== current.memoizedState && null !== current.memoizedState.dehydrated) {
          type = popHydrationState(workInProgress2);
          if (null !== newProps && null !== newProps.dehydrated) {
            if (null === current) {
              if (!type) throw Error(formatProdErrorMessage(318));
              type = workInProgress2.memoizedState;
              type = null !== type ? type.dehydrated : null;
              if (!type) throw Error(formatProdErrorMessage(317));
              type[internalInstanceKey] = workInProgress2;
            } else
              resetHydrationState(), 0 === (workInProgress2.flags & 128) && (workInProgress2.memoizedState = null), workInProgress2.flags |= 4;
            bubbleProperties(workInProgress2);
            type = false;
          } else
            type = upgradeHydrationErrorsToRecoverable(), null !== current && null !== current.memoizedState && (current.memoizedState.hydrationErrors = type), type = true;
          if (!type) {
            if (workInProgress2.flags & 256)
              return popSuspenseHandler(workInProgress2), workInProgress2;
            popSuspenseHandler(workInProgress2);
            return null;
          }
        }
        popSuspenseHandler(workInProgress2);
        if (0 !== (workInProgress2.flags & 128))
          return workInProgress2.lanes = renderLanes2, workInProgress2;
        renderLanes2 = null !== newProps;
        current = null !== current && null !== current.memoizedState;
        renderLanes2 && (newProps = workInProgress2.child, type = null, null !== newProps.alternate && null !== newProps.alternate.memoizedState && null !== newProps.alternate.memoizedState.cachePool && (type = newProps.alternate.memoizedState.cachePool.pool), nextResource = null, null !== newProps.memoizedState && null !== newProps.memoizedState.cachePool && (nextResource = newProps.memoizedState.cachePool.pool), nextResource !== type && (newProps.flags |= 2048));
        renderLanes2 !== current && renderLanes2 && (workInProgress2.child.flags |= 8192);
        scheduleRetryEffect(workInProgress2, workInProgress2.updateQueue);
        bubbleProperties(workInProgress2);
        return null;
      case 4:
        return popHostContainer(), null === current && listenToAllSupportedEvents(workInProgress2.stateNode.containerInfo), bubbleProperties(workInProgress2), null;
      case 10:
        return popProvider(workInProgress2.type), bubbleProperties(workInProgress2), null;
      case 19:
        pop(suspenseStackCursor);
        newProps = workInProgress2.memoizedState;
        if (null === newProps) return bubbleProperties(workInProgress2), null;
        type = 0 !== (workInProgress2.flags & 128);
        nextResource = newProps.rendering;
        if (null === nextResource)
          if (type) cutOffTailIfNeeded(newProps, false);
          else {
            if (0 !== workInProgressRootExitStatus || null !== current && 0 !== (current.flags & 128))
              for (current = workInProgress2.child; null !== current; ) {
                nextResource = findFirstSuspended(current);
                if (null !== nextResource) {
                  workInProgress2.flags |= 128;
                  cutOffTailIfNeeded(newProps, false);
                  current = nextResource.updateQueue;
                  workInProgress2.updateQueue = current;
                  scheduleRetryEffect(workInProgress2, current);
                  workInProgress2.subtreeFlags = 0;
                  current = renderLanes2;
                  for (renderLanes2 = workInProgress2.child; null !== renderLanes2; )
                    resetWorkInProgress(renderLanes2, current), renderLanes2 = renderLanes2.sibling;
                  push2(
                    suspenseStackCursor,
                    suspenseStackCursor.current & 1 | 2
                  );
                  isHydrating && pushTreeFork(workInProgress2, newProps.treeForkCount);
                  return workInProgress2.child;
                }
                current = current.sibling;
              }
            null !== newProps.tail && now() > workInProgressRootRenderTargetTime && (workInProgress2.flags |= 128, type = true, cutOffTailIfNeeded(newProps, false), workInProgress2.lanes = 4194304);
          }
        else {
          if (!type)
            if (current = findFirstSuspended(nextResource), null !== current) {
              if (workInProgress2.flags |= 128, type = true, current = current.updateQueue, workInProgress2.updateQueue = current, scheduleRetryEffect(workInProgress2, current), cutOffTailIfNeeded(newProps, true), null === newProps.tail && "hidden" === newProps.tailMode && !nextResource.alternate && !isHydrating)
                return bubbleProperties(workInProgress2), null;
            } else
              2 * now() - newProps.renderingStartTime > workInProgressRootRenderTargetTime && 536870912 !== renderLanes2 && (workInProgress2.flags |= 128, type = true, cutOffTailIfNeeded(newProps, false), workInProgress2.lanes = 4194304);
          newProps.isBackwards ? (nextResource.sibling = workInProgress2.child, workInProgress2.child = nextResource) : (current = newProps.last, null !== current ? current.sibling = nextResource : workInProgress2.child = nextResource, newProps.last = nextResource);
        }
        if (null !== newProps.tail)
          return current = newProps.tail, newProps.rendering = current, newProps.tail = current.sibling, newProps.renderingStartTime = now(), current.sibling = null, renderLanes2 = suspenseStackCursor.current, push2(
            suspenseStackCursor,
            type ? renderLanes2 & 1 | 2 : renderLanes2 & 1
          ), isHydrating && pushTreeFork(workInProgress2, newProps.treeForkCount), current;
        bubbleProperties(workInProgress2);
        return null;
      case 22:
      case 23:
        return popSuspenseHandler(workInProgress2), popHiddenContext(), newProps = null !== workInProgress2.memoizedState, null !== current ? null !== current.memoizedState !== newProps && (workInProgress2.flags |= 8192) : newProps && (workInProgress2.flags |= 8192), newProps ? 0 !== (renderLanes2 & 536870912) && 0 === (workInProgress2.flags & 128) && (bubbleProperties(workInProgress2), workInProgress2.subtreeFlags & 6 && (workInProgress2.flags |= 8192)) : bubbleProperties(workInProgress2), renderLanes2 = workInProgress2.updateQueue, null !== renderLanes2 && scheduleRetryEffect(workInProgress2, renderLanes2.retryQueue), renderLanes2 = null, null !== current && null !== current.memoizedState && null !== current.memoizedState.cachePool && (renderLanes2 = current.memoizedState.cachePool.pool), newProps = null, null !== workInProgress2.memoizedState && null !== workInProgress2.memoizedState.cachePool && (newProps = workInProgress2.memoizedState.cachePool.pool), newProps !== renderLanes2 && (workInProgress2.flags |= 2048), null !== current && pop(resumedCache), null;
      case 24:
        return renderLanes2 = null, null !== current && (renderLanes2 = current.memoizedState.cache), workInProgress2.memoizedState.cache !== renderLanes2 && (workInProgress2.flags |= 2048), popProvider(CacheContext), bubbleProperties(workInProgress2), null;
      case 25:
        return null;
      case 30:
        return null;
    }
    throw Error(formatProdErrorMessage(156, workInProgress2.tag));
  }
  function unwindWork(current, workInProgress2) {
    popTreeContext(workInProgress2);
    switch (workInProgress2.tag) {
      case 1:
        return current = workInProgress2.flags, current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
      case 3:
        return popProvider(CacheContext), popHostContainer(), current = workInProgress2.flags, 0 !== (current & 65536) && 0 === (current & 128) ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
      case 26:
      case 27:
      case 5:
        return popHostContext(workInProgress2), null;
      case 31:
        if (null !== workInProgress2.memoizedState) {
          popSuspenseHandler(workInProgress2);
          if (null === workInProgress2.alternate)
            throw Error(formatProdErrorMessage(340));
          resetHydrationState();
        }
        current = workInProgress2.flags;
        return current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
      case 13:
        popSuspenseHandler(workInProgress2);
        current = workInProgress2.memoizedState;
        if (null !== current && null !== current.dehydrated) {
          if (null === workInProgress2.alternate)
            throw Error(formatProdErrorMessage(340));
          resetHydrationState();
        }
        current = workInProgress2.flags;
        return current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
      case 19:
        return pop(suspenseStackCursor), null;
      case 4:
        return popHostContainer(), null;
      case 10:
        return popProvider(workInProgress2.type), null;
      case 22:
      case 23:
        return popSuspenseHandler(workInProgress2), popHiddenContext(), null !== current && pop(resumedCache), current = workInProgress2.flags, current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
      case 24:
        return popProvider(CacheContext), null;
      case 25:
        return null;
      default:
        return null;
    }
  }
  function unwindInterruptedWork(current, interruptedWork) {
    popTreeContext(interruptedWork);
    switch (interruptedWork.tag) {
      case 3:
        popProvider(CacheContext);
        popHostContainer();
        break;
      case 26:
      case 27:
      case 5:
        popHostContext(interruptedWork);
        break;
      case 4:
        popHostContainer();
        break;
      case 31:
        null !== interruptedWork.memoizedState && popSuspenseHandler(interruptedWork);
        break;
      case 13:
        popSuspenseHandler(interruptedWork);
        break;
      case 19:
        pop(suspenseStackCursor);
        break;
      case 10:
        popProvider(interruptedWork.type);
        break;
      case 22:
      case 23:
        popSuspenseHandler(interruptedWork);
        popHiddenContext();
        null !== current && pop(resumedCache);
        break;
      case 24:
        popProvider(CacheContext);
    }
  }
  function commitHookEffectListMount(flags, finishedWork) {
    try {
      var updateQueue = finishedWork.updateQueue, lastEffect = null !== updateQueue ? updateQueue.lastEffect : null;
      if (null !== lastEffect) {
        var firstEffect = lastEffect.next;
        updateQueue = firstEffect;
        do {
          if ((updateQueue.tag & flags) === flags) {
            lastEffect = void 0;
            var create2 = updateQueue.create, inst = updateQueue.inst;
            lastEffect = create2();
            inst.destroy = lastEffect;
          }
          updateQueue = updateQueue.next;
        } while (updateQueue !== firstEffect);
      }
    } catch (error) {
      captureCommitPhaseError(finishedWork, finishedWork.return, error);
    }
  }
  function commitHookEffectListUnmount(flags, finishedWork, nearestMountedAncestor$jscomp$0) {
    try {
      var updateQueue = finishedWork.updateQueue, lastEffect = null !== updateQueue ? updateQueue.lastEffect : null;
      if (null !== lastEffect) {
        var firstEffect = lastEffect.next;
        updateQueue = firstEffect;
        do {
          if ((updateQueue.tag & flags) === flags) {
            var inst = updateQueue.inst, destroy = inst.destroy;
            if (void 0 !== destroy) {
              inst.destroy = void 0;
              lastEffect = finishedWork;
              var nearestMountedAncestor = nearestMountedAncestor$jscomp$0, destroy_ = destroy;
              try {
                destroy_();
              } catch (error) {
                captureCommitPhaseError(
                  lastEffect,
                  nearestMountedAncestor,
                  error
                );
              }
            }
          }
          updateQueue = updateQueue.next;
        } while (updateQueue !== firstEffect);
      }
    } catch (error) {
      captureCommitPhaseError(finishedWork, finishedWork.return, error);
    }
  }
  function commitClassCallbacks(finishedWork) {
    var updateQueue = finishedWork.updateQueue;
    if (null !== updateQueue) {
      var instance = finishedWork.stateNode;
      try {
        commitCallbacks(updateQueue, instance);
      } catch (error) {
        captureCommitPhaseError(finishedWork, finishedWork.return, error);
      }
    }
  }
  function safelyCallComponentWillUnmount(current, nearestMountedAncestor, instance) {
    instance.props = resolveClassComponentProps(
      current.type,
      current.memoizedProps
    );
    instance.state = current.memoizedState;
    try {
      instance.componentWillUnmount();
    } catch (error) {
      captureCommitPhaseError(current, nearestMountedAncestor, error);
    }
  }
  function safelyAttachRef(current, nearestMountedAncestor) {
    try {
      var ref = current.ref;
      if (null !== ref) {
        switch (current.tag) {
          case 26:
          case 27:
          case 5:
            var instanceToUse = current.stateNode;
            break;
          case 30:
            instanceToUse = current.stateNode;
            break;
          default:
            instanceToUse = current.stateNode;
        }
        "function" === typeof ref ? current.refCleanup = ref(instanceToUse) : ref.current = instanceToUse;
      }
    } catch (error) {
      captureCommitPhaseError(current, nearestMountedAncestor, error);
    }
  }
  function safelyDetachRef(current, nearestMountedAncestor) {
    var ref = current.ref, refCleanup = current.refCleanup;
    if (null !== ref)
      if ("function" === typeof refCleanup)
        try {
          refCleanup();
        } catch (error) {
          captureCommitPhaseError(current, nearestMountedAncestor, error);
        } finally {
          current.refCleanup = null, current = current.alternate, null != current && (current.refCleanup = null);
        }
      else if ("function" === typeof ref)
        try {
          ref(null);
        } catch (error$140) {
          captureCommitPhaseError(current, nearestMountedAncestor, error$140);
        }
      else ref.current = null;
  }
  function commitHostMount(finishedWork) {
    var type = finishedWork.type, props = finishedWork.memoizedProps, instance = finishedWork.stateNode;
    try {
      a: switch (type) {
        case "button":
        case "input":
        case "select":
        case "textarea":
          props.autoFocus && instance.focus();
          break a;
        case "img":
          props.src ? instance.src = props.src : props.srcSet && (instance.srcset = props.srcSet);
      }
    } catch (error) {
      captureCommitPhaseError(finishedWork, finishedWork.return, error);
    }
  }
  function commitHostUpdate(finishedWork, newProps, oldProps) {
    try {
      var domElement = finishedWork.stateNode;
      updateProperties(domElement, finishedWork.type, oldProps, newProps);
      domElement[internalPropsKey] = newProps;
    } catch (error) {
      captureCommitPhaseError(finishedWork, finishedWork.return, error);
    }
  }
  function isHostParent(fiber) {
    return 5 === fiber.tag || 3 === fiber.tag || 26 === fiber.tag || 27 === fiber.tag && isSingletonScope(fiber.type) || 4 === fiber.tag;
  }
  function getHostSibling(fiber) {
    a: for (; ; ) {
      for (; null === fiber.sibling; ) {
        if (null === fiber.return || isHostParent(fiber.return)) return null;
        fiber = fiber.return;
      }
      fiber.sibling.return = fiber.return;
      for (fiber = fiber.sibling; 5 !== fiber.tag && 6 !== fiber.tag && 18 !== fiber.tag; ) {
        if (27 === fiber.tag && isSingletonScope(fiber.type)) continue a;
        if (fiber.flags & 2) continue a;
        if (null === fiber.child || 4 === fiber.tag) continue a;
        else fiber.child.return = fiber, fiber = fiber.child;
      }
      if (!(fiber.flags & 2)) return fiber.stateNode;
    }
  }
  function insertOrAppendPlacementNodeIntoContainer(node2, before, parent) {
    var tag = node2.tag;
    if (5 === tag || 6 === tag)
      node2 = node2.stateNode, before ? (9 === parent.nodeType ? parent.body : "HTML" === parent.nodeName ? parent.ownerDocument.body : parent).insertBefore(node2, before) : (before = 9 === parent.nodeType ? parent.body : "HTML" === parent.nodeName ? parent.ownerDocument.body : parent, before.appendChild(node2), parent = parent._reactRootContainer, null !== parent && void 0 !== parent || null !== before.onclick || (before.onclick = noop$1));
    else if (4 !== tag && (27 === tag && isSingletonScope(node2.type) && (parent = node2.stateNode, before = null), node2 = node2.child, null !== node2))
      for (insertOrAppendPlacementNodeIntoContainer(node2, before, parent), node2 = node2.sibling; null !== node2; )
        insertOrAppendPlacementNodeIntoContainer(node2, before, parent), node2 = node2.sibling;
  }
  function insertOrAppendPlacementNode(node2, before, parent) {
    var tag = node2.tag;
    if (5 === tag || 6 === tag)
      node2 = node2.stateNode, before ? parent.insertBefore(node2, before) : parent.appendChild(node2);
    else if (4 !== tag && (27 === tag && isSingletonScope(node2.type) && (parent = node2.stateNode), node2 = node2.child, null !== node2))
      for (insertOrAppendPlacementNode(node2, before, parent), node2 = node2.sibling; null !== node2; )
        insertOrAppendPlacementNode(node2, before, parent), node2 = node2.sibling;
  }
  function commitHostSingletonAcquisition(finishedWork) {
    var singleton = finishedWork.stateNode, props = finishedWork.memoizedProps;
    try {
      for (var type = finishedWork.type, attributes = singleton.attributes; attributes.length; )
        singleton.removeAttributeNode(attributes[0]);
      setInitialProperties(singleton, type, props);
      singleton[internalInstanceKey] = finishedWork;
      singleton[internalPropsKey] = props;
    } catch (error) {
      captureCommitPhaseError(finishedWork, finishedWork.return, error);
    }
  }
  var offscreenSubtreeIsHidden = false, offscreenSubtreeWasHidden = false, needsFormReset = false, PossiblyWeakSet = "function" === typeof WeakSet ? WeakSet : Set, nextEffect = null;
  function commitBeforeMutationEffects(root3, firstChild) {
    root3 = root3.containerInfo;
    eventsEnabled = _enabled;
    root3 = getActiveElementDeep(root3);
    if (hasSelectionCapabilities(root3)) {
      if ("selectionStart" in root3)
        var JSCompiler_temp = {
          start: root3.selectionStart,
          end: root3.selectionEnd
        };
      else
        a: {
          JSCompiler_temp = (JSCompiler_temp = root3.ownerDocument) && JSCompiler_temp.defaultView || window;
          var selection = JSCompiler_temp.getSelection && JSCompiler_temp.getSelection();
          if (selection && 0 !== selection.rangeCount) {
            JSCompiler_temp = selection.anchorNode;
            var anchorOffset = selection.anchorOffset, focusNode = selection.focusNode;
            selection = selection.focusOffset;
            try {
              JSCompiler_temp.nodeType, focusNode.nodeType;
            } catch (e$20) {
              JSCompiler_temp = null;
              break a;
            }
            var length = 0, start = -1, end = -1, indexWithinAnchor = 0, indexWithinFocus = 0, node2 = root3, parentNode = null;
            b: for (; ; ) {
              for (var next; ; ) {
                node2 !== JSCompiler_temp || 0 !== anchorOffset && 3 !== node2.nodeType || (start = length + anchorOffset);
                node2 !== focusNode || 0 !== selection && 3 !== node2.nodeType || (end = length + selection);
                3 === node2.nodeType && (length += node2.nodeValue.length);
                if (null === (next = node2.firstChild)) break;
                parentNode = node2;
                node2 = next;
              }
              for (; ; ) {
                if (node2 === root3) break b;
                parentNode === JSCompiler_temp && ++indexWithinAnchor === anchorOffset && (start = length);
                parentNode === focusNode && ++indexWithinFocus === selection && (end = length);
                if (null !== (next = node2.nextSibling)) break;
                node2 = parentNode;
                parentNode = node2.parentNode;
              }
              node2 = next;
            }
            JSCompiler_temp = -1 === start || -1 === end ? null : { start, end };
          } else JSCompiler_temp = null;
        }
      JSCompiler_temp = JSCompiler_temp || { start: 0, end: 0 };
    } else JSCompiler_temp = null;
    selectionInformation = { focusedElem: root3, selectionRange: JSCompiler_temp };
    _enabled = false;
    for (nextEffect = firstChild; null !== nextEffect; )
      if (firstChild = nextEffect, root3 = firstChild.child, 0 !== (firstChild.subtreeFlags & 1028) && null !== root3)
        root3.return = firstChild, nextEffect = root3;
      else
        for (; null !== nextEffect; ) {
          firstChild = nextEffect;
          focusNode = firstChild.alternate;
          root3 = firstChild.flags;
          switch (firstChild.tag) {
            case 0:
              if (0 !== (root3 & 4) && (root3 = firstChild.updateQueue, root3 = null !== root3 ? root3.events : null, null !== root3))
                for (JSCompiler_temp = 0; JSCompiler_temp < root3.length; JSCompiler_temp++)
                  anchorOffset = root3[JSCompiler_temp], anchorOffset.ref.impl = anchorOffset.nextImpl;
              break;
            case 11:
            case 15:
              break;
            case 1:
              if (0 !== (root3 & 1024) && null !== focusNode) {
                root3 = void 0;
                JSCompiler_temp = firstChild;
                anchorOffset = focusNode.memoizedProps;
                focusNode = focusNode.memoizedState;
                selection = JSCompiler_temp.stateNode;
                try {
                  var resolvedPrevProps = resolveClassComponentProps(
                    JSCompiler_temp.type,
                    anchorOffset
                  );
                  root3 = selection.getSnapshotBeforeUpdate(
                    resolvedPrevProps,
                    focusNode
                  );
                  selection.__reactInternalSnapshotBeforeUpdate = root3;
                } catch (error) {
                  captureCommitPhaseError(
                    JSCompiler_temp,
                    JSCompiler_temp.return,
                    error
                  );
                }
              }
              break;
            case 3:
              if (0 !== (root3 & 1024)) {
                if (root3 = firstChild.stateNode.containerInfo, JSCompiler_temp = root3.nodeType, 9 === JSCompiler_temp)
                  clearContainerSparingly(root3);
                else if (1 === JSCompiler_temp)
                  switch (root3.nodeName) {
                    case "HEAD":
                    case "HTML":
                    case "BODY":
                      clearContainerSparingly(root3);
                      break;
                    default:
                      root3.textContent = "";
                  }
              }
              break;
            case 5:
            case 26:
            case 27:
            case 6:
            case 4:
            case 17:
              break;
            default:
              if (0 !== (root3 & 1024)) throw Error(formatProdErrorMessage(163));
          }
          root3 = firstChild.sibling;
          if (null !== root3) {
            root3.return = firstChild.return;
            nextEffect = root3;
            break;
          }
          nextEffect = firstChild.return;
        }
  }
  function commitLayoutEffectOnFiber(finishedRoot, current, finishedWork) {
    var flags = finishedWork.flags;
    switch (finishedWork.tag) {
      case 0:
      case 11:
      case 15:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        flags & 4 && commitHookEffectListMount(5, finishedWork);
        break;
      case 1:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        if (flags & 4)
          if (finishedRoot = finishedWork.stateNode, null === current)
            try {
              finishedRoot.componentDidMount();
            } catch (error) {
              captureCommitPhaseError(finishedWork, finishedWork.return, error);
            }
          else {
            var prevProps = resolveClassComponentProps(
              finishedWork.type,
              current.memoizedProps
            );
            current = current.memoizedState;
            try {
              finishedRoot.componentDidUpdate(
                prevProps,
                current,
                finishedRoot.__reactInternalSnapshotBeforeUpdate
              );
            } catch (error$139) {
              captureCommitPhaseError(
                finishedWork,
                finishedWork.return,
                error$139
              );
            }
          }
        flags & 64 && commitClassCallbacks(finishedWork);
        flags & 512 && safelyAttachRef(finishedWork, finishedWork.return);
        break;
      case 3:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        if (flags & 64 && (finishedRoot = finishedWork.updateQueue, null !== finishedRoot)) {
          current = null;
          if (null !== finishedWork.child)
            switch (finishedWork.child.tag) {
              case 27:
              case 5:
                current = finishedWork.child.stateNode;
                break;
              case 1:
                current = finishedWork.child.stateNode;
            }
          try {
            commitCallbacks(finishedRoot, current);
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        }
        break;
      case 27:
        null === current && flags & 4 && commitHostSingletonAcquisition(finishedWork);
      case 26:
      case 5:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        null === current && flags & 4 && commitHostMount(finishedWork);
        flags & 512 && safelyAttachRef(finishedWork, finishedWork.return);
        break;
      case 12:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        break;
      case 31:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        flags & 4 && commitActivityHydrationCallbacks(finishedRoot, finishedWork);
        break;
      case 13:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        flags & 4 && commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
        flags & 64 && (finishedRoot = finishedWork.memoizedState, null !== finishedRoot && (finishedRoot = finishedRoot.dehydrated, null !== finishedRoot && (finishedWork = retryDehydratedSuspenseBoundary.bind(
          null,
          finishedWork
        ), registerSuspenseInstanceRetry(finishedRoot, finishedWork))));
        break;
      case 22:
        flags = null !== finishedWork.memoizedState || offscreenSubtreeIsHidden;
        if (!flags) {
          current = null !== current && null !== current.memoizedState || offscreenSubtreeWasHidden;
          prevProps = offscreenSubtreeIsHidden;
          var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
          offscreenSubtreeIsHidden = flags;
          (offscreenSubtreeWasHidden = current) && !prevOffscreenSubtreeWasHidden ? recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            0 !== (finishedWork.subtreeFlags & 8772)
          ) : recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
          offscreenSubtreeIsHidden = prevProps;
          offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
        }
        break;
      case 30:
        break;
      default:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
    }
  }
  function detachFiberAfterEffects(fiber) {
    var alternate = fiber.alternate;
    null !== alternate && (fiber.alternate = null, detachFiberAfterEffects(alternate));
    fiber.child = null;
    fiber.deletions = null;
    fiber.sibling = null;
    5 === fiber.tag && (alternate = fiber.stateNode, null !== alternate && detachDeletedInstance(alternate));
    fiber.stateNode = null;
    fiber.return = null;
    fiber.dependencies = null;
    fiber.memoizedProps = null;
    fiber.memoizedState = null;
    fiber.pendingProps = null;
    fiber.stateNode = null;
    fiber.updateQueue = null;
  }
  var hostParent = null, hostParentIsContainer = false;
  function recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, parent) {
    for (parent = parent.child; null !== parent; )
      commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, parent), parent = parent.sibling;
  }
  function commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, deletedFiber) {
    if (injectedHook && "function" === typeof injectedHook.onCommitFiberUnmount)
      try {
        injectedHook.onCommitFiberUnmount(rendererID, deletedFiber);
      } catch (err) {
      }
    switch (deletedFiber.tag) {
      case 26:
        offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        deletedFiber.memoizedState ? deletedFiber.memoizedState.count-- : deletedFiber.stateNode && (deletedFiber = deletedFiber.stateNode, deletedFiber.parentNode.removeChild(deletedFiber));
        break;
      case 27:
        offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
        var prevHostParent = hostParent, prevHostParentIsContainer = hostParentIsContainer;
        isSingletonScope(deletedFiber.type) && (hostParent = deletedFiber.stateNode, hostParentIsContainer = false);
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        releaseSingletonInstance(deletedFiber.stateNode);
        hostParent = prevHostParent;
        hostParentIsContainer = prevHostParentIsContainer;
        break;
      case 5:
        offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
      case 6:
        prevHostParent = hostParent;
        prevHostParentIsContainer = hostParentIsContainer;
        hostParent = null;
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        hostParent = prevHostParent;
        hostParentIsContainer = prevHostParentIsContainer;
        if (null !== hostParent)
          if (hostParentIsContainer)
            try {
              (9 === hostParent.nodeType ? hostParent.body : "HTML" === hostParent.nodeName ? hostParent.ownerDocument.body : hostParent).removeChild(deletedFiber.stateNode);
            } catch (error) {
              captureCommitPhaseError(
                deletedFiber,
                nearestMountedAncestor,
                error
              );
            }
          else
            try {
              hostParent.removeChild(deletedFiber.stateNode);
            } catch (error) {
              captureCommitPhaseError(
                deletedFiber,
                nearestMountedAncestor,
                error
              );
            }
        break;
      case 18:
        null !== hostParent && (hostParentIsContainer ? (finishedRoot = hostParent, clearHydrationBoundary(
          9 === finishedRoot.nodeType ? finishedRoot.body : "HTML" === finishedRoot.nodeName ? finishedRoot.ownerDocument.body : finishedRoot,
          deletedFiber.stateNode
        ), retryIfBlockedOn(finishedRoot)) : clearHydrationBoundary(hostParent, deletedFiber.stateNode));
        break;
      case 4:
        prevHostParent = hostParent;
        prevHostParentIsContainer = hostParentIsContainer;
        hostParent = deletedFiber.stateNode.containerInfo;
        hostParentIsContainer = true;
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        hostParent = prevHostParent;
        hostParentIsContainer = prevHostParentIsContainer;
        break;
      case 0:
      case 11:
      case 14:
      case 15:
        commitHookEffectListUnmount(2, deletedFiber, nearestMountedAncestor);
        offscreenSubtreeWasHidden || commitHookEffectListUnmount(4, deletedFiber, nearestMountedAncestor);
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        break;
      case 1:
        offscreenSubtreeWasHidden || (safelyDetachRef(deletedFiber, nearestMountedAncestor), prevHostParent = deletedFiber.stateNode, "function" === typeof prevHostParent.componentWillUnmount && safelyCallComponentWillUnmount(
          deletedFiber,
          nearestMountedAncestor,
          prevHostParent
        ));
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        break;
      case 21:
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        break;
      case 22:
        offscreenSubtreeWasHidden = (prevHostParent = offscreenSubtreeWasHidden) || null !== deletedFiber.memoizedState;
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        offscreenSubtreeWasHidden = prevHostParent;
        break;
      default:
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
    }
  }
  function commitActivityHydrationCallbacks(finishedRoot, finishedWork) {
    if (null === finishedWork.memoizedState && (finishedRoot = finishedWork.alternate, null !== finishedRoot && (finishedRoot = finishedRoot.memoizedState, null !== finishedRoot))) {
      finishedRoot = finishedRoot.dehydrated;
      try {
        retryIfBlockedOn(finishedRoot);
      } catch (error) {
        captureCommitPhaseError(finishedWork, finishedWork.return, error);
      }
    }
  }
  function commitSuspenseHydrationCallbacks(finishedRoot, finishedWork) {
    if (null === finishedWork.memoizedState && (finishedRoot = finishedWork.alternate, null !== finishedRoot && (finishedRoot = finishedRoot.memoizedState, null !== finishedRoot && (finishedRoot = finishedRoot.dehydrated, null !== finishedRoot))))
      try {
        retryIfBlockedOn(finishedRoot);
      } catch (error) {
        captureCommitPhaseError(finishedWork, finishedWork.return, error);
      }
  }
  function getRetryCache(finishedWork) {
    switch (finishedWork.tag) {
      case 31:
      case 13:
      case 19:
        var retryCache = finishedWork.stateNode;
        null === retryCache && (retryCache = finishedWork.stateNode = new PossiblyWeakSet());
        return retryCache;
      case 22:
        return finishedWork = finishedWork.stateNode, retryCache = finishedWork._retryCache, null === retryCache && (retryCache = finishedWork._retryCache = new PossiblyWeakSet()), retryCache;
      default:
        throw Error(formatProdErrorMessage(435, finishedWork.tag));
    }
  }
  function attachSuspenseRetryListeners(finishedWork, wakeables) {
    var retryCache = getRetryCache(finishedWork);
    wakeables.forEach(function(wakeable) {
      if (!retryCache.has(wakeable)) {
        retryCache.add(wakeable);
        var retry = resolveRetryWakeable.bind(null, finishedWork, wakeable);
        wakeable.then(retry, retry);
      }
    });
  }
  function recursivelyTraverseMutationEffects(root$jscomp$0, parentFiber) {
    var deletions = parentFiber.deletions;
    if (null !== deletions)
      for (var i = 0; i < deletions.length; i++) {
        var childToDelete = deletions[i], root3 = root$jscomp$0, returnFiber = parentFiber, parent = returnFiber;
        a: for (; null !== parent; ) {
          switch (parent.tag) {
            case 27:
              if (isSingletonScope(parent.type)) {
                hostParent = parent.stateNode;
                hostParentIsContainer = false;
                break a;
              }
              break;
            case 5:
              hostParent = parent.stateNode;
              hostParentIsContainer = false;
              break a;
            case 3:
            case 4:
              hostParent = parent.stateNode.containerInfo;
              hostParentIsContainer = true;
              break a;
          }
          parent = parent.return;
        }
        if (null === hostParent) throw Error(formatProdErrorMessage(160));
        commitDeletionEffectsOnFiber(root3, returnFiber, childToDelete);
        hostParent = null;
        hostParentIsContainer = false;
        root3 = childToDelete.alternate;
        null !== root3 && (root3.return = null);
        childToDelete.return = null;
      }
    if (parentFiber.subtreeFlags & 13886)
      for (parentFiber = parentFiber.child; null !== parentFiber; )
        commitMutationEffectsOnFiber(parentFiber, root$jscomp$0), parentFiber = parentFiber.sibling;
  }
  var currentHoistableRoot = null;
  function commitMutationEffectsOnFiber(finishedWork, root3) {
    var current = finishedWork.alternate, flags = finishedWork.flags;
    switch (finishedWork.tag) {
      case 0:
      case 11:
      case 14:
      case 15:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 4 && (commitHookEffectListUnmount(3, finishedWork, finishedWork.return), commitHookEffectListMount(3, finishedWork), commitHookEffectListUnmount(5, finishedWork, finishedWork.return));
        break;
      case 1:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
        flags & 64 && offscreenSubtreeIsHidden && (finishedWork = finishedWork.updateQueue, null !== finishedWork && (flags = finishedWork.callbacks, null !== flags && (current = finishedWork.shared.hiddenCallbacks, finishedWork.shared.hiddenCallbacks = null === current ? flags : current.concat(flags))));
        break;
      case 26:
        var hoistableRoot = currentHoistableRoot;
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
        if (flags & 4) {
          var currentResource = null !== current ? current.memoizedState : null;
          flags = finishedWork.memoizedState;
          if (null === current)
            if (null === flags)
              if (null === finishedWork.stateNode) {
                a: {
                  flags = finishedWork.type;
                  current = finishedWork.memoizedProps;
                  hoistableRoot = hoistableRoot.ownerDocument || hoistableRoot;
                  b: switch (flags) {
                    case "title":
                      currentResource = hoistableRoot.getElementsByTagName("title")[0];
                      if (!currentResource || currentResource[internalHoistableMarker] || currentResource[internalInstanceKey] || "http://www.w3.org/2000/svg" === currentResource.namespaceURI || currentResource.hasAttribute("itemprop"))
                        currentResource = hoistableRoot.createElement(flags), hoistableRoot.head.insertBefore(
                          currentResource,
                          hoistableRoot.querySelector("head > title")
                        );
                      setInitialProperties(currentResource, flags, current);
                      currentResource[internalInstanceKey] = finishedWork;
                      markNodeAsHoistable(currentResource);
                      flags = currentResource;
                      break a;
                    case "link":
                      var maybeNodes = getHydratableHoistableCache(
                        "link",
                        "href",
                        hoistableRoot
                      ).get(flags + (current.href || ""));
                      if (maybeNodes) {
                        for (var i = 0; i < maybeNodes.length; i++)
                          if (currentResource = maybeNodes[i], currentResource.getAttribute("href") === (null == current.href || "" === current.href ? null : current.href) && currentResource.getAttribute("rel") === (null == current.rel ? null : current.rel) && currentResource.getAttribute("title") === (null == current.title ? null : current.title) && currentResource.getAttribute("crossorigin") === (null == current.crossOrigin ? null : current.crossOrigin)) {
                            maybeNodes.splice(i, 1);
                            break b;
                          }
                      }
                      currentResource = hoistableRoot.createElement(flags);
                      setInitialProperties(currentResource, flags, current);
                      hoistableRoot.head.appendChild(currentResource);
                      break;
                    case "meta":
                      if (maybeNodes = getHydratableHoistableCache(
                        "meta",
                        "content",
                        hoistableRoot
                      ).get(flags + (current.content || ""))) {
                        for (i = 0; i < maybeNodes.length; i++)
                          if (currentResource = maybeNodes[i], currentResource.getAttribute("content") === (null == current.content ? null : "" + current.content) && currentResource.getAttribute("name") === (null == current.name ? null : current.name) && currentResource.getAttribute("property") === (null == current.property ? null : current.property) && currentResource.getAttribute("http-equiv") === (null == current.httpEquiv ? null : current.httpEquiv) && currentResource.getAttribute("charset") === (null == current.charSet ? null : current.charSet)) {
                            maybeNodes.splice(i, 1);
                            break b;
                          }
                      }
                      currentResource = hoistableRoot.createElement(flags);
                      setInitialProperties(currentResource, flags, current);
                      hoistableRoot.head.appendChild(currentResource);
                      break;
                    default:
                      throw Error(formatProdErrorMessage(468, flags));
                  }
                  currentResource[internalInstanceKey] = finishedWork;
                  markNodeAsHoistable(currentResource);
                  flags = currentResource;
                }
                finishedWork.stateNode = flags;
              } else
                mountHoistable(
                  hoistableRoot,
                  finishedWork.type,
                  finishedWork.stateNode
                );
            else
              finishedWork.stateNode = acquireResource(
                hoistableRoot,
                flags,
                finishedWork.memoizedProps
              );
          else
            currentResource !== flags ? (null === currentResource ? null !== current.stateNode && (current = current.stateNode, current.parentNode.removeChild(current)) : currentResource.count--, null === flags ? mountHoistable(
              hoistableRoot,
              finishedWork.type,
              finishedWork.stateNode
            ) : acquireResource(
              hoistableRoot,
              flags,
              finishedWork.memoizedProps
            )) : null === flags && null !== finishedWork.stateNode && commitHostUpdate(
              finishedWork,
              finishedWork.memoizedProps,
              current.memoizedProps
            );
        }
        break;
      case 27:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
        null !== current && flags & 4 && commitHostUpdate(
          finishedWork,
          finishedWork.memoizedProps,
          current.memoizedProps
        );
        break;
      case 5:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
        if (finishedWork.flags & 32) {
          hoistableRoot = finishedWork.stateNode;
          try {
            setTextContent(hoistableRoot, "");
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        }
        flags & 4 && null != finishedWork.stateNode && (hoistableRoot = finishedWork.memoizedProps, commitHostUpdate(
          finishedWork,
          hoistableRoot,
          null !== current ? current.memoizedProps : hoistableRoot
        ));
        flags & 1024 && (needsFormReset = true);
        break;
      case 6:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        if (flags & 4) {
          if (null === finishedWork.stateNode)
            throw Error(formatProdErrorMessage(162));
          flags = finishedWork.memoizedProps;
          current = finishedWork.stateNode;
          try {
            current.nodeValue = flags;
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        }
        break;
      case 3:
        tagCaches = null;
        hoistableRoot = currentHoistableRoot;
        currentHoistableRoot = getHoistableRoot(root3.containerInfo);
        recursivelyTraverseMutationEffects(root3, finishedWork);
        currentHoistableRoot = hoistableRoot;
        commitReconciliationEffects(finishedWork);
        if (flags & 4 && null !== current && current.memoizedState.isDehydrated)
          try {
            retryIfBlockedOn(root3.containerInfo);
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        needsFormReset && (needsFormReset = false, recursivelyResetForms(finishedWork));
        break;
      case 4:
        flags = currentHoistableRoot;
        currentHoistableRoot = getHoistableRoot(
          finishedWork.stateNode.containerInfo
        );
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        currentHoistableRoot = flags;
        break;
      case 12:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        break;
      case 31:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
        break;
      case 13:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        finishedWork.child.flags & 8192 && null !== finishedWork.memoizedState !== (null !== current && null !== current.memoizedState) && (globalMostRecentFallbackTime = now());
        flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
        break;
      case 22:
        hoistableRoot = null !== finishedWork.memoizedState;
        var wasHidden = null !== current && null !== current.memoizedState, prevOffscreenSubtreeIsHidden = offscreenSubtreeIsHidden, prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
        offscreenSubtreeIsHidden = prevOffscreenSubtreeIsHidden || hoistableRoot;
        offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden || wasHidden;
        recursivelyTraverseMutationEffects(root3, finishedWork);
        offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
        offscreenSubtreeIsHidden = prevOffscreenSubtreeIsHidden;
        commitReconciliationEffects(finishedWork);
        if (flags & 8192)
          a: for (root3 = finishedWork.stateNode, root3._visibility = hoistableRoot ? root3._visibility & -2 : root3._visibility | 1, hoistableRoot && (null === current || wasHidden || offscreenSubtreeIsHidden || offscreenSubtreeWasHidden || recursivelyTraverseDisappearLayoutEffects(finishedWork)), current = null, root3 = finishedWork; ; ) {
            if (5 === root3.tag || 26 === root3.tag) {
              if (null === current) {
                wasHidden = current = root3;
                try {
                  if (currentResource = wasHidden.stateNode, hoistableRoot)
                    maybeNodes = currentResource.style, "function" === typeof maybeNodes.setProperty ? maybeNodes.setProperty("display", "none", "important") : maybeNodes.display = "none";
                  else {
                    i = wasHidden.stateNode;
                    var styleProp = wasHidden.memoizedProps.style, display = void 0 !== styleProp && null !== styleProp && styleProp.hasOwnProperty("display") ? styleProp.display : null;
                    i.style.display = null == display || "boolean" === typeof display ? "" : ("" + display).trim();
                  }
                } catch (error) {
                  captureCommitPhaseError(wasHidden, wasHidden.return, error);
                }
              }
            } else if (6 === root3.tag) {
              if (null === current) {
                wasHidden = root3;
                try {
                  wasHidden.stateNode.nodeValue = hoistableRoot ? "" : wasHidden.memoizedProps;
                } catch (error) {
                  captureCommitPhaseError(wasHidden, wasHidden.return, error);
                }
              }
            } else if (18 === root3.tag) {
              if (null === current) {
                wasHidden = root3;
                try {
                  var instance = wasHidden.stateNode;
                  hoistableRoot ? hideOrUnhideDehydratedBoundary(instance, true) : hideOrUnhideDehydratedBoundary(wasHidden.stateNode, false);
                } catch (error) {
                  captureCommitPhaseError(wasHidden, wasHidden.return, error);
                }
              }
            } else if ((22 !== root3.tag && 23 !== root3.tag || null === root3.memoizedState || root3 === finishedWork) && null !== root3.child) {
              root3.child.return = root3;
              root3 = root3.child;
              continue;
            }
            if (root3 === finishedWork) break a;
            for (; null === root3.sibling; ) {
              if (null === root3.return || root3.return === finishedWork) break a;
              current === root3 && (current = null);
              root3 = root3.return;
            }
            current === root3 && (current = null);
            root3.sibling.return = root3.return;
            root3 = root3.sibling;
          }
        flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (current = flags.retryQueue, null !== current && (flags.retryQueue = null, attachSuspenseRetryListeners(finishedWork, current))));
        break;
      case 19:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
        break;
      case 30:
        break;
      case 21:
        break;
      default:
        recursivelyTraverseMutationEffects(root3, finishedWork), commitReconciliationEffects(finishedWork);
    }
  }
  function commitReconciliationEffects(finishedWork) {
    var flags = finishedWork.flags;
    if (flags & 2) {
      try {
        for (var hostParentFiber, parentFiber = finishedWork.return; null !== parentFiber; ) {
          if (isHostParent(parentFiber)) {
            hostParentFiber = parentFiber;
            break;
          }
          parentFiber = parentFiber.return;
        }
        if (null == hostParentFiber) throw Error(formatProdErrorMessage(160));
        switch (hostParentFiber.tag) {
          case 27:
            var parent = hostParentFiber.stateNode, before = getHostSibling(finishedWork);
            insertOrAppendPlacementNode(finishedWork, before, parent);
            break;
          case 5:
            var parent$141 = hostParentFiber.stateNode;
            hostParentFiber.flags & 32 && (setTextContent(parent$141, ""), hostParentFiber.flags &= -33);
            var before$142 = getHostSibling(finishedWork);
            insertOrAppendPlacementNode(finishedWork, before$142, parent$141);
            break;
          case 3:
          case 4:
            var parent$143 = hostParentFiber.stateNode.containerInfo, before$144 = getHostSibling(finishedWork);
            insertOrAppendPlacementNodeIntoContainer(
              finishedWork,
              before$144,
              parent$143
            );
            break;
          default:
            throw Error(formatProdErrorMessage(161));
        }
      } catch (error) {
        captureCommitPhaseError(finishedWork, finishedWork.return, error);
      }
      finishedWork.flags &= -3;
    }
    flags & 4096 && (finishedWork.flags &= -4097);
  }
  function recursivelyResetForms(parentFiber) {
    if (parentFiber.subtreeFlags & 1024)
      for (parentFiber = parentFiber.child; null !== parentFiber; ) {
        var fiber = parentFiber;
        recursivelyResetForms(fiber);
        5 === fiber.tag && fiber.flags & 1024 && fiber.stateNode.reset();
        parentFiber = parentFiber.sibling;
      }
  }
  function recursivelyTraverseLayoutEffects(root3, parentFiber) {
    if (parentFiber.subtreeFlags & 8772)
      for (parentFiber = parentFiber.child; null !== parentFiber; )
        commitLayoutEffectOnFiber(root3, parentFiber.alternate, parentFiber), parentFiber = parentFiber.sibling;
  }
  function recursivelyTraverseDisappearLayoutEffects(parentFiber) {
    for (parentFiber = parentFiber.child; null !== parentFiber; ) {
      var finishedWork = parentFiber;
      switch (finishedWork.tag) {
        case 0:
        case 11:
        case 14:
        case 15:
          commitHookEffectListUnmount(4, finishedWork, finishedWork.return);
          recursivelyTraverseDisappearLayoutEffects(finishedWork);
          break;
        case 1:
          safelyDetachRef(finishedWork, finishedWork.return);
          var instance = finishedWork.stateNode;
          "function" === typeof instance.componentWillUnmount && safelyCallComponentWillUnmount(
            finishedWork,
            finishedWork.return,
            instance
          );
          recursivelyTraverseDisappearLayoutEffects(finishedWork);
          break;
        case 27:
          releaseSingletonInstance(finishedWork.stateNode);
        case 26:
        case 5:
          safelyDetachRef(finishedWork, finishedWork.return);
          recursivelyTraverseDisappearLayoutEffects(finishedWork);
          break;
        case 22:
          null === finishedWork.memoizedState && recursivelyTraverseDisappearLayoutEffects(finishedWork);
          break;
        case 30:
          recursivelyTraverseDisappearLayoutEffects(finishedWork);
          break;
        default:
          recursivelyTraverseDisappearLayoutEffects(finishedWork);
      }
      parentFiber = parentFiber.sibling;
    }
  }
  function recursivelyTraverseReappearLayoutEffects(finishedRoot$jscomp$0, parentFiber, includeWorkInProgressEffects) {
    includeWorkInProgressEffects = includeWorkInProgressEffects && 0 !== (parentFiber.subtreeFlags & 8772);
    for (parentFiber = parentFiber.child; null !== parentFiber; ) {
      var current = parentFiber.alternate, finishedRoot = finishedRoot$jscomp$0, finishedWork = parentFiber, flags = finishedWork.flags;
      switch (finishedWork.tag) {
        case 0:
        case 11:
        case 15:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          commitHookEffectListMount(4, finishedWork);
          break;
        case 1:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          current = finishedWork;
          finishedRoot = current.stateNode;
          if ("function" === typeof finishedRoot.componentDidMount)
            try {
              finishedRoot.componentDidMount();
            } catch (error) {
              captureCommitPhaseError(current, current.return, error);
            }
          current = finishedWork;
          finishedRoot = current.updateQueue;
          if (null !== finishedRoot) {
            var instance = current.stateNode;
            try {
              var hiddenCallbacks = finishedRoot.shared.hiddenCallbacks;
              if (null !== hiddenCallbacks)
                for (finishedRoot.shared.hiddenCallbacks = null, finishedRoot = 0; finishedRoot < hiddenCallbacks.length; finishedRoot++)
                  callCallback(hiddenCallbacks[finishedRoot], instance);
            } catch (error) {
              captureCommitPhaseError(current, current.return, error);
            }
          }
          includeWorkInProgressEffects && flags & 64 && commitClassCallbacks(finishedWork);
          safelyAttachRef(finishedWork, finishedWork.return);
          break;
        case 27:
          commitHostSingletonAcquisition(finishedWork);
        case 26:
        case 5:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          includeWorkInProgressEffects && null === current && flags & 4 && commitHostMount(finishedWork);
          safelyAttachRef(finishedWork, finishedWork.return);
          break;
        case 12:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          break;
        case 31:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          includeWorkInProgressEffects && flags & 4 && commitActivityHydrationCallbacks(finishedRoot, finishedWork);
          break;
        case 13:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          includeWorkInProgressEffects && flags & 4 && commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
          break;
        case 22:
          null === finishedWork.memoizedState && recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          safelyAttachRef(finishedWork, finishedWork.return);
          break;
        case 30:
          break;
        default:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
      }
      parentFiber = parentFiber.sibling;
    }
  }
  function commitOffscreenPassiveMountEffects(current, finishedWork) {
    var previousCache = null;
    null !== current && null !== current.memoizedState && null !== current.memoizedState.cachePool && (previousCache = current.memoizedState.cachePool.pool);
    current = null;
    null !== finishedWork.memoizedState && null !== finishedWork.memoizedState.cachePool && (current = finishedWork.memoizedState.cachePool.pool);
    current !== previousCache && (null != current && current.refCount++, null != previousCache && releaseCache(previousCache));
  }
  function commitCachePassiveMountEffect(current, finishedWork) {
    current = null;
    null !== finishedWork.alternate && (current = finishedWork.alternate.memoizedState.cache);
    finishedWork = finishedWork.memoizedState.cache;
    finishedWork !== current && (finishedWork.refCount++, null != current && releaseCache(current));
  }
  function recursivelyTraversePassiveMountEffects(root3, parentFiber, committedLanes, committedTransitions) {
    if (parentFiber.subtreeFlags & 10256)
      for (parentFiber = parentFiber.child; null !== parentFiber; )
        commitPassiveMountOnFiber(
          root3,
          parentFiber,
          committedLanes,
          committedTransitions
        ), parentFiber = parentFiber.sibling;
  }
  function commitPassiveMountOnFiber(finishedRoot, finishedWork, committedLanes, committedTransitions) {
    var flags = finishedWork.flags;
    switch (finishedWork.tag) {
      case 0:
      case 11:
      case 15:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        flags & 2048 && commitHookEffectListMount(9, finishedWork);
        break;
      case 1:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        break;
      case 3:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        flags & 2048 && (finishedRoot = null, null !== finishedWork.alternate && (finishedRoot = finishedWork.alternate.memoizedState.cache), finishedWork = finishedWork.memoizedState.cache, finishedWork !== finishedRoot && (finishedWork.refCount++, null != finishedRoot && releaseCache(finishedRoot)));
        break;
      case 12:
        if (flags & 2048) {
          recursivelyTraversePassiveMountEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions
          );
          finishedRoot = finishedWork.stateNode;
          try {
            var _finishedWork$memoize2 = finishedWork.memoizedProps, id2 = _finishedWork$memoize2.id, onPostCommit = _finishedWork$memoize2.onPostCommit;
            "function" === typeof onPostCommit && onPostCommit(
              id2,
              null === finishedWork.alternate ? "mount" : "update",
              finishedRoot.passiveEffectDuration,
              -0
            );
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        } else
          recursivelyTraversePassiveMountEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions
          );
        break;
      case 31:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        break;
      case 13:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        break;
      case 23:
        break;
      case 22:
        _finishedWork$memoize2 = finishedWork.stateNode;
        id2 = finishedWork.alternate;
        null !== finishedWork.memoizedState ? _finishedWork$memoize2._visibility & 2 ? recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        ) : recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork) : _finishedWork$memoize2._visibility & 2 ? recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        ) : (_finishedWork$memoize2._visibility |= 2, recursivelyTraverseReconnectPassiveEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions,
          0 !== (finishedWork.subtreeFlags & 10256) || false
        ));
        flags & 2048 && commitOffscreenPassiveMountEffects(id2, finishedWork);
        break;
      case 24:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        flags & 2048 && commitCachePassiveMountEffect(finishedWork.alternate, finishedWork);
        break;
      default:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
    }
  }
  function recursivelyTraverseReconnectPassiveEffects(finishedRoot$jscomp$0, parentFiber, committedLanes$jscomp$0, committedTransitions$jscomp$0, includeWorkInProgressEffects) {
    includeWorkInProgressEffects = includeWorkInProgressEffects && (0 !== (parentFiber.subtreeFlags & 10256) || false);
    for (parentFiber = parentFiber.child; null !== parentFiber; ) {
      var finishedRoot = finishedRoot$jscomp$0, finishedWork = parentFiber, committedLanes = committedLanes$jscomp$0, committedTransitions = committedTransitions$jscomp$0, flags = finishedWork.flags;
      switch (finishedWork.tag) {
        case 0:
        case 11:
        case 15:
          recursivelyTraverseReconnectPassiveEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions,
            includeWorkInProgressEffects
          );
          commitHookEffectListMount(8, finishedWork);
          break;
        case 23:
          break;
        case 22:
          var instance = finishedWork.stateNode;
          null !== finishedWork.memoizedState ? instance._visibility & 2 ? recursivelyTraverseReconnectPassiveEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions,
            includeWorkInProgressEffects
          ) : recursivelyTraverseAtomicPassiveEffects(
            finishedRoot,
            finishedWork
          ) : (instance._visibility |= 2, recursivelyTraverseReconnectPassiveEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions,
            includeWorkInProgressEffects
          ));
          includeWorkInProgressEffects && flags & 2048 && commitOffscreenPassiveMountEffects(
            finishedWork.alternate,
            finishedWork
          );
          break;
        case 24:
          recursivelyTraverseReconnectPassiveEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions,
            includeWorkInProgressEffects
          );
          includeWorkInProgressEffects && flags & 2048 && commitCachePassiveMountEffect(finishedWork.alternate, finishedWork);
          break;
        default:
          recursivelyTraverseReconnectPassiveEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions,
            includeWorkInProgressEffects
          );
      }
      parentFiber = parentFiber.sibling;
    }
  }
  function recursivelyTraverseAtomicPassiveEffects(finishedRoot$jscomp$0, parentFiber) {
    if (parentFiber.subtreeFlags & 10256)
      for (parentFiber = parentFiber.child; null !== parentFiber; ) {
        var finishedRoot = finishedRoot$jscomp$0, finishedWork = parentFiber, flags = finishedWork.flags;
        switch (finishedWork.tag) {
          case 22:
            recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork);
            flags & 2048 && commitOffscreenPassiveMountEffects(
              finishedWork.alternate,
              finishedWork
            );
            break;
          case 24:
            recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork);
            flags & 2048 && commitCachePassiveMountEffect(finishedWork.alternate, finishedWork);
            break;
          default:
            recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork);
        }
        parentFiber = parentFiber.sibling;
      }
  }
  var suspenseyCommitFlag = 8192;
  function recursivelyAccumulateSuspenseyCommit(parentFiber, committedLanes, suspendedState) {
    if (parentFiber.subtreeFlags & suspenseyCommitFlag)
      for (parentFiber = parentFiber.child; null !== parentFiber; )
        accumulateSuspenseyCommitOnFiber(
          parentFiber,
          committedLanes,
          suspendedState
        ), parentFiber = parentFiber.sibling;
  }
  function accumulateSuspenseyCommitOnFiber(fiber, committedLanes, suspendedState) {
    switch (fiber.tag) {
      case 26:
        recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        );
        fiber.flags & suspenseyCommitFlag && null !== fiber.memoizedState && suspendResource(
          suspendedState,
          currentHoistableRoot,
          fiber.memoizedState,
          fiber.memoizedProps
        );
        break;
      case 5:
        recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        );
        break;
      case 3:
      case 4:
        var previousHoistableRoot = currentHoistableRoot;
        currentHoistableRoot = getHoistableRoot(fiber.stateNode.containerInfo);
        recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        );
        currentHoistableRoot = previousHoistableRoot;
        break;
      case 22:
        null === fiber.memoizedState && (previousHoistableRoot = fiber.alternate, null !== previousHoistableRoot && null !== previousHoistableRoot.memoizedState ? (previousHoistableRoot = suspenseyCommitFlag, suspenseyCommitFlag = 16777216, recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        ), suspenseyCommitFlag = previousHoistableRoot) : recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        ));
        break;
      default:
        recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        );
    }
  }
  function detachAlternateSiblings(parentFiber) {
    var previousFiber = parentFiber.alternate;
    if (null !== previousFiber && (parentFiber = previousFiber.child, null !== parentFiber)) {
      previousFiber.child = null;
      do
        previousFiber = parentFiber.sibling, parentFiber.sibling = null, parentFiber = previousFiber;
      while (null !== parentFiber);
    }
  }
  function recursivelyTraversePassiveUnmountEffects(parentFiber) {
    var deletions = parentFiber.deletions;
    if (0 !== (parentFiber.flags & 16)) {
      if (null !== deletions)
        for (var i = 0; i < deletions.length; i++) {
          var childToDelete = deletions[i];
          nextEffect = childToDelete;
          commitPassiveUnmountEffectsInsideOfDeletedTree_begin(
            childToDelete,
            parentFiber
          );
        }
      detachAlternateSiblings(parentFiber);
    }
    if (parentFiber.subtreeFlags & 10256)
      for (parentFiber = parentFiber.child; null !== parentFiber; )
        commitPassiveUnmountOnFiber(parentFiber), parentFiber = parentFiber.sibling;
  }
  function commitPassiveUnmountOnFiber(finishedWork) {
    switch (finishedWork.tag) {
      case 0:
      case 11:
      case 15:
        recursivelyTraversePassiveUnmountEffects(finishedWork);
        finishedWork.flags & 2048 && commitHookEffectListUnmount(9, finishedWork, finishedWork.return);
        break;
      case 3:
        recursivelyTraversePassiveUnmountEffects(finishedWork);
        break;
      case 12:
        recursivelyTraversePassiveUnmountEffects(finishedWork);
        break;
      case 22:
        var instance = finishedWork.stateNode;
        null !== finishedWork.memoizedState && instance._visibility & 2 && (null === finishedWork.return || 13 !== finishedWork.return.tag) ? (instance._visibility &= -3, recursivelyTraverseDisconnectPassiveEffects(finishedWork)) : recursivelyTraversePassiveUnmountEffects(finishedWork);
        break;
      default:
        recursivelyTraversePassiveUnmountEffects(finishedWork);
    }
  }
  function recursivelyTraverseDisconnectPassiveEffects(parentFiber) {
    var deletions = parentFiber.deletions;
    if (0 !== (parentFiber.flags & 16)) {
      if (null !== deletions)
        for (var i = 0; i < deletions.length; i++) {
          var childToDelete = deletions[i];
          nextEffect = childToDelete;
          commitPassiveUnmountEffectsInsideOfDeletedTree_begin(
            childToDelete,
            parentFiber
          );
        }
      detachAlternateSiblings(parentFiber);
    }
    for (parentFiber = parentFiber.child; null !== parentFiber; ) {
      deletions = parentFiber;
      switch (deletions.tag) {
        case 0:
        case 11:
        case 15:
          commitHookEffectListUnmount(8, deletions, deletions.return);
          recursivelyTraverseDisconnectPassiveEffects(deletions);
          break;
        case 22:
          i = deletions.stateNode;
          i._visibility & 2 && (i._visibility &= -3, recursivelyTraverseDisconnectPassiveEffects(deletions));
          break;
        default:
          recursivelyTraverseDisconnectPassiveEffects(deletions);
      }
      parentFiber = parentFiber.sibling;
    }
  }
  function commitPassiveUnmountEffectsInsideOfDeletedTree_begin(deletedSubtreeRoot, nearestMountedAncestor) {
    for (; null !== nextEffect; ) {
      var fiber = nextEffect;
      switch (fiber.tag) {
        case 0:
        case 11:
        case 15:
          commitHookEffectListUnmount(8, fiber, nearestMountedAncestor);
          break;
        case 23:
        case 22:
          if (null !== fiber.memoizedState && null !== fiber.memoizedState.cachePool) {
            var cache = fiber.memoizedState.cachePool.pool;
            null != cache && cache.refCount++;
          }
          break;
        case 24:
          releaseCache(fiber.memoizedState.cache);
      }
      cache = fiber.child;
      if (null !== cache) cache.return = fiber, nextEffect = cache;
      else
        a: for (fiber = deletedSubtreeRoot; null !== nextEffect; ) {
          cache = nextEffect;
          var sibling = cache.sibling, returnFiber = cache.return;
          detachFiberAfterEffects(cache);
          if (cache === fiber) {
            nextEffect = null;
            break a;
          }
          if (null !== sibling) {
            sibling.return = returnFiber;
            nextEffect = sibling;
            break a;
          }
          nextEffect = returnFiber;
        }
    }
  }
  var DefaultAsyncDispatcher = {
    getCacheForType: function(resourceType) {
      var cache = readContext(CacheContext), cacheForType = cache.data.get(resourceType);
      void 0 === cacheForType && (cacheForType = resourceType(), cache.data.set(resourceType, cacheForType));
      return cacheForType;
    },
    cacheSignal: function() {
      return readContext(CacheContext).controller.signal;
    }
  }, PossiblyWeakMap = "function" === typeof WeakMap ? WeakMap : Map, executionContext = 0, workInProgressRoot = null, workInProgress = null, workInProgressRootRenderLanes = 0, workInProgressSuspendedReason = 0, workInProgressThrownValue = null, workInProgressRootDidSkipSuspendedSiblings = false, workInProgressRootIsPrerendering = false, workInProgressRootDidAttachPingListener = false, entangledRenderLanes = 0, workInProgressRootExitStatus = 0, workInProgressRootSkippedLanes = 0, workInProgressRootInterleavedUpdatedLanes = 0, workInProgressRootPingedLanes = 0, workInProgressDeferredLane = 0, workInProgressSuspendedRetryLanes = 0, workInProgressRootConcurrentErrors = null, workInProgressRootRecoverableErrors = null, workInProgressRootDidIncludeRecursiveRenderUpdate = false, globalMostRecentFallbackTime = 0, globalMostRecentTransitionTime = 0, workInProgressRootRenderTargetTime = Infinity, workInProgressTransitions = null, legacyErrorBoundariesThatAlreadyFailed = null, pendingEffectsStatus = 0, pendingEffectsRoot = null, pendingFinishedWork = null, pendingEffectsLanes = 0, pendingEffectsRemainingLanes = 0, pendingPassiveTransitions = null, pendingRecoverableErrors = null, nestedUpdateCount = 0, rootWithNestedUpdates = null;
  function requestUpdateLane() {
    return 0 !== (executionContext & 2) && 0 !== workInProgressRootRenderLanes ? workInProgressRootRenderLanes & -workInProgressRootRenderLanes : null !== ReactSharedInternals.T ? requestTransitionLane() : resolveUpdatePriority();
  }
  function requestDeferredLane() {
    if (0 === workInProgressDeferredLane)
      if (0 === (workInProgressRootRenderLanes & 536870912) || isHydrating) {
        var lane = nextTransitionDeferredLane;
        nextTransitionDeferredLane <<= 1;
        0 === (nextTransitionDeferredLane & 3932160) && (nextTransitionDeferredLane = 262144);
        workInProgressDeferredLane = lane;
      } else workInProgressDeferredLane = 536870912;
    lane = suspenseHandlerStackCursor.current;
    null !== lane && (lane.flags |= 32);
    return workInProgressDeferredLane;
  }
  function scheduleUpdateOnFiber(root3, fiber, lane) {
    if (root3 === workInProgressRoot && (2 === workInProgressSuspendedReason || 9 === workInProgressSuspendedReason) || null !== root3.cancelPendingCommit)
      prepareFreshStack(root3, 0), markRootSuspended(
        root3,
        workInProgressRootRenderLanes,
        workInProgressDeferredLane,
        false
      );
    markRootUpdated$1(root3, lane);
    if (0 === (executionContext & 2) || root3 !== workInProgressRoot)
      root3 === workInProgressRoot && (0 === (executionContext & 2) && (workInProgressRootInterleavedUpdatedLanes |= lane), 4 === workInProgressRootExitStatus && markRootSuspended(
        root3,
        workInProgressRootRenderLanes,
        workInProgressDeferredLane,
        false
      )), ensureRootIsScheduled(root3);
  }
  function performWorkOnRoot(root$jscomp$0, lanes, forceSync) {
    if (0 !== (executionContext & 6)) throw Error(formatProdErrorMessage(327));
    var shouldTimeSlice = !forceSync && 0 === (lanes & 127) && 0 === (lanes & root$jscomp$0.expiredLanes) || checkIfRootIsPrerendering(root$jscomp$0, lanes), exitStatus = shouldTimeSlice ? renderRootConcurrent(root$jscomp$0, lanes) : renderRootSync(root$jscomp$0, lanes, true), renderWasConcurrent = shouldTimeSlice;
    do {
      if (0 === exitStatus) {
        workInProgressRootIsPrerendering && !shouldTimeSlice && markRootSuspended(root$jscomp$0, lanes, 0, false);
        break;
      } else {
        forceSync = root$jscomp$0.current.alternate;
        if (renderWasConcurrent && !isRenderConsistentWithExternalStores(forceSync)) {
          exitStatus = renderRootSync(root$jscomp$0, lanes, false);
          renderWasConcurrent = false;
          continue;
        }
        if (2 === exitStatus) {
          renderWasConcurrent = lanes;
          if (root$jscomp$0.errorRecoveryDisabledLanes & renderWasConcurrent)
            var JSCompiler_inline_result = 0;
          else
            JSCompiler_inline_result = root$jscomp$0.pendingLanes & -536870913, JSCompiler_inline_result = 0 !== JSCompiler_inline_result ? JSCompiler_inline_result : JSCompiler_inline_result & 536870912 ? 536870912 : 0;
          if (0 !== JSCompiler_inline_result) {
            lanes = JSCompiler_inline_result;
            a: {
              var root3 = root$jscomp$0;
              exitStatus = workInProgressRootConcurrentErrors;
              var wasRootDehydrated = root3.current.memoizedState.isDehydrated;
              wasRootDehydrated && (prepareFreshStack(root3, JSCompiler_inline_result).flags |= 256);
              JSCompiler_inline_result = renderRootSync(
                root3,
                JSCompiler_inline_result,
                false
              );
              if (2 !== JSCompiler_inline_result) {
                if (workInProgressRootDidAttachPingListener && !wasRootDehydrated) {
                  root3.errorRecoveryDisabledLanes |= renderWasConcurrent;
                  workInProgressRootInterleavedUpdatedLanes |= renderWasConcurrent;
                  exitStatus = 4;
                  break a;
                }
                renderWasConcurrent = workInProgressRootRecoverableErrors;
                workInProgressRootRecoverableErrors = exitStatus;
                null !== renderWasConcurrent && (null === workInProgressRootRecoverableErrors ? workInProgressRootRecoverableErrors = renderWasConcurrent : workInProgressRootRecoverableErrors.push.apply(
                  workInProgressRootRecoverableErrors,
                  renderWasConcurrent
                ));
              }
              exitStatus = JSCompiler_inline_result;
            }
            renderWasConcurrent = false;
            if (2 !== exitStatus) continue;
          }
        }
        if (1 === exitStatus) {
          prepareFreshStack(root$jscomp$0, 0);
          markRootSuspended(root$jscomp$0, lanes, 0, true);
          break;
        }
        a: {
          shouldTimeSlice = root$jscomp$0;
          renderWasConcurrent = exitStatus;
          switch (renderWasConcurrent) {
            case 0:
            case 1:
              throw Error(formatProdErrorMessage(345));
            case 4:
              if ((lanes & 4194048) !== lanes) break;
            case 6:
              markRootSuspended(
                shouldTimeSlice,
                lanes,
                workInProgressDeferredLane,
                !workInProgressRootDidSkipSuspendedSiblings
              );
              break a;
            case 2:
              workInProgressRootRecoverableErrors = null;
              break;
            case 3:
            case 5:
              break;
            default:
              throw Error(formatProdErrorMessage(329));
          }
          if ((lanes & 62914560) === lanes && (exitStatus = globalMostRecentFallbackTime + 300 - now(), 10 < exitStatus)) {
            markRootSuspended(
              shouldTimeSlice,
              lanes,
              workInProgressDeferredLane,
              !workInProgressRootDidSkipSuspendedSiblings
            );
            if (0 !== getNextLanes(shouldTimeSlice, 0, true)) break a;
            pendingEffectsLanes = lanes;
            shouldTimeSlice.timeoutHandle = scheduleTimeout(
              commitRootWhenReady.bind(
                null,
                shouldTimeSlice,
                forceSync,
                workInProgressRootRecoverableErrors,
                workInProgressTransitions,
                workInProgressRootDidIncludeRecursiveRenderUpdate,
                lanes,
                workInProgressDeferredLane,
                workInProgressRootInterleavedUpdatedLanes,
                workInProgressSuspendedRetryLanes,
                workInProgressRootDidSkipSuspendedSiblings,
                renderWasConcurrent,
                "Throttled",
                -0,
                0
              ),
              exitStatus
            );
            break a;
          }
          commitRootWhenReady(
            shouldTimeSlice,
            forceSync,
            workInProgressRootRecoverableErrors,
            workInProgressTransitions,
            workInProgressRootDidIncludeRecursiveRenderUpdate,
            lanes,
            workInProgressDeferredLane,
            workInProgressRootInterleavedUpdatedLanes,
            workInProgressSuspendedRetryLanes,
            workInProgressRootDidSkipSuspendedSiblings,
            renderWasConcurrent,
            null,
            -0,
            0
          );
        }
      }
      break;
    } while (1);
    ensureRootIsScheduled(root$jscomp$0);
  }
  function commitRootWhenReady(root3, finishedWork, recoverableErrors, transitions, didIncludeRenderPhaseUpdate, lanes, spawnedLane, updatedLanes, suspendedRetryLanes, didSkipSuspendedSiblings, exitStatus, suspendedCommitReason, completedRenderStartTime, completedRenderEndTime) {
    root3.timeoutHandle = -1;
    suspendedCommitReason = finishedWork.subtreeFlags;
    if (suspendedCommitReason & 8192 || 16785408 === (suspendedCommitReason & 16785408)) {
      suspendedCommitReason = {
        stylesheets: null,
        count: 0,
        imgCount: 0,
        imgBytes: 0,
        suspenseyImages: [],
        waitingForImages: true,
        waitingForViewTransition: false,
        unsuspend: noop$1
      };
      accumulateSuspenseyCommitOnFiber(
        finishedWork,
        lanes,
        suspendedCommitReason
      );
      var timeoutOffset = (lanes & 62914560) === lanes ? globalMostRecentFallbackTime - now() : (lanes & 4194048) === lanes ? globalMostRecentTransitionTime - now() : 0;
      timeoutOffset = waitForCommitToBeReady(
        suspendedCommitReason,
        timeoutOffset
      );
      if (null !== timeoutOffset) {
        pendingEffectsLanes = lanes;
        root3.cancelPendingCommit = timeoutOffset(
          commitRoot.bind(
            null,
            root3,
            finishedWork,
            lanes,
            recoverableErrors,
            transitions,
            didIncludeRenderPhaseUpdate,
            spawnedLane,
            updatedLanes,
            suspendedRetryLanes,
            exitStatus,
            suspendedCommitReason,
            null,
            completedRenderStartTime,
            completedRenderEndTime
          )
        );
        markRootSuspended(root3, lanes, spawnedLane, !didSkipSuspendedSiblings);
        return;
      }
    }
    commitRoot(
      root3,
      finishedWork,
      lanes,
      recoverableErrors,
      transitions,
      didIncludeRenderPhaseUpdate,
      spawnedLane,
      updatedLanes,
      suspendedRetryLanes
    );
  }
  function isRenderConsistentWithExternalStores(finishedWork) {
    for (var node2 = finishedWork; ; ) {
      var tag = node2.tag;
      if ((0 === tag || 11 === tag || 15 === tag) && node2.flags & 16384 && (tag = node2.updateQueue, null !== tag && (tag = tag.stores, null !== tag)))
        for (var i = 0; i < tag.length; i++) {
          var check = tag[i], getSnapshot = check.getSnapshot;
          check = check.value;
          try {
            if (!objectIs(getSnapshot(), check)) return false;
          } catch (error) {
            return false;
          }
        }
      tag = node2.child;
      if (node2.subtreeFlags & 16384 && null !== tag)
        tag.return = node2, node2 = tag;
      else {
        if (node2 === finishedWork) break;
        for (; null === node2.sibling; ) {
          if (null === node2.return || node2.return === finishedWork) return true;
          node2 = node2.return;
        }
        node2.sibling.return = node2.return;
        node2 = node2.sibling;
      }
    }
    return true;
  }
  function markRootSuspended(root3, suspendedLanes, spawnedLane, didAttemptEntireTree) {
    suspendedLanes &= ~workInProgressRootPingedLanes;
    suspendedLanes &= ~workInProgressRootInterleavedUpdatedLanes;
    root3.suspendedLanes |= suspendedLanes;
    root3.pingedLanes &= ~suspendedLanes;
    didAttemptEntireTree && (root3.warmLanes |= suspendedLanes);
    didAttemptEntireTree = root3.expirationTimes;
    for (var lanes = suspendedLanes; 0 < lanes; ) {
      var index$6 = 31 - clz32(lanes), lane = 1 << index$6;
      didAttemptEntireTree[index$6] = -1;
      lanes &= ~lane;
    }
    0 !== spawnedLane && markSpawnedDeferredLane(root3, spawnedLane, suspendedLanes);
  }
  function flushSyncWork$1() {
    return 0 === (executionContext & 6) ? (flushSyncWorkAcrossRoots_impl(0), false) : true;
  }
  function resetWorkInProgressStack() {
    if (null !== workInProgress) {
      if (0 === workInProgressSuspendedReason)
        var interruptedWork = workInProgress.return;
      else
        interruptedWork = workInProgress, lastContextDependency = currentlyRenderingFiber$1 = null, resetHooksOnUnwind(interruptedWork), thenableState$1 = null, thenableIndexCounter$1 = 0, interruptedWork = workInProgress;
      for (; null !== interruptedWork; )
        unwindInterruptedWork(interruptedWork.alternate, interruptedWork), interruptedWork = interruptedWork.return;
      workInProgress = null;
    }
  }
  function prepareFreshStack(root3, lanes) {
    var timeoutHandle = root3.timeoutHandle;
    -1 !== timeoutHandle && (root3.timeoutHandle = -1, cancelTimeout(timeoutHandle));
    timeoutHandle = root3.cancelPendingCommit;
    null !== timeoutHandle && (root3.cancelPendingCommit = null, timeoutHandle());
    pendingEffectsLanes = 0;
    resetWorkInProgressStack();
    workInProgressRoot = root3;
    workInProgress = timeoutHandle = createWorkInProgress(root3.current, null);
    workInProgressRootRenderLanes = lanes;
    workInProgressSuspendedReason = 0;
    workInProgressThrownValue = null;
    workInProgressRootDidSkipSuspendedSiblings = false;
    workInProgressRootIsPrerendering = checkIfRootIsPrerendering(root3, lanes);
    workInProgressRootDidAttachPingListener = false;
    workInProgressSuspendedRetryLanes = workInProgressDeferredLane = workInProgressRootPingedLanes = workInProgressRootInterleavedUpdatedLanes = workInProgressRootSkippedLanes = workInProgressRootExitStatus = 0;
    workInProgressRootRecoverableErrors = workInProgressRootConcurrentErrors = null;
    workInProgressRootDidIncludeRecursiveRenderUpdate = false;
    0 !== (lanes & 8) && (lanes |= lanes & 32);
    var allEntangledLanes = root3.entangledLanes;
    if (0 !== allEntangledLanes)
      for (root3 = root3.entanglements, allEntangledLanes &= lanes; 0 < allEntangledLanes; ) {
        var index$4 = 31 - clz32(allEntangledLanes), lane = 1 << index$4;
        lanes |= root3[index$4];
        allEntangledLanes &= ~lane;
      }
    entangledRenderLanes = lanes;
    finishQueueingConcurrentUpdates();
    return timeoutHandle;
  }
  function handleThrow(root3, thrownValue) {
    currentlyRenderingFiber = null;
    ReactSharedInternals.H = ContextOnlyDispatcher;
    thrownValue === SuspenseException || thrownValue === SuspenseActionException ? (thrownValue = getSuspendedThenable(), workInProgressSuspendedReason = 3) : thrownValue === SuspenseyCommitException ? (thrownValue = getSuspendedThenable(), workInProgressSuspendedReason = 4) : workInProgressSuspendedReason = thrownValue === SelectiveHydrationException ? 8 : null !== thrownValue && "object" === typeof thrownValue && "function" === typeof thrownValue.then ? 6 : 1;
    workInProgressThrownValue = thrownValue;
    null === workInProgress && (workInProgressRootExitStatus = 1, logUncaughtError(
      root3,
      createCapturedValueAtFiber(thrownValue, root3.current)
    ));
  }
  function shouldRemainOnPreviousScreen() {
    var handler = suspenseHandlerStackCursor.current;
    return null === handler ? true : (workInProgressRootRenderLanes & 4194048) === workInProgressRootRenderLanes ? null === shellBoundary ? true : false : (workInProgressRootRenderLanes & 62914560) === workInProgressRootRenderLanes || 0 !== (workInProgressRootRenderLanes & 536870912) ? handler === shellBoundary : false;
  }
  function pushDispatcher() {
    var prevDispatcher = ReactSharedInternals.H;
    ReactSharedInternals.H = ContextOnlyDispatcher;
    return null === prevDispatcher ? ContextOnlyDispatcher : prevDispatcher;
  }
  function pushAsyncDispatcher() {
    var prevAsyncDispatcher = ReactSharedInternals.A;
    ReactSharedInternals.A = DefaultAsyncDispatcher;
    return prevAsyncDispatcher;
  }
  function renderDidSuspendDelayIfPossible() {
    workInProgressRootExitStatus = 4;
    workInProgressRootDidSkipSuspendedSiblings || (workInProgressRootRenderLanes & 4194048) !== workInProgressRootRenderLanes && null !== suspenseHandlerStackCursor.current || (workInProgressRootIsPrerendering = true);
    0 === (workInProgressRootSkippedLanes & 134217727) && 0 === (workInProgressRootInterleavedUpdatedLanes & 134217727) || null === workInProgressRoot || markRootSuspended(
      workInProgressRoot,
      workInProgressRootRenderLanes,
      workInProgressDeferredLane,
      false
    );
  }
  function renderRootSync(root3, lanes, shouldYieldForPrerendering) {
    var prevExecutionContext = executionContext;
    executionContext |= 2;
    var prevDispatcher = pushDispatcher(), prevAsyncDispatcher = pushAsyncDispatcher();
    if (workInProgressRoot !== root3 || workInProgressRootRenderLanes !== lanes)
      workInProgressTransitions = null, prepareFreshStack(root3, lanes);
    lanes = false;
    var exitStatus = workInProgressRootExitStatus;
    a: do
      try {
        if (0 !== workInProgressSuspendedReason && null !== workInProgress) {
          var unitOfWork = workInProgress, thrownValue = workInProgressThrownValue;
          switch (workInProgressSuspendedReason) {
            case 8:
              resetWorkInProgressStack();
              exitStatus = 6;
              break a;
            case 3:
            case 2:
            case 9:
            case 6:
              null === suspenseHandlerStackCursor.current && (lanes = true);
              var reason = workInProgressSuspendedReason;
              workInProgressSuspendedReason = 0;
              workInProgressThrownValue = null;
              throwAndUnwindWorkLoop(root3, unitOfWork, thrownValue, reason);
              if (shouldYieldForPrerendering && workInProgressRootIsPrerendering) {
                exitStatus = 0;
                break a;
              }
              break;
            default:
              reason = workInProgressSuspendedReason, workInProgressSuspendedReason = 0, workInProgressThrownValue = null, throwAndUnwindWorkLoop(root3, unitOfWork, thrownValue, reason);
          }
        }
        workLoopSync();
        exitStatus = workInProgressRootExitStatus;
        break;
      } catch (thrownValue$165) {
        handleThrow(root3, thrownValue$165);
      }
    while (1);
    lanes && root3.shellSuspendCounter++;
    lastContextDependency = currentlyRenderingFiber$1 = null;
    executionContext = prevExecutionContext;
    ReactSharedInternals.H = prevDispatcher;
    ReactSharedInternals.A = prevAsyncDispatcher;
    null === workInProgress && (workInProgressRoot = null, workInProgressRootRenderLanes = 0, finishQueueingConcurrentUpdates());
    return exitStatus;
  }
  function workLoopSync() {
    for (; null !== workInProgress; ) performUnitOfWork(workInProgress);
  }
  function renderRootConcurrent(root3, lanes) {
    var prevExecutionContext = executionContext;
    executionContext |= 2;
    var prevDispatcher = pushDispatcher(), prevAsyncDispatcher = pushAsyncDispatcher();
    workInProgressRoot !== root3 || workInProgressRootRenderLanes !== lanes ? (workInProgressTransitions = null, workInProgressRootRenderTargetTime = now() + 500, prepareFreshStack(root3, lanes)) : workInProgressRootIsPrerendering = checkIfRootIsPrerendering(
      root3,
      lanes
    );
    a: do
      try {
        if (0 !== workInProgressSuspendedReason && null !== workInProgress) {
          lanes = workInProgress;
          var thrownValue = workInProgressThrownValue;
          b: switch (workInProgressSuspendedReason) {
            case 1:
              workInProgressSuspendedReason = 0;
              workInProgressThrownValue = null;
              throwAndUnwindWorkLoop(root3, lanes, thrownValue, 1);
              break;
            case 2:
            case 9:
              if (isThenableResolved(thrownValue)) {
                workInProgressSuspendedReason = 0;
                workInProgressThrownValue = null;
                replaySuspendedUnitOfWork(lanes);
                break;
              }
              lanes = function() {
                2 !== workInProgressSuspendedReason && 9 !== workInProgressSuspendedReason || workInProgressRoot !== root3 || (workInProgressSuspendedReason = 7);
                ensureRootIsScheduled(root3);
              };
              thrownValue.then(lanes, lanes);
              break a;
            case 3:
              workInProgressSuspendedReason = 7;
              break a;
            case 4:
              workInProgressSuspendedReason = 5;
              break a;
            case 7:
              isThenableResolved(thrownValue) ? (workInProgressSuspendedReason = 0, workInProgressThrownValue = null, replaySuspendedUnitOfWork(lanes)) : (workInProgressSuspendedReason = 0, workInProgressThrownValue = null, throwAndUnwindWorkLoop(root3, lanes, thrownValue, 7));
              break;
            case 5:
              var resource = null;
              switch (workInProgress.tag) {
                case 26:
                  resource = workInProgress.memoizedState;
                case 5:
                case 27:
                  var hostFiber = workInProgress;
                  if (resource ? preloadResource(resource) : hostFiber.stateNode.complete) {
                    workInProgressSuspendedReason = 0;
                    workInProgressThrownValue = null;
                    var sibling = hostFiber.sibling;
                    if (null !== sibling) workInProgress = sibling;
                    else {
                      var returnFiber = hostFiber.return;
                      null !== returnFiber ? (workInProgress = returnFiber, completeUnitOfWork(returnFiber)) : workInProgress = null;
                    }
                    break b;
                  }
              }
              workInProgressSuspendedReason = 0;
              workInProgressThrownValue = null;
              throwAndUnwindWorkLoop(root3, lanes, thrownValue, 5);
              break;
            case 6:
              workInProgressSuspendedReason = 0;
              workInProgressThrownValue = null;
              throwAndUnwindWorkLoop(root3, lanes, thrownValue, 6);
              break;
            case 8:
              resetWorkInProgressStack();
              workInProgressRootExitStatus = 6;
              break a;
            default:
              throw Error(formatProdErrorMessage(462));
          }
        }
        workLoopConcurrentByScheduler();
        break;
      } catch (thrownValue$167) {
        handleThrow(root3, thrownValue$167);
      }
    while (1);
    lastContextDependency = currentlyRenderingFiber$1 = null;
    ReactSharedInternals.H = prevDispatcher;
    ReactSharedInternals.A = prevAsyncDispatcher;
    executionContext = prevExecutionContext;
    if (null !== workInProgress) return 0;
    workInProgressRoot = null;
    workInProgressRootRenderLanes = 0;
    finishQueueingConcurrentUpdates();
    return workInProgressRootExitStatus;
  }
  function workLoopConcurrentByScheduler() {
    for (; null !== workInProgress && !shouldYield(); )
      performUnitOfWork(workInProgress);
  }
  function performUnitOfWork(unitOfWork) {
    var next = beginWork(unitOfWork.alternate, unitOfWork, entangledRenderLanes);
    unitOfWork.memoizedProps = unitOfWork.pendingProps;
    null === next ? completeUnitOfWork(unitOfWork) : workInProgress = next;
  }
  function replaySuspendedUnitOfWork(unitOfWork) {
    var next = unitOfWork;
    var current = next.alternate;
    switch (next.tag) {
      case 15:
      case 0:
        next = replayFunctionComponent(
          current,
          next,
          next.pendingProps,
          next.type,
          void 0,
          workInProgressRootRenderLanes
        );
        break;
      case 11:
        next = replayFunctionComponent(
          current,
          next,
          next.pendingProps,
          next.type.render,
          next.ref,
          workInProgressRootRenderLanes
        );
        break;
      case 5:
        resetHooksOnUnwind(next);
      default:
        unwindInterruptedWork(current, next), next = workInProgress = resetWorkInProgress(next, entangledRenderLanes), next = beginWork(current, next, entangledRenderLanes);
    }
    unitOfWork.memoizedProps = unitOfWork.pendingProps;
    null === next ? completeUnitOfWork(unitOfWork) : workInProgress = next;
  }
  function throwAndUnwindWorkLoop(root3, unitOfWork, thrownValue, suspendedReason) {
    lastContextDependency = currentlyRenderingFiber$1 = null;
    resetHooksOnUnwind(unitOfWork);
    thenableState$1 = null;
    thenableIndexCounter$1 = 0;
    var returnFiber = unitOfWork.return;
    try {
      if (throwException(
        root3,
        returnFiber,
        unitOfWork,
        thrownValue,
        workInProgressRootRenderLanes
      )) {
        workInProgressRootExitStatus = 1;
        logUncaughtError(
          root3,
          createCapturedValueAtFiber(thrownValue, root3.current)
        );
        workInProgress = null;
        return;
      }
    } catch (error) {
      if (null !== returnFiber) throw workInProgress = returnFiber, error;
      workInProgressRootExitStatus = 1;
      logUncaughtError(
        root3,
        createCapturedValueAtFiber(thrownValue, root3.current)
      );
      workInProgress = null;
      return;
    }
    if (unitOfWork.flags & 32768) {
      if (isHydrating || 1 === suspendedReason) root3 = true;
      else if (workInProgressRootIsPrerendering || 0 !== (workInProgressRootRenderLanes & 536870912))
        root3 = false;
      else if (workInProgressRootDidSkipSuspendedSiblings = root3 = true, 2 === suspendedReason || 9 === suspendedReason || 3 === suspendedReason || 6 === suspendedReason)
        suspendedReason = suspenseHandlerStackCursor.current, null !== suspendedReason && 13 === suspendedReason.tag && (suspendedReason.flags |= 16384);
      unwindUnitOfWork(unitOfWork, root3);
    } else completeUnitOfWork(unitOfWork);
  }
  function completeUnitOfWork(unitOfWork) {
    var completedWork = unitOfWork;
    do {
      if (0 !== (completedWork.flags & 32768)) {
        unwindUnitOfWork(
          completedWork,
          workInProgressRootDidSkipSuspendedSiblings
        );
        return;
      }
      unitOfWork = completedWork.return;
      var next = completeWork(
        completedWork.alternate,
        completedWork,
        entangledRenderLanes
      );
      if (null !== next) {
        workInProgress = next;
        return;
      }
      completedWork = completedWork.sibling;
      if (null !== completedWork) {
        workInProgress = completedWork;
        return;
      }
      workInProgress = completedWork = unitOfWork;
    } while (null !== completedWork);
    0 === workInProgressRootExitStatus && (workInProgressRootExitStatus = 5);
  }
  function unwindUnitOfWork(unitOfWork, skipSiblings) {
    do {
      var next = unwindWork(unitOfWork.alternate, unitOfWork);
      if (null !== next) {
        next.flags &= 32767;
        workInProgress = next;
        return;
      }
      next = unitOfWork.return;
      null !== next && (next.flags |= 32768, next.subtreeFlags = 0, next.deletions = null);
      if (!skipSiblings && (unitOfWork = unitOfWork.sibling, null !== unitOfWork)) {
        workInProgress = unitOfWork;
        return;
      }
      workInProgress = unitOfWork = next;
    } while (null !== unitOfWork);
    workInProgressRootExitStatus = 6;
    workInProgress = null;
  }
  function commitRoot(root3, finishedWork, lanes, recoverableErrors, transitions, didIncludeRenderPhaseUpdate, spawnedLane, updatedLanes, suspendedRetryLanes) {
    root3.cancelPendingCommit = null;
    do
      flushPendingEffects();
    while (0 !== pendingEffectsStatus);
    if (0 !== (executionContext & 6)) throw Error(formatProdErrorMessage(327));
    if (null !== finishedWork) {
      if (finishedWork === root3.current) throw Error(formatProdErrorMessage(177));
      didIncludeRenderPhaseUpdate = finishedWork.lanes | finishedWork.childLanes;
      didIncludeRenderPhaseUpdate |= concurrentlyUpdatedLanes;
      markRootFinished(
        root3,
        lanes,
        didIncludeRenderPhaseUpdate,
        spawnedLane,
        updatedLanes,
        suspendedRetryLanes
      );
      root3 === workInProgressRoot && (workInProgress = workInProgressRoot = null, workInProgressRootRenderLanes = 0);
      pendingFinishedWork = finishedWork;
      pendingEffectsRoot = root3;
      pendingEffectsLanes = lanes;
      pendingEffectsRemainingLanes = didIncludeRenderPhaseUpdate;
      pendingPassiveTransitions = transitions;
      pendingRecoverableErrors = recoverableErrors;
      0 !== (finishedWork.subtreeFlags & 10256) || 0 !== (finishedWork.flags & 10256) ? (root3.callbackNode = null, root3.callbackPriority = 0, scheduleCallback$1(NormalPriority$1, function() {
        flushPassiveEffects();
        return null;
      })) : (root3.callbackNode = null, root3.callbackPriority = 0);
      recoverableErrors = 0 !== (finishedWork.flags & 13878);
      if (0 !== (finishedWork.subtreeFlags & 13878) || recoverableErrors) {
        recoverableErrors = ReactSharedInternals.T;
        ReactSharedInternals.T = null;
        transitions = ReactDOMSharedInternals.p;
        ReactDOMSharedInternals.p = 2;
        spawnedLane = executionContext;
        executionContext |= 4;
        try {
          commitBeforeMutationEffects(root3, finishedWork, lanes);
        } finally {
          executionContext = spawnedLane, ReactDOMSharedInternals.p = transitions, ReactSharedInternals.T = recoverableErrors;
        }
      }
      pendingEffectsStatus = 1;
      flushMutationEffects();
      flushLayoutEffects();
      flushSpawnedWork();
    }
  }
  function flushMutationEffects() {
    if (1 === pendingEffectsStatus) {
      pendingEffectsStatus = 0;
      var root3 = pendingEffectsRoot, finishedWork = pendingFinishedWork, rootMutationHasEffect = 0 !== (finishedWork.flags & 13878);
      if (0 !== (finishedWork.subtreeFlags & 13878) || rootMutationHasEffect) {
        rootMutationHasEffect = ReactSharedInternals.T;
        ReactSharedInternals.T = null;
        var previousPriority = ReactDOMSharedInternals.p;
        ReactDOMSharedInternals.p = 2;
        var prevExecutionContext = executionContext;
        executionContext |= 4;
        try {
          commitMutationEffectsOnFiber(finishedWork, root3);
          var priorSelectionInformation = selectionInformation, curFocusedElem = getActiveElementDeep(root3.containerInfo), priorFocusedElem = priorSelectionInformation.focusedElem, priorSelectionRange = priorSelectionInformation.selectionRange;
          if (curFocusedElem !== priorFocusedElem && priorFocusedElem && priorFocusedElem.ownerDocument && containsNode(
            priorFocusedElem.ownerDocument.documentElement,
            priorFocusedElem
          )) {
            if (null !== priorSelectionRange && hasSelectionCapabilities(priorFocusedElem)) {
              var start = priorSelectionRange.start, end = priorSelectionRange.end;
              void 0 === end && (end = start);
              if ("selectionStart" in priorFocusedElem)
                priorFocusedElem.selectionStart = start, priorFocusedElem.selectionEnd = Math.min(
                  end,
                  priorFocusedElem.value.length
                );
              else {
                var doc = priorFocusedElem.ownerDocument || document, win = doc && doc.defaultView || window;
                if (win.getSelection) {
                  var selection = win.getSelection(), length = priorFocusedElem.textContent.length, start$jscomp$0 = Math.min(priorSelectionRange.start, length), end$jscomp$0 = void 0 === priorSelectionRange.end ? start$jscomp$0 : Math.min(priorSelectionRange.end, length);
                  !selection.extend && start$jscomp$0 > end$jscomp$0 && (curFocusedElem = end$jscomp$0, end$jscomp$0 = start$jscomp$0, start$jscomp$0 = curFocusedElem);
                  var startMarker = getNodeForCharacterOffset(
                    priorFocusedElem,
                    start$jscomp$0
                  ), endMarker = getNodeForCharacterOffset(
                    priorFocusedElem,
                    end$jscomp$0
                  );
                  if (startMarker && endMarker && (1 !== selection.rangeCount || selection.anchorNode !== startMarker.node || selection.anchorOffset !== startMarker.offset || selection.focusNode !== endMarker.node || selection.focusOffset !== endMarker.offset)) {
                    var range = doc.createRange();
                    range.setStart(startMarker.node, startMarker.offset);
                    selection.removeAllRanges();
                    start$jscomp$0 > end$jscomp$0 ? (selection.addRange(range), selection.extend(endMarker.node, endMarker.offset)) : (range.setEnd(endMarker.node, endMarker.offset), selection.addRange(range));
                  }
                }
              }
            }
            doc = [];
            for (selection = priorFocusedElem; selection = selection.parentNode; )
              1 === selection.nodeType && doc.push({
                element: selection,
                left: selection.scrollLeft,
                top: selection.scrollTop
              });
            "function" === typeof priorFocusedElem.focus && priorFocusedElem.focus();
            for (priorFocusedElem = 0; priorFocusedElem < doc.length; priorFocusedElem++) {
              var info = doc[priorFocusedElem];
              info.element.scrollLeft = info.left;
              info.element.scrollTop = info.top;
            }
          }
          _enabled = !!eventsEnabled;
          selectionInformation = eventsEnabled = null;
        } finally {
          executionContext = prevExecutionContext, ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = rootMutationHasEffect;
        }
      }
      root3.current = finishedWork;
      pendingEffectsStatus = 2;
    }
  }
  function flushLayoutEffects() {
    if (2 === pendingEffectsStatus) {
      pendingEffectsStatus = 0;
      var root3 = pendingEffectsRoot, finishedWork = pendingFinishedWork, rootHasLayoutEffect = 0 !== (finishedWork.flags & 8772);
      if (0 !== (finishedWork.subtreeFlags & 8772) || rootHasLayoutEffect) {
        rootHasLayoutEffect = ReactSharedInternals.T;
        ReactSharedInternals.T = null;
        var previousPriority = ReactDOMSharedInternals.p;
        ReactDOMSharedInternals.p = 2;
        var prevExecutionContext = executionContext;
        executionContext |= 4;
        try {
          commitLayoutEffectOnFiber(root3, finishedWork.alternate, finishedWork);
        } finally {
          executionContext = prevExecutionContext, ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = rootHasLayoutEffect;
        }
      }
      pendingEffectsStatus = 3;
    }
  }
  function flushSpawnedWork() {
    if (4 === pendingEffectsStatus || 3 === pendingEffectsStatus) {
      pendingEffectsStatus = 0;
      requestPaint();
      var root3 = pendingEffectsRoot, finishedWork = pendingFinishedWork, lanes = pendingEffectsLanes, recoverableErrors = pendingRecoverableErrors;
      0 !== (finishedWork.subtreeFlags & 10256) || 0 !== (finishedWork.flags & 10256) ? pendingEffectsStatus = 5 : (pendingEffectsStatus = 0, pendingFinishedWork = pendingEffectsRoot = null, releaseRootPooledCache(root3, root3.pendingLanes));
      var remainingLanes = root3.pendingLanes;
      0 === remainingLanes && (legacyErrorBoundariesThatAlreadyFailed = null);
      lanesToEventPriority(lanes);
      finishedWork = finishedWork.stateNode;
      if (injectedHook && "function" === typeof injectedHook.onCommitFiberRoot)
        try {
          injectedHook.onCommitFiberRoot(
            rendererID,
            finishedWork,
            void 0,
            128 === (finishedWork.current.flags & 128)
          );
        } catch (err) {
        }
      if (null !== recoverableErrors) {
        finishedWork = ReactSharedInternals.T;
        remainingLanes = ReactDOMSharedInternals.p;
        ReactDOMSharedInternals.p = 2;
        ReactSharedInternals.T = null;
        try {
          for (var onRecoverableError = root3.onRecoverableError, i = 0; i < recoverableErrors.length; i++) {
            var recoverableError = recoverableErrors[i];
            onRecoverableError(recoverableError.value, {
              componentStack: recoverableError.stack
            });
          }
        } finally {
          ReactSharedInternals.T = finishedWork, ReactDOMSharedInternals.p = remainingLanes;
        }
      }
      0 !== (pendingEffectsLanes & 3) && flushPendingEffects();
      ensureRootIsScheduled(root3);
      remainingLanes = root3.pendingLanes;
      0 !== (lanes & 261930) && 0 !== (remainingLanes & 42) ? root3 === rootWithNestedUpdates ? nestedUpdateCount++ : (nestedUpdateCount = 0, rootWithNestedUpdates = root3) : nestedUpdateCount = 0;
      flushSyncWorkAcrossRoots_impl(0);
    }
  }
  function releaseRootPooledCache(root3, remainingLanes) {
    0 === (root3.pooledCacheLanes &= remainingLanes) && (remainingLanes = root3.pooledCache, null != remainingLanes && (root3.pooledCache = null, releaseCache(remainingLanes)));
  }
  function flushPendingEffects() {
    flushMutationEffects();
    flushLayoutEffects();
    flushSpawnedWork();
    return flushPassiveEffects();
  }
  function flushPassiveEffects() {
    if (5 !== pendingEffectsStatus) return false;
    var root3 = pendingEffectsRoot, remainingLanes = pendingEffectsRemainingLanes;
    pendingEffectsRemainingLanes = 0;
    var renderPriority = lanesToEventPriority(pendingEffectsLanes), prevTransition = ReactSharedInternals.T, previousPriority = ReactDOMSharedInternals.p;
    try {
      ReactDOMSharedInternals.p = 32 > renderPriority ? 32 : renderPriority;
      ReactSharedInternals.T = null;
      renderPriority = pendingPassiveTransitions;
      pendingPassiveTransitions = null;
      var root$jscomp$0 = pendingEffectsRoot, lanes = pendingEffectsLanes;
      pendingEffectsStatus = 0;
      pendingFinishedWork = pendingEffectsRoot = null;
      pendingEffectsLanes = 0;
      if (0 !== (executionContext & 6)) throw Error(formatProdErrorMessage(331));
      var prevExecutionContext = executionContext;
      executionContext |= 4;
      commitPassiveUnmountOnFiber(root$jscomp$0.current);
      commitPassiveMountOnFiber(
        root$jscomp$0,
        root$jscomp$0.current,
        lanes,
        renderPriority
      );
      executionContext = prevExecutionContext;
      flushSyncWorkAcrossRoots_impl(0, false);
      if (injectedHook && "function" === typeof injectedHook.onPostCommitFiberRoot)
        try {
          injectedHook.onPostCommitFiberRoot(rendererID, root$jscomp$0);
        } catch (err) {
        }
      return true;
    } finally {
      ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = prevTransition, releaseRootPooledCache(root3, remainingLanes);
    }
  }
  function captureCommitPhaseErrorOnRoot(rootFiber, sourceFiber, error) {
    sourceFiber = createCapturedValueAtFiber(error, sourceFiber);
    sourceFiber = createRootErrorUpdate(rootFiber.stateNode, sourceFiber, 2);
    rootFiber = enqueueUpdate(rootFiber, sourceFiber, 2);
    null !== rootFiber && (markRootUpdated$1(rootFiber, 2), ensureRootIsScheduled(rootFiber));
  }
  function captureCommitPhaseError(sourceFiber, nearestMountedAncestor, error) {
    if (3 === sourceFiber.tag)
      captureCommitPhaseErrorOnRoot(sourceFiber, sourceFiber, error);
    else
      for (; null !== nearestMountedAncestor; ) {
        if (3 === nearestMountedAncestor.tag) {
          captureCommitPhaseErrorOnRoot(
            nearestMountedAncestor,
            sourceFiber,
            error
          );
          break;
        } else if (1 === nearestMountedAncestor.tag) {
          var instance = nearestMountedAncestor.stateNode;
          if ("function" === typeof nearestMountedAncestor.type.getDerivedStateFromError || "function" === typeof instance.componentDidCatch && (null === legacyErrorBoundariesThatAlreadyFailed || !legacyErrorBoundariesThatAlreadyFailed.has(instance))) {
            sourceFiber = createCapturedValueAtFiber(error, sourceFiber);
            error = createClassErrorUpdate(2);
            instance = enqueueUpdate(nearestMountedAncestor, error, 2);
            null !== instance && (initializeClassErrorUpdate(
              error,
              instance,
              nearestMountedAncestor,
              sourceFiber
            ), markRootUpdated$1(instance, 2), ensureRootIsScheduled(instance));
            break;
          }
        }
        nearestMountedAncestor = nearestMountedAncestor.return;
      }
  }
  function attachPingListener(root3, wakeable, lanes) {
    var pingCache = root3.pingCache;
    if (null === pingCache) {
      pingCache = root3.pingCache = new PossiblyWeakMap();
      var threadIDs = /* @__PURE__ */ new Set();
      pingCache.set(wakeable, threadIDs);
    } else
      threadIDs = pingCache.get(wakeable), void 0 === threadIDs && (threadIDs = /* @__PURE__ */ new Set(), pingCache.set(wakeable, threadIDs));
    threadIDs.has(lanes) || (workInProgressRootDidAttachPingListener = true, threadIDs.add(lanes), root3 = pingSuspendedRoot.bind(null, root3, wakeable, lanes), wakeable.then(root3, root3));
  }
  function pingSuspendedRoot(root3, wakeable, pingedLanes) {
    var pingCache = root3.pingCache;
    null !== pingCache && pingCache.delete(wakeable);
    root3.pingedLanes |= root3.suspendedLanes & pingedLanes;
    root3.warmLanes &= ~pingedLanes;
    workInProgressRoot === root3 && (workInProgressRootRenderLanes & pingedLanes) === pingedLanes && (4 === workInProgressRootExitStatus || 3 === workInProgressRootExitStatus && (workInProgressRootRenderLanes & 62914560) === workInProgressRootRenderLanes && 300 > now() - globalMostRecentFallbackTime ? 0 === (executionContext & 2) && prepareFreshStack(root3, 0) : workInProgressRootPingedLanes |= pingedLanes, workInProgressSuspendedRetryLanes === workInProgressRootRenderLanes && (workInProgressSuspendedRetryLanes = 0));
    ensureRootIsScheduled(root3);
  }
  function retryTimedOutBoundary(boundaryFiber, retryLane) {
    0 === retryLane && (retryLane = claimNextRetryLane());
    boundaryFiber = enqueueConcurrentRenderForLane(boundaryFiber, retryLane);
    null !== boundaryFiber && (markRootUpdated$1(boundaryFiber, retryLane), ensureRootIsScheduled(boundaryFiber));
  }
  function retryDehydratedSuspenseBoundary(boundaryFiber) {
    var suspenseState = boundaryFiber.memoizedState, retryLane = 0;
    null !== suspenseState && (retryLane = suspenseState.retryLane);
    retryTimedOutBoundary(boundaryFiber, retryLane);
  }
  function resolveRetryWakeable(boundaryFiber, wakeable) {
    var retryLane = 0;
    switch (boundaryFiber.tag) {
      case 31:
      case 13:
        var retryCache = boundaryFiber.stateNode;
        var suspenseState = boundaryFiber.memoizedState;
        null !== suspenseState && (retryLane = suspenseState.retryLane);
        break;
      case 19:
        retryCache = boundaryFiber.stateNode;
        break;
      case 22:
        retryCache = boundaryFiber.stateNode._retryCache;
        break;
      default:
        throw Error(formatProdErrorMessage(314));
    }
    null !== retryCache && retryCache.delete(wakeable);
    retryTimedOutBoundary(boundaryFiber, retryLane);
  }
  function scheduleCallback$1(priorityLevel, callback) {
    return scheduleCallback$3(priorityLevel, callback);
  }
  var firstScheduledRoot = null, lastScheduledRoot = null, didScheduleMicrotask = false, mightHavePendingSyncWork = false, isFlushingWork = false, currentEventTransitionLane = 0;
  function ensureRootIsScheduled(root3) {
    root3 !== lastScheduledRoot && null === root3.next && (null === lastScheduledRoot ? firstScheduledRoot = lastScheduledRoot = root3 : lastScheduledRoot = lastScheduledRoot.next = root3);
    mightHavePendingSyncWork = true;
    didScheduleMicrotask || (didScheduleMicrotask = true, scheduleImmediateRootScheduleTask());
  }
  function flushSyncWorkAcrossRoots_impl(syncTransitionLanes, onlyLegacy) {
    if (!isFlushingWork && mightHavePendingSyncWork) {
      isFlushingWork = true;
      do {
        var didPerformSomeWork = false;
        for (var root$170 = firstScheduledRoot; null !== root$170; ) {
          if (0 !== syncTransitionLanes) {
            var pendingLanes = root$170.pendingLanes;
            if (0 === pendingLanes) var JSCompiler_inline_result = 0;
            else {
              var suspendedLanes = root$170.suspendedLanes, pingedLanes = root$170.pingedLanes;
              JSCompiler_inline_result = (1 << 31 - clz32(42 | syncTransitionLanes) + 1) - 1;
              JSCompiler_inline_result &= pendingLanes & ~(suspendedLanes & ~pingedLanes);
              JSCompiler_inline_result = JSCompiler_inline_result & 201326741 ? JSCompiler_inline_result & 201326741 | 1 : JSCompiler_inline_result ? JSCompiler_inline_result | 2 : 0;
            }
            0 !== JSCompiler_inline_result && (didPerformSomeWork = true, performSyncWorkOnRoot(root$170, JSCompiler_inline_result));
          } else
            JSCompiler_inline_result = workInProgressRootRenderLanes, JSCompiler_inline_result = getNextLanes(
              root$170,
              root$170 === workInProgressRoot ? JSCompiler_inline_result : 0,
              null !== root$170.cancelPendingCommit || -1 !== root$170.timeoutHandle
            ), 0 === (JSCompiler_inline_result & 3) || checkIfRootIsPrerendering(root$170, JSCompiler_inline_result) || (didPerformSomeWork = true, performSyncWorkOnRoot(root$170, JSCompiler_inline_result));
          root$170 = root$170.next;
        }
      } while (didPerformSomeWork);
      isFlushingWork = false;
    }
  }
  function processRootScheduleInImmediateTask() {
    processRootScheduleInMicrotask();
  }
  function processRootScheduleInMicrotask() {
    mightHavePendingSyncWork = didScheduleMicrotask = false;
    var syncTransitionLanes = 0;
    0 !== currentEventTransitionLane && shouldAttemptEagerTransition() && (syncTransitionLanes = currentEventTransitionLane);
    for (var currentTime = now(), prev = null, root3 = firstScheduledRoot; null !== root3; ) {
      var next = root3.next, nextLanes = scheduleTaskForRootDuringMicrotask(root3, currentTime);
      if (0 === nextLanes)
        root3.next = null, null === prev ? firstScheduledRoot = next : prev.next = next, null === next && (lastScheduledRoot = prev);
      else if (prev = root3, 0 !== syncTransitionLanes || 0 !== (nextLanes & 3))
        mightHavePendingSyncWork = true;
      root3 = next;
    }
    0 !== pendingEffectsStatus && 5 !== pendingEffectsStatus || flushSyncWorkAcrossRoots_impl(syncTransitionLanes);
    0 !== currentEventTransitionLane && (currentEventTransitionLane = 0);
  }
  function scheduleTaskForRootDuringMicrotask(root3, currentTime) {
    for (var suspendedLanes = root3.suspendedLanes, pingedLanes = root3.pingedLanes, expirationTimes = root3.expirationTimes, lanes = root3.pendingLanes & -62914561; 0 < lanes; ) {
      var index$5 = 31 - clz32(lanes), lane = 1 << index$5, expirationTime = expirationTimes[index$5];
      if (-1 === expirationTime) {
        if (0 === (lane & suspendedLanes) || 0 !== (lane & pingedLanes))
          expirationTimes[index$5] = computeExpirationTime(lane, currentTime);
      } else expirationTime <= currentTime && (root3.expiredLanes |= lane);
      lanes &= ~lane;
    }
    currentTime = workInProgressRoot;
    suspendedLanes = workInProgressRootRenderLanes;
    suspendedLanes = getNextLanes(
      root3,
      root3 === currentTime ? suspendedLanes : 0,
      null !== root3.cancelPendingCommit || -1 !== root3.timeoutHandle
    );
    pingedLanes = root3.callbackNode;
    if (0 === suspendedLanes || root3 === currentTime && (2 === workInProgressSuspendedReason || 9 === workInProgressSuspendedReason) || null !== root3.cancelPendingCommit)
      return null !== pingedLanes && null !== pingedLanes && cancelCallback$1(pingedLanes), root3.callbackNode = null, root3.callbackPriority = 0;
    if (0 === (suspendedLanes & 3) || checkIfRootIsPrerendering(root3, suspendedLanes)) {
      currentTime = suspendedLanes & -suspendedLanes;
      if (currentTime === root3.callbackPriority) return currentTime;
      null !== pingedLanes && cancelCallback$1(pingedLanes);
      switch (lanesToEventPriority(suspendedLanes)) {
        case 2:
        case 8:
          suspendedLanes = UserBlockingPriority;
          break;
        case 32:
          suspendedLanes = NormalPriority$1;
          break;
        case 268435456:
          suspendedLanes = IdlePriority;
          break;
        default:
          suspendedLanes = NormalPriority$1;
      }
      pingedLanes = performWorkOnRootViaSchedulerTask.bind(null, root3);
      suspendedLanes = scheduleCallback$3(suspendedLanes, pingedLanes);
      root3.callbackPriority = currentTime;
      root3.callbackNode = suspendedLanes;
      return currentTime;
    }
    null !== pingedLanes && null !== pingedLanes && cancelCallback$1(pingedLanes);
    root3.callbackPriority = 2;
    root3.callbackNode = null;
    return 2;
  }
  function performWorkOnRootViaSchedulerTask(root3, didTimeout) {
    if (0 !== pendingEffectsStatus && 5 !== pendingEffectsStatus)
      return root3.callbackNode = null, root3.callbackPriority = 0, null;
    var originalCallbackNode = root3.callbackNode;
    if (flushPendingEffects() && root3.callbackNode !== originalCallbackNode)
      return null;
    var workInProgressRootRenderLanes$jscomp$0 = workInProgressRootRenderLanes;
    workInProgressRootRenderLanes$jscomp$0 = getNextLanes(
      root3,
      root3 === workInProgressRoot ? workInProgressRootRenderLanes$jscomp$0 : 0,
      null !== root3.cancelPendingCommit || -1 !== root3.timeoutHandle
    );
    if (0 === workInProgressRootRenderLanes$jscomp$0) return null;
    performWorkOnRoot(root3, workInProgressRootRenderLanes$jscomp$0, didTimeout);
    scheduleTaskForRootDuringMicrotask(root3, now());
    return null != root3.callbackNode && root3.callbackNode === originalCallbackNode ? performWorkOnRootViaSchedulerTask.bind(null, root3) : null;
  }
  function performSyncWorkOnRoot(root3, lanes) {
    if (flushPendingEffects()) return null;
    performWorkOnRoot(root3, lanes, true);
  }
  function scheduleImmediateRootScheduleTask() {
    scheduleMicrotask(function() {
      0 !== (executionContext & 6) ? scheduleCallback$3(
        ImmediatePriority,
        processRootScheduleInImmediateTask
      ) : processRootScheduleInMicrotask();
    });
  }
  function requestTransitionLane() {
    if (0 === currentEventTransitionLane) {
      var actionScopeLane = currentEntangledLane;
      0 === actionScopeLane && (actionScopeLane = nextTransitionUpdateLane, nextTransitionUpdateLane <<= 1, 0 === (nextTransitionUpdateLane & 261888) && (nextTransitionUpdateLane = 256));
      currentEventTransitionLane = actionScopeLane;
    }
    return currentEventTransitionLane;
  }
  function coerceFormActionProp(actionProp) {
    return null == actionProp || "symbol" === typeof actionProp || "boolean" === typeof actionProp ? null : "function" === typeof actionProp ? actionProp : sanitizeURL("" + actionProp);
  }
  function createFormDataWithSubmitter(form, submitter) {
    var temp = submitter.ownerDocument.createElement("input");
    temp.name = submitter.name;
    temp.value = submitter.value;
    form.id && temp.setAttribute("form", form.id);
    submitter.parentNode.insertBefore(temp, submitter);
    form = new FormData(form);
    temp.parentNode.removeChild(temp);
    return form;
  }
  function extractEvents$1(dispatchQueue, domEventName, maybeTargetInst, nativeEvent, nativeEventTarget) {
    if ("submit" === domEventName && maybeTargetInst && maybeTargetInst.stateNode === nativeEventTarget) {
      var action = coerceFormActionProp(
        (nativeEventTarget[internalPropsKey] || null).action
      ), submitter = nativeEvent.submitter;
      submitter && (domEventName = (domEventName = submitter[internalPropsKey] || null) ? coerceFormActionProp(domEventName.formAction) : submitter.getAttribute("formAction"), null !== domEventName && (action = domEventName, submitter = null));
      var event = new SyntheticEvent(
        "action",
        "action",
        null,
        nativeEvent,
        nativeEventTarget
      );
      dispatchQueue.push({
        event,
        listeners: [
          {
            instance: null,
            listener: function() {
              if (nativeEvent.defaultPrevented) {
                if (0 !== currentEventTransitionLane) {
                  var formData = submitter ? createFormDataWithSubmitter(nativeEventTarget, submitter) : new FormData(nativeEventTarget);
                  startHostTransition(
                    maybeTargetInst,
                    {
                      pending: true,
                      data: formData,
                      method: nativeEventTarget.method,
                      action
                    },
                    null,
                    formData
                  );
                }
              } else
                "function" === typeof action && (event.preventDefault(), formData = submitter ? createFormDataWithSubmitter(nativeEventTarget, submitter) : new FormData(nativeEventTarget), startHostTransition(
                  maybeTargetInst,
                  {
                    pending: true,
                    data: formData,
                    method: nativeEventTarget.method,
                    action
                  },
                  action,
                  formData
                ));
            },
            currentTarget: nativeEventTarget
          }
        ]
      });
    }
  }
  for (var i$jscomp$inline_1577 = 0; i$jscomp$inline_1577 < simpleEventPluginEvents.length; i$jscomp$inline_1577++) {
    var eventName$jscomp$inline_1578 = simpleEventPluginEvents[i$jscomp$inline_1577], domEventName$jscomp$inline_1579 = eventName$jscomp$inline_1578.toLowerCase(), capitalizedEvent$jscomp$inline_1580 = eventName$jscomp$inline_1578[0].toUpperCase() + eventName$jscomp$inline_1578.slice(1);
    registerSimpleEvent(
      domEventName$jscomp$inline_1579,
      "on" + capitalizedEvent$jscomp$inline_1580
    );
  }
  registerSimpleEvent(ANIMATION_END, "onAnimationEnd");
  registerSimpleEvent(ANIMATION_ITERATION, "onAnimationIteration");
  registerSimpleEvent(ANIMATION_START, "onAnimationStart");
  registerSimpleEvent("dblclick", "onDoubleClick");
  registerSimpleEvent("focusin", "onFocus");
  registerSimpleEvent("focusout", "onBlur");
  registerSimpleEvent(TRANSITION_RUN, "onTransitionRun");
  registerSimpleEvent(TRANSITION_START, "onTransitionStart");
  registerSimpleEvent(TRANSITION_CANCEL, "onTransitionCancel");
  registerSimpleEvent(TRANSITION_END, "onTransitionEnd");
  registerDirectEvent("onMouseEnter", ["mouseout", "mouseover"]);
  registerDirectEvent("onMouseLeave", ["mouseout", "mouseover"]);
  registerDirectEvent("onPointerEnter", ["pointerout", "pointerover"]);
  registerDirectEvent("onPointerLeave", ["pointerout", "pointerover"]);
  registerTwoPhaseEvent(
    "onChange",
    "change click focusin focusout input keydown keyup selectionchange".split(" ")
  );
  registerTwoPhaseEvent(
    "onSelect",
    "focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(
      " "
    )
  );
  registerTwoPhaseEvent("onBeforeInput", [
    "compositionend",
    "keypress",
    "textInput",
    "paste"
  ]);
  registerTwoPhaseEvent(
    "onCompositionEnd",
    "compositionend focusout keydown keypress keyup mousedown".split(" ")
  );
  registerTwoPhaseEvent(
    "onCompositionStart",
    "compositionstart focusout keydown keypress keyup mousedown".split(" ")
  );
  registerTwoPhaseEvent(
    "onCompositionUpdate",
    "compositionupdate focusout keydown keypress keyup mousedown".split(" ")
  );
  var mediaEventTypes = "abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(
    " "
  ), nonDelegatedEvents = new Set(
    "beforetoggle cancel close invalid load scroll scrollend toggle".split(" ").concat(mediaEventTypes)
  );
  function processDispatchQueue(dispatchQueue, eventSystemFlags) {
    eventSystemFlags = 0 !== (eventSystemFlags & 4);
    for (var i = 0; i < dispatchQueue.length; i++) {
      var _dispatchQueue$i = dispatchQueue[i], event = _dispatchQueue$i.event;
      _dispatchQueue$i = _dispatchQueue$i.listeners;
      a: {
        var previousInstance = void 0;
        if (eventSystemFlags)
          for (var i$jscomp$0 = _dispatchQueue$i.length - 1; 0 <= i$jscomp$0; i$jscomp$0--) {
            var _dispatchListeners$i = _dispatchQueue$i[i$jscomp$0], instance = _dispatchListeners$i.instance, currentTarget = _dispatchListeners$i.currentTarget;
            _dispatchListeners$i = _dispatchListeners$i.listener;
            if (instance !== previousInstance && event.isPropagationStopped())
              break a;
            previousInstance = _dispatchListeners$i;
            event.currentTarget = currentTarget;
            try {
              previousInstance(event);
            } catch (error) {
              reportGlobalError(error);
            }
            event.currentTarget = null;
            previousInstance = instance;
          }
        else
          for (i$jscomp$0 = 0; i$jscomp$0 < _dispatchQueue$i.length; i$jscomp$0++) {
            _dispatchListeners$i = _dispatchQueue$i[i$jscomp$0];
            instance = _dispatchListeners$i.instance;
            currentTarget = _dispatchListeners$i.currentTarget;
            _dispatchListeners$i = _dispatchListeners$i.listener;
            if (instance !== previousInstance && event.isPropagationStopped())
              break a;
            previousInstance = _dispatchListeners$i;
            event.currentTarget = currentTarget;
            try {
              previousInstance(event);
            } catch (error) {
              reportGlobalError(error);
            }
            event.currentTarget = null;
            previousInstance = instance;
          }
      }
    }
  }
  function listenToNonDelegatedEvent(domEventName, targetElement) {
    var JSCompiler_inline_result = targetElement[internalEventHandlersKey];
    void 0 === JSCompiler_inline_result && (JSCompiler_inline_result = targetElement[internalEventHandlersKey] = /* @__PURE__ */ new Set());
    var listenerSetKey = domEventName + "__bubble";
    JSCompiler_inline_result.has(listenerSetKey) || (addTrappedEventListener(targetElement, domEventName, 2, false), JSCompiler_inline_result.add(listenerSetKey));
  }
  function listenToNativeEvent(domEventName, isCapturePhaseListener, target) {
    var eventSystemFlags = 0;
    isCapturePhaseListener && (eventSystemFlags |= 4);
    addTrappedEventListener(
      target,
      domEventName,
      eventSystemFlags,
      isCapturePhaseListener
    );
  }
  var listeningMarker = "_reactListening" + Math.random().toString(36).slice(2);
  function listenToAllSupportedEvents(rootContainerElement) {
    if (!rootContainerElement[listeningMarker]) {
      rootContainerElement[listeningMarker] = true;
      allNativeEvents.forEach(function(domEventName) {
        "selectionchange" !== domEventName && (nonDelegatedEvents.has(domEventName) || listenToNativeEvent(domEventName, false, rootContainerElement), listenToNativeEvent(domEventName, true, rootContainerElement));
      });
      var ownerDocument = 9 === rootContainerElement.nodeType ? rootContainerElement : rootContainerElement.ownerDocument;
      null === ownerDocument || ownerDocument[listeningMarker] || (ownerDocument[listeningMarker] = true, listenToNativeEvent("selectionchange", false, ownerDocument));
    }
  }
  function addTrappedEventListener(targetContainer, domEventName, eventSystemFlags, isCapturePhaseListener) {
    switch (getEventPriority(domEventName)) {
      case 2:
        var listenerWrapper = dispatchDiscreteEvent;
        break;
      case 8:
        listenerWrapper = dispatchContinuousEvent;
        break;
      default:
        listenerWrapper = dispatchEvent;
    }
    eventSystemFlags = listenerWrapper.bind(
      null,
      domEventName,
      eventSystemFlags,
      targetContainer
    );
    listenerWrapper = void 0;
    !passiveBrowserEventsSupported || "touchstart" !== domEventName && "touchmove" !== domEventName && "wheel" !== domEventName || (listenerWrapper = true);
    isCapturePhaseListener ? void 0 !== listenerWrapper ? targetContainer.addEventListener(domEventName, eventSystemFlags, {
      capture: true,
      passive: listenerWrapper
    }) : targetContainer.addEventListener(domEventName, eventSystemFlags, true) : void 0 !== listenerWrapper ? targetContainer.addEventListener(domEventName, eventSystemFlags, {
      passive: listenerWrapper
    }) : targetContainer.addEventListener(domEventName, eventSystemFlags, false);
  }
  function dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, targetInst$jscomp$0, targetContainer) {
    var ancestorInst = targetInst$jscomp$0;
    if (0 === (eventSystemFlags & 1) && 0 === (eventSystemFlags & 2) && null !== targetInst$jscomp$0)
      a: for (; ; ) {
        if (null === targetInst$jscomp$0) return;
        var nodeTag = targetInst$jscomp$0.tag;
        if (3 === nodeTag || 4 === nodeTag) {
          var container = targetInst$jscomp$0.stateNode.containerInfo;
          if (container === targetContainer) break;
          if (4 === nodeTag)
            for (nodeTag = targetInst$jscomp$0.return; null !== nodeTag; ) {
              var grandTag = nodeTag.tag;
              if ((3 === grandTag || 4 === grandTag) && nodeTag.stateNode.containerInfo === targetContainer)
                return;
              nodeTag = nodeTag.return;
            }
          for (; null !== container; ) {
            nodeTag = getClosestInstanceFromNode(container);
            if (null === nodeTag) return;
            grandTag = nodeTag.tag;
            if (5 === grandTag || 6 === grandTag || 26 === grandTag || 27 === grandTag) {
              targetInst$jscomp$0 = ancestorInst = nodeTag;
              continue a;
            }
            container = container.parentNode;
          }
        }
        targetInst$jscomp$0 = targetInst$jscomp$0.return;
      }
    batchedUpdates$1(function() {
      var targetInst = ancestorInst, nativeEventTarget = getEventTarget(nativeEvent), dispatchQueue = [];
      a: {
        var reactName = topLevelEventsToReactNames.get(domEventName);
        if (void 0 !== reactName) {
          var SyntheticEventCtor = SyntheticEvent, reactEventType = domEventName;
          switch (domEventName) {
            case "keypress":
              if (0 === getEventCharCode(nativeEvent)) break a;
            case "keydown":
            case "keyup":
              SyntheticEventCtor = SyntheticKeyboardEvent;
              break;
            case "focusin":
              reactEventType = "focus";
              SyntheticEventCtor = SyntheticFocusEvent;
              break;
            case "focusout":
              reactEventType = "blur";
              SyntheticEventCtor = SyntheticFocusEvent;
              break;
            case "beforeblur":
            case "afterblur":
              SyntheticEventCtor = SyntheticFocusEvent;
              break;
            case "click":
              if (2 === nativeEvent.button) break a;
            case "auxclick":
            case "dblclick":
            case "mousedown":
            case "mousemove":
            case "mouseup":
            case "mouseout":
            case "mouseover":
            case "contextmenu":
              SyntheticEventCtor = SyntheticMouseEvent;
              break;
            case "drag":
            case "dragend":
            case "dragenter":
            case "dragexit":
            case "dragleave":
            case "dragover":
            case "dragstart":
            case "drop":
              SyntheticEventCtor = SyntheticDragEvent;
              break;
            case "touchcancel":
            case "touchend":
            case "touchmove":
            case "touchstart":
              SyntheticEventCtor = SyntheticTouchEvent;
              break;
            case ANIMATION_END:
            case ANIMATION_ITERATION:
            case ANIMATION_START:
              SyntheticEventCtor = SyntheticAnimationEvent;
              break;
            case TRANSITION_END:
              SyntheticEventCtor = SyntheticTransitionEvent;
              break;
            case "scroll":
            case "scrollend":
              SyntheticEventCtor = SyntheticUIEvent;
              break;
            case "wheel":
              SyntheticEventCtor = SyntheticWheelEvent;
              break;
            case "copy":
            case "cut":
            case "paste":
              SyntheticEventCtor = SyntheticClipboardEvent;
              break;
            case "gotpointercapture":
            case "lostpointercapture":
            case "pointercancel":
            case "pointerdown":
            case "pointermove":
            case "pointerout":
            case "pointerover":
            case "pointerup":
              SyntheticEventCtor = SyntheticPointerEvent;
              break;
            case "toggle":
            case "beforetoggle":
              SyntheticEventCtor = SyntheticToggleEvent;
          }
          var inCapturePhase = 0 !== (eventSystemFlags & 4), accumulateTargetOnly = !inCapturePhase && ("scroll" === domEventName || "scrollend" === domEventName), reactEventName = inCapturePhase ? null !== reactName ? reactName + "Capture" : null : reactName;
          inCapturePhase = [];
          for (var instance = targetInst, lastHostComponent; null !== instance; ) {
            var _instance = instance;
            lastHostComponent = _instance.stateNode;
            _instance = _instance.tag;
            5 !== _instance && 26 !== _instance && 27 !== _instance || null === lastHostComponent || null === reactEventName || (_instance = getListener(instance, reactEventName), null != _instance && inCapturePhase.push(
              createDispatchListener(instance, _instance, lastHostComponent)
            ));
            if (accumulateTargetOnly) break;
            instance = instance.return;
          }
          0 < inCapturePhase.length && (reactName = new SyntheticEventCtor(
            reactName,
            reactEventType,
            null,
            nativeEvent,
            nativeEventTarget
          ), dispatchQueue.push({ event: reactName, listeners: inCapturePhase }));
        }
      }
      if (0 === (eventSystemFlags & 7)) {
        a: {
          reactName = "mouseover" === domEventName || "pointerover" === domEventName;
          SyntheticEventCtor = "mouseout" === domEventName || "pointerout" === domEventName;
          if (reactName && nativeEvent !== currentReplayingEvent && (reactEventType = nativeEvent.relatedTarget || nativeEvent.fromElement) && (getClosestInstanceFromNode(reactEventType) || reactEventType[internalContainerInstanceKey]))
            break a;
          if (SyntheticEventCtor || reactName) {
            reactName = nativeEventTarget.window === nativeEventTarget ? nativeEventTarget : (reactName = nativeEventTarget.ownerDocument) ? reactName.defaultView || reactName.parentWindow : window;
            if (SyntheticEventCtor) {
              if (reactEventType = nativeEvent.relatedTarget || nativeEvent.toElement, SyntheticEventCtor = targetInst, reactEventType = reactEventType ? getClosestInstanceFromNode(reactEventType) : null, null !== reactEventType && (accumulateTargetOnly = getNearestMountedFiber(reactEventType), inCapturePhase = reactEventType.tag, reactEventType !== accumulateTargetOnly || 5 !== inCapturePhase && 27 !== inCapturePhase && 6 !== inCapturePhase))
                reactEventType = null;
            } else SyntheticEventCtor = null, reactEventType = targetInst;
            if (SyntheticEventCtor !== reactEventType) {
              inCapturePhase = SyntheticMouseEvent;
              _instance = "onMouseLeave";
              reactEventName = "onMouseEnter";
              instance = "mouse";
              if ("pointerout" === domEventName || "pointerover" === domEventName)
                inCapturePhase = SyntheticPointerEvent, _instance = "onPointerLeave", reactEventName = "onPointerEnter", instance = "pointer";
              accumulateTargetOnly = null == SyntheticEventCtor ? reactName : getNodeFromInstance(SyntheticEventCtor);
              lastHostComponent = null == reactEventType ? reactName : getNodeFromInstance(reactEventType);
              reactName = new inCapturePhase(
                _instance,
                instance + "leave",
                SyntheticEventCtor,
                nativeEvent,
                nativeEventTarget
              );
              reactName.target = accumulateTargetOnly;
              reactName.relatedTarget = lastHostComponent;
              _instance = null;
              getClosestInstanceFromNode(nativeEventTarget) === targetInst && (inCapturePhase = new inCapturePhase(
                reactEventName,
                instance + "enter",
                reactEventType,
                nativeEvent,
                nativeEventTarget
              ), inCapturePhase.target = lastHostComponent, inCapturePhase.relatedTarget = accumulateTargetOnly, _instance = inCapturePhase);
              accumulateTargetOnly = _instance;
              if (SyntheticEventCtor && reactEventType)
                b: {
                  inCapturePhase = getParent;
                  reactEventName = SyntheticEventCtor;
                  instance = reactEventType;
                  lastHostComponent = 0;
                  for (_instance = reactEventName; _instance; _instance = inCapturePhase(_instance))
                    lastHostComponent++;
                  _instance = 0;
                  for (var tempB = instance; tempB; tempB = inCapturePhase(tempB))
                    _instance++;
                  for (; 0 < lastHostComponent - _instance; )
                    reactEventName = inCapturePhase(reactEventName), lastHostComponent--;
                  for (; 0 < _instance - lastHostComponent; )
                    instance = inCapturePhase(instance), _instance--;
                  for (; lastHostComponent--; ) {
                    if (reactEventName === instance || null !== instance && reactEventName === instance.alternate) {
                      inCapturePhase = reactEventName;
                      break b;
                    }
                    reactEventName = inCapturePhase(reactEventName);
                    instance = inCapturePhase(instance);
                  }
                  inCapturePhase = null;
                }
              else inCapturePhase = null;
              null !== SyntheticEventCtor && accumulateEnterLeaveListenersForEvent(
                dispatchQueue,
                reactName,
                SyntheticEventCtor,
                inCapturePhase,
                false
              );
              null !== reactEventType && null !== accumulateTargetOnly && accumulateEnterLeaveListenersForEvent(
                dispatchQueue,
                accumulateTargetOnly,
                reactEventType,
                inCapturePhase,
                true
              );
            }
          }
        }
        a: {
          reactName = targetInst ? getNodeFromInstance(targetInst) : window;
          SyntheticEventCtor = reactName.nodeName && reactName.nodeName.toLowerCase();
          if ("select" === SyntheticEventCtor || "input" === SyntheticEventCtor && "file" === reactName.type)
            var getTargetInstFunc = getTargetInstForChangeEvent;
          else if (isTextInputElement(reactName))
            if (isInputEventSupported)
              getTargetInstFunc = getTargetInstForInputOrChangeEvent;
            else {
              getTargetInstFunc = getTargetInstForInputEventPolyfill;
              var handleEventFunc = handleEventsForInputEventPolyfill;
            }
          else
            SyntheticEventCtor = reactName.nodeName, !SyntheticEventCtor || "input" !== SyntheticEventCtor.toLowerCase() || "checkbox" !== reactName.type && "radio" !== reactName.type ? targetInst && isCustomElement(targetInst.elementType) && (getTargetInstFunc = getTargetInstForChangeEvent) : getTargetInstFunc = getTargetInstForClickEvent;
          if (getTargetInstFunc && (getTargetInstFunc = getTargetInstFunc(domEventName, targetInst))) {
            createAndAccumulateChangeEvent(
              dispatchQueue,
              getTargetInstFunc,
              nativeEvent,
              nativeEventTarget
            );
            break a;
          }
          handleEventFunc && handleEventFunc(domEventName, reactName, targetInst);
          "focusout" === domEventName && targetInst && "number" === reactName.type && null != targetInst.memoizedProps.value && setDefaultValue(reactName, "number", reactName.value);
        }
        handleEventFunc = targetInst ? getNodeFromInstance(targetInst) : window;
        switch (domEventName) {
          case "focusin":
            if (isTextInputElement(handleEventFunc) || "true" === handleEventFunc.contentEditable)
              activeElement = handleEventFunc, activeElementInst = targetInst, lastSelection = null;
            break;
          case "focusout":
            lastSelection = activeElementInst = activeElement = null;
            break;
          case "mousedown":
            mouseDown = true;
            break;
          case "contextmenu":
          case "mouseup":
          case "dragend":
            mouseDown = false;
            constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
            break;
          case "selectionchange":
            if (skipSelectionChangeEvent) break;
          case "keydown":
          case "keyup":
            constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
        }
        var fallbackData;
        if (canUseCompositionEvent)
          b: {
            switch (domEventName) {
              case "compositionstart":
                var eventType = "onCompositionStart";
                break b;
              case "compositionend":
                eventType = "onCompositionEnd";
                break b;
              case "compositionupdate":
                eventType = "onCompositionUpdate";
                break b;
            }
            eventType = void 0;
          }
        else
          isComposing ? isFallbackCompositionEnd(domEventName, nativeEvent) && (eventType = "onCompositionEnd") : "keydown" === domEventName && 229 === nativeEvent.keyCode && (eventType = "onCompositionStart");
        eventType && (useFallbackCompositionData && "ko" !== nativeEvent.locale && (isComposing || "onCompositionStart" !== eventType ? "onCompositionEnd" === eventType && isComposing && (fallbackData = getData()) : (root2 = nativeEventTarget, startText = "value" in root2 ? root2.value : root2.textContent, isComposing = true)), handleEventFunc = accumulateTwoPhaseListeners(targetInst, eventType), 0 < handleEventFunc.length && (eventType = new SyntheticCompositionEvent(
          eventType,
          domEventName,
          null,
          nativeEvent,
          nativeEventTarget
        ), dispatchQueue.push({ event: eventType, listeners: handleEventFunc }), fallbackData ? eventType.data = fallbackData : (fallbackData = getDataFromCustomEvent(nativeEvent), null !== fallbackData && (eventType.data = fallbackData))));
        if (fallbackData = canUseTextInputEvent ? getNativeBeforeInputChars(domEventName, nativeEvent) : getFallbackBeforeInputChars(domEventName, nativeEvent))
          eventType = accumulateTwoPhaseListeners(targetInst, "onBeforeInput"), 0 < eventType.length && (handleEventFunc = new SyntheticCompositionEvent(
            "onBeforeInput",
            "beforeinput",
            null,
            nativeEvent,
            nativeEventTarget
          ), dispatchQueue.push({
            event: handleEventFunc,
            listeners: eventType
          }), handleEventFunc.data = fallbackData);
        extractEvents$1(
          dispatchQueue,
          domEventName,
          targetInst,
          nativeEvent,
          nativeEventTarget
        );
      }
      processDispatchQueue(dispatchQueue, eventSystemFlags);
    });
  }
  function createDispatchListener(instance, listener, currentTarget) {
    return {
      instance,
      listener,
      currentTarget
    };
  }
  function accumulateTwoPhaseListeners(targetFiber, reactName) {
    for (var captureName = reactName + "Capture", listeners = []; null !== targetFiber; ) {
      var _instance2 = targetFiber, stateNode = _instance2.stateNode;
      _instance2 = _instance2.tag;
      5 !== _instance2 && 26 !== _instance2 && 27 !== _instance2 || null === stateNode || (_instance2 = getListener(targetFiber, captureName), null != _instance2 && listeners.unshift(
        createDispatchListener(targetFiber, _instance2, stateNode)
      ), _instance2 = getListener(targetFiber, reactName), null != _instance2 && listeners.push(
        createDispatchListener(targetFiber, _instance2, stateNode)
      ));
      if (3 === targetFiber.tag) return listeners;
      targetFiber = targetFiber.return;
    }
    return [];
  }
  function getParent(inst) {
    if (null === inst) return null;
    do
      inst = inst.return;
    while (inst && 5 !== inst.tag && 27 !== inst.tag);
    return inst ? inst : null;
  }
  function accumulateEnterLeaveListenersForEvent(dispatchQueue, event, target, common, inCapturePhase) {
    for (var registrationName = event._reactName, listeners = []; null !== target && target !== common; ) {
      var _instance3 = target, alternate = _instance3.alternate, stateNode = _instance3.stateNode;
      _instance3 = _instance3.tag;
      if (null !== alternate && alternate === common) break;
      5 !== _instance3 && 26 !== _instance3 && 27 !== _instance3 || null === stateNode || (alternate = stateNode, inCapturePhase ? (stateNode = getListener(target, registrationName), null != stateNode && listeners.unshift(
        createDispatchListener(target, stateNode, alternate)
      )) : inCapturePhase || (stateNode = getListener(target, registrationName), null != stateNode && listeners.push(
        createDispatchListener(target, stateNode, alternate)
      )));
      target = target.return;
    }
    0 !== listeners.length && dispatchQueue.push({ event, listeners });
  }
  var NORMALIZE_NEWLINES_REGEX = /\r\n?/g, NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g;
  function normalizeMarkupForTextOrAttribute(markup) {
    return ("string" === typeof markup ? markup : "" + markup).replace(NORMALIZE_NEWLINES_REGEX, "\n").replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, "");
  }
  function checkForUnmatchedText(serverText, clientText) {
    clientText = normalizeMarkupForTextOrAttribute(clientText);
    return normalizeMarkupForTextOrAttribute(serverText) === clientText ? true : false;
  }
  function setProp(domElement, tag, key, value, props, prevValue) {
    switch (key) {
      case "children":
        "string" === typeof value ? "body" === tag || "textarea" === tag && "" === value || setTextContent(domElement, value) : ("number" === typeof value || "bigint" === typeof value) && "body" !== tag && setTextContent(domElement, "" + value);
        break;
      case "className":
        setValueForKnownAttribute(domElement, "class", value);
        break;
      case "tabIndex":
        setValueForKnownAttribute(domElement, "tabindex", value);
        break;
      case "dir":
      case "role":
      case "viewBox":
      case "width":
      case "height":
        setValueForKnownAttribute(domElement, key, value);
        break;
      case "style":
        setValueForStyles(domElement, value, prevValue);
        break;
      case "data":
        if ("object" !== tag) {
          setValueForKnownAttribute(domElement, "data", value);
          break;
        }
      case "src":
      case "href":
        if ("" === value && ("a" !== tag || "href" !== key)) {
          domElement.removeAttribute(key);
          break;
        }
        if (null == value || "function" === typeof value || "symbol" === typeof value || "boolean" === typeof value) {
          domElement.removeAttribute(key);
          break;
        }
        value = sanitizeURL("" + value);
        domElement.setAttribute(key, value);
        break;
      case "action":
      case "formAction":
        if ("function" === typeof value) {
          domElement.setAttribute(
            key,
            "javascript:throw new Error('A React form was unexpectedly submitted. If you called form.submit() manually, consider using form.requestSubmit() instead. If you\\'re trying to use event.stopPropagation() in a submit event handler, consider also calling event.preventDefault().')"
          );
          break;
        } else
          "function" === typeof prevValue && ("formAction" === key ? ("input" !== tag && setProp(domElement, tag, "name", props.name, props, null), setProp(
            domElement,
            tag,
            "formEncType",
            props.formEncType,
            props,
            null
          ), setProp(
            domElement,
            tag,
            "formMethod",
            props.formMethod,
            props,
            null
          ), setProp(
            domElement,
            tag,
            "formTarget",
            props.formTarget,
            props,
            null
          )) : (setProp(domElement, tag, "encType", props.encType, props, null), setProp(domElement, tag, "method", props.method, props, null), setProp(domElement, tag, "target", props.target, props, null)));
        if (null == value || "symbol" === typeof value || "boolean" === typeof value) {
          domElement.removeAttribute(key);
          break;
        }
        value = sanitizeURL("" + value);
        domElement.setAttribute(key, value);
        break;
      case "onClick":
        null != value && (domElement.onclick = noop$1);
        break;
      case "onScroll":
        null != value && listenToNonDelegatedEvent("scroll", domElement);
        break;
      case "onScrollEnd":
        null != value && listenToNonDelegatedEvent("scrollend", domElement);
        break;
      case "dangerouslySetInnerHTML":
        if (null != value) {
          if ("object" !== typeof value || !("__html" in value))
            throw Error(formatProdErrorMessage(61));
          key = value.__html;
          if (null != key) {
            if (null != props.children) throw Error(formatProdErrorMessage(60));
            domElement.innerHTML = key;
          }
        }
        break;
      case "multiple":
        domElement.multiple = value && "function" !== typeof value && "symbol" !== typeof value;
        break;
      case "muted":
        domElement.muted = value && "function" !== typeof value && "symbol" !== typeof value;
        break;
      case "suppressContentEditableWarning":
      case "suppressHydrationWarning":
      case "defaultValue":
      case "defaultChecked":
      case "innerHTML":
      case "ref":
        break;
      case "autoFocus":
        break;
      case "xlinkHref":
        if (null == value || "function" === typeof value || "boolean" === typeof value || "symbol" === typeof value) {
          domElement.removeAttribute("xlink:href");
          break;
        }
        key = sanitizeURL("" + value);
        domElement.setAttributeNS(
          "http://www.w3.org/1999/xlink",
          "xlink:href",
          key
        );
        break;
      case "contentEditable":
      case "spellCheck":
      case "draggable":
      case "value":
      case "autoReverse":
      case "externalResourcesRequired":
      case "focusable":
      case "preserveAlpha":
        null != value && "function" !== typeof value && "symbol" !== typeof value ? domElement.setAttribute(key, "" + value) : domElement.removeAttribute(key);
        break;
      case "inert":
      case "allowFullScreen":
      case "async":
      case "autoPlay":
      case "controls":
      case "default":
      case "defer":
      case "disabled":
      case "disablePictureInPicture":
      case "disableRemotePlayback":
      case "formNoValidate":
      case "hidden":
      case "loop":
      case "noModule":
      case "noValidate":
      case "open":
      case "playsInline":
      case "readOnly":
      case "required":
      case "reversed":
      case "scoped":
      case "seamless":
      case "itemScope":
        value && "function" !== typeof value && "symbol" !== typeof value ? domElement.setAttribute(key, "") : domElement.removeAttribute(key);
        break;
      case "capture":
      case "download":
        true === value ? domElement.setAttribute(key, "") : false !== value && null != value && "function" !== typeof value && "symbol" !== typeof value ? domElement.setAttribute(key, value) : domElement.removeAttribute(key);
        break;
      case "cols":
      case "rows":
      case "size":
      case "span":
        null != value && "function" !== typeof value && "symbol" !== typeof value && !isNaN(value) && 1 <= value ? domElement.setAttribute(key, value) : domElement.removeAttribute(key);
        break;
      case "rowSpan":
      case "start":
        null == value || "function" === typeof value || "symbol" === typeof value || isNaN(value) ? domElement.removeAttribute(key) : domElement.setAttribute(key, value);
        break;
      case "popover":
        listenToNonDelegatedEvent("beforetoggle", domElement);
        listenToNonDelegatedEvent("toggle", domElement);
        setValueForAttribute(domElement, "popover", value);
        break;
      case "xlinkActuate":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:actuate",
          value
        );
        break;
      case "xlinkArcrole":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:arcrole",
          value
        );
        break;
      case "xlinkRole":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:role",
          value
        );
        break;
      case "xlinkShow":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:show",
          value
        );
        break;
      case "xlinkTitle":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:title",
          value
        );
        break;
      case "xlinkType":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:type",
          value
        );
        break;
      case "xmlBase":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/XML/1998/namespace",
          "xml:base",
          value
        );
        break;
      case "xmlLang":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/XML/1998/namespace",
          "xml:lang",
          value
        );
        break;
      case "xmlSpace":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/XML/1998/namespace",
          "xml:space",
          value
        );
        break;
      case "is":
        setValueForAttribute(domElement, "is", value);
        break;
      case "innerText":
      case "textContent":
        break;
      default:
        if (!(2 < key.length) || "o" !== key[0] && "O" !== key[0] || "n" !== key[1] && "N" !== key[1])
          key = aliases.get(key) || key, setValueForAttribute(domElement, key, value);
    }
  }
  function setPropOnCustomElement(domElement, tag, key, value, props, prevValue) {
    switch (key) {
      case "style":
        setValueForStyles(domElement, value, prevValue);
        break;
      case "dangerouslySetInnerHTML":
        if (null != value) {
          if ("object" !== typeof value || !("__html" in value))
            throw Error(formatProdErrorMessage(61));
          key = value.__html;
          if (null != key) {
            if (null != props.children) throw Error(formatProdErrorMessage(60));
            domElement.innerHTML = key;
          }
        }
        break;
      case "children":
        "string" === typeof value ? setTextContent(domElement, value) : ("number" === typeof value || "bigint" === typeof value) && setTextContent(domElement, "" + value);
        break;
      case "onScroll":
        null != value && listenToNonDelegatedEvent("scroll", domElement);
        break;
      case "onScrollEnd":
        null != value && listenToNonDelegatedEvent("scrollend", domElement);
        break;
      case "onClick":
        null != value && (domElement.onclick = noop$1);
        break;
      case "suppressContentEditableWarning":
      case "suppressHydrationWarning":
      case "innerHTML":
      case "ref":
        break;
      case "innerText":
      case "textContent":
        break;
      default:
        if (!registrationNameDependencies.hasOwnProperty(key))
          a: {
            if ("o" === key[0] && "n" === key[1] && (props = key.endsWith("Capture"), tag = key.slice(2, props ? key.length - 7 : void 0), prevValue = domElement[internalPropsKey] || null, prevValue = null != prevValue ? prevValue[key] : null, "function" === typeof prevValue && domElement.removeEventListener(tag, prevValue, props), "function" === typeof value)) {
              "function" !== typeof prevValue && null !== prevValue && (key in domElement ? domElement[key] = null : domElement.hasAttribute(key) && domElement.removeAttribute(key));
              domElement.addEventListener(tag, value, props);
              break a;
            }
            key in domElement ? domElement[key] = value : true === value ? domElement.setAttribute(key, "") : setValueForAttribute(domElement, key, value);
          }
    }
  }
  function setInitialProperties(domElement, tag, props) {
    switch (tag) {
      case "div":
      case "span":
      case "svg":
      case "path":
      case "a":
      case "g":
      case "p":
      case "li":
        break;
      case "img":
        listenToNonDelegatedEvent("error", domElement);
        listenToNonDelegatedEvent("load", domElement);
        var hasSrc = false, hasSrcSet = false, propKey;
        for (propKey in props)
          if (props.hasOwnProperty(propKey)) {
            var propValue = props[propKey];
            if (null != propValue)
              switch (propKey) {
                case "src":
                  hasSrc = true;
                  break;
                case "srcSet":
                  hasSrcSet = true;
                  break;
                case "children":
                case "dangerouslySetInnerHTML":
                  throw Error(formatProdErrorMessage(137, tag));
                default:
                  setProp(domElement, tag, propKey, propValue, props, null);
              }
          }
        hasSrcSet && setProp(domElement, tag, "srcSet", props.srcSet, props, null);
        hasSrc && setProp(domElement, tag, "src", props.src, props, null);
        return;
      case "input":
        listenToNonDelegatedEvent("invalid", domElement);
        var defaultValue = propKey = propValue = hasSrcSet = null, checked = null, defaultChecked = null;
        for (hasSrc in props)
          if (props.hasOwnProperty(hasSrc)) {
            var propValue$184 = props[hasSrc];
            if (null != propValue$184)
              switch (hasSrc) {
                case "name":
                  hasSrcSet = propValue$184;
                  break;
                case "type":
                  propValue = propValue$184;
                  break;
                case "checked":
                  checked = propValue$184;
                  break;
                case "defaultChecked":
                  defaultChecked = propValue$184;
                  break;
                case "value":
                  propKey = propValue$184;
                  break;
                case "defaultValue":
                  defaultValue = propValue$184;
                  break;
                case "children":
                case "dangerouslySetInnerHTML":
                  if (null != propValue$184)
                    throw Error(formatProdErrorMessage(137, tag));
                  break;
                default:
                  setProp(domElement, tag, hasSrc, propValue$184, props, null);
              }
          }
        initInput(
          domElement,
          propKey,
          defaultValue,
          checked,
          defaultChecked,
          propValue,
          hasSrcSet,
          false
        );
        return;
      case "select":
        listenToNonDelegatedEvent("invalid", domElement);
        hasSrc = propValue = propKey = null;
        for (hasSrcSet in props)
          if (props.hasOwnProperty(hasSrcSet) && (defaultValue = props[hasSrcSet], null != defaultValue))
            switch (hasSrcSet) {
              case "value":
                propKey = defaultValue;
                break;
              case "defaultValue":
                propValue = defaultValue;
                break;
              case "multiple":
                hasSrc = defaultValue;
              default:
                setProp(domElement, tag, hasSrcSet, defaultValue, props, null);
            }
        tag = propKey;
        props = propValue;
        domElement.multiple = !!hasSrc;
        null != tag ? updateOptions(domElement, !!hasSrc, tag, false) : null != props && updateOptions(domElement, !!hasSrc, props, true);
        return;
      case "textarea":
        listenToNonDelegatedEvent("invalid", domElement);
        propKey = hasSrcSet = hasSrc = null;
        for (propValue in props)
          if (props.hasOwnProperty(propValue) && (defaultValue = props[propValue], null != defaultValue))
            switch (propValue) {
              case "value":
                hasSrc = defaultValue;
                break;
              case "defaultValue":
                hasSrcSet = defaultValue;
                break;
              case "children":
                propKey = defaultValue;
                break;
              case "dangerouslySetInnerHTML":
                if (null != defaultValue) throw Error(formatProdErrorMessage(91));
                break;
              default:
                setProp(domElement, tag, propValue, defaultValue, props, null);
            }
        initTextarea(domElement, hasSrc, hasSrcSet, propKey);
        return;
      case "option":
        for (checked in props)
          if (props.hasOwnProperty(checked) && (hasSrc = props[checked], null != hasSrc))
            switch (checked) {
              case "selected":
                domElement.selected = hasSrc && "function" !== typeof hasSrc && "symbol" !== typeof hasSrc;
                break;
              default:
                setProp(domElement, tag, checked, hasSrc, props, null);
            }
        return;
      case "dialog":
        listenToNonDelegatedEvent("beforetoggle", domElement);
        listenToNonDelegatedEvent("toggle", domElement);
        listenToNonDelegatedEvent("cancel", domElement);
        listenToNonDelegatedEvent("close", domElement);
        break;
      case "iframe":
      case "object":
        listenToNonDelegatedEvent("load", domElement);
        break;
      case "video":
      case "audio":
        for (hasSrc = 0; hasSrc < mediaEventTypes.length; hasSrc++)
          listenToNonDelegatedEvent(mediaEventTypes[hasSrc], domElement);
        break;
      case "image":
        listenToNonDelegatedEvent("error", domElement);
        listenToNonDelegatedEvent("load", domElement);
        break;
      case "details":
        listenToNonDelegatedEvent("toggle", domElement);
        break;
      case "embed":
      case "source":
      case "link":
        listenToNonDelegatedEvent("error", domElement), listenToNonDelegatedEvent("load", domElement);
      case "area":
      case "base":
      case "br":
      case "col":
      case "hr":
      case "keygen":
      case "meta":
      case "param":
      case "track":
      case "wbr":
      case "menuitem":
        for (defaultChecked in props)
          if (props.hasOwnProperty(defaultChecked) && (hasSrc = props[defaultChecked], null != hasSrc))
            switch (defaultChecked) {
              case "children":
              case "dangerouslySetInnerHTML":
                throw Error(formatProdErrorMessage(137, tag));
              default:
                setProp(domElement, tag, defaultChecked, hasSrc, props, null);
            }
        return;
      default:
        if (isCustomElement(tag)) {
          for (propValue$184 in props)
            props.hasOwnProperty(propValue$184) && (hasSrc = props[propValue$184], void 0 !== hasSrc && setPropOnCustomElement(
              domElement,
              tag,
              propValue$184,
              hasSrc,
              props,
              void 0
            ));
          return;
        }
    }
    for (defaultValue in props)
      props.hasOwnProperty(defaultValue) && (hasSrc = props[defaultValue], null != hasSrc && setProp(domElement, tag, defaultValue, hasSrc, props, null));
  }
  function updateProperties(domElement, tag, lastProps, nextProps) {
    switch (tag) {
      case "div":
      case "span":
      case "svg":
      case "path":
      case "a":
      case "g":
      case "p":
      case "li":
        break;
      case "input":
        var name2 = null, type = null, value = null, defaultValue = null, lastDefaultValue = null, checked = null, defaultChecked = null;
        for (propKey in lastProps) {
          var lastProp = lastProps[propKey];
          if (lastProps.hasOwnProperty(propKey) && null != lastProp)
            switch (propKey) {
              case "checked":
                break;
              case "value":
                break;
              case "defaultValue":
                lastDefaultValue = lastProp;
              default:
                nextProps.hasOwnProperty(propKey) || setProp(domElement, tag, propKey, null, nextProps, lastProp);
            }
        }
        for (var propKey$201 in nextProps) {
          var propKey = nextProps[propKey$201];
          lastProp = lastProps[propKey$201];
          if (nextProps.hasOwnProperty(propKey$201) && (null != propKey || null != lastProp))
            switch (propKey$201) {
              case "type":
                type = propKey;
                break;
              case "name":
                name2 = propKey;
                break;
              case "checked":
                checked = propKey;
                break;
              case "defaultChecked":
                defaultChecked = propKey;
                break;
              case "value":
                value = propKey;
                break;
              case "defaultValue":
                defaultValue = propKey;
                break;
              case "children":
              case "dangerouslySetInnerHTML":
                if (null != propKey)
                  throw Error(formatProdErrorMessage(137, tag));
                break;
              default:
                propKey !== lastProp && setProp(
                  domElement,
                  tag,
                  propKey$201,
                  propKey,
                  nextProps,
                  lastProp
                );
            }
        }
        updateInput(
          domElement,
          value,
          defaultValue,
          lastDefaultValue,
          checked,
          defaultChecked,
          type,
          name2
        );
        return;
      case "select":
        propKey = value = defaultValue = propKey$201 = null;
        for (type in lastProps)
          if (lastDefaultValue = lastProps[type], lastProps.hasOwnProperty(type) && null != lastDefaultValue)
            switch (type) {
              case "value":
                break;
              case "multiple":
                propKey = lastDefaultValue;
              default:
                nextProps.hasOwnProperty(type) || setProp(
                  domElement,
                  tag,
                  type,
                  null,
                  nextProps,
                  lastDefaultValue
                );
            }
        for (name2 in nextProps)
          if (type = nextProps[name2], lastDefaultValue = lastProps[name2], nextProps.hasOwnProperty(name2) && (null != type || null != lastDefaultValue))
            switch (name2) {
              case "value":
                propKey$201 = type;
                break;
              case "defaultValue":
                defaultValue = type;
                break;
              case "multiple":
                value = type;
              default:
                type !== lastDefaultValue && setProp(
                  domElement,
                  tag,
                  name2,
                  type,
                  nextProps,
                  lastDefaultValue
                );
            }
        tag = defaultValue;
        lastProps = value;
        nextProps = propKey;
        null != propKey$201 ? updateOptions(domElement, !!lastProps, propKey$201, false) : !!nextProps !== !!lastProps && (null != tag ? updateOptions(domElement, !!lastProps, tag, true) : updateOptions(domElement, !!lastProps, lastProps ? [] : "", false));
        return;
      case "textarea":
        propKey = propKey$201 = null;
        for (defaultValue in lastProps)
          if (name2 = lastProps[defaultValue], lastProps.hasOwnProperty(defaultValue) && null != name2 && !nextProps.hasOwnProperty(defaultValue))
            switch (defaultValue) {
              case "value":
                break;
              case "children":
                break;
              default:
                setProp(domElement, tag, defaultValue, null, nextProps, name2);
            }
        for (value in nextProps)
          if (name2 = nextProps[value], type = lastProps[value], nextProps.hasOwnProperty(value) && (null != name2 || null != type))
            switch (value) {
              case "value":
                propKey$201 = name2;
                break;
              case "defaultValue":
                propKey = name2;
                break;
              case "children":
                break;
              case "dangerouslySetInnerHTML":
                if (null != name2) throw Error(formatProdErrorMessage(91));
                break;
              default:
                name2 !== type && setProp(domElement, tag, value, name2, nextProps, type);
            }
        updateTextarea(domElement, propKey$201, propKey);
        return;
      case "option":
        for (var propKey$217 in lastProps)
          if (propKey$201 = lastProps[propKey$217], lastProps.hasOwnProperty(propKey$217) && null != propKey$201 && !nextProps.hasOwnProperty(propKey$217))
            switch (propKey$217) {
              case "selected":
                domElement.selected = false;
                break;
              default:
                setProp(
                  domElement,
                  tag,
                  propKey$217,
                  null,
                  nextProps,
                  propKey$201
                );
            }
        for (lastDefaultValue in nextProps)
          if (propKey$201 = nextProps[lastDefaultValue], propKey = lastProps[lastDefaultValue], nextProps.hasOwnProperty(lastDefaultValue) && propKey$201 !== propKey && (null != propKey$201 || null != propKey))
            switch (lastDefaultValue) {
              case "selected":
                domElement.selected = propKey$201 && "function" !== typeof propKey$201 && "symbol" !== typeof propKey$201;
                break;
              default:
                setProp(
                  domElement,
                  tag,
                  lastDefaultValue,
                  propKey$201,
                  nextProps,
                  propKey
                );
            }
        return;
      case "img":
      case "link":
      case "area":
      case "base":
      case "br":
      case "col":
      case "embed":
      case "hr":
      case "keygen":
      case "meta":
      case "param":
      case "source":
      case "track":
      case "wbr":
      case "menuitem":
        for (var propKey$222 in lastProps)
          propKey$201 = lastProps[propKey$222], lastProps.hasOwnProperty(propKey$222) && null != propKey$201 && !nextProps.hasOwnProperty(propKey$222) && setProp(domElement, tag, propKey$222, null, nextProps, propKey$201);
        for (checked in nextProps)
          if (propKey$201 = nextProps[checked], propKey = lastProps[checked], nextProps.hasOwnProperty(checked) && propKey$201 !== propKey && (null != propKey$201 || null != propKey))
            switch (checked) {
              case "children":
              case "dangerouslySetInnerHTML":
                if (null != propKey$201)
                  throw Error(formatProdErrorMessage(137, tag));
                break;
              default:
                setProp(
                  domElement,
                  tag,
                  checked,
                  propKey$201,
                  nextProps,
                  propKey
                );
            }
        return;
      default:
        if (isCustomElement(tag)) {
          for (var propKey$227 in lastProps)
            propKey$201 = lastProps[propKey$227], lastProps.hasOwnProperty(propKey$227) && void 0 !== propKey$201 && !nextProps.hasOwnProperty(propKey$227) && setPropOnCustomElement(
              domElement,
              tag,
              propKey$227,
              void 0,
              nextProps,
              propKey$201
            );
          for (defaultChecked in nextProps)
            propKey$201 = nextProps[defaultChecked], propKey = lastProps[defaultChecked], !nextProps.hasOwnProperty(defaultChecked) || propKey$201 === propKey || void 0 === propKey$201 && void 0 === propKey || setPropOnCustomElement(
              domElement,
              tag,
              defaultChecked,
              propKey$201,
              nextProps,
              propKey
            );
          return;
        }
    }
    for (var propKey$232 in lastProps)
      propKey$201 = lastProps[propKey$232], lastProps.hasOwnProperty(propKey$232) && null != propKey$201 && !nextProps.hasOwnProperty(propKey$232) && setProp(domElement, tag, propKey$232, null, nextProps, propKey$201);
    for (lastProp in nextProps)
      propKey$201 = nextProps[lastProp], propKey = lastProps[lastProp], !nextProps.hasOwnProperty(lastProp) || propKey$201 === propKey || null == propKey$201 && null == propKey || setProp(domElement, tag, lastProp, propKey$201, nextProps, propKey);
  }
  function isLikelyStaticResource(initiatorType) {
    switch (initiatorType) {
      case "css":
      case "script":
      case "font":
      case "img":
      case "image":
      case "input":
      case "link":
        return true;
      default:
        return false;
    }
  }
  function estimateBandwidth() {
    if ("function" === typeof performance.getEntriesByType) {
      for (var count = 0, bits = 0, resourceEntries = performance.getEntriesByType("resource"), i = 0; i < resourceEntries.length; i++) {
        var entry2 = resourceEntries[i], transferSize = entry2.transferSize, initiatorType = entry2.initiatorType, duration = entry2.duration;
        if (transferSize && duration && isLikelyStaticResource(initiatorType)) {
          initiatorType = 0;
          duration = entry2.responseEnd;
          for (i += 1; i < resourceEntries.length; i++) {
            var overlapEntry = resourceEntries[i], overlapStartTime = overlapEntry.startTime;
            if (overlapStartTime > duration) break;
            var overlapTransferSize = overlapEntry.transferSize, overlapInitiatorType = overlapEntry.initiatorType;
            overlapTransferSize && isLikelyStaticResource(overlapInitiatorType) && (overlapEntry = overlapEntry.responseEnd, initiatorType += overlapTransferSize * (overlapEntry < duration ? 1 : (duration - overlapStartTime) / (overlapEntry - overlapStartTime)));
          }
          --i;
          bits += 8 * (transferSize + initiatorType) / (entry2.duration / 1e3);
          count++;
          if (10 < count) break;
        }
      }
      if (0 < count) return bits / count / 1e6;
    }
    return navigator.connection && (count = navigator.connection.downlink, "number" === typeof count) ? count : 5;
  }
  var eventsEnabled = null, selectionInformation = null;
  function getOwnerDocumentFromRootContainer(rootContainerElement) {
    return 9 === rootContainerElement.nodeType ? rootContainerElement : rootContainerElement.ownerDocument;
  }
  function getOwnHostContext(namespaceURI) {
    switch (namespaceURI) {
      case "http://www.w3.org/2000/svg":
        return 1;
      case "http://www.w3.org/1998/Math/MathML":
        return 2;
      default:
        return 0;
    }
  }
  function getChildHostContextProd(parentNamespace, type) {
    if (0 === parentNamespace)
      switch (type) {
        case "svg":
          return 1;
        case "math":
          return 2;
        default:
          return 0;
      }
    return 1 === parentNamespace && "foreignObject" === type ? 0 : parentNamespace;
  }
  function shouldSetTextContent(type, props) {
    return "textarea" === type || "noscript" === type || "string" === typeof props.children || "number" === typeof props.children || "bigint" === typeof props.children || "object" === typeof props.dangerouslySetInnerHTML && null !== props.dangerouslySetInnerHTML && null != props.dangerouslySetInnerHTML.__html;
  }
  var currentPopstateTransitionEvent = null;
  function shouldAttemptEagerTransition() {
    var event = window.event;
    if (event && "popstate" === event.type) {
      if (event === currentPopstateTransitionEvent) return false;
      currentPopstateTransitionEvent = event;
      return true;
    }
    currentPopstateTransitionEvent = null;
    return false;
  }
  var scheduleTimeout = "function" === typeof setTimeout ? setTimeout : void 0, cancelTimeout = "function" === typeof clearTimeout ? clearTimeout : void 0, localPromise = "function" === typeof Promise ? Promise : void 0, scheduleMicrotask = "function" === typeof queueMicrotask ? queueMicrotask : "undefined" !== typeof localPromise ? function(callback) {
    return localPromise.resolve(null).then(callback).catch(handleErrorInNextTick);
  } : scheduleTimeout;
  function handleErrorInNextTick(error) {
    setTimeout(function() {
      throw error;
    });
  }
  function isSingletonScope(type) {
    return "head" === type;
  }
  function clearHydrationBoundary(parentInstance, hydrationInstance) {
    var node2 = hydrationInstance, depth = 0;
    do {
      var nextNode = node2.nextSibling;
      parentInstance.removeChild(node2);
      if (nextNode && 8 === nextNode.nodeType)
        if (node2 = nextNode.data, "/$" === node2 || "/&" === node2) {
          if (0 === depth) {
            parentInstance.removeChild(nextNode);
            retryIfBlockedOn(hydrationInstance);
            return;
          }
          depth--;
        } else if ("$" === node2 || "$?" === node2 || "$~" === node2 || "$!" === node2 || "&" === node2)
          depth++;
        else if ("html" === node2)
          releaseSingletonInstance(parentInstance.ownerDocument.documentElement);
        else if ("head" === node2) {
          node2 = parentInstance.ownerDocument.head;
          releaseSingletonInstance(node2);
          for (var node$jscomp$0 = node2.firstChild; node$jscomp$0; ) {
            var nextNode$jscomp$0 = node$jscomp$0.nextSibling, nodeName = node$jscomp$0.nodeName;
            node$jscomp$0[internalHoistableMarker] || "SCRIPT" === nodeName || "STYLE" === nodeName || "LINK" === nodeName && "stylesheet" === node$jscomp$0.rel.toLowerCase() || node2.removeChild(node$jscomp$0);
            node$jscomp$0 = nextNode$jscomp$0;
          }
        } else
          "body" === node2 && releaseSingletonInstance(parentInstance.ownerDocument.body);
      node2 = nextNode;
    } while (node2);
    retryIfBlockedOn(hydrationInstance);
  }
  function hideOrUnhideDehydratedBoundary(suspenseInstance, isHidden) {
    var node2 = suspenseInstance;
    suspenseInstance = 0;
    do {
      var nextNode = node2.nextSibling;
      1 === node2.nodeType ? isHidden ? (node2._stashedDisplay = node2.style.display, node2.style.display = "none") : (node2.style.display = node2._stashedDisplay || "", "" === node2.getAttribute("style") && node2.removeAttribute("style")) : 3 === node2.nodeType && (isHidden ? (node2._stashedText = node2.nodeValue, node2.nodeValue = "") : node2.nodeValue = node2._stashedText || "");
      if (nextNode && 8 === nextNode.nodeType)
        if (node2 = nextNode.data, "/$" === node2)
          if (0 === suspenseInstance) break;
          else suspenseInstance--;
        else
          "$" !== node2 && "$?" !== node2 && "$~" !== node2 && "$!" !== node2 || suspenseInstance++;
      node2 = nextNode;
    } while (node2);
  }
  function clearContainerSparingly(container) {
    var nextNode = container.firstChild;
    nextNode && 10 === nextNode.nodeType && (nextNode = nextNode.nextSibling);
    for (; nextNode; ) {
      var node2 = nextNode;
      nextNode = nextNode.nextSibling;
      switch (node2.nodeName) {
        case "HTML":
        case "HEAD":
        case "BODY":
          clearContainerSparingly(node2);
          detachDeletedInstance(node2);
          continue;
        case "SCRIPT":
        case "STYLE":
          continue;
        case "LINK":
          if ("stylesheet" === node2.rel.toLowerCase()) continue;
      }
      container.removeChild(node2);
    }
  }
  function canHydrateInstance(instance, type, props, inRootOrSingleton) {
    for (; 1 === instance.nodeType; ) {
      var anyProps = props;
      if (instance.nodeName.toLowerCase() !== type.toLowerCase()) {
        if (!inRootOrSingleton && ("INPUT" !== instance.nodeName || "hidden" !== instance.type))
          break;
      } else if (!inRootOrSingleton)
        if ("input" === type && "hidden" === instance.type) {
          var name2 = null == anyProps.name ? null : "" + anyProps.name;
          if ("hidden" === anyProps.type && instance.getAttribute("name") === name2)
            return instance;
        } else return instance;
      else if (!instance[internalHoistableMarker])
        switch (type) {
          case "meta":
            if (!instance.hasAttribute("itemprop")) break;
            return instance;
          case "link":
            name2 = instance.getAttribute("rel");
            if ("stylesheet" === name2 && instance.hasAttribute("data-precedence"))
              break;
            else if (name2 !== anyProps.rel || instance.getAttribute("href") !== (null == anyProps.href || "" === anyProps.href ? null : anyProps.href) || instance.getAttribute("crossorigin") !== (null == anyProps.crossOrigin ? null : anyProps.crossOrigin) || instance.getAttribute("title") !== (null == anyProps.title ? null : anyProps.title))
              break;
            return instance;
          case "style":
            if (instance.hasAttribute("data-precedence")) break;
            return instance;
          case "script":
            name2 = instance.getAttribute("src");
            if ((name2 !== (null == anyProps.src ? null : anyProps.src) || instance.getAttribute("type") !== (null == anyProps.type ? null : anyProps.type) || instance.getAttribute("crossorigin") !== (null == anyProps.crossOrigin ? null : anyProps.crossOrigin)) && name2 && instance.hasAttribute("async") && !instance.hasAttribute("itemprop"))
              break;
            return instance;
          default:
            return instance;
        }
      instance = getNextHydratable(instance.nextSibling);
      if (null === instance) break;
    }
    return null;
  }
  function canHydrateTextInstance(instance, text2, inRootOrSingleton) {
    if ("" === text2) return null;
    for (; 3 !== instance.nodeType; ) {
      if ((1 !== instance.nodeType || "INPUT" !== instance.nodeName || "hidden" !== instance.type) && !inRootOrSingleton)
        return null;
      instance = getNextHydratable(instance.nextSibling);
      if (null === instance) return null;
    }
    return instance;
  }
  function canHydrateHydrationBoundary(instance, inRootOrSingleton) {
    for (; 8 !== instance.nodeType; ) {
      if ((1 !== instance.nodeType || "INPUT" !== instance.nodeName || "hidden" !== instance.type) && !inRootOrSingleton)
        return null;
      instance = getNextHydratable(instance.nextSibling);
      if (null === instance) return null;
    }
    return instance;
  }
  function isSuspenseInstancePending(instance) {
    return "$?" === instance.data || "$~" === instance.data;
  }
  function isSuspenseInstanceFallback(instance) {
    return "$!" === instance.data || "$?" === instance.data && "loading" !== instance.ownerDocument.readyState;
  }
  function registerSuspenseInstanceRetry(instance, callback) {
    var ownerDocument = instance.ownerDocument;
    if ("$~" === instance.data) instance._reactRetry = callback;
    else if ("$?" !== instance.data || "loading" !== ownerDocument.readyState)
      callback();
    else {
      var listener = function() {
        callback();
        ownerDocument.removeEventListener("DOMContentLoaded", listener);
      };
      ownerDocument.addEventListener("DOMContentLoaded", listener);
      instance._reactRetry = listener;
    }
  }
  function getNextHydratable(node2) {
    for (; null != node2; node2 = node2.nextSibling) {
      var nodeType = node2.nodeType;
      if (1 === nodeType || 3 === nodeType) break;
      if (8 === nodeType) {
        nodeType = node2.data;
        if ("$" === nodeType || "$!" === nodeType || "$?" === nodeType || "$~" === nodeType || "&" === nodeType || "F!" === nodeType || "F" === nodeType)
          break;
        if ("/$" === nodeType || "/&" === nodeType) return null;
      }
    }
    return node2;
  }
  var previousHydratableOnEnteringScopedSingleton = null;
  function getNextHydratableInstanceAfterHydrationBoundary(hydrationInstance) {
    hydrationInstance = hydrationInstance.nextSibling;
    for (var depth = 0; hydrationInstance; ) {
      if (8 === hydrationInstance.nodeType) {
        var data2 = hydrationInstance.data;
        if ("/$" === data2 || "/&" === data2) {
          if (0 === depth)
            return getNextHydratable(hydrationInstance.nextSibling);
          depth--;
        } else
          "$" !== data2 && "$!" !== data2 && "$?" !== data2 && "$~" !== data2 && "&" !== data2 || depth++;
      }
      hydrationInstance = hydrationInstance.nextSibling;
    }
    return null;
  }
  function getParentHydrationBoundary(targetInstance) {
    targetInstance = targetInstance.previousSibling;
    for (var depth = 0; targetInstance; ) {
      if (8 === targetInstance.nodeType) {
        var data2 = targetInstance.data;
        if ("$" === data2 || "$!" === data2 || "$?" === data2 || "$~" === data2 || "&" === data2) {
          if (0 === depth) return targetInstance;
          depth--;
        } else "/$" !== data2 && "/&" !== data2 || depth++;
      }
      targetInstance = targetInstance.previousSibling;
    }
    return null;
  }
  function resolveSingletonInstance(type, props, rootContainerInstance) {
    props = getOwnerDocumentFromRootContainer(rootContainerInstance);
    switch (type) {
      case "html":
        type = props.documentElement;
        if (!type) throw Error(formatProdErrorMessage(452));
        return type;
      case "head":
        type = props.head;
        if (!type) throw Error(formatProdErrorMessage(453));
        return type;
      case "body":
        type = props.body;
        if (!type) throw Error(formatProdErrorMessage(454));
        return type;
      default:
        throw Error(formatProdErrorMessage(451));
    }
  }
  function releaseSingletonInstance(instance) {
    for (var attributes = instance.attributes; attributes.length; )
      instance.removeAttributeNode(attributes[0]);
    detachDeletedInstance(instance);
  }
  var preloadPropsMap = /* @__PURE__ */ new Map(), preconnectsSet = /* @__PURE__ */ new Set();
  function getHoistableRoot(container) {
    return "function" === typeof container.getRootNode ? container.getRootNode() : 9 === container.nodeType ? container : container.ownerDocument;
  }
  var previousDispatcher = ReactDOMSharedInternals.d;
  ReactDOMSharedInternals.d = {
    f: flushSyncWork,
    r: requestFormReset,
    D: prefetchDNS,
    C: preconnect,
    L: preload,
    m: preloadModule,
    X: preinitScript,
    S: preinitStyle,
    M: preinitModuleScript
  };
  function flushSyncWork() {
    var previousWasRendering = previousDispatcher.f(), wasRendering = flushSyncWork$1();
    return previousWasRendering || wasRendering;
  }
  function requestFormReset(form) {
    var formInst = getInstanceFromNode(form);
    null !== formInst && 5 === formInst.tag && "form" === formInst.type ? requestFormReset$1(formInst) : previousDispatcher.r(form);
  }
  var globalDocument = "undefined" === typeof document ? null : document;
  function preconnectAs(rel, href, crossOrigin) {
    var ownerDocument = globalDocument;
    if (ownerDocument && "string" === typeof href && href) {
      var limitedEscapedHref = escapeSelectorAttributeValueInsideDoubleQuotes(href);
      limitedEscapedHref = 'link[rel="' + rel + '"][href="' + limitedEscapedHref + '"]';
      "string" === typeof crossOrigin && (limitedEscapedHref += '[crossorigin="' + crossOrigin + '"]');
      preconnectsSet.has(limitedEscapedHref) || (preconnectsSet.add(limitedEscapedHref), rel = { rel, crossOrigin, href }, null === ownerDocument.querySelector(limitedEscapedHref) && (href = ownerDocument.createElement("link"), setInitialProperties(href, "link", rel), markNodeAsHoistable(href), ownerDocument.head.appendChild(href)));
    }
  }
  function prefetchDNS(href) {
    previousDispatcher.D(href);
    preconnectAs("dns-prefetch", href, null);
  }
  function preconnect(href, crossOrigin) {
    previousDispatcher.C(href, crossOrigin);
    preconnectAs("preconnect", href, crossOrigin);
  }
  function preload(href, as, options2) {
    previousDispatcher.L(href, as, options2);
    var ownerDocument = globalDocument;
    if (ownerDocument && href && as) {
      var preloadSelector = 'link[rel="preload"][as="' + escapeSelectorAttributeValueInsideDoubleQuotes(as) + '"]';
      "image" === as ? options2 && options2.imageSrcSet ? (preloadSelector += '[imagesrcset="' + escapeSelectorAttributeValueInsideDoubleQuotes(
        options2.imageSrcSet
      ) + '"]', "string" === typeof options2.imageSizes && (preloadSelector += '[imagesizes="' + escapeSelectorAttributeValueInsideDoubleQuotes(
        options2.imageSizes
      ) + '"]')) : preloadSelector += '[href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"]' : preloadSelector += '[href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"]';
      var key = preloadSelector;
      switch (as) {
        case "style":
          key = getStyleKey(href);
          break;
        case "script":
          key = getScriptKey(href);
      }
      preloadPropsMap.has(key) || (href = assign(
        {
          rel: "preload",
          href: "image" === as && options2 && options2.imageSrcSet ? void 0 : href,
          as
        },
        options2
      ), preloadPropsMap.set(key, href), null !== ownerDocument.querySelector(preloadSelector) || "style" === as && ownerDocument.querySelector(getStylesheetSelectorFromKey(key)) || "script" === as && ownerDocument.querySelector(getScriptSelectorFromKey(key)) || (as = ownerDocument.createElement("link"), setInitialProperties(as, "link", href), markNodeAsHoistable(as), ownerDocument.head.appendChild(as)));
    }
  }
  function preloadModule(href, options2) {
    previousDispatcher.m(href, options2);
    var ownerDocument = globalDocument;
    if (ownerDocument && href) {
      var as = options2 && "string" === typeof options2.as ? options2.as : "script", preloadSelector = 'link[rel="modulepreload"][as="' + escapeSelectorAttributeValueInsideDoubleQuotes(as) + '"][href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"]', key = preloadSelector;
      switch (as) {
        case "audioworklet":
        case "paintworklet":
        case "serviceworker":
        case "sharedworker":
        case "worker":
        case "script":
          key = getScriptKey(href);
      }
      if (!preloadPropsMap.has(key) && (href = assign({ rel: "modulepreload", href }, options2), preloadPropsMap.set(key, href), null === ownerDocument.querySelector(preloadSelector))) {
        switch (as) {
          case "audioworklet":
          case "paintworklet":
          case "serviceworker":
          case "sharedworker":
          case "worker":
          case "script":
            if (ownerDocument.querySelector(getScriptSelectorFromKey(key)))
              return;
        }
        as = ownerDocument.createElement("link");
        setInitialProperties(as, "link", href);
        markNodeAsHoistable(as);
        ownerDocument.head.appendChild(as);
      }
    }
  }
  function preinitStyle(href, precedence, options2) {
    previousDispatcher.S(href, precedence, options2);
    var ownerDocument = globalDocument;
    if (ownerDocument && href) {
      var styles = getResourcesFromRoot(ownerDocument).hoistableStyles, key = getStyleKey(href);
      precedence = precedence || "default";
      var resource = styles.get(key);
      if (!resource) {
        var state = { loading: 0, preload: null };
        if (resource = ownerDocument.querySelector(
          getStylesheetSelectorFromKey(key)
        ))
          state.loading = 5;
        else {
          href = assign(
            { rel: "stylesheet", href, "data-precedence": precedence },
            options2
          );
          (options2 = preloadPropsMap.get(key)) && adoptPreloadPropsForStylesheet(href, options2);
          var link2 = resource = ownerDocument.createElement("link");
          markNodeAsHoistable(link2);
          setInitialProperties(link2, "link", href);
          link2._p = new Promise(function(resolve, reject) {
            link2.onload = resolve;
            link2.onerror = reject;
          });
          link2.addEventListener("load", function() {
            state.loading |= 1;
          });
          link2.addEventListener("error", function() {
            state.loading |= 2;
          });
          state.loading |= 4;
          insertStylesheet(resource, precedence, ownerDocument);
        }
        resource = {
          type: "stylesheet",
          instance: resource,
          count: 1,
          state
        };
        styles.set(key, resource);
      }
    }
  }
  function preinitScript(src, options2) {
    previousDispatcher.X(src, options2);
    var ownerDocument = globalDocument;
    if (ownerDocument && src) {
      var scripts = getResourcesFromRoot(ownerDocument).hoistableScripts, key = getScriptKey(src), resource = scripts.get(key);
      resource || (resource = ownerDocument.querySelector(getScriptSelectorFromKey(key)), resource || (src = assign({ src, async: true }, options2), (options2 = preloadPropsMap.get(key)) && adoptPreloadPropsForScript(src, options2), resource = ownerDocument.createElement("script"), markNodeAsHoistable(resource), setInitialProperties(resource, "link", src), ownerDocument.head.appendChild(resource)), resource = {
        type: "script",
        instance: resource,
        count: 1,
        state: null
      }, scripts.set(key, resource));
    }
  }
  function preinitModuleScript(src, options2) {
    previousDispatcher.M(src, options2);
    var ownerDocument = globalDocument;
    if (ownerDocument && src) {
      var scripts = getResourcesFromRoot(ownerDocument).hoistableScripts, key = getScriptKey(src), resource = scripts.get(key);
      resource || (resource = ownerDocument.querySelector(getScriptSelectorFromKey(key)), resource || (src = assign({ src, async: true, type: "module" }, options2), (options2 = preloadPropsMap.get(key)) && adoptPreloadPropsForScript(src, options2), resource = ownerDocument.createElement("script"), markNodeAsHoistable(resource), setInitialProperties(resource, "link", src), ownerDocument.head.appendChild(resource)), resource = {
        type: "script",
        instance: resource,
        count: 1,
        state: null
      }, scripts.set(key, resource));
    }
  }
  function getResource(type, currentProps, pendingProps, currentResource) {
    var JSCompiler_inline_result = (JSCompiler_inline_result = rootInstanceStackCursor.current) ? getHoistableRoot(JSCompiler_inline_result) : null;
    if (!JSCompiler_inline_result) throw Error(formatProdErrorMessage(446));
    switch (type) {
      case "meta":
      case "title":
        return null;
      case "style":
        return "string" === typeof pendingProps.precedence && "string" === typeof pendingProps.href ? (currentProps = getStyleKey(pendingProps.href), pendingProps = getResourcesFromRoot(
          JSCompiler_inline_result
        ).hoistableStyles, currentResource = pendingProps.get(currentProps), currentResource || (currentResource = {
          type: "style",
          instance: null,
          count: 0,
          state: null
        }, pendingProps.set(currentProps, currentResource)), currentResource) : { type: "void", instance: null, count: 0, state: null };
      case "link":
        if ("stylesheet" === pendingProps.rel && "string" === typeof pendingProps.href && "string" === typeof pendingProps.precedence) {
          type = getStyleKey(pendingProps.href);
          var styles$243 = getResourcesFromRoot(
            JSCompiler_inline_result
          ).hoistableStyles, resource$244 = styles$243.get(type);
          resource$244 || (JSCompiler_inline_result = JSCompiler_inline_result.ownerDocument || JSCompiler_inline_result, resource$244 = {
            type: "stylesheet",
            instance: null,
            count: 0,
            state: { loading: 0, preload: null }
          }, styles$243.set(type, resource$244), (styles$243 = JSCompiler_inline_result.querySelector(
            getStylesheetSelectorFromKey(type)
          )) && !styles$243._p && (resource$244.instance = styles$243, resource$244.state.loading = 5), preloadPropsMap.has(type) || (pendingProps = {
            rel: "preload",
            as: "style",
            href: pendingProps.href,
            crossOrigin: pendingProps.crossOrigin,
            integrity: pendingProps.integrity,
            media: pendingProps.media,
            hrefLang: pendingProps.hrefLang,
            referrerPolicy: pendingProps.referrerPolicy
          }, preloadPropsMap.set(type, pendingProps), styles$243 || preloadStylesheet(
            JSCompiler_inline_result,
            type,
            pendingProps,
            resource$244.state
          )));
          if (currentProps && null === currentResource)
            throw Error(formatProdErrorMessage(528, ""));
          return resource$244;
        }
        if (currentProps && null !== currentResource)
          throw Error(formatProdErrorMessage(529, ""));
        return null;
      case "script":
        return currentProps = pendingProps.async, pendingProps = pendingProps.src, "string" === typeof pendingProps && currentProps && "function" !== typeof currentProps && "symbol" !== typeof currentProps ? (currentProps = getScriptKey(pendingProps), pendingProps = getResourcesFromRoot(
          JSCompiler_inline_result
        ).hoistableScripts, currentResource = pendingProps.get(currentProps), currentResource || (currentResource = {
          type: "script",
          instance: null,
          count: 0,
          state: null
        }, pendingProps.set(currentProps, currentResource)), currentResource) : { type: "void", instance: null, count: 0, state: null };
      default:
        throw Error(formatProdErrorMessage(444, type));
    }
  }
  function getStyleKey(href) {
    return 'href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"';
  }
  function getStylesheetSelectorFromKey(key) {
    return 'link[rel="stylesheet"][' + key + "]";
  }
  function stylesheetPropsFromRawProps(rawProps) {
    return assign({}, rawProps, {
      "data-precedence": rawProps.precedence,
      precedence: null
    });
  }
  function preloadStylesheet(ownerDocument, key, preloadProps, state) {
    ownerDocument.querySelector('link[rel="preload"][as="style"][' + key + "]") ? state.loading = 1 : (key = ownerDocument.createElement("link"), state.preload = key, key.addEventListener("load", function() {
      return state.loading |= 1;
    }), key.addEventListener("error", function() {
      return state.loading |= 2;
    }), setInitialProperties(key, "link", preloadProps), markNodeAsHoistable(key), ownerDocument.head.appendChild(key));
  }
  function getScriptKey(src) {
    return '[src="' + escapeSelectorAttributeValueInsideDoubleQuotes(src) + '"]';
  }
  function getScriptSelectorFromKey(key) {
    return "script[async]" + key;
  }
  function acquireResource(hoistableRoot, resource, props) {
    resource.count++;
    if (null === resource.instance)
      switch (resource.type) {
        case "style":
          var instance = hoistableRoot.querySelector(
            'style[data-href~="' + escapeSelectorAttributeValueInsideDoubleQuotes(props.href) + '"]'
          );
          if (instance)
            return resource.instance = instance, markNodeAsHoistable(instance), instance;
          var styleProps = assign({}, props, {
            "data-href": props.href,
            "data-precedence": props.precedence,
            href: null,
            precedence: null
          });
          instance = (hoistableRoot.ownerDocument || hoistableRoot).createElement(
            "style"
          );
          markNodeAsHoistable(instance);
          setInitialProperties(instance, "style", styleProps);
          insertStylesheet(instance, props.precedence, hoistableRoot);
          return resource.instance = instance;
        case "stylesheet":
          styleProps = getStyleKey(props.href);
          var instance$249 = hoistableRoot.querySelector(
            getStylesheetSelectorFromKey(styleProps)
          );
          if (instance$249)
            return resource.state.loading |= 4, resource.instance = instance$249, markNodeAsHoistable(instance$249), instance$249;
          instance = stylesheetPropsFromRawProps(props);
          (styleProps = preloadPropsMap.get(styleProps)) && adoptPreloadPropsForStylesheet(instance, styleProps);
          instance$249 = (hoistableRoot.ownerDocument || hoistableRoot).createElement("link");
          markNodeAsHoistable(instance$249);
          var linkInstance = instance$249;
          linkInstance._p = new Promise(function(resolve, reject) {
            linkInstance.onload = resolve;
            linkInstance.onerror = reject;
          });
          setInitialProperties(instance$249, "link", instance);
          resource.state.loading |= 4;
          insertStylesheet(instance$249, props.precedence, hoistableRoot);
          return resource.instance = instance$249;
        case "script":
          instance$249 = getScriptKey(props.src);
          if (styleProps = hoistableRoot.querySelector(
            getScriptSelectorFromKey(instance$249)
          ))
            return resource.instance = styleProps, markNodeAsHoistable(styleProps), styleProps;
          instance = props;
          if (styleProps = preloadPropsMap.get(instance$249))
            instance = assign({}, props), adoptPreloadPropsForScript(instance, styleProps);
          hoistableRoot = hoistableRoot.ownerDocument || hoistableRoot;
          styleProps = hoistableRoot.createElement("script");
          markNodeAsHoistable(styleProps);
          setInitialProperties(styleProps, "link", instance);
          hoistableRoot.head.appendChild(styleProps);
          return resource.instance = styleProps;
        case "void":
          return null;
        default:
          throw Error(formatProdErrorMessage(443, resource.type));
      }
    else
      "stylesheet" === resource.type && 0 === (resource.state.loading & 4) && (instance = resource.instance, resource.state.loading |= 4, insertStylesheet(instance, props.precedence, hoistableRoot));
    return resource.instance;
  }
  function insertStylesheet(instance, precedence, root3) {
    for (var nodes = root3.querySelectorAll(
      'link[rel="stylesheet"][data-precedence],style[data-precedence]'
    ), last = nodes.length ? nodes[nodes.length - 1] : null, prior = last, i = 0; i < nodes.length; i++) {
      var node2 = nodes[i];
      if (node2.dataset.precedence === precedence) prior = node2;
      else if (prior !== last) break;
    }
    prior ? prior.parentNode.insertBefore(instance, prior.nextSibling) : (precedence = 9 === root3.nodeType ? root3.head : root3, precedence.insertBefore(instance, precedence.firstChild));
  }
  function adoptPreloadPropsForStylesheet(stylesheetProps, preloadProps) {
    null == stylesheetProps.crossOrigin && (stylesheetProps.crossOrigin = preloadProps.crossOrigin);
    null == stylesheetProps.referrerPolicy && (stylesheetProps.referrerPolicy = preloadProps.referrerPolicy);
    null == stylesheetProps.title && (stylesheetProps.title = preloadProps.title);
  }
  function adoptPreloadPropsForScript(scriptProps, preloadProps) {
    null == scriptProps.crossOrigin && (scriptProps.crossOrigin = preloadProps.crossOrigin);
    null == scriptProps.referrerPolicy && (scriptProps.referrerPolicy = preloadProps.referrerPolicy);
    null == scriptProps.integrity && (scriptProps.integrity = preloadProps.integrity);
  }
  var tagCaches = null;
  function getHydratableHoistableCache(type, keyAttribute, ownerDocument) {
    if (null === tagCaches) {
      var cache = /* @__PURE__ */ new Map();
      var caches = tagCaches = /* @__PURE__ */ new Map();
      caches.set(ownerDocument, cache);
    } else
      caches = tagCaches, cache = caches.get(ownerDocument), cache || (cache = /* @__PURE__ */ new Map(), caches.set(ownerDocument, cache));
    if (cache.has(type)) return cache;
    cache.set(type, null);
    ownerDocument = ownerDocument.getElementsByTagName(type);
    for (caches = 0; caches < ownerDocument.length; caches++) {
      var node2 = ownerDocument[caches];
      if (!(node2[internalHoistableMarker] || node2[internalInstanceKey] || "link" === type && "stylesheet" === node2.getAttribute("rel")) && "http://www.w3.org/2000/svg" !== node2.namespaceURI) {
        var nodeKey = node2.getAttribute(keyAttribute) || "";
        nodeKey = type + nodeKey;
        var existing = cache.get(nodeKey);
        existing ? existing.push(node2) : cache.set(nodeKey, [node2]);
      }
    }
    return cache;
  }
  function mountHoistable(hoistableRoot, type, instance) {
    hoistableRoot = hoistableRoot.ownerDocument || hoistableRoot;
    hoistableRoot.head.insertBefore(
      instance,
      "title" === type ? hoistableRoot.querySelector("head > title") : null
    );
  }
  function isHostHoistableType(type, props, hostContext) {
    if (1 === hostContext || null != props.itemProp) return false;
    switch (type) {
      case "meta":
      case "title":
        return true;
      case "style":
        if ("string" !== typeof props.precedence || "string" !== typeof props.href || "" === props.href)
          break;
        return true;
      case "link":
        if ("string" !== typeof props.rel || "string" !== typeof props.href || "" === props.href || props.onLoad || props.onError)
          break;
        switch (props.rel) {
          case "stylesheet":
            return type = props.disabled, "string" === typeof props.precedence && null == type;
          default:
            return true;
        }
      case "script":
        if (props.async && "function" !== typeof props.async && "symbol" !== typeof props.async && !props.onLoad && !props.onError && props.src && "string" === typeof props.src)
          return true;
    }
    return false;
  }
  function preloadResource(resource) {
    return "stylesheet" === resource.type && 0 === (resource.state.loading & 3) ? false : true;
  }
  function suspendResource(state, hoistableRoot, resource, props) {
    if ("stylesheet" === resource.type && ("string" !== typeof props.media || false !== matchMedia(props.media).matches) && 0 === (resource.state.loading & 4)) {
      if (null === resource.instance) {
        var key = getStyleKey(props.href), instance = hoistableRoot.querySelector(
          getStylesheetSelectorFromKey(key)
        );
        if (instance) {
          hoistableRoot = instance._p;
          null !== hoistableRoot && "object" === typeof hoistableRoot && "function" === typeof hoistableRoot.then && (state.count++, state = onUnsuspend.bind(state), hoistableRoot.then(state, state));
          resource.state.loading |= 4;
          resource.instance = instance;
          markNodeAsHoistable(instance);
          return;
        }
        instance = hoistableRoot.ownerDocument || hoistableRoot;
        props = stylesheetPropsFromRawProps(props);
        (key = preloadPropsMap.get(key)) && adoptPreloadPropsForStylesheet(props, key);
        instance = instance.createElement("link");
        markNodeAsHoistable(instance);
        var linkInstance = instance;
        linkInstance._p = new Promise(function(resolve, reject) {
          linkInstance.onload = resolve;
          linkInstance.onerror = reject;
        });
        setInitialProperties(instance, "link", props);
        resource.instance = instance;
      }
      null === state.stylesheets && (state.stylesheets = /* @__PURE__ */ new Map());
      state.stylesheets.set(resource, hoistableRoot);
      (hoistableRoot = resource.state.preload) && 0 === (resource.state.loading & 3) && (state.count++, resource = onUnsuspend.bind(state), hoistableRoot.addEventListener("load", resource), hoistableRoot.addEventListener("error", resource));
    }
  }
  var estimatedBytesWithinLimit = 0;
  function waitForCommitToBeReady(state, timeoutOffset) {
    state.stylesheets && 0 === state.count && insertSuspendedStylesheets(state, state.stylesheets);
    return 0 < state.count || 0 < state.imgCount ? function(commit) {
      var stylesheetTimer = setTimeout(function() {
        state.stylesheets && insertSuspendedStylesheets(state, state.stylesheets);
        if (state.unsuspend) {
          var unsuspend = state.unsuspend;
          state.unsuspend = null;
          unsuspend();
        }
      }, 6e4 + timeoutOffset);
      0 < state.imgBytes && 0 === estimatedBytesWithinLimit && (estimatedBytesWithinLimit = 62500 * estimateBandwidth());
      var imgTimer = setTimeout(
        function() {
          state.waitingForImages = false;
          if (0 === state.count && (state.stylesheets && insertSuspendedStylesheets(state, state.stylesheets), state.unsuspend)) {
            var unsuspend = state.unsuspend;
            state.unsuspend = null;
            unsuspend();
          }
        },
        (state.imgBytes > estimatedBytesWithinLimit ? 50 : 800) + timeoutOffset
      );
      state.unsuspend = commit;
      return function() {
        state.unsuspend = null;
        clearTimeout(stylesheetTimer);
        clearTimeout(imgTimer);
      };
    } : null;
  }
  function onUnsuspend() {
    this.count--;
    if (0 === this.count && (0 === this.imgCount || !this.waitingForImages)) {
      if (this.stylesheets) insertSuspendedStylesheets(this, this.stylesheets);
      else if (this.unsuspend) {
        var unsuspend = this.unsuspend;
        this.unsuspend = null;
        unsuspend();
      }
    }
  }
  var precedencesByRoot = null;
  function insertSuspendedStylesheets(state, resources) {
    state.stylesheets = null;
    null !== state.unsuspend && (state.count++, precedencesByRoot = /* @__PURE__ */ new Map(), resources.forEach(insertStylesheetIntoRoot, state), precedencesByRoot = null, onUnsuspend.call(state));
  }
  function insertStylesheetIntoRoot(root3, resource) {
    if (!(resource.state.loading & 4)) {
      var precedences = precedencesByRoot.get(root3);
      if (precedences) var last = precedences.get(null);
      else {
        precedences = /* @__PURE__ */ new Map();
        precedencesByRoot.set(root3, precedences);
        for (var nodes = root3.querySelectorAll(
          "link[data-precedence],style[data-precedence]"
        ), i = 0; i < nodes.length; i++) {
          var node2 = nodes[i];
          if ("LINK" === node2.nodeName || "not all" !== node2.getAttribute("media"))
            precedences.set(node2.dataset.precedence, node2), last = node2;
        }
        last && precedences.set(null, last);
      }
      nodes = resource.instance;
      node2 = nodes.getAttribute("data-precedence");
      i = precedences.get(node2) || last;
      i === last && precedences.set(null, nodes);
      precedences.set(node2, nodes);
      this.count++;
      last = onUnsuspend.bind(this);
      nodes.addEventListener("load", last);
      nodes.addEventListener("error", last);
      i ? i.parentNode.insertBefore(nodes, i.nextSibling) : (root3 = 9 === root3.nodeType ? root3.head : root3, root3.insertBefore(nodes, root3.firstChild));
      resource.state.loading |= 4;
    }
  }
  var HostTransitionContext = {
    $$typeof: REACT_CONTEXT_TYPE,
    Provider: null,
    Consumer: null,
    _currentValue: sharedNotPendingObject,
    _currentValue2: sharedNotPendingObject,
    _threadCount: 0
  };
  function FiberRootNode(containerInfo, tag, hydrate, identifierPrefix, onUncaughtError, onCaughtError, onRecoverableError, onDefaultTransitionIndicator, formState) {
    this.tag = 1;
    this.containerInfo = containerInfo;
    this.pingCache = this.current = this.pendingChildren = null;
    this.timeoutHandle = -1;
    this.callbackNode = this.next = this.pendingContext = this.context = this.cancelPendingCommit = null;
    this.callbackPriority = 0;
    this.expirationTimes = createLaneMap(-1);
    this.entangledLanes = this.shellSuspendCounter = this.errorRecoveryDisabledLanes = this.expiredLanes = this.warmLanes = this.pingedLanes = this.suspendedLanes = this.pendingLanes = 0;
    this.entanglements = createLaneMap(0);
    this.hiddenUpdates = createLaneMap(null);
    this.identifierPrefix = identifierPrefix;
    this.onUncaughtError = onUncaughtError;
    this.onCaughtError = onCaughtError;
    this.onRecoverableError = onRecoverableError;
    this.pooledCache = null;
    this.pooledCacheLanes = 0;
    this.formState = formState;
    this.incompleteTransitions = /* @__PURE__ */ new Map();
  }
  function createFiberRoot(containerInfo, tag, hydrate, initialChildren, hydrationCallbacks, isStrictMode, identifierPrefix, formState, onUncaughtError, onCaughtError, onRecoverableError, onDefaultTransitionIndicator) {
    containerInfo = new FiberRootNode(
      containerInfo,
      tag,
      hydrate,
      identifierPrefix,
      onUncaughtError,
      onCaughtError,
      onRecoverableError,
      onDefaultTransitionIndicator,
      formState
    );
    tag = 1;
    true === isStrictMode && (tag |= 24);
    isStrictMode = createFiberImplClass(3, null, null, tag);
    containerInfo.current = isStrictMode;
    isStrictMode.stateNode = containerInfo;
    tag = createCache();
    tag.refCount++;
    containerInfo.pooledCache = tag;
    tag.refCount++;
    isStrictMode.memoizedState = {
      element: initialChildren,
      isDehydrated: hydrate,
      cache: tag
    };
    initializeUpdateQueue(isStrictMode);
    return containerInfo;
  }
  function getContextForSubtree(parentComponent) {
    if (!parentComponent) return emptyContextObject;
    parentComponent = emptyContextObject;
    return parentComponent;
  }
  function updateContainerImpl(rootFiber, lane, element2, container, parentComponent, callback) {
    parentComponent = getContextForSubtree(parentComponent);
    null === container.context ? container.context = parentComponent : container.pendingContext = parentComponent;
    container = createUpdate(lane);
    container.payload = { element: element2 };
    callback = void 0 === callback ? null : callback;
    null !== callback && (container.callback = callback);
    element2 = enqueueUpdate(rootFiber, container, lane);
    null !== element2 && (scheduleUpdateOnFiber(element2, rootFiber, lane), entangleTransitions(element2, rootFiber, lane));
  }
  function markRetryLaneImpl(fiber, retryLane) {
    fiber = fiber.memoizedState;
    if (null !== fiber && null !== fiber.dehydrated) {
      var a = fiber.retryLane;
      fiber.retryLane = 0 !== a && a < retryLane ? a : retryLane;
    }
  }
  function markRetryLaneIfNotHydrated(fiber, retryLane) {
    markRetryLaneImpl(fiber, retryLane);
    (fiber = fiber.alternate) && markRetryLaneImpl(fiber, retryLane);
  }
  function attemptContinuousHydration(fiber) {
    if (13 === fiber.tag || 31 === fiber.tag) {
      var root3 = enqueueConcurrentRenderForLane(fiber, 67108864);
      null !== root3 && scheduleUpdateOnFiber(root3, fiber, 67108864);
      markRetryLaneIfNotHydrated(fiber, 67108864);
    }
  }
  function attemptHydrationAtCurrentPriority(fiber) {
    if (13 === fiber.tag || 31 === fiber.tag) {
      var lane = requestUpdateLane();
      lane = getBumpedLaneForHydrationByLane(lane);
      var root3 = enqueueConcurrentRenderForLane(fiber, lane);
      null !== root3 && scheduleUpdateOnFiber(root3, fiber, lane);
      markRetryLaneIfNotHydrated(fiber, lane);
    }
  }
  var _enabled = true;
  function dispatchDiscreteEvent(domEventName, eventSystemFlags, container, nativeEvent) {
    var prevTransition = ReactSharedInternals.T;
    ReactSharedInternals.T = null;
    var previousPriority = ReactDOMSharedInternals.p;
    try {
      ReactDOMSharedInternals.p = 2, dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
    } finally {
      ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = prevTransition;
    }
  }
  function dispatchContinuousEvent(domEventName, eventSystemFlags, container, nativeEvent) {
    var prevTransition = ReactSharedInternals.T;
    ReactSharedInternals.T = null;
    var previousPriority = ReactDOMSharedInternals.p;
    try {
      ReactDOMSharedInternals.p = 8, dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
    } finally {
      ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = prevTransition;
    }
  }
  function dispatchEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
    if (_enabled) {
      var blockedOn = findInstanceBlockingEvent(nativeEvent);
      if (null === blockedOn)
        dispatchEventForPluginEventSystem(
          domEventName,
          eventSystemFlags,
          nativeEvent,
          return_targetInst,
          targetContainer
        ), clearIfContinuousEvent(domEventName, nativeEvent);
      else if (queueIfContinuousEvent(
        blockedOn,
        domEventName,
        eventSystemFlags,
        targetContainer,
        nativeEvent
      ))
        nativeEvent.stopPropagation();
      else if (clearIfContinuousEvent(domEventName, nativeEvent), eventSystemFlags & 4 && -1 < discreteReplayableEvents.indexOf(domEventName)) {
        for (; null !== blockedOn; ) {
          var fiber = getInstanceFromNode(blockedOn);
          if (null !== fiber)
            switch (fiber.tag) {
              case 3:
                fiber = fiber.stateNode;
                if (fiber.current.memoizedState.isDehydrated) {
                  var lanes = getHighestPriorityLanes(fiber.pendingLanes);
                  if (0 !== lanes) {
                    var root3 = fiber;
                    root3.pendingLanes |= 2;
                    for (root3.entangledLanes |= 2; lanes; ) {
                      var lane = 1 << 31 - clz32(lanes);
                      root3.entanglements[1] |= lane;
                      lanes &= ~lane;
                    }
                    ensureRootIsScheduled(fiber);
                    0 === (executionContext & 6) && (workInProgressRootRenderTargetTime = now() + 500, flushSyncWorkAcrossRoots_impl(0));
                  }
                }
                break;
              case 31:
              case 13:
                root3 = enqueueConcurrentRenderForLane(fiber, 2), null !== root3 && scheduleUpdateOnFiber(root3, fiber, 2), flushSyncWork$1(), markRetryLaneIfNotHydrated(fiber, 2);
            }
          fiber = findInstanceBlockingEvent(nativeEvent);
          null === fiber && dispatchEventForPluginEventSystem(
            domEventName,
            eventSystemFlags,
            nativeEvent,
            return_targetInst,
            targetContainer
          );
          if (fiber === blockedOn) break;
          blockedOn = fiber;
        }
        null !== blockedOn && nativeEvent.stopPropagation();
      } else
        dispatchEventForPluginEventSystem(
          domEventName,
          eventSystemFlags,
          nativeEvent,
          null,
          targetContainer
        );
    }
  }
  function findInstanceBlockingEvent(nativeEvent) {
    nativeEvent = getEventTarget(nativeEvent);
    return findInstanceBlockingTarget(nativeEvent);
  }
  var return_targetInst = null;
  function findInstanceBlockingTarget(targetNode) {
    return_targetInst = null;
    targetNode = getClosestInstanceFromNode(targetNode);
    if (null !== targetNode) {
      var nearestMounted = getNearestMountedFiber(targetNode);
      if (null === nearestMounted) targetNode = null;
      else {
        var tag = nearestMounted.tag;
        if (13 === tag) {
          targetNode = getSuspenseInstanceFromFiber(nearestMounted);
          if (null !== targetNode) return targetNode;
          targetNode = null;
        } else if (31 === tag) {
          targetNode = getActivityInstanceFromFiber(nearestMounted);
          if (null !== targetNode) return targetNode;
          targetNode = null;
        } else if (3 === tag) {
          if (nearestMounted.stateNode.current.memoizedState.isDehydrated)
            return 3 === nearestMounted.tag ? nearestMounted.stateNode.containerInfo : null;
          targetNode = null;
        } else nearestMounted !== targetNode && (targetNode = null);
      }
    }
    return_targetInst = targetNode;
    return null;
  }
  function getEventPriority(domEventName) {
    switch (domEventName) {
      case "beforetoggle":
      case "cancel":
      case "click":
      case "close":
      case "contextmenu":
      case "copy":
      case "cut":
      case "auxclick":
      case "dblclick":
      case "dragend":
      case "dragstart":
      case "drop":
      case "focusin":
      case "focusout":
      case "input":
      case "invalid":
      case "keydown":
      case "keypress":
      case "keyup":
      case "mousedown":
      case "mouseup":
      case "paste":
      case "pause":
      case "play":
      case "pointercancel":
      case "pointerdown":
      case "pointerup":
      case "ratechange":
      case "reset":
      case "resize":
      case "seeked":
      case "submit":
      case "toggle":
      case "touchcancel":
      case "touchend":
      case "touchstart":
      case "volumechange":
      case "change":
      case "selectionchange":
      case "textInput":
      case "compositionstart":
      case "compositionend":
      case "compositionupdate":
      case "beforeblur":
      case "afterblur":
      case "beforeinput":
      case "blur":
      case "fullscreenchange":
      case "focus":
      case "hashchange":
      case "popstate":
      case "select":
      case "selectstart":
        return 2;
      case "drag":
      case "dragenter":
      case "dragexit":
      case "dragleave":
      case "dragover":
      case "mousemove":
      case "mouseout":
      case "mouseover":
      case "pointermove":
      case "pointerout":
      case "pointerover":
      case "scroll":
      case "touchmove":
      case "wheel":
      case "mouseenter":
      case "mouseleave":
      case "pointerenter":
      case "pointerleave":
        return 8;
      case "message":
        switch (getCurrentPriorityLevel()) {
          case ImmediatePriority:
            return 2;
          case UserBlockingPriority:
            return 8;
          case NormalPriority$1:
          case LowPriority:
            return 32;
          case IdlePriority:
            return 268435456;
          default:
            return 32;
        }
      default:
        return 32;
    }
  }
  var hasScheduledReplayAttempt = false, queuedFocus = null, queuedDrag = null, queuedMouse = null, queuedPointers = /* @__PURE__ */ new Map(), queuedPointerCaptures = /* @__PURE__ */ new Map(), queuedExplicitHydrationTargets = [], discreteReplayableEvents = "mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset".split(
    " "
  );
  function clearIfContinuousEvent(domEventName, nativeEvent) {
    switch (domEventName) {
      case "focusin":
      case "focusout":
        queuedFocus = null;
        break;
      case "dragenter":
      case "dragleave":
        queuedDrag = null;
        break;
      case "mouseover":
      case "mouseout":
        queuedMouse = null;
        break;
      case "pointerover":
      case "pointerout":
        queuedPointers.delete(nativeEvent.pointerId);
        break;
      case "gotpointercapture":
      case "lostpointercapture":
        queuedPointerCaptures.delete(nativeEvent.pointerId);
    }
  }
  function accumulateOrCreateContinuousQueuedReplayableEvent(existingQueuedEvent, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
    if (null === existingQueuedEvent || existingQueuedEvent.nativeEvent !== nativeEvent)
      return existingQueuedEvent = {
        blockedOn,
        domEventName,
        eventSystemFlags,
        nativeEvent,
        targetContainers: [targetContainer]
      }, null !== blockedOn && (blockedOn = getInstanceFromNode(blockedOn), null !== blockedOn && attemptContinuousHydration(blockedOn)), existingQueuedEvent;
    existingQueuedEvent.eventSystemFlags |= eventSystemFlags;
    blockedOn = existingQueuedEvent.targetContainers;
    null !== targetContainer && -1 === blockedOn.indexOf(targetContainer) && blockedOn.push(targetContainer);
    return existingQueuedEvent;
  }
  function queueIfContinuousEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
    switch (domEventName) {
      case "focusin":
        return queuedFocus = accumulateOrCreateContinuousQueuedReplayableEvent(
          queuedFocus,
          blockedOn,
          domEventName,
          eventSystemFlags,
          targetContainer,
          nativeEvent
        ), true;
      case "dragenter":
        return queuedDrag = accumulateOrCreateContinuousQueuedReplayableEvent(
          queuedDrag,
          blockedOn,
          domEventName,
          eventSystemFlags,
          targetContainer,
          nativeEvent
        ), true;
      case "mouseover":
        return queuedMouse = accumulateOrCreateContinuousQueuedReplayableEvent(
          queuedMouse,
          blockedOn,
          domEventName,
          eventSystemFlags,
          targetContainer,
          nativeEvent
        ), true;
      case "pointerover":
        var pointerId = nativeEvent.pointerId;
        queuedPointers.set(
          pointerId,
          accumulateOrCreateContinuousQueuedReplayableEvent(
            queuedPointers.get(pointerId) || null,
            blockedOn,
            domEventName,
            eventSystemFlags,
            targetContainer,
            nativeEvent
          )
        );
        return true;
      case "gotpointercapture":
        return pointerId = nativeEvent.pointerId, queuedPointerCaptures.set(
          pointerId,
          accumulateOrCreateContinuousQueuedReplayableEvent(
            queuedPointerCaptures.get(pointerId) || null,
            blockedOn,
            domEventName,
            eventSystemFlags,
            targetContainer,
            nativeEvent
          )
        ), true;
    }
    return false;
  }
  function attemptExplicitHydrationTarget(queuedTarget) {
    var targetInst = getClosestInstanceFromNode(queuedTarget.target);
    if (null !== targetInst) {
      var nearestMounted = getNearestMountedFiber(targetInst);
      if (null !== nearestMounted) {
        if (targetInst = nearestMounted.tag, 13 === targetInst) {
          if (targetInst = getSuspenseInstanceFromFiber(nearestMounted), null !== targetInst) {
            queuedTarget.blockedOn = targetInst;
            runWithPriority(queuedTarget.priority, function() {
              attemptHydrationAtCurrentPriority(nearestMounted);
            });
            return;
          }
        } else if (31 === targetInst) {
          if (targetInst = getActivityInstanceFromFiber(nearestMounted), null !== targetInst) {
            queuedTarget.blockedOn = targetInst;
            runWithPriority(queuedTarget.priority, function() {
              attemptHydrationAtCurrentPriority(nearestMounted);
            });
            return;
          }
        } else if (3 === targetInst && nearestMounted.stateNode.current.memoizedState.isDehydrated) {
          queuedTarget.blockedOn = 3 === nearestMounted.tag ? nearestMounted.stateNode.containerInfo : null;
          return;
        }
      }
    }
    queuedTarget.blockedOn = null;
  }
  function attemptReplayContinuousQueuedEvent(queuedEvent) {
    if (null !== queuedEvent.blockedOn) return false;
    for (var targetContainers = queuedEvent.targetContainers; 0 < targetContainers.length; ) {
      var nextBlockedOn = findInstanceBlockingEvent(queuedEvent.nativeEvent);
      if (null === nextBlockedOn) {
        nextBlockedOn = queuedEvent.nativeEvent;
        var nativeEventClone = new nextBlockedOn.constructor(
          nextBlockedOn.type,
          nextBlockedOn
        );
        currentReplayingEvent = nativeEventClone;
        nextBlockedOn.target.dispatchEvent(nativeEventClone);
        currentReplayingEvent = null;
      } else
        return targetContainers = getInstanceFromNode(nextBlockedOn), null !== targetContainers && attemptContinuousHydration(targetContainers), queuedEvent.blockedOn = nextBlockedOn, false;
      targetContainers.shift();
    }
    return true;
  }
  function attemptReplayContinuousQueuedEventInMap(queuedEvent, key, map2) {
    attemptReplayContinuousQueuedEvent(queuedEvent) && map2.delete(key);
  }
  function replayUnblockedEvents() {
    hasScheduledReplayAttempt = false;
    null !== queuedFocus && attemptReplayContinuousQueuedEvent(queuedFocus) && (queuedFocus = null);
    null !== queuedDrag && attemptReplayContinuousQueuedEvent(queuedDrag) && (queuedDrag = null);
    null !== queuedMouse && attemptReplayContinuousQueuedEvent(queuedMouse) && (queuedMouse = null);
    queuedPointers.forEach(attemptReplayContinuousQueuedEventInMap);
    queuedPointerCaptures.forEach(attemptReplayContinuousQueuedEventInMap);
  }
  function scheduleCallbackIfUnblocked(queuedEvent, unblocked) {
    queuedEvent.blockedOn === unblocked && (queuedEvent.blockedOn = null, hasScheduledReplayAttempt || (hasScheduledReplayAttempt = true, Scheduler.unstable_scheduleCallback(
      Scheduler.unstable_NormalPriority,
      replayUnblockedEvents
    )));
  }
  var lastScheduledReplayQueue = null;
  function scheduleReplayQueueIfNeeded(formReplayingQueue) {
    lastScheduledReplayQueue !== formReplayingQueue && (lastScheduledReplayQueue = formReplayingQueue, Scheduler.unstable_scheduleCallback(
      Scheduler.unstable_NormalPriority,
      function() {
        lastScheduledReplayQueue === formReplayingQueue && (lastScheduledReplayQueue = null);
        for (var i = 0; i < formReplayingQueue.length; i += 3) {
          var form = formReplayingQueue[i], submitterOrAction = formReplayingQueue[i + 1], formData = formReplayingQueue[i + 2];
          if ("function" !== typeof submitterOrAction)
            if (null === findInstanceBlockingTarget(submitterOrAction || form))
              continue;
            else break;
          var formInst = getInstanceFromNode(form);
          null !== formInst && (formReplayingQueue.splice(i, 3), i -= 3, startHostTransition(
            formInst,
            {
              pending: true,
              data: formData,
              method: form.method,
              action: submitterOrAction
            },
            submitterOrAction,
            formData
          ));
        }
      }
    ));
  }
  function retryIfBlockedOn(unblocked) {
    function unblock(queuedEvent) {
      return scheduleCallbackIfUnblocked(queuedEvent, unblocked);
    }
    null !== queuedFocus && scheduleCallbackIfUnblocked(queuedFocus, unblocked);
    null !== queuedDrag && scheduleCallbackIfUnblocked(queuedDrag, unblocked);
    null !== queuedMouse && scheduleCallbackIfUnblocked(queuedMouse, unblocked);
    queuedPointers.forEach(unblock);
    queuedPointerCaptures.forEach(unblock);
    for (var i = 0; i < queuedExplicitHydrationTargets.length; i++) {
      var queuedTarget = queuedExplicitHydrationTargets[i];
      queuedTarget.blockedOn === unblocked && (queuedTarget.blockedOn = null);
    }
    for (; 0 < queuedExplicitHydrationTargets.length && (i = queuedExplicitHydrationTargets[0], null === i.blockedOn); )
      attemptExplicitHydrationTarget(i), null === i.blockedOn && queuedExplicitHydrationTargets.shift();
    i = (unblocked.ownerDocument || unblocked).$$reactFormReplay;
    if (null != i)
      for (queuedTarget = 0; queuedTarget < i.length; queuedTarget += 3) {
        var form = i[queuedTarget], submitterOrAction = i[queuedTarget + 1], formProps = form[internalPropsKey] || null;
        if ("function" === typeof submitterOrAction)
          formProps || scheduleReplayQueueIfNeeded(i);
        else if (formProps) {
          var action = null;
          if (submitterOrAction && submitterOrAction.hasAttribute("formAction"))
            if (form = submitterOrAction, formProps = submitterOrAction[internalPropsKey] || null)
              action = formProps.formAction;
            else {
              if (null !== findInstanceBlockingTarget(form)) continue;
            }
          else action = formProps.action;
          "function" === typeof action ? i[queuedTarget + 1] = action : (i.splice(queuedTarget, 3), queuedTarget -= 3);
          scheduleReplayQueueIfNeeded(i);
        }
      }
  }
  function defaultOnDefaultTransitionIndicator() {
    function handleNavigate(event) {
      event.canIntercept && "react-transition" === event.info && event.intercept({
        handler: function() {
          return new Promise(function(resolve) {
            return pendingResolve = resolve;
          });
        },
        focusReset: "manual",
        scroll: "manual"
      });
    }
    function handleNavigateComplete() {
      null !== pendingResolve && (pendingResolve(), pendingResolve = null);
      isCancelled || setTimeout(startFakeNavigation, 20);
    }
    function startFakeNavigation() {
      if (!isCancelled && !navigation.transition) {
        var currentEntry = navigation.currentEntry;
        currentEntry && null != currentEntry.url && navigation.navigate(currentEntry.url, {
          state: currentEntry.getState(),
          info: "react-transition",
          history: "replace"
        });
      }
    }
    if ("object" === typeof navigation) {
      var isCancelled = false, pendingResolve = null;
      navigation.addEventListener("navigate", handleNavigate);
      navigation.addEventListener("navigatesuccess", handleNavigateComplete);
      navigation.addEventListener("navigateerror", handleNavigateComplete);
      setTimeout(startFakeNavigation, 100);
      return function() {
        isCancelled = true;
        navigation.removeEventListener("navigate", handleNavigate);
        navigation.removeEventListener("navigatesuccess", handleNavigateComplete);
        navigation.removeEventListener("navigateerror", handleNavigateComplete);
        null !== pendingResolve && (pendingResolve(), pendingResolve = null);
      };
    }
  }
  function ReactDOMRoot(internalRoot) {
    this._internalRoot = internalRoot;
  }
  ReactDOMHydrationRoot.prototype.render = ReactDOMRoot.prototype.render = function(children) {
    var root3 = this._internalRoot;
    if (null === root3) throw Error(formatProdErrorMessage(409));
    var current = root3.current, lane = requestUpdateLane();
    updateContainerImpl(current, lane, children, root3, null, null);
  };
  ReactDOMHydrationRoot.prototype.unmount = ReactDOMRoot.prototype.unmount = function() {
    var root3 = this._internalRoot;
    if (null !== root3) {
      this._internalRoot = null;
      var container = root3.containerInfo;
      updateContainerImpl(root3.current, 2, null, root3, null, null);
      flushSyncWork$1();
      container[internalContainerInstanceKey] = null;
    }
  };
  function ReactDOMHydrationRoot(internalRoot) {
    this._internalRoot = internalRoot;
  }
  ReactDOMHydrationRoot.prototype.unstable_scheduleHydration = function(target) {
    if (target) {
      var updatePriority = resolveUpdatePriority();
      target = { blockedOn: null, target, priority: updatePriority };
      for (var i = 0; i < queuedExplicitHydrationTargets.length && 0 !== updatePriority && updatePriority < queuedExplicitHydrationTargets[i].priority; i++) ;
      queuedExplicitHydrationTargets.splice(i, 0, target);
      0 === i && attemptExplicitHydrationTarget(target);
    }
  };
  var isomorphicReactPackageVersion$jscomp$inline_1840 = React2.version;
  if ("19.2.7" !== isomorphicReactPackageVersion$jscomp$inline_1840)
    throw Error(
      formatProdErrorMessage(
        527,
        isomorphicReactPackageVersion$jscomp$inline_1840,
        "19.2.7"
      )
    );
  ReactDOMSharedInternals.findDOMNode = function(componentOrElement) {
    var fiber = componentOrElement._reactInternals;
    if (void 0 === fiber) {
      if ("function" === typeof componentOrElement.render)
        throw Error(formatProdErrorMessage(188));
      componentOrElement = Object.keys(componentOrElement).join(",");
      throw Error(formatProdErrorMessage(268, componentOrElement));
    }
    componentOrElement = findCurrentFiberUsingSlowPath(fiber);
    componentOrElement = null !== componentOrElement ? findCurrentHostFiberImpl(componentOrElement) : null;
    componentOrElement = null === componentOrElement ? null : componentOrElement.stateNode;
    return componentOrElement;
  };
  var internals$jscomp$inline_2347 = {
    bundleType: 0,
    version: "19.2.7",
    rendererPackageName: "react-dom",
    currentDispatcherRef: ReactSharedInternals,
    reconcilerVersion: "19.2.7"
  };
  if ("undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__) {
    var hook$jscomp$inline_2348 = __REACT_DEVTOOLS_GLOBAL_HOOK__;
    if (!hook$jscomp$inline_2348.isDisabled && hook$jscomp$inline_2348.supportsFiber)
      try {
        rendererID = hook$jscomp$inline_2348.inject(
          internals$jscomp$inline_2347
        ), injectedHook = hook$jscomp$inline_2348;
      } catch (err) {
      }
  }
  reactDomClient_production.createRoot = function(container, options2) {
    if (!isValidContainer(container)) throw Error(formatProdErrorMessage(299));
    var isStrictMode = false, identifierPrefix = "", onUncaughtError = defaultOnUncaughtError, onCaughtError = defaultOnCaughtError, onRecoverableError = defaultOnRecoverableError;
    null !== options2 && void 0 !== options2 && (true === options2.unstable_strictMode && (isStrictMode = true), void 0 !== options2.identifierPrefix && (identifierPrefix = options2.identifierPrefix), void 0 !== options2.onUncaughtError && (onUncaughtError = options2.onUncaughtError), void 0 !== options2.onCaughtError && (onCaughtError = options2.onCaughtError), void 0 !== options2.onRecoverableError && (onRecoverableError = options2.onRecoverableError));
    options2 = createFiberRoot(
      container,
      1,
      false,
      null,
      null,
      isStrictMode,
      identifierPrefix,
      null,
      onUncaughtError,
      onCaughtError,
      onRecoverableError,
      defaultOnDefaultTransitionIndicator
    );
    container[internalContainerInstanceKey] = options2.current;
    listenToAllSupportedEvents(container);
    return new ReactDOMRoot(options2);
  };
  reactDomClient_production.hydrateRoot = function(container, initialChildren, options2) {
    if (!isValidContainer(container)) throw Error(formatProdErrorMessage(299));
    var isStrictMode = false, identifierPrefix = "", onUncaughtError = defaultOnUncaughtError, onCaughtError = defaultOnCaughtError, onRecoverableError = defaultOnRecoverableError, formState = null;
    null !== options2 && void 0 !== options2 && (true === options2.unstable_strictMode && (isStrictMode = true), void 0 !== options2.identifierPrefix && (identifierPrefix = options2.identifierPrefix), void 0 !== options2.onUncaughtError && (onUncaughtError = options2.onUncaughtError), void 0 !== options2.onCaughtError && (onCaughtError = options2.onCaughtError), void 0 !== options2.onRecoverableError && (onRecoverableError = options2.onRecoverableError), void 0 !== options2.formState && (formState = options2.formState));
    initialChildren = createFiberRoot(
      container,
      1,
      true,
      initialChildren,
      null != options2 ? options2 : null,
      isStrictMode,
      identifierPrefix,
      formState,
      onUncaughtError,
      onCaughtError,
      onRecoverableError,
      defaultOnDefaultTransitionIndicator
    );
    initialChildren.context = getContextForSubtree(null);
    options2 = initialChildren.current;
    isStrictMode = requestUpdateLane();
    isStrictMode = getBumpedLaneForHydrationByLane(isStrictMode);
    identifierPrefix = createUpdate(isStrictMode);
    identifierPrefix.callback = null;
    enqueueUpdate(options2, identifierPrefix, isStrictMode);
    options2 = isStrictMode;
    initialChildren.current.lanes = options2;
    markRootUpdated$1(initialChildren, options2);
    ensureRootIsScheduled(initialChildren);
    container[internalContainerInstanceKey] = initialChildren.current;
    listenToAllSupportedEvents(container);
    return new ReactDOMHydrationRoot(initialChildren);
  };
  reactDomClient_production.version = "19.2.7";
  return reactDomClient_production;
}
var hasRequiredClient;
function requireClient() {
  if (hasRequiredClient) return client.exports;
  hasRequiredClient = 1;
  function checkDCE() {
    if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === "undefined" || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== "function") {
      return;
    }
    try {
      __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
    } catch (err) {
      console.error(err);
    }
  }
  {
    checkDCE();
    client.exports = requireReactDomClient_production();
  }
  return client.exports;
}
var clientExports = requireClient();
function ok$1() {
}
function unreachable() {
}
function stringify$1(values, options) {
  const settings = {};
  const input = values[values.length - 1] === "" ? [...values, ""] : values;
  return input.join(
    (settings.padRight ? " " : "") + "," + (settings.padLeft === false ? "" : " ")
  ).trim();
}
const nameRe = /^[$_\p{ID_Start}][$_\u{200C}\u{200D}\p{ID_Continue}]*$/u;
const nameReJsx = /^[$_\p{ID_Start}][-$_\u{200C}\u{200D}\p{ID_Continue}]*$/u;
const emptyOptions$3 = {};
function name$2(name2, options) {
  const settings = emptyOptions$3;
  const re2 = settings.jsx ? nameReJsx : nameRe;
  return re2.test(name2);
}
const re = /[ \t\n\f\r]/g;
function whitespace(thing) {
  return typeof thing === "object" ? thing.type === "text" ? empty$1(thing.value) : false : empty$1(thing);
}
function empty$1(value) {
  return value.replace(re, "") === "";
}
class Schema {
  /**
   * @param {SchemaType['property']} property
   *   Property.
   * @param {SchemaType['normal']} normal
   *   Normal.
   * @param {Space | undefined} [space]
   *   Space.
   * @returns
   *   Schema.
   */
  constructor(property, normal, space2) {
    this.normal = normal;
    this.property = property;
    if (space2) {
      this.space = space2;
    }
  }
}
Schema.prototype.normal = {};
Schema.prototype.property = {};
Schema.prototype.space = void 0;
function merge(definitions, space2) {
  const property = {};
  const normal = {};
  for (const definition2 of definitions) {
    Object.assign(property, definition2.property);
    Object.assign(normal, definition2.normal);
  }
  return new Schema(property, normal, space2);
}
function normalize$1(value) {
  return value.toLowerCase();
}
class Info {
  /**
   * @param {string} property
   *   Property.
   * @param {string} attribute
   *   Attribute.
   * @returns
   *   Info.
   */
  constructor(property, attribute) {
    this.attribute = attribute;
    this.property = property;
  }
}
Info.prototype.attribute = "";
Info.prototype.booleanish = false;
Info.prototype.boolean = false;
Info.prototype.commaOrSpaceSeparated = false;
Info.prototype.commaSeparated = false;
Info.prototype.defined = false;
Info.prototype.mustUseProperty = false;
Info.prototype.number = false;
Info.prototype.overloadedBoolean = false;
Info.prototype.property = "";
Info.prototype.spaceSeparated = false;
Info.prototype.space = void 0;
let powers = 0;
const boolean = increment();
const booleanish = increment();
const overloadedBoolean = increment();
const number = increment();
const spaceSeparated = increment();
const commaSeparated = increment();
const commaOrSpaceSeparated = increment();
function increment() {
  return 2 ** ++powers;
}
const types = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  boolean,
  booleanish,
  commaOrSpaceSeparated,
  commaSeparated,
  number,
  overloadedBoolean,
  spaceSeparated
}, Symbol.toStringTag, { value: "Module" }));
const checks = (
  /** @type {ReadonlyArray<keyof typeof types>} */
  Object.keys(types)
);
class DefinedInfo extends Info {
  /**
   * @constructor
   * @param {string} property
   *   Property.
   * @param {string} attribute
   *   Attribute.
   * @param {number | null | undefined} [mask]
   *   Mask.
   * @param {Space | undefined} [space]
   *   Space.
   * @returns
   *   Info.
   */
  constructor(property, attribute, mask, space2) {
    let index2 = -1;
    super(property, attribute);
    mark(this, "space", space2);
    if (typeof mask === "number") {
      while (++index2 < checks.length) {
        const check = checks[index2];
        mark(this, checks[index2], (mask & types[check]) === types[check]);
      }
    }
  }
}
DefinedInfo.prototype.defined = true;
function mark(values, key, value) {
  if (value) {
    values[key] = value;
  }
}
function create(definition2) {
  const properties = {};
  const normals = {};
  for (const [property, value] of Object.entries(definition2.properties)) {
    const info = new DefinedInfo(
      property,
      definition2.transform(definition2.attributes || {}, property),
      value,
      definition2.space
    );
    if (definition2.mustUseProperty && definition2.mustUseProperty.includes(property)) {
      info.mustUseProperty = true;
    }
    properties[property] = info;
    normals[normalize$1(property)] = property;
    normals[normalize$1(info.attribute)] = property;
  }
  return new Schema(properties, normals, definition2.space);
}
const aria = create({
  properties: {
    ariaActiveDescendant: null,
    ariaAtomic: booleanish,
    ariaAutoComplete: null,
    ariaBusy: booleanish,
    ariaChecked: booleanish,
    ariaColCount: number,
    ariaColIndex: number,
    ariaColSpan: number,
    ariaControls: spaceSeparated,
    ariaCurrent: null,
    ariaDescribedBy: spaceSeparated,
    ariaDetails: null,
    ariaDisabled: booleanish,
    ariaDropEffect: spaceSeparated,
    ariaErrorMessage: null,
    ariaExpanded: booleanish,
    ariaFlowTo: spaceSeparated,
    ariaGrabbed: booleanish,
    ariaHasPopup: null,
    ariaHidden: booleanish,
    ariaInvalid: null,
    ariaKeyShortcuts: null,
    ariaLabel: null,
    ariaLabelledBy: spaceSeparated,
    ariaLevel: number,
    ariaLive: null,
    ariaModal: booleanish,
    ariaMultiLine: booleanish,
    ariaMultiSelectable: booleanish,
    ariaOrientation: null,
    ariaOwns: spaceSeparated,
    ariaPlaceholder: null,
    ariaPosInSet: number,
    ariaPressed: booleanish,
    ariaReadOnly: booleanish,
    ariaRelevant: null,
    ariaRequired: booleanish,
    ariaRoleDescription: spaceSeparated,
    ariaRowCount: number,
    ariaRowIndex: number,
    ariaRowSpan: number,
    ariaSelected: booleanish,
    ariaSetSize: number,
    ariaSort: null,
    ariaValueMax: number,
    ariaValueMin: number,
    ariaValueNow: number,
    ariaValueText: null,
    role: null
  },
  transform(_, property) {
    return property === "role" ? property : "aria-" + property.slice(4).toLowerCase();
  }
});
function caseSensitiveTransform(attributes, attribute) {
  return attribute in attributes ? attributes[attribute] : attribute;
}
function caseInsensitiveTransform(attributes, property) {
  return caseSensitiveTransform(attributes, property.toLowerCase());
}
const html$3 = create({
  attributes: {
    acceptcharset: "accept-charset",
    classname: "class",
    htmlfor: "for",
    httpequiv: "http-equiv"
  },
  mustUseProperty: ["checked", "multiple", "muted", "selected"],
  properties: {
    // Standard Properties.
    abbr: null,
    accept: commaSeparated,
    acceptCharset: spaceSeparated,
    accessKey: spaceSeparated,
    action: null,
    allow: null,
    allowFullScreen: boolean,
    allowPaymentRequest: boolean,
    allowUserMedia: boolean,
    alpha: boolean,
    alt: null,
    as: null,
    async: boolean,
    autoCapitalize: null,
    autoComplete: spaceSeparated,
    autoFocus: boolean,
    autoPlay: boolean,
    blocking: spaceSeparated,
    capture: null,
    charSet: null,
    checked: boolean,
    cite: null,
    className: spaceSeparated,
    closedBy: null,
    colorSpace: null,
    cols: number,
    colSpan: number,
    command: null,
    commandFor: null,
    content: null,
    contentEditable: booleanish,
    controls: boolean,
    controlsList: spaceSeparated,
    coords: number | commaSeparated,
    crossOrigin: null,
    data: null,
    dateTime: null,
    decoding: null,
    default: boolean,
    defer: boolean,
    dir: null,
    dirName: null,
    disabled: boolean,
    download: overloadedBoolean,
    draggable: booleanish,
    encType: null,
    enterKeyHint: null,
    fetchPriority: null,
    form: null,
    formAction: null,
    formEncType: null,
    formMethod: null,
    formNoValidate: boolean,
    formTarget: null,
    headers: spaceSeparated,
    height: number,
    hidden: overloadedBoolean,
    high: number,
    href: null,
    hrefLang: null,
    htmlFor: spaceSeparated,
    httpEquiv: spaceSeparated,
    id: null,
    imageSizes: null,
    imageSrcSet: null,
    inert: boolean,
    inputMode: null,
    integrity: null,
    is: null,
    isMap: boolean,
    itemId: null,
    itemProp: spaceSeparated,
    itemRef: spaceSeparated,
    itemScope: boolean,
    itemType: spaceSeparated,
    kind: null,
    label: null,
    lang: null,
    language: null,
    list: null,
    loading: null,
    loop: boolean,
    low: number,
    manifest: null,
    max: null,
    maxLength: number,
    media: null,
    method: null,
    min: null,
    minLength: number,
    multiple: boolean,
    muted: boolean,
    name: null,
    nonce: null,
    noModule: boolean,
    noValidate: boolean,
    onAbort: null,
    onAfterPrint: null,
    onAuxClick: null,
    onBeforeMatch: null,
    onBeforePrint: null,
    onBeforeToggle: null,
    onBeforeUnload: null,
    onBlur: null,
    onCancel: null,
    onCanPlay: null,
    onCanPlayThrough: null,
    onChange: null,
    onClick: null,
    onClose: null,
    onContextLost: null,
    onContextMenu: null,
    onContextRestored: null,
    onCopy: null,
    onCueChange: null,
    onCut: null,
    onDblClick: null,
    onDrag: null,
    onDragEnd: null,
    onDragEnter: null,
    onDragExit: null,
    onDragLeave: null,
    onDragOver: null,
    onDragStart: null,
    onDrop: null,
    onDurationChange: null,
    onEmptied: null,
    onEnded: null,
    onError: null,
    onFocus: null,
    onFormData: null,
    onHashChange: null,
    onInput: null,
    onInvalid: null,
    onKeyDown: null,
    onKeyPress: null,
    onKeyUp: null,
    onLanguageChange: null,
    onLoad: null,
    onLoadedData: null,
    onLoadedMetadata: null,
    onLoadEnd: null,
    onLoadStart: null,
    onMessage: null,
    onMessageError: null,
    onMouseDown: null,
    onMouseEnter: null,
    onMouseLeave: null,
    onMouseMove: null,
    onMouseOut: null,
    onMouseOver: null,
    onMouseUp: null,
    onOffline: null,
    onOnline: null,
    onPageHide: null,
    onPageShow: null,
    onPaste: null,
    onPause: null,
    onPlay: null,
    onPlaying: null,
    onPopState: null,
    onProgress: null,
    onRateChange: null,
    onRejectionHandled: null,
    onReset: null,
    onResize: null,
    onScroll: null,
    onScrollEnd: null,
    onSecurityPolicyViolation: null,
    onSeeked: null,
    onSeeking: null,
    onSelect: null,
    onSlotChange: null,
    onStalled: null,
    onStorage: null,
    onSubmit: null,
    onSuspend: null,
    onTimeUpdate: null,
    onToggle: null,
    onUnhandledRejection: null,
    onUnload: null,
    onVolumeChange: null,
    onWaiting: null,
    onWheel: null,
    open: boolean,
    optimum: number,
    pattern: null,
    ping: spaceSeparated,
    placeholder: null,
    playsInline: boolean,
    popover: null,
    popoverTarget: null,
    popoverTargetAction: null,
    poster: null,
    preload: null,
    readOnly: boolean,
    referrerPolicy: null,
    rel: spaceSeparated,
    required: boolean,
    reversed: boolean,
    rows: number,
    rowSpan: number,
    sandbox: spaceSeparated,
    scope: null,
    scoped: boolean,
    seamless: boolean,
    selected: boolean,
    shadowRootClonable: boolean,
    shadowRootCustomElementRegistry: boolean,
    shadowRootDelegatesFocus: boolean,
    shadowRootMode: null,
    shadowRootSerializable: boolean,
    shape: null,
    size: number,
    sizes: null,
    slot: null,
    span: number,
    spellCheck: booleanish,
    src: null,
    srcDoc: null,
    srcLang: null,
    srcSet: null,
    start: number,
    step: null,
    style: null,
    tabIndex: number,
    target: null,
    title: null,
    translate: null,
    type: null,
    typeMustMatch: boolean,
    useMap: null,
    value: booleanish,
    width: number,
    wrap: null,
    writingSuggestions: null,
    // Legacy.
    // See: https://html.spec.whatwg.org/#other-elements,-attributes-and-apis
    align: null,
    // Several. Use CSS `text-align` instead,
    aLink: null,
    // `<body>`. Use CSS `a:active {color}` instead
    archive: spaceSeparated,
    // `<object>`. List of URIs to archives
    axis: null,
    // `<td>` and `<th>`. Use `scope` on `<th>`
    background: null,
    // `<body>`. Use CSS `background-image` instead
    bgColor: null,
    // `<body>` and table elements. Use CSS `background-color` instead
    border: number,
    // `<table>`. Use CSS `border-width` instead,
    borderColor: null,
    // `<table>`. Use CSS `border-color` instead,
    bottomMargin: number,
    // `<body>`
    cellPadding: null,
    // `<table>`
    cellSpacing: null,
    // `<table>`
    char: null,
    // Several table elements. When `align=char`, sets the character to align on
    charOff: null,
    // Several table elements. When `char`, offsets the alignment
    classId: null,
    // `<object>`
    clear: null,
    // `<br>`. Use CSS `clear` instead
    code: null,
    // `<object>`
    codeBase: null,
    // `<object>`
    codeType: null,
    // `<object>`
    color: null,
    // `<font>` and `<hr>`. Use CSS instead
    compact: boolean,
    // Lists. Use CSS to reduce space between items instead
    declare: boolean,
    // `<object>`
    event: null,
    // `<script>`
    face: null,
    // `<font>`. Use CSS instead
    frame: null,
    // `<table>`
    frameBorder: null,
    // `<iframe>`. Use CSS `border` instead
    hSpace: number,
    // `<img>` and `<object>`
    leftMargin: number,
    // `<body>`
    link: null,
    // `<body>`. Use CSS `a:link {color: *}` instead
    longDesc: null,
    // `<frame>`, `<iframe>`, and `<img>`. Use an `<a>`
    lowSrc: null,
    // `<img>`. Use a `<picture>`
    marginHeight: number,
    // `<body>`
    marginWidth: number,
    // `<body>`
    noResize: boolean,
    // `<frame>`
    noHref: boolean,
    // `<area>`. Use no href instead of an explicit `nohref`
    noShade: boolean,
    // `<hr>`. Use background-color and height instead of borders
    noWrap: boolean,
    // `<td>` and `<th>`
    object: null,
    // `<applet>`
    profile: null,
    // `<head>`
    prompt: null,
    // `<isindex>`
    rev: null,
    // `<link>`
    rightMargin: number,
    // `<body>`
    rules: null,
    // `<table>`
    scheme: null,
    // `<meta>`
    scrolling: booleanish,
    // `<frame>`. Use overflow in the child context
    standby: null,
    // `<object>`
    summary: null,
    // `<table>`
    text: null,
    // `<body>`. Use CSS `color` instead
    topMargin: number,
    // `<body>`
    valueType: null,
    // `<param>`
    version: null,
    // `<html>`. Use a doctype.
    vAlign: null,
    // Several. Use CSS `vertical-align` instead
    vLink: null,
    // `<body>`. Use CSS `a:visited {color}` instead
    vSpace: number,
    // `<img>` and `<object>`
    // Non-standard Properties.
    allowTransparency: null,
    autoCorrect: null,
    autoSave: null,
    credentialless: boolean,
    disablePictureInPicture: boolean,
    disableRemotePlayback: boolean,
    exportParts: commaSeparated,
    part: spaceSeparated,
    prefix: null,
    property: null,
    results: number,
    security: null,
    unselectable: null
  },
  space: "html",
  transform: caseInsensitiveTransform
});
const svg$1 = create({
  attributes: {
    accentHeight: "accent-height",
    alignmentBaseline: "alignment-baseline",
    arabicForm: "arabic-form",
    baselineShift: "baseline-shift",
    capHeight: "cap-height",
    className: "class",
    clipPath: "clip-path",
    clipRule: "clip-rule",
    colorInterpolation: "color-interpolation",
    colorInterpolationFilters: "color-interpolation-filters",
    colorProfile: "color-profile",
    colorRendering: "color-rendering",
    crossOrigin: "crossorigin",
    dataType: "datatype",
    dominantBaseline: "dominant-baseline",
    enableBackground: "enable-background",
    fillOpacity: "fill-opacity",
    fillRule: "fill-rule",
    floodColor: "flood-color",
    floodOpacity: "flood-opacity",
    fontFamily: "font-family",
    fontSize: "font-size",
    fontSizeAdjust: "font-size-adjust",
    fontStretch: "font-stretch",
    fontStyle: "font-style",
    fontVariant: "font-variant",
    fontWeight: "font-weight",
    glyphName: "glyph-name",
    glyphOrientationHorizontal: "glyph-orientation-horizontal",
    glyphOrientationVertical: "glyph-orientation-vertical",
    hrefLang: "hreflang",
    horizAdvX: "horiz-adv-x",
    horizOriginX: "horiz-origin-x",
    horizOriginY: "horiz-origin-y",
    imageRendering: "image-rendering",
    letterSpacing: "letter-spacing",
    lightingColor: "lighting-color",
    markerEnd: "marker-end",
    markerMid: "marker-mid",
    markerStart: "marker-start",
    maskType: "mask-type",
    navDown: "nav-down",
    navDownLeft: "nav-down-left",
    navDownRight: "nav-down-right",
    navLeft: "nav-left",
    navNext: "nav-next",
    navPrev: "nav-prev",
    navRight: "nav-right",
    navUp: "nav-up",
    navUpLeft: "nav-up-left",
    navUpRight: "nav-up-right",
    onAbort: "onabort",
    onActivate: "onactivate",
    onAfterPrint: "onafterprint",
    onBeforePrint: "onbeforeprint",
    onBegin: "onbegin",
    onCancel: "oncancel",
    onCanPlay: "oncanplay",
    onCanPlayThrough: "oncanplaythrough",
    onChange: "onchange",
    onClick: "onclick",
    onClose: "onclose",
    onCopy: "oncopy",
    onCueChange: "oncuechange",
    onCut: "oncut",
    onDblClick: "ondblclick",
    onDrag: "ondrag",
    onDragEnd: "ondragend",
    onDragEnter: "ondragenter",
    onDragExit: "ondragexit",
    onDragLeave: "ondragleave",
    onDragOver: "ondragover",
    onDragStart: "ondragstart",
    onDrop: "ondrop",
    onDurationChange: "ondurationchange",
    onEmptied: "onemptied",
    onEnd: "onend",
    onEnded: "onended",
    onError: "onerror",
    onFocus: "onfocus",
    onFocusIn: "onfocusin",
    onFocusOut: "onfocusout",
    onHashChange: "onhashchange",
    onInput: "oninput",
    onInvalid: "oninvalid",
    onKeyDown: "onkeydown",
    onKeyPress: "onkeypress",
    onKeyUp: "onkeyup",
    onLoad: "onload",
    onLoadedData: "onloadeddata",
    onLoadedMetadata: "onloadedmetadata",
    onLoadStart: "onloadstart",
    onMessage: "onmessage",
    onMouseDown: "onmousedown",
    onMouseEnter: "onmouseenter",
    onMouseLeave: "onmouseleave",
    onMouseMove: "onmousemove",
    onMouseOut: "onmouseout",
    onMouseOver: "onmouseover",
    onMouseUp: "onmouseup",
    onMouseWheel: "onmousewheel",
    onOffline: "onoffline",
    onOnline: "ononline",
    onPageHide: "onpagehide",
    onPageShow: "onpageshow",
    onPaste: "onpaste",
    onPause: "onpause",
    onPlay: "onplay",
    onPlaying: "onplaying",
    onPopState: "onpopstate",
    onProgress: "onprogress",
    onRateChange: "onratechange",
    onRepeat: "onrepeat",
    onReset: "onreset",
    onResize: "onresize",
    onScroll: "onscroll",
    onSeeked: "onseeked",
    onSeeking: "onseeking",
    onSelect: "onselect",
    onShow: "onshow",
    onStalled: "onstalled",
    onStorage: "onstorage",
    onSubmit: "onsubmit",
    onSuspend: "onsuspend",
    onTimeUpdate: "ontimeupdate",
    onToggle: "ontoggle",
    onUnload: "onunload",
    onVolumeChange: "onvolumechange",
    onWaiting: "onwaiting",
    onZoom: "onzoom",
    overlinePosition: "overline-position",
    overlineThickness: "overline-thickness",
    paintOrder: "paint-order",
    panose1: "panose-1",
    pointerEvents: "pointer-events",
    referrerPolicy: "referrerpolicy",
    renderingIntent: "rendering-intent",
    shapeRendering: "shape-rendering",
    stopColor: "stop-color",
    stopOpacity: "stop-opacity",
    strikethroughPosition: "strikethrough-position",
    strikethroughThickness: "strikethrough-thickness",
    strokeDashArray: "stroke-dasharray",
    strokeDashOffset: "stroke-dashoffset",
    strokeLineCap: "stroke-linecap",
    strokeLineJoin: "stroke-linejoin",
    strokeMiterLimit: "stroke-miterlimit",
    strokeOpacity: "stroke-opacity",
    strokeWidth: "stroke-width",
    tabIndex: "tabindex",
    textAnchor: "text-anchor",
    textDecoration: "text-decoration",
    textRendering: "text-rendering",
    transformOrigin: "transform-origin",
    typeOf: "typeof",
    underlinePosition: "underline-position",
    underlineThickness: "underline-thickness",
    unicodeBidi: "unicode-bidi",
    unicodeRange: "unicode-range",
    unitsPerEm: "units-per-em",
    vAlphabetic: "v-alphabetic",
    vHanging: "v-hanging",
    vIdeographic: "v-ideographic",
    vMathematical: "v-mathematical",
    vectorEffect: "vector-effect",
    vertAdvY: "vert-adv-y",
    vertOriginX: "vert-origin-x",
    vertOriginY: "vert-origin-y",
    wordSpacing: "word-spacing",
    writingMode: "writing-mode",
    xHeight: "x-height",
    // These were camelcased in Tiny. Now lowercased in SVG 2
    playbackOrder: "playbackorder",
    timelineBegin: "timelinebegin"
  },
  properties: {
    about: commaOrSpaceSeparated,
    accentHeight: number,
    accumulate: null,
    additive: null,
    alignmentBaseline: null,
    alphabetic: number,
    amplitude: number,
    arabicForm: null,
    ascent: number,
    attributeName: null,
    attributeType: null,
    azimuth: number,
    bandwidth: null,
    baselineShift: null,
    baseFrequency: null,
    baseProfile: null,
    bbox: null,
    begin: null,
    bias: number,
    by: null,
    calcMode: null,
    capHeight: number,
    className: spaceSeparated,
    clip: null,
    clipPath: null,
    clipPathUnits: null,
    clipRule: null,
    color: null,
    colorInterpolation: null,
    colorInterpolationFilters: null,
    colorProfile: null,
    colorRendering: null,
    content: null,
    contentScriptType: null,
    contentStyleType: null,
    crossOrigin: null,
    cursor: null,
    cx: null,
    cy: null,
    d: null,
    dataType: null,
    defaultAction: null,
    descent: number,
    diffuseConstant: number,
    direction: null,
    display: null,
    dur: null,
    divisor: number,
    dominantBaseline: null,
    download: boolean,
    dx: null,
    dy: null,
    edgeMode: null,
    editable: null,
    elevation: number,
    enableBackground: null,
    end: null,
    event: null,
    exponent: number,
    externalResourcesRequired: null,
    fill: null,
    fillOpacity: number,
    fillRule: null,
    filter: null,
    filterRes: null,
    filterUnits: null,
    floodColor: null,
    floodOpacity: null,
    focusable: null,
    focusHighlight: null,
    fontFamily: null,
    fontSize: null,
    fontSizeAdjust: null,
    fontStretch: null,
    fontStyle: null,
    fontVariant: null,
    fontWeight: null,
    format: null,
    fr: null,
    from: null,
    fx: null,
    fy: null,
    g1: commaSeparated,
    g2: commaSeparated,
    glyphName: commaSeparated,
    glyphOrientationHorizontal: null,
    glyphOrientationVertical: null,
    glyphRef: null,
    gradientTransform: null,
    gradientUnits: null,
    handler: null,
    hanging: number,
    hatchContentUnits: null,
    hatchUnits: null,
    height: null,
    href: null,
    hrefLang: null,
    horizAdvX: number,
    horizOriginX: number,
    horizOriginY: number,
    id: null,
    ideographic: number,
    imageRendering: null,
    initialVisibility: null,
    in: null,
    in2: null,
    intercept: number,
    k: number,
    k1: number,
    k2: number,
    k3: number,
    k4: number,
    kernelMatrix: commaOrSpaceSeparated,
    kernelUnitLength: null,
    keyPoints: null,
    // SEMI_COLON_SEPARATED
    keySplines: null,
    // SEMI_COLON_SEPARATED
    keyTimes: null,
    // SEMI_COLON_SEPARATED
    kerning: null,
    lang: null,
    lengthAdjust: null,
    letterSpacing: null,
    lightingColor: null,
    limitingConeAngle: number,
    local: null,
    markerEnd: null,
    markerMid: null,
    markerStart: null,
    markerHeight: null,
    markerUnits: null,
    markerWidth: null,
    mask: null,
    maskContentUnits: null,
    maskType: null,
    maskUnits: null,
    mathematical: null,
    max: null,
    media: null,
    mediaCharacterEncoding: null,
    mediaContentEncodings: null,
    mediaSize: number,
    mediaTime: null,
    method: null,
    min: null,
    mode: null,
    name: null,
    navDown: null,
    navDownLeft: null,
    navDownRight: null,
    navLeft: null,
    navNext: null,
    navPrev: null,
    navRight: null,
    navUp: null,
    navUpLeft: null,
    navUpRight: null,
    numOctaves: null,
    observer: null,
    offset: null,
    onAbort: null,
    onActivate: null,
    onAfterPrint: null,
    onBeforePrint: null,
    onBegin: null,
    onCancel: null,
    onCanPlay: null,
    onCanPlayThrough: null,
    onChange: null,
    onClick: null,
    onClose: null,
    onCopy: null,
    onCueChange: null,
    onCut: null,
    onDblClick: null,
    onDrag: null,
    onDragEnd: null,
    onDragEnter: null,
    onDragExit: null,
    onDragLeave: null,
    onDragOver: null,
    onDragStart: null,
    onDrop: null,
    onDurationChange: null,
    onEmptied: null,
    onEnd: null,
    onEnded: null,
    onError: null,
    onFocus: null,
    onFocusIn: null,
    onFocusOut: null,
    onHashChange: null,
    onInput: null,
    onInvalid: null,
    onKeyDown: null,
    onKeyPress: null,
    onKeyUp: null,
    onLoad: null,
    onLoadedData: null,
    onLoadedMetadata: null,
    onLoadStart: null,
    onMessage: null,
    onMouseDown: null,
    onMouseEnter: null,
    onMouseLeave: null,
    onMouseMove: null,
    onMouseOut: null,
    onMouseOver: null,
    onMouseUp: null,
    onMouseWheel: null,
    onOffline: null,
    onOnline: null,
    onPageHide: null,
    onPageShow: null,
    onPaste: null,
    onPause: null,
    onPlay: null,
    onPlaying: null,
    onPopState: null,
    onProgress: null,
    onRateChange: null,
    onRepeat: null,
    onReset: null,
    onResize: null,
    onScroll: null,
    onSeeked: null,
    onSeeking: null,
    onSelect: null,
    onShow: null,
    onStalled: null,
    onStorage: null,
    onSubmit: null,
    onSuspend: null,
    onTimeUpdate: null,
    onToggle: null,
    onUnload: null,
    onVolumeChange: null,
    onWaiting: null,
    onZoom: null,
    opacity: null,
    operator: null,
    order: null,
    orient: null,
    orientation: null,
    origin: null,
    overflow: null,
    overlay: null,
    overlinePosition: number,
    overlineThickness: number,
    paintOrder: null,
    panose1: null,
    path: null,
    pathLength: number,
    patternContentUnits: null,
    patternTransform: null,
    patternUnits: null,
    phase: null,
    ping: spaceSeparated,
    pitch: null,
    playbackOrder: null,
    pointerEvents: null,
    points: null,
    pointsAtX: number,
    pointsAtY: number,
    pointsAtZ: number,
    preserveAlpha: null,
    preserveAspectRatio: null,
    primitiveUnits: null,
    propagate: null,
    property: commaOrSpaceSeparated,
    r: null,
    radius: null,
    referrerPolicy: null,
    refX: null,
    refY: null,
    rel: commaOrSpaceSeparated,
    rev: commaOrSpaceSeparated,
    renderingIntent: null,
    repeatCount: null,
    repeatDur: null,
    requiredExtensions: commaOrSpaceSeparated,
    requiredFeatures: commaOrSpaceSeparated,
    requiredFonts: commaOrSpaceSeparated,
    requiredFormats: commaOrSpaceSeparated,
    resource: null,
    restart: null,
    result: null,
    rotate: null,
    rx: null,
    ry: null,
    scale: null,
    seed: null,
    shapeRendering: null,
    side: null,
    slope: null,
    snapshotTime: null,
    specularConstant: number,
    specularExponent: number,
    spreadMethod: null,
    spacing: null,
    startOffset: null,
    stdDeviation: null,
    stemh: null,
    stemv: null,
    stitchTiles: null,
    stopColor: null,
    stopOpacity: null,
    strikethroughPosition: number,
    strikethroughThickness: number,
    string: null,
    stroke: null,
    strokeDashArray: commaOrSpaceSeparated,
    strokeDashOffset: null,
    strokeLineCap: null,
    strokeLineJoin: null,
    strokeMiterLimit: number,
    strokeOpacity: number,
    strokeWidth: null,
    style: null,
    surfaceScale: number,
    syncBehavior: null,
    syncBehaviorDefault: null,
    syncMaster: null,
    syncTolerance: null,
    syncToleranceDefault: null,
    systemLanguage: commaOrSpaceSeparated,
    tabIndex: number,
    tableValues: null,
    target: null,
    targetX: number,
    targetY: number,
    textAnchor: null,
    textDecoration: null,
    textRendering: null,
    textLength: null,
    timelineBegin: null,
    title: null,
    transformBehavior: null,
    type: null,
    typeOf: commaOrSpaceSeparated,
    to: null,
    transform: null,
    transformOrigin: null,
    u1: null,
    u2: null,
    underlinePosition: number,
    underlineThickness: number,
    unicode: null,
    unicodeBidi: null,
    unicodeRange: null,
    unitsPerEm: number,
    values: null,
    vAlphabetic: number,
    vMathematical: number,
    vectorEffect: null,
    vHanging: number,
    vIdeographic: number,
    version: null,
    vertAdvY: number,
    vertOriginX: number,
    vertOriginY: number,
    viewBox: null,
    viewTarget: null,
    visibility: null,
    width: null,
    widths: null,
    wordSpacing: null,
    writingMode: null,
    x: null,
    x1: null,
    x2: null,
    xChannelSelector: null,
    xHeight: number,
    y: null,
    y1: null,
    y2: null,
    yChannelSelector: null,
    z: null,
    zoomAndPan: null
  },
  space: "svg",
  transform: caseSensitiveTransform
});
const xlink = create({
  properties: {
    xLinkActuate: null,
    xLinkArcRole: null,
    xLinkHref: null,
    xLinkRole: null,
    xLinkShow: null,
    xLinkTitle: null,
    xLinkType: null
  },
  space: "xlink",
  transform(_, property) {
    return "xlink:" + property.slice(5).toLowerCase();
  }
});
const xmlns = create({
  attributes: { xmlnsxlink: "xmlns:xlink" },
  properties: { xmlnsXLink: null, xmlns: null },
  space: "xmlns",
  transform: caseInsensitiveTransform
});
const xml = create({
  properties: { xmlBase: null, xmlLang: null, xmlSpace: null },
  space: "xml",
  transform(_, property) {
    return "xml:" + property.slice(3).toLowerCase();
  }
});
const hastToReact = {
  classId: "classID",
  dataType: "datatype",
  itemId: "itemID",
  strokeDashArray: "strokeDasharray",
  strokeDashOffset: "strokeDashoffset",
  strokeLineCap: "strokeLinecap",
  strokeLineJoin: "strokeLinejoin",
  strokeMiterLimit: "strokeMiterlimit",
  typeOf: "typeof",
  xLinkActuate: "xlinkActuate",
  xLinkArcRole: "xlinkArcrole",
  xLinkHref: "xlinkHref",
  xLinkRole: "xlinkRole",
  xLinkShow: "xlinkShow",
  xLinkTitle: "xlinkTitle",
  xLinkType: "xlinkType",
  xmlnsXLink: "xmlnsXlink"
};
const cap$1 = /[A-Z]/g;
const dash = /-[a-z]/g;
const valid = /^data[-\w.:]+$/i;
function find(schema, value) {
  const normal = normalize$1(value);
  let property = value;
  let Type = Info;
  if (normal in schema.normal) {
    return schema.property[schema.normal[normal]];
  }
  if (normal.length > 4 && normal.slice(0, 4) === "data" && valid.test(value)) {
    if (value.charAt(4) === "-") {
      const rest = value.slice(5).replace(dash, camelcase);
      property = "data" + rest.charAt(0).toUpperCase() + rest.slice(1);
    } else {
      const rest = value.slice(4);
      if (!dash.test(rest)) {
        let dashes = rest.replace(cap$1, kebab);
        if (dashes.charAt(0) !== "-") {
          dashes = "-" + dashes;
        }
        value = "data" + dashes;
      }
    }
    Type = DefinedInfo;
  }
  return new Type(property, value);
}
function kebab($0) {
  return "-" + $0.toLowerCase();
}
function camelcase($0) {
  return $0.charAt(1).toUpperCase();
}
const html$2 = merge([aria, html$3, xlink, xmlns, xml], "html");
const svg = merge([aria, svg$1, xlink, xmlns, xml], "svg");
function stringify(values) {
  return values.join(" ").trim();
}
var cjs$2 = {};
var cjs$1;
var hasRequiredCjs$2;
function requireCjs$2() {
  if (hasRequiredCjs$2) return cjs$1;
  hasRequiredCjs$2 = 1;
  var COMMENT_REGEX = /\/\*[^*]*\*+([^/*][^*]*\*+)*\//g;
  var NEWLINE_REGEX = /\n/g;
  var WHITESPACE_REGEX = /^\s*/;
  var PROPERTY_REGEX = /^(\*?[-#/*\\\w]+(\[[0-9a-z_-]+\])?)\s*/;
  var COLON_REGEX = /^:\s*/;
  var VALUE_REGEX = /^((?:'(?:\\'|.)*?'|"(?:\\"|.)*?"|\([^)]*?\)|[^};])+)/;
  var SEMICOLON_REGEX = /^[;\s]*/;
  var TRIM_REGEX = /^\s+|\s+$/g;
  var NEWLINE = "\n";
  var FORWARD_SLASH = "/";
  var ASTERISK = "*";
  var EMPTY_STRING = "";
  var TYPE_COMMENT = "comment";
  var TYPE_DECLARATION = "declaration";
  function index2(style, options) {
    if (typeof style !== "string") {
      throw new TypeError("First argument must be a string");
    }
    if (!style) return [];
    options = options || {};
    var lineno = 1;
    var column = 1;
    function updatePosition(str) {
      var lines = str.match(NEWLINE_REGEX);
      if (lines) lineno += lines.length;
      var i = str.lastIndexOf(NEWLINE);
      column = ~i ? str.length - i : column + str.length;
    }
    function position2() {
      var start = { line: lineno, column };
      return function(node2) {
        node2.position = new Position(start);
        whitespace2();
        return node2;
      };
    }
    function Position(start) {
      this.start = start;
      this.end = { line: lineno, column };
      this.source = options.source;
    }
    Position.prototype.content = style;
    function error(msg) {
      var err = new Error(
        options.source + ":" + lineno + ":" + column + ": " + msg
      );
      err.reason = msg;
      err.filename = options.source;
      err.line = lineno;
      err.column = column;
      err.source = style;
      if (options.silent) ;
      else {
        throw err;
      }
    }
    function match(re2) {
      var m = re2.exec(style);
      if (!m) return;
      var str = m[0];
      updatePosition(str);
      style = style.slice(str.length);
      return m;
    }
    function whitespace2() {
      match(WHITESPACE_REGEX);
    }
    function comments(rules) {
      var c;
      rules = rules || [];
      while (c = comment()) {
        if (c !== false) {
          rules.push(c);
        }
      }
      return rules;
    }
    function comment() {
      var pos = position2();
      if (FORWARD_SLASH != style.charAt(0) || ASTERISK != style.charAt(1)) return;
      var i = 2;
      while (EMPTY_STRING != style.charAt(i) && (ASTERISK != style.charAt(i) || FORWARD_SLASH != style.charAt(i + 1))) {
        ++i;
      }
      i += 2;
      if (EMPTY_STRING === style.charAt(i - 1)) {
        return error("End of comment missing");
      }
      var str = style.slice(2, i - 2);
      column += 2;
      updatePosition(str);
      style = style.slice(i);
      column += 2;
      return pos({
        type: TYPE_COMMENT,
        comment: str
      });
    }
    function declaration() {
      var pos = position2();
      var prop = match(PROPERTY_REGEX);
      if (!prop) return;
      comment();
      if (!match(COLON_REGEX)) return error("property missing ':'");
      var val = match(VALUE_REGEX);
      var ret = pos({
        type: TYPE_DECLARATION,
        property: trim(prop[0].replace(COMMENT_REGEX, EMPTY_STRING)),
        value: val ? trim(val[0].replace(COMMENT_REGEX, EMPTY_STRING)) : EMPTY_STRING
      });
      match(SEMICOLON_REGEX);
      return ret;
    }
    function declarations() {
      var decls = [];
      comments(decls);
      var decl;
      while (decl = declaration()) {
        if (decl !== false) {
          decls.push(decl);
          comments(decls);
        }
      }
      return decls;
    }
    whitespace2();
    return declarations();
  }
  function trim(str) {
    return str ? str.replace(TRIM_REGEX, EMPTY_STRING) : EMPTY_STRING;
  }
  cjs$1 = index2;
  return cjs$1;
}
var hasRequiredCjs$1;
function requireCjs$1() {
  if (hasRequiredCjs$1) return cjs$2;
  hasRequiredCjs$1 = 1;
  var __importDefault = cjs$2 && cjs$2.__importDefault || function(mod) {
    return mod && mod.__esModule ? mod : { "default": mod };
  };
  Object.defineProperty(cjs$2, "__esModule", { value: true });
  cjs$2.default = StyleToObject;
  const inline_style_parser_1 = __importDefault(requireCjs$2());
  function StyleToObject(style, iterator) {
    let styleObject = null;
    if (!style || typeof style !== "string") {
      return styleObject;
    }
    const declarations = (0, inline_style_parser_1.default)(style);
    const hasIterator = typeof iterator === "function";
    declarations.forEach((declaration) => {
      if (declaration.type !== "declaration") {
        return;
      }
      const { property, value } = declaration;
      if (hasIterator) {
        iterator(property, value, declaration);
      } else if (value) {
        styleObject = styleObject || {};
        styleObject[property] = value;
      }
    });
    return styleObject;
  }
  return cjs$2;
}
var utilities = {};
var hasRequiredUtilities;
function requireUtilities() {
  if (hasRequiredUtilities) return utilities;
  hasRequiredUtilities = 1;
  Object.defineProperty(utilities, "__esModule", { value: true });
  utilities.camelCase = void 0;
  var CUSTOM_PROPERTY_REGEX = /^--[a-zA-Z0-9_-]+$/;
  var HYPHEN_REGEX = /-([a-z])/g;
  var NO_HYPHEN_REGEX = /^[^-]+$/;
  var VENDOR_PREFIX_REGEX = /^-(webkit|moz|ms|o|khtml)-/;
  var MS_VENDOR_PREFIX_REGEX = /^-(ms)-/;
  var skipCamelCase = function(property) {
    return !property || NO_HYPHEN_REGEX.test(property) || CUSTOM_PROPERTY_REGEX.test(property);
  };
  var capitalize = function(match, character) {
    return character.toUpperCase();
  };
  var trimHyphen = function(match, prefix) {
    return "".concat(prefix, "-");
  };
  var camelCase = function(property, options) {
    if (options === void 0) {
      options = {};
    }
    if (skipCamelCase(property)) {
      return property;
    }
    property = property.toLowerCase();
    if (options.reactCompat) {
      property = property.replace(MS_VENDOR_PREFIX_REGEX, trimHyphen);
    } else {
      property = property.replace(VENDOR_PREFIX_REGEX, trimHyphen);
    }
    return property.replace(HYPHEN_REGEX, capitalize);
  };
  utilities.camelCase = camelCase;
  return utilities;
}
var cjs;
var hasRequiredCjs;
function requireCjs() {
  if (hasRequiredCjs) return cjs;
  hasRequiredCjs = 1;
  var __importDefault = cjs && cjs.__importDefault || function(mod) {
    return mod && mod.__esModule ? mod : { "default": mod };
  };
  var style_to_object_1 = __importDefault(requireCjs$1());
  var utilities_1 = requireUtilities();
  function StyleToJS(style, options) {
    var output = {};
    if (!style || typeof style !== "string") {
      return output;
    }
    (0, style_to_object_1.default)(style, function(property, value) {
      if (property && value) {
        output[(0, utilities_1.camelCase)(property, options)] = value;
      }
    });
    return output;
  }
  StyleToJS.default = StyleToJS;
  cjs = StyleToJS;
  return cjs;
}
var cjsExports = requireCjs();
const styleToJs = /* @__PURE__ */ getDefaultExportFromCjs(cjsExports);
const pointEnd = point$2("end");
const pointStart = point$2("start");
function point$2(type) {
  return point2;
  function point2(node2) {
    const point3 = node2 && node2.position && node2.position[type] || {};
    if (typeof point3.line === "number" && point3.line > 0 && typeof point3.column === "number" && point3.column > 0) {
      return {
        line: point3.line,
        column: point3.column,
        offset: typeof point3.offset === "number" && point3.offset > -1 ? point3.offset : void 0
      };
    }
  }
}
function position$1(node2) {
  const start = pointStart(node2);
  const end = pointEnd(node2);
  if (start && end) {
    return { start, end };
  }
}
function stringifyPosition(value) {
  if (!value || typeof value !== "object") {
    return "";
  }
  if ("position" in value || "type" in value) {
    return position(value.position);
  }
  if ("start" in value || "end" in value) {
    return position(value);
  }
  if ("line" in value || "column" in value) {
    return point$1(value);
  }
  return "";
}
function point$1(point2) {
  return index(point2 && point2.line) + ":" + index(point2 && point2.column);
}
function position(pos) {
  return point$1(pos && pos.start) + "-" + point$1(pos && pos.end);
}
function index(value) {
  return value && typeof value === "number" ? value : 1;
}
class VFileMessage extends Error {
  /**
   * Create a message for `reason`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {Options | null | undefined} [options]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | Options | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns
   *   Instance of `VFileMessage`.
   */
  // eslint-disable-next-line complexity
  constructor(causeOrReason, optionsOrParentOrPlace, origin) {
    super();
    if (typeof optionsOrParentOrPlace === "string") {
      origin = optionsOrParentOrPlace;
      optionsOrParentOrPlace = void 0;
    }
    let reason = "";
    let options = {};
    let legacyCause = false;
    if (optionsOrParentOrPlace) {
      if ("line" in optionsOrParentOrPlace && "column" in optionsOrParentOrPlace) {
        options = { place: optionsOrParentOrPlace };
      } else if ("start" in optionsOrParentOrPlace && "end" in optionsOrParentOrPlace) {
        options = { place: optionsOrParentOrPlace };
      } else if ("type" in optionsOrParentOrPlace) {
        options = {
          ancestors: [optionsOrParentOrPlace],
          place: optionsOrParentOrPlace.position
        };
      } else {
        options = { ...optionsOrParentOrPlace };
      }
    }
    if (typeof causeOrReason === "string") {
      reason = causeOrReason;
    } else if (!options.cause && causeOrReason) {
      legacyCause = true;
      reason = causeOrReason.message;
      options.cause = causeOrReason;
    }
    if (!options.ruleId && !options.source && typeof origin === "string") {
      const index2 = origin.indexOf(":");
      if (index2 === -1) {
        options.ruleId = origin;
      } else {
        options.source = origin.slice(0, index2);
        options.ruleId = origin.slice(index2 + 1);
      }
    }
    if (!options.place && options.ancestors && options.ancestors) {
      const parent = options.ancestors[options.ancestors.length - 1];
      if (parent) {
        options.place = parent.position;
      }
    }
    const start = options.place && "start" in options.place ? options.place.start : options.place;
    this.ancestors = options.ancestors || void 0;
    this.cause = options.cause || void 0;
    this.column = start ? start.column : void 0;
    this.fatal = void 0;
    this.file = "";
    this.message = reason;
    this.line = start ? start.line : void 0;
    this.name = stringifyPosition(options.place) || "1:1";
    this.place = options.place || void 0;
    this.reason = this.message;
    this.ruleId = options.ruleId || void 0;
    this.source = options.source || void 0;
    this.stack = legacyCause && options.cause && typeof options.cause.stack === "string" ? options.cause.stack : "";
    this.actual = void 0;
    this.expected = void 0;
    this.note = void 0;
    this.url = void 0;
  }
}
VFileMessage.prototype.file = "";
VFileMessage.prototype.name = "";
VFileMessage.prototype.reason = "";
VFileMessage.prototype.message = "";
VFileMessage.prototype.stack = "";
VFileMessage.prototype.column = void 0;
VFileMessage.prototype.line = void 0;
VFileMessage.prototype.ancestors = void 0;
VFileMessage.prototype.cause = void 0;
VFileMessage.prototype.fatal = void 0;
VFileMessage.prototype.place = void 0;
VFileMessage.prototype.ruleId = void 0;
VFileMessage.prototype.source = void 0;
const own$3 = {}.hasOwnProperty;
const emptyMap = /* @__PURE__ */ new Map();
const cap = /[A-Z]/g;
const tableElements = /* @__PURE__ */ new Set(["table", "tbody", "thead", "tfoot", "tr"]);
const tableCellElement = /* @__PURE__ */ new Set(["td", "th"]);
const docs = "https://github.com/syntax-tree/hast-util-to-jsx-runtime";
function toJsxRuntime(tree, options) {
  if (!options || options.Fragment === void 0) {
    throw new TypeError("Expected `Fragment` in options");
  }
  const filePath = options.filePath || void 0;
  let create2;
  if (options.development) {
    if (typeof options.jsxDEV !== "function") {
      throw new TypeError(
        "Expected `jsxDEV` in options when `development: true`"
      );
    }
    create2 = developmentCreate(filePath, options.jsxDEV);
  } else {
    if (typeof options.jsx !== "function") {
      throw new TypeError("Expected `jsx` in production options");
    }
    if (typeof options.jsxs !== "function") {
      throw new TypeError("Expected `jsxs` in production options");
    }
    create2 = productionCreate(filePath, options.jsx, options.jsxs);
  }
  const state = {
    Fragment: options.Fragment,
    ancestors: [],
    components: options.components || {},
    create: create2,
    elementAttributeNameCase: options.elementAttributeNameCase || "react",
    evaluater: options.createEvaluater ? options.createEvaluater() : void 0,
    filePath,
    ignoreInvalidStyle: options.ignoreInvalidStyle || false,
    passKeys: options.passKeys !== false,
    passNode: options.passNode || false,
    schema: options.space === "svg" ? svg : html$2,
    stylePropertyNameCase: options.stylePropertyNameCase || "dom",
    tableCellAlignToStyle: options.tableCellAlignToStyle !== false
  };
  const result = one$1(state, tree, void 0);
  if (result && typeof result !== "string") {
    return result;
  }
  return state.create(
    tree,
    state.Fragment,
    { children: result || void 0 },
    void 0
  );
}
function one$1(state, node2, key) {
  if (node2.type === "element") {
    return element$1(state, node2, key);
  }
  if (node2.type === "mdxFlowExpression" || node2.type === "mdxTextExpression") {
    return mdxExpression(state, node2);
  }
  if (node2.type === "mdxJsxFlowElement" || node2.type === "mdxJsxTextElement") {
    return mdxJsxElement(state, node2, key);
  }
  if (node2.type === "mdxjsEsm") {
    return mdxEsm(state, node2);
  }
  if (node2.type === "root") {
    return root$2(state, node2, key);
  }
  if (node2.type === "text") {
    return text$5(state, node2);
  }
}
function element$1(state, node2, key) {
  const parentSchema = state.schema;
  let schema = parentSchema;
  if (node2.tagName.toLowerCase() === "svg" && parentSchema.space === "html") {
    schema = svg;
    state.schema = schema;
  }
  state.ancestors.push(node2);
  const type = findComponentFromName(state, node2.tagName, false);
  const props = createElementProps(state, node2);
  let children = createChildren(state, node2);
  if (tableElements.has(node2.tagName)) {
    children = children.filter(function(child) {
      return typeof child === "string" ? !whitespace(child) : true;
    });
  }
  addNode(state, props, type, node2);
  addChildren(props, children);
  state.ancestors.pop();
  state.schema = parentSchema;
  return state.create(node2, type, props, key);
}
function mdxExpression(state, node2) {
  if (node2.data && node2.data.estree && state.evaluater) {
    const program = node2.data.estree;
    const expression = program.body[0];
    ok$1(expression.type === "ExpressionStatement");
    return (
      /** @type {Child | undefined} */
      state.evaluater.evaluateExpression(expression.expression)
    );
  }
  crashEstree(state, node2.position);
}
function mdxEsm(state, node2) {
  if (node2.data && node2.data.estree && state.evaluater) {
    return (
      /** @type {Child | undefined} */
      state.evaluater.evaluateProgram(node2.data.estree)
    );
  }
  crashEstree(state, node2.position);
}
function mdxJsxElement(state, node2, key) {
  const parentSchema = state.schema;
  let schema = parentSchema;
  if (node2.name === "svg" && parentSchema.space === "html") {
    schema = svg;
    state.schema = schema;
  }
  state.ancestors.push(node2);
  const type = node2.name === null ? state.Fragment : findComponentFromName(state, node2.name, true);
  const props = createJsxElementProps(state, node2);
  const children = createChildren(state, node2);
  addNode(state, props, type, node2);
  addChildren(props, children);
  state.ancestors.pop();
  state.schema = parentSchema;
  return state.create(node2, type, props, key);
}
function root$2(state, node2, key) {
  const props = {};
  addChildren(props, createChildren(state, node2));
  return state.create(node2, state.Fragment, props, key);
}
function text$5(_, node2) {
  return node2.value;
}
function addNode(state, props, type, node2) {
  if (typeof type !== "string" && type !== state.Fragment && state.passNode) {
    props.node = node2;
  }
}
function addChildren(props, children) {
  if (children.length > 0) {
    const value = children.length > 1 ? children : children[0];
    if (value) {
      props.children = value;
    }
  }
}
function productionCreate(_, jsx, jsxs) {
  return create2;
  function create2(_2, type, props, key) {
    const isStaticChildren = Array.isArray(props.children);
    const fn = isStaticChildren ? jsxs : jsx;
    return key ? fn(type, props, key) : fn(type, props);
  }
}
function developmentCreate(filePath, jsxDEV) {
  return create2;
  function create2(node2, type, props, key) {
    const isStaticChildren = Array.isArray(props.children);
    const point2 = pointStart(node2);
    return jsxDEV(
      type,
      props,
      key,
      isStaticChildren,
      {
        columnNumber: point2 ? point2.column - 1 : void 0,
        fileName: filePath,
        lineNumber: point2 ? point2.line : void 0
      },
      void 0
    );
  }
}
function createElementProps(state, node2) {
  const props = {};
  let alignValue;
  let prop;
  for (prop in node2.properties) {
    if (prop !== "children" && own$3.call(node2.properties, prop)) {
      const result = createProperty(state, prop, node2.properties[prop]);
      if (result) {
        const [key, value] = result;
        if (state.tableCellAlignToStyle && key === "align" && typeof value === "string" && tableCellElement.has(node2.tagName)) {
          alignValue = value;
        } else {
          props[key] = value;
        }
      }
    }
  }
  if (alignValue) {
    const style = (
      /** @type {Style} */
      props.style || (props.style = {})
    );
    style[state.stylePropertyNameCase === "css" ? "text-align" : "textAlign"] = alignValue;
  }
  return props;
}
function createJsxElementProps(state, node2) {
  const props = {};
  for (const attribute of node2.attributes) {
    if (attribute.type === "mdxJsxExpressionAttribute") {
      if (attribute.data && attribute.data.estree && state.evaluater) {
        const program = attribute.data.estree;
        const expression = program.body[0];
        ok$1(expression.type === "ExpressionStatement");
        const objectExpression = expression.expression;
        ok$1(objectExpression.type === "ObjectExpression");
        const property = objectExpression.properties[0];
        ok$1(property.type === "SpreadElement");
        Object.assign(
          props,
          state.evaluater.evaluateExpression(property.argument)
        );
      } else {
        crashEstree(state, node2.position);
      }
    } else {
      const name2 = attribute.name;
      let value;
      if (attribute.value && typeof attribute.value === "object") {
        if (attribute.value.data && attribute.value.data.estree && state.evaluater) {
          const program = attribute.value.data.estree;
          const expression = program.body[0];
          ok$1(expression.type === "ExpressionStatement");
          value = state.evaluater.evaluateExpression(expression.expression);
        } else {
          crashEstree(state, node2.position);
        }
      } else {
        value = attribute.value === null ? true : attribute.value;
      }
      props[name2] = /** @type {Props[keyof Props]} */
      value;
    }
  }
  return props;
}
function createChildren(state, node2) {
  const children = [];
  let index2 = -1;
  const countsByName = state.passKeys ? /* @__PURE__ */ new Map() : emptyMap;
  while (++index2 < node2.children.length) {
    const child = node2.children[index2];
    let key;
    if (state.passKeys) {
      const name2 = child.type === "element" ? child.tagName : child.type === "mdxJsxFlowElement" || child.type === "mdxJsxTextElement" ? child.name : void 0;
      if (name2) {
        const count = countsByName.get(name2) || 0;
        key = name2 + "-" + count;
        countsByName.set(name2, count + 1);
      }
    }
    const result = one$1(state, child, key);
    if (result !== void 0) children.push(result);
  }
  return children;
}
function createProperty(state, prop, value) {
  const info = find(state.schema, prop);
  if (value === null || value === void 0 || typeof value === "number" && Number.isNaN(value)) {
    return;
  }
  if (Array.isArray(value)) {
    value = info.commaSeparated ? stringify$1(value) : stringify(value);
  }
  if (info.property === "style") {
    let styleObject = typeof value === "object" ? value : parseStyle(state, String(value));
    if (state.stylePropertyNameCase === "css") {
      styleObject = transformStylesToCssCasing(styleObject);
    }
    return ["style", styleObject];
  }
  return [
    state.elementAttributeNameCase === "react" && info.space ? hastToReact[info.property] || info.property : info.attribute,
    value
  ];
}
function parseStyle(state, value) {
  try {
    return styleToJs(value, { reactCompat: true });
  } catch (error) {
    if (state.ignoreInvalidStyle) {
      return {};
    }
    const cause = (
      /** @type {Error} */
      error
    );
    const message = new VFileMessage("Cannot parse `style` attribute", {
      ancestors: state.ancestors,
      cause,
      ruleId: "style",
      source: "hast-util-to-jsx-runtime"
    });
    message.file = state.filePath || void 0;
    message.url = docs + "#cannot-parse-style-attribute";
    throw message;
  }
}
function findComponentFromName(state, name2, allowExpression) {
  let result;
  if (!allowExpression) {
    result = { type: "Literal", value: name2 };
  } else if (name2.includes(".")) {
    const identifiers = name2.split(".");
    let index2 = -1;
    let node2;
    while (++index2 < identifiers.length) {
      const prop = name$2(identifiers[index2]) ? { type: "Identifier", name: identifiers[index2] } : { type: "Literal", value: identifiers[index2] };
      node2 = node2 ? {
        type: "MemberExpression",
        object: node2,
        property: prop,
        computed: Boolean(index2 && prop.type === "Literal"),
        optional: false
      } : prop;
    }
    result = node2;
  } else {
    result = name$2(name2) && !/^[a-z]/.test(name2) ? { type: "Identifier", name: name2 } : { type: "Literal", value: name2 };
  }
  if (result.type === "Literal") {
    const name3 = (
      /** @type {string | number} */
      result.value
    );
    return own$3.call(state.components, name3) ? state.components[name3] : name3;
  }
  if (state.evaluater) {
    return state.evaluater.evaluateExpression(result);
  }
  crashEstree(state);
}
function crashEstree(state, place) {
  const message = new VFileMessage(
    "Cannot handle MDX estrees without `createEvaluater`",
    {
      ancestors: state.ancestors,
      place,
      ruleId: "mdx-estree",
      source: "hast-util-to-jsx-runtime"
    }
  );
  message.file = state.filePath || void 0;
  message.url = docs + "#cannot-handle-mdx-estrees-without-createevaluater";
  throw message;
}
function transformStylesToCssCasing(domCasing) {
  const cssCasing = {};
  let from;
  for (from in domCasing) {
    if (own$3.call(domCasing, from)) {
      cssCasing[transformStyleToCssCasing(from)] = domCasing[from];
    }
  }
  return cssCasing;
}
function transformStyleToCssCasing(from) {
  let to = from.replace(cap, toDash);
  if (to.slice(0, 3) === "ms-") to = "-" + to;
  return to;
}
function toDash($0) {
  return "-" + $0.toLowerCase();
}
const urlAttributes = {
  action: ["form"],
  cite: ["blockquote", "del", "ins", "q"],
  data: ["object"],
  formAction: ["button", "input"],
  href: ["a", "area", "base", "link"],
  icon: ["menuitem"],
  itemId: null,
  manifest: ["html"],
  ping: ["a", "area"],
  poster: ["video"],
  src: [
    "audio",
    "embed",
    "iframe",
    "img",
    "input",
    "script",
    "source",
    "track",
    "video"
  ]
};
const emptyOptions$2 = {};
function toString$1(value, options) {
  const settings = emptyOptions$2;
  const includeImageAlt = typeof settings.includeImageAlt === "boolean" ? settings.includeImageAlt : true;
  const includeHtml = typeof settings.includeHtml === "boolean" ? settings.includeHtml : true;
  return one(value, includeImageAlt, includeHtml);
}
function one(value, includeImageAlt, includeHtml) {
  if (node(value)) {
    if ("value" in value) {
      return value.type === "html" && !includeHtml ? "" : value.value;
    }
    if (includeImageAlt && "alt" in value && value.alt) {
      return value.alt;
    }
    if ("children" in value) {
      return all(value.children, includeImageAlt, includeHtml);
    }
  }
  if (Array.isArray(value)) {
    return all(value, includeImageAlt, includeHtml);
  }
  return "";
}
function all(values, includeImageAlt, includeHtml) {
  const result = [];
  let index2 = -1;
  while (++index2 < values.length) {
    result[index2] = one(values[index2], includeImageAlt, includeHtml);
  }
  return result.join("");
}
function node(value) {
  return Boolean(value && typeof value === "object");
}
const element = document.createElement("i");
function decodeNamedCharacterReference(value) {
  const characterReference2 = "&" + value + ";";
  element.innerHTML = characterReference2;
  const character = element.textContent;
  if (character.charCodeAt(character.length - 1) === 59 && value !== "semi") {
    return false;
  }
  return character === characterReference2 ? false : character;
}
function splice(list2, start, remove, items) {
  const end = list2.length;
  let chunkStart = 0;
  let parameters;
  if (start < 0) {
    start = -start > end ? 0 : end + start;
  } else {
    start = start > end ? end : start;
  }
  remove = remove > 0 ? remove : 0;
  if (items.length < 1e4) {
    parameters = Array.from(items);
    parameters.unshift(start, remove);
    list2.splice(...parameters);
  } else {
    if (remove) list2.splice(start, remove);
    while (chunkStart < items.length) {
      parameters = items.slice(chunkStart, chunkStart + 1e4);
      parameters.unshift(start, 0);
      list2.splice(...parameters);
      chunkStart += 1e4;
      start += 1e4;
    }
  }
}
function push(list2, items) {
  if (list2.length > 0) {
    splice(list2, list2.length, 0, items);
    return list2;
  }
  return items;
}
const hasOwnProperty = {}.hasOwnProperty;
function combineExtensions(extensions) {
  const all2 = {};
  let index2 = -1;
  while (++index2 < extensions.length) {
    syntaxExtension(all2, extensions[index2]);
  }
  return all2;
}
function syntaxExtension(all2, extension2) {
  let hook;
  for (hook in extension2) {
    const maybe = hasOwnProperty.call(all2, hook) ? all2[hook] : void 0;
    const left = maybe || (all2[hook] = {});
    const right = extension2[hook];
    let code2;
    if (right) {
      for (code2 in right) {
        if (!hasOwnProperty.call(left, code2)) left[code2] = [];
        const value = right[code2];
        constructs(
          // @ts-expect-error Looks like a list.
          left[code2],
          Array.isArray(value) ? value : value ? [value] : []
        );
      }
    }
  }
}
function constructs(existing, list2) {
  let index2 = -1;
  const before = [];
  while (++index2 < list2.length) {
    (list2[index2].add === "after" ? existing : before).push(list2[index2]);
  }
  splice(existing, 0, 0, before);
}
function decodeNumericCharacterReference(value, base) {
  const code2 = Number.parseInt(value, base);
  if (
    // C0 except for HT, LF, FF, CR, space.
    code2 < 9 || code2 === 11 || code2 > 13 && code2 < 32 || // Control character (DEL) of C0, and C1 controls.
    code2 > 126 && code2 < 160 || // Lone high surrogates and low surrogates.
    code2 > 55295 && code2 < 57344 || // Noncharacters.
    code2 > 64975 && code2 < 65008 || /* eslint-disable no-bitwise */
    (code2 & 65535) === 65535 || (code2 & 65535) === 65534 || /* eslint-enable no-bitwise */
    // Out of range
    code2 > 1114111
  ) {
    return "�";
  }
  return String.fromCodePoint(code2);
}
function normalizeIdentifier(value) {
  return value.replace(/[\t\n\r ]+/g, " ").replace(/^ | $/g, "").toLowerCase().toUpperCase();
}
const asciiAlpha = regexCheck(/[A-Za-z]/);
const asciiAlphanumeric = regexCheck(/[\dA-Za-z]/);
const asciiAtext = regexCheck(/[#-'*+\--9=?A-Z^-~]/);
function asciiControl(code2) {
  return (
    // Special whitespace codes (which have negative values), C0 and Control
    // character DEL
    code2 !== null && (code2 < 32 || code2 === 127)
  );
}
const asciiDigit = regexCheck(/\d/);
const asciiHexDigit = regexCheck(/[\dA-Fa-f]/);
const asciiPunctuation = regexCheck(/[!-/:-@[-`{-~]/);
function markdownLineEnding(code2) {
  return code2 !== null && code2 < -2;
}
function markdownLineEndingOrSpace(code2) {
  return code2 !== null && (code2 < 0 || code2 === 32);
}
function markdownSpace(code2) {
  return code2 === -2 || code2 === -1 || code2 === 32;
}
const unicodePunctuation = regexCheck(new RegExp("\\p{P}|\\p{S}", "u"));
const unicodeWhitespace = regexCheck(/\s/);
function regexCheck(regex) {
  return check;
  function check(code2) {
    return code2 !== null && code2 > -1 && regex.test(String.fromCharCode(code2));
  }
}
function normalizeUri(value) {
  const result = [];
  let index2 = -1;
  let start = 0;
  let skip = 0;
  while (++index2 < value.length) {
    const code2 = value.charCodeAt(index2);
    let replace2 = "";
    if (code2 === 37 && asciiAlphanumeric(value.charCodeAt(index2 + 1)) && asciiAlphanumeric(value.charCodeAt(index2 + 2))) {
      skip = 2;
    } else if (code2 < 128) {
      if (!/[!#$&-;=?-Z_a-z~]/.test(String.fromCharCode(code2))) {
        replace2 = String.fromCharCode(code2);
      }
    } else if (code2 > 55295 && code2 < 57344) {
      const next = value.charCodeAt(index2 + 1);
      if (code2 < 56320 && next > 56319 && next < 57344) {
        replace2 = String.fromCharCode(code2, next);
        skip = 1;
      } else {
        replace2 = "�";
      }
    } else {
      replace2 = String.fromCharCode(code2);
    }
    if (replace2) {
      result.push(value.slice(start, index2), encodeURIComponent(replace2));
      start = index2 + skip + 1;
      replace2 = "";
    }
    if (skip) {
      index2 += skip;
      skip = 0;
    }
  }
  return result.join("") + value.slice(start);
}
function factorySpace(effects, ok2, type, max) {
  const limit = max ? max - 1 : Number.POSITIVE_INFINITY;
  let size = 0;
  return start;
  function start(code2) {
    if (markdownSpace(code2)) {
      effects.enter(type);
      return prefix(code2);
    }
    return ok2(code2);
  }
  function prefix(code2) {
    if (markdownSpace(code2) && size++ < limit) {
      effects.consume(code2);
      return prefix;
    }
    effects.exit(type);
    return ok2(code2);
  }
}
const content$1 = {
  tokenize: initializeContent
};
function initializeContent(effects) {
  const contentStart = effects.attempt(this.parser.constructs.contentInitial, afterContentStartConstruct, paragraphInitial);
  let previous2;
  return contentStart;
  function afterContentStartConstruct(code2) {
    if (code2 === null) {
      effects.consume(code2);
      return;
    }
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return factorySpace(effects, contentStart, "linePrefix");
  }
  function paragraphInitial(code2) {
    effects.enter("paragraph");
    return lineStart(code2);
  }
  function lineStart(code2) {
    const token = effects.enter("chunkText", {
      contentType: "text",
      previous: previous2
    });
    if (previous2) {
      previous2.next = token;
    }
    previous2 = token;
    return data2(code2);
  }
  function data2(code2) {
    if (code2 === null) {
      effects.exit("chunkText");
      effects.exit("paragraph");
      effects.consume(code2);
      return;
    }
    if (markdownLineEnding(code2)) {
      effects.consume(code2);
      effects.exit("chunkText");
      return lineStart;
    }
    effects.consume(code2);
    return data2;
  }
}
const document$2 = {
  tokenize: initializeDocument
};
const containerConstruct = {
  tokenize: tokenizeContainer
};
function initializeDocument(effects) {
  const self2 = this;
  const stack = [];
  let continued = 0;
  let childFlow;
  let childToken;
  let lineStartOffset;
  return start;
  function start(code2) {
    if (continued < stack.length) {
      const item = stack[continued];
      self2.containerState = item[1];
      return effects.attempt(item[0].continuation, documentContinue, checkNewContainers)(code2);
    }
    return checkNewContainers(code2);
  }
  function documentContinue(code2) {
    continued++;
    if (self2.containerState._closeFlow) {
      self2.containerState._closeFlow = void 0;
      if (childFlow) {
        closeFlow();
      }
      const indexBeforeExits = self2.events.length;
      let indexBeforeFlow = indexBeforeExits;
      let point2;
      while (indexBeforeFlow--) {
        if (self2.events[indexBeforeFlow][0] === "exit" && self2.events[indexBeforeFlow][1].type === "chunkFlow") {
          point2 = self2.events[indexBeforeFlow][1].end;
          break;
        }
      }
      exitContainers(continued);
      let index2 = indexBeforeExits;
      while (index2 < self2.events.length) {
        self2.events[index2][1].end = {
          ...point2
        };
        index2++;
      }
      splice(self2.events, indexBeforeFlow + 1, 0, self2.events.slice(indexBeforeExits));
      self2.events.length = index2;
      return checkNewContainers(code2);
    }
    return start(code2);
  }
  function checkNewContainers(code2) {
    if (continued === stack.length) {
      if (!childFlow) {
        return documentContinued(code2);
      }
      if (childFlow.currentConstruct && childFlow.currentConstruct.concrete) {
        return flowStart(code2);
      }
      self2.interrupt = Boolean(childFlow.currentConstruct && !childFlow._gfmTableDynamicInterruptHack);
    }
    self2.containerState = {};
    return effects.check(containerConstruct, thereIsANewContainer, thereIsNoNewContainer)(code2);
  }
  function thereIsANewContainer(code2) {
    if (childFlow) closeFlow();
    exitContainers(continued);
    return documentContinued(code2);
  }
  function thereIsNoNewContainer(code2) {
    self2.parser.lazy[self2.now().line] = continued !== stack.length;
    lineStartOffset = self2.now().offset;
    return flowStart(code2);
  }
  function documentContinued(code2) {
    self2.containerState = {};
    return effects.attempt(containerConstruct, containerContinue, flowStart)(code2);
  }
  function containerContinue(code2) {
    continued++;
    stack.push([self2.currentConstruct, self2.containerState]);
    return documentContinued(code2);
  }
  function flowStart(code2) {
    if (code2 === null) {
      if (childFlow) closeFlow();
      exitContainers(0);
      effects.consume(code2);
      return;
    }
    childFlow = childFlow || self2.parser.flow(self2.now());
    effects.enter("chunkFlow", {
      _tokenizer: childFlow,
      contentType: "flow",
      previous: childToken
    });
    return flowContinue(code2);
  }
  function flowContinue(code2) {
    if (code2 === null) {
      writeToChild(effects.exit("chunkFlow"), true);
      exitContainers(0);
      effects.consume(code2);
      return;
    }
    if (markdownLineEnding(code2)) {
      effects.consume(code2);
      writeToChild(effects.exit("chunkFlow"));
      continued = 0;
      self2.interrupt = void 0;
      return start;
    }
    effects.consume(code2);
    return flowContinue;
  }
  function writeToChild(token, endOfFile) {
    const stream = self2.sliceStream(token);
    if (endOfFile) stream.push(null);
    token.previous = childToken;
    if (childToken) childToken.next = token;
    childToken = token;
    childFlow.defineSkip(token.start);
    childFlow.write(stream);
    if (self2.parser.lazy[token.start.line]) {
      let index2 = childFlow.events.length;
      while (index2--) {
        if (
          // The token starts before the line ending…
          childFlow.events[index2][1].start.offset < lineStartOffset && // …and either is not ended yet…
          (!childFlow.events[index2][1].end || // …or ends after it.
          childFlow.events[index2][1].end.offset > lineStartOffset)
        ) {
          return;
        }
      }
      const indexBeforeExits = self2.events.length;
      let indexBeforeFlow = indexBeforeExits;
      let seen;
      let point2;
      while (indexBeforeFlow--) {
        if (self2.events[indexBeforeFlow][0] === "exit" && self2.events[indexBeforeFlow][1].type === "chunkFlow") {
          if (seen) {
            point2 = self2.events[indexBeforeFlow][1].end;
            break;
          }
          seen = true;
        }
      }
      exitContainers(continued);
      index2 = indexBeforeExits;
      while (index2 < self2.events.length) {
        self2.events[index2][1].end = {
          ...point2
        };
        index2++;
      }
      splice(self2.events, indexBeforeFlow + 1, 0, self2.events.slice(indexBeforeExits));
      self2.events.length = index2;
    }
  }
  function exitContainers(size) {
    let index2 = stack.length;
    while (index2-- > size) {
      const entry2 = stack[index2];
      self2.containerState = entry2[1];
      entry2[0].exit.call(self2, effects);
    }
    stack.length = size;
  }
  function closeFlow() {
    childFlow.write([null]);
    childToken = void 0;
    childFlow = void 0;
    self2.containerState._closeFlow = void 0;
  }
}
function tokenizeContainer(effects, ok2, nok) {
  return factorySpace(effects, effects.attempt(this.parser.constructs.document, ok2, nok), "linePrefix", this.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4);
}
function classifyCharacter(code2) {
  if (code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)) {
    return 1;
  }
  if (unicodePunctuation(code2)) {
    return 2;
  }
}
function resolveAll(constructs2, events2, context) {
  const called = [];
  let index2 = -1;
  while (++index2 < constructs2.length) {
    const resolve = constructs2[index2].resolveAll;
    if (resolve && !called.includes(resolve)) {
      events2 = resolve(events2, context);
      called.push(resolve);
    }
  }
  return events2;
}
const attention = {
  name: "attention",
  resolveAll: resolveAllAttention,
  tokenize: tokenizeAttention
};
function resolveAllAttention(events2, context) {
  let index2 = -1;
  let open;
  let group;
  let text2;
  let openingSequence;
  let closingSequence;
  let use;
  let nextEvents;
  let offset;
  while (++index2 < events2.length) {
    if (events2[index2][0] === "enter" && events2[index2][1].type === "attentionSequence" && events2[index2][1]._close) {
      open = index2;
      while (open--) {
        if (events2[open][0] === "exit" && events2[open][1].type === "attentionSequence" && events2[open][1]._open && // If the markers are the same:
        context.sliceSerialize(events2[open][1]).charCodeAt(0) === context.sliceSerialize(events2[index2][1]).charCodeAt(0)) {
          if ((events2[open][1]._close || events2[index2][1]._open) && (events2[index2][1].end.offset - events2[index2][1].start.offset) % 3 && !((events2[open][1].end.offset - events2[open][1].start.offset + events2[index2][1].end.offset - events2[index2][1].start.offset) % 3)) {
            continue;
          }
          use = events2[open][1].end.offset - events2[open][1].start.offset > 1 && events2[index2][1].end.offset - events2[index2][1].start.offset > 1 ? 2 : 1;
          const start = {
            ...events2[open][1].end
          };
          const end = {
            ...events2[index2][1].start
          };
          movePoint(start, -use);
          movePoint(end, use);
          openingSequence = {
            type: use > 1 ? "strongSequence" : "emphasisSequence",
            start,
            end: {
              ...events2[open][1].end
            }
          };
          closingSequence = {
            type: use > 1 ? "strongSequence" : "emphasisSequence",
            start: {
              ...events2[index2][1].start
            },
            end
          };
          text2 = {
            type: use > 1 ? "strongText" : "emphasisText",
            start: {
              ...events2[open][1].end
            },
            end: {
              ...events2[index2][1].start
            }
          };
          group = {
            type: use > 1 ? "strong" : "emphasis",
            start: {
              ...openingSequence.start
            },
            end: {
              ...closingSequence.end
            }
          };
          events2[open][1].end = {
            ...openingSequence.start
          };
          events2[index2][1].start = {
            ...closingSequence.end
          };
          nextEvents = [];
          if (events2[open][1].end.offset - events2[open][1].start.offset) {
            nextEvents = push(nextEvents, [["enter", events2[open][1], context], ["exit", events2[open][1], context]]);
          }
          nextEvents = push(nextEvents, [["enter", group, context], ["enter", openingSequence, context], ["exit", openingSequence, context], ["enter", text2, context]]);
          nextEvents = push(nextEvents, resolveAll(context.parser.constructs.insideSpan.null, events2.slice(open + 1, index2), context));
          nextEvents = push(nextEvents, [["exit", text2, context], ["enter", closingSequence, context], ["exit", closingSequence, context], ["exit", group, context]]);
          if (events2[index2][1].end.offset - events2[index2][1].start.offset) {
            offset = 2;
            nextEvents = push(nextEvents, [["enter", events2[index2][1], context], ["exit", events2[index2][1], context]]);
          } else {
            offset = 0;
          }
          splice(events2, open - 1, index2 - open + 3, nextEvents);
          index2 = open + nextEvents.length - offset - 2;
          break;
        }
      }
    }
  }
  index2 = -1;
  while (++index2 < events2.length) {
    if (events2[index2][1].type === "attentionSequence") {
      events2[index2][1].type = "data";
    }
  }
  return events2;
}
function tokenizeAttention(effects, ok2) {
  const attentionMarkers2 = this.parser.constructs.attentionMarkers.null;
  const previous2 = this.previous;
  const before = classifyCharacter(previous2);
  let marker;
  return start;
  function start(code2) {
    marker = code2;
    effects.enter("attentionSequence");
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      return inside;
    }
    const token = effects.exit("attentionSequence");
    const after = classifyCharacter(code2);
    const open = !after || after === 2 && before || attentionMarkers2.includes(code2);
    const close = !before || before === 2 && after || attentionMarkers2.includes(previous2);
    token._open = Boolean(marker === 42 ? open : open && (before || !close));
    token._close = Boolean(marker === 42 ? close : close && (after || !open));
    return ok2(code2);
  }
}
function movePoint(point2, offset) {
  point2.column += offset;
  point2.offset += offset;
  point2._bufferIndex += offset;
}
const autolink = {
  name: "autolink",
  tokenize: tokenizeAutolink
};
function tokenizeAutolink(effects, ok2, nok) {
  let size = 0;
  return start;
  function start(code2) {
    effects.enter("autolink");
    effects.enter("autolinkMarker");
    effects.consume(code2);
    effects.exit("autolinkMarker");
    effects.enter("autolinkProtocol");
    return open;
  }
  function open(code2) {
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return schemeOrEmailAtext;
    }
    if (code2 === 64) {
      return nok(code2);
    }
    return emailAtext(code2);
  }
  function schemeOrEmailAtext(code2) {
    if (code2 === 43 || code2 === 45 || code2 === 46 || asciiAlphanumeric(code2)) {
      size = 1;
      return schemeInsideOrEmailAtext(code2);
    }
    return emailAtext(code2);
  }
  function schemeInsideOrEmailAtext(code2) {
    if (code2 === 58) {
      effects.consume(code2);
      size = 0;
      return urlInside;
    }
    if ((code2 === 43 || code2 === 45 || code2 === 46 || asciiAlphanumeric(code2)) && size++ < 32) {
      effects.consume(code2);
      return schemeInsideOrEmailAtext;
    }
    size = 0;
    return emailAtext(code2);
  }
  function urlInside(code2) {
    if (code2 === 62) {
      effects.exit("autolinkProtocol");
      effects.enter("autolinkMarker");
      effects.consume(code2);
      effects.exit("autolinkMarker");
      effects.exit("autolink");
      return ok2;
    }
    if (code2 === null || code2 === 32 || code2 === 60 || asciiControl(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return urlInside;
  }
  function emailAtext(code2) {
    if (code2 === 64) {
      effects.consume(code2);
      return emailAtSignOrDot;
    }
    if (asciiAtext(code2)) {
      effects.consume(code2);
      return emailAtext;
    }
    return nok(code2);
  }
  function emailAtSignOrDot(code2) {
    return asciiAlphanumeric(code2) ? emailLabel(code2) : nok(code2);
  }
  function emailLabel(code2) {
    if (code2 === 46) {
      effects.consume(code2);
      size = 0;
      return emailAtSignOrDot;
    }
    if (code2 === 62) {
      effects.exit("autolinkProtocol").type = "autolinkEmail";
      effects.enter("autolinkMarker");
      effects.consume(code2);
      effects.exit("autolinkMarker");
      effects.exit("autolink");
      return ok2;
    }
    return emailValue(code2);
  }
  function emailValue(code2) {
    if ((code2 === 45 || asciiAlphanumeric(code2)) && size++ < 63) {
      const next = code2 === 45 ? emailValue : emailLabel;
      effects.consume(code2);
      return next;
    }
    return nok(code2);
  }
}
const blankLine = {
  partial: true,
  tokenize: tokenizeBlankLine
};
function tokenizeBlankLine(effects, ok2, nok) {
  return start;
  function start(code2) {
    return markdownSpace(code2) ? factorySpace(effects, after, "linePrefix")(code2) : after(code2);
  }
  function after(code2) {
    return code2 === null || markdownLineEnding(code2) ? ok2(code2) : nok(code2);
  }
}
const blockQuote = {
  continuation: {
    tokenize: tokenizeBlockQuoteContinuation
  },
  exit: exit$1,
  name: "blockQuote",
  tokenize: tokenizeBlockQuoteStart
};
function tokenizeBlockQuoteStart(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    if (code2 === 62) {
      const state = self2.containerState;
      if (!state.open) {
        effects.enter("blockQuote", {
          _container: true
        });
        state.open = true;
      }
      effects.enter("blockQuotePrefix");
      effects.enter("blockQuoteMarker");
      effects.consume(code2);
      effects.exit("blockQuoteMarker");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    if (markdownSpace(code2)) {
      effects.enter("blockQuotePrefixWhitespace");
      effects.consume(code2);
      effects.exit("blockQuotePrefixWhitespace");
      effects.exit("blockQuotePrefix");
      return ok2;
    }
    effects.exit("blockQuotePrefix");
    return ok2(code2);
  }
}
function tokenizeBlockQuoteContinuation(effects, ok2, nok) {
  const self2 = this;
  return contStart;
  function contStart(code2) {
    if (markdownSpace(code2)) {
      return factorySpace(effects, contBefore, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2);
    }
    return contBefore(code2);
  }
  function contBefore(code2) {
    return effects.attempt(blockQuote, ok2, nok)(code2);
  }
}
function exit$1(effects) {
  effects.exit("blockQuote");
}
const characterEscape = {
  name: "characterEscape",
  tokenize: tokenizeCharacterEscape
};
function tokenizeCharacterEscape(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.enter("characterEscape");
    effects.enter("escapeMarker");
    effects.consume(code2);
    effects.exit("escapeMarker");
    return inside;
  }
  function inside(code2) {
    if (asciiPunctuation(code2)) {
      effects.enter("characterEscapeValue");
      effects.consume(code2);
      effects.exit("characterEscapeValue");
      effects.exit("characterEscape");
      return ok2;
    }
    return nok(code2);
  }
}
const characterReference = {
  name: "characterReference",
  tokenize: tokenizeCharacterReference
};
function tokenizeCharacterReference(effects, ok2, nok) {
  const self2 = this;
  let size = 0;
  let max;
  let test;
  return start;
  function start(code2) {
    effects.enter("characterReference");
    effects.enter("characterReferenceMarker");
    effects.consume(code2);
    effects.exit("characterReferenceMarker");
    return open;
  }
  function open(code2) {
    if (code2 === 35) {
      effects.enter("characterReferenceMarkerNumeric");
      effects.consume(code2);
      effects.exit("characterReferenceMarkerNumeric");
      return numeric;
    }
    effects.enter("characterReferenceValue");
    max = 31;
    test = asciiAlphanumeric;
    return value(code2);
  }
  function numeric(code2) {
    if (code2 === 88 || code2 === 120) {
      effects.enter("characterReferenceMarkerHexadecimal");
      effects.consume(code2);
      effects.exit("characterReferenceMarkerHexadecimal");
      effects.enter("characterReferenceValue");
      max = 6;
      test = asciiHexDigit;
      return value;
    }
    effects.enter("characterReferenceValue");
    max = 7;
    test = asciiDigit;
    return value(code2);
  }
  function value(code2) {
    if (code2 === 59 && size) {
      const token = effects.exit("characterReferenceValue");
      if (test === asciiAlphanumeric && !decodeNamedCharacterReference(self2.sliceSerialize(token))) {
        return nok(code2);
      }
      effects.enter("characterReferenceMarker");
      effects.consume(code2);
      effects.exit("characterReferenceMarker");
      effects.exit("characterReference");
      return ok2;
    }
    if (test(code2) && size++ < max) {
      effects.consume(code2);
      return value;
    }
    return nok(code2);
  }
}
const nonLazyContinuation = {
  partial: true,
  tokenize: tokenizeNonLazyContinuation
};
const codeFenced = {
  concrete: true,
  name: "codeFenced",
  tokenize: tokenizeCodeFenced
};
function tokenizeCodeFenced(effects, ok2, nok) {
  const self2 = this;
  const closeStart = {
    partial: true,
    tokenize: tokenizeCloseStart
  };
  let initialPrefix = 0;
  let sizeOpen = 0;
  let marker;
  return start;
  function start(code2) {
    return beforeSequenceOpen(code2);
  }
  function beforeSequenceOpen(code2) {
    const tail = self2.events[self2.events.length - 1];
    initialPrefix = tail && tail[1].type === "linePrefix" ? tail[2].sliceSerialize(tail[1], true).length : 0;
    marker = code2;
    effects.enter("codeFenced");
    effects.enter("codeFencedFence");
    effects.enter("codeFencedFenceSequence");
    return sequenceOpen(code2);
  }
  function sequenceOpen(code2) {
    if (code2 === marker) {
      sizeOpen++;
      effects.consume(code2);
      return sequenceOpen;
    }
    if (sizeOpen < 3) {
      return nok(code2);
    }
    effects.exit("codeFencedFenceSequence");
    return markdownSpace(code2) ? factorySpace(effects, infoBefore, "whitespace")(code2) : infoBefore(code2);
  }
  function infoBefore(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("codeFencedFence");
      return self2.interrupt ? ok2(code2) : effects.check(nonLazyContinuation, atNonLazyBreak, after)(code2);
    }
    effects.enter("codeFencedFenceInfo");
    effects.enter("chunkString", {
      contentType: "string"
    });
    return info(code2);
  }
  function info(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("chunkString");
      effects.exit("codeFencedFenceInfo");
      return infoBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.exit("chunkString");
      effects.exit("codeFencedFenceInfo");
      return factorySpace(effects, metaBefore, "whitespace")(code2);
    }
    if (code2 === 96 && code2 === marker) {
      return nok(code2);
    }
    effects.consume(code2);
    return info;
  }
  function metaBefore(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return infoBefore(code2);
    }
    effects.enter("codeFencedFenceMeta");
    effects.enter("chunkString", {
      contentType: "string"
    });
    return meta(code2);
  }
  function meta(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("chunkString");
      effects.exit("codeFencedFenceMeta");
      return infoBefore(code2);
    }
    if (code2 === 96 && code2 === marker) {
      return nok(code2);
    }
    effects.consume(code2);
    return meta;
  }
  function atNonLazyBreak(code2) {
    return effects.attempt(closeStart, after, contentBefore)(code2);
  }
  function contentBefore(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return contentStart;
  }
  function contentStart(code2) {
    return initialPrefix > 0 && markdownSpace(code2) ? factorySpace(effects, beforeContentChunk, "linePrefix", initialPrefix + 1)(code2) : beforeContentChunk(code2);
  }
  function beforeContentChunk(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return effects.check(nonLazyContinuation, atNonLazyBreak, after)(code2);
    }
    effects.enter("codeFlowValue");
    return contentChunk(code2);
  }
  function contentChunk(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("codeFlowValue");
      return beforeContentChunk(code2);
    }
    effects.consume(code2);
    return contentChunk;
  }
  function after(code2) {
    effects.exit("codeFenced");
    return ok2(code2);
  }
  function tokenizeCloseStart(effects2, ok3, nok2) {
    let size = 0;
    return startBefore;
    function startBefore(code2) {
      effects2.enter("lineEnding");
      effects2.consume(code2);
      effects2.exit("lineEnding");
      return start2;
    }
    function start2(code2) {
      effects2.enter("codeFencedFence");
      return markdownSpace(code2) ? factorySpace(effects2, beforeSequenceClose, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2) : beforeSequenceClose(code2);
    }
    function beforeSequenceClose(code2) {
      if (code2 === marker) {
        effects2.enter("codeFencedFenceSequence");
        return sequenceClose(code2);
      }
      return nok2(code2);
    }
    function sequenceClose(code2) {
      if (code2 === marker) {
        size++;
        effects2.consume(code2);
        return sequenceClose;
      }
      if (size >= sizeOpen) {
        effects2.exit("codeFencedFenceSequence");
        return markdownSpace(code2) ? factorySpace(effects2, sequenceCloseAfter, "whitespace")(code2) : sequenceCloseAfter(code2);
      }
      return nok2(code2);
    }
    function sequenceCloseAfter(code2) {
      if (code2 === null || markdownLineEnding(code2)) {
        effects2.exit("codeFencedFence");
        return ok3(code2);
      }
      return nok2(code2);
    }
  }
}
function tokenizeNonLazyContinuation(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return lineStart;
  }
  function lineStart(code2) {
    return self2.parser.lazy[self2.now().line] ? nok(code2) : ok2(code2);
  }
}
const codeIndented = {
  name: "codeIndented",
  tokenize: tokenizeCodeIndented
};
const furtherStart = {
  partial: true,
  tokenize: tokenizeFurtherStart
};
function tokenizeCodeIndented(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    effects.enter("codeIndented");
    return factorySpace(effects, afterPrefix, "linePrefix", 4 + 1)(code2);
  }
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4 ? atBreak(code2) : nok(code2);
  }
  function atBreak(code2) {
    if (code2 === null) {
      return after(code2);
    }
    if (markdownLineEnding(code2)) {
      return effects.attempt(furtherStart, atBreak, after)(code2);
    }
    effects.enter("codeFlowValue");
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("codeFlowValue");
      return atBreak(code2);
    }
    effects.consume(code2);
    return inside;
  }
  function after(code2) {
    effects.exit("codeIndented");
    return ok2(code2);
  }
}
function tokenizeFurtherStart(effects, ok2, nok) {
  const self2 = this;
  return furtherStart2;
  function furtherStart2(code2) {
    if (self2.parser.lazy[self2.now().line]) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return furtherStart2;
    }
    return factorySpace(effects, afterPrefix, "linePrefix", 4 + 1)(code2);
  }
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4 ? ok2(code2) : markdownLineEnding(code2) ? furtherStart2(code2) : nok(code2);
  }
}
const codeText = {
  name: "codeText",
  previous: previous$1,
  resolve: resolveCodeText,
  tokenize: tokenizeCodeText
};
function resolveCodeText(events2) {
  let tailExitIndex = events2.length - 4;
  let headEnterIndex = 3;
  let index2;
  let enter;
  if ((events2[headEnterIndex][1].type === "lineEnding" || events2[headEnterIndex][1].type === "space") && (events2[tailExitIndex][1].type === "lineEnding" || events2[tailExitIndex][1].type === "space")) {
    index2 = headEnterIndex;
    while (++index2 < tailExitIndex) {
      if (events2[index2][1].type === "codeTextData") {
        events2[headEnterIndex][1].type = "codeTextPadding";
        events2[tailExitIndex][1].type = "codeTextPadding";
        headEnterIndex += 2;
        tailExitIndex -= 2;
        break;
      }
    }
  }
  index2 = headEnterIndex - 1;
  tailExitIndex++;
  while (++index2 <= tailExitIndex) {
    if (enter === void 0) {
      if (index2 !== tailExitIndex && events2[index2][1].type !== "lineEnding") {
        enter = index2;
      }
    } else if (index2 === tailExitIndex || events2[index2][1].type === "lineEnding") {
      events2[enter][1].type = "codeTextData";
      if (index2 !== enter + 2) {
        events2[enter][1].end = events2[index2 - 1][1].end;
        events2.splice(enter + 2, index2 - enter - 2);
        tailExitIndex -= index2 - enter - 2;
        index2 = enter + 2;
      }
      enter = void 0;
    }
  }
  return events2;
}
function previous$1(code2) {
  return code2 !== 96 || this.events[this.events.length - 1][1].type === "characterEscape";
}
function tokenizeCodeText(effects, ok2, nok) {
  let sizeOpen = 0;
  let size;
  let token;
  return start;
  function start(code2) {
    effects.enter("codeText");
    effects.enter("codeTextSequence");
    return sequenceOpen(code2);
  }
  function sequenceOpen(code2) {
    if (code2 === 96) {
      effects.consume(code2);
      sizeOpen++;
      return sequenceOpen;
    }
    effects.exit("codeTextSequence");
    return between(code2);
  }
  function between(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 32) {
      effects.enter("space");
      effects.consume(code2);
      effects.exit("space");
      return between;
    }
    if (code2 === 96) {
      token = effects.enter("codeTextSequence");
      size = 0;
      return sequenceClose(code2);
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return between;
    }
    effects.enter("codeTextData");
    return data2(code2);
  }
  function data2(code2) {
    if (code2 === null || code2 === 32 || code2 === 96 || markdownLineEnding(code2)) {
      effects.exit("codeTextData");
      return between(code2);
    }
    effects.consume(code2);
    return data2;
  }
  function sequenceClose(code2) {
    if (code2 === 96) {
      effects.consume(code2);
      size++;
      return sequenceClose;
    }
    if (size === sizeOpen) {
      effects.exit("codeTextSequence");
      effects.exit("codeText");
      return ok2(code2);
    }
    token.type = "codeTextData";
    return data2(code2);
  }
}
class SpliceBuffer {
  /**
   * @param {ReadonlyArray<T> | null | undefined} [initial]
   *   Initial items (optional).
   * @returns
   *   Splice buffer.
   */
  constructor(initial) {
    this.left = initial ? [...initial] : [];
    this.right = [];
  }
  /**
   * Array access;
   * does not move the cursor.
   *
   * @param {number} index
   *   Index.
   * @return {T}
   *   Item.
   */
  get(index2) {
    if (index2 < 0 || index2 >= this.left.length + this.right.length) {
      throw new RangeError("Cannot access index `" + index2 + "` in a splice buffer of size `" + (this.left.length + this.right.length) + "`");
    }
    if (index2 < this.left.length) return this.left[index2];
    return this.right[this.right.length - index2 + this.left.length - 1];
  }
  /**
   * The length of the splice buffer, one greater than the largest index in the
   * array.
   */
  get length() {
    return this.left.length + this.right.length;
  }
  /**
   * Remove and return `list[0]`;
   * moves the cursor to `0`.
   *
   * @returns {T | undefined}
   *   Item, optional.
   */
  shift() {
    this.setCursor(0);
    return this.right.pop();
  }
  /**
   * Slice the buffer to get an array;
   * does not move the cursor.
   *
   * @param {number} start
   *   Start.
   * @param {number | null | undefined} [end]
   *   End (optional).
   * @returns {Array<T>}
   *   Array of items.
   */
  slice(start, end) {
    const stop = end === null || end === void 0 ? Number.POSITIVE_INFINITY : end;
    if (stop < this.left.length) {
      return this.left.slice(start, stop);
    }
    if (start > this.left.length) {
      return this.right.slice(this.right.length - stop + this.left.length, this.right.length - start + this.left.length).reverse();
    }
    return this.left.slice(start).concat(this.right.slice(this.right.length - stop + this.left.length).reverse());
  }
  /**
   * Mimics the behavior of Array.prototype.splice() except for the change of
   * interface necessary to avoid segfaults when patching in very large arrays.
   *
   * This operation moves cursor is moved to `start` and results in the cursor
   * placed after any inserted items.
   *
   * @param {number} start
   *   Start;
   *   zero-based index at which to start changing the array;
   *   negative numbers count backwards from the end of the array and values
   *   that are out-of bounds are clamped to the appropriate end of the array.
   * @param {number | null | undefined} [deleteCount=0]
   *   Delete count (default: `0`);
   *   maximum number of elements to delete, starting from start.
   * @param {Array<T> | null | undefined} [items=[]]
   *   Items to include in place of the deleted items (default: `[]`).
   * @return {Array<T>}
   *   Any removed items.
   */
  splice(start, deleteCount, items) {
    const count = deleteCount || 0;
    this.setCursor(Math.trunc(start));
    const removed = this.right.splice(this.right.length - count, Number.POSITIVE_INFINITY);
    if (items) chunkedPush(this.left, items);
    return removed.reverse();
  }
  /**
   * Remove and return the highest-numbered item in the array, so
   * `list[list.length - 1]`;
   * Moves the cursor to `length`.
   *
   * @returns {T | undefined}
   *   Item, optional.
   */
  pop() {
    this.setCursor(Number.POSITIVE_INFINITY);
    return this.left.pop();
  }
  /**
   * Inserts a single item to the high-numbered side of the array;
   * moves the cursor to `length`.
   *
   * @param {T} item
   *   Item.
   * @returns {undefined}
   *   Nothing.
   */
  push(item) {
    this.setCursor(Number.POSITIVE_INFINITY);
    this.left.push(item);
  }
  /**
   * Inserts many items to the high-numbered side of the array.
   * Moves the cursor to `length`.
   *
   * @param {Array<T>} items
   *   Items.
   * @returns {undefined}
   *   Nothing.
   */
  pushMany(items) {
    this.setCursor(Number.POSITIVE_INFINITY);
    chunkedPush(this.left, items);
  }
  /**
   * Inserts a single item to the low-numbered side of the array;
   * Moves the cursor to `0`.
   *
   * @param {T} item
   *   Item.
   * @returns {undefined}
   *   Nothing.
   */
  unshift(item) {
    this.setCursor(0);
    this.right.push(item);
  }
  /**
   * Inserts many items to the low-numbered side of the array;
   * moves the cursor to `0`.
   *
   * @param {Array<T>} items
   *   Items.
   * @returns {undefined}
   *   Nothing.
   */
  unshiftMany(items) {
    this.setCursor(0);
    chunkedPush(this.right, items.reverse());
  }
  /**
   * Move the cursor to a specific position in the array. Requires
   * time proportional to the distance moved.
   *
   * If `n < 0`, the cursor will end up at the beginning.
   * If `n > length`, the cursor will end up at the end.
   *
   * @param {number} n
   *   Position.
   * @return {undefined}
   *   Nothing.
   */
  setCursor(n) {
    if (n === this.left.length || n > this.left.length && this.right.length === 0 || n < 0 && this.left.length === 0) return;
    if (n < this.left.length) {
      const removed = this.left.splice(n, Number.POSITIVE_INFINITY);
      chunkedPush(this.right, removed.reverse());
    } else {
      const removed = this.right.splice(this.left.length + this.right.length - n, Number.POSITIVE_INFINITY);
      chunkedPush(this.left, removed.reverse());
    }
  }
}
function chunkedPush(list2, right) {
  let chunkStart = 0;
  if (right.length < 1e4) {
    list2.push(...right);
  } else {
    while (chunkStart < right.length) {
      list2.push(...right.slice(chunkStart, chunkStart + 1e4));
      chunkStart += 1e4;
    }
  }
}
function subtokenize(eventsArray) {
  const jumps = {};
  let index2 = -1;
  let event;
  let lineIndex;
  let otherIndex;
  let otherEvent;
  let parameters;
  let subevents;
  let more;
  const events2 = new SpliceBuffer(eventsArray);
  while (++index2 < events2.length) {
    while (index2 in jumps) {
      index2 = jumps[index2];
    }
    event = events2.get(index2);
    if (index2 && event[1].type === "chunkFlow" && events2.get(index2 - 1)[1].type === "listItemPrefix") {
      subevents = event[1]._tokenizer.events;
      otherIndex = 0;
      if (otherIndex < subevents.length && subevents[otherIndex][1].type === "lineEndingBlank") {
        otherIndex += 2;
      }
      if (otherIndex < subevents.length && subevents[otherIndex][1].type === "content") {
        while (++otherIndex < subevents.length) {
          if (subevents[otherIndex][1].type === "content") {
            break;
          }
          if (subevents[otherIndex][1].type === "chunkText") {
            subevents[otherIndex][1]._isInFirstContentOfListItem = true;
            otherIndex++;
          }
        }
      }
    }
    if (event[0] === "enter") {
      if (event[1].contentType) {
        Object.assign(jumps, subcontent(events2, index2));
        index2 = jumps[index2];
        more = true;
      }
    } else if (event[1]._container) {
      otherIndex = index2;
      lineIndex = void 0;
      while (otherIndex--) {
        otherEvent = events2.get(otherIndex);
        if (otherEvent[1].type === "lineEnding" || otherEvent[1].type === "lineEndingBlank") {
          if (otherEvent[0] === "enter") {
            if (lineIndex) {
              events2.get(lineIndex)[1].type = "lineEndingBlank";
            }
            otherEvent[1].type = "lineEnding";
            lineIndex = otherIndex;
          }
        } else if (otherEvent[1].type === "linePrefix" || otherEvent[1].type === "listItemIndent") ;
        else {
          break;
        }
      }
      if (lineIndex) {
        event[1].end = {
          ...events2.get(lineIndex)[1].start
        };
        parameters = events2.slice(lineIndex, index2);
        parameters.unshift(event);
        events2.splice(lineIndex, index2 - lineIndex + 1, parameters);
      }
    }
  }
  splice(eventsArray, 0, Number.POSITIVE_INFINITY, events2.slice(0));
  return !more;
}
function subcontent(events2, eventIndex) {
  const token = events2.get(eventIndex)[1];
  const context = events2.get(eventIndex)[2];
  let startPosition = eventIndex - 1;
  const startPositions = [];
  let tokenizer = token._tokenizer;
  if (!tokenizer) {
    tokenizer = context.parser[token.contentType](token.start);
    if (token._contentTypeTextTrailing) {
      tokenizer._contentTypeTextTrailing = true;
    }
  }
  const childEvents = tokenizer.events;
  const jumps = [];
  const gaps = {};
  let stream;
  let previous2;
  let index2 = -1;
  let current = token;
  let adjust = 0;
  let start = 0;
  const breaks = [start];
  while (current) {
    while (events2.get(++startPosition)[1] !== current) {
    }
    startPositions.push(startPosition);
    if (!current._tokenizer) {
      stream = context.sliceStream(current);
      if (!current.next) {
        stream.push(null);
      }
      if (previous2) {
        tokenizer.defineSkip(current.start);
      }
      if (current._isInFirstContentOfListItem) {
        tokenizer._gfmTasklistFirstContentOfListItem = true;
      }
      tokenizer.write(stream);
      if (current._isInFirstContentOfListItem) {
        tokenizer._gfmTasklistFirstContentOfListItem = void 0;
      }
    }
    previous2 = current;
    current = current.next;
  }
  current = token;
  while (++index2 < childEvents.length) {
    if (
      // Find a void token that includes a break.
      childEvents[index2][0] === "exit" && childEvents[index2 - 1][0] === "enter" && childEvents[index2][1].type === childEvents[index2 - 1][1].type && childEvents[index2][1].start.line !== childEvents[index2][1].end.line
    ) {
      start = index2 + 1;
      breaks.push(start);
      current._tokenizer = void 0;
      current.previous = void 0;
      current = current.next;
    }
  }
  tokenizer.events = [];
  if (current) {
    current._tokenizer = void 0;
    current.previous = void 0;
  } else {
    breaks.pop();
  }
  index2 = breaks.length;
  while (index2--) {
    const slice = childEvents.slice(breaks[index2], breaks[index2 + 1]);
    const start2 = startPositions.pop();
    jumps.push([start2, start2 + slice.length - 1]);
    events2.splice(start2, 2, slice);
  }
  jumps.reverse();
  index2 = -1;
  while (++index2 < jumps.length) {
    gaps[adjust + jumps[index2][0]] = adjust + jumps[index2][1];
    adjust += jumps[index2][1] - jumps[index2][0] - 1;
  }
  return gaps;
}
const content = {
  resolve: resolveContent,
  tokenize: tokenizeContent
};
const continuationConstruct = {
  partial: true,
  tokenize: tokenizeContinuation
};
function resolveContent(events2) {
  subtokenize(events2);
  return events2;
}
function tokenizeContent(effects, ok2) {
  let previous2;
  return chunkStart;
  function chunkStart(code2) {
    effects.enter("content");
    previous2 = effects.enter("chunkContent", {
      contentType: "content"
    });
    return chunkInside(code2);
  }
  function chunkInside(code2) {
    if (code2 === null) {
      return contentEnd(code2);
    }
    if (markdownLineEnding(code2)) {
      return effects.check(continuationConstruct, contentContinue, contentEnd)(code2);
    }
    effects.consume(code2);
    return chunkInside;
  }
  function contentEnd(code2) {
    effects.exit("chunkContent");
    effects.exit("content");
    return ok2(code2);
  }
  function contentContinue(code2) {
    effects.consume(code2);
    effects.exit("chunkContent");
    previous2.next = effects.enter("chunkContent", {
      contentType: "content",
      previous: previous2
    });
    previous2 = previous2.next;
    return chunkInside;
  }
}
function tokenizeContinuation(effects, ok2, nok) {
  const self2 = this;
  return startLookahead;
  function startLookahead(code2) {
    effects.exit("chunkContent");
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return factorySpace(effects, prefixed, "linePrefix");
  }
  function prefixed(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return nok(code2);
    }
    const tail = self2.events[self2.events.length - 1];
    if (!self2.parser.constructs.disable.null.includes("codeIndented") && tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4) {
      return ok2(code2);
    }
    return effects.interrupt(self2.parser.constructs.flow, nok, ok2)(code2);
  }
}
function factoryDestination(effects, ok2, nok, type, literalType, literalMarkerType, rawType, stringType, max) {
  const limit = max || Number.POSITIVE_INFINITY;
  let balance = 0;
  return start;
  function start(code2) {
    if (code2 === 60) {
      effects.enter(type);
      effects.enter(literalType);
      effects.enter(literalMarkerType);
      effects.consume(code2);
      effects.exit(literalMarkerType);
      return enclosedBefore;
    }
    if (code2 === null || code2 === 32 || code2 === 41 || asciiControl(code2)) {
      return nok(code2);
    }
    effects.enter(type);
    effects.enter(rawType);
    effects.enter(stringType);
    effects.enter("chunkString", {
      contentType: "string"
    });
    return raw(code2);
  }
  function enclosedBefore(code2) {
    if (code2 === 62) {
      effects.enter(literalMarkerType);
      effects.consume(code2);
      effects.exit(literalMarkerType);
      effects.exit(literalType);
      effects.exit(type);
      return ok2;
    }
    effects.enter(stringType);
    effects.enter("chunkString", {
      contentType: "string"
    });
    return enclosed(code2);
  }
  function enclosed(code2) {
    if (code2 === 62) {
      effects.exit("chunkString");
      effects.exit(stringType);
      return enclosedBefore(code2);
    }
    if (code2 === null || code2 === 60 || markdownLineEnding(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? enclosedEscape : enclosed;
  }
  function enclosedEscape(code2) {
    if (code2 === 60 || code2 === 62 || code2 === 92) {
      effects.consume(code2);
      return enclosed;
    }
    return enclosed(code2);
  }
  function raw(code2) {
    if (!balance && (code2 === null || code2 === 41 || markdownLineEndingOrSpace(code2))) {
      effects.exit("chunkString");
      effects.exit(stringType);
      effects.exit(rawType);
      effects.exit(type);
      return ok2(code2);
    }
    if (balance < limit && code2 === 40) {
      effects.consume(code2);
      balance++;
      return raw;
    }
    if (code2 === 41) {
      effects.consume(code2);
      balance--;
      return raw;
    }
    if (code2 === null || code2 === 32 || code2 === 40 || asciiControl(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? rawEscape : raw;
  }
  function rawEscape(code2) {
    if (code2 === 40 || code2 === 41 || code2 === 92) {
      effects.consume(code2);
      return raw;
    }
    return raw(code2);
  }
}
function factoryLabel(effects, ok2, nok, type, markerType, stringType) {
  const self2 = this;
  let size = 0;
  let seen;
  return start;
  function start(code2) {
    effects.enter(type);
    effects.enter(markerType);
    effects.consume(code2);
    effects.exit(markerType);
    effects.enter(stringType);
    return atBreak;
  }
  function atBreak(code2) {
    if (size > 999 || code2 === null || code2 === 91 || code2 === 93 && !seen || // To do: remove in the future once we’ve switched from
    // `micromark-extension-footnote` to `micromark-extension-gfm-footnote`,
    // which doesn’t need this.
    // Hidden footnotes hook.
    /* c8 ignore next 3 */
    code2 === 94 && !size && "_hiddenFootnoteSupport" in self2.parser.constructs) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.exit(stringType);
      effects.enter(markerType);
      effects.consume(code2);
      effects.exit(markerType);
      effects.exit(type);
      return ok2;
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return atBreak;
    }
    effects.enter("chunkString", {
      contentType: "string"
    });
    return labelInside(code2);
  }
  function labelInside(code2) {
    if (code2 === null || code2 === 91 || code2 === 93 || markdownLineEnding(code2) || size++ > 999) {
      effects.exit("chunkString");
      return atBreak(code2);
    }
    effects.consume(code2);
    if (!seen) seen = !markdownSpace(code2);
    return code2 === 92 ? labelEscape : labelInside;
  }
  function labelEscape(code2) {
    if (code2 === 91 || code2 === 92 || code2 === 93) {
      effects.consume(code2);
      size++;
      return labelInside;
    }
    return labelInside(code2);
  }
}
function factoryTitle(effects, ok2, nok, type, markerType, stringType) {
  let marker;
  return start;
  function start(code2) {
    if (code2 === 34 || code2 === 39 || code2 === 40) {
      effects.enter(type);
      effects.enter(markerType);
      effects.consume(code2);
      effects.exit(markerType);
      marker = code2 === 40 ? 41 : code2;
      return begin;
    }
    return nok(code2);
  }
  function begin(code2) {
    if (code2 === marker) {
      effects.enter(markerType);
      effects.consume(code2);
      effects.exit(markerType);
      effects.exit(type);
      return ok2;
    }
    effects.enter(stringType);
    return atBreak(code2);
  }
  function atBreak(code2) {
    if (code2 === marker) {
      effects.exit(stringType);
      return begin(marker);
    }
    if (code2 === null) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return factorySpace(effects, atBreak, "linePrefix");
    }
    effects.enter("chunkString", {
      contentType: "string"
    });
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === marker || code2 === null || markdownLineEnding(code2)) {
      effects.exit("chunkString");
      return atBreak(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? escape : inside;
  }
  function escape(code2) {
    if (code2 === marker || code2 === 92) {
      effects.consume(code2);
      return inside;
    }
    return inside(code2);
  }
}
function factoryWhitespace(effects, ok2) {
  let seen;
  return start;
  function start(code2) {
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      seen = true;
      return start;
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, start, seen ? "linePrefix" : "lineSuffix")(code2);
    }
    return ok2(code2);
  }
}
const definition$1 = {
  name: "definition",
  tokenize: tokenizeDefinition
};
const titleBefore = {
  partial: true,
  tokenize: tokenizeTitleBefore
};
function tokenizeDefinition(effects, ok2, nok) {
  const self2 = this;
  let identifier;
  return start;
  function start(code2) {
    effects.enter("definition");
    return before(code2);
  }
  function before(code2) {
    return factoryLabel.call(
      self2,
      effects,
      labelAfter,
      // Note: we don’t need to reset the way `markdown-rs` does.
      nok,
      "definitionLabel",
      "definitionLabelMarker",
      "definitionLabelString"
    )(code2);
  }
  function labelAfter(code2) {
    identifier = normalizeIdentifier(self2.sliceSerialize(self2.events[self2.events.length - 1][1]).slice(1, -1));
    if (code2 === 58) {
      effects.enter("definitionMarker");
      effects.consume(code2);
      effects.exit("definitionMarker");
      return markerAfter;
    }
    return nok(code2);
  }
  function markerAfter(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, destinationBefore)(code2) : destinationBefore(code2);
  }
  function destinationBefore(code2) {
    return factoryDestination(
      effects,
      destinationAfter,
      // Note: we don’t need to reset the way `markdown-rs` does.
      nok,
      "definitionDestination",
      "definitionDestinationLiteral",
      "definitionDestinationLiteralMarker",
      "definitionDestinationRaw",
      "definitionDestinationString"
    )(code2);
  }
  function destinationAfter(code2) {
    return effects.attempt(titleBefore, after, after)(code2);
  }
  function after(code2) {
    return markdownSpace(code2) ? factorySpace(effects, afterWhitespace, "whitespace")(code2) : afterWhitespace(code2);
  }
  function afterWhitespace(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("definition");
      self2.parser.defined.push(identifier);
      return ok2(code2);
    }
    return nok(code2);
  }
}
function tokenizeTitleBefore(effects, ok2, nok) {
  return titleBefore2;
  function titleBefore2(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, beforeMarker)(code2) : nok(code2);
  }
  function beforeMarker(code2) {
    return factoryTitle(effects, titleAfter, nok, "definitionTitle", "definitionTitleMarker", "definitionTitleString")(code2);
  }
  function titleAfter(code2) {
    return markdownSpace(code2) ? factorySpace(effects, titleAfterOptionalWhitespace, "whitespace")(code2) : titleAfterOptionalWhitespace(code2);
  }
  function titleAfterOptionalWhitespace(code2) {
    return code2 === null || markdownLineEnding(code2) ? ok2(code2) : nok(code2);
  }
}
const hardBreakEscape = {
  name: "hardBreakEscape",
  tokenize: tokenizeHardBreakEscape
};
function tokenizeHardBreakEscape(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.enter("hardBreakEscape");
    effects.consume(code2);
    return after;
  }
  function after(code2) {
    if (markdownLineEnding(code2)) {
      effects.exit("hardBreakEscape");
      return ok2(code2);
    }
    return nok(code2);
  }
}
const headingAtx = {
  name: "headingAtx",
  resolve: resolveHeadingAtx,
  tokenize: tokenizeHeadingAtx
};
function resolveHeadingAtx(events2, context) {
  let contentEnd = events2.length - 2;
  let contentStart = 3;
  let content2;
  let text2;
  if (events2[contentStart][1].type === "whitespace") {
    contentStart += 2;
  }
  if (contentEnd - 2 > contentStart && events2[contentEnd][1].type === "whitespace") {
    contentEnd -= 2;
  }
  if (events2[contentEnd][1].type === "atxHeadingSequence" && (contentStart === contentEnd - 1 || contentEnd - 4 > contentStart && events2[contentEnd - 2][1].type === "whitespace")) {
    contentEnd -= contentStart + 1 === contentEnd ? 2 : 4;
  }
  if (contentEnd > contentStart) {
    content2 = {
      type: "atxHeadingText",
      start: events2[contentStart][1].start,
      end: events2[contentEnd][1].end
    };
    text2 = {
      type: "chunkText",
      start: events2[contentStart][1].start,
      end: events2[contentEnd][1].end,
      contentType: "text"
    };
    splice(events2, contentStart, contentEnd - contentStart + 1, [["enter", content2, context], ["enter", text2, context], ["exit", text2, context], ["exit", content2, context]]);
  }
  return events2;
}
function tokenizeHeadingAtx(effects, ok2, nok) {
  let size = 0;
  return start;
  function start(code2) {
    effects.enter("atxHeading");
    return before(code2);
  }
  function before(code2) {
    effects.enter("atxHeadingSequence");
    return sequenceOpen(code2);
  }
  function sequenceOpen(code2) {
    if (code2 === 35 && size++ < 6) {
      effects.consume(code2);
      return sequenceOpen;
    }
    if (code2 === null || markdownLineEndingOrSpace(code2)) {
      effects.exit("atxHeadingSequence");
      return atBreak(code2);
    }
    return nok(code2);
  }
  function atBreak(code2) {
    if (code2 === 35) {
      effects.enter("atxHeadingSequence");
      return sequenceFurther(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("atxHeading");
      return ok2(code2);
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, atBreak, "whitespace")(code2);
    }
    effects.enter("atxHeadingText");
    return data2(code2);
  }
  function sequenceFurther(code2) {
    if (code2 === 35) {
      effects.consume(code2);
      return sequenceFurther;
    }
    effects.exit("atxHeadingSequence");
    return atBreak(code2);
  }
  function data2(code2) {
    if (code2 === null || code2 === 35 || markdownLineEndingOrSpace(code2)) {
      effects.exit("atxHeadingText");
      return atBreak(code2);
    }
    effects.consume(code2);
    return data2;
  }
}
const htmlBlockNames = [
  "address",
  "article",
  "aside",
  "base",
  "basefont",
  "blockquote",
  "body",
  "caption",
  "center",
  "col",
  "colgroup",
  "dd",
  "details",
  "dialog",
  "dir",
  "div",
  "dl",
  "dt",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "frame",
  "frameset",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hr",
  "html",
  "iframe",
  "legend",
  "li",
  "link",
  "main",
  "menu",
  "menuitem",
  "nav",
  "noframes",
  "ol",
  "optgroup",
  "option",
  "p",
  "param",
  "search",
  "section",
  "summary",
  "table",
  "tbody",
  "td",
  "tfoot",
  "th",
  "thead",
  "title",
  "tr",
  "track",
  "ul"
];
const htmlRawNames = ["pre", "script", "style", "textarea"];
const htmlFlow = {
  concrete: true,
  name: "htmlFlow",
  resolveTo: resolveToHtmlFlow,
  tokenize: tokenizeHtmlFlow
};
const blankLineBefore = {
  partial: true,
  tokenize: tokenizeBlankLineBefore
};
const nonLazyContinuationStart = {
  partial: true,
  tokenize: tokenizeNonLazyContinuationStart
};
function resolveToHtmlFlow(events2) {
  let index2 = events2.length;
  while (index2--) {
    if (events2[index2][0] === "enter" && events2[index2][1].type === "htmlFlow") {
      break;
    }
  }
  if (index2 > 1 && events2[index2 - 2][1].type === "linePrefix") {
    events2[index2][1].start = events2[index2 - 2][1].start;
    events2[index2 + 1][1].start = events2[index2 - 2][1].start;
    events2.splice(index2 - 2, 2);
  }
  return events2;
}
function tokenizeHtmlFlow(effects, ok2, nok) {
  const self2 = this;
  let marker;
  let closingTag;
  let buffer;
  let index2;
  let markerB;
  return start;
  function start(code2) {
    return before(code2);
  }
  function before(code2) {
    effects.enter("htmlFlow");
    effects.enter("htmlFlowData");
    effects.consume(code2);
    return open;
  }
  function open(code2) {
    if (code2 === 33) {
      effects.consume(code2);
      return declarationOpen;
    }
    if (code2 === 47) {
      effects.consume(code2);
      closingTag = true;
      return tagCloseStart;
    }
    if (code2 === 63) {
      effects.consume(code2);
      marker = 3;
      return self2.interrupt ? ok2 : continuationDeclarationInside;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      buffer = String.fromCharCode(code2);
      return tagName;
    }
    return nok(code2);
  }
  function declarationOpen(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      marker = 2;
      return commentOpenInside;
    }
    if (code2 === 91) {
      effects.consume(code2);
      marker = 5;
      index2 = 0;
      return cdataOpenInside;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      marker = 4;
      return self2.interrupt ? ok2 : continuationDeclarationInside;
    }
    return nok(code2);
  }
  function commentOpenInside(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return self2.interrupt ? ok2 : continuationDeclarationInside;
    }
    return nok(code2);
  }
  function cdataOpenInside(code2) {
    const value = "CDATA[";
    if (code2 === value.charCodeAt(index2++)) {
      effects.consume(code2);
      if (index2 === value.length) {
        return self2.interrupt ? ok2 : continuation;
      }
      return cdataOpenInside;
    }
    return nok(code2);
  }
  function tagCloseStart(code2) {
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      buffer = String.fromCharCode(code2);
      return tagName;
    }
    return nok(code2);
  }
  function tagName(code2) {
    if (code2 === null || code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      const slash = code2 === 47;
      const name2 = buffer.toLowerCase();
      if (!slash && !closingTag && htmlRawNames.includes(name2)) {
        marker = 1;
        return self2.interrupt ? ok2(code2) : continuation(code2);
      }
      if (htmlBlockNames.includes(buffer.toLowerCase())) {
        marker = 6;
        if (slash) {
          effects.consume(code2);
          return basicSelfClosing;
        }
        return self2.interrupt ? ok2(code2) : continuation(code2);
      }
      marker = 7;
      return self2.interrupt && !self2.parser.lazy[self2.now().line] ? nok(code2) : closingTag ? completeClosingTagAfter(code2) : completeAttributeNameBefore(code2);
    }
    if (code2 === 45 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      buffer += String.fromCharCode(code2);
      return tagName;
    }
    return nok(code2);
  }
  function basicSelfClosing(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      return self2.interrupt ? ok2 : continuation;
    }
    return nok(code2);
  }
  function completeClosingTagAfter(code2) {
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeClosingTagAfter;
    }
    return completeEnd(code2);
  }
  function completeAttributeNameBefore(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      return completeEnd;
    }
    if (code2 === 58 || code2 === 95 || asciiAlpha(code2)) {
      effects.consume(code2);
      return completeAttributeName;
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAttributeNameBefore;
    }
    return completeEnd(code2);
  }
  function completeAttributeName(code2) {
    if (code2 === 45 || code2 === 46 || code2 === 58 || code2 === 95 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return completeAttributeName;
    }
    return completeAttributeNameAfter(code2);
  }
  function completeAttributeNameAfter(code2) {
    if (code2 === 61) {
      effects.consume(code2);
      return completeAttributeValueBefore;
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAttributeNameAfter;
    }
    return completeAttributeNameBefore(code2);
  }
  function completeAttributeValueBefore(code2) {
    if (code2 === null || code2 === 60 || code2 === 61 || code2 === 62 || code2 === 96) {
      return nok(code2);
    }
    if (code2 === 34 || code2 === 39) {
      effects.consume(code2);
      markerB = code2;
      return completeAttributeValueQuoted;
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAttributeValueBefore;
    }
    return completeAttributeValueUnquoted(code2);
  }
  function completeAttributeValueQuoted(code2) {
    if (code2 === markerB) {
      effects.consume(code2);
      markerB = null;
      return completeAttributeValueQuotedAfter;
    }
    if (code2 === null || markdownLineEnding(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return completeAttributeValueQuoted;
  }
  function completeAttributeValueUnquoted(code2) {
    if (code2 === null || code2 === 34 || code2 === 39 || code2 === 47 || code2 === 60 || code2 === 61 || code2 === 62 || code2 === 96 || markdownLineEndingOrSpace(code2)) {
      return completeAttributeNameAfter(code2);
    }
    effects.consume(code2);
    return completeAttributeValueUnquoted;
  }
  function completeAttributeValueQuotedAfter(code2) {
    if (code2 === 47 || code2 === 62 || markdownSpace(code2)) {
      return completeAttributeNameBefore(code2);
    }
    return nok(code2);
  }
  function completeEnd(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      return completeAfter;
    }
    return nok(code2);
  }
  function completeAfter(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return continuation(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAfter;
    }
    return nok(code2);
  }
  function continuation(code2) {
    if (code2 === 45 && marker === 2) {
      effects.consume(code2);
      return continuationCommentInside;
    }
    if (code2 === 60 && marker === 1) {
      effects.consume(code2);
      return continuationRawTagOpen;
    }
    if (code2 === 62 && marker === 4) {
      effects.consume(code2);
      return continuationClose;
    }
    if (code2 === 63 && marker === 3) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    if (code2 === 93 && marker === 5) {
      effects.consume(code2);
      return continuationCdataInside;
    }
    if (markdownLineEnding(code2) && (marker === 6 || marker === 7)) {
      effects.exit("htmlFlowData");
      return effects.check(blankLineBefore, continuationAfter, continuationStart)(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("htmlFlowData");
      return continuationStart(code2);
    }
    effects.consume(code2);
    return continuation;
  }
  function continuationStart(code2) {
    return effects.check(nonLazyContinuationStart, continuationStartNonLazy, continuationAfter)(code2);
  }
  function continuationStartNonLazy(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return continuationBefore;
  }
  function continuationBefore(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return continuationStart(code2);
    }
    effects.enter("htmlFlowData");
    return continuation(code2);
  }
  function continuationCommentInside(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    return continuation(code2);
  }
  function continuationRawTagOpen(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      buffer = "";
      return continuationRawEndTag;
    }
    return continuation(code2);
  }
  function continuationRawEndTag(code2) {
    if (code2 === 62) {
      const name2 = buffer.toLowerCase();
      if (htmlRawNames.includes(name2)) {
        effects.consume(code2);
        return continuationClose;
      }
      return continuation(code2);
    }
    if (asciiAlpha(code2) && buffer.length < 8) {
      effects.consume(code2);
      buffer += String.fromCharCode(code2);
      return continuationRawEndTag;
    }
    return continuation(code2);
  }
  function continuationCdataInside(code2) {
    if (code2 === 93) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    return continuation(code2);
  }
  function continuationDeclarationInside(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      return continuationClose;
    }
    if (code2 === 45 && marker === 2) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    return continuation(code2);
  }
  function continuationClose(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("htmlFlowData");
      return continuationAfter(code2);
    }
    effects.consume(code2);
    return continuationClose;
  }
  function continuationAfter(code2) {
    effects.exit("htmlFlow");
    return ok2(code2);
  }
}
function tokenizeNonLazyContinuationStart(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    return self2.parser.lazy[self2.now().line] ? nok(code2) : ok2(code2);
  }
}
function tokenizeBlankLineBefore(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return effects.attempt(blankLine, ok2, nok);
  }
}
const htmlText = {
  name: "htmlText",
  tokenize: tokenizeHtmlText
};
function tokenizeHtmlText(effects, ok2, nok) {
  const self2 = this;
  let marker;
  let index2;
  let returnState;
  return start;
  function start(code2) {
    effects.enter("htmlText");
    effects.enter("htmlTextData");
    effects.consume(code2);
    return open;
  }
  function open(code2) {
    if (code2 === 33) {
      effects.consume(code2);
      return declarationOpen;
    }
    if (code2 === 47) {
      effects.consume(code2);
      return tagCloseStart;
    }
    if (code2 === 63) {
      effects.consume(code2);
      return instruction;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return tagOpen;
    }
    return nok(code2);
  }
  function declarationOpen(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return commentOpenInside;
    }
    if (code2 === 91) {
      effects.consume(code2);
      index2 = 0;
      return cdataOpenInside;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return declaration;
    }
    return nok(code2);
  }
  function commentOpenInside(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return commentEnd;
    }
    return nok(code2);
  }
  function comment(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 45) {
      effects.consume(code2);
      return commentClose;
    }
    if (markdownLineEnding(code2)) {
      returnState = comment;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return comment;
  }
  function commentClose(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return commentEnd;
    }
    return comment(code2);
  }
  function commentEnd(code2) {
    return code2 === 62 ? end(code2) : code2 === 45 ? commentClose(code2) : comment(code2);
  }
  function cdataOpenInside(code2) {
    const value = "CDATA[";
    if (code2 === value.charCodeAt(index2++)) {
      effects.consume(code2);
      return index2 === value.length ? cdata : cdataOpenInside;
    }
    return nok(code2);
  }
  function cdata(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.consume(code2);
      return cdataClose;
    }
    if (markdownLineEnding(code2)) {
      returnState = cdata;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return cdata;
  }
  function cdataClose(code2) {
    if (code2 === 93) {
      effects.consume(code2);
      return cdataEnd;
    }
    return cdata(code2);
  }
  function cdataEnd(code2) {
    if (code2 === 62) {
      return end(code2);
    }
    if (code2 === 93) {
      effects.consume(code2);
      return cdataEnd;
    }
    return cdata(code2);
  }
  function declaration(code2) {
    if (code2 === null || code2 === 62) {
      return end(code2);
    }
    if (markdownLineEnding(code2)) {
      returnState = declaration;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return declaration;
  }
  function instruction(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 63) {
      effects.consume(code2);
      return instructionClose;
    }
    if (markdownLineEnding(code2)) {
      returnState = instruction;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return instruction;
  }
  function instructionClose(code2) {
    return code2 === 62 ? end(code2) : instruction(code2);
  }
  function tagCloseStart(code2) {
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return tagClose;
    }
    return nok(code2);
  }
  function tagClose(code2) {
    if (code2 === 45 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return tagClose;
    }
    return tagCloseBetween(code2);
  }
  function tagCloseBetween(code2) {
    if (markdownLineEnding(code2)) {
      returnState = tagCloseBetween;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagCloseBetween;
    }
    return end(code2);
  }
  function tagOpen(code2) {
    if (code2 === 45 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return tagOpen;
    }
    if (code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      return tagOpenBetween(code2);
    }
    return nok(code2);
  }
  function tagOpenBetween(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      return end;
    }
    if (code2 === 58 || code2 === 95 || asciiAlpha(code2)) {
      effects.consume(code2);
      return tagOpenAttributeName;
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenBetween;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagOpenBetween;
    }
    return end(code2);
  }
  function tagOpenAttributeName(code2) {
    if (code2 === 45 || code2 === 46 || code2 === 58 || code2 === 95 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return tagOpenAttributeName;
    }
    return tagOpenAttributeNameAfter(code2);
  }
  function tagOpenAttributeNameAfter(code2) {
    if (code2 === 61) {
      effects.consume(code2);
      return tagOpenAttributeValueBefore;
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenAttributeNameAfter;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagOpenAttributeNameAfter;
    }
    return tagOpenBetween(code2);
  }
  function tagOpenAttributeValueBefore(code2) {
    if (code2 === null || code2 === 60 || code2 === 61 || code2 === 62 || code2 === 96) {
      return nok(code2);
    }
    if (code2 === 34 || code2 === 39) {
      effects.consume(code2);
      marker = code2;
      return tagOpenAttributeValueQuoted;
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenAttributeValueBefore;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagOpenAttributeValueBefore;
    }
    effects.consume(code2);
    return tagOpenAttributeValueUnquoted;
  }
  function tagOpenAttributeValueQuoted(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      marker = void 0;
      return tagOpenAttributeValueQuotedAfter;
    }
    if (code2 === null) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenAttributeValueQuoted;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return tagOpenAttributeValueQuoted;
  }
  function tagOpenAttributeValueUnquoted(code2) {
    if (code2 === null || code2 === 34 || code2 === 39 || code2 === 60 || code2 === 61 || code2 === 96) {
      return nok(code2);
    }
    if (code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      return tagOpenBetween(code2);
    }
    effects.consume(code2);
    return tagOpenAttributeValueUnquoted;
  }
  function tagOpenAttributeValueQuotedAfter(code2) {
    if (code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      return tagOpenBetween(code2);
    }
    return nok(code2);
  }
  function end(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      effects.exit("htmlTextData");
      effects.exit("htmlText");
      return ok2;
    }
    return nok(code2);
  }
  function lineEndingBefore(code2) {
    effects.exit("htmlTextData");
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return lineEndingAfter;
  }
  function lineEndingAfter(code2) {
    return markdownSpace(code2) ? factorySpace(effects, lineEndingAfterPrefix, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2) : lineEndingAfterPrefix(code2);
  }
  function lineEndingAfterPrefix(code2) {
    effects.enter("htmlTextData");
    return returnState(code2);
  }
}
const labelEnd = {
  name: "labelEnd",
  resolveAll: resolveAllLabelEnd,
  resolveTo: resolveToLabelEnd,
  tokenize: tokenizeLabelEnd
};
const resourceConstruct = {
  tokenize: tokenizeResource
};
const referenceFullConstruct = {
  tokenize: tokenizeReferenceFull
};
const referenceCollapsedConstruct = {
  tokenize: tokenizeReferenceCollapsed
};
function resolveAllLabelEnd(events2) {
  let index2 = -1;
  const newEvents = [];
  while (++index2 < events2.length) {
    const token = events2[index2][1];
    newEvents.push(events2[index2]);
    if (token.type === "labelImage" || token.type === "labelLink" || token.type === "labelEnd") {
      const offset = token.type === "labelImage" ? 4 : 2;
      token.type = "data";
      index2 += offset;
    }
  }
  if (events2.length !== newEvents.length) {
    splice(events2, 0, events2.length, newEvents);
  }
  return events2;
}
function resolveToLabelEnd(events2, context) {
  let index2 = events2.length;
  let offset = 0;
  let token;
  let open;
  let close;
  let media;
  while (index2--) {
    token = events2[index2][1];
    if (open) {
      if (token.type === "link" || token.type === "labelLink" && token._inactive) {
        break;
      }
      if (events2[index2][0] === "enter" && token.type === "labelLink") {
        token._inactive = true;
      }
    } else if (close) {
      if (events2[index2][0] === "enter" && (token.type === "labelImage" || token.type === "labelLink") && !token._balanced) {
        open = index2;
        if (token.type !== "labelLink") {
          offset = 2;
          break;
        }
      }
    } else if (token.type === "labelEnd") {
      close = index2;
    }
  }
  const group = {
    type: events2[open][1].type === "labelLink" ? "link" : "image",
    start: {
      ...events2[open][1].start
    },
    end: {
      ...events2[events2.length - 1][1].end
    }
  };
  const label = {
    type: "label",
    start: {
      ...events2[open][1].start
    },
    end: {
      ...events2[close][1].end
    }
  };
  const text2 = {
    type: "labelText",
    start: {
      ...events2[open + offset + 2][1].end
    },
    end: {
      ...events2[close - 2][1].start
    }
  };
  media = [["enter", group, context], ["enter", label, context]];
  media = push(media, events2.slice(open + 1, open + offset + 3));
  media = push(media, [["enter", text2, context]]);
  media = push(media, resolveAll(context.parser.constructs.insideSpan.null, events2.slice(open + offset + 4, close - 3), context));
  media = push(media, [["exit", text2, context], events2[close - 2], events2[close - 1], ["exit", label, context]]);
  media = push(media, events2.slice(close + 1));
  media = push(media, [["exit", group, context]]);
  splice(events2, open, events2.length, media);
  return events2;
}
function tokenizeLabelEnd(effects, ok2, nok) {
  const self2 = this;
  let index2 = self2.events.length;
  let labelStart;
  let defined;
  while (index2--) {
    if ((self2.events[index2][1].type === "labelImage" || self2.events[index2][1].type === "labelLink") && !self2.events[index2][1]._balanced) {
      labelStart = self2.events[index2][1];
      break;
    }
  }
  return start;
  function start(code2) {
    if (!labelStart) {
      return nok(code2);
    }
    if (labelStart._inactive) {
      return labelEndNok(code2);
    }
    defined = self2.parser.defined.includes(normalizeIdentifier(self2.sliceSerialize({
      start: labelStart.end,
      end: self2.now()
    })));
    effects.enter("labelEnd");
    effects.enter("labelMarker");
    effects.consume(code2);
    effects.exit("labelMarker");
    effects.exit("labelEnd");
    return after;
  }
  function after(code2) {
    if (code2 === 40) {
      return effects.attempt(resourceConstruct, labelEndOk, defined ? labelEndOk : labelEndNok)(code2);
    }
    if (code2 === 91) {
      return effects.attempt(referenceFullConstruct, labelEndOk, defined ? referenceNotFull : labelEndNok)(code2);
    }
    return defined ? labelEndOk(code2) : labelEndNok(code2);
  }
  function referenceNotFull(code2) {
    return effects.attempt(referenceCollapsedConstruct, labelEndOk, labelEndNok)(code2);
  }
  function labelEndOk(code2) {
    return ok2(code2);
  }
  function labelEndNok(code2) {
    labelStart._balanced = true;
    return nok(code2);
  }
}
function tokenizeResource(effects, ok2, nok) {
  return resourceStart;
  function resourceStart(code2) {
    effects.enter("resource");
    effects.enter("resourceMarker");
    effects.consume(code2);
    effects.exit("resourceMarker");
    return resourceBefore;
  }
  function resourceBefore(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, resourceOpen)(code2) : resourceOpen(code2);
  }
  function resourceOpen(code2) {
    if (code2 === 41) {
      return resourceEnd(code2);
    }
    return factoryDestination(effects, resourceDestinationAfter, resourceDestinationMissing, "resourceDestination", "resourceDestinationLiteral", "resourceDestinationLiteralMarker", "resourceDestinationRaw", "resourceDestinationString", 32)(code2);
  }
  function resourceDestinationAfter(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, resourceBetween)(code2) : resourceEnd(code2);
  }
  function resourceDestinationMissing(code2) {
    return nok(code2);
  }
  function resourceBetween(code2) {
    if (code2 === 34 || code2 === 39 || code2 === 40) {
      return factoryTitle(effects, resourceTitleAfter, nok, "resourceTitle", "resourceTitleMarker", "resourceTitleString")(code2);
    }
    return resourceEnd(code2);
  }
  function resourceTitleAfter(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, resourceEnd)(code2) : resourceEnd(code2);
  }
  function resourceEnd(code2) {
    if (code2 === 41) {
      effects.enter("resourceMarker");
      effects.consume(code2);
      effects.exit("resourceMarker");
      effects.exit("resource");
      return ok2;
    }
    return nok(code2);
  }
}
function tokenizeReferenceFull(effects, ok2, nok) {
  const self2 = this;
  return referenceFull;
  function referenceFull(code2) {
    return factoryLabel.call(self2, effects, referenceFullAfter, referenceFullMissing, "reference", "referenceMarker", "referenceString")(code2);
  }
  function referenceFullAfter(code2) {
    return self2.parser.defined.includes(normalizeIdentifier(self2.sliceSerialize(self2.events[self2.events.length - 1][1]).slice(1, -1))) ? ok2(code2) : nok(code2);
  }
  function referenceFullMissing(code2) {
    return nok(code2);
  }
}
function tokenizeReferenceCollapsed(effects, ok2, nok) {
  return referenceCollapsedStart;
  function referenceCollapsedStart(code2) {
    effects.enter("reference");
    effects.enter("referenceMarker");
    effects.consume(code2);
    effects.exit("referenceMarker");
    return referenceCollapsedOpen;
  }
  function referenceCollapsedOpen(code2) {
    if (code2 === 93) {
      effects.enter("referenceMarker");
      effects.consume(code2);
      effects.exit("referenceMarker");
      effects.exit("reference");
      return ok2;
    }
    return nok(code2);
  }
}
const labelStartImage = {
  name: "labelStartImage",
  resolveAll: labelEnd.resolveAll,
  tokenize: tokenizeLabelStartImage
};
function tokenizeLabelStartImage(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    effects.enter("labelImage");
    effects.enter("labelImageMarker");
    effects.consume(code2);
    effects.exit("labelImageMarker");
    return open;
  }
  function open(code2) {
    if (code2 === 91) {
      effects.enter("labelMarker");
      effects.consume(code2);
      effects.exit("labelMarker");
      effects.exit("labelImage");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    return code2 === 94 && "_hiddenFootnoteSupport" in self2.parser.constructs ? nok(code2) : ok2(code2);
  }
}
const labelStartLink = {
  name: "labelStartLink",
  resolveAll: labelEnd.resolveAll,
  tokenize: tokenizeLabelStartLink
};
function tokenizeLabelStartLink(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    effects.enter("labelLink");
    effects.enter("labelMarker");
    effects.consume(code2);
    effects.exit("labelMarker");
    effects.exit("labelLink");
    return after;
  }
  function after(code2) {
    return code2 === 94 && "_hiddenFootnoteSupport" in self2.parser.constructs ? nok(code2) : ok2(code2);
  }
}
const lineEnding = {
  name: "lineEnding",
  tokenize: tokenizeLineEnding
};
function tokenizeLineEnding(effects, ok2) {
  return start;
  function start(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return factorySpace(effects, ok2, "linePrefix");
  }
}
const thematicBreak$2 = {
  name: "thematicBreak",
  tokenize: tokenizeThematicBreak
};
function tokenizeThematicBreak(effects, ok2, nok) {
  let size = 0;
  let marker;
  return start;
  function start(code2) {
    effects.enter("thematicBreak");
    return before(code2);
  }
  function before(code2) {
    marker = code2;
    return atBreak(code2);
  }
  function atBreak(code2) {
    if (code2 === marker) {
      effects.enter("thematicBreakSequence");
      return sequence(code2);
    }
    if (size >= 3 && (code2 === null || markdownLineEnding(code2))) {
      effects.exit("thematicBreak");
      return ok2(code2);
    }
    return nok(code2);
  }
  function sequence(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      size++;
      return sequence;
    }
    effects.exit("thematicBreakSequence");
    return markdownSpace(code2) ? factorySpace(effects, atBreak, "whitespace")(code2) : atBreak(code2);
  }
}
const list$2 = {
  continuation: {
    tokenize: tokenizeListContinuation
  },
  exit: tokenizeListEnd,
  name: "list",
  tokenize: tokenizeListStart
};
const listItemPrefixWhitespaceConstruct = {
  partial: true,
  tokenize: tokenizeListItemPrefixWhitespace
};
const indentConstruct = {
  partial: true,
  tokenize: tokenizeIndent$1
};
function tokenizeListStart(effects, ok2, nok) {
  const self2 = this;
  const tail = self2.events[self2.events.length - 1];
  let initialSize = tail && tail[1].type === "linePrefix" ? tail[2].sliceSerialize(tail[1], true).length : 0;
  let size = 0;
  return start;
  function start(code2) {
    const kind = self2.containerState.type || (code2 === 42 || code2 === 43 || code2 === 45 ? "listUnordered" : "listOrdered");
    if (kind === "listUnordered" ? !self2.containerState.marker || code2 === self2.containerState.marker : asciiDigit(code2)) {
      if (!self2.containerState.type) {
        self2.containerState.type = kind;
        effects.enter(kind, {
          _container: true
        });
      }
      if (kind === "listUnordered") {
        effects.enter("listItemPrefix");
        return code2 === 42 || code2 === 45 ? effects.check(thematicBreak$2, nok, atMarker)(code2) : atMarker(code2);
      }
      if (!self2.interrupt || code2 === 49) {
        effects.enter("listItemPrefix");
        effects.enter("listItemValue");
        return inside(code2);
      }
    }
    return nok(code2);
  }
  function inside(code2) {
    if (asciiDigit(code2) && ++size < 10) {
      effects.consume(code2);
      return inside;
    }
    if ((!self2.interrupt || size < 2) && (self2.containerState.marker ? code2 === self2.containerState.marker : code2 === 41 || code2 === 46)) {
      effects.exit("listItemValue");
      return atMarker(code2);
    }
    return nok(code2);
  }
  function atMarker(code2) {
    effects.enter("listItemMarker");
    effects.consume(code2);
    effects.exit("listItemMarker");
    self2.containerState.marker = self2.containerState.marker || code2;
    return effects.check(
      blankLine,
      // Can’t be empty when interrupting.
      self2.interrupt ? nok : onBlank,
      effects.attempt(listItemPrefixWhitespaceConstruct, endOfPrefix, otherPrefix)
    );
  }
  function onBlank(code2) {
    self2.containerState.initialBlankLine = true;
    initialSize++;
    return endOfPrefix(code2);
  }
  function otherPrefix(code2) {
    if (markdownSpace(code2)) {
      effects.enter("listItemPrefixWhitespace");
      effects.consume(code2);
      effects.exit("listItemPrefixWhitespace");
      return endOfPrefix;
    }
    return nok(code2);
  }
  function endOfPrefix(code2) {
    self2.containerState.size = initialSize + self2.sliceSerialize(effects.exit("listItemPrefix"), true).length;
    return ok2(code2);
  }
}
function tokenizeListContinuation(effects, ok2, nok) {
  const self2 = this;
  self2.containerState._closeFlow = void 0;
  return effects.check(blankLine, onBlank, notBlank);
  function onBlank(code2) {
    self2.containerState.furtherBlankLines = self2.containerState.furtherBlankLines || self2.containerState.initialBlankLine;
    return factorySpace(effects, ok2, "listItemIndent", self2.containerState.size + 1)(code2);
  }
  function notBlank(code2) {
    if (self2.containerState.furtherBlankLines || !markdownSpace(code2)) {
      self2.containerState.furtherBlankLines = void 0;
      self2.containerState.initialBlankLine = void 0;
      return notInCurrentItem(code2);
    }
    self2.containerState.furtherBlankLines = void 0;
    self2.containerState.initialBlankLine = void 0;
    return effects.attempt(indentConstruct, ok2, notInCurrentItem)(code2);
  }
  function notInCurrentItem(code2) {
    self2.containerState._closeFlow = true;
    self2.interrupt = void 0;
    return factorySpace(effects, effects.attempt(list$2, ok2, nok), "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2);
  }
}
function tokenizeIndent$1(effects, ok2, nok) {
  const self2 = this;
  return factorySpace(effects, afterPrefix, "listItemIndent", self2.containerState.size + 1);
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "listItemIndent" && tail[2].sliceSerialize(tail[1], true).length === self2.containerState.size ? ok2(code2) : nok(code2);
  }
}
function tokenizeListEnd(effects) {
  effects.exit(this.containerState.type);
}
function tokenizeListItemPrefixWhitespace(effects, ok2, nok) {
  const self2 = this;
  return factorySpace(effects, afterPrefix, "listItemPrefixWhitespace", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4 + 1);
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return !markdownSpace(code2) && tail && tail[1].type === "listItemPrefixWhitespace" ? ok2(code2) : nok(code2);
  }
}
const setextUnderline = {
  name: "setextUnderline",
  resolveTo: resolveToSetextUnderline,
  tokenize: tokenizeSetextUnderline
};
function resolveToSetextUnderline(events2, context) {
  let index2 = events2.length;
  let content2;
  let text2;
  let definition2;
  while (index2--) {
    if (events2[index2][0] === "enter") {
      if (events2[index2][1].type === "content") {
        content2 = index2;
        break;
      }
      if (events2[index2][1].type === "paragraph") {
        text2 = index2;
      }
    } else {
      if (events2[index2][1].type === "content") {
        events2.splice(index2, 1);
      }
      if (!definition2 && events2[index2][1].type === "definition") {
        definition2 = index2;
      }
    }
  }
  const heading2 = {
    type: "setextHeading",
    start: {
      ...events2[content2][1].start
    },
    end: {
      ...events2[events2.length - 1][1].end
    }
  };
  events2[text2][1].type = "setextHeadingText";
  if (definition2) {
    events2.splice(text2, 0, ["enter", heading2, context]);
    events2.splice(definition2 + 1, 0, ["exit", events2[content2][1], context]);
    events2[content2][1].end = {
      ...events2[definition2][1].end
    };
  } else {
    events2[content2][1] = heading2;
  }
  events2.push(["exit", heading2, context]);
  return events2;
}
function tokenizeSetextUnderline(effects, ok2, nok) {
  const self2 = this;
  let marker;
  return start;
  function start(code2) {
    let index2 = self2.events.length;
    let paragraph2;
    while (index2--) {
      if (self2.events[index2][1].type !== "lineEnding" && self2.events[index2][1].type !== "linePrefix" && self2.events[index2][1].type !== "content") {
        paragraph2 = self2.events[index2][1].type === "paragraph";
        break;
      }
    }
    if (!self2.parser.lazy[self2.now().line] && (self2.interrupt || paragraph2)) {
      effects.enter("setextHeadingLine");
      marker = code2;
      return before(code2);
    }
    return nok(code2);
  }
  function before(code2) {
    effects.enter("setextHeadingLineSequence");
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      return inside;
    }
    effects.exit("setextHeadingLineSequence");
    return markdownSpace(code2) ? factorySpace(effects, after, "lineSuffix")(code2) : after(code2);
  }
  function after(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("setextHeadingLine");
      return ok2(code2);
    }
    return nok(code2);
  }
}
const flow$1 = {
  tokenize: initializeFlow
};
function initializeFlow(effects) {
  const self2 = this;
  const initial = effects.attempt(
    // Try to parse a blank line.
    blankLine,
    atBlankEnding,
    // Try to parse initial flow (essentially, only code).
    effects.attempt(this.parser.constructs.flowInitial, afterConstruct, factorySpace(effects, effects.attempt(this.parser.constructs.flow, afterConstruct, effects.attempt(content, afterConstruct)), "linePrefix"))
  );
  return initial;
  function atBlankEnding(code2) {
    if (code2 === null) {
      effects.consume(code2);
      return;
    }
    effects.enter("lineEndingBlank");
    effects.consume(code2);
    effects.exit("lineEndingBlank");
    self2.currentConstruct = void 0;
    return initial;
  }
  function afterConstruct(code2) {
    if (code2 === null) {
      effects.consume(code2);
      return;
    }
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    self2.currentConstruct = void 0;
    return initial;
  }
}
const resolver = {
  resolveAll: createResolver()
};
const string$1 = initializeFactory("string");
const text$4 = initializeFactory("text");
function initializeFactory(field) {
  return {
    resolveAll: createResolver(field === "text" ? resolveAllLineSuffixes : void 0),
    tokenize: initializeText
  };
  function initializeText(effects) {
    const self2 = this;
    const constructs2 = this.parser.constructs[field];
    const text2 = effects.attempt(constructs2, start, notText);
    return start;
    function start(code2) {
      return atBreak(code2) ? text2(code2) : notText(code2);
    }
    function notText(code2) {
      if (code2 === null) {
        effects.consume(code2);
        return;
      }
      effects.enter("data");
      effects.consume(code2);
      return data2;
    }
    function data2(code2) {
      if (atBreak(code2)) {
        effects.exit("data");
        return text2(code2);
      }
      effects.consume(code2);
      return data2;
    }
    function atBreak(code2) {
      if (code2 === null) {
        return true;
      }
      const list2 = constructs2[code2];
      let index2 = -1;
      if (list2) {
        while (++index2 < list2.length) {
          const item = list2[index2];
          if (!item.previous || item.previous.call(self2, self2.previous)) {
            return true;
          }
        }
      }
      return false;
    }
  }
}
function createResolver(extraResolver) {
  return resolveAllText;
  function resolveAllText(events2, context) {
    let index2 = -1;
    let enter;
    while (++index2 <= events2.length) {
      if (enter === void 0) {
        if (events2[index2] && events2[index2][1].type === "data") {
          enter = index2;
          index2++;
        }
      } else if (!events2[index2] || events2[index2][1].type !== "data") {
        if (index2 !== enter + 2) {
          events2[enter][1].end = events2[index2 - 1][1].end;
          events2.splice(enter + 2, index2 - enter - 2);
          index2 = enter + 2;
        }
        enter = void 0;
      }
    }
    return extraResolver ? extraResolver(events2, context) : events2;
  }
}
function resolveAllLineSuffixes(events2, context) {
  let eventIndex = 0;
  while (++eventIndex <= events2.length) {
    if ((eventIndex === events2.length || events2[eventIndex][1].type === "lineEnding") && events2[eventIndex - 1][1].type === "data") {
      const data2 = events2[eventIndex - 1][1];
      const chunks = context.sliceStream(data2);
      let index2 = chunks.length;
      let bufferIndex = -1;
      let size = 0;
      let tabs;
      while (index2--) {
        const chunk = chunks[index2];
        if (typeof chunk === "string") {
          bufferIndex = chunk.length;
          while (chunk.charCodeAt(bufferIndex - 1) === 32) {
            size++;
            bufferIndex--;
          }
          if (bufferIndex) break;
          bufferIndex = -1;
        } else if (chunk === -2) {
          tabs = true;
          size++;
        } else if (chunk === -1) ;
        else {
          index2++;
          break;
        }
      }
      if (context._contentTypeTextTrailing && eventIndex === events2.length) {
        size = 0;
      }
      if (size) {
        const token = {
          type: eventIndex === events2.length || tabs || size < 2 ? "lineSuffix" : "hardBreakTrailing",
          start: {
            _bufferIndex: index2 ? bufferIndex : data2.start._bufferIndex + bufferIndex,
            _index: data2.start._index + index2,
            line: data2.end.line,
            column: data2.end.column - size,
            offset: data2.end.offset - size
          },
          end: {
            ...data2.end
          }
        };
        data2.end = {
          ...token.start
        };
        if (data2.start.offset === data2.end.offset) {
          Object.assign(data2, token);
        } else {
          events2.splice(eventIndex, 0, ["enter", token, context], ["exit", token, context]);
          eventIndex += 2;
        }
      }
      eventIndex++;
    }
  }
  return events2;
}
const document$1 = {
  [42]: list$2,
  [43]: list$2,
  [45]: list$2,
  [48]: list$2,
  [49]: list$2,
  [50]: list$2,
  [51]: list$2,
  [52]: list$2,
  [53]: list$2,
  [54]: list$2,
  [55]: list$2,
  [56]: list$2,
  [57]: list$2,
  [62]: blockQuote
};
const contentInitial = {
  [91]: definition$1
};
const flowInitial = {
  [-2]: codeIndented,
  [-1]: codeIndented,
  [32]: codeIndented
};
const flow = {
  [35]: headingAtx,
  [42]: thematicBreak$2,
  [45]: [setextUnderline, thematicBreak$2],
  [60]: htmlFlow,
  [61]: setextUnderline,
  [95]: thematicBreak$2,
  [96]: codeFenced,
  [126]: codeFenced
};
const string = {
  [38]: characterReference,
  [92]: characterEscape
};
const text$3 = {
  [-5]: lineEnding,
  [-4]: lineEnding,
  [-3]: lineEnding,
  [33]: labelStartImage,
  [38]: characterReference,
  [42]: attention,
  [60]: [autolink, htmlText],
  [91]: labelStartLink,
  [92]: [hardBreakEscape, characterEscape],
  [93]: labelEnd,
  [95]: attention,
  [96]: codeText
};
const insideSpan = {
  null: [attention, resolver]
};
const attentionMarkers = {
  null: [42, 95]
};
const disable = {
  null: []
};
const defaultConstructs = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  attentionMarkers,
  contentInitial,
  disable,
  document: document$1,
  flow,
  flowInitial,
  insideSpan,
  string,
  text: text$3
}, Symbol.toStringTag, { value: "Module" }));
function createTokenizer(parser, initialize, from) {
  let point2 = {
    _bufferIndex: -1,
    _index: 0,
    line: from && from.line || 1,
    column: from && from.column || 1,
    offset: from && from.offset || 0
  };
  const columnStart = {};
  const resolveAllConstructs = [];
  let chunks = [];
  let stack = [];
  const effects = {
    attempt: constructFactory(onsuccessfulconstruct),
    check: constructFactory(onsuccessfulcheck),
    consume,
    enter,
    exit: exit2,
    interrupt: constructFactory(onsuccessfulcheck, {
      interrupt: true
    })
  };
  const context = {
    code: null,
    containerState: {},
    defineSkip,
    events: [],
    now,
    parser,
    previous: null,
    sliceSerialize,
    sliceStream,
    write
  };
  let state = initialize.tokenize.call(context, effects);
  if (initialize.resolveAll) {
    resolveAllConstructs.push(initialize);
  }
  return context;
  function write(slice) {
    chunks = push(chunks, slice);
    main();
    if (chunks[chunks.length - 1] !== null) {
      return [];
    }
    addResult(initialize, 0);
    context.events = resolveAll(resolveAllConstructs, context.events, context);
    return context.events;
  }
  function sliceSerialize(token, expandTabs) {
    return serializeChunks(sliceStream(token), expandTabs);
  }
  function sliceStream(token) {
    return sliceChunks(chunks, token);
  }
  function now() {
    const {
      _bufferIndex,
      _index,
      line,
      column,
      offset
    } = point2;
    return {
      _bufferIndex,
      _index,
      line,
      column,
      offset
    };
  }
  function defineSkip(value) {
    columnStart[value.line] = value.column;
    accountForPotentialSkip();
  }
  function main() {
    let chunkIndex;
    while (point2._index < chunks.length) {
      const chunk = chunks[point2._index];
      if (typeof chunk === "string") {
        chunkIndex = point2._index;
        if (point2._bufferIndex < 0) {
          point2._bufferIndex = 0;
        }
        while (point2._index === chunkIndex && point2._bufferIndex < chunk.length) {
          go(chunk.charCodeAt(point2._bufferIndex));
        }
      } else {
        go(chunk);
      }
    }
  }
  function go(code2) {
    state = state(code2);
  }
  function consume(code2) {
    if (markdownLineEnding(code2)) {
      point2.line++;
      point2.column = 1;
      point2.offset += code2 === -3 ? 2 : 1;
      accountForPotentialSkip();
    } else if (code2 !== -1) {
      point2.column++;
      point2.offset++;
    }
    if (point2._bufferIndex < 0) {
      point2._index++;
    } else {
      point2._bufferIndex++;
      if (point2._bufferIndex === // Points w/ non-negative `_bufferIndex` reference
      // strings.
      /** @type {string} */
      chunks[point2._index].length) {
        point2._bufferIndex = -1;
        point2._index++;
      }
    }
    context.previous = code2;
  }
  function enter(type, fields) {
    const token = fields || {};
    token.type = type;
    token.start = now();
    context.events.push(["enter", token, context]);
    stack.push(token);
    return token;
  }
  function exit2(type) {
    const token = stack.pop();
    token.end = now();
    context.events.push(["exit", token, context]);
    return token;
  }
  function onsuccessfulconstruct(construct, info) {
    addResult(construct, info.from);
  }
  function onsuccessfulcheck(_, info) {
    info.restore();
  }
  function constructFactory(onreturn, fields) {
    return hook;
    function hook(constructs2, returnState, bogusState) {
      let listOfConstructs;
      let constructIndex;
      let currentConstruct;
      let info;
      return Array.isArray(constructs2) ? (
        /* c8 ignore next 1 */
        handleListOfConstructs(constructs2)
      ) : "tokenize" in constructs2 ? (
        // Looks like a construct.
        handleListOfConstructs([
          /** @type {Construct} */
          constructs2
        ])
      ) : handleMapOfConstructs(constructs2);
      function handleMapOfConstructs(map2) {
        return start;
        function start(code2) {
          const left = code2 !== null && map2[code2];
          const all2 = code2 !== null && map2.null;
          const list2 = [
            // To do: add more extension tests.
            /* c8 ignore next 2 */
            ...Array.isArray(left) ? left : left ? [left] : [],
            ...Array.isArray(all2) ? all2 : all2 ? [all2] : []
          ];
          return handleListOfConstructs(list2)(code2);
        }
      }
      function handleListOfConstructs(list2) {
        listOfConstructs = list2;
        constructIndex = 0;
        if (list2.length === 0) {
          return bogusState;
        }
        return handleConstruct(list2[constructIndex]);
      }
      function handleConstruct(construct) {
        return start;
        function start(code2) {
          info = store();
          currentConstruct = construct;
          if (!construct.partial) {
            context.currentConstruct = construct;
          }
          if (construct.name && context.parser.constructs.disable.null.includes(construct.name)) {
            return nok();
          }
          return construct.tokenize.call(
            // If we do have fields, create an object w/ `context` as its
            // prototype.
            // This allows a “live binding”, which is needed for `interrupt`.
            fields ? Object.assign(Object.create(context), fields) : context,
            effects,
            ok2,
            nok
          )(code2);
        }
      }
      function ok2(code2) {
        onreturn(currentConstruct, info);
        return returnState;
      }
      function nok(code2) {
        info.restore();
        if (++constructIndex < listOfConstructs.length) {
          return handleConstruct(listOfConstructs[constructIndex]);
        }
        return bogusState;
      }
    }
  }
  function addResult(construct, from2) {
    if (construct.resolveAll && !resolveAllConstructs.includes(construct)) {
      resolveAllConstructs.push(construct);
    }
    if (construct.resolve) {
      splice(context.events, from2, context.events.length - from2, construct.resolve(context.events.slice(from2), context));
    }
    if (construct.resolveTo) {
      context.events = construct.resolveTo(context.events, context);
    }
  }
  function store() {
    const startPoint = now();
    const startPrevious = context.previous;
    const startCurrentConstruct = context.currentConstruct;
    const startEventsIndex = context.events.length;
    const startStack = Array.from(stack);
    return {
      from: startEventsIndex,
      restore
    };
    function restore() {
      point2 = startPoint;
      context.previous = startPrevious;
      context.currentConstruct = startCurrentConstruct;
      context.events.length = startEventsIndex;
      stack = startStack;
      accountForPotentialSkip();
    }
  }
  function accountForPotentialSkip() {
    if (point2.line in columnStart && point2.column < 2) {
      point2.column = columnStart[point2.line];
      point2.offset += columnStart[point2.line] - 1;
    }
  }
}
function sliceChunks(chunks, token) {
  const startIndex = token.start._index;
  const startBufferIndex = token.start._bufferIndex;
  const endIndex = token.end._index;
  const endBufferIndex = token.end._bufferIndex;
  let view;
  if (startIndex === endIndex) {
    view = [chunks[startIndex].slice(startBufferIndex, endBufferIndex)];
  } else {
    view = chunks.slice(startIndex, endIndex);
    if (startBufferIndex > -1) {
      const head = view[0];
      if (typeof head === "string") {
        view[0] = head.slice(startBufferIndex);
      } else {
        view.shift();
      }
    }
    if (endBufferIndex > 0) {
      view.push(chunks[endIndex].slice(0, endBufferIndex));
    }
  }
  return view;
}
function serializeChunks(chunks, expandTabs) {
  let index2 = -1;
  const result = [];
  let atTab;
  while (++index2 < chunks.length) {
    const chunk = chunks[index2];
    let value;
    if (typeof chunk === "string") {
      value = chunk;
    } else switch (chunk) {
      case -5: {
        value = "\r";
        break;
      }
      case -4: {
        value = "\n";
        break;
      }
      case -3: {
        value = "\r\n";
        break;
      }
      case -2: {
        value = expandTabs ? " " : "	";
        break;
      }
      case -1: {
        if (!expandTabs && atTab) continue;
        value = " ";
        break;
      }
      default: {
        value = String.fromCharCode(chunk);
      }
    }
    atTab = chunk === -2;
    result.push(value);
  }
  return result.join("");
}
function parse(options) {
  const settings = options || {};
  const constructs2 = (
    /** @type {FullNormalizedExtension} */
    combineExtensions([defaultConstructs, ...settings.extensions || []])
  );
  const parser = {
    constructs: constructs2,
    content: create2(content$1),
    defined: [],
    document: create2(document$2),
    flow: create2(flow$1),
    lazy: {},
    string: create2(string$1),
    text: create2(text$4)
  };
  return parser;
  function create2(initial) {
    return creator;
    function creator(from) {
      return createTokenizer(parser, initial, from);
    }
  }
}
function postprocess(events2) {
  while (!subtokenize(events2)) {
  }
  return events2;
}
const search = /[\0\t\n\r]/g;
function preprocess() {
  let column = 1;
  let buffer = "";
  let start = true;
  let atCarriageReturn;
  return preprocessor;
  function preprocessor(value, encoding, end) {
    const chunks = [];
    let match;
    let next;
    let startPosition;
    let endPosition;
    let code2;
    value = buffer + (typeof value === "string" ? value.toString() : new TextDecoder(encoding || void 0).decode(value));
    startPosition = 0;
    buffer = "";
    if (start) {
      if (value.charCodeAt(0) === 65279) {
        startPosition++;
      }
      start = void 0;
    }
    while (startPosition < value.length) {
      search.lastIndex = startPosition;
      match = search.exec(value);
      endPosition = match && match.index !== void 0 ? match.index : value.length;
      code2 = value.charCodeAt(endPosition);
      if (!match) {
        buffer = value.slice(startPosition);
        break;
      }
      if (code2 === 10 && startPosition === endPosition && atCarriageReturn) {
        chunks.push(-3);
        atCarriageReturn = void 0;
      } else {
        if (atCarriageReturn) {
          chunks.push(-5);
          atCarriageReturn = void 0;
        }
        if (startPosition < endPosition) {
          chunks.push(value.slice(startPosition, endPosition));
          column += endPosition - startPosition;
        }
        switch (code2) {
          case 0: {
            chunks.push(65533);
            column++;
            break;
          }
          case 9: {
            next = Math.ceil(column / 4) * 4;
            chunks.push(-2);
            while (column++ < next) chunks.push(-1);
            break;
          }
          case 10: {
            chunks.push(-4);
            column = 1;
            break;
          }
          default: {
            atCarriageReturn = true;
            column = 1;
          }
        }
      }
      startPosition = endPosition + 1;
    }
    if (end) {
      if (atCarriageReturn) chunks.push(-5);
      if (buffer) chunks.push(buffer);
      chunks.push(null);
    }
    return chunks;
  }
}
const characterEscapeOrReference = /\\([!-/:-@[-`{-~])|&(#(?:\d{1,7}|x[\da-f]{1,6})|[\da-z]{1,31});/gi;
function decodeString(value) {
  return value.replace(characterEscapeOrReference, decode);
}
function decode($0, $1, $2) {
  if ($1) {
    return $1;
  }
  const head = $2.charCodeAt(0);
  if (head === 35) {
    const head2 = $2.charCodeAt(1);
    const hex = head2 === 120 || head2 === 88;
    return decodeNumericCharacterReference($2.slice(hex ? 2 : 1), hex ? 16 : 10);
  }
  return decodeNamedCharacterReference($2) || $0;
}
const own$2 = {}.hasOwnProperty;
function fromMarkdown(value, encoding, options) {
  if (encoding && typeof encoding === "object") {
    options = encoding;
    encoding = void 0;
  }
  return compiler(options)(postprocess(parse(options).document().write(preprocess()(value, encoding, true))));
}
function compiler(options) {
  const config = {
    transforms: [],
    canContainEols: ["emphasis", "fragment", "heading", "paragraph", "strong"],
    enter: {
      autolink: opener(link2),
      autolinkProtocol: onenterdata,
      autolinkEmail: onenterdata,
      atxHeading: opener(heading2),
      blockQuote: opener(blockQuote2),
      characterEscape: onenterdata,
      characterReference: onenterdata,
      codeFenced: opener(codeFlow),
      codeFencedFenceInfo: buffer,
      codeFencedFenceMeta: buffer,
      codeIndented: opener(codeFlow, buffer),
      codeText: opener(codeText2, buffer),
      codeTextData: onenterdata,
      data: onenterdata,
      codeFlowValue: onenterdata,
      definition: opener(definition2),
      definitionDestinationString: buffer,
      definitionLabelString: buffer,
      definitionTitleString: buffer,
      emphasis: opener(emphasis2),
      hardBreakEscape: opener(hardBreak2),
      hardBreakTrailing: opener(hardBreak2),
      htmlFlow: opener(html2, buffer),
      htmlFlowData: onenterdata,
      htmlText: opener(html2, buffer),
      htmlTextData: onenterdata,
      image: opener(image2),
      label: buffer,
      link: opener(link2),
      listItem: opener(listItem2),
      listItemValue: onenterlistitemvalue,
      listOrdered: opener(list2, onenterlistordered),
      listUnordered: opener(list2),
      paragraph: opener(paragraph2),
      reference: onenterreference,
      referenceString: buffer,
      resourceDestinationString: buffer,
      resourceTitleString: buffer,
      setextHeading: opener(heading2),
      strong: opener(strong2),
      thematicBreak: opener(thematicBreak2)
    },
    exit: {
      atxHeading: closer(),
      atxHeadingSequence: onexitatxheadingsequence,
      autolink: closer(),
      autolinkEmail: onexitautolinkemail,
      autolinkProtocol: onexitautolinkprotocol,
      blockQuote: closer(),
      characterEscapeValue: onexitdata,
      characterReferenceMarkerHexadecimal: onexitcharacterreferencemarker,
      characterReferenceMarkerNumeric: onexitcharacterreferencemarker,
      characterReferenceValue: onexitcharacterreferencevalue,
      characterReference: onexitcharacterreference,
      codeFenced: closer(onexitcodefenced),
      codeFencedFence: onexitcodefencedfence,
      codeFencedFenceInfo: onexitcodefencedfenceinfo,
      codeFencedFenceMeta: onexitcodefencedfencemeta,
      codeFlowValue: onexitdata,
      codeIndented: closer(onexitcodeindented),
      codeText: closer(onexitcodetext),
      codeTextData: onexitdata,
      data: onexitdata,
      definition: closer(),
      definitionDestinationString: onexitdefinitiondestinationstring,
      definitionLabelString: onexitdefinitionlabelstring,
      definitionTitleString: onexitdefinitiontitlestring,
      emphasis: closer(),
      hardBreakEscape: closer(onexithardbreak),
      hardBreakTrailing: closer(onexithardbreak),
      htmlFlow: closer(onexithtmlflow),
      htmlFlowData: onexitdata,
      htmlText: closer(onexithtmltext),
      htmlTextData: onexitdata,
      image: closer(onexitimage),
      label: onexitlabel,
      labelText: onexitlabeltext,
      lineEnding: onexitlineending,
      link: closer(onexitlink),
      listItem: closer(),
      listOrdered: closer(),
      listUnordered: closer(),
      paragraph: closer(),
      referenceString: onexitreferencestring,
      resourceDestinationString: onexitresourcedestinationstring,
      resourceTitleString: onexitresourcetitlestring,
      resource: onexitresource,
      setextHeading: closer(onexitsetextheading),
      setextHeadingLineSequence: onexitsetextheadinglinesequence,
      setextHeadingText: onexitsetextheadingtext,
      strong: closer(),
      thematicBreak: closer()
    }
  };
  configure(config, (options || {}).mdastExtensions || []);
  const data2 = {};
  return compile;
  function compile(events2) {
    let tree = {
      type: "root",
      children: []
    };
    const context = {
      stack: [tree],
      tokenStack: [],
      config,
      enter,
      exit: exit2,
      buffer,
      resume,
      data: data2
    };
    const listStack = [];
    let index2 = -1;
    while (++index2 < events2.length) {
      if (events2[index2][1].type === "listOrdered" || events2[index2][1].type === "listUnordered") {
        if (events2[index2][0] === "enter") {
          listStack.push(index2);
        } else {
          const tail = listStack.pop();
          index2 = prepareList(events2, tail, index2);
        }
      }
    }
    index2 = -1;
    while (++index2 < events2.length) {
      const handler = config[events2[index2][0]];
      if (own$2.call(handler, events2[index2][1].type)) {
        handler[events2[index2][1].type].call(Object.assign({
          sliceSerialize: events2[index2][2].sliceSerialize
        }, context), events2[index2][1]);
      }
    }
    if (context.tokenStack.length > 0) {
      const tail = context.tokenStack[context.tokenStack.length - 1];
      const handler = tail[1] || defaultOnError;
      handler.call(context, void 0, tail[0]);
    }
    tree.position = {
      start: point(events2.length > 0 ? events2[0][1].start : {
        line: 1,
        column: 1,
        offset: 0
      }),
      end: point(events2.length > 0 ? events2[events2.length - 2][1].end : {
        line: 1,
        column: 1,
        offset: 0
      })
    };
    index2 = -1;
    while (++index2 < config.transforms.length) {
      tree = config.transforms[index2](tree) || tree;
    }
    return tree;
  }
  function prepareList(events2, start, length) {
    let index2 = start - 1;
    let containerBalance = -1;
    let listSpread = false;
    let listItem3;
    let lineIndex;
    let firstBlankLineIndex;
    let atMarker;
    while (++index2 <= length) {
      const event = events2[index2];
      switch (event[1].type) {
        case "listUnordered":
        case "listOrdered":
        case "blockQuote": {
          if (event[0] === "enter") {
            containerBalance++;
          } else {
            containerBalance--;
          }
          atMarker = void 0;
          break;
        }
        case "lineEndingBlank": {
          if (event[0] === "enter") {
            if (listItem3 && !atMarker && !containerBalance && !firstBlankLineIndex) {
              firstBlankLineIndex = index2;
            }
            atMarker = void 0;
          }
          break;
        }
        case "linePrefix":
        case "listItemValue":
        case "listItemMarker":
        case "listItemPrefix":
        case "listItemPrefixWhitespace": {
          break;
        }
        default: {
          atMarker = void 0;
        }
      }
      if (!containerBalance && event[0] === "enter" && event[1].type === "listItemPrefix" || containerBalance === -1 && event[0] === "exit" && (event[1].type === "listUnordered" || event[1].type === "listOrdered")) {
        if (listItem3) {
          let tailIndex = index2;
          lineIndex = void 0;
          while (tailIndex--) {
            const tailEvent = events2[tailIndex];
            if (tailEvent[1].type === "lineEnding" || tailEvent[1].type === "lineEndingBlank") {
              if (tailEvent[0] === "exit") continue;
              if (lineIndex) {
                events2[lineIndex][1].type = "lineEndingBlank";
                listSpread = true;
              }
              tailEvent[1].type = "lineEnding";
              lineIndex = tailIndex;
            } else if (tailEvent[1].type === "linePrefix" || tailEvent[1].type === "blockQuotePrefix" || tailEvent[1].type === "blockQuotePrefixWhitespace" || tailEvent[1].type === "blockQuoteMarker" || tailEvent[1].type === "listItemIndent") ;
            else {
              break;
            }
          }
          if (firstBlankLineIndex && (!lineIndex || firstBlankLineIndex < lineIndex)) {
            listItem3._spread = true;
          }
          listItem3.end = Object.assign({}, lineIndex ? events2[lineIndex][1].start : event[1].end);
          events2.splice(lineIndex || index2, 0, ["exit", listItem3, event[2]]);
          index2++;
          length++;
        }
        if (event[1].type === "listItemPrefix") {
          const item = {
            type: "listItem",
            _spread: false,
            start: Object.assign({}, event[1].start),
            // @ts-expect-error: we’ll add `end` in a second.
            end: void 0
          };
          listItem3 = item;
          events2.splice(index2, 0, ["enter", item, event[2]]);
          index2++;
          length++;
          firstBlankLineIndex = void 0;
          atMarker = true;
        }
      }
    }
    events2[start][1]._spread = listSpread;
    return length;
  }
  function opener(create2, and) {
    return open;
    function open(token) {
      enter.call(this, create2(token), token);
      if (and) and.call(this, token);
    }
  }
  function buffer() {
    this.stack.push({
      type: "fragment",
      children: []
    });
  }
  function enter(node2, token, errorHandler) {
    const parent = this.stack[this.stack.length - 1];
    const siblings = parent.children;
    siblings.push(node2);
    this.stack.push(node2);
    this.tokenStack.push([token, errorHandler || void 0]);
    node2.position = {
      start: point(token.start),
      // @ts-expect-error: `end` will be patched later.
      end: void 0
    };
  }
  function closer(and) {
    return close;
    function close(token) {
      if (and) and.call(this, token);
      exit2.call(this, token);
    }
  }
  function exit2(token, onExitError) {
    const node2 = this.stack.pop();
    const open = this.tokenStack.pop();
    if (!open) {
      throw new Error("Cannot close `" + token.type + "` (" + stringifyPosition({
        start: token.start,
        end: token.end
      }) + "): it’s not open");
    } else if (open[0].type !== token.type) {
      if (onExitError) {
        onExitError.call(this, token, open[0]);
      } else {
        const handler = open[1] || defaultOnError;
        handler.call(this, token, open[0]);
      }
    }
    node2.position.end = point(token.end);
  }
  function resume() {
    return toString$1(this.stack.pop());
  }
  function onenterlistordered() {
    this.data.expectingFirstListItemValue = true;
  }
  function onenterlistitemvalue(token) {
    if (this.data.expectingFirstListItemValue) {
      const ancestor = this.stack[this.stack.length - 2];
      ancestor.start = Number.parseInt(this.sliceSerialize(token), 10);
      this.data.expectingFirstListItemValue = void 0;
    }
  }
  function onexitcodefencedfenceinfo() {
    const data3 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.lang = data3;
  }
  function onexitcodefencedfencemeta() {
    const data3 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.meta = data3;
  }
  function onexitcodefencedfence() {
    if (this.data.flowCodeInside) return;
    this.buffer();
    this.data.flowCodeInside = true;
  }
  function onexitcodefenced() {
    const data3 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data3.replace(/^(\r?\n|\r)|(\r?\n|\r)$/g, "");
    this.data.flowCodeInside = void 0;
  }
  function onexitcodeindented() {
    const data3 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data3.replace(/(\r?\n|\r)$/g, "");
  }
  function onexitdefinitionlabelstring(token) {
    const label = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.label = label;
    node2.identifier = normalizeIdentifier(this.sliceSerialize(token)).toLowerCase();
  }
  function onexitdefinitiontitlestring() {
    const data3 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.title = data3;
  }
  function onexitdefinitiondestinationstring() {
    const data3 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.url = data3;
  }
  function onexitatxheadingsequence(token) {
    const node2 = this.stack[this.stack.length - 1];
    if (!node2.depth) {
      const depth = this.sliceSerialize(token).length;
      node2.depth = depth;
    }
  }
  function onexitsetextheadingtext() {
    this.data.setextHeadingSlurpLineEnding = true;
  }
  function onexitsetextheadinglinesequence(token) {
    const node2 = this.stack[this.stack.length - 1];
    node2.depth = this.sliceSerialize(token).codePointAt(0) === 61 ? 1 : 2;
  }
  function onexitsetextheading() {
    this.data.setextHeadingSlurpLineEnding = void 0;
  }
  function onenterdata(token) {
    const node2 = this.stack[this.stack.length - 1];
    const siblings = node2.children;
    let tail = siblings[siblings.length - 1];
    if (!tail || tail.type !== "text") {
      tail = text2();
      tail.position = {
        start: point(token.start),
        // @ts-expect-error: we’ll add `end` later.
        end: void 0
      };
      siblings.push(tail);
    }
    this.stack.push(tail);
  }
  function onexitdata(token) {
    const tail = this.stack.pop();
    tail.value += this.sliceSerialize(token);
    tail.position.end = point(token.end);
  }
  function onexitlineending(token) {
    const context = this.stack[this.stack.length - 1];
    if (this.data.atHardBreak) {
      const tail = context.children[context.children.length - 1];
      tail.position.end = point(token.end);
      this.data.atHardBreak = void 0;
      return;
    }
    if (!this.data.setextHeadingSlurpLineEnding && config.canContainEols.includes(context.type)) {
      onenterdata.call(this, token);
      onexitdata.call(this, token);
    }
  }
  function onexithardbreak() {
    this.data.atHardBreak = true;
  }
  function onexithtmlflow() {
    const data3 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data3;
  }
  function onexithtmltext() {
    const data3 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data3;
  }
  function onexitcodetext() {
    const data3 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data3;
  }
  function onexitlink() {
    const node2 = this.stack[this.stack.length - 1];
    if (this.data.inReference) {
      const referenceType = this.data.referenceType || "shortcut";
      node2.type += "Reference";
      node2.referenceType = referenceType;
      delete node2.url;
      delete node2.title;
    } else {
      delete node2.identifier;
      delete node2.label;
    }
    this.data.referenceType = void 0;
  }
  function onexitimage() {
    const node2 = this.stack[this.stack.length - 1];
    if (this.data.inReference) {
      const referenceType = this.data.referenceType || "shortcut";
      node2.type += "Reference";
      node2.referenceType = referenceType;
      delete node2.url;
      delete node2.title;
    } else {
      delete node2.identifier;
      delete node2.label;
    }
    this.data.referenceType = void 0;
  }
  function onexitlabeltext(token) {
    const string2 = this.sliceSerialize(token);
    const ancestor = this.stack[this.stack.length - 2];
    ancestor.label = decodeString(string2);
    ancestor.identifier = normalizeIdentifier(string2).toLowerCase();
  }
  function onexitlabel() {
    const fragment = this.stack[this.stack.length - 1];
    const value = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    this.data.inReference = true;
    if (node2.type === "link") {
      const children = fragment.children;
      node2.children = children;
    } else {
      node2.alt = value;
    }
  }
  function onexitresourcedestinationstring() {
    const data3 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.url = data3;
  }
  function onexitresourcetitlestring() {
    const data3 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.title = data3;
  }
  function onexitresource() {
    this.data.inReference = void 0;
  }
  function onenterreference() {
    this.data.referenceType = "collapsed";
  }
  function onexitreferencestring(token) {
    const label = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.label = label;
    node2.identifier = normalizeIdentifier(this.sliceSerialize(token)).toLowerCase();
    this.data.referenceType = "full";
  }
  function onexitcharacterreferencemarker(token) {
    this.data.characterReferenceType = token.type;
  }
  function onexitcharacterreferencevalue(token) {
    const data3 = this.sliceSerialize(token);
    const type = this.data.characterReferenceType;
    let value;
    if (type) {
      value = decodeNumericCharacterReference(data3, type === "characterReferenceMarkerNumeric" ? 10 : 16);
      this.data.characterReferenceType = void 0;
    } else {
      const result = decodeNamedCharacterReference(data3);
      value = result;
    }
    const tail = this.stack[this.stack.length - 1];
    tail.value += value;
  }
  function onexitcharacterreference(token) {
    const tail = this.stack.pop();
    tail.position.end = point(token.end);
  }
  function onexitautolinkprotocol(token) {
    onexitdata.call(this, token);
    const node2 = this.stack[this.stack.length - 1];
    node2.url = this.sliceSerialize(token);
  }
  function onexitautolinkemail(token) {
    onexitdata.call(this, token);
    const node2 = this.stack[this.stack.length - 1];
    node2.url = "mailto:" + this.sliceSerialize(token);
  }
  function blockQuote2() {
    return {
      type: "blockquote",
      children: []
    };
  }
  function codeFlow() {
    return {
      type: "code",
      lang: null,
      meta: null,
      value: ""
    };
  }
  function codeText2() {
    return {
      type: "inlineCode",
      value: ""
    };
  }
  function definition2() {
    return {
      type: "definition",
      identifier: "",
      label: null,
      title: null,
      url: ""
    };
  }
  function emphasis2() {
    return {
      type: "emphasis",
      children: []
    };
  }
  function heading2() {
    return {
      type: "heading",
      // @ts-expect-error `depth` will be set later.
      depth: 0,
      children: []
    };
  }
  function hardBreak2() {
    return {
      type: "break"
    };
  }
  function html2() {
    return {
      type: "html",
      value: ""
    };
  }
  function image2() {
    return {
      type: "image",
      title: null,
      url: "",
      alt: null
    };
  }
  function link2() {
    return {
      type: "link",
      title: null,
      url: "",
      children: []
    };
  }
  function list2(token) {
    return {
      type: "list",
      ordered: token.type === "listOrdered",
      start: null,
      spread: token._spread,
      children: []
    };
  }
  function listItem2(token) {
    return {
      type: "listItem",
      spread: token._spread,
      checked: null,
      children: []
    };
  }
  function paragraph2() {
    return {
      type: "paragraph",
      children: []
    };
  }
  function strong2() {
    return {
      type: "strong",
      children: []
    };
  }
  function text2() {
    return {
      type: "text",
      value: ""
    };
  }
  function thematicBreak2() {
    return {
      type: "thematicBreak"
    };
  }
}
function point(d) {
  return {
    line: d.line,
    column: d.column,
    offset: d.offset
  };
}
function configure(combined, extensions) {
  let index2 = -1;
  while (++index2 < extensions.length) {
    const value = extensions[index2];
    if (Array.isArray(value)) {
      configure(combined, value);
    } else {
      extension(combined, value);
    }
  }
}
function extension(combined, extension2) {
  let key;
  for (key in extension2) {
    if (own$2.call(extension2, key)) {
      switch (key) {
        case "canContainEols": {
          const right = extension2[key];
          if (right) {
            combined[key].push(...right);
          }
          break;
        }
        case "transforms": {
          const right = extension2[key];
          if (right) {
            combined[key].push(...right);
          }
          break;
        }
        case "enter":
        case "exit": {
          const right = extension2[key];
          if (right) {
            Object.assign(combined[key], right);
          }
          break;
        }
      }
    }
  }
}
function defaultOnError(left, right) {
  if (left) {
    throw new Error("Cannot close `" + left.type + "` (" + stringifyPosition({
      start: left.start,
      end: left.end
    }) + "): a different token (`" + right.type + "`, " + stringifyPosition({
      start: right.start,
      end: right.end
    }) + ") is open");
  } else {
    throw new Error("Cannot close document, a token (`" + right.type + "`, " + stringifyPosition({
      start: right.start,
      end: right.end
    }) + ") is still open");
  }
}
function remarkParse(options) {
  const self2 = this;
  self2.parser = parser;
  function parser(doc) {
    return fromMarkdown(doc, {
      ...self2.data("settings"),
      ...options,
      // Note: these options are not in the readme.
      // The goal is for them to be set by plugins on `data` instead of being
      // passed by users.
      extensions: self2.data("micromarkExtensions") || [],
      mdastExtensions: self2.data("fromMarkdownExtensions") || []
    });
  }
}
function blockquote$1(state, node2) {
  const result = {
    type: "element",
    tagName: "blockquote",
    properties: {},
    children: state.wrap(state.all(node2), true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function hardBreak$1(state, node2) {
  const result = { type: "element", tagName: "br", properties: {}, children: [] };
  state.patch(node2, result);
  return [state.applyData(node2, result), { type: "text", value: "\n" }];
}
function code$2(state, node2) {
  const value = node2.value ? node2.value + "\n" : "";
  const properties = {};
  const language = node2.lang ? node2.lang.split(/\s+/) : [];
  if (language.length > 0) {
    properties.className = ["language-" + language[0]];
  }
  let result = {
    type: "element",
    tagName: "code",
    properties,
    children: [{ type: "text", value }]
  };
  if (node2.meta) {
    result.data = { meta: node2.meta };
  }
  state.patch(node2, result);
  result = state.applyData(node2, result);
  result = { type: "element", tagName: "pre", properties: {}, children: [result] };
  state.patch(node2, result);
  return result;
}
function strikethrough(state, node2) {
  const result = {
    type: "element",
    tagName: "del",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function emphasis$1(state, node2) {
  const result = {
    type: "element",
    tagName: "em",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function footnoteReference$1(state, node2) {
  const clobberPrefix = typeof state.options.clobberPrefix === "string" ? state.options.clobberPrefix : "user-content-";
  const id2 = String(node2.identifier).toUpperCase();
  const safeId = normalizeUri(id2.toLowerCase());
  const index2 = state.footnoteOrder.indexOf(id2);
  let counter;
  let reuseCounter = state.footnoteCounts.get(id2);
  if (reuseCounter === void 0) {
    reuseCounter = 0;
    state.footnoteOrder.push(id2);
    counter = state.footnoteOrder.length;
  } else {
    counter = index2 + 1;
  }
  reuseCounter += 1;
  state.footnoteCounts.set(id2, reuseCounter);
  const link2 = {
    type: "element",
    tagName: "a",
    properties: {
      href: "#" + clobberPrefix + "fn-" + safeId,
      id: clobberPrefix + "fnref-" + safeId + (reuseCounter > 1 ? "-" + reuseCounter : ""),
      dataFootnoteRef: true,
      ariaDescribedBy: ["footnote-label"]
    },
    children: [{ type: "text", value: String(counter) }]
  };
  state.patch(node2, link2);
  const sup = {
    type: "element",
    tagName: "sup",
    properties: {},
    children: [link2]
  };
  state.patch(node2, sup);
  return state.applyData(node2, sup);
}
function heading$1(state, node2) {
  const result = {
    type: "element",
    tagName: "h" + node2.depth,
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function html$1(state, node2) {
  if (state.options.allowDangerousHtml) {
    const result = { type: "raw", value: node2.value };
    state.patch(node2, result);
    return state.applyData(node2, result);
  }
  return void 0;
}
function revert(state, node2) {
  const subtype = node2.referenceType;
  let suffix = "]";
  if (subtype === "collapsed") {
    suffix += "[]";
  } else if (subtype === "full") {
    suffix += "[" + (node2.label || node2.identifier) + "]";
  }
  if (node2.type === "imageReference") {
    return [{ type: "text", value: "![" + node2.alt + suffix }];
  }
  const contents = state.all(node2);
  const head = contents[0];
  if (head && head.type === "text") {
    head.value = "[" + head.value;
  } else {
    contents.unshift({ type: "text", value: "[" });
  }
  const tail = contents[contents.length - 1];
  if (tail && tail.type === "text") {
    tail.value += suffix;
  } else {
    contents.push({ type: "text", value: suffix });
  }
  return contents;
}
function imageReference$1(state, node2) {
  const id2 = String(node2.identifier).toUpperCase();
  const definition2 = state.definitionById.get(id2);
  if (!definition2) {
    return revert(state, node2);
  }
  const properties = { src: normalizeUri(definition2.url || ""), alt: node2.alt };
  if (definition2.title !== null && definition2.title !== void 0) {
    properties.title = definition2.title;
  }
  const result = { type: "element", tagName: "img", properties, children: [] };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function image$1(state, node2) {
  const properties = { src: normalizeUri(node2.url) };
  if (node2.alt !== null && node2.alt !== void 0) {
    properties.alt = node2.alt;
  }
  if (node2.title !== null && node2.title !== void 0) {
    properties.title = node2.title;
  }
  const result = { type: "element", tagName: "img", properties, children: [] };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function inlineCode$1(state, node2) {
  const text2 = { type: "text", value: node2.value.replace(/\r?\n|\r/g, " ") };
  state.patch(node2, text2);
  const result = {
    type: "element",
    tagName: "code",
    properties: {},
    children: [text2]
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function linkReference$1(state, node2) {
  const id2 = String(node2.identifier).toUpperCase();
  const definition2 = state.definitionById.get(id2);
  if (!definition2) {
    return revert(state, node2);
  }
  const properties = { href: normalizeUri(definition2.url || "") };
  if (definition2.title !== null && definition2.title !== void 0) {
    properties.title = definition2.title;
  }
  const result = {
    type: "element",
    tagName: "a",
    properties,
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function link$1(state, node2) {
  const properties = { href: normalizeUri(node2.url) };
  if (node2.title !== null && node2.title !== void 0) {
    properties.title = node2.title;
  }
  const result = {
    type: "element",
    tagName: "a",
    properties,
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function listItem$1(state, node2, parent) {
  const results = state.all(node2);
  const loose = parent ? listLoose(parent) : listItemLoose(node2);
  const properties = {};
  const children = [];
  if (typeof node2.checked === "boolean") {
    const head = results[0];
    let paragraph2;
    if (head && head.type === "element" && head.tagName === "p") {
      paragraph2 = head;
    } else {
      paragraph2 = { type: "element", tagName: "p", properties: {}, children: [] };
      results.unshift(paragraph2);
    }
    if (paragraph2.children.length > 0) {
      paragraph2.children.unshift({ type: "text", value: " " });
    }
    paragraph2.children.unshift({
      type: "element",
      tagName: "input",
      properties: { type: "checkbox", checked: node2.checked, disabled: true },
      children: []
    });
    properties.className = ["task-list-item"];
  }
  let index2 = -1;
  while (++index2 < results.length) {
    const child = results[index2];
    if (loose || index2 !== 0 || child.type !== "element" || child.tagName !== "p") {
      children.push({ type: "text", value: "\n" });
    }
    if (child.type === "element" && child.tagName === "p" && !loose) {
      children.push(...child.children);
    } else {
      children.push(child);
    }
  }
  const tail = results[results.length - 1];
  if (tail && (loose || tail.type !== "element" || tail.tagName !== "p")) {
    children.push({ type: "text", value: "\n" });
  }
  const result = { type: "element", tagName: "li", properties, children };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function listLoose(node2) {
  let loose = false;
  if (node2.type === "list") {
    loose = node2.spread || false;
    const children = node2.children;
    let index2 = -1;
    while (!loose && ++index2 < children.length) {
      loose = listItemLoose(children[index2]);
    }
  }
  return loose;
}
function listItemLoose(node2) {
  const spread = node2.spread;
  return spread === null || spread === void 0 ? node2.children.length > 1 : spread;
}
function list$1(state, node2) {
  const properties = {};
  const results = state.all(node2);
  let index2 = -1;
  if (typeof node2.start === "number" && node2.start !== 1) {
    properties.start = node2.start;
  }
  while (++index2 < results.length) {
    const child = results[index2];
    if (child.type === "element" && child.tagName === "li" && child.properties && Array.isArray(child.properties.className) && child.properties.className.includes("task-list-item")) {
      properties.className = ["contains-task-list"];
      break;
    }
  }
  const result = {
    type: "element",
    tagName: node2.ordered ? "ol" : "ul",
    properties,
    children: state.wrap(results, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function paragraph$1(state, node2) {
  const result = {
    type: "element",
    tagName: "p",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function root$1(state, node2) {
  const result = { type: "root", children: state.wrap(state.all(node2)) };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function strong$1(state, node2) {
  const result = {
    type: "element",
    tagName: "strong",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function table(state, node2) {
  const rows = state.all(node2);
  const firstRow = rows.shift();
  const tableContent = [];
  if (firstRow) {
    const head = {
      type: "element",
      tagName: "thead",
      properties: {},
      children: state.wrap([firstRow], true)
    };
    state.patch(node2.children[0], head);
    tableContent.push(head);
  }
  if (rows.length > 0) {
    const body = {
      type: "element",
      tagName: "tbody",
      properties: {},
      children: state.wrap(rows, true)
    };
    const start = pointStart(node2.children[1]);
    const end = pointEnd(node2.children[node2.children.length - 1]);
    if (start && end) body.position = { start, end };
    tableContent.push(body);
  }
  const result = {
    type: "element",
    tagName: "table",
    properties: {},
    children: state.wrap(tableContent, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function tableRow(state, node2, parent) {
  const siblings = parent ? parent.children : void 0;
  const rowIndex = siblings ? siblings.indexOf(node2) : 1;
  const tagName = rowIndex === 0 ? "th" : "td";
  const align = parent && parent.type === "table" ? parent.align : void 0;
  const length = align ? align.length : node2.children.length;
  let cellIndex = -1;
  const cells = [];
  while (++cellIndex < length) {
    const cell = node2.children[cellIndex];
    const properties = {};
    const alignValue = align ? align[cellIndex] : void 0;
    if (alignValue) {
      properties.align = alignValue;
    }
    let result2 = { type: "element", tagName, properties, children: [] };
    if (cell) {
      result2.children = state.all(cell);
      state.patch(cell, result2);
      result2 = state.applyData(cell, result2);
    }
    cells.push(result2);
  }
  const result = {
    type: "element",
    tagName: "tr",
    properties: {},
    children: state.wrap(cells, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function tableCell(state, node2) {
  const result = {
    type: "element",
    tagName: "td",
    // Assume body cell.
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
const tab = 9;
const space = 32;
function trimLines(value) {
  const source = String(value);
  const search2 = /\r?\n|\r/g;
  let match = search2.exec(source);
  let last = 0;
  const lines = [];
  while (match) {
    lines.push(
      trimLine(source.slice(last, match.index), last > 0, true),
      match[0]
    );
    last = match.index + match[0].length;
    match = search2.exec(source);
  }
  lines.push(trimLine(source.slice(last), last > 0, false));
  return lines.join("");
}
function trimLine(value, start, end) {
  let startIndex = 0;
  let endIndex = value.length;
  if (start) {
    let code2 = value.codePointAt(startIndex);
    while (code2 === tab || code2 === space) {
      startIndex++;
      code2 = value.codePointAt(startIndex);
    }
  }
  if (end) {
    let code2 = value.codePointAt(endIndex - 1);
    while (code2 === tab || code2 === space) {
      endIndex--;
      code2 = value.codePointAt(endIndex - 1);
    }
  }
  return endIndex > startIndex ? value.slice(startIndex, endIndex) : "";
}
function text$2(state, node2) {
  const result = { type: "text", value: trimLines(String(node2.value)) };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function thematicBreak$1(state, node2) {
  const result = {
    type: "element",
    tagName: "hr",
    properties: {},
    children: []
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
const handlers = {
  blockquote: blockquote$1,
  break: hardBreak$1,
  code: code$2,
  delete: strikethrough,
  emphasis: emphasis$1,
  footnoteReference: footnoteReference$1,
  heading: heading$1,
  html: html$1,
  imageReference: imageReference$1,
  image: image$1,
  inlineCode: inlineCode$1,
  linkReference: linkReference$1,
  link: link$1,
  listItem: listItem$1,
  list: list$1,
  paragraph: paragraph$1,
  // @ts-expect-error: root is different, but hard to type.
  root: root$1,
  strong: strong$1,
  table,
  tableCell,
  tableRow,
  text: text$2,
  thematicBreak: thematicBreak$1,
  toml: ignore,
  yaml: ignore,
  definition: ignore,
  footnoteDefinition: ignore
};
function ignore() {
  return void 0;
}
const VOID = -1;
const PRIMITIVE = 0;
const ARRAY = 1;
const OBJECT = 2;
const DATE = 3;
const REGEXP = 4;
const MAP = 5;
const SET = 6;
const ERROR = 7;
const BIGINT = 8;
const env = typeof self === "object" ? self : globalThis;
const guard = (name2, init) => {
  switch (name2) {
    case "Function":
    case "SharedWorker":
    case "Worker":
    case "eval":
    case "setInterval":
    case "setTimeout":
      throw new TypeError("unable to deserialize " + name2);
  }
  return new env[name2](init);
};
const deserializer = ($, _) => {
  const as = (out, index2) => {
    $.set(index2, out);
    return out;
  };
  const unpair = (index2) => {
    if ($.has(index2))
      return $.get(index2);
    const [type, value] = _[index2];
    switch (type) {
      case PRIMITIVE:
      case VOID:
        return as(value, index2);
      case ARRAY: {
        const arr = as([], index2);
        for (const index3 of value)
          arr.push(unpair(index3));
        return arr;
      }
      case OBJECT: {
        const object = as({}, index2);
        for (const [key, index3] of value)
          object[unpair(key)] = unpair(index3);
        return object;
      }
      case DATE:
        return as(new Date(value), index2);
      case REGEXP: {
        const { source, flags } = value;
        return as(new RegExp(source, flags), index2);
      }
      case MAP: {
        const map2 = as(/* @__PURE__ */ new Map(), index2);
        for (const [key, index3] of value)
          map2.set(unpair(key), unpair(index3));
        return map2;
      }
      case SET: {
        const set = as(/* @__PURE__ */ new Set(), index2);
        for (const index3 of value)
          set.add(unpair(index3));
        return set;
      }
      case ERROR: {
        const { name: name2, message } = value;
        return as(
          typeof env[name2] === "function" ? guard(name2, message) : new Error(message),
          index2
        );
      }
      case BIGINT:
        return as(BigInt(value), index2);
      case "BigInt":
        return as(Object(BigInt(value)), index2);
      case "ArrayBuffer":
        return as(new Uint8Array(value).buffer, value);
      case "DataView": {
        const { buffer } = new Uint8Array(value);
        return as(new DataView(buffer), value);
      }
    }
    return as(guard(type, value), index2);
  };
  return unpair;
};
const deserialize = (serialized) => deserializer(/* @__PURE__ */ new Map(), serialized)(0);
const EMPTY = "";
const { toString } = {};
const { keys } = Object;
const typeOf = (value) => {
  const type = typeof value;
  if (type !== "object" || !value)
    return [PRIMITIVE, type];
  const asString = toString.call(value).slice(8, -1);
  switch (asString) {
    case "Array":
      return [ARRAY, EMPTY];
    case "Object":
      return [OBJECT, EMPTY];
    case "Date":
      return [DATE, EMPTY];
    case "RegExp":
      return [REGEXP, EMPTY];
    case "Map":
      return [MAP, EMPTY];
    case "Set":
      return [SET, EMPTY];
    case "DataView":
      return [ARRAY, asString];
  }
  if (asString.includes("Array"))
    return [ARRAY, asString];
  if (value instanceof Error)
    return [ERROR, value.name || "Error"];
  return [OBJECT, asString];
};
const shouldSkip = ([TYPE, type]) => TYPE === PRIMITIVE && (type === "function" || type === "symbol");
const serializer = (strict, json, $, _) => {
  const as = (out, value) => {
    const index2 = _.push(out) - 1;
    $.set(value, index2);
    return index2;
  };
  const pair = (value) => {
    if ($.has(value))
      return $.get(value);
    let [TYPE, type] = typeOf(value);
    switch (TYPE) {
      case PRIMITIVE: {
        let entry2 = value;
        switch (type) {
          case "bigint":
            TYPE = BIGINT;
            entry2 = value.toString();
            break;
          case "function":
          case "symbol":
            if (strict)
              throw new TypeError("unable to serialize " + type);
            entry2 = null;
            break;
          case "undefined":
            return as([VOID], value);
        }
        return as([TYPE, entry2], value);
      }
      case ARRAY: {
        if (type) {
          let spread = value;
          if (type === "DataView") {
            spread = new Uint8Array(value.buffer);
          } else if (type === "ArrayBuffer") {
            spread = new Uint8Array(value);
          }
          return as([type, [...spread]], value);
        }
        const arr = [];
        const index2 = as([TYPE, arr], value);
        for (const entry2 of value)
          arr.push(pair(entry2));
        return index2;
      }
      case OBJECT: {
        if (type) {
          switch (type) {
            case "BigInt":
              return as([type, value.toString()], value);
            case "Boolean":
            case "Number":
            case "String":
              return as([type, value.valueOf()], value);
          }
        }
        if (json && "toJSON" in value)
          return pair(value.toJSON());
        const entries = [];
        const index2 = as([TYPE, entries], value);
        for (const key of keys(value)) {
          if (strict || !shouldSkip(typeOf(value[key])))
            entries.push([pair(key), pair(value[key])]);
        }
        return index2;
      }
      case DATE:
        return as([TYPE, isNaN(value.getTime()) ? EMPTY : value.toISOString()], value);
      case REGEXP: {
        const { source, flags } = value;
        return as([TYPE, { source, flags }], value);
      }
      case MAP: {
        const entries = [];
        const index2 = as([TYPE, entries], value);
        for (const [key, entry2] of value) {
          if (strict || !(shouldSkip(typeOf(key)) || shouldSkip(typeOf(entry2))))
            entries.push([pair(key), pair(entry2)]);
        }
        return index2;
      }
      case SET: {
        const entries = [];
        const index2 = as([TYPE, entries], value);
        for (const entry2 of value) {
          if (strict || !shouldSkip(typeOf(entry2)))
            entries.push(pair(entry2));
        }
        return index2;
      }
    }
    const { message } = value;
    return as([TYPE, { name: type, message }], value);
  };
  return pair;
};
const serialize$1 = (value, { json, lossy } = {}) => {
  const _ = [];
  return serializer(!(json || lossy), !!json, /* @__PURE__ */ new Map(), _)(value), _;
};
const structuredClone$1 = typeof structuredClone === "function" ? (
  /* c8 ignore start */
  (any, options) => options && ("json" in options || "lossy" in options) ? deserialize(serialize$1(any, options)) : structuredClone(any)
) : (any, options) => deserialize(serialize$1(any, options));
function defaultFootnoteBackContent(_, rereferenceIndex) {
  const result = [{ type: "text", value: "↩" }];
  if (rereferenceIndex > 1) {
    result.push({
      type: "element",
      tagName: "sup",
      properties: {},
      children: [{ type: "text", value: String(rereferenceIndex) }]
    });
  }
  return result;
}
function defaultFootnoteBackLabel(referenceIndex, rereferenceIndex) {
  return "Back to reference " + (referenceIndex + 1) + (rereferenceIndex > 1 ? "-" + rereferenceIndex : "");
}
function footer(state) {
  const clobberPrefix = typeof state.options.clobberPrefix === "string" ? state.options.clobberPrefix : "user-content-";
  const footnoteBackContent = state.options.footnoteBackContent || defaultFootnoteBackContent;
  const footnoteBackLabel = state.options.footnoteBackLabel || defaultFootnoteBackLabel;
  const footnoteLabel = state.options.footnoteLabel || "Footnotes";
  const footnoteLabelTagName = state.options.footnoteLabelTagName || "h2";
  const footnoteLabelProperties = state.options.footnoteLabelProperties || {
    className: ["sr-only"]
  };
  const listItems = [];
  let referenceIndex = -1;
  while (++referenceIndex < state.footnoteOrder.length) {
    const definition2 = state.footnoteById.get(
      state.footnoteOrder[referenceIndex]
    );
    if (!definition2) {
      continue;
    }
    const content2 = state.all(definition2);
    const id2 = String(definition2.identifier).toUpperCase();
    const safeId = normalizeUri(id2.toLowerCase());
    let rereferenceIndex = 0;
    const backReferences = [];
    const counts = state.footnoteCounts.get(id2);
    while (counts !== void 0 && ++rereferenceIndex <= counts) {
      if (backReferences.length > 0) {
        backReferences.push({ type: "text", value: " " });
      }
      let children = typeof footnoteBackContent === "string" ? footnoteBackContent : footnoteBackContent(referenceIndex, rereferenceIndex);
      if (typeof children === "string") {
        children = { type: "text", value: children };
      }
      backReferences.push({
        type: "element",
        tagName: "a",
        properties: {
          href: "#" + clobberPrefix + "fnref-" + safeId + (rereferenceIndex > 1 ? "-" + rereferenceIndex : ""),
          dataFootnoteBackref: "",
          ariaLabel: typeof footnoteBackLabel === "string" ? footnoteBackLabel : footnoteBackLabel(referenceIndex, rereferenceIndex),
          className: ["data-footnote-backref"]
        },
        children: Array.isArray(children) ? children : [children]
      });
    }
    const tail = content2[content2.length - 1];
    if (tail && tail.type === "element" && tail.tagName === "p") {
      const tailTail = tail.children[tail.children.length - 1];
      if (tailTail && tailTail.type === "text") {
        tailTail.value += " ";
      } else {
        tail.children.push({ type: "text", value: " " });
      }
      tail.children.push(...backReferences);
    } else {
      content2.push(...backReferences);
    }
    const listItem2 = {
      type: "element",
      tagName: "li",
      properties: { id: clobberPrefix + "fn-" + safeId },
      children: state.wrap(content2, true)
    };
    state.patch(definition2, listItem2);
    listItems.push(listItem2);
  }
  if (listItems.length === 0) {
    return;
  }
  return {
    type: "element",
    tagName: "section",
    properties: { dataFootnotes: true, className: ["footnotes"] },
    children: [
      {
        type: "element",
        tagName: footnoteLabelTagName,
        properties: {
          ...structuredClone$1(footnoteLabelProperties),
          id: "footnote-label"
        },
        children: [{ type: "text", value: footnoteLabel }]
      },
      { type: "text", value: "\n" },
      {
        type: "element",
        tagName: "ol",
        properties: {},
        children: state.wrap(listItems, true)
      },
      { type: "text", value: "\n" }
    ]
  };
}
const convert = (
  // Note: overloads in JSDoc can’t yet use different `@template`s.
  /**
   * @type {(
   *   (<Condition extends string>(test: Condition) => (node: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node & {type: Condition}) &
   *   (<Condition extends Props>(test: Condition) => (node: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node & Condition) &
   *   (<Condition extends TestFunction>(test: Condition) => (node: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node & Predicate<Condition, Node>) &
   *   ((test?: null | undefined) => (node?: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node) &
   *   ((test?: Test) => Check)
   * )}
   */
  /**
   * @param {Test} [test]
   * @returns {Check}
   */
  (function(test) {
    if (test === null || test === void 0) {
      return ok;
    }
    if (typeof test === "function") {
      return castFactory(test);
    }
    if (typeof test === "object") {
      return Array.isArray(test) ? anyFactory(test) : (
        // Cast because `ReadonlyArray` goes into the above but `isArray`
        // narrows to `Array`.
        propertiesFactory(
          /** @type {Props} */
          test
        )
      );
    }
    if (typeof test === "string") {
      return typeFactory(test);
    }
    throw new Error("Expected function, string, or object as test");
  })
);
function anyFactory(tests) {
  const checks2 = [];
  let index2 = -1;
  while (++index2 < tests.length) {
    checks2[index2] = convert(tests[index2]);
  }
  return castFactory(any);
  function any(...parameters) {
    let index3 = -1;
    while (++index3 < checks2.length) {
      if (checks2[index3].apply(this, parameters)) return true;
    }
    return false;
  }
}
function propertiesFactory(check) {
  const checkAsRecord = (
    /** @type {Record<string, unknown>} */
    check
  );
  return castFactory(all2);
  function all2(node2) {
    const nodeAsRecord = (
      /** @type {Record<string, unknown>} */
      /** @type {unknown} */
      node2
    );
    let key;
    for (key in check) {
      if (nodeAsRecord[key] !== checkAsRecord[key]) return false;
    }
    return true;
  }
}
function typeFactory(check) {
  return castFactory(type);
  function type(node2) {
    return node2 && node2.type === check;
  }
}
function castFactory(testFunction) {
  return check;
  function check(value, index2, parent) {
    return Boolean(
      looksLikeANode(value) && testFunction.call(
        this,
        value,
        typeof index2 === "number" ? index2 : void 0,
        parent || void 0
      )
    );
  }
}
function ok() {
  return true;
}
function looksLikeANode(value) {
  return value !== null && typeof value === "object" && "type" in value;
}
function color(d) {
  return d;
}
const empty = [];
const CONTINUE = true;
const EXIT = false;
const SKIP = "skip";
function visitParents(tree, test, visitor, reverse) {
  let check;
  if (typeof test === "function" && typeof visitor !== "function") {
    reverse = visitor;
    visitor = test;
  } else {
    check = test;
  }
  const is = convert(check);
  const step = reverse ? -1 : 1;
  factory(tree, void 0, [])();
  function factory(node2, index2, parents) {
    const value = (
      /** @type {Record<string, unknown>} */
      node2 && typeof node2 === "object" ? node2 : {}
    );
    if (typeof value.type === "string") {
      const name2 = (
        // `hast`
        typeof value.tagName === "string" ? value.tagName : (
          // `xast`
          typeof value.name === "string" ? value.name : void 0
        )
      );
      Object.defineProperty(visit2, "name", {
        value: "node (" + color(node2.type + (name2 ? "<" + name2 + ">" : "")) + ")"
      });
    }
    return visit2;
    function visit2() {
      let result = empty;
      let subresult;
      let offset;
      let grandparents;
      if (!test || is(node2, index2, parents[parents.length - 1] || void 0)) {
        result = toResult(visitor(node2, parents));
        if (result[0] === EXIT) {
          return result;
        }
      }
      if ("children" in node2 && node2.children) {
        const nodeAsParent = (
          /** @type {UnistParent} */
          node2
        );
        if (nodeAsParent.children && result[0] !== SKIP) {
          offset = (reverse ? nodeAsParent.children.length : -1) + step;
          grandparents = parents.concat(nodeAsParent);
          while (offset > -1 && offset < nodeAsParent.children.length) {
            const child = nodeAsParent.children[offset];
            subresult = factory(child, offset, grandparents)();
            if (subresult[0] === EXIT) {
              return subresult;
            }
            offset = typeof subresult[1] === "number" ? subresult[1] : offset + step;
          }
        }
      }
      return result;
    }
  }
}
function toResult(value) {
  if (Array.isArray(value)) {
    return value;
  }
  if (typeof value === "number") {
    return [CONTINUE, value];
  }
  return value === null || value === void 0 ? empty : [value];
}
function visit(tree, testOrVisitor, visitorOrReverse, maybeReverse) {
  let reverse;
  let test;
  let visitor;
  if (typeof testOrVisitor === "function" && typeof visitorOrReverse !== "function") {
    test = void 0;
    visitor = testOrVisitor;
    reverse = visitorOrReverse;
  } else {
    test = testOrVisitor;
    visitor = visitorOrReverse;
    reverse = maybeReverse;
  }
  visitParents(tree, test, overload, reverse);
  function overload(node2, parents) {
    const parent = parents[parents.length - 1];
    const index2 = parent ? parent.children.indexOf(node2) : void 0;
    return visitor(node2, index2, parent);
  }
}
const own$1 = {}.hasOwnProperty;
const emptyOptions$1 = {};
function createState(tree, options) {
  const settings = options || emptyOptions$1;
  const definitionById = /* @__PURE__ */ new Map();
  const footnoteById = /* @__PURE__ */ new Map();
  const footnoteCounts = /* @__PURE__ */ new Map();
  const handlers$1 = { ...handlers, ...settings.handlers };
  const state = {
    all: all2,
    applyData,
    definitionById,
    footnoteById,
    footnoteCounts,
    footnoteOrder: [],
    handlers: handlers$1,
    one: one2,
    options: settings,
    patch,
    wrap: wrap$1
  };
  visit(tree, function(node2) {
    if (node2.type === "definition" || node2.type === "footnoteDefinition") {
      const map2 = node2.type === "definition" ? definitionById : footnoteById;
      const id2 = String(node2.identifier).toUpperCase();
      if (!map2.has(id2)) {
        map2.set(id2, node2);
      }
    }
  });
  return state;
  function one2(node2, parent) {
    const type = node2.type;
    const handle2 = state.handlers[type];
    if (own$1.call(state.handlers, type) && handle2) {
      return handle2(state, node2, parent);
    }
    if (state.options.passThrough && state.options.passThrough.includes(type)) {
      if ("children" in node2) {
        const { children, ...shallow } = node2;
        const result = structuredClone$1(shallow);
        result.children = state.all(node2);
        return result;
      }
      return structuredClone$1(node2);
    }
    const unknown = state.options.unknownHandler || defaultUnknownHandler;
    return unknown(state, node2, parent);
  }
  function all2(parent) {
    const values = [];
    if ("children" in parent) {
      const nodes = parent.children;
      let index2 = -1;
      while (++index2 < nodes.length) {
        const result = state.one(nodes[index2], parent);
        if (result) {
          if (index2 && nodes[index2 - 1].type === "break") {
            if (!Array.isArray(result) && result.type === "text") {
              result.value = trimMarkdownSpaceStart(result.value);
            }
            if (!Array.isArray(result) && result.type === "element") {
              const head = result.children[0];
              if (head && head.type === "text") {
                head.value = trimMarkdownSpaceStart(head.value);
              }
            }
          }
          if (Array.isArray(result)) {
            values.push(...result);
          } else {
            values.push(result);
          }
        }
      }
    }
    return values;
  }
}
function patch(from, to) {
  if (from.position) to.position = position$1(from);
}
function applyData(from, to) {
  let result = to;
  if (from && from.data) {
    const hName = from.data.hName;
    const hChildren = from.data.hChildren;
    const hProperties = from.data.hProperties;
    if (typeof hName === "string") {
      if (result.type === "element") {
        result.tagName = hName;
      } else {
        const children = "children" in result ? result.children : [result];
        result = { type: "element", tagName: hName, properties: {}, children };
      }
    }
    if (result.type === "element" && hProperties) {
      Object.assign(result.properties, structuredClone$1(hProperties));
    }
    if ("children" in result && result.children && hChildren !== null && hChildren !== void 0) {
      result.children = hChildren;
    }
  }
  return result;
}
function defaultUnknownHandler(state, node2) {
  const data2 = node2.data || {};
  const result = "value" in node2 && !(own$1.call(data2, "hProperties") || own$1.call(data2, "hChildren")) ? { type: "text", value: node2.value } : {
    type: "element",
    tagName: "div",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function wrap$1(nodes, loose) {
  const result = [];
  let index2 = -1;
  if (loose) {
    result.push({ type: "text", value: "\n" });
  }
  while (++index2 < nodes.length) {
    if (index2) result.push({ type: "text", value: "\n" });
    result.push(nodes[index2]);
  }
  if (loose && nodes.length > 0) {
    result.push({ type: "text", value: "\n" });
  }
  return result;
}
function trimMarkdownSpaceStart(value) {
  let index2 = 0;
  let code2 = value.charCodeAt(index2);
  while (code2 === 9 || code2 === 32) {
    index2++;
    code2 = value.charCodeAt(index2);
  }
  return value.slice(index2);
}
function toHast(tree, options) {
  const state = createState(tree, options);
  const node2 = state.one(tree, void 0);
  const foot = footer(state);
  const result = Array.isArray(node2) ? { type: "root", children: node2 } : node2 || { type: "root", children: [] };
  if (foot) {
    result.children.push({ type: "text", value: "\n" }, foot);
  }
  return result;
}
function remarkRehype(destination, options) {
  if (destination && "run" in destination) {
    return async function(tree, file) {
      const hastTree = (
        /** @type {HastRoot} */
        toHast(tree, { file, ...options })
      );
      await destination.run(hastTree, file);
    };
  }
  return function(tree, file) {
    return (
      /** @type {HastRoot} */
      toHast(tree, { file, ...destination || options })
    );
  };
}
function bail(error) {
  if (error) {
    throw error;
  }
}
var extend$1;
var hasRequiredExtend;
function requireExtend() {
  if (hasRequiredExtend) return extend$1;
  hasRequiredExtend = 1;
  var hasOwn2 = Object.prototype.hasOwnProperty;
  var toStr = Object.prototype.toString;
  var defineProperty = Object.defineProperty;
  var gOPD = Object.getOwnPropertyDescriptor;
  var isArray = function isArray2(arr) {
    if (typeof Array.isArray === "function") {
      return Array.isArray(arr);
    }
    return toStr.call(arr) === "[object Array]";
  };
  var isPlainObject2 = function isPlainObject3(obj) {
    if (!obj || toStr.call(obj) !== "[object Object]") {
      return false;
    }
    var hasOwnConstructor = hasOwn2.call(obj, "constructor");
    var hasIsPrototypeOf = obj.constructor && obj.constructor.prototype && hasOwn2.call(obj.constructor.prototype, "isPrototypeOf");
    if (obj.constructor && !hasOwnConstructor && !hasIsPrototypeOf) {
      return false;
    }
    var key;
    for (key in obj) {
    }
    return typeof key === "undefined" || hasOwn2.call(obj, key);
  };
  var setProperty = function setProperty2(target, options) {
    if (defineProperty && options.name === "__proto__") {
      defineProperty(target, options.name, {
        enumerable: true,
        configurable: true,
        value: options.newValue,
        writable: true
      });
    } else {
      target[options.name] = options.newValue;
    }
  };
  var getProperty = function getProperty2(obj, name2) {
    if (name2 === "__proto__") {
      if (!hasOwn2.call(obj, name2)) {
        return void 0;
      } else if (gOPD) {
        return gOPD(obj, name2).value;
      }
    }
    return obj[name2];
  };
  extend$1 = function extend2() {
    var options, name2, src, copy, copyIsArray, clone;
    var target = arguments[0];
    var i = 1;
    var length = arguments.length;
    var deep = false;
    if (typeof target === "boolean") {
      deep = target;
      target = arguments[1] || {};
      i = 2;
    }
    if (target == null || typeof target !== "object" && typeof target !== "function") {
      target = {};
    }
    for (; i < length; ++i) {
      options = arguments[i];
      if (options != null) {
        for (name2 in options) {
          src = getProperty(target, name2);
          copy = getProperty(options, name2);
          if (target !== copy) {
            if (deep && copy && (isPlainObject2(copy) || (copyIsArray = isArray(copy)))) {
              if (copyIsArray) {
                copyIsArray = false;
                clone = src && isArray(src) ? src : [];
              } else {
                clone = src && isPlainObject2(src) ? src : {};
              }
              setProperty(target, { name: name2, newValue: extend2(deep, clone, copy) });
            } else if (typeof copy !== "undefined") {
              setProperty(target, { name: name2, newValue: copy });
            }
          }
        }
      }
    }
    return target;
  };
  return extend$1;
}
var extendExports = requireExtend();
const extend = /* @__PURE__ */ getDefaultExportFromCjs(extendExports);
function isPlainObject$2(value) {
  if (typeof value !== "object" || value === null) {
    return false;
  }
  const prototype = Object.getPrototypeOf(value);
  return (prototype === null || prototype === Object.prototype || Object.getPrototypeOf(prototype) === null) && !(Symbol.toStringTag in value) && !(Symbol.iterator in value);
}
function trough() {
  const fns = [];
  const pipeline = { run, use };
  return pipeline;
  function run(...values) {
    let middlewareIndex = -1;
    const callback = values.pop();
    if (typeof callback !== "function") {
      throw new TypeError("Expected function as last argument, not " + callback);
    }
    next(null, ...values);
    function next(error, ...output) {
      const fn = fns[++middlewareIndex];
      let index2 = -1;
      if (error) {
        callback(error);
        return;
      }
      while (++index2 < values.length) {
        if (output[index2] === null || output[index2] === void 0) {
          output[index2] = values[index2];
        }
      }
      values = output;
      if (fn) {
        wrap(fn, next)(...output);
      } else {
        callback(null, ...output);
      }
    }
  }
  function use(middelware) {
    if (typeof middelware !== "function") {
      throw new TypeError(
        "Expected `middelware` to be a function, not " + middelware
      );
    }
    fns.push(middelware);
    return pipeline;
  }
}
function wrap(middleware, callback) {
  let called;
  return wrapped;
  function wrapped(...parameters) {
    const fnExpectsCallback = middleware.length > parameters.length;
    let result;
    if (fnExpectsCallback) {
      parameters.push(done);
    }
    try {
      result = middleware.apply(this, parameters);
    } catch (error) {
      const exception = (
        /** @type {Error} */
        error
      );
      if (fnExpectsCallback && called) {
        throw exception;
      }
      return done(exception);
    }
    if (!fnExpectsCallback) {
      if (result && result.then && typeof result.then === "function") {
        result.then(then, done);
      } else if (result instanceof Error) {
        done(result);
      } else {
        then(result);
      }
    }
  }
  function done(error, ...output) {
    if (!called) {
      called = true;
      callback(error, ...output);
    }
  }
  function then(value) {
    done(null, value);
  }
}
const minpath = { basename, dirname, extname, join, sep: "/" };
function basename(path2, extname2) {
  if (extname2 !== void 0 && typeof extname2 !== "string") {
    throw new TypeError('"ext" argument must be a string');
  }
  assertPath$1(path2);
  let start = 0;
  let end = -1;
  let index2 = path2.length;
  let seenNonSlash;
  if (extname2 === void 0 || extname2.length === 0 || extname2.length > path2.length) {
    while (index2--) {
      if (path2.codePointAt(index2) === 47) {
        if (seenNonSlash) {
          start = index2 + 1;
          break;
        }
      } else if (end < 0) {
        seenNonSlash = true;
        end = index2 + 1;
      }
    }
    return end < 0 ? "" : path2.slice(start, end);
  }
  if (extname2 === path2) {
    return "";
  }
  let firstNonSlashEnd = -1;
  let extnameIndex = extname2.length - 1;
  while (index2--) {
    if (path2.codePointAt(index2) === 47) {
      if (seenNonSlash) {
        start = index2 + 1;
        break;
      }
    } else {
      if (firstNonSlashEnd < 0) {
        seenNonSlash = true;
        firstNonSlashEnd = index2 + 1;
      }
      if (extnameIndex > -1) {
        if (path2.codePointAt(index2) === extname2.codePointAt(extnameIndex--)) {
          if (extnameIndex < 0) {
            end = index2;
          }
        } else {
          extnameIndex = -1;
          end = firstNonSlashEnd;
        }
      }
    }
  }
  if (start === end) {
    end = firstNonSlashEnd;
  } else if (end < 0) {
    end = path2.length;
  }
  return path2.slice(start, end);
}
function dirname(path2) {
  assertPath$1(path2);
  if (path2.length === 0) {
    return ".";
  }
  let end = -1;
  let index2 = path2.length;
  let unmatchedSlash;
  while (--index2) {
    if (path2.codePointAt(index2) === 47) {
      if (unmatchedSlash) {
        end = index2;
        break;
      }
    } else if (!unmatchedSlash) {
      unmatchedSlash = true;
    }
  }
  return end < 0 ? path2.codePointAt(0) === 47 ? "/" : "." : end === 1 && path2.codePointAt(0) === 47 ? "//" : path2.slice(0, end);
}
function extname(path2) {
  assertPath$1(path2);
  let index2 = path2.length;
  let end = -1;
  let startPart = 0;
  let startDot = -1;
  let preDotState = 0;
  let unmatchedSlash;
  while (index2--) {
    const code2 = path2.codePointAt(index2);
    if (code2 === 47) {
      if (unmatchedSlash) {
        startPart = index2 + 1;
        break;
      }
      continue;
    }
    if (end < 0) {
      unmatchedSlash = true;
      end = index2 + 1;
    }
    if (code2 === 46) {
      if (startDot < 0) {
        startDot = index2;
      } else if (preDotState !== 1) {
        preDotState = 1;
      }
    } else if (startDot > -1) {
      preDotState = -1;
    }
  }
  if (startDot < 0 || end < 0 || // We saw a non-dot character immediately before the dot.
  preDotState === 0 || // The (right-most) trimmed path component is exactly `..`.
  preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) {
    return "";
  }
  return path2.slice(startDot, end);
}
function join(...segments) {
  let index2 = -1;
  let joined;
  while (++index2 < segments.length) {
    assertPath$1(segments[index2]);
    if (segments[index2]) {
      joined = joined === void 0 ? segments[index2] : joined + "/" + segments[index2];
    }
  }
  return joined === void 0 ? "." : normalize(joined);
}
function normalize(path2) {
  assertPath$1(path2);
  const absolute = path2.codePointAt(0) === 47;
  let value = normalizeString(path2, !absolute);
  if (value.length === 0 && !absolute) {
    value = ".";
  }
  if (value.length > 0 && path2.codePointAt(path2.length - 1) === 47) {
    value += "/";
  }
  return absolute ? "/" + value : value;
}
function normalizeString(path2, allowAboveRoot) {
  let result = "";
  let lastSegmentLength = 0;
  let lastSlash = -1;
  let dots = 0;
  let index2 = -1;
  let code2;
  let lastSlashIndex;
  while (++index2 <= path2.length) {
    if (index2 < path2.length) {
      code2 = path2.codePointAt(index2);
    } else if (code2 === 47) {
      break;
    } else {
      code2 = 47;
    }
    if (code2 === 47) {
      if (lastSlash === index2 - 1 || dots === 1) ;
      else if (lastSlash !== index2 - 1 && dots === 2) {
        if (result.length < 2 || lastSegmentLength !== 2 || result.codePointAt(result.length - 1) !== 46 || result.codePointAt(result.length - 2) !== 46) {
          if (result.length > 2) {
            lastSlashIndex = result.lastIndexOf("/");
            if (lastSlashIndex !== result.length - 1) {
              if (lastSlashIndex < 0) {
                result = "";
                lastSegmentLength = 0;
              } else {
                result = result.slice(0, lastSlashIndex);
                lastSegmentLength = result.length - 1 - result.lastIndexOf("/");
              }
              lastSlash = index2;
              dots = 0;
              continue;
            }
          } else if (result.length > 0) {
            result = "";
            lastSegmentLength = 0;
            lastSlash = index2;
            dots = 0;
            continue;
          }
        }
        if (allowAboveRoot) {
          result = result.length > 0 ? result + "/.." : "..";
          lastSegmentLength = 2;
        }
      } else {
        if (result.length > 0) {
          result += "/" + path2.slice(lastSlash + 1, index2);
        } else {
          result = path2.slice(lastSlash + 1, index2);
        }
        lastSegmentLength = index2 - lastSlash - 1;
      }
      lastSlash = index2;
      dots = 0;
    } else if (code2 === 46 && dots > -1) {
      dots++;
    } else {
      dots = -1;
    }
  }
  return result;
}
function assertPath$1(path2) {
  if (typeof path2 !== "string") {
    throw new TypeError(
      "Path must be a string. Received " + JSON.stringify(path2)
    );
  }
}
const minproc = { cwd };
function cwd() {
  return "/";
}
function isUrl(fileUrlOrPath) {
  return Boolean(
    fileUrlOrPath !== null && typeof fileUrlOrPath === "object" && "href" in fileUrlOrPath && fileUrlOrPath.href && "protocol" in fileUrlOrPath && fileUrlOrPath.protocol && // @ts-expect-error: indexing is fine.
    fileUrlOrPath.auth === void 0
  );
}
function urlToPath(path2) {
  if (typeof path2 === "string") {
    path2 = new URL(path2);
  } else if (!isUrl(path2)) {
    const error = new TypeError(
      'The "path" argument must be of type string or an instance of URL. Received `' + path2 + "`"
    );
    error.code = "ERR_INVALID_ARG_TYPE";
    throw error;
  }
  if (path2.protocol !== "file:") {
    const error = new TypeError("The URL must be of scheme file");
    error.code = "ERR_INVALID_URL_SCHEME";
    throw error;
  }
  return getPathFromURLPosix(path2);
}
function getPathFromURLPosix(url) {
  if (url.hostname !== "") {
    const error = new TypeError(
      'File URL host must be "localhost" or empty on darwin'
    );
    error.code = "ERR_INVALID_FILE_URL_HOST";
    throw error;
  }
  const pathname = url.pathname;
  let index2 = -1;
  while (++index2 < pathname.length) {
    if (pathname.codePointAt(index2) === 37 && pathname.codePointAt(index2 + 1) === 50) {
      const third = pathname.codePointAt(index2 + 2);
      if (third === 70 || third === 102) {
        const error = new TypeError(
          "File URL path must not include encoded / characters"
        );
        error.code = "ERR_INVALID_FILE_URL_PATH";
        throw error;
      }
    }
  }
  return decodeURIComponent(pathname);
}
const order = (
  /** @type {const} */
  [
    "history",
    "path",
    "basename",
    "stem",
    "extname",
    "dirname"
  ]
);
class VFile {
  /**
   * Create a new virtual file.
   *
   * `options` is treated as:
   *
   * *   `string` or `Uint8Array` — `{value: options}`
   * *   `URL` — `{path: options}`
   * *   `VFile` — shallow copies its data over to the new file
   * *   `object` — all fields are shallow copied over to the new file
   *
   * Path related fields are set in the following order (least specific to
   * most specific): `history`, `path`, `basename`, `stem`, `extname`,
   * `dirname`.
   *
   * You cannot set `dirname` or `extname` without setting either `history`,
   * `path`, `basename`, or `stem` too.
   *
   * @param {Compatible | null | undefined} [value]
   *   File value.
   * @returns
   *   New instance.
   */
  constructor(value) {
    let options;
    if (!value) {
      options = {};
    } else if (isUrl(value)) {
      options = { path: value };
    } else if (typeof value === "string" || isUint8Array$1(value)) {
      options = { value };
    } else {
      options = value;
    }
    this.cwd = "cwd" in options ? "" : minproc.cwd();
    this.data = {};
    this.history = [];
    this.messages = [];
    this.value;
    this.map;
    this.result;
    this.stored;
    let index2 = -1;
    while (++index2 < order.length) {
      const field2 = order[index2];
      if (field2 in options && options[field2] !== void 0 && options[field2] !== null) {
        this[field2] = field2 === "history" ? [...options[field2]] : options[field2];
      }
    }
    let field;
    for (field in options) {
      if (!order.includes(field)) {
        this[field] = options[field];
      }
    }
  }
  /**
   * Get the basename (including extname) (example: `'index.min.js'`).
   *
   * @returns {string | undefined}
   *   Basename.
   */
  get basename() {
    return typeof this.path === "string" ? minpath.basename(this.path) : void 0;
  }
  /**
   * Set basename (including extname) (`'index.min.js'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be nullified (use `file.path = file.dirname` instead).
   *
   * @param {string} basename
   *   Basename.
   * @returns {undefined}
   *   Nothing.
   */
  set basename(basename2) {
    assertNonEmpty(basename2, "basename");
    assertPart(basename2, "basename");
    this.path = minpath.join(this.dirname || "", basename2);
  }
  /**
   * Get the parent path (example: `'~'`).
   *
   * @returns {string | undefined}
   *   Dirname.
   */
  get dirname() {
    return typeof this.path === "string" ? minpath.dirname(this.path) : void 0;
  }
  /**
   * Set the parent path (example: `'~'`).
   *
   * Cannot be set if there’s no `path` yet.
   *
   * @param {string | undefined} dirname
   *   Dirname.
   * @returns {undefined}
   *   Nothing.
   */
  set dirname(dirname2) {
    assertPath(this.basename, "dirname");
    this.path = minpath.join(dirname2 || "", this.basename);
  }
  /**
   * Get the extname (including dot) (example: `'.js'`).
   *
   * @returns {string | undefined}
   *   Extname.
   */
  get extname() {
    return typeof this.path === "string" ? minpath.extname(this.path) : void 0;
  }
  /**
   * Set the extname (including dot) (example: `'.js'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be set if there’s no `path` yet.
   *
   * @param {string | undefined} extname
   *   Extname.
   * @returns {undefined}
   *   Nothing.
   */
  set extname(extname2) {
    assertPart(extname2, "extname");
    assertPath(this.dirname, "extname");
    if (extname2) {
      if (extname2.codePointAt(0) !== 46) {
        throw new Error("`extname` must start with `.`");
      }
      if (extname2.includes(".", 1)) {
        throw new Error("`extname` cannot contain multiple dots");
      }
    }
    this.path = minpath.join(this.dirname, this.stem + (extname2 || ""));
  }
  /**
   * Get the full path (example: `'~/index.min.js'`).
   *
   * @returns {string}
   *   Path.
   */
  get path() {
    return this.history[this.history.length - 1];
  }
  /**
   * Set the full path (example: `'~/index.min.js'`).
   *
   * Cannot be nullified.
   * You can set a file URL (a `URL` object with a `file:` protocol) which will
   * be turned into a path with `url.fileURLToPath`.
   *
   * @param {URL | string} path
   *   Path.
   * @returns {undefined}
   *   Nothing.
   */
  set path(path2) {
    if (isUrl(path2)) {
      path2 = urlToPath(path2);
    }
    assertNonEmpty(path2, "path");
    if (this.path !== path2) {
      this.history.push(path2);
    }
  }
  /**
   * Get the stem (basename w/o extname) (example: `'index.min'`).
   *
   * @returns {string | undefined}
   *   Stem.
   */
  get stem() {
    return typeof this.path === "string" ? minpath.basename(this.path, this.extname) : void 0;
  }
  /**
   * Set the stem (basename w/o extname) (example: `'index.min'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be nullified (use `file.path = file.dirname` instead).
   *
   * @param {string} stem
   *   Stem.
   * @returns {undefined}
   *   Nothing.
   */
  set stem(stem) {
    assertNonEmpty(stem, "stem");
    assertPart(stem, "stem");
    this.path = minpath.join(this.dirname || "", stem + (this.extname || ""));
  }
  // Normal prototypal methods.
  /**
   * Create a fatal message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `true` (error; file not usable)
   * and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {never}
   *   Never.
   * @throws {VFileMessage}
   *   Message.
   */
  fail(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = this.message(causeOrReason, optionsOrParentOrPlace, origin);
    message.fatal = true;
    throw message;
  }
  /**
   * Create an info message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `undefined` (info; change
   * likely not needed) and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {VFileMessage}
   *   Message.
   */
  info(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = this.message(causeOrReason, optionsOrParentOrPlace, origin);
    message.fatal = void 0;
    return message;
  }
  /**
   * Create a message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `false` (warning; change may be
   * needed) and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {VFileMessage}
   *   Message.
   */
  message(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = new VFileMessage(
      // @ts-expect-error: the overloads are fine.
      causeOrReason,
      optionsOrParentOrPlace,
      origin
    );
    if (this.path) {
      message.name = this.path + ":" + message.name;
      message.file = this.path;
    }
    message.fatal = false;
    this.messages.push(message);
    return message;
  }
  /**
   * Serialize the file.
   *
   * > **Note**: which encodings are supported depends on the engine.
   * > For info on Node.js, see:
   * > <https://nodejs.org/api/util.html#whatwg-supported-encodings>.
   *
   * @param {string | null | undefined} [encoding='utf8']
   *   Character encoding to understand `value` as when it’s a `Uint8Array`
   *   (default: `'utf-8'`).
   * @returns {string}
   *   Serialized file.
   */
  toString(encoding) {
    if (this.value === void 0) {
      return "";
    }
    if (typeof this.value === "string") {
      return this.value;
    }
    const decoder = new TextDecoder(encoding || void 0);
    return decoder.decode(this.value);
  }
}
function assertPart(part, name2) {
  if (part && part.includes(minpath.sep)) {
    throw new Error(
      "`" + name2 + "` cannot be a path: did not expect `" + minpath.sep + "`"
    );
  }
}
function assertNonEmpty(part, name2) {
  if (!part) {
    throw new Error("`" + name2 + "` cannot be empty");
  }
}
function assertPath(path2, name2) {
  if (!path2) {
    throw new Error("Setting `" + name2 + "` requires `path` to be set too");
  }
}
function isUint8Array$1(value) {
  return Boolean(
    value && typeof value === "object" && "byteLength" in value && "byteOffset" in value
  );
}
const CallableInstance = (
  /**
   * @type {new <Parameters extends Array<unknown>, Result>(property: string | symbol) => (...parameters: Parameters) => Result}
   */
  /** @type {unknown} */
  /**
   * @this {Function}
   * @param {string | symbol} property
   * @returns {(...parameters: Array<unknown>) => unknown}
   */
  (function(property) {
    const self2 = this;
    const constr = self2.constructor;
    const proto = (
      /** @type {Record<string | symbol, Function>} */
      // Prototypes do exist.
      // type-coverage:ignore-next-line
      constr.prototype
    );
    const value = proto[property];
    const apply = function() {
      return value.apply(apply, arguments);
    };
    Object.setPrototypeOf(apply, proto);
    return apply;
  })
);
const own = {}.hasOwnProperty;
class Processor extends CallableInstance {
  /**
   * Create a processor.
   */
  constructor() {
    super("copy");
    this.Compiler = void 0;
    this.Parser = void 0;
    this.attachers = [];
    this.compiler = void 0;
    this.freezeIndex = -1;
    this.frozen = void 0;
    this.namespace = {};
    this.parser = void 0;
    this.transformers = trough();
  }
  /**
   * Copy a processor.
   *
   * @deprecated
   *   This is a private internal method and should not be used.
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   New *unfrozen* processor ({@linkcode Processor}) that is
   *   configured to work the same as its ancestor.
   *   When the descendant processor is configured in the future it does not
   *   affect the ancestral processor.
   */
  copy() {
    const destination = (
      /** @type {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>} */
      new Processor()
    );
    let index2 = -1;
    while (++index2 < this.attachers.length) {
      const attacher = this.attachers[index2];
      destination.use(...attacher);
    }
    destination.data(extend(true, {}, this.namespace));
    return destination;
  }
  /**
   * Configure the processor with info available to all plugins.
   * Information is stored in an object.
   *
   * Typically, options can be given to a specific plugin, but sometimes it
   * makes sense to have information shared with several plugins.
   * For example, a list of HTML elements that are self-closing, which is
   * needed during all phases.
   *
   * > **Note**: setting information cannot occur on *frozen* processors.
   * > Call the processor first to create a new unfrozen processor.
   *
   * > **Note**: to register custom data in TypeScript, augment the
   * > {@linkcode Data} interface.
   *
   * @example
   *   This example show how to get and set info:
   *
   *   ```js
   *   import {unified} from 'unified'
   *
   *   const processor = unified().data('alpha', 'bravo')
   *
   *   processor.data('alpha') // => 'bravo'
   *
   *   processor.data() // => {alpha: 'bravo'}
   *
   *   processor.data({charlie: 'delta'})
   *
   *   processor.data() // => {charlie: 'delta'}
   *   ```
   *
   * @template {keyof Data} Key
   *
   * @overload
   * @returns {Data}
   *
   * @overload
   * @param {Data} dataset
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {Key} key
   * @returns {Data[Key]}
   *
   * @overload
   * @param {Key} key
   * @param {Data[Key]} value
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @param {Data | Key} [key]
   *   Key to get or set, or entire dataset to set, or nothing to get the
   *   entire dataset (optional).
   * @param {Data[Key]} [value]
   *   Value to set (optional).
   * @returns {unknown}
   *   The current processor when setting, the value at `key` when getting, or
   *   the entire dataset when getting without key.
   */
  data(key, value) {
    if (typeof key === "string") {
      if (arguments.length === 2) {
        assertUnfrozen("data", this.frozen);
        this.namespace[key] = value;
        return this;
      }
      return own.call(this.namespace, key) && this.namespace[key] || void 0;
    }
    if (key) {
      assertUnfrozen("data", this.frozen);
      this.namespace = key;
      return this;
    }
    return this.namespace;
  }
  /**
   * Freeze a processor.
   *
   * Frozen processors are meant to be extended and not to be configured
   * directly.
   *
   * When a processor is frozen it cannot be unfrozen.
   * New processors working the same way can be created by calling the
   * processor.
   *
   * It’s possible to freeze processors explicitly by calling `.freeze()`.
   * Processors freeze automatically when `.parse()`, `.run()`, `.runSync()`,
   * `.stringify()`, `.process()`, or `.processSync()` are called.
   *
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   The current processor.
   */
  freeze() {
    if (this.frozen) {
      return this;
    }
    const self2 = (
      /** @type {Processor} */
      /** @type {unknown} */
      this
    );
    while (++this.freezeIndex < this.attachers.length) {
      const [attacher, ...options] = this.attachers[this.freezeIndex];
      if (options[0] === false) {
        continue;
      }
      if (options[0] === true) {
        options[0] = void 0;
      }
      const transformer = attacher.call(self2, ...options);
      if (typeof transformer === "function") {
        this.transformers.use(transformer);
      }
    }
    this.frozen = true;
    this.freezeIndex = Number.POSITIVE_INFINITY;
    return this;
  }
  /**
   * Parse text to a syntax tree.
   *
   * > **Note**: `parse` freezes the processor if not already *frozen*.
   *
   * > **Note**: `parse` performs the parse phase, not the run phase or other
   * > phases.
   *
   * @param {Compatible | undefined} [file]
   *   file to parse (optional); typically `string` or `VFile`; any value
   *   accepted as `x` in `new VFile(x)`.
   * @returns {ParseTree extends undefined ? Node : ParseTree}
   *   Syntax tree representing `file`.
   */
  parse(file) {
    this.freeze();
    const realFile = vfile(file);
    const parser = this.parser || this.Parser;
    assertParser("parse", parser);
    return parser(String(realFile), realFile);
  }
  /**
   * Process the given file as configured on the processor.
   *
   * > **Note**: `process` freezes the processor if not already *frozen*.
   *
   * > **Note**: `process` performs the parse, run, and stringify phases.
   *
   * @overload
   * @param {Compatible | undefined} file
   * @param {ProcessCallback<VFileWithOutput<CompileResult>>} done
   * @returns {undefined}
   *
   * @overload
   * @param {Compatible | undefined} [file]
   * @returns {Promise<VFileWithOutput<CompileResult>>}
   *
   * @param {Compatible | undefined} [file]
   *   File (optional); typically `string` or `VFile`]; any value accepted as
   *   `x` in `new VFile(x)`.
   * @param {ProcessCallback<VFileWithOutput<CompileResult>> | undefined} [done]
   *   Callback (optional).
   * @returns {Promise<VFile> | undefined}
   *   Nothing if `done` is given.
   *   Otherwise a promise, rejected with a fatal error or resolved with the
   *   processed file.
   *
   *   The parsed, transformed, and compiled value is available at
   *   `file.value` (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most
   *   > compilers return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  process(file, done) {
    const self2 = this;
    this.freeze();
    assertParser("process", this.parser || this.Parser);
    assertCompiler("process", this.compiler || this.Compiler);
    return done ? executor(void 0, done) : new Promise(executor);
    function executor(resolve, reject) {
      const realFile = vfile(file);
      const parseTree = (
        /** @type {HeadTree extends undefined ? Node : HeadTree} */
        /** @type {unknown} */
        self2.parse(realFile)
      );
      self2.run(parseTree, realFile, function(error, tree, file2) {
        if (error || !tree || !file2) {
          return realDone(error);
        }
        const compileTree = (
          /** @type {CompileTree extends undefined ? Node : CompileTree} */
          /** @type {unknown} */
          tree
        );
        const compileResult = self2.stringify(compileTree, file2);
        if (looksLikeAValue(compileResult)) {
          file2.value = compileResult;
        } else {
          file2.result = compileResult;
        }
        realDone(
          error,
          /** @type {VFileWithOutput<CompileResult>} */
          file2
        );
      });
      function realDone(error, file2) {
        if (error || !file2) {
          reject(error);
        } else if (resolve) {
          resolve(file2);
        } else {
          done(void 0, file2);
        }
      }
    }
  }
  /**
   * Process the given file as configured on the processor.
   *
   * An error is thrown if asynchronous transforms are configured.
   *
   * > **Note**: `processSync` freezes the processor if not already *frozen*.
   *
   * > **Note**: `processSync` performs the parse, run, and stringify phases.
   *
   * @param {Compatible | undefined} [file]
   *   File (optional); typically `string` or `VFile`; any value accepted as
   *   `x` in `new VFile(x)`.
   * @returns {VFileWithOutput<CompileResult>}
   *   The processed file.
   *
   *   The parsed, transformed, and compiled value is available at
   *   `file.value` (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most
   *   > compilers return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  processSync(file) {
    let complete = false;
    let result;
    this.freeze();
    assertParser("processSync", this.parser || this.Parser);
    assertCompiler("processSync", this.compiler || this.Compiler);
    this.process(file, realDone);
    assertDone("processSync", "process", complete);
    return result;
    function realDone(error, file2) {
      complete = true;
      bail(error);
      result = file2;
    }
  }
  /**
   * Run *transformers* on a syntax tree.
   *
   * > **Note**: `run` freezes the processor if not already *frozen*.
   *
   * > **Note**: `run` performs the run phase, not other phases.
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} done
   * @returns {undefined}
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {Compatible | undefined} file
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} done
   * @returns {undefined}
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {Compatible | undefined} [file]
   * @returns {Promise<TailTree extends undefined ? Node : TailTree>}
   *
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   *   Tree to transform and inspect.
   * @param {(
   *   RunCallback<TailTree extends undefined ? Node : TailTree> |
   *   Compatible
   * )} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} [done]
   *   Callback (optional).
   * @returns {Promise<TailTree extends undefined ? Node : TailTree> | undefined}
   *   Nothing if `done` is given.
   *   Otherwise, a promise rejected with a fatal error or resolved with the
   *   transformed tree.
   */
  run(tree, file, done) {
    assertNode(tree);
    this.freeze();
    const transformers = this.transformers;
    if (!done && typeof file === "function") {
      done = file;
      file = void 0;
    }
    return done ? executor(void 0, done) : new Promise(executor);
    function executor(resolve, reject) {
      const realFile = vfile(file);
      transformers.run(tree, realFile, realDone);
      function realDone(error, outputTree, file2) {
        const resultingTree = (
          /** @type {TailTree extends undefined ? Node : TailTree} */
          outputTree || tree
        );
        if (error) {
          reject(error);
        } else if (resolve) {
          resolve(resultingTree);
        } else {
          done(void 0, resultingTree, file2);
        }
      }
    }
  }
  /**
   * Run *transformers* on a syntax tree.
   *
   * An error is thrown if asynchronous transforms are configured.
   *
   * > **Note**: `runSync` freezes the processor if not already *frozen*.
   *
   * > **Note**: `runSync` performs the run phase, not other phases.
   *
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   *   Tree to transform and inspect.
   * @param {Compatible | undefined} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @returns {TailTree extends undefined ? Node : TailTree}
   *   Transformed tree.
   */
  runSync(tree, file) {
    let complete = false;
    let result;
    this.run(tree, file, realDone);
    assertDone("runSync", "run", complete);
    return result;
    function realDone(error, tree2) {
      bail(error);
      result = tree2;
      complete = true;
    }
  }
  /**
   * Compile a syntax tree.
   *
   * > **Note**: `stringify` freezes the processor if not already *frozen*.
   *
   * > **Note**: `stringify` performs the stringify phase, not the run phase
   * > or other phases.
   *
   * @param {CompileTree extends undefined ? Node : CompileTree} tree
   *   Tree to compile.
   * @param {Compatible | undefined} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @returns {CompileResult extends undefined ? Value : CompileResult}
   *   Textual representation of the tree (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most compilers
   *   > return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  stringify(tree, file) {
    this.freeze();
    const realFile = vfile(file);
    const compiler2 = this.compiler || this.Compiler;
    assertCompiler("stringify", compiler2);
    assertNode(tree);
    return compiler2(tree, realFile);
  }
  /**
   * Configure the processor to use a plugin, a list of usable values, or a
   * preset.
   *
   * If the processor is already using a plugin, the previous plugin
   * configuration is changed based on the options that are passed in.
   * In other words, the plugin is not added a second time.
   *
   * > **Note**: `use` cannot be called on *frozen* processors.
   * > Call the processor first to create a new unfrozen processor.
   *
   * @example
   *   There are many ways to pass plugins to `.use()`.
   *   This example gives an overview:
   *
   *   ```js
   *   import {unified} from 'unified'
   *
   *   unified()
   *     // Plugin with options:
   *     .use(pluginA, {x: true, y: true})
   *     // Passing the same plugin again merges configuration (to `{x: true, y: false, z: true}`):
   *     .use(pluginA, {y: false, z: true})
   *     // Plugins:
   *     .use([pluginB, pluginC])
   *     // Two plugins, the second with options:
   *     .use([pluginD, [pluginE, {}]])
   *     // Preset with plugins and settings:
   *     .use({plugins: [pluginF, [pluginG, {}]], settings: {position: false}})
   *     // Settings only:
   *     .use({settings: {position: false}})
   *   ```
   *
   * @template {Array<unknown>} [Parameters=[]]
   * @template {Node | string | undefined} [Input=undefined]
   * @template [Output=Input]
   *
   * @overload
   * @param {Preset | null | undefined} [preset]
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {PluggableList} list
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {Plugin<Parameters, Input, Output>} plugin
   * @param {...(Parameters | [boolean])} parameters
   * @returns {UsePlugin<ParseTree, HeadTree, TailTree, CompileTree, CompileResult, Input, Output>}
   *
   * @param {PluggableList | Plugin | Preset | null | undefined} value
   *   Usable value.
   * @param {...unknown} parameters
   *   Parameters, when a plugin is given as a usable value.
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   Current processor.
   */
  use(value, ...parameters) {
    const attachers = this.attachers;
    const namespace = this.namespace;
    assertUnfrozen("use", this.frozen);
    if (value === null || value === void 0) ;
    else if (typeof value === "function") {
      addPlugin(value, parameters);
    } else if (typeof value === "object") {
      if (Array.isArray(value)) {
        addList(value);
      } else {
        addPreset(value);
      }
    } else {
      throw new TypeError("Expected usable value, not `" + value + "`");
    }
    return this;
    function add(value2) {
      if (typeof value2 === "function") {
        addPlugin(value2, []);
      } else if (typeof value2 === "object") {
        if (Array.isArray(value2)) {
          const [plugin2, ...parameters2] = (
            /** @type {PluginTuple<Array<unknown>>} */
            value2
          );
          addPlugin(plugin2, parameters2);
        } else {
          addPreset(value2);
        }
      } else {
        throw new TypeError("Expected usable value, not `" + value2 + "`");
      }
    }
    function addPreset(result) {
      if (!("plugins" in result) && !("settings" in result)) {
        throw new Error(
          "Expected usable value but received an empty preset, which is probably a mistake: presets typically come with `plugins` and sometimes with `settings`, but this has neither"
        );
      }
      addList(result.plugins);
      if (result.settings) {
        namespace.settings = extend(true, namespace.settings, result.settings);
      }
    }
    function addList(plugins) {
      let index2 = -1;
      if (plugins === null || plugins === void 0) ;
      else if (Array.isArray(plugins)) {
        while (++index2 < plugins.length) {
          const thing = plugins[index2];
          add(thing);
        }
      } else {
        throw new TypeError("Expected a list of plugins, not `" + plugins + "`");
      }
    }
    function addPlugin(plugin2, parameters2) {
      let index2 = -1;
      let entryIndex = -1;
      while (++index2 < attachers.length) {
        if (attachers[index2][0] === plugin2) {
          entryIndex = index2;
          break;
        }
      }
      if (entryIndex === -1) {
        attachers.push([plugin2, ...parameters2]);
      } else if (parameters2.length > 0) {
        let [primary, ...rest] = parameters2;
        const currentPrimary = attachers[entryIndex][1];
        if (isPlainObject$2(currentPrimary) && isPlainObject$2(primary)) {
          primary = extend(true, currentPrimary, primary);
        }
        attachers[entryIndex] = [plugin2, primary, ...rest];
      }
    }
  }
}
const unified = new Processor().freeze();
function assertParser(name2, value) {
  if (typeof value !== "function") {
    throw new TypeError("Cannot `" + name2 + "` without `parser`");
  }
}
function assertCompiler(name2, value) {
  if (typeof value !== "function") {
    throw new TypeError("Cannot `" + name2 + "` without `compiler`");
  }
}
function assertUnfrozen(name2, frozen) {
  if (frozen) {
    throw new Error(
      "Cannot call `" + name2 + "` on a frozen processor.\nCreate a new processor first, by calling it: use `processor()` instead of `processor`."
    );
  }
}
function assertNode(node2) {
  if (!isPlainObject$2(node2) || typeof node2.type !== "string") {
    throw new TypeError("Expected node, got `" + node2 + "`");
  }
}
function assertDone(name2, asyncName, complete) {
  if (!complete) {
    throw new Error(
      "`" + name2 + "` finished async. Use `" + asyncName + "` instead"
    );
  }
}
function vfile(value) {
  return looksLikeAVFile(value) ? value : new VFile(value);
}
function looksLikeAVFile(value) {
  return Boolean(
    value && typeof value === "object" && "message" in value && "messages" in value
  );
}
function looksLikeAValue(value) {
  return typeof value === "string" || isUint8Array(value);
}
function isUint8Array(value) {
  return Boolean(
    value && typeof value === "object" && "byteLength" in value && "byteOffset" in value
  );
}
const changelog = "https://github.com/remarkjs/react-markdown/blob/main/changelog.md";
const emptyPlugins = [];
const emptyRemarkRehypeOptions = { allowDangerousHtml: true };
const safeProtocol = /^(https?|ircs?|mailto|xmpp)$/i;
const deprecations = [
  { from: "astPlugins", id: "remove-buggy-html-in-markdown-parser" },
  { from: "allowDangerousHtml", id: "remove-buggy-html-in-markdown-parser" },
  {
    from: "allowNode",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "allowElement"
  },
  {
    from: "allowedTypes",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "allowedElements"
  },
  { from: "className", id: "remove-classname" },
  {
    from: "disallowedTypes",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "disallowedElements"
  },
  { from: "escapeHtml", id: "remove-buggy-html-in-markdown-parser" },
  { from: "includeElementIndex", id: "#remove-includeelementindex" },
  {
    from: "includeNodeIndex",
    id: "change-includenodeindex-to-includeelementindex"
  },
  { from: "linkTarget", id: "remove-linktarget" },
  { from: "plugins", id: "change-plugins-to-remarkplugins", to: "remarkPlugins" },
  { from: "rawSourcePos", id: "#remove-rawsourcepos" },
  { from: "renderers", id: "change-renderers-to-components", to: "components" },
  { from: "source", id: "change-source-to-children", to: "children" },
  { from: "sourcePos", id: "#remove-sourcepos" },
  { from: "transformImageUri", id: "#add-urltransform", to: "urlTransform" },
  { from: "transformLinkUri", id: "#add-urltransform", to: "urlTransform" }
];
function Markdown(options) {
  const processor = createProcessor(options);
  const file = createFile(options);
  return post(processor.runSync(processor.parse(file), file), options);
}
function createProcessor(options) {
  const rehypePlugins = options.rehypePlugins || emptyPlugins;
  const remarkPlugins = options.remarkPlugins || emptyPlugins;
  const remarkRehypeOptions = options.remarkRehypeOptions ? { ...options.remarkRehypeOptions, ...emptyRemarkRehypeOptions } : emptyRemarkRehypeOptions;
  const processor = unified().use(remarkParse).use(remarkPlugins).use(remarkRehype, remarkRehypeOptions).use(rehypePlugins);
  return processor;
}
function createFile(options) {
  const children = options.children || "";
  const file = new VFile();
  if (typeof children === "string") {
    file.value = children;
  }
  return file;
}
function post(tree, options) {
  const allowedElements = options.allowedElements;
  const allowElement = options.allowElement;
  const components = options.components;
  const disallowedElements = options.disallowedElements;
  const skipHtml = options.skipHtml;
  const unwrapDisallowed = options.unwrapDisallowed;
  const urlTransform = options.urlTransform || defaultUrlTransform;
  for (const deprecation of deprecations) {
    if (Object.hasOwn(options, deprecation.from)) {
      unreachable(
        "Unexpected `" + deprecation.from + "` prop, " + (deprecation.to ? "use `" + deprecation.to + "` instead" : "remove it") + " (see <" + changelog + "#" + deprecation.id + "> for more info)"
      );
    }
  }
  visit(tree, transform);
  return toJsxRuntime(tree, {
    Fragment: jsxRuntimeExports.Fragment,
    components,
    ignoreInvalidStyle: true,
    jsx: jsxRuntimeExports.jsx,
    jsxs: jsxRuntimeExports.jsxs,
    passKeys: true,
    passNode: true
  });
  function transform(node2, index2, parent) {
    if (node2.type === "raw" && parent && typeof index2 === "number") {
      if (skipHtml) {
        parent.children.splice(index2, 1);
      } else {
        parent.children[index2] = { type: "text", value: node2.value };
      }
      return index2;
    }
    if (node2.type === "element") {
      let key;
      for (key in urlAttributes) {
        if (Object.hasOwn(urlAttributes, key) && Object.hasOwn(node2.properties, key)) {
          const value = node2.properties[key];
          const test = urlAttributes[key];
          if (test === null || test.includes(node2.tagName)) {
            node2.properties[key] = urlTransform(String(value || ""), key, node2);
          }
        }
      }
    }
    if (node2.type === "element") {
      let remove = allowedElements ? !allowedElements.includes(node2.tagName) : disallowedElements ? disallowedElements.includes(node2.tagName) : false;
      if (!remove && allowElement && typeof index2 === "number") {
        remove = !allowElement(node2, index2, parent);
      }
      if (remove && parent && typeof index2 === "number") {
        if (unwrapDisallowed && node2.children) {
          parent.children.splice(index2, 1, ...node2.children);
        } else {
          parent.children.splice(index2, 1);
        }
        return index2;
      }
    }
  }
}
function defaultUrlTransform(value) {
  const colon = value.indexOf(":");
  const questionMark = value.indexOf("?");
  const numberSign = value.indexOf("#");
  const slash = value.indexOf("/");
  if (
    // If there is no protocol, it’s relative.
    colon === -1 || // If the first colon is after a `?`, `#`, or `/`, it’s not a protocol.
    slash !== -1 && colon > slash || questionMark !== -1 && colon > questionMark || numberSign !== -1 && colon > numberSign || // It is a protocol, it should be allowed.
    safeProtocol.test(value.slice(0, colon))
  ) {
    return value;
  }
  return "";
}
function ccount(value, character) {
  const source = String(value);
  if (typeof character !== "string") {
    throw new TypeError("Expected character");
  }
  let count = 0;
  let index2 = source.indexOf(character);
  while (index2 !== -1) {
    count++;
    index2 = source.indexOf(character, index2 + character.length);
  }
  return count;
}
function escapeStringRegexp(string2) {
  if (typeof string2 !== "string") {
    throw new TypeError("Expected a string");
  }
  return string2.replace(/[|\\{}()[\]^$+*?.]/g, "\\$&").replace(/-/g, "\\x2d");
}
function findAndReplace(tree, list2, options) {
  const settings = options || {};
  const ignored = convert(settings.ignore || []);
  const pairs = toPairs(list2);
  let pairIndex = -1;
  while (++pairIndex < pairs.length) {
    visitParents(tree, "text", visitor);
  }
  function visitor(node2, parents) {
    let index2 = -1;
    let grandparent;
    while (++index2 < parents.length) {
      const parent = parents[index2];
      const siblings = grandparent ? grandparent.children : void 0;
      if (ignored(
        parent,
        siblings ? siblings.indexOf(parent) : void 0,
        grandparent
      )) {
        return;
      }
      grandparent = parent;
    }
    if (grandparent) {
      return handler(node2, parents);
    }
  }
  function handler(node2, parents) {
    const parent = parents[parents.length - 1];
    const find2 = pairs[pairIndex][0];
    const replace2 = pairs[pairIndex][1];
    let start = 0;
    const siblings = parent.children;
    const index2 = siblings.indexOf(node2);
    let change = false;
    let nodes = [];
    find2.lastIndex = 0;
    let match = find2.exec(node2.value);
    while (match) {
      const position2 = match.index;
      const matchObject = {
        index: match.index,
        input: match.input,
        stack: [...parents, node2]
      };
      let value = replace2(...match, matchObject);
      if (typeof value === "string") {
        value = value.length > 0 ? { type: "text", value } : void 0;
      }
      if (value === false) {
        find2.lastIndex = position2 + 1;
      } else {
        if (start !== position2) {
          nodes.push({
            type: "text",
            value: node2.value.slice(start, position2)
          });
        }
        if (Array.isArray(value)) {
          nodes.push(...value);
        } else if (value) {
          nodes.push(value);
        }
        start = position2 + match[0].length;
        change = true;
      }
      if (!find2.global) {
        break;
      }
      match = find2.exec(node2.value);
    }
    if (change) {
      if (start < node2.value.length) {
        nodes.push({ type: "text", value: node2.value.slice(start) });
      }
      parent.children.splice(index2, 1, ...nodes);
    } else {
      nodes = [node2];
    }
    return index2 + nodes.length;
  }
}
function toPairs(tupleOrList) {
  const result = [];
  if (!Array.isArray(tupleOrList)) {
    throw new TypeError("Expected find and replace tuple or list of tuples");
  }
  const list2 = !tupleOrList[0] || Array.isArray(tupleOrList[0]) ? tupleOrList : [tupleOrList];
  let index2 = -1;
  while (++index2 < list2.length) {
    const tuple = list2[index2];
    result.push([toExpression(tuple[0]), toFunction(tuple[1])]);
  }
  return result;
}
function toExpression(find2) {
  return typeof find2 === "string" ? new RegExp(escapeStringRegexp(find2), "g") : find2;
}
function toFunction(replace2) {
  return typeof replace2 === "function" ? replace2 : function() {
    return replace2;
  };
}
const inConstruct = "phrasing";
const notInConstruct = ["autolink", "link", "image", "label"];
function gfmAutolinkLiteralFromMarkdown() {
  return {
    transforms: [transformGfmAutolinkLiterals],
    enter: {
      literalAutolink: enterLiteralAutolink,
      literalAutolinkEmail: enterLiteralAutolinkValue,
      literalAutolinkHttp: enterLiteralAutolinkValue,
      literalAutolinkWww: enterLiteralAutolinkValue
    },
    exit: {
      literalAutolink: exitLiteralAutolink,
      literalAutolinkEmail: exitLiteralAutolinkEmail,
      literalAutolinkHttp: exitLiteralAutolinkHttp,
      literalAutolinkWww: exitLiteralAutolinkWww
    }
  };
}
function gfmAutolinkLiteralToMarkdown() {
  return {
    unsafe: [
      {
        character: "@",
        before: "[+\\-.\\w]",
        after: "[\\-.\\w]",
        inConstruct,
        notInConstruct
      },
      {
        character: ".",
        before: "[Ww]",
        after: "[\\-.\\w]",
        inConstruct,
        notInConstruct
      },
      {
        character: ":",
        before: "[ps]",
        after: "\\/",
        inConstruct,
        notInConstruct
      }
    ]
  };
}
function enterLiteralAutolink(token) {
  this.enter({ type: "link", title: null, url: "", children: [] }, token);
}
function enterLiteralAutolinkValue(token) {
  this.config.enter.autolinkProtocol.call(this, token);
}
function exitLiteralAutolinkHttp(token) {
  this.config.exit.autolinkProtocol.call(this, token);
}
function exitLiteralAutolinkWww(token) {
  this.config.exit.data.call(this, token);
  const node2 = this.stack[this.stack.length - 1];
  ok$1(node2.type === "link");
  node2.url = "http://" + this.sliceSerialize(token);
}
function exitLiteralAutolinkEmail(token) {
  this.config.exit.autolinkEmail.call(this, token);
}
function exitLiteralAutolink(token) {
  this.exit(token);
}
function transformGfmAutolinkLiterals(tree) {
  findAndReplace(
    tree,
    [
      [/(https?:\/\/|www(?=\.))([-.\w]+)([^ \t\r\n]*)/gi, findUrl],
      [new RegExp("(?<=^|\\s|\\p{P}|\\p{S})([-.\\w+]+)@([-\\w]+(?:\\.[-\\w]+)+)", "gu"), findEmail]
    ],
    { ignore: ["link", "linkReference"] }
  );
}
function findUrl(_, protocol, domain2, path2, match) {
  let prefix = "";
  if (!previous(match)) {
    return false;
  }
  if (/^w/i.test(protocol)) {
    domain2 = protocol + domain2;
    protocol = "";
    prefix = "http://";
  }
  if (!isCorrectDomain(domain2)) {
    return false;
  }
  const parts = splitUrl(domain2 + path2);
  if (!parts[0]) return false;
  const result = {
    type: "link",
    title: null,
    url: prefix + protocol + parts[0],
    children: [{ type: "text", value: protocol + parts[0] }]
  };
  if (parts[1]) {
    return [result, { type: "text", value: parts[1] }];
  }
  return result;
}
function findEmail(_, atext, label, match) {
  if (
    // Not an expected previous character.
    !previous(match, true) || // Label ends in not allowed character.
    /[-\d_]$/.test(label)
  ) {
    return false;
  }
  return {
    type: "link",
    title: null,
    url: "mailto:" + atext + "@" + label,
    children: [{ type: "text", value: atext + "@" + label }]
  };
}
function isCorrectDomain(domain2) {
  const parts = domain2.split(".");
  if (parts.length < 2 || parts[parts.length - 1] && (/_/.test(parts[parts.length - 1]) || !/[a-zA-Z\d]/.test(parts[parts.length - 1])) || parts[parts.length - 2] && (/_/.test(parts[parts.length - 2]) || !/[a-zA-Z\d]/.test(parts[parts.length - 2]))) {
    return false;
  }
  return true;
}
function splitUrl(url) {
  const trailExec = /[!"&'),.:;<>?\]}]+$/.exec(url);
  if (!trailExec) {
    return [url, void 0];
  }
  url = url.slice(0, trailExec.index);
  let trail2 = trailExec[0];
  let closingParenIndex = trail2.indexOf(")");
  const openingParens = ccount(url, "(");
  let closingParens = ccount(url, ")");
  while (closingParenIndex !== -1 && openingParens > closingParens) {
    url += trail2.slice(0, closingParenIndex + 1);
    trail2 = trail2.slice(closingParenIndex + 1);
    closingParenIndex = trail2.indexOf(")");
    closingParens++;
  }
  return [url, trail2];
}
function previous(match, email) {
  const code2 = match.input.charCodeAt(match.index - 1);
  return (match.index === 0 || unicodeWhitespace(code2) || unicodePunctuation(code2)) && // If it’s an email, the previous character should not be a slash.
  (!email || code2 !== 47);
}
footnoteReference.peek = footnoteReferencePeek;
function enterFootnoteCallString() {
  this.buffer();
}
function enterFootnoteCall(token) {
  this.enter({ type: "footnoteReference", identifier: "", label: "" }, token);
}
function enterFootnoteDefinitionLabelString() {
  this.buffer();
}
function enterFootnoteDefinition(token) {
  this.enter(
    { type: "footnoteDefinition", identifier: "", label: "", children: [] },
    token
  );
}
function exitFootnoteCallString(token) {
  const label = this.resume();
  const node2 = this.stack[this.stack.length - 1];
  ok$1(node2.type === "footnoteReference");
  node2.identifier = normalizeIdentifier(
    this.sliceSerialize(token)
  ).toLowerCase();
  node2.label = label;
}
function exitFootnoteCall(token) {
  this.exit(token);
}
function exitFootnoteDefinitionLabelString(token) {
  const label = this.resume();
  const node2 = this.stack[this.stack.length - 1];
  ok$1(node2.type === "footnoteDefinition");
  node2.identifier = normalizeIdentifier(
    this.sliceSerialize(token)
  ).toLowerCase();
  node2.label = label;
}
function exitFootnoteDefinition(token) {
  this.exit(token);
}
function footnoteReferencePeek() {
  return "[";
}
function footnoteReference(node2, _, state, info) {
  const tracker = state.createTracker(info);
  let value = tracker.move("[^");
  const exit2 = state.enter("footnoteReference");
  const subexit = state.enter("reference");
  value += tracker.move(
    state.safe(state.associationId(node2), { after: "]", before: value })
  );
  subexit();
  exit2();
  value += tracker.move("]");
  return value;
}
function gfmFootnoteFromMarkdown() {
  return {
    enter: {
      gfmFootnoteCallString: enterFootnoteCallString,
      gfmFootnoteCall: enterFootnoteCall,
      gfmFootnoteDefinitionLabelString: enterFootnoteDefinitionLabelString,
      gfmFootnoteDefinition: enterFootnoteDefinition
    },
    exit: {
      gfmFootnoteCallString: exitFootnoteCallString,
      gfmFootnoteCall: exitFootnoteCall,
      gfmFootnoteDefinitionLabelString: exitFootnoteDefinitionLabelString,
      gfmFootnoteDefinition: exitFootnoteDefinition
    }
  };
}
function gfmFootnoteToMarkdown(options) {
  let firstLineBlank = false;
  if (options && options.firstLineBlank) {
    firstLineBlank = true;
  }
  return {
    handlers: { footnoteDefinition, footnoteReference },
    // This is on by default already.
    unsafe: [{ character: "[", inConstruct: ["label", "phrasing", "reference"] }]
  };
  function footnoteDefinition(node2, _, state, info) {
    const tracker = state.createTracker(info);
    let value = tracker.move("[^");
    const exit2 = state.enter("footnoteDefinition");
    const subexit = state.enter("label");
    value += tracker.move(
      state.safe(state.associationId(node2), { before: value, after: "]" })
    );
    subexit();
    value += tracker.move("]:");
    if (node2.children && node2.children.length > 0) {
      tracker.shift(4);
      value += tracker.move(
        (firstLineBlank ? "\n" : " ") + state.indentLines(
          state.containerFlow(node2, tracker.current()),
          firstLineBlank ? mapAll : mapExceptFirst
        )
      );
    }
    exit2();
    return value;
  }
}
function mapExceptFirst(line, index2, blank) {
  return index2 === 0 ? line : mapAll(line, index2, blank);
}
function mapAll(line, index2, blank) {
  return (blank ? "" : "    ") + line;
}
const constructsWithoutStrikethrough = [
  "autolink",
  "destinationLiteral",
  "destinationRaw",
  "reference",
  "titleQuote",
  "titleApostrophe"
];
handleDelete.peek = peekDelete;
function gfmStrikethroughFromMarkdown() {
  return {
    canContainEols: ["delete"],
    enter: { strikethrough: enterStrikethrough },
    exit: { strikethrough: exitStrikethrough }
  };
}
function gfmStrikethroughToMarkdown() {
  return {
    unsafe: [
      {
        character: "~",
        inConstruct: "phrasing",
        notInConstruct: constructsWithoutStrikethrough
      }
    ],
    handlers: { delete: handleDelete }
  };
}
function enterStrikethrough(token) {
  this.enter({ type: "delete", children: [] }, token);
}
function exitStrikethrough(token) {
  this.exit(token);
}
function handleDelete(node2, _, state, info) {
  const tracker = state.createTracker(info);
  const exit2 = state.enter("strikethrough");
  let value = tracker.move("~~");
  value += state.containerPhrasing(node2, {
    ...tracker.current(),
    before: value,
    after: "~"
  });
  value += tracker.move("~~");
  exit2();
  return value;
}
function peekDelete() {
  return "~";
}
function defaultStringLength(value) {
  return value.length;
}
function markdownTable(table2, options) {
  const settings = options || {};
  const align = (settings.align || []).concat();
  const stringLength = settings.stringLength || defaultStringLength;
  const alignments = [];
  const cellMatrix = [];
  const sizeMatrix = [];
  const longestCellByColumn = [];
  let mostCellsPerRow = 0;
  let rowIndex = -1;
  while (++rowIndex < table2.length) {
    const row2 = [];
    const sizes2 = [];
    let columnIndex2 = -1;
    if (table2[rowIndex].length > mostCellsPerRow) {
      mostCellsPerRow = table2[rowIndex].length;
    }
    while (++columnIndex2 < table2[rowIndex].length) {
      const cell = serialize(table2[rowIndex][columnIndex2]);
      if (settings.alignDelimiters !== false) {
        const size = stringLength(cell);
        sizes2[columnIndex2] = size;
        if (longestCellByColumn[columnIndex2] === void 0 || size > longestCellByColumn[columnIndex2]) {
          longestCellByColumn[columnIndex2] = size;
        }
      }
      row2.push(cell);
    }
    cellMatrix[rowIndex] = row2;
    sizeMatrix[rowIndex] = sizes2;
  }
  let columnIndex = -1;
  if (typeof align === "object" && "length" in align) {
    while (++columnIndex < mostCellsPerRow) {
      alignments[columnIndex] = toAlignment(align[columnIndex]);
    }
  } else {
    const code2 = toAlignment(align);
    while (++columnIndex < mostCellsPerRow) {
      alignments[columnIndex] = code2;
    }
  }
  columnIndex = -1;
  const row = [];
  const sizes = [];
  while (++columnIndex < mostCellsPerRow) {
    const code2 = alignments[columnIndex];
    let before = "";
    let after = "";
    if (code2 === 99) {
      before = ":";
      after = ":";
    } else if (code2 === 108) {
      before = ":";
    } else if (code2 === 114) {
      after = ":";
    }
    let size = settings.alignDelimiters === false ? 1 : Math.max(
      1,
      longestCellByColumn[columnIndex] - before.length - after.length
    );
    const cell = before + "-".repeat(size) + after;
    if (settings.alignDelimiters !== false) {
      size = before.length + size + after.length;
      if (size > longestCellByColumn[columnIndex]) {
        longestCellByColumn[columnIndex] = size;
      }
      sizes[columnIndex] = size;
    }
    row[columnIndex] = cell;
  }
  cellMatrix.splice(1, 0, row);
  sizeMatrix.splice(1, 0, sizes);
  rowIndex = -1;
  const lines = [];
  while (++rowIndex < cellMatrix.length) {
    const row2 = cellMatrix[rowIndex];
    const sizes2 = sizeMatrix[rowIndex];
    columnIndex = -1;
    const line = [];
    while (++columnIndex < mostCellsPerRow) {
      const cell = row2[columnIndex] || "";
      let before = "";
      let after = "";
      if (settings.alignDelimiters !== false) {
        const size = longestCellByColumn[columnIndex] - (sizes2[columnIndex] || 0);
        const code2 = alignments[columnIndex];
        if (code2 === 114) {
          before = " ".repeat(size);
        } else if (code2 === 99) {
          if (size % 2) {
            before = " ".repeat(size / 2 + 0.5);
            after = " ".repeat(size / 2 - 0.5);
          } else {
            before = " ".repeat(size / 2);
            after = before;
          }
        } else {
          after = " ".repeat(size);
        }
      }
      if (settings.delimiterStart !== false && !columnIndex) {
        line.push("|");
      }
      if (settings.padding !== false && // Don’t add the opening space if we’re not aligning and the cell is
      // empty: there will be a closing space.
      !(settings.alignDelimiters === false && cell === "") && (settings.delimiterStart !== false || columnIndex)) {
        line.push(" ");
      }
      if (settings.alignDelimiters !== false) {
        line.push(before);
      }
      line.push(cell);
      if (settings.alignDelimiters !== false) {
        line.push(after);
      }
      if (settings.padding !== false) {
        line.push(" ");
      }
      if (settings.delimiterEnd !== false || columnIndex !== mostCellsPerRow - 1) {
        line.push("|");
      }
    }
    lines.push(
      settings.delimiterEnd === false ? line.join("").replace(/ +$/, "") : line.join("")
    );
  }
  return lines.join("\n");
}
function serialize(value) {
  return value === null || value === void 0 ? "" : String(value);
}
function toAlignment(value) {
  const code2 = typeof value === "string" ? value.codePointAt(0) : 0;
  return code2 === 67 || code2 === 99 ? 99 : code2 === 76 || code2 === 108 ? 108 : code2 === 82 || code2 === 114 ? 114 : 0;
}
function blockquote(node2, _, state, info) {
  const exit2 = state.enter("blockquote");
  const tracker = state.createTracker(info);
  tracker.move("> ");
  tracker.shift(2);
  const value = state.indentLines(
    state.containerFlow(node2, tracker.current()),
    map$1
  );
  exit2();
  return value;
}
function map$1(line, _, blank) {
  return ">" + (blank ? "" : " ") + line;
}
function patternInScope(stack, pattern) {
  return listInScope(stack, pattern.inConstruct, true) && !listInScope(stack, pattern.notInConstruct, false);
}
function listInScope(stack, list2, none) {
  if (typeof list2 === "string") {
    list2 = [list2];
  }
  if (!list2 || list2.length === 0) {
    return none;
  }
  let index2 = -1;
  while (++index2 < list2.length) {
    if (stack.includes(list2[index2])) {
      return true;
    }
  }
  return false;
}
function hardBreak(_, _1, state, info) {
  let index2 = -1;
  while (++index2 < state.unsafe.length) {
    if (state.unsafe[index2].character === "\n" && patternInScope(state.stack, state.unsafe[index2])) {
      return /[ \t]/.test(info.before) ? "" : " ";
    }
  }
  return "\\\n";
}
function longestStreak(value, substring) {
  const source = String(value);
  let index2 = source.indexOf(substring);
  let expected = index2;
  let count = 0;
  let max = 0;
  if (typeof substring !== "string") {
    throw new TypeError("Expected substring");
  }
  while (index2 !== -1) {
    if (index2 === expected) {
      if (++count > max) {
        max = count;
      }
    } else {
      count = 1;
    }
    expected = index2 + substring.length;
    index2 = source.indexOf(substring, expected);
  }
  return max;
}
function formatCodeAsIndented(node2, state) {
  return Boolean(
    state.options.fences === false && node2.value && // If there’s no info…
    !node2.lang && // And there’s a non-whitespace character…
    /[^ \r\n]/.test(node2.value) && // And the value doesn’t start or end in a blank…
    !/^[\t ]*(?:[\r\n]|$)|(?:^|[\r\n])[\t ]*$/.test(node2.value)
  );
}
function checkFence(state) {
  const marker = state.options.fence || "`";
  if (marker !== "`" && marker !== "~") {
    throw new Error(
      "Cannot serialize code with `" + marker + "` for `options.fence`, expected `` ` `` or `~`"
    );
  }
  return marker;
}
function code$1(node2, _, state, info) {
  const marker = checkFence(state);
  const raw = node2.value || "";
  const suffix = marker === "`" ? "GraveAccent" : "Tilde";
  if (formatCodeAsIndented(node2, state)) {
    const exit3 = state.enter("codeIndented");
    const value2 = state.indentLines(raw, map);
    exit3();
    return value2;
  }
  const tracker = state.createTracker(info);
  const sequence = marker.repeat(Math.max(longestStreak(raw, marker) + 1, 3));
  const exit2 = state.enter("codeFenced");
  let value = tracker.move(sequence);
  if (node2.lang) {
    const subexit = state.enter(`codeFencedLang${suffix}`);
    value += tracker.move(
      state.safe(node2.lang, {
        before: value,
        after: " ",
        encode: ["`"],
        ...tracker.current()
      })
    );
    subexit();
  }
  if (node2.lang && node2.meta) {
    const subexit = state.enter(`codeFencedMeta${suffix}`);
    value += tracker.move(" ");
    value += tracker.move(
      state.safe(node2.meta, {
        before: value,
        after: "\n",
        encode: ["`"],
        ...tracker.current()
      })
    );
    subexit();
  }
  value += tracker.move("\n");
  if (raw) {
    value += tracker.move(raw + "\n");
  }
  value += tracker.move(sequence);
  exit2();
  return value;
}
function map(line, _, blank) {
  return (blank ? "" : "    ") + line;
}
function checkQuote(state) {
  const marker = state.options.quote || '"';
  if (marker !== '"' && marker !== "'") {
    throw new Error(
      "Cannot serialize title with `" + marker + "` for `options.quote`, expected `\"`, or `'`"
    );
  }
  return marker;
}
function definition(node2, _, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const exit2 = state.enter("definition");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("[");
  value += tracker.move(
    state.safe(state.associationId(node2), {
      before: value,
      after: "]",
      ...tracker.current()
    })
  );
  value += tracker.move("]: ");
  subexit();
  if (
    // If there’s no url, or…
    !node2.url || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : "\n",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  exit2();
  return value;
}
function checkEmphasis(state) {
  const marker = state.options.emphasis || "*";
  if (marker !== "*" && marker !== "_") {
    throw new Error(
      "Cannot serialize emphasis with `" + marker + "` for `options.emphasis`, expected `*`, or `_`"
    );
  }
  return marker;
}
function encodeCharacterReference(code2) {
  return "&#x" + code2.toString(16).toUpperCase() + ";";
}
function encodeInfo(outside, inside, marker) {
  const outsideKind = classifyCharacter(outside);
  const insideKind = classifyCharacter(inside);
  if (outsideKind === void 0) {
    return insideKind === void 0 ? (
      // Letter inside:
      // we have to encode *both* letters for `_` as it is looser.
      // it already forms for `*` (and GFMs `~`).
      marker === "_" ? { inside: true, outside: true } : { inside: false, outside: false }
    ) : insideKind === 1 ? (
      // Whitespace inside: encode both (letter, whitespace).
      { inside: true, outside: true }
    ) : (
      // Punctuation inside: encode outer (letter)
      { inside: false, outside: true }
    );
  }
  if (outsideKind === 1) {
    return insideKind === void 0 ? (
      // Letter inside: already forms.
      { inside: false, outside: false }
    ) : insideKind === 1 ? (
      // Whitespace inside: encode both (whitespace).
      { inside: true, outside: true }
    ) : (
      // Punctuation inside: already forms.
      { inside: false, outside: false }
    );
  }
  return insideKind === void 0 ? (
    // Letter inside: already forms.
    { inside: false, outside: false }
  ) : insideKind === 1 ? (
    // Whitespace inside: encode inner (whitespace).
    { inside: true, outside: false }
  ) : (
    // Punctuation inside: already forms.
    { inside: false, outside: false }
  );
}
emphasis.peek = emphasisPeek;
function emphasis(node2, _, state, info) {
  const marker = checkEmphasis(state);
  const exit2 = state.enter("emphasis");
  const tracker = state.createTracker(info);
  const before = tracker.move(marker);
  let between = tracker.move(
    state.containerPhrasing(node2, {
      after: marker,
      before,
      ...tracker.current()
    })
  );
  const betweenHead = between.charCodeAt(0);
  const open = encodeInfo(
    info.before.charCodeAt(info.before.length - 1),
    betweenHead,
    marker
  );
  if (open.inside) {
    between = encodeCharacterReference(betweenHead) + between.slice(1);
  }
  const betweenTail = between.charCodeAt(between.length - 1);
  const close = encodeInfo(info.after.charCodeAt(0), betweenTail, marker);
  if (close.inside) {
    between = between.slice(0, -1) + encodeCharacterReference(betweenTail);
  }
  const after = tracker.move(marker);
  exit2();
  state.attentionEncodeSurroundingInfo = {
    after: close.outside,
    before: open.outside
  };
  return before + between + after;
}
function emphasisPeek(_, _1, state) {
  return state.options.emphasis || "*";
}
function formatHeadingAsSetext(node2, state) {
  let literalWithBreak = false;
  visit(node2, function(node3) {
    if ("value" in node3 && /\r?\n|\r/.test(node3.value) || node3.type === "break") {
      literalWithBreak = true;
      return EXIT;
    }
  });
  return Boolean(
    (!node2.depth || node2.depth < 3) && toString$1(node2) && (state.options.setext || literalWithBreak)
  );
}
function heading(node2, _, state, info) {
  const rank = Math.max(Math.min(6, node2.depth || 1), 1);
  const tracker = state.createTracker(info);
  if (formatHeadingAsSetext(node2, state)) {
    const exit3 = state.enter("headingSetext");
    const subexit2 = state.enter("phrasing");
    const value2 = state.containerPhrasing(node2, {
      ...tracker.current(),
      before: "\n",
      after: "\n"
    });
    subexit2();
    exit3();
    return value2 + "\n" + (rank === 1 ? "=" : "-").repeat(
      // The whole size…
      value2.length - // Minus the position of the character after the last EOL (or
      // 0 if there is none)…
      (Math.max(value2.lastIndexOf("\r"), value2.lastIndexOf("\n")) + 1)
    );
  }
  const sequence = "#".repeat(rank);
  const exit2 = state.enter("headingAtx");
  const subexit = state.enter("phrasing");
  tracker.move(sequence + " ");
  let value = state.containerPhrasing(node2, {
    before: "# ",
    after: "\n",
    ...tracker.current()
  });
  if (/^[\t ]/.test(value)) {
    value = encodeCharacterReference(value.charCodeAt(0)) + value.slice(1);
  }
  value = value ? sequence + " " + value : sequence;
  if (state.options.closeAtx) {
    value += " " + sequence;
  }
  subexit();
  exit2();
  return value;
}
html.peek = htmlPeek;
function html(node2) {
  return node2.value || "";
}
function htmlPeek() {
  return "<";
}
image.peek = imagePeek;
function image(node2, _, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const exit2 = state.enter("image");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("![");
  value += tracker.move(
    state.safe(node2.alt, { before: value, after: "]", ...tracker.current() })
  );
  value += tracker.move("](");
  subexit();
  if (
    // If there’s no url but there is a title…
    !node2.url && node2.title || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : ")",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  value += tracker.move(")");
  exit2();
  return value;
}
function imagePeek() {
  return "!";
}
imageReference.peek = imageReferencePeek;
function imageReference(node2, _, state, info) {
  const type = node2.referenceType;
  const exit2 = state.enter("imageReference");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("![");
  const alt = state.safe(node2.alt, {
    before: value,
    after: "]",
    ...tracker.current()
  });
  value += tracker.move(alt + "][");
  subexit();
  const stack = state.stack;
  state.stack = [];
  subexit = state.enter("reference");
  const reference = state.safe(state.associationId(node2), {
    before: value,
    after: "]",
    ...tracker.current()
  });
  subexit();
  state.stack = stack;
  exit2();
  if (type === "full" || !alt || alt !== reference) {
    value += tracker.move(reference + "]");
  } else if (type === "shortcut") {
    value = value.slice(0, -1);
  } else {
    value += tracker.move("]");
  }
  return value;
}
function imageReferencePeek() {
  return "!";
}
inlineCode.peek = inlineCodePeek;
function inlineCode(node2, _, state) {
  let value = node2.value || "";
  let sequence = "`";
  let index2 = -1;
  while (new RegExp("(^|[^`])" + sequence + "([^`]|$)").test(value)) {
    sequence += "`";
  }
  if (/[^ \r\n]/.test(value) && (/^[ \r\n]/.test(value) && /[ \r\n]$/.test(value) || /^`|`$/.test(value))) {
    value = " " + value + " ";
  }
  while (++index2 < state.unsafe.length) {
    const pattern = state.unsafe[index2];
    const expression = state.compilePattern(pattern);
    let match;
    if (!pattern.atBreak) continue;
    while (match = expression.exec(value)) {
      let position2 = match.index;
      if (value.charCodeAt(position2) === 10 && value.charCodeAt(position2 - 1) === 13) {
        position2--;
      }
      value = value.slice(0, position2) + " " + value.slice(match.index + 1);
    }
  }
  return sequence + value + sequence;
}
function inlineCodePeek() {
  return "`";
}
function formatLinkAsAutolink(node2, state) {
  const raw = toString$1(node2);
  return Boolean(
    !state.options.resourceLink && // If there’s a url…
    node2.url && // And there’s a no title…
    !node2.title && // And the content of `node` is a single text node…
    node2.children && node2.children.length === 1 && node2.children[0].type === "text" && // And if the url is the same as the content…
    (raw === node2.url || "mailto:" + raw === node2.url) && // And that starts w/ a protocol…
    /^[a-z][a-z+.-]+:/i.test(node2.url) && // And that doesn’t contain ASCII control codes (character escapes and
    // references don’t work), space, or angle brackets…
    !/[\0- <>\u007F]/.test(node2.url)
  );
}
link.peek = linkPeek;
function link(node2, _, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const tracker = state.createTracker(info);
  let exit2;
  let subexit;
  if (formatLinkAsAutolink(node2, state)) {
    const stack = state.stack;
    state.stack = [];
    exit2 = state.enter("autolink");
    let value2 = tracker.move("<");
    value2 += tracker.move(
      state.containerPhrasing(node2, {
        before: value2,
        after: ">",
        ...tracker.current()
      })
    );
    value2 += tracker.move(">");
    exit2();
    state.stack = stack;
    return value2;
  }
  exit2 = state.enter("link");
  subexit = state.enter("label");
  let value = tracker.move("[");
  value += tracker.move(
    state.containerPhrasing(node2, {
      before: value,
      after: "](",
      ...tracker.current()
    })
  );
  value += tracker.move("](");
  subexit();
  if (
    // If there’s no url but there is a title…
    !node2.url && node2.title || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : ")",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  value += tracker.move(")");
  exit2();
  return value;
}
function linkPeek(node2, _, state) {
  return formatLinkAsAutolink(node2, state) ? "<" : "[";
}
linkReference.peek = linkReferencePeek;
function linkReference(node2, _, state, info) {
  const type = node2.referenceType;
  const exit2 = state.enter("linkReference");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("[");
  const text2 = state.containerPhrasing(node2, {
    before: value,
    after: "]",
    ...tracker.current()
  });
  value += tracker.move(text2 + "][");
  subexit();
  const stack = state.stack;
  state.stack = [];
  subexit = state.enter("reference");
  const reference = state.safe(state.associationId(node2), {
    before: value,
    after: "]",
    ...tracker.current()
  });
  subexit();
  state.stack = stack;
  exit2();
  if (type === "full" || !text2 || text2 !== reference) {
    value += tracker.move(reference + "]");
  } else if (type === "shortcut") {
    value = value.slice(0, -1);
  } else {
    value += tracker.move("]");
  }
  return value;
}
function linkReferencePeek() {
  return "[";
}
function checkBullet(state) {
  const marker = state.options.bullet || "*";
  if (marker !== "*" && marker !== "+" && marker !== "-") {
    throw new Error(
      "Cannot serialize items with `" + marker + "` for `options.bullet`, expected `*`, `+`, or `-`"
    );
  }
  return marker;
}
function checkBulletOther(state) {
  const bullet = checkBullet(state);
  const bulletOther = state.options.bulletOther;
  if (!bulletOther) {
    return bullet === "*" ? "-" : "*";
  }
  if (bulletOther !== "*" && bulletOther !== "+" && bulletOther !== "-") {
    throw new Error(
      "Cannot serialize items with `" + bulletOther + "` for `options.bulletOther`, expected `*`, `+`, or `-`"
    );
  }
  if (bulletOther === bullet) {
    throw new Error(
      "Expected `bullet` (`" + bullet + "`) and `bulletOther` (`" + bulletOther + "`) to be different"
    );
  }
  return bulletOther;
}
function checkBulletOrdered(state) {
  const marker = state.options.bulletOrdered || ".";
  if (marker !== "." && marker !== ")") {
    throw new Error(
      "Cannot serialize items with `" + marker + "` for `options.bulletOrdered`, expected `.` or `)`"
    );
  }
  return marker;
}
function checkRule(state) {
  const marker = state.options.rule || "*";
  if (marker !== "*" && marker !== "-" && marker !== "_") {
    throw new Error(
      "Cannot serialize rules with `" + marker + "` for `options.rule`, expected `*`, `-`, or `_`"
    );
  }
  return marker;
}
function list(node2, parent, state, info) {
  const exit2 = state.enter("list");
  const bulletCurrent = state.bulletCurrent;
  let bullet = node2.ordered ? checkBulletOrdered(state) : checkBullet(state);
  const bulletOther = node2.ordered ? bullet === "." ? ")" : "." : checkBulletOther(state);
  let useDifferentMarker = parent && state.bulletLastUsed ? bullet === state.bulletLastUsed : false;
  if (!node2.ordered) {
    const firstListItem = node2.children ? node2.children[0] : void 0;
    if (
      // Bullet could be used as a thematic break marker:
      (bullet === "*" || bullet === "-") && // Empty first list item:
      firstListItem && (!firstListItem.children || !firstListItem.children[0]) && // Directly in two other list items:
      state.stack[state.stack.length - 1] === "list" && state.stack[state.stack.length - 2] === "listItem" && state.stack[state.stack.length - 3] === "list" && state.stack[state.stack.length - 4] === "listItem" && // That are each the first child.
      state.indexStack[state.indexStack.length - 1] === 0 && state.indexStack[state.indexStack.length - 2] === 0 && state.indexStack[state.indexStack.length - 3] === 0
    ) {
      useDifferentMarker = true;
    }
    if (checkRule(state) === bullet && firstListItem) {
      let index2 = -1;
      while (++index2 < node2.children.length) {
        const item = node2.children[index2];
        if (item && item.type === "listItem" && item.children && item.children[0] && item.children[0].type === "thematicBreak") {
          useDifferentMarker = true;
          break;
        }
      }
    }
  }
  if (useDifferentMarker) {
    bullet = bulletOther;
  }
  state.bulletCurrent = bullet;
  const value = state.containerFlow(node2, info);
  state.bulletLastUsed = bullet;
  state.bulletCurrent = bulletCurrent;
  exit2();
  return value;
}
function checkListItemIndent(state) {
  const style = state.options.listItemIndent || "one";
  if (style !== "tab" && style !== "one" && style !== "mixed") {
    throw new Error(
      "Cannot serialize items with `" + style + "` for `options.listItemIndent`, expected `tab`, `one`, or `mixed`"
    );
  }
  return style;
}
function listItem(node2, parent, state, info) {
  const listItemIndent = checkListItemIndent(state);
  let bullet = state.bulletCurrent || checkBullet(state);
  if (parent && parent.type === "list" && parent.ordered) {
    bullet = (typeof parent.start === "number" && parent.start > -1 ? parent.start : 1) + (state.options.incrementListMarker === false ? 0 : parent.children.indexOf(node2)) + bullet;
  }
  let size = bullet.length + 1;
  if (listItemIndent === "tab" || listItemIndent === "mixed" && (parent && parent.type === "list" && parent.spread || node2.spread)) {
    size = Math.ceil(size / 4) * 4;
  }
  const tracker = state.createTracker(info);
  tracker.move(bullet + " ".repeat(size - bullet.length));
  tracker.shift(size);
  const exit2 = state.enter("listItem");
  const value = state.indentLines(
    state.containerFlow(node2, tracker.current()),
    map2
  );
  exit2();
  return value;
  function map2(line, index2, blank) {
    if (index2) {
      return (blank ? "" : " ".repeat(size)) + line;
    }
    return (blank ? bullet : bullet + " ".repeat(size - bullet.length)) + line;
  }
}
function paragraph(node2, _, state, info) {
  const exit2 = state.enter("paragraph");
  const subexit = state.enter("phrasing");
  const value = state.containerPhrasing(node2, info);
  subexit();
  exit2();
  return value;
}
const phrasing = (
  /** @type {(node?: unknown) => node is Exclude<PhrasingContent, Html>} */
  convert([
    "break",
    "delete",
    "emphasis",
    // To do: next major: removed since footnotes were added to GFM.
    "footnote",
    "footnoteReference",
    "image",
    "imageReference",
    "inlineCode",
    // Enabled by `mdast-util-math`:
    "inlineMath",
    "link",
    "linkReference",
    // Enabled by `mdast-util-mdx`:
    "mdxJsxTextElement",
    // Enabled by `mdast-util-mdx`:
    "mdxTextExpression",
    "strong",
    "text",
    // Enabled by `mdast-util-directive`:
    "textDirective"
  ])
);
function root(node2, _, state, info) {
  const hasPhrasing = node2.children.some(function(d) {
    return phrasing(d);
  });
  const container = hasPhrasing ? state.containerPhrasing : state.containerFlow;
  return container.call(state, node2, info);
}
function checkStrong(state) {
  const marker = state.options.strong || "*";
  if (marker !== "*" && marker !== "_") {
    throw new Error(
      "Cannot serialize strong with `" + marker + "` for `options.strong`, expected `*`, or `_`"
    );
  }
  return marker;
}
strong.peek = strongPeek;
function strong(node2, _, state, info) {
  const marker = checkStrong(state);
  const exit2 = state.enter("strong");
  const tracker = state.createTracker(info);
  const before = tracker.move(marker + marker);
  let between = tracker.move(
    state.containerPhrasing(node2, {
      after: marker,
      before,
      ...tracker.current()
    })
  );
  const betweenHead = between.charCodeAt(0);
  const open = encodeInfo(
    info.before.charCodeAt(info.before.length - 1),
    betweenHead,
    marker
  );
  if (open.inside) {
    between = encodeCharacterReference(betweenHead) + between.slice(1);
  }
  const betweenTail = between.charCodeAt(between.length - 1);
  const close = encodeInfo(info.after.charCodeAt(0), betweenTail, marker);
  if (close.inside) {
    between = between.slice(0, -1) + encodeCharacterReference(betweenTail);
  }
  const after = tracker.move(marker + marker);
  exit2();
  state.attentionEncodeSurroundingInfo = {
    after: close.outside,
    before: open.outside
  };
  return before + between + after;
}
function strongPeek(_, _1, state) {
  return state.options.strong || "*";
}
function text$1(node2, _, state, info) {
  return state.safe(node2.value, info);
}
function checkRuleRepetition(state) {
  const repetition = state.options.ruleRepetition || 3;
  if (repetition < 3) {
    throw new Error(
      "Cannot serialize rules with repetition `" + repetition + "` for `options.ruleRepetition`, expected `3` or more"
    );
  }
  return repetition;
}
function thematicBreak(_, _1, state) {
  const value = (checkRule(state) + (state.options.ruleSpaces ? " " : "")).repeat(checkRuleRepetition(state));
  return state.options.ruleSpaces ? value.slice(0, -1) : value;
}
const handle = {
  blockquote,
  break: hardBreak,
  code: code$1,
  definition,
  emphasis,
  hardBreak,
  heading,
  html,
  image,
  imageReference,
  inlineCode,
  link,
  linkReference,
  list,
  listItem,
  paragraph,
  root,
  strong,
  text: text$1,
  thematicBreak
};
function gfmTableFromMarkdown() {
  return {
    enter: {
      table: enterTable,
      tableData: enterCell,
      tableHeader: enterCell,
      tableRow: enterRow
    },
    exit: {
      codeText: exitCodeText,
      table: exitTable,
      tableData: exit,
      tableHeader: exit,
      tableRow: exit
    }
  };
}
function enterTable(token) {
  const align = token._align;
  this.enter(
    {
      type: "table",
      align: align.map(function(d) {
        return d === "none" ? null : d;
      }),
      children: []
    },
    token
  );
  this.data.inTable = true;
}
function exitTable(token) {
  this.exit(token);
  this.data.inTable = void 0;
}
function enterRow(token) {
  this.enter({ type: "tableRow", children: [] }, token);
}
function exit(token) {
  this.exit(token);
}
function enterCell(token) {
  this.enter({ type: "tableCell", children: [] }, token);
}
function exitCodeText(token) {
  let value = this.resume();
  if (this.data.inTable) {
    value = value.replace(/\\([\\|])/g, replace);
  }
  const node2 = this.stack[this.stack.length - 1];
  ok$1(node2.type === "inlineCode");
  node2.value = value;
  this.exit(token);
}
function replace($0, $1) {
  return $1 === "|" ? $1 : $0;
}
function gfmTableToMarkdown(options) {
  const settings = options || {};
  const padding = settings.tableCellPadding;
  const alignDelimiters = settings.tablePipeAlign;
  const stringLength = settings.stringLength;
  const around = padding ? " " : "|";
  return {
    unsafe: [
      { character: "\r", inConstruct: "tableCell" },
      { character: "\n", inConstruct: "tableCell" },
      // A pipe, when followed by a tab or space (padding), or a dash or colon
      // (unpadded delimiter row), could result in a table.
      { atBreak: true, character: "|", after: "[	 :-]" },
      // A pipe in a cell must be encoded.
      { character: "|", inConstruct: "tableCell" },
      // A colon must be followed by a dash, in which case it could start a
      // delimiter row.
      { atBreak: true, character: ":", after: "-" },
      // A delimiter row can also start with a dash, when followed by more
      // dashes, a colon, or a pipe.
      // This is a stricter version than the built in check for lists, thematic
      // breaks, and setex heading underlines though:
      // <https://github.com/syntax-tree/mdast-util-to-markdown/blob/51a2038/lib/unsafe.js#L57>
      { atBreak: true, character: "-", after: "[:|-]" }
    ],
    handlers: {
      inlineCode: inlineCodeWithTable,
      table: handleTable,
      tableCell: handleTableCell,
      tableRow: handleTableRow
    }
  };
  function handleTable(node2, _, state, info) {
    return serializeData(handleTableAsData(node2, state, info), node2.align);
  }
  function handleTableRow(node2, _, state, info) {
    const row = handleTableRowAsData(node2, state, info);
    const value = serializeData([row]);
    return value.slice(0, value.indexOf("\n"));
  }
  function handleTableCell(node2, _, state, info) {
    const exit2 = state.enter("tableCell");
    const subexit = state.enter("phrasing");
    const value = state.containerPhrasing(node2, {
      ...info,
      before: around,
      after: around
    });
    subexit();
    exit2();
    return value;
  }
  function serializeData(matrix, align) {
    return markdownTable(matrix, {
      align,
      // @ts-expect-error: `markdown-table` types should support `null`.
      alignDelimiters,
      // @ts-expect-error: `markdown-table` types should support `null`.
      padding,
      // @ts-expect-error: `markdown-table` types should support `null`.
      stringLength
    });
  }
  function handleTableAsData(node2, state, info) {
    const children = node2.children;
    let index2 = -1;
    const result = [];
    const subexit = state.enter("table");
    while (++index2 < children.length) {
      result[index2] = handleTableRowAsData(children[index2], state, info);
    }
    subexit();
    return result;
  }
  function handleTableRowAsData(node2, state, info) {
    const children = node2.children;
    let index2 = -1;
    const result = [];
    const subexit = state.enter("tableRow");
    while (++index2 < children.length) {
      result[index2] = handleTableCell(children[index2], node2, state, info);
    }
    subexit();
    return result;
  }
  function inlineCodeWithTable(node2, parent, state) {
    let value = handle.inlineCode(node2, parent, state);
    if (state.stack.includes("tableCell")) {
      value = value.replace(/\|/g, "\\$&");
    }
    return value;
  }
}
function gfmTaskListItemFromMarkdown() {
  return {
    exit: {
      taskListCheckValueChecked: exitCheck,
      taskListCheckValueUnchecked: exitCheck,
      paragraph: exitParagraphWithTaskListItem
    }
  };
}
function gfmTaskListItemToMarkdown() {
  return {
    unsafe: [{ atBreak: true, character: "-", after: "[:|-]" }],
    handlers: { listItem: listItemWithTaskListItem }
  };
}
function exitCheck(token) {
  const node2 = this.stack[this.stack.length - 2];
  ok$1(node2.type === "listItem");
  node2.checked = token.type === "taskListCheckValueChecked";
}
function exitParagraphWithTaskListItem(token) {
  const parent = this.stack[this.stack.length - 2];
  if (parent && parent.type === "listItem" && typeof parent.checked === "boolean") {
    const node2 = this.stack[this.stack.length - 1];
    ok$1(node2.type === "paragraph");
    const head = node2.children[0];
    if (head && head.type === "text") {
      const siblings = parent.children;
      let index2 = -1;
      let firstParaghraph;
      while (++index2 < siblings.length) {
        const sibling = siblings[index2];
        if (sibling.type === "paragraph") {
          firstParaghraph = sibling;
          break;
        }
      }
      if (firstParaghraph === node2) {
        head.value = head.value.slice(1);
        if (head.value.length === 0) {
          node2.children.shift();
        } else if (node2.position && head.position && typeof head.position.start.offset === "number") {
          head.position.start.column++;
          head.position.start.offset++;
          node2.position.start = Object.assign({}, head.position.start);
        }
      }
    }
  }
  this.exit(token);
}
function listItemWithTaskListItem(node2, parent, state, info) {
  const head = node2.children[0];
  const checkable = typeof node2.checked === "boolean" && head && head.type === "paragraph";
  const checkbox = "[" + (node2.checked ? "x" : " ") + "] ";
  const tracker = state.createTracker(info);
  if (checkable) {
    tracker.move(checkbox);
  }
  let value = handle.listItem(node2, parent, state, {
    ...info,
    ...tracker.current()
  });
  if (checkable) {
    value = value.replace(/^(?:[*+-]|\d+\.)([\r\n]| {1,3})/, check);
  }
  return value;
  function check($0) {
    return $0 + checkbox;
  }
}
function gfmFromMarkdown() {
  return [
    gfmAutolinkLiteralFromMarkdown(),
    gfmFootnoteFromMarkdown(),
    gfmStrikethroughFromMarkdown(),
    gfmTableFromMarkdown(),
    gfmTaskListItemFromMarkdown()
  ];
}
function gfmToMarkdown(options) {
  return {
    extensions: [
      gfmAutolinkLiteralToMarkdown(),
      gfmFootnoteToMarkdown(options),
      gfmStrikethroughToMarkdown(),
      gfmTableToMarkdown(options),
      gfmTaskListItemToMarkdown()
    ]
  };
}
const wwwPrefix = {
  tokenize: tokenizeWwwPrefix,
  partial: true
};
const domain = {
  tokenize: tokenizeDomain,
  partial: true
};
const path = {
  tokenize: tokenizePath,
  partial: true
};
const trail = {
  tokenize: tokenizeTrail,
  partial: true
};
const emailDomainDotTrail = {
  tokenize: tokenizeEmailDomainDotTrail,
  partial: true
};
const wwwAutolink = {
  name: "wwwAutolink",
  tokenize: tokenizeWwwAutolink,
  previous: previousWww
};
const protocolAutolink = {
  name: "protocolAutolink",
  tokenize: tokenizeProtocolAutolink,
  previous: previousProtocol
};
const emailAutolink = {
  name: "emailAutolink",
  tokenize: tokenizeEmailAutolink,
  previous: previousEmail
};
const text = {};
function gfmAutolinkLiteral() {
  return {
    text
  };
}
let code = 48;
while (code < 123) {
  text[code] = emailAutolink;
  code++;
  if (code === 58) code = 65;
  else if (code === 91) code = 97;
}
text[43] = emailAutolink;
text[45] = emailAutolink;
text[46] = emailAutolink;
text[95] = emailAutolink;
text[72] = [emailAutolink, protocolAutolink];
text[104] = [emailAutolink, protocolAutolink];
text[87] = [emailAutolink, wwwAutolink];
text[119] = [emailAutolink, wwwAutolink];
function tokenizeEmailAutolink(effects, ok2, nok) {
  const self2 = this;
  let dot;
  let data2;
  return start;
  function start(code2) {
    if (!gfmAtext(code2) || !previousEmail.call(self2, self2.previous) || previousUnbalanced(self2.events)) {
      return nok(code2);
    }
    effects.enter("literalAutolink");
    effects.enter("literalAutolinkEmail");
    return atext(code2);
  }
  function atext(code2) {
    if (gfmAtext(code2)) {
      effects.consume(code2);
      return atext;
    }
    if (code2 === 64) {
      effects.consume(code2);
      return emailDomain;
    }
    return nok(code2);
  }
  function emailDomain(code2) {
    if (code2 === 46) {
      return effects.check(emailDomainDotTrail, emailDomainAfter, emailDomainDot)(code2);
    }
    if (code2 === 45 || code2 === 95 || asciiAlphanumeric(code2)) {
      data2 = true;
      effects.consume(code2);
      return emailDomain;
    }
    return emailDomainAfter(code2);
  }
  function emailDomainDot(code2) {
    effects.consume(code2);
    dot = true;
    return emailDomain;
  }
  function emailDomainAfter(code2) {
    if (data2 && dot && asciiAlpha(self2.previous)) {
      effects.exit("literalAutolinkEmail");
      effects.exit("literalAutolink");
      return ok2(code2);
    }
    return nok(code2);
  }
}
function tokenizeWwwAutolink(effects, ok2, nok) {
  const self2 = this;
  return wwwStart;
  function wwwStart(code2) {
    if (code2 !== 87 && code2 !== 119 || !previousWww.call(self2, self2.previous) || previousUnbalanced(self2.events)) {
      return nok(code2);
    }
    effects.enter("literalAutolink");
    effects.enter("literalAutolinkWww");
    return effects.check(wwwPrefix, effects.attempt(domain, effects.attempt(path, wwwAfter), nok), nok)(code2);
  }
  function wwwAfter(code2) {
    effects.exit("literalAutolinkWww");
    effects.exit("literalAutolink");
    return ok2(code2);
  }
}
function tokenizeProtocolAutolink(effects, ok2, nok) {
  const self2 = this;
  let buffer = "";
  let seen = false;
  return protocolStart;
  function protocolStart(code2) {
    if ((code2 === 72 || code2 === 104) && previousProtocol.call(self2, self2.previous) && !previousUnbalanced(self2.events)) {
      effects.enter("literalAutolink");
      effects.enter("literalAutolinkHttp");
      buffer += String.fromCodePoint(code2);
      effects.consume(code2);
      return protocolPrefixInside;
    }
    return nok(code2);
  }
  function protocolPrefixInside(code2) {
    if (asciiAlpha(code2) && buffer.length < 5) {
      buffer += String.fromCodePoint(code2);
      effects.consume(code2);
      return protocolPrefixInside;
    }
    if (code2 === 58) {
      const protocol = buffer.toLowerCase();
      if (protocol === "http" || protocol === "https") {
        effects.consume(code2);
        return protocolSlashesInside;
      }
    }
    return nok(code2);
  }
  function protocolSlashesInside(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      if (seen) {
        return afterProtocol;
      }
      seen = true;
      return protocolSlashesInside;
    }
    return nok(code2);
  }
  function afterProtocol(code2) {
    return code2 === null || asciiControl(code2) || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2) || unicodePunctuation(code2) ? nok(code2) : effects.attempt(domain, effects.attempt(path, protocolAfter), nok)(code2);
  }
  function protocolAfter(code2) {
    effects.exit("literalAutolinkHttp");
    effects.exit("literalAutolink");
    return ok2(code2);
  }
}
function tokenizeWwwPrefix(effects, ok2, nok) {
  let size = 0;
  return wwwPrefixInside;
  function wwwPrefixInside(code2) {
    if ((code2 === 87 || code2 === 119) && size < 3) {
      size++;
      effects.consume(code2);
      return wwwPrefixInside;
    }
    if (code2 === 46 && size === 3) {
      effects.consume(code2);
      return wwwPrefixAfter;
    }
    return nok(code2);
  }
  function wwwPrefixAfter(code2) {
    return code2 === null ? nok(code2) : ok2(code2);
  }
}
function tokenizeDomain(effects, ok2, nok) {
  let underscoreInLastSegment;
  let underscoreInLastLastSegment;
  let seen;
  return domainInside;
  function domainInside(code2) {
    if (code2 === 46 || code2 === 95) {
      return effects.check(trail, domainAfter, domainAtPunctuation)(code2);
    }
    if (code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2) || code2 !== 45 && unicodePunctuation(code2)) {
      return domainAfter(code2);
    }
    seen = true;
    effects.consume(code2);
    return domainInside;
  }
  function domainAtPunctuation(code2) {
    if (code2 === 95) {
      underscoreInLastSegment = true;
    } else {
      underscoreInLastLastSegment = underscoreInLastSegment;
      underscoreInLastSegment = void 0;
    }
    effects.consume(code2);
    return domainInside;
  }
  function domainAfter(code2) {
    if (underscoreInLastLastSegment || underscoreInLastSegment || !seen) {
      return nok(code2);
    }
    return ok2(code2);
  }
}
function tokenizePath(effects, ok2) {
  let sizeOpen = 0;
  let sizeClose = 0;
  return pathInside;
  function pathInside(code2) {
    if (code2 === 40) {
      sizeOpen++;
      effects.consume(code2);
      return pathInside;
    }
    if (code2 === 41 && sizeClose < sizeOpen) {
      return pathAtPunctuation(code2);
    }
    if (code2 === 33 || code2 === 34 || code2 === 38 || code2 === 39 || code2 === 41 || code2 === 42 || code2 === 44 || code2 === 46 || code2 === 58 || code2 === 59 || code2 === 60 || code2 === 63 || code2 === 93 || code2 === 95 || code2 === 126) {
      return effects.check(trail, ok2, pathAtPunctuation)(code2);
    }
    if (code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)) {
      return ok2(code2);
    }
    effects.consume(code2);
    return pathInside;
  }
  function pathAtPunctuation(code2) {
    if (code2 === 41) {
      sizeClose++;
    }
    effects.consume(code2);
    return pathInside;
  }
}
function tokenizeTrail(effects, ok2, nok) {
  return trail2;
  function trail2(code2) {
    if (code2 === 33 || code2 === 34 || code2 === 39 || code2 === 41 || code2 === 42 || code2 === 44 || code2 === 46 || code2 === 58 || code2 === 59 || code2 === 63 || code2 === 95 || code2 === 126) {
      effects.consume(code2);
      return trail2;
    }
    if (code2 === 38) {
      effects.consume(code2);
      return trailCharacterReferenceStart;
    }
    if (code2 === 93) {
      effects.consume(code2);
      return trailBracketAfter;
    }
    if (
      // `<` is an end.
      code2 === 60 || // So is whitespace.
      code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)
    ) {
      return ok2(code2);
    }
    return nok(code2);
  }
  function trailBracketAfter(code2) {
    if (code2 === null || code2 === 40 || code2 === 91 || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)) {
      return ok2(code2);
    }
    return trail2(code2);
  }
  function trailCharacterReferenceStart(code2) {
    return asciiAlpha(code2) ? trailCharacterReferenceInside(code2) : nok(code2);
  }
  function trailCharacterReferenceInside(code2) {
    if (code2 === 59) {
      effects.consume(code2);
      return trail2;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return trailCharacterReferenceInside;
    }
    return nok(code2);
  }
}
function tokenizeEmailDomainDotTrail(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.consume(code2);
    return after;
  }
  function after(code2) {
    return asciiAlphanumeric(code2) ? nok(code2) : ok2(code2);
  }
}
function previousWww(code2) {
  return code2 === null || code2 === 40 || code2 === 42 || code2 === 95 || code2 === 91 || code2 === 93 || code2 === 126 || markdownLineEndingOrSpace(code2);
}
function previousProtocol(code2) {
  return !asciiAlpha(code2);
}
function previousEmail(code2) {
  return !(code2 === 47 || gfmAtext(code2));
}
function gfmAtext(code2) {
  return code2 === 43 || code2 === 45 || code2 === 46 || code2 === 95 || asciiAlphanumeric(code2);
}
function previousUnbalanced(events2) {
  let index2 = events2.length;
  let result = false;
  while (index2--) {
    const token = events2[index2][1];
    if ((token.type === "labelLink" || token.type === "labelImage") && !token._balanced) {
      result = true;
      break;
    }
    if (token._gfmAutolinkLiteralWalkedInto) {
      result = false;
      break;
    }
  }
  if (events2.length > 0 && !result) {
    events2[events2.length - 1][1]._gfmAutolinkLiteralWalkedInto = true;
  }
  return result;
}
const indent = {
  tokenize: tokenizeIndent,
  partial: true
};
function gfmFootnote() {
  return {
    document: {
      [91]: {
        name: "gfmFootnoteDefinition",
        tokenize: tokenizeDefinitionStart,
        continuation: {
          tokenize: tokenizeDefinitionContinuation
        },
        exit: gfmFootnoteDefinitionEnd
      }
    },
    text: {
      [91]: {
        name: "gfmFootnoteCall",
        tokenize: tokenizeGfmFootnoteCall
      },
      [93]: {
        name: "gfmPotentialFootnoteCall",
        add: "after",
        tokenize: tokenizePotentialGfmFootnoteCall,
        resolveTo: resolveToPotentialGfmFootnoteCall
      }
    }
  };
}
function tokenizePotentialGfmFootnoteCall(effects, ok2, nok) {
  const self2 = this;
  let index2 = self2.events.length;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let labelStart;
  while (index2--) {
    const token = self2.events[index2][1];
    if (token.type === "labelImage") {
      labelStart = token;
      break;
    }
    if (token.type === "gfmFootnoteCall" || token.type === "labelLink" || token.type === "label" || token.type === "image" || token.type === "link") {
      break;
    }
  }
  return start;
  function start(code2) {
    if (!labelStart || !labelStart._balanced) {
      return nok(code2);
    }
    const id2 = normalizeIdentifier(self2.sliceSerialize({
      start: labelStart.end,
      end: self2.now()
    }));
    if (id2.codePointAt(0) !== 94 || !defined.includes(id2.slice(1))) {
      return nok(code2);
    }
    effects.enter("gfmFootnoteCallLabelMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteCallLabelMarker");
    return ok2(code2);
  }
}
function resolveToPotentialGfmFootnoteCall(events2, context) {
  let index2 = events2.length;
  while (index2--) {
    if (events2[index2][1].type === "labelImage" && events2[index2][0] === "enter") {
      events2[index2][1];
      break;
    }
  }
  events2[index2 + 1][1].type = "data";
  events2[index2 + 3][1].type = "gfmFootnoteCallLabelMarker";
  const call = {
    type: "gfmFootnoteCall",
    start: Object.assign({}, events2[index2 + 3][1].start),
    end: Object.assign({}, events2[events2.length - 1][1].end)
  };
  const marker = {
    type: "gfmFootnoteCallMarker",
    start: Object.assign({}, events2[index2 + 3][1].end),
    end: Object.assign({}, events2[index2 + 3][1].end)
  };
  marker.end.column++;
  marker.end.offset++;
  marker.end._bufferIndex++;
  const string2 = {
    type: "gfmFootnoteCallString",
    start: Object.assign({}, marker.end),
    end: Object.assign({}, events2[events2.length - 1][1].start)
  };
  const chunk = {
    type: "chunkString",
    contentType: "string",
    start: Object.assign({}, string2.start),
    end: Object.assign({}, string2.end)
  };
  const replacement = [
    // Take the `labelImageMarker` (now `data`, the `!`)
    events2[index2 + 1],
    events2[index2 + 2],
    ["enter", call, context],
    // The `[`
    events2[index2 + 3],
    events2[index2 + 4],
    // The `^`.
    ["enter", marker, context],
    ["exit", marker, context],
    // Everything in between.
    ["enter", string2, context],
    ["enter", chunk, context],
    ["exit", chunk, context],
    ["exit", string2, context],
    // The ending (`]`, properly parsed and labelled).
    events2[events2.length - 2],
    events2[events2.length - 1],
    ["exit", call, context]
  ];
  events2.splice(index2, events2.length - index2 + 1, ...replacement);
  return events2;
}
function tokenizeGfmFootnoteCall(effects, ok2, nok) {
  const self2 = this;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let size = 0;
  let data2;
  return start;
  function start(code2) {
    effects.enter("gfmFootnoteCall");
    effects.enter("gfmFootnoteCallLabelMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteCallLabelMarker");
    return callStart;
  }
  function callStart(code2) {
    if (code2 !== 94) return nok(code2);
    effects.enter("gfmFootnoteCallMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteCallMarker");
    effects.enter("gfmFootnoteCallString");
    effects.enter("chunkString").contentType = "string";
    return callData;
  }
  function callData(code2) {
    if (
      // Too long.
      size > 999 || // Closing brace with nothing.
      code2 === 93 && !data2 || // Space or tab is not supported by GFM for some reason.
      // `\n` and `[` not being supported makes sense.
      code2 === null || code2 === 91 || markdownLineEndingOrSpace(code2)
    ) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.exit("chunkString");
      const token = effects.exit("gfmFootnoteCallString");
      if (!defined.includes(normalizeIdentifier(self2.sliceSerialize(token)))) {
        return nok(code2);
      }
      effects.enter("gfmFootnoteCallLabelMarker");
      effects.consume(code2);
      effects.exit("gfmFootnoteCallLabelMarker");
      effects.exit("gfmFootnoteCall");
      return ok2;
    }
    if (!markdownLineEndingOrSpace(code2)) {
      data2 = true;
    }
    size++;
    effects.consume(code2);
    return code2 === 92 ? callEscape : callData;
  }
  function callEscape(code2) {
    if (code2 === 91 || code2 === 92 || code2 === 93) {
      effects.consume(code2);
      size++;
      return callData;
    }
    return callData(code2);
  }
}
function tokenizeDefinitionStart(effects, ok2, nok) {
  const self2 = this;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let identifier;
  let size = 0;
  let data2;
  return start;
  function start(code2) {
    effects.enter("gfmFootnoteDefinition")._container = true;
    effects.enter("gfmFootnoteDefinitionLabel");
    effects.enter("gfmFootnoteDefinitionLabelMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteDefinitionLabelMarker");
    return labelAtMarker;
  }
  function labelAtMarker(code2) {
    if (code2 === 94) {
      effects.enter("gfmFootnoteDefinitionMarker");
      effects.consume(code2);
      effects.exit("gfmFootnoteDefinitionMarker");
      effects.enter("gfmFootnoteDefinitionLabelString");
      effects.enter("chunkString").contentType = "string";
      return labelInside;
    }
    return nok(code2);
  }
  function labelInside(code2) {
    if (
      // Too long.
      size > 999 || // Closing brace with nothing.
      code2 === 93 && !data2 || // Space or tab is not supported by GFM for some reason.
      // `\n` and `[` not being supported makes sense.
      code2 === null || code2 === 91 || markdownLineEndingOrSpace(code2)
    ) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.exit("chunkString");
      const token = effects.exit("gfmFootnoteDefinitionLabelString");
      identifier = normalizeIdentifier(self2.sliceSerialize(token));
      effects.enter("gfmFootnoteDefinitionLabelMarker");
      effects.consume(code2);
      effects.exit("gfmFootnoteDefinitionLabelMarker");
      effects.exit("gfmFootnoteDefinitionLabel");
      return labelAfter;
    }
    if (!markdownLineEndingOrSpace(code2)) {
      data2 = true;
    }
    size++;
    effects.consume(code2);
    return code2 === 92 ? labelEscape : labelInside;
  }
  function labelEscape(code2) {
    if (code2 === 91 || code2 === 92 || code2 === 93) {
      effects.consume(code2);
      size++;
      return labelInside;
    }
    return labelInside(code2);
  }
  function labelAfter(code2) {
    if (code2 === 58) {
      effects.enter("definitionMarker");
      effects.consume(code2);
      effects.exit("definitionMarker");
      if (!defined.includes(identifier)) {
        defined.push(identifier);
      }
      return factorySpace(effects, whitespaceAfter, "gfmFootnoteDefinitionWhitespace");
    }
    return nok(code2);
  }
  function whitespaceAfter(code2) {
    return ok2(code2);
  }
}
function tokenizeDefinitionContinuation(effects, ok2, nok) {
  return effects.check(blankLine, ok2, effects.attempt(indent, ok2, nok));
}
function gfmFootnoteDefinitionEnd(effects) {
  effects.exit("gfmFootnoteDefinition");
}
function tokenizeIndent(effects, ok2, nok) {
  const self2 = this;
  return factorySpace(effects, afterPrefix, "gfmFootnoteDefinitionIndent", 4 + 1);
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "gfmFootnoteDefinitionIndent" && tail[2].sliceSerialize(tail[1], true).length === 4 ? ok2(code2) : nok(code2);
  }
}
function gfmStrikethrough(options) {
  const options_ = options || {};
  let single = options_.singleTilde;
  const tokenizer = {
    name: "strikethrough",
    tokenize: tokenizeStrikethrough,
    resolveAll: resolveAllStrikethrough
  };
  if (single === null || single === void 0) {
    single = true;
  }
  return {
    text: {
      [126]: tokenizer
    },
    insideSpan: {
      null: [tokenizer]
    },
    attentionMarkers: {
      null: [126]
    }
  };
  function resolveAllStrikethrough(events2, context) {
    let index2 = -1;
    while (++index2 < events2.length) {
      if (events2[index2][0] === "enter" && events2[index2][1].type === "strikethroughSequenceTemporary" && events2[index2][1]._close) {
        let open = index2;
        while (open--) {
          if (events2[open][0] === "exit" && events2[open][1].type === "strikethroughSequenceTemporary" && events2[open][1]._open && // If the sizes are the same:
          events2[index2][1].end.offset - events2[index2][1].start.offset === events2[open][1].end.offset - events2[open][1].start.offset) {
            events2[index2][1].type = "strikethroughSequence";
            events2[open][1].type = "strikethroughSequence";
            const strikethrough2 = {
              type: "strikethrough",
              start: Object.assign({}, events2[open][1].start),
              end: Object.assign({}, events2[index2][1].end)
            };
            const text2 = {
              type: "strikethroughText",
              start: Object.assign({}, events2[open][1].end),
              end: Object.assign({}, events2[index2][1].start)
            };
            const nextEvents = [["enter", strikethrough2, context], ["enter", events2[open][1], context], ["exit", events2[open][1], context], ["enter", text2, context]];
            const insideSpan2 = context.parser.constructs.insideSpan.null;
            if (insideSpan2) {
              splice(nextEvents, nextEvents.length, 0, resolveAll(insideSpan2, events2.slice(open + 1, index2), context));
            }
            splice(nextEvents, nextEvents.length, 0, [["exit", text2, context], ["enter", events2[index2][1], context], ["exit", events2[index2][1], context], ["exit", strikethrough2, context]]);
            splice(events2, open - 1, index2 - open + 3, nextEvents);
            index2 = open + nextEvents.length - 2;
            break;
          }
        }
      }
    }
    index2 = -1;
    while (++index2 < events2.length) {
      if (events2[index2][1].type === "strikethroughSequenceTemporary") {
        events2[index2][1].type = "data";
      }
    }
    return events2;
  }
  function tokenizeStrikethrough(effects, ok2, nok) {
    const previous2 = this.previous;
    const events2 = this.events;
    let size = 0;
    return start;
    function start(code2) {
      if (previous2 === 126 && events2[events2.length - 1][1].type !== "characterEscape") {
        return nok(code2);
      }
      effects.enter("strikethroughSequenceTemporary");
      return more(code2);
    }
    function more(code2) {
      const before = classifyCharacter(previous2);
      if (code2 === 126) {
        if (size > 1) return nok(code2);
        effects.consume(code2);
        size++;
        return more;
      }
      if (size < 2 && !single) return nok(code2);
      const token = effects.exit("strikethroughSequenceTemporary");
      const after = classifyCharacter(code2);
      token._open = !after || after === 2 && Boolean(before);
      token._close = !before || before === 2 && Boolean(after);
      return ok2(code2);
    }
  }
}
class EditMap {
  /**
   * Create a new edit map.
   */
  constructor() {
    this.map = [];
  }
  /**
   * Create an edit: a remove and/or add at a certain place.
   *
   * @param {number} index
   * @param {number} remove
   * @param {Array<Event>} add
   * @returns {undefined}
   */
  add(index2, remove, add) {
    addImplementation(this, index2, remove, add);
  }
  // To do: add this when moving to `micromark`.
  // /**
  //  * Create an edit: but insert `add` before existing additions.
  //  *
  //  * @param {number} index
  //  * @param {number} remove
  //  * @param {Array<Event>} add
  //  * @returns {undefined}
  //  */
  // addBefore(index, remove, add) {
  //   addImplementation(this, index, remove, add, true)
  // }
  /**
   * Done, change the events.
   *
   * @param {Array<Event>} events
   * @returns {undefined}
   */
  consume(events2) {
    this.map.sort(function(a, b) {
      return a[0] - b[0];
    });
    if (this.map.length === 0) {
      return;
    }
    let index2 = this.map.length;
    const vecs = [];
    while (index2 > 0) {
      index2 -= 1;
      vecs.push(events2.slice(this.map[index2][0] + this.map[index2][1]), this.map[index2][2]);
      events2.length = this.map[index2][0];
    }
    vecs.push(events2.slice());
    events2.length = 0;
    let slice = vecs.pop();
    while (slice) {
      for (const element2 of slice) {
        events2.push(element2);
      }
      slice = vecs.pop();
    }
    this.map.length = 0;
  }
}
function addImplementation(editMap, at, remove, add) {
  let index2 = 0;
  if (remove === 0 && add.length === 0) {
    return;
  }
  while (index2 < editMap.map.length) {
    if (editMap.map[index2][0] === at) {
      editMap.map[index2][1] += remove;
      editMap.map[index2][2].push(...add);
      return;
    }
    index2 += 1;
  }
  editMap.map.push([at, remove, add]);
}
function gfmTableAlign(events2, index2) {
  let inDelimiterRow = false;
  const align = [];
  while (index2 < events2.length) {
    const event = events2[index2];
    if (inDelimiterRow) {
      if (event[0] === "enter") {
        if (event[1].type === "tableContent") {
          align.push(events2[index2 + 1][1].type === "tableDelimiterMarker" ? "left" : "none");
        }
      } else if (event[1].type === "tableContent") {
        if (events2[index2 - 1][1].type === "tableDelimiterMarker") {
          const alignIndex = align.length - 1;
          align[alignIndex] = align[alignIndex] === "left" ? "center" : "right";
        }
      } else if (event[1].type === "tableDelimiterRow") {
        break;
      }
    } else if (event[0] === "enter" && event[1].type === "tableDelimiterRow") {
      inDelimiterRow = true;
    }
    index2 += 1;
  }
  return align;
}
function gfmTable() {
  return {
    flow: {
      null: {
        name: "table",
        tokenize: tokenizeTable,
        resolveAll: resolveTable
      }
    }
  };
}
function tokenizeTable(effects, ok2, nok) {
  const self2 = this;
  let size = 0;
  let sizeB = 0;
  let seen;
  return start;
  function start(code2) {
    let index2 = self2.events.length - 1;
    while (index2 > -1) {
      const type = self2.events[index2][1].type;
      if (type === "lineEnding" || // Note: markdown-rs uses `whitespace` instead of `linePrefix`
      type === "linePrefix") index2--;
      else break;
    }
    const tail = index2 > -1 ? self2.events[index2][1].type : null;
    const next = tail === "tableHead" || tail === "tableRow" ? bodyRowStart : headRowBefore;
    if (next === bodyRowStart && self2.parser.lazy[self2.now().line]) {
      return nok(code2);
    }
    return next(code2);
  }
  function headRowBefore(code2) {
    effects.enter("tableHead");
    effects.enter("tableRow");
    return headRowStart(code2);
  }
  function headRowStart(code2) {
    if (code2 === 124) {
      return headRowBreak(code2);
    }
    seen = true;
    sizeB += 1;
    return headRowBreak(code2);
  }
  function headRowBreak(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      if (sizeB > 1) {
        sizeB = 0;
        self2.interrupt = true;
        effects.exit("tableRow");
        effects.enter("lineEnding");
        effects.consume(code2);
        effects.exit("lineEnding");
        return headDelimiterStart;
      }
      return nok(code2);
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, headRowBreak, "whitespace")(code2);
    }
    sizeB += 1;
    if (seen) {
      seen = false;
      size += 1;
    }
    if (code2 === 124) {
      effects.enter("tableCellDivider");
      effects.consume(code2);
      effects.exit("tableCellDivider");
      seen = true;
      return headRowBreak;
    }
    effects.enter("data");
    return headRowData(code2);
  }
  function headRowData(code2) {
    if (code2 === null || code2 === 124 || markdownLineEndingOrSpace(code2)) {
      effects.exit("data");
      return headRowBreak(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? headRowEscape : headRowData;
  }
  function headRowEscape(code2) {
    if (code2 === 92 || code2 === 124) {
      effects.consume(code2);
      return headRowData;
    }
    return headRowData(code2);
  }
  function headDelimiterStart(code2) {
    self2.interrupt = false;
    if (self2.parser.lazy[self2.now().line]) {
      return nok(code2);
    }
    effects.enter("tableDelimiterRow");
    seen = false;
    if (markdownSpace(code2)) {
      return factorySpace(effects, headDelimiterBefore, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2);
    }
    return headDelimiterBefore(code2);
  }
  function headDelimiterBefore(code2) {
    if (code2 === 45 || code2 === 58) {
      return headDelimiterValueBefore(code2);
    }
    if (code2 === 124) {
      seen = true;
      effects.enter("tableCellDivider");
      effects.consume(code2);
      effects.exit("tableCellDivider");
      return headDelimiterCellBefore;
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterCellBefore(code2) {
    if (markdownSpace(code2)) {
      return factorySpace(effects, headDelimiterValueBefore, "whitespace")(code2);
    }
    return headDelimiterValueBefore(code2);
  }
  function headDelimiterValueBefore(code2) {
    if (code2 === 58) {
      sizeB += 1;
      seen = true;
      effects.enter("tableDelimiterMarker");
      effects.consume(code2);
      effects.exit("tableDelimiterMarker");
      return headDelimiterLeftAlignmentAfter;
    }
    if (code2 === 45) {
      sizeB += 1;
      return headDelimiterLeftAlignmentAfter(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      return headDelimiterCellAfter(code2);
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterLeftAlignmentAfter(code2) {
    if (code2 === 45) {
      effects.enter("tableDelimiterFiller");
      return headDelimiterFiller(code2);
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterFiller(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return headDelimiterFiller;
    }
    if (code2 === 58) {
      seen = true;
      effects.exit("tableDelimiterFiller");
      effects.enter("tableDelimiterMarker");
      effects.consume(code2);
      effects.exit("tableDelimiterMarker");
      return headDelimiterRightAlignmentAfter;
    }
    effects.exit("tableDelimiterFiller");
    return headDelimiterRightAlignmentAfter(code2);
  }
  function headDelimiterRightAlignmentAfter(code2) {
    if (markdownSpace(code2)) {
      return factorySpace(effects, headDelimiterCellAfter, "whitespace")(code2);
    }
    return headDelimiterCellAfter(code2);
  }
  function headDelimiterCellAfter(code2) {
    if (code2 === 124) {
      return headDelimiterBefore(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      if (!seen || size !== sizeB) {
        return headDelimiterNok(code2);
      }
      effects.exit("tableDelimiterRow");
      effects.exit("tableHead");
      return ok2(code2);
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterNok(code2) {
    return nok(code2);
  }
  function bodyRowStart(code2) {
    effects.enter("tableRow");
    return bodyRowBreak(code2);
  }
  function bodyRowBreak(code2) {
    if (code2 === 124) {
      effects.enter("tableCellDivider");
      effects.consume(code2);
      effects.exit("tableCellDivider");
      return bodyRowBreak;
    }
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("tableRow");
      return ok2(code2);
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, bodyRowBreak, "whitespace")(code2);
    }
    effects.enter("data");
    return bodyRowData(code2);
  }
  function bodyRowData(code2) {
    if (code2 === null || code2 === 124 || markdownLineEndingOrSpace(code2)) {
      effects.exit("data");
      return bodyRowBreak(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? bodyRowEscape : bodyRowData;
  }
  function bodyRowEscape(code2) {
    if (code2 === 92 || code2 === 124) {
      effects.consume(code2);
      return bodyRowData;
    }
    return bodyRowData(code2);
  }
}
function resolveTable(events2, context) {
  let index2 = -1;
  let inFirstCellAwaitingPipe = true;
  let rowKind = 0;
  let lastCell = [0, 0, 0, 0];
  let cell = [0, 0, 0, 0];
  let afterHeadAwaitingFirstBodyRow = false;
  let lastTableEnd = 0;
  let currentTable;
  let currentBody;
  let currentCell;
  const map2 = new EditMap();
  while (++index2 < events2.length) {
    const event = events2[index2];
    const token = event[1];
    if (event[0] === "enter") {
      if (token.type === "tableHead") {
        afterHeadAwaitingFirstBodyRow = false;
        if (lastTableEnd !== 0) {
          flushTableEnd(map2, context, lastTableEnd, currentTable, currentBody);
          currentBody = void 0;
          lastTableEnd = 0;
        }
        currentTable = {
          type: "table",
          start: Object.assign({}, token.start),
          // Note: correct end is set later.
          end: Object.assign({}, token.end)
        };
        map2.add(index2, 0, [["enter", currentTable, context]]);
      } else if (token.type === "tableRow" || token.type === "tableDelimiterRow") {
        inFirstCellAwaitingPipe = true;
        currentCell = void 0;
        lastCell = [0, 0, 0, 0];
        cell = [0, index2 + 1, 0, 0];
        if (afterHeadAwaitingFirstBodyRow) {
          afterHeadAwaitingFirstBodyRow = false;
          currentBody = {
            type: "tableBody",
            start: Object.assign({}, token.start),
            // Note: correct end is set later.
            end: Object.assign({}, token.end)
          };
          map2.add(index2, 0, [["enter", currentBody, context]]);
        }
        rowKind = token.type === "tableDelimiterRow" ? 2 : currentBody ? 3 : 1;
      } else if (rowKind && (token.type === "data" || token.type === "tableDelimiterMarker" || token.type === "tableDelimiterFiller")) {
        inFirstCellAwaitingPipe = false;
        if (cell[2] === 0) {
          if (lastCell[1] !== 0) {
            cell[0] = cell[1];
            currentCell = flushCell(map2, context, lastCell, rowKind, void 0, currentCell);
            lastCell = [0, 0, 0, 0];
          }
          cell[2] = index2;
        }
      } else if (token.type === "tableCellDivider") {
        if (inFirstCellAwaitingPipe) {
          inFirstCellAwaitingPipe = false;
        } else {
          if (lastCell[1] !== 0) {
            cell[0] = cell[1];
            currentCell = flushCell(map2, context, lastCell, rowKind, void 0, currentCell);
          }
          lastCell = cell;
          cell = [lastCell[1], index2, 0, 0];
        }
      }
    } else if (token.type === "tableHead") {
      afterHeadAwaitingFirstBodyRow = true;
      lastTableEnd = index2;
    } else if (token.type === "tableRow" || token.type === "tableDelimiterRow") {
      lastTableEnd = index2;
      if (lastCell[1] !== 0) {
        cell[0] = cell[1];
        currentCell = flushCell(map2, context, lastCell, rowKind, index2, currentCell);
      } else if (cell[1] !== 0) {
        currentCell = flushCell(map2, context, cell, rowKind, index2, currentCell);
      }
      rowKind = 0;
    } else if (rowKind && (token.type === "data" || token.type === "tableDelimiterMarker" || token.type === "tableDelimiterFiller")) {
      cell[3] = index2;
    }
  }
  if (lastTableEnd !== 0) {
    flushTableEnd(map2, context, lastTableEnd, currentTable, currentBody);
  }
  map2.consume(context.events);
  index2 = -1;
  while (++index2 < context.events.length) {
    const event = context.events[index2];
    if (event[0] === "enter" && event[1].type === "table") {
      event[1]._align = gfmTableAlign(context.events, index2);
    }
  }
  return events2;
}
function flushCell(map2, context, range, rowKind, rowEnd, previousCell) {
  const groupName = rowKind === 1 ? "tableHeader" : rowKind === 2 ? "tableDelimiter" : "tableData";
  const valueName = "tableContent";
  if (range[0] !== 0) {
    previousCell.end = Object.assign({}, getPoint(context.events, range[0]));
    map2.add(range[0], 0, [["exit", previousCell, context]]);
  }
  const now = getPoint(context.events, range[1]);
  previousCell = {
    type: groupName,
    start: Object.assign({}, now),
    // Note: correct end is set later.
    end: Object.assign({}, now)
  };
  map2.add(range[1], 0, [["enter", previousCell, context]]);
  if (range[2] !== 0) {
    const relatedStart = getPoint(context.events, range[2]);
    const relatedEnd = getPoint(context.events, range[3]);
    const valueToken = {
      type: valueName,
      start: Object.assign({}, relatedStart),
      end: Object.assign({}, relatedEnd)
    };
    map2.add(range[2], 0, [["enter", valueToken, context]]);
    if (rowKind !== 2) {
      const start = context.events[range[2]];
      const end = context.events[range[3]];
      start[1].end = Object.assign({}, end[1].end);
      start[1].type = "chunkText";
      start[1].contentType = "text";
      if (range[3] > range[2] + 1) {
        const a = range[2] + 1;
        const b = range[3] - range[2] - 1;
        map2.add(a, b, []);
      }
    }
    map2.add(range[3] + 1, 0, [["exit", valueToken, context]]);
  }
  if (rowEnd !== void 0) {
    previousCell.end = Object.assign({}, getPoint(context.events, rowEnd));
    map2.add(rowEnd, 0, [["exit", previousCell, context]]);
    previousCell = void 0;
  }
  return previousCell;
}
function flushTableEnd(map2, context, index2, table2, tableBody) {
  const exits = [];
  const related = getPoint(context.events, index2);
  if (tableBody) {
    tableBody.end = Object.assign({}, related);
    exits.push(["exit", tableBody, context]);
  }
  table2.end = Object.assign({}, related);
  exits.push(["exit", table2, context]);
  map2.add(index2 + 1, 0, exits);
}
function getPoint(events2, index2) {
  const event = events2[index2];
  const side = event[0] === "enter" ? "start" : "end";
  return event[1][side];
}
const tasklistCheck = {
  name: "tasklistCheck",
  tokenize: tokenizeTasklistCheck
};
function gfmTaskListItem() {
  return {
    text: {
      [91]: tasklistCheck
    }
  };
}
function tokenizeTasklistCheck(effects, ok2, nok) {
  const self2 = this;
  return open;
  function open(code2) {
    if (
      // Exit if there’s stuff before.
      self2.previous !== null || // Exit if not in the first content that is the first child of a list
      // item.
      !self2._gfmTasklistFirstContentOfListItem
    ) {
      return nok(code2);
    }
    effects.enter("taskListCheck");
    effects.enter("taskListCheckMarker");
    effects.consume(code2);
    effects.exit("taskListCheckMarker");
    return inside;
  }
  function inside(code2) {
    if (markdownLineEndingOrSpace(code2)) {
      effects.enter("taskListCheckValueUnchecked");
      effects.consume(code2);
      effects.exit("taskListCheckValueUnchecked");
      return close;
    }
    if (code2 === 88 || code2 === 120) {
      effects.enter("taskListCheckValueChecked");
      effects.consume(code2);
      effects.exit("taskListCheckValueChecked");
      return close;
    }
    return nok(code2);
  }
  function close(code2) {
    if (code2 === 93) {
      effects.enter("taskListCheckMarker");
      effects.consume(code2);
      effects.exit("taskListCheckMarker");
      effects.exit("taskListCheck");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    if (markdownLineEnding(code2)) {
      return ok2(code2);
    }
    if (markdownSpace(code2)) {
      return effects.check({
        tokenize: spaceThenNonSpace
      }, ok2, nok)(code2);
    }
    return nok(code2);
  }
}
function spaceThenNonSpace(effects, ok2, nok) {
  return factorySpace(effects, after, "whitespace");
  function after(code2) {
    return code2 === null ? nok(code2) : ok2(code2);
  }
}
function gfm(options) {
  return combineExtensions([
    gfmAutolinkLiteral(),
    gfmFootnote(),
    gfmStrikethrough(options),
    gfmTable(),
    gfmTaskListItem()
  ]);
}
const emptyOptions = {};
function remarkGfm(options) {
  const self2 = (
    /** @type {Processor<Root>} */
    this
  );
  const settings = options || emptyOptions;
  const data2 = self2.data();
  const micromarkExtensions = data2.micromarkExtensions || (data2.micromarkExtensions = []);
  const fromMarkdownExtensions = data2.fromMarkdownExtensions || (data2.fromMarkdownExtensions = []);
  const toMarkdownExtensions = data2.toMarkdownExtensions || (data2.toMarkdownExtensions = []);
  micromarkExtensions.push(gfm(settings));
  fromMarkdownExtensions.push(gfmFromMarkdown());
  toMarkdownExtensions.push(gfmToMarkdown(settings));
}
const nativeFetch = window.fetch.bind(window);
let refreshRequest = null;
function isAuthenticationRoute(request) {
  try {
    const url = new URL(request.url, window.location.origin);
    return url.origin === window.location.origin && url.pathname.startsWith("/auth/");
  } catch {
    return false;
  }
}
async function refreshSession() {
  if (!refreshRequest) {
    refreshRequest = nativeFetch("/auth/refresh", {
      method: "POST",
      credentials: "same-origin"
    }).finally(() => {
      refreshRequest = null;
    });
  }
  return refreshRequest;
}
window.fetch = async function authenticatedFetch(input, init) {
  const request = new Request(input, init);
  const response = await nativeFetch(request.clone());
  if (response.status !== 401 || isAuthenticationRoute(request)) return response;
  const refreshed = await refreshSession();
  if (refreshed.ok) return nativeFetch(request.clone());
  const returnTo = `${window.location.pathname}${window.location.search}${window.location.hash}`;
  window.location.assign(`/auth/login?return_to=${encodeURIComponent(returnTo)}`);
  return response;
};
const PATH_PARAM_RE = /\{[^{}]+\}/g;
const supportsRequestInitExt = () => {
  var _a2, _b;
  return typeof process === "object" && Number.parseInt((_b = (_a2 = process == null ? void 0 : process.versions) == null ? void 0 : _a2.node) == null ? void 0 : _b.substring(0, 2)) >= 18 && process.versions.undici;
};
function randomID() {
  return Math.random().toString(36).slice(2, 11);
}
function createClient(clientOptions) {
  let {
    baseUrl = "",
    Request: CustomRequest = globalThis.Request,
    fetch: baseFetch = globalThis.fetch,
    querySerializer: globalQuerySerializer,
    bodySerializer: globalBodySerializer,
    pathSerializer: globalPathSerializer,
    headers: baseHeaders,
    requestInitExt = void 0,
    ...baseOptions
  } = { ...clientOptions };
  requestInitExt = supportsRequestInitExt() ? requestInitExt : void 0;
  baseUrl = removeTrailingSlash(baseUrl);
  const globalMiddlewares = [];
  async function coreFetch(schemaPath, fetchOptions) {
    var _a2;
    const {
      baseUrl: localBaseUrl,
      fetch: fetch2 = baseFetch,
      Request: Request2 = CustomRequest,
      headers,
      params = {},
      parseAs = "json",
      querySerializer: requestQuerySerializer,
      bodySerializer = globalBodySerializer ?? defaultBodySerializer,
      pathSerializer: requestPathSerializer,
      body,
      middleware: requestMiddlewares = [],
      ...init
    } = fetchOptions || {};
    let finalBaseUrl = baseUrl;
    if (localBaseUrl) {
      finalBaseUrl = removeTrailingSlash(localBaseUrl) ?? baseUrl;
    }
    let querySerializer = typeof globalQuerySerializer === "function" ? globalQuerySerializer : createQuerySerializer(globalQuerySerializer);
    if (requestQuerySerializer) {
      querySerializer = typeof requestQuerySerializer === "function" ? requestQuerySerializer : createQuerySerializer({
        ...typeof globalQuerySerializer === "object" ? globalQuerySerializer : {},
        ...requestQuerySerializer
      });
    }
    const pathSerializer = requestPathSerializer || globalPathSerializer || defaultPathSerializer;
    const serializedBody = body === void 0 ? void 0 : bodySerializer(
      body,
      // Note: we declare mergeHeaders() both here and below because it’s a bit of a chicken-or-egg situation:
      // bodySerializer() needs all headers so we aren’t dropping ones set by the user, however,
      // the result of this ALSO sets the lowest-priority content-type header. So we re-merge below,
      // setting the content-type at the very beginning to be overwritten.
      // Lastly, based on the way headers work, it’s not a simple “present-or-not” check becauase null intentionally un-sets headers.
      mergeHeaders(baseHeaders, headers, params.header)
    );
    const finalHeaders = mergeHeaders(
      // with no body, we should not to set Content-Type
      serializedBody === void 0 || // if serialized body is FormData; browser will correctly set Content-Type & boundary expression
      serializedBody instanceof FormData ? {} : {
        "Content-Type": "application/json"
      },
      baseHeaders,
      headers,
      params.header
    );
    const finalMiddlewares = [...globalMiddlewares, ...requestMiddlewares];
    const requestInit = {
      redirect: "follow",
      ...baseOptions,
      ...init,
      body: serializedBody,
      headers: finalHeaders
    };
    let id2;
    let options;
    let request = new Request2(
      createFinalURL(schemaPath, { baseUrl: finalBaseUrl, params, querySerializer, pathSerializer }),
      requestInit
    );
    let response;
    for (const key in init) {
      if (!(key in request)) {
        request[key] = init[key];
      }
    }
    if (finalMiddlewares.length) {
      id2 = randomID();
      options = Object.freeze({
        baseUrl: finalBaseUrl,
        fetch: fetch2,
        parseAs,
        querySerializer,
        bodySerializer,
        pathSerializer
      });
      for (const m of finalMiddlewares) {
        if (m && typeof m === "object" && typeof m.onRequest === "function") {
          const result = await m.onRequest({
            request,
            schemaPath,
            params,
            options,
            id: id2
          });
          if (result) {
            if (result instanceof Request2) {
              request = result;
            } else if (result instanceof Response) {
              response = result;
              break;
            } else {
              throw new Error("onRequest: must return new Request() or Response() when modifying the request");
            }
          }
        }
      }
    }
    if (!response) {
      try {
        response = await fetch2(request, requestInitExt);
      } catch (error2) {
        let errorAfterMiddleware = error2;
        if (finalMiddlewares.length) {
          for (let i = finalMiddlewares.length - 1; i >= 0; i--) {
            const m = finalMiddlewares[i];
            if (m && typeof m === "object" && typeof m.onError === "function") {
              const result = await m.onError({
                request,
                error: errorAfterMiddleware,
                schemaPath,
                params,
                options,
                id: id2
              });
              if (result) {
                if (result instanceof Response) {
                  errorAfterMiddleware = void 0;
                  response = result;
                  break;
                }
                if (result instanceof Error) {
                  errorAfterMiddleware = result;
                  continue;
                }
                throw new Error("onError: must return new Response() or instance of Error");
              }
            }
          }
        }
        if (errorAfterMiddleware) {
          throw errorAfterMiddleware;
        }
      }
      if (finalMiddlewares.length) {
        for (let i = finalMiddlewares.length - 1; i >= 0; i--) {
          const m = finalMiddlewares[i];
          if (m && typeof m === "object" && typeof m.onResponse === "function") {
            const result = await m.onResponse({
              request,
              response,
              schemaPath,
              params,
              options,
              id: id2
            });
            if (result) {
              if (!(result instanceof Response)) {
                throw new Error("onResponse: must return new Response() when modifying the response");
              }
              response = result;
            }
          }
        }
      }
    }
    const contentLength = response.headers.get("Content-Length");
    if (response.status === 204 || request.method === "HEAD" || contentLength === "0" && !((_a2 = response.headers.get("Transfer-Encoding")) == null ? void 0 : _a2.includes("chunked"))) {
      return response.ok ? { data: void 0, response } : { error: void 0, response };
    }
    if (response.ok) {
      const getResponseData = async () => {
        if (parseAs === "stream") {
          return response.body;
        }
        if (parseAs === "json" && !contentLength) {
          const raw = await response.text();
          return raw ? JSON.parse(raw) : void 0;
        }
        return await response[parseAs]();
      };
      return { data: await getResponseData(), response };
    }
    let error = await response.text();
    try {
      error = JSON.parse(error);
    } catch {
    }
    return { error, response };
  }
  return {
    request(method, url, init) {
      return coreFetch(url, { ...init, method: method.toUpperCase() });
    },
    /** Call a GET endpoint */
    GET(url, init) {
      return coreFetch(url, { ...init, method: "GET" });
    },
    /** Call a PUT endpoint */
    PUT(url, init) {
      return coreFetch(url, { ...init, method: "PUT" });
    },
    /** Call a POST endpoint */
    POST(url, init) {
      return coreFetch(url, { ...init, method: "POST" });
    },
    /** Call a DELETE endpoint */
    DELETE(url, init) {
      return coreFetch(url, { ...init, method: "DELETE" });
    },
    /** Call a OPTIONS endpoint */
    OPTIONS(url, init) {
      return coreFetch(url, { ...init, method: "OPTIONS" });
    },
    /** Call a HEAD endpoint */
    HEAD(url, init) {
      return coreFetch(url, { ...init, method: "HEAD" });
    },
    /** Call a PATCH endpoint */
    PATCH(url, init) {
      return coreFetch(url, { ...init, method: "PATCH" });
    },
    /** Call a TRACE endpoint */
    TRACE(url, init) {
      return coreFetch(url, { ...init, method: "TRACE" });
    },
    /** Register middleware */
    use(...middleware) {
      for (const m of middleware) {
        if (!m) {
          continue;
        }
        if (typeof m !== "object" || !("onRequest" in m || "onResponse" in m || "onError" in m)) {
          throw new Error("Middleware must be an object with one of `onRequest()`, `onResponse() or `onError()`");
        }
        globalMiddlewares.push(m);
      }
    },
    /** Unregister middleware */
    eject(...middleware) {
      for (const m of middleware) {
        const i = globalMiddlewares.indexOf(m);
        if (i !== -1) {
          globalMiddlewares.splice(i, 1);
        }
      }
    }
  };
}
function serializePrimitiveParam(name2, value, options) {
  if (value === void 0 || value === null) {
    return "";
  }
  if (typeof value === "object") {
    throw new Error(
      "Deeply-nested arrays/objects aren’t supported. Provide your own `querySerializer()` to handle these."
    );
  }
  return `${name2}=${(options == null ? void 0 : options.allowReserved) === true ? value : encodeURIComponent(value)}`;
}
function serializeObjectParam(name2, value, options) {
  if (!value || typeof value !== "object") {
    return "";
  }
  const values = [];
  const joiner = {
    simple: ",",
    label: ".",
    matrix: ";"
  }[options.style] || "&";
  if (options.style !== "deepObject" && options.explode === false) {
    for (const k in value) {
      values.push(k, options.allowReserved === true ? value[k] : encodeURIComponent(value[k]));
    }
    const final2 = values.join(",");
    switch (options.style) {
      case "form": {
        return `${name2}=${final2}`;
      }
      case "label": {
        return `.${final2}`;
      }
      case "matrix": {
        return `;${name2}=${final2}`;
      }
      default: {
        return final2;
      }
    }
  }
  for (const k in value) {
    const finalName = options.style === "deepObject" ? `${name2}[${k}]` : k;
    values.push(serializePrimitiveParam(finalName, value[k], options));
  }
  const final = values.join(joiner);
  return options.style === "label" || options.style === "matrix" ? `${joiner}${final}` : final;
}
function serializeArrayParam(name2, value, options) {
  if (!Array.isArray(value)) {
    return "";
  }
  if (options.explode === false) {
    const joiner2 = { form: ",", spaceDelimited: "%20", pipeDelimited: "|" }[options.style] || ",";
    const final = (options.allowReserved === true ? value : value.map((v) => encodeURIComponent(v))).join(joiner2);
    switch (options.style) {
      case "simple": {
        return final;
      }
      case "label": {
        return `.${final}`;
      }
      case "matrix": {
        return `;${name2}=${final}`;
      }
      // case "spaceDelimited":
      // case "pipeDelimited":
      default: {
        return `${name2}=${final}`;
      }
    }
  }
  const joiner = { simple: ",", label: ".", matrix: ";" }[options.style] || "&";
  const values = [];
  for (const v of value) {
    if (options.style === "simple" || options.style === "label") {
      values.push(options.allowReserved === true ? v : encodeURIComponent(v));
    } else {
      values.push(serializePrimitiveParam(name2, v, options));
    }
  }
  return options.style === "label" || options.style === "matrix" ? `${joiner}${values.join(joiner)}` : values.join(joiner);
}
function createQuerySerializer(options) {
  return function querySerializer(queryParams) {
    const search2 = [];
    if (queryParams && typeof queryParams === "object") {
      for (const name2 in queryParams) {
        const value = queryParams[name2];
        if (value === void 0 || value === null) {
          continue;
        }
        if (Array.isArray(value)) {
          if (value.length === 0) {
            continue;
          }
          search2.push(
            serializeArrayParam(name2, value, {
              style: "form",
              explode: true,
              ...options == null ? void 0 : options.array,
              allowReserved: (options == null ? void 0 : options.allowReserved) || false
            })
          );
          continue;
        }
        if (typeof value === "object") {
          search2.push(
            serializeObjectParam(name2, value, {
              style: "deepObject",
              explode: true,
              ...options == null ? void 0 : options.object,
              allowReserved: (options == null ? void 0 : options.allowReserved) || false
            })
          );
          continue;
        }
        search2.push(serializePrimitiveParam(name2, value, options));
      }
    }
    return search2.join("&");
  };
}
function defaultPathSerializer(pathname, pathParams) {
  let nextURL = pathname;
  for (const match of pathname.match(PATH_PARAM_RE) ?? []) {
    let name2 = match.substring(1, match.length - 1);
    let explode = false;
    let style = "simple";
    if (name2.endsWith("*")) {
      explode = true;
      name2 = name2.substring(0, name2.length - 1);
    }
    if (name2.startsWith(".")) {
      style = "label";
      name2 = name2.substring(1);
    } else if (name2.startsWith(";")) {
      style = "matrix";
      name2 = name2.substring(1);
    }
    if (!pathParams || pathParams[name2] === void 0 || pathParams[name2] === null) {
      continue;
    }
    const value = pathParams[name2];
    if (Array.isArray(value)) {
      nextURL = nextURL.replace(match, serializeArrayParam(name2, value, { style, explode }));
      continue;
    }
    if (typeof value === "object") {
      nextURL = nextURL.replace(match, serializeObjectParam(name2, value, { style, explode }));
      continue;
    }
    if (style === "matrix") {
      nextURL = nextURL.replace(match, `;${serializePrimitiveParam(name2, value)}`);
      continue;
    }
    nextURL = nextURL.replace(match, style === "label" ? `.${encodeURIComponent(value)}` : encodeURIComponent(value));
  }
  return nextURL;
}
function defaultBodySerializer(body, headers) {
  if (body instanceof FormData) {
    return body;
  }
  if (headers) {
    const contentType = headers.get instanceof Function ? headers.get("Content-Type") ?? headers.get("content-type") : headers["Content-Type"] ?? headers["content-type"];
    if (contentType === "application/x-www-form-urlencoded") {
      return new URLSearchParams(body).toString();
    }
  }
  return JSON.stringify(body);
}
function createFinalURL(pathname, options) {
  var _a2;
  let finalURL = `${options.baseUrl}${pathname}`;
  if ((_a2 = options.params) == null ? void 0 : _a2.path) {
    finalURL = options.pathSerializer(finalURL, options.params.path);
  }
  let search2 = options.querySerializer(options.params.query ?? {});
  if (search2.startsWith("?")) {
    search2 = search2.substring(1);
  }
  if (search2) {
    finalURL += `?${search2}`;
  }
  return finalURL;
}
function mergeHeaders(...allHeaders) {
  const finalHeaders = new Headers();
  for (const h of allHeaders) {
    if (!h || typeof h !== "object") {
      continue;
    }
    const iterator = h instanceof Headers ? h.entries() : Object.entries(h);
    for (const [k, v] of iterator) {
      if (v === null) {
        finalHeaders.delete(k);
      } else if (Array.isArray(v)) {
        for (const v2 of v) {
          finalHeaders.append(k, v2);
        }
      } else if (v !== void 0) {
        finalHeaders.set(k, v);
      }
    }
  }
  return finalHeaders;
}
function removeTrailingSlash(url) {
  if (url.endsWith("/")) {
    return url.substring(0, url.length - 1);
  }
  return url;
}
function createLowLevelClient(baseUrl, fetchImpl) {
  return createClient({
    baseUrl: baseUrl.replace(/\/+$/, ""),
    ...fetchImpl === void 0 ? {} : { fetch: fetchImpl }
  });
}
class APIError extends Error {
  constructor(status, body) {
    super(body.message || `TMA request failed with HTTP ${status}`);
    __publicField(this, "status");
    __publicField(this, "code");
    __publicField(this, "requestId");
    __publicField(this, "retryable");
    __publicField(this, "details");
    this.name = "APIError";
    this.status = status;
    this.code = body.code || defaultErrorCode(status);
    this.requestId = body.request_id ?? "";
    this.retryable = body.retryable ?? defaultRetryable(status);
    if (body.details !== void 0)
      this.details = body.details;
  }
  static async fromResponse(response) {
    let body;
    try {
      const decoded = await response.json();
      body = "error" in decoded && decoded.error ? decoded.error : decoded;
    } catch {
      body = void 0;
    }
    return new APIError(response.status, body ?? {
      code: defaultErrorCode(response.status),
      message: response.statusText || `TMA request failed with HTTP ${response.status}`,
      request_id: response.headers.get("x-request-id") ?? "",
      retryable: defaultRetryable(response.status)
    });
  }
}
class SSESchemaError extends Error {
  constructor(message) {
    super(message);
    this.name = "SSESchemaError";
  }
}
function defaultErrorCode(status) {
  if (status === 400 || status === 422)
    return "invalid_request";
  if (status === 401)
    return "unauthorized";
  if (status === 403)
    return "forbidden";
  if (status === 404)
    return "not_found";
  if (status === 409)
    return "conflict";
  if (status === 412)
    return "revision_conflict";
  if (status === 413)
    return "payload_too_large";
  if (status === 415)
    return "unsupported_media_type";
  if (status === 429)
    return "rate_limited";
  if (status === 502)
    return "upstream_error";
  if (status === 503)
    return "service_unavailable";
  if (status === 504)
    return "upstream_timeout";
  return "internal_error";
}
function defaultRetryable(status) {
  return status === 429 || status === 502 || status === 503 || status === 504;
}
class Transport {
  constructor(baseURL2, options = {}) {
    __publicField(this, "baseURL");
    __publicField(this, "staticToken");
    __publicField(this, "tokenSource");
    __publicField(this, "fetchImpl");
    __publicField(this, "defaultHeaders");
    __publicField(this, "fetch", async (input, init = {}) => {
      var _a2;
      const headers = new Headers(input instanceof Request ? input.headers : void 0);
      for (const [key, value] of this.defaultHeaders) {
        if (!headers.has(key))
          headers.set(key, value);
      }
      for (const [key, value] of new Headers(init.headers))
        headers.set(key, value);
      const token = this.staticToken || await ((_a2 = this.tokenSource) == null ? void 0 : _a2.call(this, init.signal ?? void 0)) || "";
      if (token)
        headers.set("Authorization", `Bearer ${token}`);
      return this.fetchImpl(input, { ...init, headers });
    });
    var _a2;
    const parsed = new URL(baseURL2.trim());
    if (parsed.protocol !== "http:" && parsed.protocol !== "https:") {
      throw new TypeError("TMA baseURL must be an absolute HTTP(S) URL");
    }
    this.baseURL = baseURL2.trim().replace(/\/+$/, "");
    this.staticToken = ((_a2 = options.token) == null ? void 0 : _a2.trim()) ?? "";
    this.tokenSource = options.tokenSource;
    this.fetchImpl = options.fetch ?? globalThis.fetch;
    if (!this.fetchImpl)
      throw new TypeError("A Fetch API implementation is required");
    this.defaultHeaders = new Headers(options.headers);
  }
  url(path2) {
    return `${this.baseURL}${path2.startsWith("/") ? path2 : `/${path2}`}`;
  }
  async requestJSON(method, path2, body, options = {}) {
    const headers = new Headers(options.headers);
    headers.set("Accept", "application/json");
    let encoded;
    if (body !== void 0) {
      headers.set("Content-Type", "application/json");
      encoded = JSON.stringify(body);
    }
    const response = await this.fetch(this.url(path2), {
      method,
      headers,
      ...encoded === void 0 ? {} : { body: encoded },
      ...options.signal === void 0 ? {} : { signal: options.signal }
    });
    if (!response.ok)
      throw await APIError.fromResponse(response);
    if (response.status === 204)
      return void 0;
    return await response.json();
  }
  async request(method, path2, init = {}) {
    const response = await this.fetch(this.url(path2), { ...init, method });
    if (!response.ok)
      throw await APIError.fromResponse(response);
    return response;
  }
}
class ServiceBase {
  constructor(transport) {
    __publicField(this, "transport");
    this.transport = transport;
  }
}
function resourcePath(base, ...ids) {
  return [base.replace(/\/$/, ""), ...ids.map((id2) => encodeURIComponent(String(id2)))].join("/");
}
function withQuery(path2, values) {
  const query = new URLSearchParams();
  for (const [key, value] of Object.entries(values)) {
    if (value === void 0 || value === "")
      continue;
    query.set(key, String(value));
  }
  const encoded = query.toString();
  return encoded ? `${path2}?${encoded}` : path2;
}
class AgentsService extends ServiceBase {
  create(request, signal) {
    return this.transport.requestJSON("POST", "/v2/agents", request, signal ? { signal } : {});
  }
  default(signal) {
    return this.transport.requestJSON("GET", "/v2/agents/default", void 0, signal ? { signal } : {});
  }
  list(signal) {
    return this.transport.requestJSON("GET", "/v2/agents", void 0, signal ? { signal } : {}).then((value) => value.agents);
  }
  get(agentId, signal) {
    return this.transport.requestJSON("GET", agentPath(agentId), void 0, signal ? { signal } : {});
  }
  update(agentId, request, signal) {
    return this.transport.requestJSON("PATCH", agentPath(agentId), request, signal ? { signal } : {});
  }
  import(request, signal) {
    return this.transport.requestJSON("POST", "/v2/agents/import", request, signal ? { signal } : {});
  }
  export(agentId, signal) {
    return this.transport.requestJSON("GET", `${agentPath(agentId)}/export`, void 0, signal ? { signal } : {});
  }
  listConfigVersions(agentId, signal) {
    return this.transport.requestJSON("GET", `${agentPath(agentId)}/config-versions`, void 0, signal ? { signal } : {}).then((value) => value.config_versions);
  }
  createConfigVersion(agentId, request, signal) {
    return this.transport.requestJSON("POST", `${agentPath(agentId)}/config-versions`, request, signal ? { signal } : {});
  }
  rollback(agentId, version2, signal) {
    return this.transport.requestJSON("POST", resourcePath(`${agentPath(agentId)}/config-versions`, version2) + "/rollback", {}, signal ? { signal } : {});
  }
  toolingHealth(agentId, request = {}, signal) {
    return this.transport.requestJSON("POST", `${agentPath(agentId)}/tooling-health`, request, signal ? { signal } : {});
  }
}
function agentPath(agentId) {
  return resourcePath("/v2/agents", agentId);
}
class ParseError extends Error {
  constructor(message, options) {
    super(message), this.name = "ParseError", this.type = options.type, this.field = options.field, this.value = options.value, this.line = options.line;
  }
}
const LF = 10, CR = 13, SPACE = 32;
function noop(_arg) {
}
function createParser(config) {
  if (typeof config == "function")
    throw new TypeError(
      "`config` must be an object, got a function instead. Did you mean `createParser({onEvent: fn})`?"
    );
  const { onEvent = noop, onError = noop, onRetry = noop, onComment, maxBufferSize } = config, pendingFragments = [];
  let pendingFragmentsLength = 0, isFirstChunk = true, id2, data2 = "", dataLines = 0, eventType, terminated = false;
  function feed(chunk) {
    if (terminated)
      throw new Error(
        "Cannot feed parser: it was terminated after exceeding the configured max buffer size. Call `reset()` to resume parsing."
      );
    if (isFirstChunk && (isFirstChunk = false, chunk.charCodeAt(0) === 239 && chunk.charCodeAt(1) === 187 && chunk.charCodeAt(2) === 191 && (chunk = chunk.slice(3))), pendingFragments.length === 0) {
      const trailing2 = processLines(chunk);
      trailing2 !== "" && (pendingFragments.push(trailing2), pendingFragmentsLength = trailing2.length), checkBufferSize();
      return;
    }
    if (chunk.indexOf(`
`) === -1 && chunk.indexOf("\r") === -1) {
      pendingFragments.push(chunk), pendingFragmentsLength += chunk.length, checkBufferSize();
      return;
    }
    pendingFragments.push(chunk);
    const input = pendingFragments.join("");
    pendingFragments.length = 0, pendingFragmentsLength = 0;
    const trailing = processLines(input);
    trailing !== "" && (pendingFragments.push(trailing), pendingFragmentsLength = trailing.length), checkBufferSize();
  }
  function checkBufferSize() {
    maxBufferSize !== void 0 && (pendingFragmentsLength + data2.length <= maxBufferSize || (terminated = true, pendingFragments.length = 0, pendingFragmentsLength = 0, id2 = void 0, data2 = "", dataLines = 0, eventType = void 0, onError(
      new ParseError(`Buffered data exceeded max buffer size of ${maxBufferSize} characters`, {
        type: "max-buffer-size-exceeded"
      })
    )));
  }
  function processLines(chunk) {
    let searchIndex = 0;
    if (chunk.indexOf("\r") === -1) {
      let lfIndex = chunk.indexOf(`
`, searchIndex);
      for (; lfIndex !== -1; ) {
        if (searchIndex === lfIndex) {
          dataLines > 0 && onEvent({ id: id2, event: eventType, data: data2 }), id2 = void 0, data2 = "", dataLines = 0, eventType = void 0, searchIndex = lfIndex + 1, lfIndex = chunk.indexOf(`
`, searchIndex);
          continue;
        }
        const firstCharCode = chunk.charCodeAt(searchIndex);
        if (isDataPrefix(chunk, searchIndex, firstCharCode)) {
          const valueStart = chunk.charCodeAt(searchIndex + 5) === SPACE ? searchIndex + 6 : searchIndex + 5, value = chunk.slice(valueStart, lfIndex);
          if (dataLines === 0 && chunk.charCodeAt(lfIndex + 1) === LF) {
            onEvent({ id: id2, event: eventType, data: value }), id2 = void 0, data2 = "", eventType = void 0, searchIndex = lfIndex + 2, lfIndex = chunk.indexOf(`
`, searchIndex);
            continue;
          }
          data2 = dataLines === 0 ? value : `${data2}
${value}`, dataLines++;
        } else isEventPrefix(chunk, searchIndex, firstCharCode) ? eventType = chunk.slice(
          chunk.charCodeAt(searchIndex + 6) === SPACE ? searchIndex + 7 : searchIndex + 6,
          lfIndex
        ) || void 0 : parseLine(chunk, searchIndex, lfIndex);
        searchIndex = lfIndex + 1, lfIndex = chunk.indexOf(`
`, searchIndex);
      }
      return chunk.slice(searchIndex);
    }
    for (; searchIndex < chunk.length; ) {
      const crIndex = chunk.indexOf("\r", searchIndex), lfIndex = chunk.indexOf(`
`, searchIndex);
      let lineEnd = -1;
      if (crIndex !== -1 && lfIndex !== -1 ? lineEnd = crIndex < lfIndex ? crIndex : lfIndex : crIndex !== -1 ? crIndex === chunk.length - 1 ? lineEnd = -1 : lineEnd = crIndex : lfIndex !== -1 && (lineEnd = lfIndex), lineEnd === -1)
        break;
      parseLine(chunk, searchIndex, lineEnd), searchIndex = lineEnd + 1, chunk.charCodeAt(searchIndex - 1) === CR && chunk.charCodeAt(searchIndex) === LF && searchIndex++;
    }
    return chunk.slice(searchIndex);
  }
  function parseLine(chunk, start, end) {
    if (start === end) {
      dispatchEvent();
      return;
    }
    const firstCharCode = chunk.charCodeAt(start);
    if (isDataPrefix(chunk, start, firstCharCode)) {
      const valueStart = chunk.charCodeAt(start + 5) === SPACE ? start + 6 : start + 5, value2 = chunk.slice(valueStart, end);
      data2 = dataLines === 0 ? value2 : `${data2}
${value2}`, dataLines++;
      return;
    }
    if (isEventPrefix(chunk, start, firstCharCode)) {
      eventType = chunk.slice(chunk.charCodeAt(start + 6) === SPACE ? start + 7 : start + 6, end) || void 0;
      return;
    }
    if (firstCharCode === 105 && chunk.charCodeAt(start + 1) === 100 && chunk.charCodeAt(start + 2) === 58) {
      const value2 = chunk.slice(chunk.charCodeAt(start + 3) === SPACE ? start + 4 : start + 3, end);
      id2 = value2.includes("\0") ? void 0 : value2;
      return;
    }
    if (firstCharCode === 58) {
      if (onComment) {
        const line2 = chunk.slice(start, end);
        onComment(line2.slice(chunk.charCodeAt(start + 1) === SPACE ? 2 : 1));
      }
      return;
    }
    const line = chunk.slice(start, end), fieldSeparatorIndex = line.indexOf(":");
    if (fieldSeparatorIndex === -1) {
      processField(line, "", line);
      return;
    }
    const field = line.slice(0, fieldSeparatorIndex), offset = line.charCodeAt(fieldSeparatorIndex + 1) === SPACE ? 2 : 1, value = line.slice(fieldSeparatorIndex + offset);
    processField(field, value, line);
  }
  function processField(field, value, line) {
    switch (field) {
      case "event":
        eventType = value || void 0;
        break;
      case "data":
        data2 = dataLines === 0 ? value : `${data2}
${value}`, dataLines++;
        break;
      case "id":
        id2 = value.includes("\0") ? void 0 : value;
        break;
      case "retry":
        /^\d+$/.test(value) ? onRetry(parseInt(value, 10)) : onError(
          new ParseError(`Invalid \`retry\` value: "${value}"`, {
            type: "invalid-retry",
            value,
            line
          })
        );
        break;
      default:
        onError(
          new ParseError(
            `Unknown field "${field.length > 20 ? `${field.slice(0, 20)}…` : field}"`,
            { type: "unknown-field", field, value, line }
          )
        );
        break;
    }
  }
  function dispatchEvent() {
    dataLines > 0 && onEvent({
      id: id2,
      event: eventType,
      data: data2
    }), id2 = void 0, data2 = "", dataLines = 0, eventType = void 0;
  }
  function reset(options = {}) {
    if (options.consume && pendingFragments.length > 0) {
      const incompleteLine = pendingFragments.join("");
      parseLine(incompleteLine, 0, incompleteLine.length);
    }
    isFirstChunk = true, id2 = void 0, data2 = "", dataLines = 0, eventType = void 0, pendingFragments.length = 0, pendingFragmentsLength = 0, terminated = false;
  }
  return { feed, reset };
}
function isDataPrefix(chunk, i, firstCharCode) {
  return firstCharCode === 100 && chunk.charCodeAt(i + 1) === 97 && chunk.charCodeAt(i + 2) === 116 && chunk.charCodeAt(i + 3) === 97 && chunk.charCodeAt(i + 4) === 58;
}
function isEventPrefix(chunk, i, firstCharCode) {
  return firstCharCode === 101 && chunk.charCodeAt(i + 1) === 118 && chunk.charCodeAt(i + 2) === 101 && chunk.charCodeAt(i + 3) === 110 && chunk.charCodeAt(i + 4) === 116 && chunk.charCodeAt(i + 5) === 58;
}
async function* streamEvents(transport, path2, options = {}) {
  var _a2, _b, _c;
  let afterSeq = options.afterSeq ?? 0;
  let retryDelay = options.retryInitialMs ?? 250;
  const retryMax = options.retryMaxMs ?? 1e4;
  while (!((_a2 = options.signal) == null ? void 0 : _a2.aborted)) {
    try {
      const url = new URL(transport.url(path2));
      if (afterSeq > 0)
        url.searchParams.set("after_seq", String(afterSeq));
      const response = await transport.fetch(url, {
        method: "GET",
        headers: { Accept: "text/event-stream" },
        ...options.signal === void 0 ? {} : { signal: options.signal }
      });
      if (!response.ok) {
        if (response.status < 500 || response.status > 599)
          throw await APIError.fromResponse(response);
        await ((_b = response.body) == null ? void 0 : _b.cancel());
        throw new RetryableSSEError(`SSE endpoint returned HTTP ${response.status}`);
      }
      if (!response.body)
        throw new SSESchemaError("SSE response has no body");
      retryDelay = options.retryInitialMs ?? 250;
      const messages = [];
      let parserError;
      const parser = createParser({
        onEvent: (message) => messages.push(message),
        onError: (error) => {
          parserError = new SSESchemaError(error.message);
        }
      });
      const decoder = new TextDecoder();
      const reader = response.body.getReader();
      try {
        while (true) {
          const { done, value } = await reader.read();
          if (done)
            break;
          parser.feed(decoder.decode(value, { stream: true }));
          if (parserError)
            throw parserError;
          while (messages.length > 0) {
            const message = messages.shift();
            if (!message)
              continue;
            const event = decodeEvent(message.data);
            if (event.seq <= afterSeq)
              continue;
            afterSeq = event.seq;
            yield event;
          }
        }
      } finally {
        await reader.cancel().catch(() => void 0);
        reader.releaseLock();
      }
      parser.feed(decoder.decode());
      if (parserError)
        throw parserError;
      throw new RetryableSSEError("SSE connection closed");
    } catch (error) {
      if ((_c = options.signal) == null ? void 0 : _c.aborted)
        throw abortError();
      if (error instanceof SSESchemaError)
        throw error;
      if (error instanceof APIError && (error.status < 500 || error.status > 599))
        throw error;
      await abortableDelay(retryDelay, options.signal);
      retryDelay = Math.min(retryDelay * 2, retryMax);
    }
  }
  throw abortError();
}
function decodeEvent(data2) {
  let decoded;
  try {
    decoded = JSON.parse(data2);
  } catch (error) {
    throw new SSESchemaError(`SSE event is not valid JSON: ${String(error)}`);
  }
  if (!decoded || typeof decoded !== "object")
    throw new SSESchemaError("SSE event must be an object");
  const event = decoded;
  if (!Number.isSafeInteger(event.seq) || typeof event.type !== "string" || typeof event.created_at !== "string") {
    throw new SSESchemaError("SSE event is missing seq, type, or created_at");
  }
  return event;
}
class RetryableSSEError extends Error {
}
async function abortableDelay(delay, signal) {
  if (signal == null ? void 0 : signal.aborted)
    throw abortError();
  await new Promise((resolve, reject) => {
    const onAbort = () => {
      clearTimeout(timeout);
      reject(abortError());
    };
    const timeout = setTimeout(() => {
      signal == null ? void 0 : signal.removeEventListener("abort", onAbort);
      resolve();
    }, delay);
    signal == null ? void 0 : signal.addEventListener("abort", onAbort, { once: true });
  });
}
function abortError() {
  return new DOMException("The operation was aborted", "AbortError");
}
class SessionsService extends ServiceBase {
  create(request, signal) {
    return this.transport.requestJSON("POST", "/v2/sessions", request, signal ? { signal } : {});
  }
  list(query = {}, signal) {
    const path2 = withQuery("/v2/sessions", {
      workspace_id: query.workspaceId,
      owner_id: query.ownerId,
      status: query.status,
      include_archived: query.includeArchived || void 0,
      limit: query.limit
    });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.sessions);
  }
  get(sessionId, signal) {
    return this.transport.requestJSON("GET", sessionPath(sessionId), void 0, signal ? { signal } : {});
  }
  updateMetadata(sessionId, request, signal) {
    return this.transport.requestJSON("PATCH", sessionPath(sessionId), request, signal ? { signal } : {});
  }
  delete(sessionId, signal) {
    return this.transport.requestJSON("DELETE", sessionPath(sessionId), void 0, signal ? { signal } : {});
  }
  archive(sessionId, signal) {
    return this.transport.requestJSON("POST", `${sessionPath(sessionId)}/archive`, {}, signal ? { signal } : {});
  }
  restore(sessionId, signal) {
    return this.transport.requestJSON("POST", `${sessionPath(sessionId)}/restore`, {}, signal ? { signal } : {});
  }
  rerun(sessionId, request = {}, signal) {
    return this.transport.requestJSON("POST", `${sessionPath(sessionId)}/rerun`, request, signal ? { signal } : {});
  }
  upgradeConfig(sessionId, request, signal) {
    return this.transport.requestJSON("POST", `${sessionPath(sessionId)}/config/upgrade`, request, signal ? { signal } : {});
  }
  compare(leftSessionId, rightSessionId, signal) {
    const path2 = withQuery("/v2/session-comparisons", { left_session_id: leftSessionId, right_session_id: rightSessionId });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {});
  }
  updateRuntimeSettings(sessionId, request, signal) {
    return this.transport.requestJSON("PATCH", `${sessionPath(sessionId)}/runtime-settings`, request, signal ? { signal } : {});
  }
  runtimeConfig(sessionId, signal) {
    return this.transport.requestJSON("GET", `${sessionPath(sessionId)}/runtime-config`, void 0, signal ? { signal } : {});
  }
  runtimeCapabilities(sessionId, signal) {
    return this.transport.requestJSON("GET", `${sessionPath(sessionId)}/runtime-capabilities`, void 0, signal ? { signal } : {});
  }
  summary(sessionId, signal) {
    return this.transport.requestJSON("GET", `${sessionPath(sessionId)}/summary`, void 0, signal ? { signal } : {});
  }
  usage(sessionId, signal) {
    return this.transport.requestJSON("GET", `${sessionPath(sessionId)}/usage`, void 0, signal ? { signal } : {});
  }
  listEvents(sessionId, afterSeq = 0, signal) {
    const path2 = withQuery(`${sessionPath(sessionId)}/events`, { after_seq: afterSeq > 0 ? afterSeq : void 0 });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.events);
  }
  appendEvents(sessionId, request, signal) {
    return this.transport.requestJSON("POST", `${sessionPath(sessionId)}/events`, request, signal ? { signal } : {});
  }
  events(sessionId, options = {}) {
    return streamEvents(this.transport, `${sessionPath(sessionId)}/events/stream`, options);
  }
}
function sessionPath(sessionId) {
  return resourcePath("/v2/sessions", sessionId);
}
class ObservabilityService extends ServiceBase {
  status(signal) {
    return this.transport.requestJSON("GET", "/v2/observability/status", void 0, signal ? { signal } : {});
  }
  retry(signal) {
    return this.transport.requestJSON("POST", "/v2/observability/retry", void 0, signal ? { signal } : {});
  }
  integrityKeys(signal) {
    return this.transport.requestJSON("GET", "/v2/observability/security-audit/integrity-keys", void 0, signal ? { signal } : {});
  }
}
class AuditService extends ServiceBase {
  list(query = {}, signal) {
    const path2 = withQuery("/v2/operator-audit", {
      workspace_id: query.workspaceId,
      session_id: query.sessionId,
      principal_id: query.principalId,
      action: query.action,
      limit: query.limit
    });
    return this.listPath(path2, signal);
  }
  listSession(sessionId, signal) {
    return this.listPath(`${sessionPath(sessionId)}/operator-audit`, signal);
  }
  integrityKeys(signal) {
    return this.transport.requestJSON("GET", "/v2/observability/security-audit/integrity-keys", void 0, signal ? { signal } : {});
  }
  replayDeadLetters(limit, signal) {
    const path2 = withQuery("/v2/observability/security-audit/replay", { limit });
    return this.transport.requestJSON("POST", path2, void 0, signal ? { signal } : {});
  }
  listPath(path2, signal) {
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.audit_records);
  }
}
class EnvironmentVariablesService extends ServiceBase {
  list(query = {}, signal) {
    return this.transport.requestJSON("GET", variablesPath(query), void 0, signal ? { signal } : {}).then((value) => value.variables);
  }
  put(name2, request, query = {}, signal) {
    return this.transport.requestJSON("PUT", variablePath(name2, query), request, signal ? { signal } : {});
  }
  delete(name2, query = {}, signal) {
    return this.transport.requestJSON("DELETE", variablePath(name2, query), void 0, signal ? { signal } : {});
  }
}
function variablesPath(query) {
  return withQuery("/v2/environment-variables", { workspace_id: query.workspaceId });
}
function variablePath(name2, query) {
  return withQuery(resourcePath("/v2/environment-variables", name2), { workspace_id: query.workspaceId });
}
class ArtifactsService extends ServiceBase {
  create(sessionId, request, signal) {
    return this.transport.requestJSON("POST", artifactsPath(sessionId), request, signal ? { signal } : {});
  }
  list(sessionId, signal) {
    return this.transport.requestJSON("GET", artifactsPath(sessionId), void 0, signal ? { signal } : {}).then((value) => value.artifacts);
  }
  async upload(sessionId, fields, file, signal) {
    const form = new FormData();
    for (const [key, value] of Object.entries(fields))
      form.set(key, value);
    const body = file.contentType && file.body.type !== file.contentType ? new Blob([file.body], { type: file.contentType }) : file.body;
    form.set("file", body, file.filename);
    const response = await this.transport.request("POST", `${artifactsPath(sessionId)}/upload`, {
      body: form,
      ...signal === void 0 ? {} : { signal }
    });
    return await response.json();
  }
  download(sessionId, artifactId, signal) {
    return this.transport.request("GET", `${artifactPath(sessionId, artifactId)}/download`, signal ? { signal } : {});
  }
  async delete(sessionId, artifactId, signal) {
    await this.transport.request("DELETE", artifactPath(sessionId, artifactId), signal ? { signal } : {});
  }
}
function artifactsPath(sessionId) {
  return `${sessionPath(sessionId)}/artifacts`;
}
function artifactPath(sessionId, artifactId) {
  return resourcePath(artifactsPath(sessionId), artifactId);
}
class AuthService extends ServiceBase {
  configuration(signal) {
    return this.transport.requestJSON("GET", "/v2/auth/config", void 0, signal ? { signal } : {});
  }
  me(signal) {
    return this.transport.requestJSON("GET", "/v2/auth/me", void 0, signal ? { signal } : {});
  }
}
class EnvironmentsService extends ServiceBase {
  create(request, signal) {
    return this.transport.requestJSON("POST", "/v2/environments", request, signal ? { signal } : {});
  }
}
class InterventionsService extends ServiceBase {
  list(sessionId, status, signal) {
    const path2 = withQuery(`${sessionPath(sessionId)}/interventions`, { status });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.interventions);
  }
  decide(sessionId, turnId, callId, decision, reason = "", signal) {
    const path2 = resourcePath(`${sessionPath(sessionId)}/interventions`, turnId, callId) + `/${decision}`;
    return this.transport.requestJSON("POST", path2, { reason }, signal ? { signal } : {});
  }
  approve(sessionId, turnId, callId, reason = "", signal) {
    return this.decide(sessionId, turnId, callId, "approve", reason, signal);
  }
  reject(sessionId, turnId, callId, reason = "", signal) {
    return this.decide(sessionId, turnId, callId, "reject", reason, signal);
  }
}
class LLMService extends ServiceBase {
  listProviders(signal) {
    return this.transport.requestJSON("GET", "/v2/llm-providers", void 0, signal ? { signal } : {}).then((value) => value.providers);
  }
  getProvider(providerId, signal) {
    return this.transport.requestJSON("GET", providerPath(providerId), void 0, signal ? { signal } : {});
  }
  createProvider(request, signal) {
    return this.transport.requestJSON("POST", "/v2/llm-providers", request, signal ? { signal } : {});
  }
  updateProvider(providerId, expectedRevision, request, signal) {
    return this.transport.requestJSON("PATCH", providerPath(providerId), request, requestOptions(expectedRevision, signal));
  }
  setProviderEnabled(providerId, expectedRevision, enabled, signal) {
    return this.transport.requestJSON("POST", `${providerPath(providerId)}/${enabled ? "enable" : "disable"}`, {}, requestOptions(expectedRevision, signal));
  }
  deleteProvider(providerId, expectedRevision, signal) {
    return this.transport.requestJSON("DELETE", providerPath(providerId), void 0, requestOptions(expectedRevision, signal));
  }
  testProvider(providerId, signal) {
    return this.transport.requestJSON("POST", `${providerPath(providerId)}/test`, {}, signal ? { signal } : {});
  }
  listModels(providerId, signal) {
    const path2 = withQuery("/v2/llm-models", { provider_id: providerId });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.models);
  }
  createModel(request, signal) {
    return this.transport.requestJSON("POST", "/v2/llm-models", request, {
      headers: { "If-None-Match": "*" },
      ...signal === void 0 ? {} : { signal }
    });
  }
  updateModel(expectedRevision, request, signal) {
    return this.transport.requestJSON("POST", "/v2/llm-models", request, requestOptions(expectedRevision, signal));
  }
  deleteModel(providerId, model, expectedRevision, signal) {
    return this.transport.requestJSON("DELETE", resourcePath("/v2/llm-models", providerId, model), void 0, requestOptions(expectedRevision, signal));
  }
  testModel(providerId, model, signal) {
    return this.transport.requestJSON("POST", `${resourcePath("/v2/llm-models", providerId, model)}/test`, {}, signal ? { signal } : {});
  }
  usage(query = {}, signal) {
    const path2 = withQuery("/v2/llm-usage", {
      workspace_id: query.workspaceId,
      provider_id: query.providerId,
      model: query.model,
      status: query.status,
      group_by: query.groupBy,
      from: formatTime$1(query.from),
      to: formatTime$1(query.to)
    });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {});
  }
}
function providerPath(providerId) {
  return resourcePath("/v2/llm-providers", providerId);
}
function requestOptions(expectedRevision, signal) {
  return {
    headers: { "If-Match": `"${expectedRevision}"` },
    ...signal === void 0 ? {} : { signal }
  };
}
function formatTime$1(value) {
  if (value === void 0)
    return void 0;
  return value instanceof Date ? value.toISOString() : value;
}
class MCPService extends ServiceBase {
  list(query = {}, signal) {
    return this.transport.requestJSON("GET", serversPath(query), void 0, signal ? { signal } : {}).then((value) => value.servers);
  }
  runtimeStatus(query = {}, signal) {
    const path2 = withQuery("/v2/mcp-servers/runtime-status", { workspace_id: query.workspaceId });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {});
  }
  create(request, signal) {
    return this.transport.requestJSON("POST", "/v2/mcp-servers", request, signal ? { signal } : {});
  }
  get(serverId, signal) {
    return this.transport.requestJSON("GET", serverPath(serverId), void 0, signal ? { signal } : {});
  }
  update(serverId, request, signal) {
    return this.transport.requestJSON("PATCH", serverPath(serverId), request, signal ? { signal } : {});
  }
  setEnabled(serverId, enabled, signal) {
    return this.transport.requestJSON("POST", `${serverPath(serverId)}/${enabled ? "enable" : "disable"}`, {}, signal ? { signal } : {});
  }
  archive(serverId, signal) {
    return this.transport.requestJSON("DELETE", serverPath(serverId), void 0, signal ? { signal } : {});
  }
  test(serverId, signal) {
    return this.transport.requestJSON("POST", `${serverPath(serverId)}/test`, {}, signal ? { signal } : {});
  }
  versions(serverId, signal) {
    return this.transport.requestJSON("GET", `${serverPath(serverId)}/versions`, void 0, signal ? { signal } : {}).then((value) => value.versions);
  }
  restoreVersion(serverId, version2, signal) {
    const path2 = resourcePath(`${serverPath(serverId)}/versions`, version2) + "/restore";
    return this.transport.requestJSON("POST", path2, void 0, signal ? { signal } : {});
  }
}
function serversPath(query) {
  return withQuery("/v2/mcp-servers", { workspace_id: query.workspaceId });
}
function serverPath(serverId) {
  return resourcePath("/v2/mcp-servers", serverId);
}
class SkillsService extends ServiceBase {
  create(request, signal) {
    return this.transport.requestJSON("POST", "/v2/skills", request, signal ? { signal } : {});
  }
  list(query = {}, signal) {
    const path2 = withQuery("/v2/skills", { workspace_id: query.workspaceId, include_archived: query.includeArchived || void 0 });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.skills);
  }
  get(skillId, signal) {
    return this.transport.requestJSON("GET", skillPath(skillId), void 0, signal ? { signal } : {});
  }
  archive(skillId, signal) {
    return this.transport.requestJSON("POST", `${skillPath(skillId)}/archive`, void 0, signal ? { signal } : {});
  }
  createVersion(skillId, request, signal) {
    return this.transport.requestJSON("POST", `${skillPath(skillId)}/versions`, request, signal ? { signal } : {});
  }
  listVersions(skillId, signal) {
    return this.transport.requestJSON("GET", `${skillPath(skillId)}/versions`, void 0, signal ? { signal } : {}).then((value) => value.versions);
  }
  getVersion(skillId, version2, signal) {
    return this.transport.requestJSON("GET", skillVersionPath(skillId, version2), void 0, signal ? { signal } : {});
  }
  downloadPackage(skillId, version2, signal) {
    return this.transport.request("GET", `${skillVersionPath(skillId, version2)}/package`, signal ? { signal } : {});
  }
  resolvePreview(request, signal) {
    return this.transport.requestJSON("POST", "/v2/skills/resolve-preview", request, signal ? { signal } : {});
  }
  listUsages(sessionId, turnId, signal) {
    const path2 = withQuery(`${sessionPath(sessionId)}/skill-usages`, { turn_id: turnId });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.skill_usages);
  }
  backfillPackages(request = {}, signal) {
    return this.transport.requestJSON("POST", "/v2/skill-packages/backfill", request, signal ? { signal } : {});
  }
  effectiveRetentionPolicy(workspaceId, signal) {
    const path2 = withQuery("/v2/skill-asset-retention/effective", { workspace_id: workspaceId });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {});
  }
  createRetentionPolicy(request, signal) {
    return this.transport.requestJSON("POST", "/v2/skill-asset-retention/policies", request, signal ? { signal } : {});
  }
  listRetentionPolicies(query = {}, signal) {
    const path2 = withQuery("/v2/skill-asset-retention/policies", {
      organization_id: query.organizationId,
      workspace_id: query.workspaceId,
      include_archived: query.includeArchived || void 0
    });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.policies);
  }
  getRetentionPolicy(policyId, signal) {
    return this.transport.requestJSON("GET", retentionPolicyPath(policyId), void 0, signal ? { signal } : {});
  }
  publishRetentionPolicyVersion(policyId, request, signal) {
    return this.transport.requestJSON("POST", `${retentionPolicyPath(policyId)}/versions`, request, signal ? { signal } : {});
  }
  getRetentionPolicyVersion(policyId, version2, signal) {
    return this.transport.requestJSON("GET", resourcePath(`${retentionPolicyPath(policyId)}/versions`, version2), void 0, signal ? { signal } : {});
  }
  archiveRetentionPolicy(policyId, signal) {
    return this.transport.requestJSON("POST", `${retentionPolicyPath(policyId)}/archive`, void 0, signal ? { signal } : {});
  }
  previewAssetGC(request, signal) {
    return this.transport.requestJSON("POST", "/v2/skill-asset-gc/preview", request, signal ? { signal } : {});
  }
  runAssetGC(request, signal) {
    return this.transport.requestJSON("POST", "/v2/skill-asset-gc/run", request, signal ? { signal } : {});
  }
  listAssetGCRuns(query = {}, signal) {
    return this.transport.requestJSON("GET", assetGCListPath("/v2/skill-asset-gc/runs", query), void 0, signal ? { signal } : {}).then((value) => value.runs);
  }
  getAssetGCRun(runId, signal) {
    return this.transport.requestJSON("GET", resourcePath("/v2/skill-asset-gc/runs", runId), void 0, signal ? { signal } : {});
  }
  listAssetGCTombstones(query = {}, signal) {
    return this.transport.requestJSON("GET", assetGCListPath("/v2/skill-asset-gc/tombstones", query), void 0, signal ? { signal } : {}).then((value) => value.tombstones);
  }
}
function skillPath(skillId) {
  return resourcePath("/v2/skills", skillId);
}
function skillVersionPath(skillId, version2) {
  return resourcePath(`${skillPath(skillId)}/versions`, version2);
}
function retentionPolicyPath(policyId) {
  return resourcePath("/v2/skill-asset-retention/policies", policyId);
}
function assetGCListPath(path2, query) {
  return withQuery(path2, { workspace_id: query.workspaceId, limit: query.limit });
}
class MarketplaceService extends ServiceBase {
  discover(query, signal) {
    const path2 = withQuery("/v2/skills/marketplace/discover", { session_id: query.sessionId, query: query.query, repository: query.repository, limit: query.limit });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {});
  }
  preview(request, signal) {
    return this.transport.requestJSON("POST", "/v2/skills/marketplace/preview", request, signal ? { signal } : {});
  }
  install(request, signal) {
    return this.transport.requestJSON("POST", "/v2/skills/marketplace/install", request, signal ? { signal } : {});
  }
  browseInternal(query, signal) {
    const values = new URLSearchParams();
    values.set("session_id", query.sessionId);
    if (query.query)
      values.set("query", query.query);
    if (query.category)
      values.set("category", query.category);
    for (const tag of query.tags ?? [])
      values.append("tag", tag);
    if (query.limit)
      values.set("limit", String(query.limit));
    return this.transport.requestJSON("GET", `/v2/skills/marketplace/internal?${values.toString()}`, void 0, signal ? { signal } : {});
  }
  previewInternal(request, signal) {
    return this.transport.requestJSON("POST", "/v2/skills/marketplace/internal/preview", request, signal ? { signal } : {});
  }
  installInternal(request, signal) {
    return this.transport.requestJSON("POST", "/v2/skills/marketplace/internal/install", request, signal ? { signal } : {});
  }
  enableInstalled(skillId, request, signal) {
    return this.transport.requestJSON("POST", `${skillPath(skillId)}/enable`, request, signal ? { signal } : {});
  }
  disableInstalled(skillId, request, signal) {
    return this.transport.requestJSON("POST", `${skillPath(skillId)}/disable`, request, signal ? { signal } : {});
  }
  createEntry(request, signal) {
    return this.transport.requestJSON("POST", "/v2/skill-marketplace-entries", request, signal ? { signal } : {});
  }
  listEntries(query = {}, signal) {
    const path2 = withQuery("/v2/skill-marketplace-entries", { workspace_id: query.workspaceId, status: query.status, include_withdrawn: query.includeWithdrawn || void 0 });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.entries);
  }
  getEntry(entryId, workspaceId, signal) {
    return this.transport.requestJSON("GET", withQuery(entryPath(entryId), { workspace_id: workspaceId }), void 0, signal ? { signal } : {});
  }
  updateEntry(entryId, request, signal) {
    return this.transport.requestJSON("PATCH", entryPath(entryId), request, signal ? { signal } : {});
  }
  submitEntry(entryId, request = {}, signal) {
    return this.transitionEntry(entryId, "submit", request, signal);
  }
  publishEntry(entryId, request = {}, signal) {
    return this.transitionEntry(entryId, "publish", request, signal);
  }
  withdrawEntry(entryId, request = {}, signal) {
    return this.transitionEntry(entryId, "withdraw", request, signal);
  }
  createPolicy(request, signal) {
    return this.transport.requestJSON("POST", "/v2/skill-marketplace-policies", request, signal ? { signal } : {});
  }
  listPolicies(query = {}, signal) {
    const path2 = withQuery("/v2/skill-marketplace-policies", { organization_id: query.organizationId, workspace_id: query.workspaceId, include_archived: query.includeArchived || void 0 });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.policies);
  }
  getPolicy(policyId, signal) {
    return this.transport.requestJSON("GET", policyPath(policyId), void 0, signal ? { signal } : {});
  }
  publishPolicyVersion(policyId, request, signal) {
    return this.transport.requestJSON("POST", `${policyPath(policyId)}/versions`, request, signal ? { signal } : {});
  }
  getPolicyVersion(policyId, version2, signal) {
    return this.transport.requestJSON("GET", resourcePath(`${policyPath(policyId)}/versions`, version2), void 0, signal ? { signal } : {});
  }
  archivePolicy(policyId, signal) {
    return this.transport.requestJSON("POST", `${policyPath(policyId)}/archive`, void 0, signal ? { signal } : {});
  }
  transitionEntry(entryId, action, request, signal) {
    return this.transport.requestJSON("POST", `${entryPath(entryId)}/${action}`, request, signal ? { signal } : {});
  }
}
function entryPath(entryId) {
  return resourcePath("/v2/skill-marketplace-entries", entryId);
}
function policyPath(policyId) {
  return resourcePath("/v2/skill-marketplace-policies", policyId);
}
class ObjectRefsService extends ServiceBase {
  create(request, signal) {
    return this.transport.requestJSON("POST", "/v2/object-refs", request, signal ? { signal } : {});
  }
  get(objectRefId, signal) {
    return this.transport.requestJSON("GET", objectRefPath(objectRefId), void 0, signal ? { signal } : {});
  }
  async delete(objectRefId, signal) {
    await this.transport.request("DELETE", objectRefPath(objectRefId), signal ? { signal } : {});
  }
  download(objectRefId, sessionId, signal) {
    const path2 = withQuery(`${objectRefPath(objectRefId)}/download`, { session_id: sessionId });
    return this.transport.request("GET", path2, signal ? { signal } : {});
  }
}
function objectRefPath(objectRefId) {
  return resourcePath("/v2/object-refs", objectRefId);
}
class OrchestrationService extends ServiceBase {
  taskGroupTemplates(signal) {
    return this.transport.requestJSON("GET", "/v2/agent/task-group-templates", void 0, signal ? { signal } : {});
  }
  discussionStrategies(signal) {
    return this.transport.requestJSON("GET", "/v2/agent/discussion-strategies", void 0, signal ? { signal } : {});
  }
  listDeliberations(sessionId, signal) {
    return this.transport.requestJSON("GET", `${sessionPath(sessionId)}/deliberations`, void 0, signal ? { signal } : {}).then((value) => value.deliberations);
  }
  getDeliberation(sessionId, deliberationId, signal) {
    return this.transport.requestJSON("GET", deliberationPath(sessionId, deliberationId), void 0, signal ? { signal } : {});
  }
  cancelDeliberation(sessionId, deliberationId, request = {}, signal) {
    return this.transport.requestJSON("POST", `${deliberationPath(sessionId, deliberationId)}/cancel`, request, signal ? { signal } : {});
  }
  retryDeliberationParticipant(sessionId, deliberationId, participantIndex, request, signal) {
    const path2 = resourcePath(`${deliberationPath(sessionId, deliberationId)}/participants`, participantIndex) + "/retry";
    return this.transport.requestJSON("POST", path2, request, signal ? { signal } : {});
  }
  listTaskGroups(sessionId, signal) {
    return this.transport.requestJSON("GET", `${sessionPath(sessionId)}/task-groups`, void 0, signal ? { signal } : {}).then((value) => value.task_groups);
  }
  taskGroupTree(sessionId, signal) {
    return this.transport.requestJSON("GET", `${sessionPath(sessionId)}/task-group-tree`, void 0, signal ? { signal } : {});
  }
  getTaskGroup(sessionId, groupId, signal) {
    return this.transport.requestJSON("GET", taskGroupPath(sessionId, groupId), void 0, signal ? { signal } : {});
  }
  cancelTaskGroup(sessionId, groupId, request = {}, signal) {
    return this.transport.requestJSON("POST", `${taskGroupPath(sessionId, groupId)}/cancel`, request, signal ? { signal } : {});
  }
  retryTaskGroup(sessionId, groupId, signal) {
    return this.transport.requestJSON("POST", `${taskGroupPath(sessionId, groupId)}/retry`, {}, signal ? { signal } : {});
  }
  retryTaskGroupItem(sessionId, groupId, itemIndex, signal) {
    const path2 = resourcePath(`${taskGroupPath(sessionId, groupId)}/items`, itemIndex) + "/retry";
    return this.transport.requestJSON("POST", path2, {}, signal ? { signal } : {});
  }
  reapOrphans(request = {}, signal) {
    return this.transport.requestJSON("POST", "/v2/subagents/reap-orphans", request, signal ? { signal } : {});
  }
}
function deliberationPath(sessionId, deliberationId) {
  return resourcePath(`${sessionPath(sessionId)}/deliberations`, deliberationId);
}
function taskGroupPath(sessionId, groupId) {
  return resourcePath(`${sessionPath(sessionId)}/task-groups`, groupId);
}
class RunsService extends ServiceBase {
  constructor(transport, interventions2) {
    super(transport);
    __publicField(this, "interventions");
    this.interventions = interventions2;
  }
  async start(sessionId, request, signal) {
    const result = await this.transport.requestJSON("POST", runsPath(sessionId), request, signal ? { signal } : {});
    return new RunHandle(this, this.interventions, result.run, Object.freeze([...result.events ?? []]), result.created);
  }
  get(sessionId, runId, signal) {
    return this.transport.requestJSON("GET", runPath(sessionId, runId), void 0, signal ? { signal } : {});
  }
  list(sessionId, signal) {
    return this.transport.requestJSON("GET", runsPath(sessionId), void 0, signal ? { signal } : {}).then((value) => value.runs);
  }
  cancel(sessionId, runId, signal) {
    return this.transport.requestJSON("POST", `${runPath(sessionId, runId)}/cancel`, {}, signal ? { signal } : {});
  }
  listEvents(sessionId, runId, afterSeq = 0, signal) {
    const path2 = withQuery(`${runPath(sessionId, runId)}/events`, { after_seq: afterSeq > 0 ? afterSeq : void 0 });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.events);
  }
  events(sessionId, runId, options = {}) {
    return streamEvents(this.transport, `${runPath(sessionId, runId)}/events/stream`, options);
  }
}
class RunHandle {
  constructor(service, interventions2, run, initialEvents = [], created = true) {
    __publicField(this, "service");
    __publicField(this, "interventions");
    __publicField(this, "run");
    __publicField(this, "initialEvents");
    __publicField(this, "created");
    this.service = service;
    this.interventions = interventions2;
    this.run = run;
    this.initialEvents = initialEvents;
    this.created = created;
  }
  events(options = {}) {
    return this.service.events(this.run.session_id, this.run.id, options);
  }
  async cancel(signal) {
    this.run = await this.service.cancel(this.run.session_id, this.run.id, signal);
    return this.run;
  }
  approve(callId, reason = "", signal) {
    return this.interventions.approve(this.run.session_id, this.run.id, callId, reason, signal);
  }
  reject(callId, reason = "", signal) {
    return this.interventions.reject(this.run.session_id, this.run.id, callId, reason, signal);
  }
  async wait(signal) {
    let lastEvent;
    let output;
    const afterSeq = Math.max((this.run.user_event_seq ?? 1) - 1, 0);
    for await (const event of this.events({ afterSeq, ...signal === void 0 ? {} : { signal } })) {
      if (effectiveTurnId(event) !== this.run.id)
        continue;
      lastEvent = event;
      if (event.type === "agent.message")
        output = event.payload;
      if (event.type !== "session.status_idle")
        continue;
      this.run = await this.service.get(this.run.session_id, this.run.id, signal);
      if (isTerminalRunStatus(this.run.status)) {
        return {
          run: this.run,
          ...lastEvent === void 0 ? {} : { lastEvent },
          ...output === void 0 ? {} : { output }
        };
      }
    }
    throw new Error("Run event stream ended before the Run reached a terminal state");
  }
}
function runsPath(sessionId) {
  return `${sessionPath(sessionId)}/runs`;
}
function runPath(sessionId, runId) {
  return resourcePath(runsPath(sessionId), runId);
}
function effectiveTurnId(event) {
  if (event.turn_id)
    return event.turn_id;
  const payload2 = event.payload;
  if (payload2 && typeof payload2 === "object" && "turn_id" in payload2 && typeof payload2.turn_id === "string")
    return payload2.turn_id;
  return "";
}
function isTerminalRunStatus(status) {
  return status === "completed" || status === "failed" || status === "interrupted";
}
class TracesService extends ServiceBase {
  list(query = {}, signal) {
    const path2 = withQuery("/v2/traces", {
      workspace_id: query.workspaceId,
      session_id: query.sessionId,
      turn_id: query.turnId,
      session_status: query.sessionStatus,
      include_archived: query.includeArchived || void 0,
      limit: query.limit,
      cursor: query.cursor
    });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {});
  }
  get(traceId, signal) {
    return this.transport.requestJSON("GET", tracePath(traceId), void 0, signal ? { signal } : {});
  }
  listSpans(query = {}, signal) {
    const path2 = withQuery("/v2/spans", {
      workspace_id: query.workspaceId,
      trace_id: query.traceId,
      session_id: query.sessionId,
      turn_id: query.turnId,
      kind: query.kind,
      status: query.status,
      q: query.search,
      critical: query.critical,
      min_duration_ms: query.minDurationMs,
      max_duration_ms: query.maxDurationMs,
      min_self_duration_ms: query.minSelfDurationMs,
      include_archived: query.includeArchived || void 0,
      limit: query.limit,
      cursor: query.cursor
    });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {});
  }
  getSpan(traceId, spanId, signal) {
    return this.transport.requestJSON("GET", resourcePath(`${tracePath(traceId)}/spans`, spanId), void 0, signal ? { signal } : {});
  }
  getSession(sessionId, turnId, signal) {
    const path2 = withQuery(`${sessionPath(sessionId)}/trace`, { turn_id: turnId });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {});
  }
  exportSession(sessionId, format, turnId, signal) {
    const path2 = withQuery(`${sessionPath(sessionId)}/trace`, { turn_id: turnId, format });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {});
  }
}
function tracePath(traceId) {
  return resourcePath("/v2/traces", traceId);
}
class WorkersService extends ServiceBase {
  list(query = {}, signal) {
    const path2 = withQuery("/v2/workers", { workspace_id: query.workspaceId, status: query.status });
    return this.transport.requestJSON("GET", path2, void 0, signal ? { signal } : {}).then((value) => value.workers);
  }
  get(workerId, signal) {
    return this.transport.requestJSON("GET", workerPath(workerId), void 0, signal ? { signal } : {});
  }
  archive(workerId, signal) {
    return this.transport.requestJSON("POST", `${workerPath(workerId)}/archive`, {}, signal ? { signal } : {});
  }
  reapExpired(request = {}, signal) {
    return this.transport.requestJSON("POST", "/v2/workers/reap-expired", request, signal ? { signal } : {});
  }
  diagnose(request, signal) {
    return this.transport.requestJSON("POST", "/v2/workers/diagnose", request, signal ? { signal } : {});
  }
}
class WorkerWorkService extends ServiceBase {
  enqueue(request, signal) {
    return this.transport.requestJSON("POST", "/v2/worker-work", request, signal ? { signal } : {});
  }
  get(workId, signal) {
    return this.transport.requestJSON("GET", workPath(workId), void 0, signal ? { signal } : {});
  }
  reapExpired(request = {}, signal) {
    return this.transport.requestJSON("POST", "/v2/worker-work/reap-expired", request, signal ? { signal } : {});
  }
  cancel(workId, request = {}, signal) {
    return this.transport.requestJSON("POST", `${workPath(workId)}/cancel`, request, signal ? { signal } : {});
  }
  requeue(workId, request = {}, signal) {
    return this.transport.requestJSON("POST", `${workPath(workId)}/requeue`, request, signal ? { signal } : {});
  }
  diagnose(workId, signal) {
    return this.transport.requestJSON("GET", `${workPath(workId)}/diagnose`, void 0, signal ? { signal } : {});
  }
}
function workerPath(workerId) {
  return resourcePath("/v2/workers", workerId);
}
function workPath(workId) {
  return resourcePath("/v2/worker-work", workId);
}
class TMAClient {
  constructor(baseURL2, options = {}) {
    __publicField(this, "raw");
    __publicField(this, "auth");
    __publicField(this, "agents");
    __publicField(this, "environments");
    __publicField(this, "sessions");
    __publicField(this, "runs");
    __publicField(this, "interventions");
    __publicField(this, "artifacts");
    __publicField(this, "traces");
    __publicField(this, "orchestration");
    __publicField(this, "llm");
    __publicField(this, "objectRefs");
    __publicField(this, "workers");
    __publicField(this, "workerWork");
    __publicField(this, "mcp");
    __publicField(this, "observability");
    __publicField(this, "audit");
    __publicField(this, "environmentVariables");
    __publicField(this, "skills");
    __publicField(this, "marketplace");
    const transport = new Transport(baseURL2, options);
    this.raw = createLowLevelClient(transport.baseURL, transport.fetch);
    this.auth = new AuthService(transport);
    this.agents = new AgentsService(transport);
    this.environments = new EnvironmentsService(transport);
    this.sessions = new SessionsService(transport);
    this.interventions = new InterventionsService(transport);
    this.runs = new RunsService(transport, this.interventions);
    this.artifacts = new ArtifactsService(transport);
    this.traces = new TracesService(transport);
    this.orchestration = new OrchestrationService(transport);
    this.llm = new LLMService(transport);
    this.objectRefs = new ObjectRefsService(transport);
    this.workers = new WorkersService(transport);
    this.workerWork = new WorkerWorkService(transport);
    this.mcp = new MCPService(transport);
    this.observability = new ObservabilityService(transport);
    this.audit = new AuditService(transport);
    this.environmentVariables = new EnvironmentVariablesService(transport);
    this.skills = new SkillsService(transport);
    this.marketplace = new MarketplaceService(transport);
  }
}
const baseURL = ((_a = globalThis.location) == null ? void 0 : _a.origin) || "http://localhost";
const coreSDK = new TMAClient(baseURL, {
  fetch: (input, init) => globalThis.fetch(input, init)
});
async function getJSON(path2) {
  const response = await fetch(path2);
  if (!response.ok) throw new Error(await responseErrorMessage(response));
  return response.json();
}
function currentPrincipal() {
  return coreSDK.auth.me();
}
async function responseErrorMessage(response) {
  const text2 = await response.text();
  try {
    const payload2 = JSON.parse(text2);
    return (payload2 == null ? void 0 : payload2.error) || text2 || `HTTP ${response.status}`;
  } catch {
    return text2 || `HTTP ${response.status}`;
  }
}
function createAgent(body, options = {}) {
  return coreSDK.agents.create(body, options.signal);
}
async function agents() {
  return { agents: await coreSDK.agents.list() };
}
function updateAgent(agentId, body, options = {}) {
  return coreSDK.agents.update(agentId, body || {}, options.signal);
}
function defaultAgent() {
  return coreSDK.agents.default();
}
function agent(agentId) {
  return coreSDK.agents.get(agentId);
}
async function agentConfigVersions(agentId) {
  return { config_versions: await coreSDK.agents.listConfigVersions(agentId) };
}
async function exportAgent(agentId, options = {}) {
  const document2 = await coreSDK.agents.export(agentId, options.signal);
  return new Response(JSON.stringify(document2, null, 2), {
    headers: {
      "Content-Type": "application/json",
      "Content-Disposition": `attachment; filename="agent-${agentId}.json"`
    }
  });
}
function importAgent(document2, options = {}) {
  return coreSDK.agents.import(document2, options.signal);
}
function rollbackAgentConfigVersion(agentId, version2, options = {}) {
  return coreSDK.agents.rollback(agentId, version2, options.signal);
}
function agentToolingHealth(agentId, body = {}, options = {}) {
  return coreSDK.agents.toolingHealth(agentId, body, options.signal);
}
async function mcpServers(workspaceId = "") {
  return { servers: await coreSDK.mcp.list(workspaceId ? { workspaceId } : {}) };
}
function mcpServerRuntimeStatus(workspaceId = "") {
  return coreSDK.mcp.runtimeStatus(workspaceId ? { workspaceId } : {});
}
function createMCPServer(body, options = {}) {
  return coreSDK.mcp.create(body, options.signal);
}
function updateMCPServer(serverId, body, options = {}) {
  return coreSDK.mcp.update(serverId, body, options.signal);
}
function setMCPServerEnabled(serverId, enabled, options = {}) {
  return coreSDK.mcp.setEnabled(serverId, enabled, options.signal);
}
function testMCPServer(serverId, options = {}) {
  return coreSDK.mcp.test(serverId, options.signal);
}
function deleteMCPServer(serverId, options = {}) {
  return coreSDK.mcp.archive(serverId, options.signal);
}
async function mcpServerVersions(serverId) {
  return { versions: await coreSDK.mcp.versions(serverId) };
}
function restoreMCPServerVersion(serverId, version2, options = {}) {
  return coreSDK.mcp.restoreVersion(serverId, version2, options.signal);
}
function createEnvironment(body, options = {}) {
  return coreSDK.environments.create(body, options.signal);
}
async function environmentVariables(workspaceId = "") {
  return { variables: await coreSDK.environmentVariables.list(workspaceId ? { workspaceId } : {}) };
}
function putEnvironmentVariable(name2, value, workspaceId = "", options = {}) {
  return coreSDK.environmentVariables.put(name2, { value }, workspaceId ? { workspaceId } : {}, options.signal);
}
function deleteEnvironmentVariable(name2, workspaceId = "", options = {}) {
  return coreSDK.environmentVariables.delete(name2, workspaceId ? { workspaceId } : {}, options.signal);
}
function createSession(body, options = {}) {
  return coreSDK.sessions.create(body, options.signal);
}
async function sessions(filters = {}) {
  const items = await coreSDK.sessions.list({
    limit: filters.limit || 30,
    ...filters.workspace ? { workspaceId: filters.workspace } : {},
    ...filters.status ? { status: filters.status } : {},
    ...filters.includeArchived ? { includeArchived: true } : {}
  });
  return { sessions: items };
}
function appendQueuedSessionMessage(sessionId, text2, options) {
  return coreSDK.sessions.appendEvents(sessionId, {
    prefer_latest: Boolean(options.preferLatest),
    events: [{
      type: "user.message",
      payload: {
        content: [{ type: "text", text: text2 }],
        attachments: options.attachments || []
      }
    }]
  }, options.signal);
}
async function sendSessionMessage(sessionId, text2, options = {}) {
  if (options.queued) return appendQueuedSessionMessage(sessionId, text2, options);
  try {
    const handle2 = await coreSDK.runs.start(sessionId, {
      input: {
        content: [{ type: "text", text: text2 }],
        attachments: options.attachments || []
      },
      ...options.idempotencyKey ? { idempotency_key: options.idempotencyKey } : {}
    }, options.signal);
    return { run: handle2.run, events: [...handle2.initialEvents], created: handle2.created };
  } catch (error) {
    if (options.queueIfBusy && (error == null ? void 0 : error.code) === "session_busy") {
      return appendQueuedSessionMessage(sessionId, text2, options);
    }
    throw error;
  }
}
async function uploadSessionArtifact(sessionId, file, options = {}) {
  return coreSDK.artifacts.upload(sessionId, {
    name: file.name,
    artifact_type: "file",
    description: options.description || "Uploaded by the user",
    metadata: JSON.stringify({ source: "user_upload" })
  }, {
    body: file,
    filename: file.name,
    ...file.type ? { contentType: file.type } : {}
  }, options.signal);
}
async function interruptSession(sessionId, options = {}) {
  const runs = await coreSDK.runs.list(sessionId, options.signal);
  const activeRun = [...runs].reverse().find((run2) => run2.status === "running" || run2.status === "waiting_approval");
  if (!activeRun) throw new Error("session has no active Run to interrupt");
  const run = await coreSDK.runs.cancel(sessionId, activeRun.id, options.signal);
  return { run, events: [] };
}
function archiveSession(sessionId, options = {}) {
  return coreSDK.sessions.archive(sessionId, options.signal);
}
function restoreSession(sessionId, options = {}) {
  return coreSDK.sessions.restore(sessionId, options.signal);
}
function rerunSession(sessionId, body = {}, options = {}) {
  return coreSDK.sessions.rerun(sessionId, body, options.signal);
}
function compareSessions(leftSessionId, rightSessionId, options = {}) {
  return coreSDK.sessions.compare(leftSessionId, rightSessionId, options.signal);
}
function deleteSession(sessionId, options = {}) {
  return coreSDK.sessions.delete(sessionId, options.signal);
}
function session(sessionId) {
  return coreSDK.sessions.get(sessionId);
}
function upgradeSessionConfig(sessionId, body, options = {}) {
  return coreSDK.sessions.upgradeConfig(sessionId, body, options.signal);
}
function updateSessionMetadata(sessionId, body, options = {}) {
  return coreSDK.sessions.updateMetadata(sessionId, body, options.signal);
}
function sessionRuntimeConfig(sessionId) {
  return coreSDK.sessions.runtimeConfig(sessionId);
}
function sessionRuntimeCapabilities(sessionId) {
  return coreSDK.sessions.runtimeCapabilities(sessionId);
}
async function skills(filters = {}) {
  return {
    skills: await coreSDK.skills.list({
      ...filters.workspaceId ? { workspaceId: filters.workspaceId } : {},
      ...filters.includeArchived ? { includeArchived: true } : {}
    })
  };
}
async function skillVersions(skillId) {
  return { versions: await coreSDK.skills.listVersions(skillId) };
}
function archiveSkill(skillId, options = {}) {
  return coreSDK.skills.archive(skillId, options.signal);
}
function discoverSkillsMarketplace(filters = {}, options = {}) {
  return coreSDK.marketplace.discover({
    sessionId: filters.sessionId || "",
    ...filters.query ? { query: filters.query } : {},
    ...filters.repository ? { repository: filters.repository } : {},
    ...filters.limit ? { limit: filters.limit } : {}
  }, options.signal);
}
function previewSkillsMarketplace(body, options = {}) {
  return coreSDK.marketplace.preview(body, options.signal);
}
function installSkillsMarketplace(body, options = {}) {
  return coreSDK.marketplace.install(body, options.signal);
}
function discoverInternalSkillsMarketplace(filters = {}, options = {}) {
  var _a2;
  return coreSDK.marketplace.browseInternal({
    sessionId: filters.sessionId || "",
    ...filters.query ? { query: filters.query } : {},
    ...filters.category ? { category: filters.category } : {},
    ...((_a2 = filters.tags) == null ? void 0 : _a2.length) ? { tags: filters.tags } : {},
    ...filters.limit ? { limit: filters.limit } : {}
  }, options.signal);
}
function previewInternalSkillsMarketplace(body, options = {}) {
  return coreSDK.marketplace.previewInternal(body, options.signal);
}
function installInternalSkillsMarketplace(body, options = {}) {
  return coreSDK.marketplace.installInternal(body, options.signal);
}
function enableSkill(skillId, body, options = {}) {
  return coreSDK.marketplace.enableInstalled(skillId, body, options.signal);
}
function disableSkill(skillId, body, options = {}) {
  return coreSDK.marketplace.disableInstalled(skillId, body, options.signal);
}
async function marketplacePolicies(filters = {}) {
  return {
    policies: await coreSDK.marketplace.listPolicies({
      ...filters.organizationId ? { organizationId: filters.organizationId } : {},
      ...filters.workspaceId ? { workspaceId: filters.workspaceId } : {},
      ...filters.includeArchived ? { includeArchived: true } : {}
    })
  };
}
function marketplacePolicy(policyId) {
  return coreSDK.marketplace.getPolicy(policyId);
}
function createMarketplacePolicy(body, options = {}) {
  return coreSDK.marketplace.createPolicy(body, options.signal);
}
function publishMarketplacePolicyVersion(policyId, config, options = {}) {
  return coreSDK.marketplace.publishPolicyVersion(policyId, { config }, options.signal);
}
function archiveMarketplacePolicy(policyId, options = {}) {
  return coreSDK.marketplace.archivePolicy(policyId, options.signal);
}
async function marketplaceEntries(filters = {}) {
  return {
    entries: await coreSDK.marketplace.listEntries({
      ...filters.workspaceId ? { workspaceId: filters.workspaceId } : {},
      ...filters.status ? { status: filters.status } : {},
      ...filters.includeWithdrawn ? { includeWithdrawn: true } : {}
    })
  };
}
function createMarketplaceEntry(body, options = {}) {
  return coreSDK.marketplace.createEntry(body, options.signal);
}
function updateMarketplaceEntry(entryId, body, options = {}) {
  return coreSDK.marketplace.updateEntry(entryId, body, options.signal);
}
function transitionMarketplaceEntry(entryId, action, body = {}, options = {}) {
  if (action === "submit") return coreSDK.marketplace.submitEntry(entryId, body, options.signal);
  if (action === "publish") return coreSDK.marketplace.publishEntry(entryId, body, options.signal);
  if (action === "withdraw") return coreSDK.marketplace.withdrawEntry(entryId, body, options.signal);
  throw new TypeError(`unsupported Marketplace entry transition: ${action}`);
}
function skillAssetRetentionEffective(workspaceId, options = {}) {
  return coreSDK.skills.effectiveRetentionPolicy(workspaceId, options.signal);
}
async function skillAssetRetentionPolicies(filters = {}, options = {}) {
  return {
    policies: await coreSDK.skills.listRetentionPolicies({
      ...filters.organizationId ? { organizationId: filters.organizationId } : {},
      ...filters.workspaceId ? { workspaceId: filters.workspaceId } : {},
      ...filters.includeArchived ? { includeArchived: true } : {}
    }, options.signal)
  };
}
function createSkillAssetRetentionPolicy(body, options = {}) {
  return coreSDK.skills.createRetentionPolicy(body, options.signal);
}
function publishSkillAssetRetentionPolicyVersion(policyId, config, options = {}) {
  return coreSDK.skills.publishRetentionPolicyVersion(policyId, { config }, options.signal);
}
function archiveSkillAssetRetentionPolicy(policyId, options = {}) {
  return coreSDK.skills.archiveRetentionPolicy(policyId, options.signal);
}
function previewSkillAssetGC(body, options = {}) {
  return coreSDK.skills.previewAssetGC(body, options.signal);
}
function runSkillAssetGC(body, options = {}) {
  return coreSDK.skills.runAssetGC(body, options.signal);
}
async function skillAssetGCRuns(workspaceId, limit = 20, options = {}) {
  return { runs: await coreSDK.skills.listAssetGCRuns({ workspaceId, limit }, options.signal) };
}
async function skillAssetGCTombstones(workspaceId, limit = 20, options = {}) {
  return { tombstones: await coreSDK.skills.listAssetGCTombstones({ workspaceId, limit }, options.signal) };
}
function updateSessionRuntimeSettings(sessionId, body, options = {}) {
  return coreSDK.sessions.updateRuntimeSettings(sessionId, body || {}, options.signal);
}
async function llmProviders() {
  return { providers: await coreSDK.llm.listProviders() };
}
function createLLMProvider(body, options = {}) {
  return coreSDK.llm.createProvider(body, options.signal);
}
function updateLLMProvider(providerId, revision, body, options = {}) {
  return coreSDK.llm.updateProvider(providerId, revision, body || {}, options.signal);
}
function setLLMProviderEnabled(providerId, revision, enabled, options = {}) {
  return coreSDK.llm.setProviderEnabled(providerId, revision, enabled, options.signal);
}
function deleteLLMProvider(providerId, revision, options = {}) {
  return coreSDK.llm.deleteProvider(providerId, revision, options.signal);
}
async function testLLMProvider(providerId) {
  return coreSDK.llm.testProvider(providerId);
}
async function llmModels(providerId) {
  return { models: await coreSDK.llm.listModels(providerId) };
}
function upsertLLMModel(body, revision, options = {}) {
  return revision == null ? coreSDK.llm.createModel(body || {}, options.signal) : coreSDK.llm.updateModel(revision, body || {}, options.signal);
}
function deleteLLMModel(providerId, model, revision, options = {}) {
  return coreSDK.llm.deleteModel(providerId, model, revision, options.signal);
}
async function testLLMModel(providerId, model) {
  return coreSDK.llm.testModel(providerId, model);
}
async function artifacts(sessionId) {
  return { artifacts: await coreSDK.artifacts.list(sessionId) };
}
function downloadArtifact(sessionId, artifactId, options = {}) {
  return coreSDK.artifacts.download(sessionId, artifactId, options.signal);
}
function artifactDownloadPath(sessionId, artifactId) {
  return `/v2/sessions/${encodeURIComponent(sessionId)}/artifacts/${encodeURIComponent(artifactId)}/download`;
}
function objectRefDownloadPath(objectRefId, sessionId) {
  const query = sessionId ? `?session_id=${encodeURIComponent(sessionId)}` : "";
  return `/v2/object-refs/${encodeURIComponent(objectRefId)}/download${query}`;
}
function skillPackageDownloadPath(skillId, version2) {
  return `/v2/skills/${encodeURIComponent(skillId)}/versions/${encodeURIComponent(version2)}/package`;
}
async function events(sessionId) {
  return { events: await coreSDK.sessions.listEvents(sessionId) };
}
function streamSessionEvents(sessionId, options = {}) {
  return coreSDK.sessions.events(sessionId, options);
}
async function interventions(sessionId, status) {
  return { interventions: await coreSDK.interventions.list(sessionId, status) };
}
function taskTemplates() {
  return getJSON("/v1/task-templates");
}
function approveIntervention(sessionId, turnId, callId, body = {}, options = {}) {
  return coreSDK.interventions.approve(sessionId, turnId, callId, body.reason || "", options.signal);
}
function rejectIntervention(sessionId, turnId, callId, body = {}, options = {}) {
  return coreSDK.interventions.reject(sessionId, turnId, callId, body.reason || "", options.signal);
}
const hasOwn = (value, key) => Object.prototype.hasOwnProperty.call(value || {}, key);
class SkillInputsValidationError extends Error {
  constructor(fields) {
    super("请检查 Skill 参数。");
    this.name = "SkillInputsValidationError";
    this.fields = fields;
  }
}
function parseObject(value) {
  if (!value) return null;
  if (typeof value === "string") {
    try {
      return parseObject(JSON.parse(value));
    } catch {
      return null;
    }
  }
  return typeof value === "object" && !Array.isArray(value) ? value : null;
}
function inputSchemaFromVersion(version2) {
  const manifest2 = parseObject(version2 == null ? void 0 : version2.manifest);
  return parseObject(manifest2 == null ? void 0 : manifest2.inputs_schema);
}
function skillBindingsFromConfig(raw) {
  let source = raw;
  if (typeof source === "string") {
    try {
      source = JSON.parse(source);
    } catch {
      return [];
    }
  }
  if (!source || typeof source !== "object" || Array.isArray(source) || !Array.isArray(source.enabled)) return [];
  return source.enabled.flatMap((item) => {
    if (!item || typeof item !== "object" || Array.isArray(item)) return [];
    const skill = String(item.skill || "").trim();
    const version2 = Number(item.version || 0);
    if (!skill || !Number.isInteger(version2) || version2 <= 0) return [];
    const priority = Number(item.priority);
    return [{
      skill,
      version: version2,
      mode: String(item.mode || "summary").trim() || "summary",
      priority: Number.isInteger(priority) && priority !== 0 ? priority : 100,
      inputs: parseObject(item.inputs) || {}
    }];
  });
}
function canonicalJSON(value) {
  if (Array.isArray(value)) return `[${value.map(canonicalJSON).join(",")}]`;
  if (value && typeof value === "object") {
    return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${canonicalJSON(value[key])}`).join(",")}}`;
  }
  return JSON.stringify(value);
}
function skillBindingsMatch(left, right) {
  if (!left || !right) return left === right;
  return left.skill === right.skill && left.version === right.version && left.mode === right.mode && left.priority === right.priority && canonicalJSON(left.inputs || {}) === canonicalJSON(right.inputs || {});
}
function skillBindingState(agentBinding, sessionBinding) {
  const synced = skillBindingsMatch(agentBinding, sessionBinding);
  return {
    synced,
    pendingApply: Boolean(agentBinding) && !synced,
    sessionStillEnabled: !agentBinding && Boolean(sessionBinding)
  };
}
function skillConfigSyncState(agentConfigVersion, sessionConfigVersion, hasSession = true) {
  const agentVersion = Number(agentConfigVersion || 0);
  const sessionVersion = Number(sessionConfigVersion || 0);
  const needsApply = Boolean(hasSession) && agentVersion > sessionVersion;
  return {
    agentVersion,
    sessionVersion,
    needsApply,
    targetVersion: needsApply ? agentVersion : 0
  };
}
function resolveLocalSchema(root2, schema, seen = /* @__PURE__ */ new Set()) {
  if (!schema || typeof schema !== "object" || Array.isArray(schema) || typeof schema.$ref !== "string") return schema;
  if (!schema.$ref.startsWith("#/") || seen.has(schema.$ref)) return schema;
  let resolved = root2;
  for (const token of schema.$ref.slice(2).split("/")) {
    const key = token.replace(/~1/g, "/").replace(/~0/g, "~");
    resolved = resolved == null ? void 0 : resolved[key];
  }
  if (!resolved || typeof resolved !== "object" || Array.isArray(resolved)) return schema;
  const nextSeen = new Set(seen);
  nextSeen.add(schema.$ref);
  return { ...resolveLocalSchema(root2, resolved, nextSeen), ...schema, $ref: void 0 };
}
function schemaType(schema) {
  if (typeof (schema == null ? void 0 : schema.type) === "string") return schema.type;
  if (Array.isArray(schema == null ? void 0 : schema.type)) return schema.type.find((value) => value !== "null") || "json";
  if (Array.isArray(schema == null ? void 0 : schema.enum) && schema.enum.length) {
    const value = schema.enum.find((item) => item !== null);
    if (Array.isArray(value)) return "array";
    if (value !== null && typeof value === "object") return "object";
    return typeof value;
  }
  return "json";
}
function enumToken(value) {
  return JSON.stringify(value);
}
function enumLabel(value) {
  if (typeof value === "string") return value;
  if (value === null) return "null";
  return JSON.stringify(value);
}
function fieldControl(schema, type) {
  if (Array.isArray(schema.enum)) return "enum";
  if (type === "boolean") return "boolean";
  if (type === "integer" || type === "number") return "number";
  if (type === "object" || type === "array" || type === "json") return "json";
  if (schema["x-tma-control"] === "textarea") return "textarea";
  return "text";
}
function schemaFields(schema) {
  const root2 = parseObject(schema);
  if (!root2) return [];
  const properties = parseObject(root2.properties) || {};
  const required = new Set(Array.isArray(root2.required) ? root2.required : []);
  return Object.entries(properties).map(([name2, unresolved]) => {
    const property = resolveLocalSchema(root2, parseObject(unresolved) || {});
    const type = schemaType(property);
    return {
      name: name2,
      title: typeof property.title === "string" && property.title.trim() ? property.title : name2,
      description: typeof property.description === "string" ? property.description : "",
      required: required.has(name2),
      type,
      control: fieldControl(property, type),
      defaultValue: hasOwn(property, "default") ? property.default : void 0,
      options: Array.isArray(property.enum) ? property.enum.map((value) => ({ value, token: enumToken(value), label: enumLabel(value) })) : [],
      minimum: typeof property.minimum === "number" ? property.minimum : void 0,
      maximum: typeof property.maximum === "number" ? property.maximum : void 0,
      minLength: Number.isInteger(property.minLength) ? property.minLength : void 0,
      maxLength: Number.isInteger(property.maxLength) ? property.maxLength : void 0
    };
  });
}
function valueForControl(field, value) {
  if (field.control === "enum") {
    const token = enumToken(value);
    return field.options.some((option) => option.token === token) ? token : void 0;
  }
  if (field.control === "boolean") return typeof value === "boolean" ? value : void 0;
  if (field.control === "json") return JSON.stringify(value, null, 2);
  return value === null || value === void 0 ? void 0 : String(value);
}
function initialSkillInputValues(schema, inputs) {
  const inputValues = parseObject(inputs);
  const entries = [];
  for (const field of schemaFields(schema)) {
    let value;
    if (inputValues && hasOwn(inputValues, field.name)) value = valueForControl(field, inputValues[field.name]);
    else if (field.defaultValue !== void 0) value = valueForControl(field, field.defaultValue);
    else if (field.required && field.control === "boolean") value = false;
    if (value !== void 0) entries.push([field.name, value]);
  }
  return Object.fromEntries(entries);
}
function isBlank(value) {
  return value === void 0 || value === null || typeof value === "string" && value.trim() === "";
}
function validateJSONType(field, value) {
  if (field.type === "array" && !Array.isArray(value)) return "必须是 JSON 数组";
  if (field.type === "object" && (!value || typeof value !== "object" || Array.isArray(value))) return "必须是 JSON 对象";
  return "";
}
function buildSkillInputs(schema, values = {}) {
  const inputs = /* @__PURE__ */ Object.create(null);
  const errors = {};
  for (const field of schemaFields(schema)) {
    const present = hasOwn(values, field.name) && !isBlank(values[field.name]);
    if (!present) {
      if (field.required) errors[field.name] = "此参数为必填项";
      continue;
    }
    const raw = values[field.name];
    if (field.control === "enum") {
      const option = field.options.find((item) => item.token === raw);
      if (!option) errors[field.name] = "请选择有效选项";
      else inputs[field.name] = option.value;
      continue;
    }
    if (field.control === "boolean") {
      if (typeof raw !== "boolean") errors[field.name] = "必须是布尔值";
      else inputs[field.name] = raw;
      continue;
    }
    if (field.control === "number") {
      const parsed2 = Number(raw);
      if (!Number.isFinite(parsed2) || field.type === "integer" && !Number.isInteger(parsed2)) {
        errors[field.name] = field.type === "integer" ? "必须是整数" : "必须是数字";
      } else if (field.minimum !== void 0 && parsed2 < field.minimum) {
        errors[field.name] = `不能小于 ${field.minimum}`;
      } else if (field.maximum !== void 0 && parsed2 > field.maximum) {
        errors[field.name] = `不能大于 ${field.maximum}`;
      } else {
        inputs[field.name] = parsed2;
      }
      continue;
    }
    if (field.control === "json") {
      try {
        const parsed2 = typeof raw === "string" ? JSON.parse(raw) : raw;
        const typeError = validateJSONType(field, parsed2);
        if (typeError) errors[field.name] = typeError;
        else inputs[field.name] = parsed2;
      } catch {
        errors[field.name] = "请输入有效 JSON";
      }
      continue;
    }
    const parsed = String(raw);
    if (field.minLength !== void 0 && parsed.length < field.minLength) errors[field.name] = `至少需要 ${field.minLength} 个字符`;
    else if (field.maxLength !== void 0 && parsed.length > field.maxLength) errors[field.name] = `最多允许 ${field.maxLength} 个字符`;
    else inputs[field.name] = parsed;
  }
  if (Object.keys(errors).length) throw new SkillInputsValidationError(errors);
  return inputs;
}
const marketplaceEntryStages = [
  { status: "draft", label: "草稿" },
  { status: "pending_review", label: "待审核" },
  { status: "published", label: "已发布" },
  { status: "withdrawn", label: "已下架" }
];
function marketplaceEntryStatusLabel(status) {
  var _a2;
  return ((_a2 = marketplaceEntryStages.find((stage) => stage.status === status)) == null ? void 0 : _a2.label) || status || "未知";
}
function marketplaceEntryStatusTone(status) {
  switch (status) {
    case "published":
      return "ok";
    case "pending_review":
      return "warn";
    case "withdrawn":
      return "danger";
    default:
      return "neutral";
  }
}
function marketplaceEntryNextAction(status) {
  switch (status) {
    case "draft":
      return { action: "submit", label: "提交审核", targetStatus: "pending_review" };
    case "pending_review":
      return { action: "publish", label: "审核并发布", targetStatus: "published" };
    case "published":
      return { action: "withdraw", label: "下架", targetStatus: "withdrawn" };
    default:
      return null;
  }
}
function marketplaceEntryStageState(currentStatus, stageStatus) {
  const currentIndex = marketplaceEntryStages.findIndex((stage) => stage.status === currentStatus);
  const stageIndex = marketplaceEntryStages.findIndex((stage) => stage.status === stageStatus);
  if (currentIndex < 0 || stageIndex < 0) return "upcoming";
  if (stageIndex === currentIndex) return "current";
  return stageIndex < currentIndex ? "complete" : "upcoming";
}
function marketplaceInstallStateMeta(state) {
  switch (state) {
    case "new_install":
      return { label: "可安装", tone: "ok", actionLabel: "安全预览" };
    case "upgrade":
      return { label: "有新版本", tone: "warn", actionLabel: "查看更新" };
    case "unchanged":
      return { label: "已安装", tone: "neutral", actionLabel: "查看详情" };
    case "blocked":
      return { label: "不可安装", tone: "danger", actionLabel: "查看原因" };
    default:
      return { label: "内部已发布", tone: "ok", actionLabel: "安全预览" };
  }
}
function marketplaceUpgradeVersions(preview) {
  var _a2;
  if ((preview == null ? void 0 : preview.install_state) !== "upgrade") return null;
  const current = Number(((_a2 = preview == null ? void 0 : preview.existing) == null ? void 0 : _a2.version) || 0);
  if (!Number.isInteger(current) || current < 1) return null;
  return { current, target: current + 1 };
}
const emptyPolicyForm = {
  scopeType: "workspace",
  organizationID: "org_default",
  allowedOwners: "",
  allowedRepositories: "",
  allowedLicenses: "",
  deniedLicenses: "",
  requireCommitSHA: false,
  requireLicense: false,
  requireAttestation: false,
  staticScanBlockSeverity: "",
  trustedAttestationKeys: "{}"
};
const emptyMarketplaceEntryForm = {
  skillID: "",
  skillVersion: "",
  summary: "",
  category: "",
  tags: ""
};
const policyCheckLabels = {
  repository_allowlist: "仓库来源",
  commit_ref_pin: "Commit 固定",
  license: "许可证",
  attestation: "包签名",
  static_scan: "静态扫描",
  binary_scan: "二进制扫描"
};
function splitValues(value) {
  return String(value || "").split(/[\n,]/).map((item) => item.trim()).filter(Boolean);
}
function joinValues(values) {
  return Array.isArray(values) ? values.join("\n") : "";
}
function shortHash(value, size = 12) {
  const text2 = String(value || "");
  return text2.length > size ? `${text2.slice(0, size)}...` : text2 || "-";
}
function formatDate$1(value) {
  if (!value) return "-";
  const date = new Date(value);
  return Number.isNaN(date.getTime()) ? "-" : date.toLocaleString("zh-CN", { hour12: false });
}
function formatBytes(value) {
  const bytes = Number(value || 0);
  if (!Number.isFinite(bytes) || bytes <= 0) return "0 B";
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KiB`;
  return `${(bytes / (1024 * 1024)).toFixed(2)} MiB`;
}
function assetBundleFromVersion(version2) {
  const value = version2 == null ? void 0 : version2.assets;
  if (!value) return { files: [], sbom: {} };
  if (Array.isArray(value)) return { files: value, sbom: {} };
  if (typeof value === "object") return { files: value.files || [], sbom: value.sbom || {} };
  try {
    const decoded = JSON.parse(value);
    return Array.isArray(decoded) ? { files: decoded, sbom: {} } : { files: decoded.files || [], sbom: decoded.sbom || {} };
  } catch {
    return { files: [], sbom: {} };
  }
}
function StateTag({ children, tone = "neutral" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `skills-state ${tone}`, children });
}
function toneForState(value) {
  switch (String(value || "").toLowerCase()) {
    case "active":
    case "verified":
    case "new_install":
    case "upgrade":
    case "passed":
    case "succeeded":
    case "deleted":
      return "ok";
    case "blocked":
    case "invalid":
    case "critical":
    case "high":
    case "archived":
    case "failed":
      return "danger";
    case "missing":
    case "untrusted":
    case "medium":
    case "unchanged":
    case "partial":
    case "skipped":
      return "warn";
    default:
      return "neutral";
  }
}
function policyFormFromDetail(detail) {
  var _a2;
  const config = ((_a2 = detail == null ? void 0 : detail.version) == null ? void 0 : _a2.config) || {};
  const policy = (detail == null ? void 0 : detail.policy) || {};
  return {
    scopeType: policy.scope_type || "workspace",
    organizationID: policy.organization_id || "org_default",
    allowedOwners: joinValues(config.allowed_owners),
    allowedRepositories: joinValues(config.allowed_repositories),
    allowedLicenses: joinValues(config.allowed_licenses),
    deniedLicenses: joinValues(config.denied_licenses),
    requireCommitSHA: Boolean(config.require_commit_sha),
    requireLicense: Boolean(config.require_license),
    requireAttestation: Boolean(config.require_attestation),
    staticScanBlockSeverity: config.static_scan_block_severity || "",
    trustedAttestationKeys: JSON.stringify(config.trusted_attestation_keys || {}, null, 2)
  };
}
function policyConfigFromForm(form) {
  let trustedKeys = {};
  const keySource = form.trustedAttestationKeys.trim();
  if (keySource) {
    trustedKeys = JSON.parse(keySource);
    if (!trustedKeys || Array.isArray(trustedKeys) || typeof trustedKeys !== "object") {
      throw new Error("Attestation 公钥必须是 key ID 到 Base64 公钥的 JSON 对象。");
    }
  }
  return {
    allowed_owners: splitValues(form.allowedOwners),
    allowed_repositories: splitValues(form.allowedRepositories),
    require_commit_sha: form.requireCommitSHA,
    allowed_licenses: splitValues(form.allowedLicenses),
    denied_licenses: splitValues(form.deniedLicenses),
    require_license: form.requireLicense,
    require_attestation: form.requireAttestation,
    trusted_attestation_keys: trustedKeys,
    static_scan_block_severity: form.staticScanBlockSeverity
  };
}
function SkillInputField({ error, field, index: index2, onChange, onClear, value }) {
  const id2 = `skill-input-${index2}`;
  const label = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    field.title,
    field.required ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "skill-input-required", "aria-label": "必填", children: "*" }) : null
  ] });
  if (field.control === "boolean") {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skill-input-field", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "skill-input-toggle", htmlFor: id2, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { id: id2, type: "checkbox", checked: value === true, onChange: (event) => onChange(event.target.checked) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: label }),
          field.description ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: field.description }) : null
        ] })
      ] }),
      !field.required && value !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "skill-input-clear", type: "button", onClick: onClear, children: "清除" }) : null,
      error ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { className: "skill-input-error", role: "alert", children: error }) : null
    ] });
  }
  let control;
  if (field.control === "enum") {
    control = /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { id: id2, value: value ?? "", onChange: (event) => onChange(event.target.value), "aria-invalid": Boolean(error), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "请选择" }),
      field.options.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: option.token, children: option.label }, option.token))
    ] });
  } else if (field.control === "number") {
    control = /* @__PURE__ */ jsxRuntimeExports.jsx("input", { id: id2, type: "number", value: value ?? "", min: field.minimum, max: field.maximum, step: field.type === "integer" ? 1 : "any", onChange: (event) => onChange(event.target.value), "aria-invalid": Boolean(error) });
  } else if (field.control === "textarea" || field.control === "json") {
    control = /* @__PURE__ */ jsxRuntimeExports.jsx("textarea", { id: id2, className: field.control === "json" ? "code-input" : "", rows: field.control === "json" ? 4 : 3, value: value ?? "", minLength: field.minLength, maxLength: field.maxLength, spellCheck: field.control !== "json", onChange: (event) => onChange(event.target.value), "aria-invalid": Boolean(error) });
  } else {
    control = /* @__PURE__ */ jsxRuntimeExports.jsx("input", { id: id2, type: "text", value: value ?? "", minLength: field.minLength, maxLength: field.maxLength, onChange: (event) => onChange(event.target.value), "aria-invalid": Boolean(error) });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skill-input-field", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "skill-input-label", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: id2, children: /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: label }) }),
      !field.required && value !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "skill-input-clear", type: "button", onClick: onClear, children: "清除" }) : null
    ] }),
    control,
    field.description ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: field.description }) : null,
    error ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { className: "skill-input-error", role: "alert", children: error }) : null
  ] });
}
function SkillVersionEnableForm({ binding, busy, onEnable, version: version2 }) {
  const schema = inputSchemaFromVersion(version2);
  const fields = schemaFields(schema);
  const bindingKey = JSON.stringify(binding || null);
  const [values, setValues] = reactExports.useState(() => initialSkillInputValues(schema, binding == null ? void 0 : binding.inputs));
  const [mode, setMode] = reactExports.useState((binding == null ? void 0 : binding.mode) || "summary");
  const [priority, setPriority] = reactExports.useState(String((binding == null ? void 0 : binding.priority) ?? 100));
  const [errors, setErrors] = reactExports.useState({});
  reactExports.useEffect(() => {
    setValues(initialSkillInputValues(schema, binding == null ? void 0 : binding.inputs));
    setMode((binding == null ? void 0 : binding.mode) || "summary");
    setPriority(String((binding == null ? void 0 : binding.priority) ?? 100));
    setErrors({});
  }, [bindingKey, version2.id]);
  function updateValue(name2, value) {
    setValues((current) => ({ ...current, [name2]: value }));
    setErrors((current) => ({ ...current, [name2]: void 0 }));
  }
  function clearValue(name2) {
    setValues((current) => Object.fromEntries(Object.entries(current).filter(([key]) => key !== name2)));
    setErrors((current) => ({ ...current, [name2]: void 0 }));
  }
  function submit(event) {
    event.preventDefault();
    try {
      const parsedPriority = Number(priority);
      if (!Number.isInteger(parsedPriority) || parsedPriority < -1e3 || parsedPriority > 1e3) {
        setErrors((current) => ({ ...current, binding: "优先级必须是 -1000 到 1000 之间的整数" }));
        return;
      }
      const inputs = schema ? buildSkillInputs(schema, values) : (binding == null ? void 0 : binding.version) === version2.version ? binding.inputs || {} : {};
      setErrors({});
      onEnable(version2, inputs, mode, parsedPriority === 0 ? 100 : parsedPriority);
    } catch (error) {
      if (error instanceof SkillInputsValidationError) setErrors(error.fields);
      else throw error;
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { className: "skill-version-enable-form", onSubmit: submit, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skill-binding-controls", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
        "渲染模式",
        /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: mode, onChange: (event) => setMode(event.target.value), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "summary", children: "Summary（按需读取全文）" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "full", children: "Full" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "examples_only", children: "Examples only" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
        "优先级",
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "number", min: "-1000", max: "1000", step: "1", value: priority, onChange: (event) => {
          setPriority(event.target.value);
          setErrors((current) => ({ ...current, binding: void 0 }));
        } })
      ] })
    ] }),
    fields.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skill-input-grid", children: fields.map((field, index2) => /* @__PURE__ */ jsxRuntimeExports.jsx(
      SkillInputField,
      {
        error: errors[field.name],
        field,
        index: `${version2.id}-${index2}`,
        value: values[field.name],
        onChange: (value) => updateValue(field.name, value),
        onClear: () => clearValue(field.name)
      },
      field.name
    )) }) : null,
    errors.binding ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { className: "skill-input-error", role: "alert", children: errors.binding }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", disabled: busy, children: busy ? "保存中" : (binding == null ? void 0 : binding.version) === version2.version ? `保存 v${version2.version} 配置` : `启用 v${version2.version}` })
  ] });
}
function BindingDetail({ binding, label }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skill-binding-column", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label }),
    binding ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
        "v",
        binding.version,
        " · ",
        binding.mode,
        " · P",
        binding.priority
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { children: JSON.stringify(binding.inputs || {}, null, 2) })
    ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "未启用" })
  ] });
}
function InstalledSkillsView({
  agentBindings,
  agentConfigVersion,
  onApplyAgentConfig,
  onSkillsChanged,
  session: session2,
  sessionBindings,
  sessionConfigVersion,
  sessionID,
  skills: skills2
}) {
  const [query, setQuery] = reactExports.useState("");
  const [selectedSkillID, setSelectedSkillID] = reactExports.useState("");
  const [versions, setVersions] = reactExports.useState([]);
  const [busyAction, setBusyAction] = reactExports.useState("");
  const [confirmArchiveID, setConfirmArchiveID] = reactExports.useState("");
  const [confirmDisableID, setConfirmDisableID] = reactExports.useState("");
  const [message, setMessage] = reactExports.useState(null);
  const agentBindingMap = reactExports.useMemo(() => new Map(agentBindings.map((binding) => [binding.skill, binding])), [agentBindings]);
  const sessionBindingMap = reactExports.useMemo(() => new Map(sessionBindings.map((binding) => [binding.skill, binding])), [sessionBindings]);
  const configSync = skillConfigSyncState(agentConfigVersion, sessionConfigVersion, Boolean(sessionID));
  const configNeedsApply = configSync.needsApply;
  const filteredSkills = reactExports.useMemo(() => {
    const normalized = query.trim().toLowerCase();
    if (!normalized) return skills2;
    return skills2.filter((skill) => [skill.identifier, skill.title, skill.description, skill.source_locator].some((value) => String(value || "").toLowerCase().includes(normalized)));
  }, [query, skills2]);
  const selectedSkill = skills2.find((skill) => skill.id === selectedSkillID) || null;
  const selectedAgentBinding = selectedSkill ? agentBindingMap.get(selectedSkill.identifier) : null;
  const selectedSessionBinding = selectedSkill ? sessionBindingMap.get(selectedSkill.identifier) : null;
  async function inspectSkill(skill) {
    setSelectedSkillID(skill.id);
    setBusyAction(`inspect:${skill.id}`);
    setMessage(null);
    try {
      const response = await skillVersions(skill.id);
      setVersions(response.versions || []);
    } catch (error) {
      setVersions([]);
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusyAction("");
    }
  }
  async function enableSkill$1(skill) {
    if (!sessionID) {
      setMessage({ tone: "warn", text: "需要先打开一个任务会话，才能把 Skill 固定到当前 Agent 配置。" });
      return;
    }
    setBusyAction(`enable:${skill.id}`);
    setMessage(null);
    try {
      const versionsResponse = await skillVersions(skill.id);
      const availableVersions = versionsResponse.versions || [];
      setSelectedSkillID(skill.id);
      setVersions(availableVersions);
      const latest = availableVersions[0];
      if (!latest) {
        setMessage({ tone: "warn", text: `${skill.identifier} 还没有可启用的版本。` });
        return;
      }
      if (schemaFields(inputSchemaFromVersion(latest)).length) {
        setMessage({ tone: "warn", text: `请先配置 ${skill.identifier} v${latest.version} 的参数。` });
        return;
      }
      const response = await enableSkill(skill.id, { session_id: sessionID, version: latest.version, mode: "summary", priority: 100, inputs: {} });
      await onSkillsChanged();
      setMessage({
        tone: "ok",
        text: response.changed === false ? `Agent 已经使用 ${skill.identifier} v${latest.version} 的相同配置。` : response.requires_session_upgrade ? `已写入 Agent 配置版本 #${response.new_config_version}；当前任务需升级配置后生效。` : `已为当前 Agent 启用 ${skill.identifier}。`
      });
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusyAction("");
    }
  }
  async function enableSkillVersion(skill, version2, inputs, mode, priority) {
    if (!sessionID) {
      setMessage({ tone: "warn", text: "需要先打开一个任务会话，才能把 Skill 固定到当前 Agent 配置。" });
      return;
    }
    const action = `enable:${skill.id}:${version2.version}`;
    setBusyAction(action);
    setMessage(null);
    try {
      const response = await enableSkill(skill.id, {
        session_id: sessionID,
        version: version2.version,
        mode,
        priority,
        inputs
      });
      await onSkillsChanged();
      setMessage({
        tone: "ok",
        text: response.changed === false ? `${skill.identifier} v${version2.version} 的 binding 没有变化，未创建新配置版本。` : response.requires_session_upgrade ? `已写入 Agent 配置版本 #${response.new_config_version}；当前任务需升级配置后生效。` : `已为当前 Agent 启用 ${skill.identifier} v${version2.version}。`
      });
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusyAction("");
    }
  }
  async function disableSkill$1(skill) {
    if (!sessionID) {
      setMessage({ tone: "warn", text: "需要先打开一个任务会话，才能修改当前 Agent 配置。" });
      return;
    }
    const action = `disable:${skill.id}`;
    setBusyAction(action);
    setMessage(null);
    try {
      const response = await disableSkill(skill.id, { session_id: sessionID });
      setConfirmDisableID("");
      await onSkillsChanged();
      setMessage({
        tone: "ok",
        text: response.removed ? `已从 Agent 最新配置停用 ${skill.identifier}；${response.requires_session_upgrade ? "当前任务仍需应用新配置。" : "当前任务已同步。"}` : `${skill.identifier} 已经处于停用状态。`
      });
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusyAction("");
    }
  }
  async function applyAgentConfig() {
    if (!configNeedsApply || !onApplyAgentConfig) return;
    setBusyAction(`apply:${agentConfigVersion}`);
    setMessage(null);
    try {
      await onApplyAgentConfig(agentConfigVersion);
      setMessage({ tone: "ok", text: `当前任务已精确应用 Agent 配置 #${agentConfigVersion}。` });
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusyAction("");
    }
  }
  async function archiveSkill$1(skill) {
    setBusyAction(`archive:${skill.id}`);
    setMessage(null);
    try {
      await archiveSkill(skill.id);
      setConfirmArchiveID("");
      setSelectedSkillID((current) => current === skill.id ? "" : current);
      setMessage({ tone: "ok", text: `${skill.identifier} 已归档，历史版本和既有会话仍可回放。` });
      await onSkillsChanged();
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusyAction("");
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-view-stack", children: [
    message ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `skills-notice ${message.tone}`, children: message.text }) : null,
    sessionID ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `skills-config-sync ${configNeedsApply ? "pending" : "synced"}`, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-config-versions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "Agent 最新" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
            "#",
            agentConfigVersion || "-"
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "当前任务" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
            "#",
            sessionConfigVersion || "-"
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: configNeedsApply ? "warn" : "ok", children: configNeedsApply ? "待应用" : "已同步" })
      ] }),
      configNeedsApply ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: busyAction === `apply:${agentConfigVersion}` || (session2 == null ? void 0 : session2.status) !== "idle", title: (session2 == null ? void 0 : session2.status) === "idle" ? "精确应用该 Agent 配置版本" : "当前任务结束后才能应用配置", onClick: applyAgentConfig, children: busyAction === `apply:${agentConfigVersion}` ? "应用中" : `应用 Agent #${agentConfigVersion}` }) : null
    ] }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-toolbar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
        "搜索已安装 Skill",
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: query, onChange: (event) => setQuery(event.target.value), placeholder: "名称、标识或来源仓库" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-count", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: filteredSkills.length }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "个结果" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `skills-master-detail ${selectedSkill ? "has-detail" : ""}`, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-list", "aria-label": "已安装 Skills", children: filteredSkills.length ? filteredSkills.map((skill) => {
        const agentBinding = agentBindingMap.get(skill.identifier);
        const sessionBinding = sessionBindingMap.get(skill.identifier);
        const bindingState = skillBindingState(agentBinding, sessionBinding);
        const active = skill.status === "active";
        return /* @__PURE__ */ jsxRuntimeExports.jsxs("article", { className: `skill-list-row ${selectedSkillID === skill.id ? "selected" : ""}`, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "skill-row-main", type: "button", onClick: () => inspectSkill(skill), children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "skill-monogram", "aria-hidden": "true", children: String(skill.title || skill.identifier).slice(0, 1).toUpperCase() }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "skill-row-copy", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "skill-row-title", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: skill.title || skill.identifier }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("code", { children: skill.identifier })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: skill.description || "未提供描述" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "skill-row-meta", children: [
                skill.source_type || "inline",
                skill.source_locator ? ` · ${skill.source_locator}` : ""
              ] })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skill-row-actions", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: toneForState(skill.status), children: skill.status }),
            agentBinding ? /* @__PURE__ */ jsxRuntimeExports.jsxs(StateTag, { tone: "ok", children: [
              "Agent v",
              agentBinding.version
            ] }) : null,
            sessionBinding ? /* @__PURE__ */ jsxRuntimeExports.jsxs(StateTag, { tone: bindingState.synced ? "ok" : "warn", children: [
              "任务 v",
              sessionBinding.version
            ] }) : null,
            bindingState.pendingApply ? /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: "warn", children: "待应用" }) : null,
            bindingState.sessionStillEnabled ? /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: "warn", children: "任务仍启用" }) : null,
            active && !agentBinding ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: busyAction === `enable:${skill.id}`, onClick: () => enableSkill$1(skill), children: busyAction === `enable:${skill.id}` ? "启用中" : "启用" }) : null,
            agentBinding && confirmDisableID !== skill.id ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary danger", type: "button", onClick: () => setConfirmDisableID(skill.id), children: "停用" }) : null,
            confirmDisableID === skill.id ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "skills-confirm-actions", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => setConfirmDisableID(""), children: "取消" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: busyAction === `disable:${skill.id}`, onClick: () => disableSkill$1(skill), children: busyAction === `disable:${skill.id}` ? "停用中" : "确认停用" })
            ] }) : null,
            active && confirmArchiveID !== skill.id ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary danger", type: "button", disabled: Boolean(agentBinding), title: agentBinding ? "先从 Agent 最新配置停用此 Skill" : "归档 Skill", onClick: () => setConfirmArchiveID(skill.id), children: "归档" }) : null,
            confirmArchiveID === skill.id ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "skills-confirm-actions", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => setConfirmArchiveID(""), children: "取消" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: busyAction === `archive:${skill.id}`, onClick: () => archiveSkill$1(skill), children: "确认归档" })
            ] }) : null
          ] })
        ] }, skill.id);
      }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty", children: "没有匹配的已安装 Skill。" }) }),
      selectedSkill ? /* @__PURE__ */ jsxRuntimeExports.jsxs("aside", { className: "skill-detail-pane", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skill-detail-header", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "版本记录" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: selectedSkill.title || selectedSkill.identifier })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => setSelectedSkillID(""), children: "关闭" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skill-binding-summary", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(BindingDetail, { binding: selectedAgentBinding, label: `Agent #${agentConfigVersion || "-"}` }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(BindingDetail, { binding: selectedSessionBinding, label: `当前任务 #${sessionConfigVersion || "-"}` })
        ] }),
        busyAction === `inspect:${selectedSkill.id}` ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty", children: "正在读取版本..." }) : null,
        versions.map((version2) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skill-version-row", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
              "v",
              version2.version
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: "neutral", children: version2.content_format })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("dl", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "Checksum" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { title: version2.checksum_sha256, children: shortHash(version2.checksum_sha256, 16) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "Revision" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { title: version2.source_revision, children: shortHash(version2.source_revision, 16) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "Package" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { children: version2.package_format || "legacy_db" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "对象路径" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { title: version2.package_root, children: version2.package_root || "数据库兼容存储" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "发布时间" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { children: formatDate$1(version2.created_at) })
          ] }),
          version2.source_url ? /* @__PURE__ */ jsxRuntimeExports.jsx("a", { href: version2.source_url, target: "_blank", rel: "noreferrer", children: "查看来源" }) : null,
          version2.package_object_ref_id ? /* @__PURE__ */ jsxRuntimeExports.jsx("a", { href: skillPackageDownloadPath(selectedSkill.id, version2.version), target: "_blank", rel: "noreferrer", children: "下载文件包" }) : null,
          assetBundleFromVersion(version2).files.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "installed-asset-list", children: assetBundleFromVersion(version2).files.map((file) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "installed-asset-row", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: file.path }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                file.binary ? file.content_type || "binary" : "text",
                " · ",
                file.size || 0,
                " bytes"
              ] }),
              file.binary && file.scan_provider ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                file.scan_provider,
                file.scan_version ? ` · ${file.scan_version}` : ""
              ] }) : null
            ] }),
            file.binary ? /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: toneForState(file.scan_status), children: file.scan_status || "unknown" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { children: "text" }),
            file.binary && file.object_ref_id && sessionID ? /* @__PURE__ */ jsxRuntimeExports.jsx("a", { href: objectRefDownloadPath(file.object_ref_id, sessionID), target: "_blank", rel: "noreferrer", children: "受控下载" }) : null
          ] }, file.path)) }) : null,
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            SkillVersionEnableForm,
            {
              binding: selectedAgentBinding,
              busy: busyAction === `enable:${selectedSkill.id}:${version2.version}`,
              onEnable: (targetVersion, inputs, mode, priority) => enableSkillVersion(selectedSkill, targetVersion, inputs, mode, priority),
              version: version2
            }
          )
        ] }, version2.id)),
        !versions.length && busyAction !== `inspect:${selectedSkill.id}` ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty", children: "还没有已发布版本。" }) : null
      ] }) : null
    ] })
  ] });
}
function MarketplaceView({ onSkillsChanged, sessionID }) {
  var _a2, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
  const [searchMode, setSearchMode] = reactExports.useState("internal");
  const [searchValue, setSearchValue] = reactExports.useState("");
  const [sourceRef, setSourceRef] = reactExports.useState("main");
  const [sourcePath, setSourcePath] = reactExports.useState("SKILL.md");
  const [offlineFile, setOfflineFile] = reactExports.useState(null);
  const [discovering, setDiscovering] = reactExports.useState(false);
  const [candidates, setCandidates] = reactExports.useState([]);
  const [previewingKey, setPreviewingKey] = reactExports.useState("");
  const [preview, setPreview] = reactExports.useState(null);
  const [identifier, setIdentifier] = reactExports.useState("");
  const [installing, setInstalling] = reactExports.useState(false);
  const [confirmingUpgrade, setConfirmingUpgrade] = reactExports.useState(false);
  const [message, setMessage] = reactExports.useState(null);
  reactExports.useEffect(() => {
    if (!sessionID) return void 0;
    let active = true;
    setDiscovering(true);
    discoverInternalSkillsMarketplace({ sessionId: sessionID, limit: 30 }).then((response) => {
      if (active) setCandidates(response.items || []);
    }).catch((error) => {
      if (active) setMessage({ tone: "danger", text: error.message });
    }).finally(() => {
      if (active) setDiscovering(false);
    });
    return () => {
      active = false;
    };
  }, [sessionID]);
  async function discover(event) {
    var _a3, _b2;
    event.preventDefault();
    if (!sessionID) {
      setMessage({ tone: "warn", text: "请先打开一个任务会话，再访问当前 workspace 的 Marketplace。" });
      return;
    }
    const value = searchValue.trim();
    if (!["offline", "internal"].includes(searchMode) && !value) return;
    if (searchMode === "offline" && !offlineFile) return;
    setDiscovering(true);
    setMessage(null);
    setPreview(null);
    setConfirmingUpgrade(false);
    try {
      if (searchMode === "internal") {
        const response2 = await discoverInternalSkillsMarketplace({
          sessionId: sessionID,
          query: value,
          limit: 30
        });
        setCandidates(response2.items || []);
        if (!(response2.items || []).length) setMessage({ tone: "neutral", text: "内部市场暂无匹配的已发布 Skill。" });
        return;
      }
      if (searchMode === "offline") {
        if (offlineFile.size <= 0 || offlineFile.size > 8 * 1024 * 1024) {
          throw new Error("ZIP 文件大小必须在 1 byte 到 8 MiB 之间。");
        }
        const upload = await uploadSessionArtifact(sessionID, offlineFile, { description: "Offline Skill package" });
        const candidate = {
          provider: "artifact",
          artifact_id: ((_a3 = upload.artifact) == null ? void 0 : _a3.id) || "",
          name: offlineFile.name,
          path: "SKILL.md",
          size: offlineFile.size,
          verified: true,
          description: `${formatBytes(offlineFile.size)} · ${((_b2 = upload.artifact) == null ? void 0 : _b2.id) || "artifact"}`
        };
        if (!candidate.artifact_id) throw new Error("ZIP 上传后未返回 artifact ID。");
        setCandidates([candidate]);
        await previewCandidate(candidate);
        return;
      }
      if (searchMode === "repository") {
        const candidate = {
          provider: "github",
          repository: value,
          ref: sourceRef.trim(),
          path: sourcePath.trim() || "SKILL.md",
          verified: true
        };
        setCandidates([candidate]);
        await previewCandidate(candidate);
        return;
      }
      const response = await discoverSkillsMarketplace({
        sessionId: sessionID,
        query: value,
        limit: 10
      });
      setCandidates(response.items || []);
      if (!(response.items || []).length) setMessage({ tone: "neutral", text: "没有找到可安装的 Skill。" });
    } catch (error) {
      setCandidates([]);
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setDiscovering(false);
    }
  }
  async function previewCandidate(candidate) {
    const provider = candidate.provider || "github";
    const source = provider === "artifact" ? { provider, artifact_id: candidate.artifact_id } : provider === "catalog" ? { provider, catalog_entry_id: candidate.id } : { provider, repository: candidate.repository, ref: candidate.ref || "", path: candidate.path || "SKILL.md" };
    const key = provider === "artifact" ? `artifact:${source.artifact_id}` : provider === "catalog" ? `catalog:${source.catalog_entry_id}` : `${source.repository}:${source.ref}:${source.path}`;
    setPreviewingKey(key);
    setMessage(null);
    setConfirmingUpgrade(false);
    try {
      const previewRequest = {
        session_id: sessionID,
        identifier: candidate.suggested_identifier || candidate.skill_identifier || "",
        source
      };
      const response = provider === "catalog" ? await previewInternalSkillsMarketplace(previewRequest) : await previewSkillsMarketplace(previewRequest);
      setPreview(response);
      setIdentifier(response.identifier || candidate.suggested_identifier || "");
    } catch (error) {
      setPreview(null);
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setPreviewingKey("");
    }
  }
  async function installPreview() {
    var _a3, _b2, _c2;
    if (!((_a3 = preview == null ? void 0 : preview.policy) == null ? void 0 : _a3.allowed) || ["blocked", "unchanged"].includes(preview.install_state)) return;
    const previousVersion = Number(((_b2 = preview.existing) == null ? void 0 : _b2.version) || 0);
    setInstalling(true);
    setMessage(null);
    try {
      const installRequest = {
        session_id: sessionID,
        identifier: identifier.trim() || preview.identifier,
        source: preview.source,
        policy_id: preview.policy.policy_id || "",
        policy_version: preview.policy.policy_version || 0,
        policy_revision: preview.policy.policy_revision || "",
        upgrade_existing: preview.install_state === "upgrade"
      };
      const response = ((_c2 = preview.source) == null ? void 0 : _c2.provider) === "catalog" ? await installInternalSkillsMarketplace(installRequest) : await installSkillsMarketplace(installRequest);
      const retainedVersion = response.upgraded && previousVersion > 0 ? `；本地 v${previousVersion} 继续保留，可在已安装详情中重新启用` : "";
      setMessage({ tone: "ok", text: `${response.skill.title || response.skill.identifier} v${response.version.version} 已${response.upgraded ? "升级" : "安装"}${retainedVersion}。` });
      await onSkillsChanged();
      setCandidates((current) => current.map((candidate) => {
        var _a4;
        return ((_a4 = preview.source) == null ? void 0 : _a4.provider) === "catalog" && candidate.id === preview.source.catalog_entry_id ? { ...candidate, install_state: "unchanged", existing: { ...candidate.existing || {}, skill_id: response.skill.id, version: response.version.version, source_ref: preview.source.catalog_entry_id, source_revision: response.version.source_revision } } : candidate;
      }));
      setPreview((current) => {
        var _a4;
        return current ? { ...current, install_state: "unchanged", existing: { ...current.existing || {}, skill_id: response.skill.id, version: response.version.version, source_ref: ((_a4 = current.source) == null ? void 0 : _a4.catalog_entry_id) || response.version.source_ref, source_revision: response.version.source_revision } } : current;
      });
      setConfirmingUpgrade(false);
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setInstalling(false);
    }
  }
  const security = (preview == null ? void 0 : preview.security) || {};
  const policy = (preview == null ? void 0 : preview.policy) || {};
  const changes = (preview == null ? void 0 : preview.changes) || {};
  const assetFiles = ((_a2 = preview == null ? void 0 : preview.assets) == null ? void 0 : _a2.files) || [];
  const binaryAssetCount = assetFiles.filter((file) => file.binary).length;
  const sbom = ((_b = preview == null ? void 0 : preview.assets) == null ? void 0 : _b.sbom) || security.sbom || {};
  const canInstall = Boolean(policy.allowed) && !["blocked", "unchanged"].includes(preview == null ? void 0 : preview.install_state);
  const previewState = marketplaceInstallStateMeta(preview == null ? void 0 : preview.install_state);
  const upgradeVersions = marketplaceUpgradeVersions(preview);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-view-stack", children: [
    message ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `skills-notice ${message.tone}`, children: message.text }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { className: `marketplace-search ${searchMode === "repository" ? "exact" : searchMode === "offline" ? "offline" : searchMode === "internal" ? "internal" : ""}`, onSubmit: discover, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-segmented", "aria-label": "发现模式", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: searchMode === "internal" ? "active" : "", type: "button", onClick: () => setSearchMode("internal"), children: "内部市场" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: searchMode === "repository" ? "active" : "", type: "button", onClick: () => setSearchMode("repository"), children: "精确仓库" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: searchMode === "query" ? "active" : "", type: "button", onClick: () => setSearchMode("query"), children: "关键词" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: searchMode === "offline" ? "active" : "", type: "button", onClick: () => setSearchMode("offline"), children: "离线 ZIP" })
      ] }),
      searchMode !== "offline" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
        searchMode === "repository" ? "GitHub 仓库" : searchMode === "internal" ? "内部市场搜索" : "搜索关键词",
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: searchValue, onChange: (event) => setSearchValue(event.target.value), placeholder: searchMode === "repository" ? "owner/repository" : searchMode === "internal" ? "名称、分类或标签，可留空" : "例如 code review" })
      ] }) : null,
      searchMode === "offline" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
        "Skill 文件包",
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "file", accept: ".zip,application/zip", onChange: (event) => {
          var _a3;
          return setOfflineFile(((_a3 = event.target.files) == null ? void 0 : _a3[0]) || null);
        } })
      ] }) : null,
      searchMode === "repository" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
        "Ref",
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: sourceRef, onChange: (event) => setSourceRef(event.target.value), placeholder: "main 或 commit SHA" })
      ] }) : null,
      searchMode === "repository" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
        "SKILL.md 路径",
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: sourcePath, onChange: (event) => setSourcePath(event.target.value), placeholder: "skills/example/SKILL.md" })
      ] }) : null,
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", disabled: discovering || (searchMode === "offline" ? !offlineFile : !["internal"].includes(searchMode) && !searchValue.trim()), children: discovering ? "检查中" : searchMode === "repository" ? "预览来源" : searchMode === "offline" ? "安全预览" : "查找" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `marketplace-layout ${preview ? "has-preview" : ""}`, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "marketplace-results", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-section-heading", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "发现结果" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
            candidates.length,
            " 个候选"
          ] })
        ] }) }),
        candidates.map((candidate) => {
          var _a3;
          const offline = candidate.provider === "artifact";
          const internal = candidate.provider === "catalog";
          const candidateState = marketplaceInstallStateMeta(candidate.install_state);
          const key = offline ? `artifact:${candidate.artifact_id}` : internal ? `catalog:${candidate.id}` : `${candidate.repository}:${candidate.ref || ""}:${candidate.path || "SKILL.md"}`;
          return /* @__PURE__ */ jsxRuntimeExports.jsxs("article", { className: "marketplace-candidate", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "marketplace-candidate-head", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: internal ? candidate.skill_title : offline ? candidate.name : candidate.repository }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("code", { children: internal ? `${candidate.skill_identifier} · v${candidate.skill_version}` : candidate.path || "SKILL.md" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: internal ? candidateState.tone : candidate.verified ? "ok" : "warn", children: internal ? candidateState.label : offline ? "Session Artifact" : candidate.verified ? "已验证路径" : "待 Preview" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: internal ? candidate.summary || candidate.skill_description || "内部发布的不可变 Skill package。" : candidate.description || "仓库候选，需要 Preview 后才能确认包内容和安全状态。" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "marketplace-candidate-meta", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: internal ? candidate.category || "未分类" : offline ? "Offline ZIP" : candidate.stars ? `${candidate.stars} stars` : "GitHub" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: internal ? (candidate.tags || []).join(" · ") || candidate.workspace_id : offline ? candidate.artifact_id : candidate.ref || "default ref" }),
              internal && ((_a3 = candidate.existing) == null ? void 0 : _a3.version) ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                "本地 v",
                candidate.existing.version
              ] }) : null
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: Boolean(previewingKey), onClick: () => previewCandidate(candidate), children: previewingKey === key ? "检查中" : internal ? candidateState.actionLabel : "安全预览" })
          ] }, key);
        }),
        !candidates.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty", children: searchMode === "offline" ? "选择一个标准 Skill ZIP 文件包。" : searchMode === "internal" ? "查询 Organization 内已发布的 Skill。" : "使用精确仓库或关键词查找 Marketplace Skill。" }) : null
      ] }),
      preview ? /* @__PURE__ */ jsxRuntimeExports.jsxs("aside", { className: "marketplace-preview", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "marketplace-preview-header", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "安装前检查" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: preview.title || preview.identifier }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: ((_c = preview.source) == null ? void 0 : _c.provider) === "artifact" ? `${preview.source.artifact_id} · SKILL.md` : ((_d = preview.source) == null ? void 0 : _d.provider) === "catalog" ? `${preview.source.catalog_entry_id} · 内部市场` : `${(_e = preview.source) == null ? void 0 : _e.repository} · ${(_f = preview.source) == null ? void 0 : _f.path}` })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: previewState.tone, children: previewState.label })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "marketplace-preview-body", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "preview-summary-grid", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "License" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: preview.license || "未声明" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Revision" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { title: preview.revision, children: shortHash(preview.revision) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "文件" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: assetFiles.length })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "二进制" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: binaryAssetCount })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "扫描" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: security.scanned_files || 0 })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "SBOM" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: sbom.format || "未生成" })
            ] }),
            ((_g = preview.existing) == null ? void 0 : _g.version) ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "本地版本" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
                "v",
                preview.existing.version
              ] })
            ] }) : null,
            upgradeVersions ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "升级到" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
                "v",
                upgradeVersions.target
              ] })
            ] }) : null
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "preview-section", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-section-heading", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Policy" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: policy.policy_source || "server" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: policy.allowed ? "ok" : "danger", children: policy.allowed ? "允许" : "阻止" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "policy-check-list", children: (policy.checks || []).map((check) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "policy-check-row", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `check-dot ${check.passed ? "passed" : "failed"}`, "aria-hidden": "true" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: policyCheckLabels[check.name] || check.name }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: check.message })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: check.enforced ? "强制" : "报告" })
            ] }, check.name)) }),
            (policy.violations || []).map((violation) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "policy-violation", children: violation }, violation))
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "preview-section", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-section-heading", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Package Security" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Attestation 与静态扫描" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: toneForState(security.highest_severity || ((_h = security.attestation) == null ? void 0 : _h.status)), children: security.highest_severity || ((_i = security.attestation) == null ? void 0 : _i.status) || "unknown" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("dl", { className: "security-facts", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "Attestation" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("dd", { children: [
                ((_j = security.attestation) == null ? void 0 : _j.status) || "unknown",
                " · ",
                ((_k = security.attestation) == null ? void 0 : _k.message) || "-"
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "Digest" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { title: security.digest_sha256, children: shortHash(security.digest_sha256, 20) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "Findings" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("dd", { children: [
                ((_l = security.findings) == null ? void 0 : _l.length) || 0,
                security.findings_limited ? "+" : ""
              ] })
            ] }),
            (security.findings || []).map((finding) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `security-finding ${finding.severity}`, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: toneForState(finding.severity), children: finding.severity }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: finding.rule_id }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: finding.message }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("code", { children: [
                  finding.path,
                  ":",
                  finding.line
                ] })
              ] })
            ] }, `${finding.rule_id}:${finding.path}:${finding.line}`))
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "preview-section", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-section-heading", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Asset Closure" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
                  assetFiles.length,
                  " 个受控引用"
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(StateTag, { tone: binaryAssetCount ? "warn" : "neutral", children: [
                binaryAssetCount,
                " binary"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("dl", { className: "security-facts", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "SBOM format" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { children: sbom.format || "-" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "Components" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { children: ((_m = sbom.components) == null ? void 0 : _m.length) || 0 }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "Package digest" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { title: sbom.package_digest_sha256, children: shortHash(sbom.package_digest_sha256, 20) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "preview-asset-list", children: [
              assetFiles.map((file) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "preview-asset-row", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "preview-asset-main", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: file.path }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    file.binary ? file.content_type || "binary" : "text",
                    " · ",
                    file.size || 0,
                    " bytes"
                  ] }),
                  file.binary && file.scan_provider ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    file.scan_provider,
                    file.scan_version ? ` · ${file.scan_version}` : ""
                  ] }) : null
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: file.binary ? toneForState(file.scan_status) : "neutral", children: file.binary ? file.scan_status || "unknown" : file.executable ? "script" : "text" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("code", { title: file.checksum_sha256, children: shortHash(file.checksum_sha256, 16) })
              ] }, file.path)),
              !assetFiles.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty compact", children: "包内没有引用资产。" }) : null
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "preview-section", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-section-heading", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Package Diff" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: changes.content_changed ? "主文件已变化" : "文件变更" })
            ] }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "diff-groups", children: [["added_files", "新增"], ["changed_files", "修改"], ["removed_files", "删除"]].map(([key, label]) => {
              var _a3;
              return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: ((_a3 = changes[key]) == null ? void 0 : _a3.length) || 0 }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: (changes[key] || []).join(" · ") || "无" })
              ] }, key);
            }) }),
            (((_n = preview.assets) == null ? void 0 : _n.warnings) || []).map((warning) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-notice warn", children: warning }, warning))
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("footer", { className: "marketplace-install-bar", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "安装标识",
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: identifier, readOnly: preview.install_state === "upgrade", onChange: (event) => setIdentifier(event.target.value) })
          ] }),
          confirmingUpgrade && upgradeVersions ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "marketplace-upgrade-confirm", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
                "确认升级到 v",
                upgradeVersions.target
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                "当前 v",
                upgradeVersions.current,
                " 将继续保留。"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-confirm-actions", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: installing, onClick: () => setConfirmingUpgrade(false), children: "取消" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: installing || !identifier.trim(), onClick: installPreview, children: installing ? "写入中" : "确认升级" })
            ] })
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: !canInstall || installing || !identifier.trim(), onClick: () => preview.install_state === "upgrade" ? setConfirmingUpgrade(true) : installPreview(), children: installing ? "写入中" : preview.install_state === "upgrade" ? "升级版本" : preview.install_state === "unchanged" ? "已是最新" : "安装 Skill" })
        ] })
      ] }) : null
    ] })
  ] });
}
function MarketplaceManagementView({ skills: skills2, workspaceID }) {
  const [entries, setEntries] = reactExports.useState([]);
  const [selectedID, setSelectedID] = reactExports.useState("");
  const [creating, setCreating] = reactExports.useState(false);
  const [versions, setVersions] = reactExports.useState([]);
  const [statusFilter, setStatusFilter] = reactExports.useState("");
  const [form, setForm] = reactExports.useState({ ...emptyMarketplaceEntryForm });
  const [transitionNote, setTransitionNote] = reactExports.useState("");
  const [confirmWithdraw, setConfirmWithdraw] = reactExports.useState(false);
  const [busy, setBusy] = reactExports.useState("");
  const [message, setMessage] = reactExports.useState(null);
  const selected = entries.find((entry2) => entry2.id === selectedID) || null;
  const activeSkills = skills2.filter((skill) => skill.status === "active");
  const visibleEntries = statusFilter ? entries.filter((entry2) => entry2.status === statusFilter) : entries;
  const counts = marketplaceEntryStages.reduce((result, stage) => {
    result[stage.status] = entries.filter((entry2) => entry2.status === stage.status).length;
    return result;
  }, {});
  async function loadEntries(preferredID = "") {
    var _a2;
    const response = await marketplaceEntries({ workspaceId: workspaceID, includeWithdrawn: true });
    const nextEntries = response.entries || [];
    setEntries(nextEntries);
    const nextID = preferredID || selectedID;
    if (nextID && nextEntries.some((entry2) => entry2.id === nextID)) {
      setSelectedID(nextID);
    } else if (!creating) {
      setSelectedID(((_a2 = nextEntries[0]) == null ? void 0 : _a2.id) || "");
    }
    return nextEntries;
  }
  reactExports.useEffect(() => {
    let active = true;
    setMessage(null);
    marketplaceEntries({ workspaceId: workspaceID, includeWithdrawn: true }).then((response) => {
      if (!active) return;
      const nextEntries = response.entries || [];
      setEntries(nextEntries);
      setSelectedID((current) => {
        var _a2;
        return current && nextEntries.some((entry2) => entry2.id === current) ? current : ((_a2 = nextEntries[0]) == null ? void 0 : _a2.id) || "";
      });
    }).catch((error) => {
      if (active) setMessage({ tone: "danger", text: error.message });
    });
    return () => {
      active = false;
    };
  }, [workspaceID]);
  reactExports.useEffect(() => {
    if (!selected || creating) return;
    setForm({
      skillID: selected.skill_id,
      skillVersion: String(selected.skill_version),
      summary: selected.summary || "",
      category: selected.category || "",
      tags: Array.isArray(selected.tags) ? selected.tags.join(", ") : ""
    });
    setTransitionNote(selected.status === "published" ? selected.withdrawal_reason || "" : selected.review_note || "");
    setConfirmWithdraw(false);
  }, [selectedID, selected == null ? void 0 : selected.updated_at, creating]);
  async function loadVersions(skillID, preferredVersion = "") {
    if (!skillID) {
      setVersions([]);
      return;
    }
    const response = await skillVersions(skillID);
    const nextVersions = response.versions || [];
    setVersions(nextVersions);
    setForm((current) => {
      var _a2;
      return current.skillID === skillID ? {
        ...current,
        skillVersion: preferredVersion || current.skillVersion || String(((_a2 = nextVersions[0]) == null ? void 0 : _a2.version) || "")
      } : current;
    });
  }
  function selectEntry(entry2) {
    setCreating(false);
    setSelectedID(entry2.id);
    setVersions([]);
    setMessage(null);
  }
  async function startCreate() {
    var _a2;
    const skillID = ((_a2 = activeSkills[0]) == null ? void 0 : _a2.id) || "";
    setCreating(true);
    setSelectedID("");
    setTransitionNote("");
    setConfirmWithdraw(false);
    setMessage(null);
    setForm({ ...emptyMarketplaceEntryForm, skillID });
    try {
      await loadVersions(skillID);
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    }
  }
  async function changeSkill(skillID) {
    setForm((current) => ({ ...current, skillID, skillVersion: "" }));
    try {
      await loadVersions(skillID);
    } catch (error) {
      setVersions([]);
      setMessage({ tone: "danger", text: error.message });
    }
  }
  async function saveEntry(event) {
    event.preventDefault();
    setBusy("save");
    setMessage(null);
    try {
      const body = {
        workspace_id: workspaceID,
        summary: form.summary,
        category: form.category,
        tags: splitValues(form.tags)
      };
      const entry2 = creating ? await createMarketplaceEntry({ ...body, skill_id: form.skillID, skill_version: Number(form.skillVersion) }) : await updateMarketplaceEntry(selected.id, body);
      setCreating(false);
      setSelectedID(entry2.id);
      await loadEntries(entry2.id);
      setMessage({ tone: "ok", text: creating ? "市场草稿已创建。" : "市场草稿已保存。" });
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusy("");
    }
  }
  async function runTransition(action) {
    if (!selected) return;
    setBusy(action.action);
    setMessage(null);
    try {
      const entry2 = await transitionMarketplaceEntry(selected.id, action.action, {
        workspace_id: workspaceID,
        note: transitionNote.trim()
      });
      await loadEntries(entry2.id);
      setConfirmWithdraw(false);
      setTransitionNote("");
      setMessage({ tone: "ok", text: `状态已更新为${marketplaceEntryStatusLabel(entry2.status)}。` });
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusy("");
    }
  }
  const nextAction = marketplaceEntryNextAction(selected == null ? void 0 : selected.status);
  const editable = creating || (selected == null ? void 0 : selected.status) === "draft";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-view-stack", children: [
    message ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `skills-notice ${message.tone}`, children: message.text }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "market-admin-summary", children: marketplaceEntryStages.map((stage) => /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: statusFilter === stage.status ? "active" : "", type: "button", onClick: () => setStatusFilter((current) => current === stage.status ? "" : stage.status), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: stage.label }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: counts[stage.status] || 0 })
    ] }, stage.status)) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "market-admin-layout", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("aside", { className: "market-admin-list", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-section-heading", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "市场条目" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
              visibleEntries.length,
              " 条记录"
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: startCreate, children: "新建草稿" })
        ] }),
        visibleEntries.map((entry2) => /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: `market-admin-list-item ${selectedID === entry2.id ? "selected" : ""}`, type: "button", onClick: () => selectEntry(entry2), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: entry2.skill_title }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: marketplaceEntryStatusTone(entry2.status), children: marketplaceEntryStatusLabel(entry2.status) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("code", { children: [
            entry2.skill_identifier,
            " · v",
            entry2.skill_version
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
            entry2.category || "未分类",
            " · ",
            formatDate$1(entry2.updated_at)
          ] })
        ] }, entry2.id)),
        !visibleEntries.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty compact", children: "当前筛选下没有市场条目。" }) : null
      ] }),
      creating || selected ? /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { className: "market-admin-editor", onSubmit: saveEntry, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: creating ? "创建市场条目" : selected.id }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: creating ? "新建草稿" : selected.skill_title })
          ] }),
          !creating ? /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: marketplaceEntryStatusTone(selected.status), children: marketplaceEntryStatusLabel(selected.status) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { children: "草稿" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "market-lifecycle", "aria-label": "市场条目生命周期", children: marketplaceEntryStages.map((stage) => {
          const state = marketplaceEntryStageState(creating ? "draft" : selected.status, stage.status);
          return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: state, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": "true" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: stage.label })
          ] }, stage.status);
        }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "market-admin-form-grid", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "Skill",
            /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: form.skillID, disabled: !creating, required: true, onChange: (event) => changeSkill(event.target.value), children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "请选择已安装 Skill" }),
              activeSkills.map((skill) => /* @__PURE__ */ jsxRuntimeExports.jsxs("option", { value: skill.id, children: [
                skill.title,
                " · ",
                skill.identifier
              ] }, skill.id))
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "精确版本",
            /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: form.skillVersion, disabled: !creating, required: true, onChange: (event) => setForm((current) => ({ ...current, skillVersion: event.target.value })), children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "请选择版本" }),
              versions.map((version2) => /* @__PURE__ */ jsxRuntimeExports.jsxs("option", { value: version2.version, children: [
                "v",
                version2.version,
                " · ",
                shortHash(version2.checksum_sha256)
              ] }, version2.id)),
              !creating && selected && !versions.some((version2) => version2.version === selected.skill_version) ? /* @__PURE__ */ jsxRuntimeExports.jsxs("option", { value: selected.skill_version, children: [
                "v",
                selected.skill_version,
                " · ",
                shortHash(selected.version_checksum_sha256)
              ] }) : null
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "分类",
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: form.category, disabled: !editable, maxLength: 80, onChange: (event) => setForm((current) => ({ ...current, category: event.target.value })), placeholder: "例如 Engineering" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "标签",
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: form.tags, disabled: !editable, onChange: (event) => setForm((current) => ({ ...current, tags: event.target.value })), placeholder: "review, quality" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "market-admin-summary-field", children: [
            "市场摘要",
            /* @__PURE__ */ jsxRuntimeExports.jsx("textarea", { value: form.summary, disabled: !editable, maxLength: 2e3, rows: 5, onChange: (event) => setForm((current) => ({ ...current, summary: event.target.value })), placeholder: "用于市场列表和审核的说明" })
          ] })
        ] }),
        !creating && selected ? /* @__PURE__ */ jsxRuntimeExports.jsxs("dl", { className: "market-admin-facts", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "Package" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { children: selected.package_format || "-" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "Version digest" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { title: selected.version_checksum_sha256, children: shortHash(selected.version_checksum_sha256, 24) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "创建人" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { children: selected.created_by }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "最后更新" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { children: formatDate$1(selected.updated_at) })
        ] }) : null,
        !creating && ["pending_review", "published"].includes(selected == null ? void 0 : selected.status) ? /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "market-transition-note", children: [
          selected.status === "pending_review" ? "审核意见" : "下架原因",
          /* @__PURE__ */ jsxRuntimeExports.jsx("textarea", { value: transitionNote, maxLength: 2e3, rows: 3, onChange: (event) => setTransitionNote(event.target.value), placeholder: selected.status === "pending_review" ? "可选，记录本次审核结论" : "记录下架原因" })
        ] }) : null,
        /* @__PURE__ */ jsxRuntimeExports.jsxs("footer", { className: "market-admin-actions", children: [
          creating ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => {
            var _a2;
            setCreating(false);
            setSelectedID(((_a2 = entries[0]) == null ? void 0 : _a2.id) || "");
          }, children: "取消" }) : null,
          editable ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", disabled: busy === "save" || !form.skillID || !form.skillVersion, children: busy === "save" ? "保存中" : creating ? "创建草稿" : "保存草稿" }) : null,
          !creating && nextAction && nextAction.action !== "withdraw" ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: Boolean(busy), onClick: () => runTransition(nextAction), children: busy === nextAction.action ? "处理中" : nextAction.label }) : null,
          !creating && (nextAction == null ? void 0 : nextAction.action) === "withdraw" && !confirmWithdraw ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary danger", type: "button", disabled: Boolean(busy), onClick: () => setConfirmWithdraw(true), children: "下架" }) : null,
          !creating && (nextAction == null ? void 0 : nextAction.action) === "withdraw" && confirmWithdraw ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "skills-confirm-actions", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => setConfirmWithdraw(false), children: "取消" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: Boolean(busy), onClick: () => runTransition(nextAction), children: busy === "withdraw" ? "下架中" : "确认下架" })
          ] }) : null,
          !creating && (selected == null ? void 0 : selected.status) === "withdrawn" ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "market-admin-terminal", children: "该版本已下架，只保留历史审计与回放。" }) : null
        ] })
      ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty", children: "选择一个市场条目，或创建新的草稿。" })
    ] })
  ] });
}
function PolicyView({ workspaceID }) {
  var _a2;
  const [policies, setPolicies] = reactExports.useState([]);
  const [selectedPolicyID, setSelectedPolicyID] = reactExports.useState("");
  const [selectedPolicy, setSelectedPolicy] = reactExports.useState(null);
  const [form, setForm] = reactExports.useState({ ...emptyPolicyForm });
  const [loading, setLoading] = reactExports.useState(true);
  const [saving, setSaving] = reactExports.useState(false);
  const [confirmArchive, setConfirmArchive] = reactExports.useState(false);
  const [message, setMessage] = reactExports.useState(null);
  async function loadPolicies() {
    setLoading(true);
    try {
      const requests = [marketplacePolicies({ workspaceId: workspaceID, includeArchived: true })];
      requests.push(marketplacePolicies({ organizationId: form.organizationID || "org_default", includeArchived: true }));
      const responses = await Promise.all(requests);
      const deduplicated = /* @__PURE__ */ new Map();
      responses.flatMap((response) => response.policies || []).forEach((policy) => deduplicated.set(policy.id, policy));
      setPolicies([...deduplicated.values()].sort((left, right) => new Date(right.created_at) - new Date(left.created_at)));
    } catch (error) {
      setPolicies([]);
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setLoading(false);
    }
  }
  reactExports.useEffect(() => {
    loadPolicies();
  }, [workspaceID]);
  async function selectPolicy(policy) {
    setSelectedPolicyID(policy.id);
    setMessage(null);
    setConfirmArchive(false);
    try {
      const detail = await marketplacePolicy(policy.id);
      setSelectedPolicy(detail);
      setForm(policyFormFromDetail(detail));
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    }
  }
  function startNewPolicy() {
    setSelectedPolicyID("");
    setSelectedPolicy(null);
    setConfirmArchive(false);
    setForm({ ...emptyPolicyForm });
    setMessage(null);
  }
  function updateForm(key, value) {
    setForm((current) => ({ ...current, [key]: value }));
  }
  async function savePolicy(event) {
    var _a3;
    event.preventDefault();
    setSaving(true);
    setMessage(null);
    try {
      const config = policyConfigFromForm(form);
      if (((_a3 = selectedPolicy == null ? void 0 : selectedPolicy.policy) == null ? void 0 : _a3.status) === "active") {
        const version2 = await publishMarketplacePolicyVersion(selectedPolicy.policy.id, config);
        const detail = await marketplacePolicy(selectedPolicy.policy.id);
        setSelectedPolicy(detail);
        setForm(policyFormFromDetail(detail));
        setMessage({ tone: "ok", text: `已发布不可变策略版本 v${version2.version}。` });
      } else {
        const body = {
          scope_type: form.scopeType,
          workspace_id: form.scopeType === "workspace" ? workspaceID : "",
          organization_id: form.scopeType === "organization" ? form.organizationID.trim() : "",
          config
        };
        const created = await createMarketplacePolicy(body);
        setSelectedPolicyID(created.policy.id);
        setSelectedPolicy(created);
        setForm(policyFormFromDetail(created));
        setMessage({ tone: "ok", text: `已创建 ${form.scopeType} 策略 v1。` });
      }
      await loadPolicies();
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setSaving(false);
    }
  }
  async function archivePolicy() {
    var _a3;
    if (!((_a3 = selectedPolicy == null ? void 0 : selectedPolicy.policy) == null ? void 0 : _a3.id)) return;
    setSaving(true);
    setMessage(null);
    try {
      await archiveMarketplacePolicy(selectedPolicy.policy.id);
      setMessage({ tone: "ok", text: "策略已归档，有效策略将自动回退到下一优先级。" });
      setSelectedPolicy(null);
      setSelectedPolicyID("");
      setConfirmArchive(false);
      setForm({ ...emptyPolicyForm });
      await loadPolicies();
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setSaving(false);
    }
  }
  const editingActive = ((_a2 = selectedPolicy == null ? void 0 : selectedPolicy.policy) == null ? void 0 : _a2.status) === "active";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-view-stack", children: [
    message ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `skills-notice ${message.tone}`, children: message.text }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "policy-layout", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("aside", { className: "policy-list-pane", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-section-heading", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "策略版本" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
              policies.length,
              " 条记录"
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: startNewPolicy, children: "新建策略" })
        ] }),
        loading ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty", children: "正在读取策略..." }) : null,
        policies.map((policy) => /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: `policy-list-item ${selectedPolicyID === policy.id ? "selected" : ""}`, type: "button", onClick: () => selectPolicy(policy), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: policy.scope_type }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: toneForState(policy.status), children: policy.status })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("code", { children: policy.workspace_id || policy.organization_id }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
            "v",
            policy.current_version,
            " · ",
            formatDate$1(policy.created_at)
          ] })
        ] }, policy.id)),
        !loading && !policies.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty", children: "当前使用 Server 默认策略。" }) : null
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { className: "policy-editor", onSubmit: savePolicy, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: editingActive ? "发布新版本" : "创建策略" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: editingActive ? selectedPolicy.policy.id : "Marketplace Policy" })
          ] }),
          (selectedPolicy == null ? void 0 : selectedPolicy.version) ? /* @__PURE__ */ jsxRuntimeExports.jsxs(StateTag, { tone: "neutral", children: [
            "v",
            selectedPolicy.version.version,
            " · ",
            shortHash(selectedPolicy.version.checksum_sha256)
          ] }) : null
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "policy-form-grid two", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "作用域",
            /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: form.scopeType, disabled: editingActive, onChange: (event) => updateForm("scopeType", event.target.value), children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "workspace", children: "Workspace" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "organization", children: "Organization" })
            ] })
          ] }),
          form.scopeType === "workspace" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "Workspace ID",
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: workspaceID, disabled: true })
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "Organization ID",
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: form.organizationID, disabled: editingActive, onChange: (event) => updateForm("organizationID", event.target.value) })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "policy-form-grid two", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "允许的 Owners",
            /* @__PURE__ */ jsxRuntimeExports.jsx("textarea", { value: form.allowedOwners, onChange: (event) => updateForm("allowedOwners", event.target.value), placeholder: "每行一个 GitHub owner" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "允许的 Repositories",
            /* @__PURE__ */ jsxRuntimeExports.jsx("textarea", { value: form.allowedRepositories, onChange: (event) => updateForm("allowedRepositories", event.target.value), placeholder: "owner/repository" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "允许的 Licenses",
            /* @__PURE__ */ jsxRuntimeExports.jsx("textarea", { value: form.allowedLicenses, onChange: (event) => updateForm("allowedLicenses", event.target.value), placeholder: "MIT\\nApache-2.0" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "拒绝的 Licenses",
            /* @__PURE__ */ jsxRuntimeExports.jsx("textarea", { value: form.deniedLicenses, onChange: (event) => updateForm("deniedLicenses", event.target.value), placeholder: "GPL-3.0" })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "policy-toggle-grid", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "policy-toggle", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", checked: form.requireCommitSHA, onChange: (event) => updateForm("requireCommitSHA", event.target.checked) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "固定 Commit SHA" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "禁止可移动的 branch/tag ref" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "policy-toggle", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", checked: form.requireLicense, onChange: (event) => updateForm("requireLicense", event.target.checked) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "必须声明许可证" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "缺失 license 时阻止安装" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "policy-toggle", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", checked: form.requireAttestation, onChange: (event) => updateForm("requireAttestation", event.target.checked) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "必须验证 Attestation" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "仅接受受信 Ed25519 签名" })
            ] })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "policy-form-grid two", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "静态扫描阻断等级",
            /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: form.staticScanBlockSeverity, onChange: (event) => updateForm("staticScanBlockSeverity", event.target.value), children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "仅报告" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "medium", children: "Medium 及以上" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "high", children: "High 及以上" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "critical", children: "Critical" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            "Trusted Attestation Keys",
            /* @__PURE__ */ jsxRuntimeExports.jsx("textarea", { className: "code-input", value: form.trustedAttestationKeys, onChange: (event) => updateForm("trustedAttestationKeys", event.target.value), spellCheck: "false" })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("footer", { className: "policy-editor-actions", children: [
          editingActive && !confirmArchive ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary danger", type: "button", onClick: () => setConfirmArchive(true), children: "归档策略" }) : null,
          confirmArchive ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "skills-confirm-actions", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => setConfirmArchive(false), children: "取消" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: saving, onClick: archivePolicy, children: "确认归档" })
          ] }) : null,
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", disabled: saving || form.scopeType === "organization" && !form.organizationID.trim(), children: saving ? "保存中" : editingActive ? "发布新版本" : "创建策略" })
        ] })
      ] })
    ] })
  ] });
}
function StorageView({ workspaceID }) {
  var _a2, _b, _c, _d, _e, _f, _g, _h, _i;
  const resolvedWorkspaceID = workspaceID || "wksp_default";
  const [effective, setEffective] = reactExports.useState(null);
  const [policies, setPolicies] = reactExports.useState([]);
  const [preview, setPreview] = reactExports.useState(null);
  const [runs, setRuns] = reactExports.useState([]);
  const [tombstones, setTombstones] = reactExports.useState([]);
  const [form, setForm] = reactExports.useState({ enabled: false, retentionDays: 30, deleteLimit: 100 });
  const [busy, setBusy] = reactExports.useState("");
  const [confirmRun, setConfirmRun] = reactExports.useState(false);
  const [message, setMessage] = reactExports.useState(null);
  async function loadStorage() {
    var _a3, _b2, _c2;
    setBusy((current) => current || "load");
    try {
      const [effectivePolicy, policyResponse, runResponse, tombstoneResponse] = await Promise.all([
        skillAssetRetentionEffective(resolvedWorkspaceID),
        skillAssetRetentionPolicies({ workspaceId: resolvedWorkspaceID, includeArchived: true }),
        skillAssetGCRuns(resolvedWorkspaceID),
        skillAssetGCTombstones(resolvedWorkspaceID)
      ]);
      setEffective(effectivePolicy);
      setPolicies(policyResponse.policies || []);
      setRuns(runResponse.runs || []);
      setTombstones(tombstoneResponse.tombstones || []);
      setForm({
        enabled: Boolean((_a3 = effectivePolicy == null ? void 0 : effectivePolicy.config) == null ? void 0 : _a3.enabled),
        retentionDays: Number(((_b2 = effectivePolicy == null ? void 0 : effectivePolicy.config) == null ? void 0 : _b2.retention_days) || 30),
        deleteLimit: Number(((_c2 = effectivePolicy == null ? void 0 : effectivePolicy.config) == null ? void 0 : _c2.delete_limit) || 100)
      });
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusy("");
    }
  }
  reactExports.useEffect(() => {
    loadStorage();
  }, [resolvedWorkspaceID]);
  async function savePolicy(event) {
    var _a3;
    event.preventDefault();
    setBusy("policy");
    setMessage(null);
    const config = {
      enabled: form.enabled,
      retention_days: Number(form.retentionDays),
      delete_limit: Number(form.deleteLimit)
    };
    try {
      if ((effective == null ? void 0 : effective.source) === "workspace" && ((_a3 = effective == null ? void 0 : effective.policy) == null ? void 0 : _a3.status) === "active") {
        const version2 = await publishSkillAssetRetentionPolicyVersion(effective.policy.id, config);
        setMessage({ tone: "ok", text: `已发布保留策略 v${version2.version}。` });
      } else {
        const created = await createSkillAssetRetentionPolicy({
          scope_type: "workspace",
          workspace_id: resolvedWorkspaceID,
          config
        });
        setMessage({ tone: "ok", text: `已创建 Workspace 保留策略 v${created.version.version}。` });
      }
      setPreview(null);
      await loadStorage();
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusy("");
    }
  }
  async function archiveWorkspacePolicy() {
    var _a3;
    if ((effective == null ? void 0 : effective.source) !== "workspace" || !((_a3 = effective == null ? void 0 : effective.policy) == null ? void 0 : _a3.id)) return;
    setBusy("archive");
    setMessage(null);
    try {
      await archiveSkillAssetRetentionPolicy(effective.policy.id);
      setPreview(null);
      setMessage({ tone: "ok", text: "Workspace 策略已归档。" });
      await loadStorage();
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusy("");
    }
  }
  async function previewGC() {
    setBusy("preview");
    setMessage(null);
    setConfirmRun(false);
    try {
      const response = await previewSkillAssetGC({ workspace_id: resolvedWorkspaceID });
      setPreview(response);
      setMessage({
        tone: response.candidate_count ? "warn" : "ok",
        text: `预览完成：${response.candidate_count} 个候选，${formatBytes(response.candidate_bytes)}。`
      });
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusy("");
    }
  }
  async function runGC() {
    setBusy("run");
    setMessage(null);
    try {
      const result = await runSkillAssetGC({ workspace_id: resolvedWorkspaceID, confirm: "DELETE" });
      setConfirmRun(false);
      setPreview(null);
      setMessage({
        tone: result.run.failed_count ? "warn" : "ok",
        text: `运行 ${result.run.id}：删除 ${result.run.deleted_count}，跳过 ${result.run.skipped_count}，失败 ${result.run.failed_count}。`
      });
      await loadStorage();
    } catch (error) {
      setMessage({ tone: "danger", text: error.message });
    } finally {
      setBusy("");
    }
  }
  const activeWorkspacePolicy = policies.find((policy) => policy.scope_type === "workspace" && policy.status === "active");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-view-stack storage-view", children: [
    message ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `skills-notice ${message.tone}`, children: message.text }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "storage-summary-band", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Policy source" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: (effective == null ? void 0 : effective.source) || "-" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: ((_a2 = effective == null ? void 0 : effective.policy) == null ? void 0 : _a2.id) || "Server fallback" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Retention" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
          ((_b = effective == null ? void 0 : effective.config) == null ? void 0 : _b.retention_days) || "-",
          " 天"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: ((_c = effective == null ? void 0 : effective.config) == null ? void 0 : _c.enabled) ? "Enabled" : "Disabled" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Delete limit" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: ((_d = effective == null ? void 0 : effective.config) == null ? void 0 : _d.delete_limit) || "-" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "Objects / run" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Reclaimed" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: formatBytes(runs.reduce((sum, run) => sum + Number(run.bytes_deleted || 0), 0)) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
          tombstones.length,
          " tombstones"
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "storage-policy-band", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "skills-section-heading", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Retention policy" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: ((_e = effective == null ? void 0 : effective.version) == null ? void 0 : _e.version) > 0 ? `v${effective.version.version} · ${shortHash(effective.revision)}` : "Server fallback" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: ((_f = effective == null ? void 0 : effective.config) == null ? void 0 : _f.enabled) ? "ok" : "warn", children: ((_g = effective == null ? void 0 : effective.config) == null ? void 0 : _g.enabled) ? "enabled" : "disabled" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { className: "storage-policy-form", onSubmit: savePolicy, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "policy-toggle", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", checked: form.enabled, onChange: (event) => setForm((value) => ({ ...value, enabled: event.target.checked })) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "自动清理许可" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "允许手动和后台 GC 执行删除" })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
          "保留天数",
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "number", min: "1", max: "3650", value: form.retentionDays, onChange: (event) => setForm((value) => ({ ...value, retentionDays: event.target.value })) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
          "单次上限",
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "number", min: "1", max: "1000", value: form.deleteLimit, onChange: (event) => setForm((value) => ({ ...value, deleteLimit: event.target.value })) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", disabled: Boolean(busy), children: busy === "policy" ? "发布中" : activeWorkspacePolicy ? "发布新版本" : "创建策略" }),
        (effective == null ? void 0 : effective.source) === "workspace" ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary danger", type: "button", disabled: Boolean(busy), onClick: archiveWorkspacePolicy, children: "归档" }) : null
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "storage-gc-band", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "skills-section-heading", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Dry-run inventory" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: preview ? `${preview.candidate_count} 个候选 · ${formatBytes(preview.candidate_bytes)}` : "尚未预览" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "storage-actions", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(busy), onClick: previewGC, children: busy === "preview" ? "扫描中" : "预览" }),
          !confirmRun ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: Boolean(busy) || !(preview == null ? void 0 : preview.candidate_count) || !((_h = effective == null ? void 0 : effective.config) == null ? void 0 : _h.enabled), onClick: () => setConfirmRun(true), children: "执行清理" }) : null,
          confirmRun ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "skills-confirm-actions", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => setConfirmRun(false), children: "取消" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: Boolean(busy), onClick: runGC, children: busy === "run" ? "执行中" : "确认删除" })
          ] }) : null
        ] })
      ] }),
      ((_i = preview == null ? void 0 : preview.candidates) == null ? void 0 : _i.length) ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "storage-object-list", children: preview.candidates.map((candidate) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "storage-object-row", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: candidate.asset_path || candidate.object_key }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("code", { children: [
            candidate.object_ref_id,
            " · ",
            shortHash(candidate.checksum_sha256)
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: candidate.reason === "orphaned_skill_asset" ? "Orphan" : "Archived" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
          candidate.scan_provider || "builtin",
          " ",
          candidate.scan_version || ""
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: formatBytes(candidate.size_bytes) })
      ] }, candidate.object_ref_id)) }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty compact", children: preview ? "没有达到保留期限且可安全删除的对象。" : "运行预览后显示候选对象。" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "storage-history-grid", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("header", { className: "skills-section-heading", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Recent runs" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
            runs.length,
            " 次"
          ] })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "storage-history-list", children: [
          runs.map((run) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "storage-history-row", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: run.id }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
                formatDate$1(run.started_at),
                " · ",
                run.policy_source
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(StateTag, { tone: toneForState(run.status), children: run.status }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              run.deleted_count,
              " deleted"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: formatBytes(run.bytes_deleted) })
          ] }, run.id)),
          !runs.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty compact", children: "暂无 GC 运行记录。" }) : null
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("header", { className: "skills-section-heading", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Tombstones" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
            tombstones.length,
            " 条"
          ] })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "storage-history-list", children: [
          tombstones.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "storage-tombstone-row", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: item.asset_path || item.object_key }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("code", { children: [
                item.object_ref_id,
                " · ",
                shortHash(item.checksum_sha256)
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: formatBytes(item.size_bytes) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: formatDate$1(item.deleted_at) })
          ] }, item.id)),
          !tombstones.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "skills-empty compact", children: "暂无 tombstone。" }) : null
        ] })
      ] })
    ] })
  ] });
}
function SkillsManagement({
  agent: agent2,
  onApplyAgentConfig,
  onSkillsChanged,
  runtimeConfig,
  session: session2,
  sessionID = "",
  skills: skills2 = [],
  workspaceID = ""
}) {
  var _a2, _b;
  const [activeView, setActiveView] = reactExports.useState("installed");
  const agentBindings = skillBindingsFromConfig((_a2 = agent2 == null ? void 0 : agent2.config_version) == null ? void 0 : _a2.skills);
  const sessionBindings = sessionID ? skillBindingsFromConfig(runtimeConfig == null ? void 0 : runtimeConfig.skills) : [];
  const agentConfigVersion = Number((agent2 == null ? void 0 : agent2.current_config_version) || ((_b = agent2 == null ? void 0 : agent2.config_version) == null ? void 0 : _b.version) || 0);
  const sessionConfigVersion = sessionID ? Number((runtimeConfig == null ? void 0 : runtimeConfig.agent_config_version) || (session2 == null ? void 0 : session2.agent_config_version) || 0) : 0;
  const activeCount = skills2.filter((skill) => skill.status === "active").length;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-console", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "skills-console-header", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-console-title", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Workspace capability registry" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Skills 管理" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: "安装、验证并固定可复用能力版本。" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skills-console-stats", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: activeCount }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Active" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: agentBindings.length }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Enabled" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: sessionID ? "Ready" : "No session" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Context" })
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("nav", { className: "skills-view-tabs", "aria-label": "Skills 管理视图", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: activeView === "installed" ? "active" : "", type: "button", onClick: () => setActiveView("installed"), children: "已安装" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: activeView === "marketplace" ? "active" : "", type: "button", onClick: () => setActiveView("marketplace"), children: "Marketplace" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: activeView === "market-admin" ? "active" : "", type: "button", onClick: () => setActiveView("market-admin"), children: "市场管理" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: activeView === "policy" ? "active" : "", type: "button", onClick: () => setActiveView("policy"), children: "Policy" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: activeView === "storage" ? "active" : "", type: "button", onClick: () => setActiveView("storage"), children: "Storage" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "skills-console-surface", children: [
      activeView === "installed" ? /* @__PURE__ */ jsxRuntimeExports.jsx(
        InstalledSkillsView,
        {
          agentBindings,
          agentConfigVersion,
          onApplyAgentConfig,
          onSkillsChanged,
          session: session2,
          sessionBindings,
          sessionConfigVersion,
          sessionID,
          skills: skills2
        }
      ) : null,
      activeView === "marketplace" ? /* @__PURE__ */ jsxRuntimeExports.jsx(MarketplaceView, { onSkillsChanged, sessionID }) : null,
      activeView === "market-admin" ? /* @__PURE__ */ jsxRuntimeExports.jsx(MarketplaceManagementView, { skills: skills2, workspaceID }) : null,
      activeView === "policy" ? /* @__PURE__ */ jsxRuntimeExports.jsx(PolicyView, { workspaceID }) : null,
      activeView === "storage" ? /* @__PURE__ */ jsxRuntimeExports.jsx(StorageView, { workspaceID }) : null
    ] })
  ] });
}
function pretty(value) {
  return JSON.stringify(value, null, 2);
}
function formatTime(value) {
  if (!value) return "-";
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return String(value);
  return date.toLocaleString();
}
function formatTaskTime(value) {
  if (!value) return "-";
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return String(value);
  const month = String(date.getMonth() + 1).padStart(2, "0");
  const day = String(date.getDate()).padStart(2, "0");
  const hours = String(date.getHours()).padStart(2, "0");
  const minutes = String(date.getMinutes()).padStart(2, "0");
  const seconds = String(date.getSeconds()).padStart(2, "0");
  return `${month}-${day} ${hours}:${minutes}:${seconds}`;
}
function formatDuration(ms) {
  const value = Number(ms || 0);
  if (value < 1e3) return `${value} ms`;
  return `${(value / 1e3).toFixed(value < 1e4 ? 2 : 1)} s`;
}
function pillClass(statusValue) {
  if (["completed", "ok", "success", "approved", "idle"].includes(statusValue)) return "pill ok";
  if (["waiting approval", "waiting_approval", "pending", "blocked", "running", "interrupting", "provisioning", "compacting"].includes(statusValue)) return "pill warn";
  if (["failed", "error", "rejected"].includes(statusValue)) return "pill err";
  return "pill";
}
function toolCallID(event) {
  var _a2;
  const data2 = (_a2 = event == null ? void 0 : event.payload) == null ? void 0 : _a2.data;
  if (!data2 || typeof data2 !== "object" || Array.isArray(data2)) return "";
  return String(data2.id || data2.call_id || "").trim();
}
function buildToolCallLifecycles(events2) {
  const lifecycles = /* @__PURE__ */ new Map();
  for (const event of events2 || []) {
    const callID = toolCallID(event);
    if (!callID) continue;
    const lifecycle = lifecycles.get(callID) || {};
    switch (event.type) {
      case "runtime.tool_call":
        lifecycle.call = latestEvent(lifecycle.call, event);
        break;
      case "runtime.tool_intervention_required":
        lifecycle.required = latestEvent(lifecycle.required, event);
        break;
      case "runtime.tool_intervention_approved":
        lifecycle.approved = latestEvent(lifecycle.approved, event);
        lifecycle.decision = latestEvent(lifecycle.decision, event);
        break;
      case "runtime.tool_intervention_rejected":
        lifecycle.rejected = latestEvent(lifecycle.rejected, event);
        lifecycle.decision = latestEvent(lifecycle.decision, event);
        break;
      case "runtime.tool_result":
        lifecycle.result = latestEvent(lifecycle.result, event);
        break;
      default:
        continue;
    }
    lifecycles.set(callID, lifecycle);
  }
  return lifecycles;
}
function latestEvent(left, right) {
  if (!left) return right;
  return Number((right == null ? void 0 : right.seq) || 0) > Number(left.seq || 0) ? right : left;
}
function terminalToolLifecycleEvent(lifecycle) {
  return latestEvent(lifecycle == null ? void 0 : lifecycle.decision, lifecycle == null ? void 0 : lifecycle.result) || null;
}
const stateMeta = Object.freeze({
  open: { label: "已熔断", rank: 4 },
  half_open: { label: "恢复探测", rank: 3 },
  saturated: { label: "并发已满", rank: 2 },
  closed: { label: "正常", rank: 1 },
  untracked: { label: "未运行", rank: 0 }
});
const failureLabels = Object.freeze({
  authentication: "认证失败",
  canceled: "调用取消",
  protocol: "协议错误",
  rate_limited: "远端限流",
  timeout: "调用超时",
  transport: "传输失败",
  unavailable: "服务不可用",
  unknown: "未知失败"
});
function groupMCPRuntimeStates(states = []) {
  return states.reduce((grouped, state) => {
    const serverID = String((state == null ? void 0 : state.server_id) || "").trim();
    if (!serverID || !stateMeta[state == null ? void 0 : state.state]) return grouped;
    (grouped[serverID] || (grouped[serverID] = [])).push(state);
    return grouped;
  }, {});
}
function summarizeMCPRuntimeStates(states = []) {
  const normalized = states.filter((state) => stateMeta[state == null ? void 0 : state.state]);
  if (!normalized.length) return { state: "untracked", label: stateMeta.untracked.label, count: 0 };
  const worst = normalized.reduce((current, state) => stateMeta[state.state].rank > stateMeta[current.state].rank ? state : current);
  return {
    state: worst.state,
    label: stateMeta[worst.state].label,
    count: normalized.length
  };
}
function mcpRuntimeStateLabel(state) {
  var _a2;
  return ((_a2 = stateMeta[state]) == null ? void 0 : _a2.label) || stateMeta.untracked.label;
}
function mcpRuntimeFailureLabel(failureClass) {
  return failureLabels[failureClass] || "";
}
const providerErrorDescriptions = Object.freeze({
  auth: "模型服务认证失败，请检查 Provider 凭据或联系管理员。",
  rate_limit: "模型服务请求过多或额度受限，请稍后重试。",
  context_length: "当前对话内容超过模型上下文上限，请压缩上下文或新建任务。",
  timeout: "模型服务响应超时，请稍后重试。",
  server: "模型服务暂时不可用，请稍后重试。",
  invalid_request: "模型服务无法处理当前请求，请检查模型与请求配置。",
  unknown: "模型请求失败，请根据原始错误排查。"
});
function positiveNumber(value) {
  const number2 = Number(value || 0);
  return Number.isFinite(number2) && number2 > 0 ? number2 : 0;
}
function providerErrorPresentation(error, fallbackMessage = "") {
  const source = error && typeof error === "object" && !Array.isArray(error) ? error : {};
  const errorClass = String(source.class || "unknown").trim().toLowerCase() || "unknown";
  const description2 = providerErrorDescriptions[errorClass] || providerErrorDescriptions.unknown;
  const original = String(source.message || fallbackMessage || "Provider 未返回错误详情。").replace(/\s+/g, " ").trim();
  const metadata = [];
  const statusCode = positiveNumber(source.status_code);
  const attempts = positiveNumber(source.attempts);
  const retryAfterMS = positiveNumber(source.retry_after_ms);
  if (statusCode) metadata.push(`HTTP ${statusCode}`);
  if (attempts) metadata.push(`已尝试 ${attempts} 次`);
  if (retryAfterMS) metadata.push(`建议等待 ${Math.ceil(retryAfterMS / 1e3)} 秒`);
  const suffix = metadata.length ? `（${metadata.join("，")}）` : "";
  return {
    description: description2,
    original,
    detail: `${description2} 原始错误：${original}${suffix}`
  };
}
function payload$1(event) {
  return (event == null ? void 0 : event.payload) && typeof event.payload === "object" ? event.payload : {};
}
function data(event) {
  const value = payload$1(event).data;
  return value && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function isReasoningChunk(event) {
  return (event == null ? void 0 : event.type) === "runtime.llm_chunk" && data(event).type === "reasoning";
}
function reasoningGroupKey(event) {
  const eventPayload = payload$1(event);
  const eventData2 = data(event);
  return `${String(eventPayload.turn_id || event.turn_id || "")}:${Number(eventData2.tool_round || 0)}`;
}
function mergeReasoningChunks(events2) {
  const merged = [];
  for (const event of events2 || []) {
    const previous2 = merged[merged.length - 1];
    if (!isReasoningChunk(event) || !isReasoningChunk(previous2) || reasoningGroupKey(event) !== reasoningGroupKey(previous2)) {
      merged.push(event);
      continue;
    }
    const previousPayload = payload$1(previous2);
    const previousData = data(previous2);
    const currentData = data(event);
    merged[merged.length - 1] = {
      ...previous2,
      payload: {
        ...previousPayload,
        data: {
          ...previousData,
          text: `${String(previousData.text || "")}${String(currentData.text || "")}`,
          end_index: currentData.index ?? previousData.end_index ?? previousData.index
        }
      }
    };
  }
  return merged;
}
const focusableSelector = [
  "button:not([disabled])",
  "input:not([disabled])",
  "select:not([disabled])",
  "textarea:not([disabled])",
  "a[href]",
  "[tabindex]:not([tabindex='-1'])"
].join(",");
function useDialogRequest(service) {
  const [request, setRequest] = reactExports.useState(() => service.getActive());
  reactExports.useEffect(() => service.subscribe(setRequest), [service]);
  return request;
}
function DialogFrame({ children, description: description2, onCancel, request, title }) {
  const dialogRef = reactExports.useRef(null);
  const onCancelRef = reactExports.useRef(onCancel);
  const titleID = reactExports.useId();
  const descriptionID = reactExports.useId();
  const dismissible2 = request.options.dismissible !== false;
  onCancelRef.current = onCancel;
  reactExports.useEffect(() => {
    const previousFocus = document.activeElement;
    const dialog = dialogRef.current;
    document.body.classList.add("workbench-dialog-open");
    const focusFrame = window.requestAnimationFrame(() => {
      var _a2;
      const preferred = dialog == null ? void 0 : dialog.querySelector("[data-dialog-autofocus]");
      const first = preferred || (dialog == null ? void 0 : dialog.querySelector(focusableSelector));
      (_a2 = first || dialog) == null ? void 0 : _a2.focus();
    });
    const handleKeyDown = (event) => {
      if (event.key === "Escape" && dismissible2) {
        event.preventDefault();
        onCancelRef.current();
        return;
      }
      if (event.key !== "Tab" || !dialog) return;
      const focusable = [...dialog.querySelectorAll(focusableSelector)];
      if (!focusable.length) {
        event.preventDefault();
        dialog.focus();
        return;
      }
      const first = focusable[0];
      const last = focusable[focusable.length - 1];
      if (event.shiftKey && document.activeElement === first) {
        event.preventDefault();
        last.focus();
      } else if (!event.shiftKey && document.activeElement === last) {
        event.preventDefault();
        first.focus();
      }
    };
    document.addEventListener("keydown", handleKeyDown);
    return () => {
      window.cancelAnimationFrame(focusFrame);
      document.removeEventListener("keydown", handleKeyDown);
      document.body.classList.remove("workbench-dialog-open");
      if (previousFocus instanceof HTMLElement && previousFocus.isConnected) previousFocus.focus();
    };
  }, [dismissible2, request.id]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: "workbench-dialog-backdrop",
      "data-dialog-kind": request.kind,
      onMouseDown: (event) => {
        if (dismissible2 && event.target === event.currentTarget) onCancel();
      },
      role: "presentation",
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "section",
        {
          "aria-describedby": description2 ? descriptionID : void 0,
          "aria-labelledby": titleID,
          "aria-modal": "true",
          className: `workbench-dialog workbench-dialog-${request.kind}`,
          ref: dialogRef,
          role: request.kind === "confirm" && request.options.tone === "danger" ? "alertdialog" : "dialog",
          tabIndex: -1,
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "workbench-dialog-header", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { id: titleID, children: title }),
                description2 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { id: descriptionID, children: description2 }) : null
              ] }),
              dismissible2 ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "workbench-dialog-close", type: "button", "aria-label": "关闭", title: "关闭", onClick: onCancel, children: "×" }) : null
            ] }),
            children
          ]
        }
      )
    }
  );
}
function ConfirmDialog({ request, service }) {
  const { options } = request;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    DialogFrame,
    {
      description: options.description,
      onCancel: () => service.cancel(request.id),
      request,
      title: options.title,
      children: [
        options.detail ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "workbench-dialog-detail", children: options.detail }) : null,
        /* @__PURE__ */ jsxRuntimeExports.jsxs("footer", { className: "workbench-dialog-actions", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => service.cancel(request.id), children: options.cancelLabel }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              className: `workbench-dialog-confirm ${options.tone}`,
              "data-dialog-autofocus": true,
              type: "button",
              onClick: () => service.resolve(request.id, true),
              children: options.confirmLabel
            }
          )
        ] })
      ]
    }
  );
}
function fieldInitialValue(schema, value) {
  if (value !== void 0) return value;
  if (schema.default !== void 0) return schema.default;
  if (schema.type === "boolean") return false;
  return "";
}
function FormDialog({ request, service }) {
  const { options } = request;
  const properties = options.schema.properties || {};
  const required = new Set(options.schema.required || []);
  const [values, setValues] = reactExports.useState(() => Object.fromEntries(
    Object.entries(properties).map(([name2, schema]) => [name2, fieldInitialValue(schema, options.initialValues[name2])])
  ));
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    DialogFrame,
    {
      description: options.description,
      onCancel: () => service.cancel(request.id),
      request,
      title: options.title,
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "form",
        {
          className: "workbench-dialog-form",
          onSubmit: (event) => {
            event.preventDefault();
            service.resolve(request.id, values);
          },
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "workbench-dialog-fields", children: Object.entries(properties).map(([name2, schema], index2) => {
              const label = schema.title || name2;
              const shared = {
                id: `dialog-field-${request.id}-${name2}`,
                name: name2,
                required: required.has(name2),
                value: values[name2],
                onChange: (event) => {
                  const next = schema.type === "boolean" ? event.target.checked : event.target.value;
                  setValues((current) => ({ ...current, [name2]: next }));
                }
              };
              return /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: `workbench-dialog-field ${schema.type === "boolean" ? "boolean" : ""}`, htmlFor: shared.id, children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                  label,
                  required.has(name2) ? " *" : ""
                ] }),
                Array.isArray(schema.enum) ? /* @__PURE__ */ jsxRuntimeExports.jsx("select", { ...shared, "data-dialog-autofocus": index2 === 0 ? "true" : void 0, children: schema.enum.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: option, children: String(option) }, String(option))) }) : schema.type === "boolean" ? /* @__PURE__ */ jsxRuntimeExports.jsx("input", { ...shared, checked: Boolean(values[name2]), type: "checkbox", value: void 0 }) : schema.format === "textarea" ? /* @__PURE__ */ jsxRuntimeExports.jsx("textarea", { ...shared, "data-dialog-autofocus": index2 === 0 ? "true" : void 0, placeholder: schema.description || "" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "input",
                  {
                    ...shared,
                    "data-dialog-autofocus": index2 === 0 ? "true" : void 0,
                    placeholder: schema.description || "",
                    type: ["integer", "number"].includes(schema.type) ? "number" : "text"
                  }
                )
              ] }, name2);
            }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("footer", { className: "workbench-dialog-actions", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => service.cancel(request.id), children: options.cancelLabel }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", children: options.submitLabel })
            ] })
          ]
        }
      )
    }
  );
}
function ChoiceDialog({ request, service }) {
  const { options } = request;
  const [query, setQuery] = reactExports.useState("");
  const [selected, setSelected] = reactExports.useState(options.initialValue);
  const filteredItems = reactExports.useMemo(() => {
    const normalized = query.trim().toLocaleLowerCase();
    if (!normalized) return options.items;
    return options.items.filter((item) => `${item.label}
${item.description}
${item.keywords}`.toLocaleLowerCase().includes(normalized));
  }, [options.items, query]);
  reactExports.useEffect(() => {
    var _a2;
    if (filteredItems.some((item) => item.value === selected && !item.disabled)) return;
    setSelected(((_a2 = filteredItems.find((item) => !item.disabled)) == null ? void 0 : _a2.value) || "");
  }, [filteredItems, selected]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    DialogFrame,
    {
      description: options.description,
      onCancel: () => service.cancel(request.id),
      request,
      title: options.title,
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "form",
        {
          className: "workbench-dialog-choice-form",
          onSubmit: (event) => {
            event.preventDefault();
            if (selected) service.resolve(request.id, selected);
          },
          children: [
            options.searchable ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "workbench-dialog-choice-search", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                "aria-label": "搜索选项",
                "data-dialog-autofocus": true,
                onChange: (event) => setQuery(event.target.value),
                placeholder: options.searchPlaceholder,
                type: "search",
                value: query
              }
            ) }) : null,
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "aria-label": options.title, className: "workbench-dialog-choice-list", role: "radiogroup", children: filteredItems.length ? filteredItems.map((item, index2) => /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: `workbench-dialog-choice-item ${selected === item.value ? "selected" : ""} ${item.disabled ? "disabled" : ""}`, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "input",
                {
                  checked: selected === item.value,
                  "data-dialog-autofocus": !options.searchable && index2 === 0 ? "true" : void 0,
                  disabled: item.disabled,
                  name: `choice-${request.id}`,
                  onChange: () => setSelected(item.value),
                  type: "radio",
                  value: item.value
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: item.label }),
                item.description ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: item.description }) : null
              ] })
            ] }, item.value)) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "workbench-dialog-choice-empty", children: options.emptyMessage }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("footer", { className: "workbench-dialog-actions", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => service.cancel(request.id), children: options.cancelLabel }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { disabled: !selected, type: "submit", children: options.submitLabel })
            ] })
          ]
        }
      )
    }
  );
}
function CustomDialog({ request, service }) {
  const Renderer = request.options.renderer;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    DialogFrame,
    {
      description: request.options.description,
      onCancel: () => service.cancel(request.id),
      request,
      title: request.options.title,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "workbench-dialog-custom", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Renderer,
        {
          cancel: () => service.cancel(request.id),
          close: (result) => service.resolve(request.id, result),
          input: request.options.input
        }
      ) })
    }
  );
}
function DialogHost({ service }) {
  const request = useDialogRequest(service);
  if (!request) return null;
  let dialog = null;
  if (request.kind === "confirm") dialog = /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog, { request, service });
  if (request.kind === "form") dialog = /* @__PURE__ */ jsxRuntimeExports.jsx(FormDialog, { request, service }, request.id);
  if (request.kind === "choice") dialog = /* @__PURE__ */ jsxRuntimeExports.jsx(ChoiceDialog, { request, service }, request.id);
  if (request.kind === "custom") dialog = /* @__PURE__ */ jsxRuntimeExports.jsx(CustomDialog, { request, service });
  return dialog ? reactDomExports.createPortal(dialog, document.body) : null;
}
const dialogIDPattern = /^[a-z][a-z0-9_-]*(?:\.[a-z][a-z0-9_-]*)+$/;
const confirmTones = /* @__PURE__ */ new Set(["default", "warning", "danger"]);
class DialogServiceError extends Error {
  constructor(code2, message) {
    super(message);
    this.name = "DialogServiceError";
    this.code = code2;
  }
}
function plainObject$1(value, field) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new DialogServiceError("invalid_options", `${field} must be an object`);
  }
  return value;
}
function requiredText$2(value, field) {
  const normalized = typeof value === "string" ? value.trim() : "";
  if (!normalized) {
    throw new DialogServiceError("invalid_options", `${field} must be a non-empty string`);
  }
  return normalized;
}
function optionalText$2(value, field) {
  if (value === void 0 || value === null || value === "") return "";
  return requiredText$2(value, field);
}
function dismissible(value) {
  return value === void 0 ? true : Boolean(value);
}
function confirmOptions(input) {
  const value = plainObject$1(input, "confirm options");
  const tone = optionalText$2(value.tone, "confirm options.tone") || "default";
  if (!confirmTones.has(tone)) {
    throw new DialogServiceError("invalid_options", "confirm options.tone must be default, warning, or danger");
  }
  return Object.freeze({
    title: requiredText$2(value.title, "confirm options.title"),
    description: optionalText$2(value.description, "confirm options.description"),
    detail: optionalText$2(value.detail, "confirm options.detail"),
    confirmLabel: optionalText$2(value.confirmLabel, "confirm options.confirmLabel") || "确认",
    cancelLabel: optionalText$2(value.cancelLabel, "confirm options.cancelLabel") || "取消",
    tone,
    dismissible: dismissible(value.dismissible)
  });
}
function formOptions(input) {
  const value = plainObject$1(input, "form options");
  const schema = plainObject$1(value.schema, "form options.schema");
  const initialValues = value.initialValues === void 0 ? {} : plainObject$1(value.initialValues, "form options.initialValues");
  return Object.freeze({
    title: requiredText$2(value.title, "form options.title"),
    description: optionalText$2(value.description, "form options.description"),
    schema,
    initialValues,
    submitLabel: optionalText$2(value.submitLabel, "form options.submitLabel") || "保存",
    cancelLabel: optionalText$2(value.cancelLabel, "form options.cancelLabel") || "取消",
    dismissible: dismissible(value.dismissible)
  });
}
function choiceOptions(input) {
  const value = plainObject$1(input, "choice options");
  if (!Array.isArray(value.items) || !value.items.length) {
    throw new DialogServiceError("invalid_options", "choice options.items must be a non-empty array");
  }
  const seen = /* @__PURE__ */ new Set();
  const items = value.items.map((item, index2) => {
    const option = plainObject$1(item, `choice options.items[${index2}]`);
    const optionValue = requiredText$2(option.value, `choice options.items[${index2}].value`);
    if (seen.has(optionValue)) {
      throw new DialogServiceError("invalid_options", `choice option value ${optionValue} is duplicated`);
    }
    seen.add(optionValue);
    return Object.freeze({
      value: optionValue,
      label: requiredText$2(option.label, `choice options.items[${index2}].label`),
      description: optionalText$2(option.description, `choice options.items[${index2}].description`),
      keywords: optionalText$2(option.keywords, `choice options.items[${index2}].keywords`),
      disabled: Boolean(option.disabled)
    });
  });
  const enabled = items.filter((item) => !item.disabled);
  if (!enabled.length) {
    throw new DialogServiceError("invalid_options", "choice options.items must include an enabled item");
  }
  const requestedInitialValue = optionalText$2(value.initialValue, "choice options.initialValue");
  const initialValue = enabled.some((item) => item.value === requestedInitialValue) ? requestedInitialValue : enabled[0].value;
  return Object.freeze({
    title: requiredText$2(value.title, "choice options.title"),
    description: optionalText$2(value.description, "choice options.description"),
    items: Object.freeze(items),
    initialValue,
    searchable: value.searchable === void 0 ? items.length > 8 : Boolean(value.searchable),
    searchPlaceholder: optionalText$2(value.searchPlaceholder, "choice options.searchPlaceholder") || "搜索...",
    emptyMessage: optionalText$2(value.emptyMessage, "choice options.emptyMessage") || "没有匹配项",
    submitLabel: optionalText$2(value.submitLabel, "choice options.submitLabel") || "选择",
    cancelLabel: optionalText$2(value.cancelLabel, "choice options.cancelLabel") || "取消",
    dismissible: dismissible(value.dismissible)
  });
}
function customOptions(dialogID, renderer, input, options) {
  const value = options === void 0 ? {} : plainObject$1(options, "dialog options");
  return Object.freeze({
    dialogID,
    renderer,
    input,
    title: optionalText$2(value.title, "dialog options.title") || dialogID,
    description: optionalText$2(value.description, "dialog options.description"),
    dismissible: dismissible(value.dismissible)
  });
}
class DialogService {
  constructor() {
    this.active = null;
    this.counter = 0;
    this.listeners = /* @__PURE__ */ new Set();
    this.queue = [];
    this.renderers = /* @__PURE__ */ new Map();
  }
  subscribe(listener) {
    if (typeof listener !== "function") {
      throw new DialogServiceError("invalid_listener", "dialog listener must be a function");
    }
    this.listeners.add(listener);
    listener(this.active);
    return () => this.listeners.delete(listener);
  }
  getActive() {
    return this.active;
  }
  confirm(options) {
    return this.enqueue("confirm", confirmOptions(options));
  }
  form(options) {
    return this.enqueue("form", formOptions(options));
  }
  choice(options) {
    return this.enqueue("choice", choiceOptions(options));
  }
  register(dialogID, renderer) {
    const normalizedID = requiredText$2(dialogID, "dialog id");
    if (!dialogIDPattern.test(normalizedID)) {
      throw new DialogServiceError("invalid_dialog_id", "dialog id must be a lowercase namespaced identifier");
    }
    if (typeof renderer !== "function") {
      throw new DialogServiceError("invalid_renderer", "dialog renderer must be a function");
    }
    if (this.renderers.has(normalizedID)) {
      throw new DialogServiceError("duplicate_dialog", `dialog ${normalizedID} is already registered`);
    }
    this.renderers.set(normalizedID, renderer);
    return () => {
      if (this.renderers.get(normalizedID) === renderer) this.renderers.delete(normalizedID);
    };
  }
  open(dialogID, input, options) {
    const normalizedID = requiredText$2(dialogID, "dialog id");
    const renderer = this.renderers.get(normalizedID);
    if (!renderer) {
      return Promise.reject(new DialogServiceError("unknown_dialog", `dialog ${normalizedID} is not registered`));
    }
    return this.enqueue("custom", customOptions(normalizedID, renderer, input, options));
  }
  resolve(requestID, result) {
    if (!this.active || this.active.id !== requestID) return false;
    const request = this.active;
    this.active = null;
    request.resolve(result);
    this.advance();
    return true;
  }
  cancel(requestID) {
    if (!this.active || this.active.id !== requestID) return false;
    return this.resolve(requestID, this.active.kind === "confirm" ? false : void 0);
  }
  dispose(reason = "dialog service disposed") {
    const error = new DialogServiceError("disposed", reason);
    const requests = [this.active, ...this.queue].filter(Boolean);
    this.active = null;
    this.queue = [];
    requests.forEach((request) => request.reject(error));
    this.notify();
  }
  enqueue(kind, options) {
    return new Promise((resolve, reject) => {
      this.counter += 1;
      this.queue.push(Object.freeze({
        id: `dialog_${this.counter}`,
        kind,
        options,
        resolve,
        reject
      }));
      this.advance();
    });
  }
  advance() {
    if (!this.active) this.active = this.queue.shift() || null;
    this.notify();
  }
  notify() {
    this.listeners.forEach((listener) => listener(this.active));
  }
}
function createDialogService() {
  return new DialogService();
}
function useNotifications(service) {
  const [notifications, setNotifications] = reactExports.useState(() => service.getSnapshot());
  reactExports.useEffect(() => service.subscribe(setNotifications), [service]);
  return notifications;
}
function NotificationItem({ notification, service }) {
  const [paused, setPaused] = reactExports.useState(false);
  reactExports.useEffect(() => {
    if (paused || notification.durationMs <= 0) return void 0;
    const timer = window.setTimeout(() => service.dismiss(notification.id), notification.durationMs);
    return () => window.clearTimeout(timer);
  }, [notification.createdAt, notification.durationMs, notification.id, paused, service]);
  const assertive = notification.level === "error" || notification.level === "warning";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "article",
    {
      className: `workbench-notification ${notification.level}`,
      onMouseEnter: () => setPaused(true),
      onMouseLeave: () => setPaused(false),
      role: assertive ? "alert" : "status",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "workbench-notification-accent", "aria-hidden": "true" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "workbench-notification-copy", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: notification.title }),
          notification.message ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: notification.message }) : null
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            className: "workbench-notification-close",
            type: "button",
            "aria-label": "关闭通知",
            title: "关闭通知",
            onClick: () => service.dismiss(notification.id),
            children: "×"
          }
        )
      ]
    }
  );
}
function NotificationHost({ service }) {
  const notifications = useNotifications(service);
  if (!notifications.length) return null;
  const visible = notifications.slice(-4);
  return reactDomExports.createPortal(
    /* @__PURE__ */ jsxRuntimeExports.jsx("section", { className: "workbench-notification-region", "aria-label": "通知", children: visible.map((notification) => /* @__PURE__ */ jsxRuntimeExports.jsx(NotificationItem, { notification, service }, notification.id)) }),
    document.body
  );
}
const NOTIFICATION_LEVELS = Object.freeze(["info", "success", "warning", "error"]);
const defaultDurations = Object.freeze({
  info: 5e3,
  success: 4e3,
  warning: 8e3,
  error: 0
});
class NotificationServiceError extends TypeError {
  constructor(field, message) {
    super(`${field}: ${message}`);
    this.name = "NotificationServiceError";
    this.field = field;
  }
}
function plainObject(value, field) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new NotificationServiceError(field, "must be an object");
  }
  return value;
}
function requiredText$1(value, field) {
  const normalized = typeof value === "string" ? value.trim() : "";
  if (!normalized) throw new NotificationServiceError(field, "must be a non-empty string");
  return normalized;
}
function optionalText$1(value, field) {
  if (value === void 0 || value === null || value === "") return "";
  return requiredText$1(value, field);
}
function normalizeNotification(input, id2, now) {
  const value = plainObject(input, "notification");
  const level = optionalText$1(value.level, "notification.level") || "info";
  if (!NOTIFICATION_LEVELS.includes(level)) {
    throw new NotificationServiceError("notification.level", `must be one of ${NOTIFICATION_LEVELS.join(", ")}`);
  }
  const durationMs = value.durationMs === void 0 ? defaultDurations[level] : Number(value.durationMs);
  if (!Number.isFinite(durationMs) || durationMs < 0) {
    throw new NotificationServiceError("notification.durationMs", "must be a non-negative finite number");
  }
  return Object.freeze({
    id: id2,
    level,
    title: requiredText$1(value.title, "notification.title"),
    message: optionalText$1(value.message, "notification.message"),
    dedupeKey: optionalText$1(value.dedupeKey, "notification.dedupeKey"),
    durationMs,
    createdAt: now
  });
}
class NotificationService {
  constructor(options = {}) {
    this.counter = 0;
    this.items = Object.freeze([]);
    this.listeners = /* @__PURE__ */ new Set();
    this.now = typeof options.now === "function" ? options.now : () => Date.now();
  }
  subscribe(listener) {
    if (typeof listener !== "function") {
      throw new NotificationServiceError("listener", "must be a function");
    }
    this.listeners.add(listener);
    listener(this.items);
    return () => this.listeners.delete(listener);
  }
  getSnapshot() {
    return this.items;
  }
  show(input) {
    const dedupeKey = typeof (input == null ? void 0 : input.dedupeKey) === "string" ? input.dedupeKey.trim() : "";
    const existing = dedupeKey ? this.items.find((item) => item.dedupeKey === dedupeKey) : null;
    if (!existing) this.counter += 1;
    const id2 = (existing == null ? void 0 : existing.id) || `notification_${this.counter}`;
    const notification = normalizeNotification(input, id2, this.now());
    this.items = Object.freeze(existing ? this.items.map((item) => item.id === existing.id ? notification : item) : [...this.items, notification]);
    this.notify();
    return id2;
  }
  dismiss(notificationID) {
    const id2 = String(notificationID || "").trim();
    if (!id2 || !this.items.some((item) => item.id === id2)) return false;
    this.items = Object.freeze(this.items.filter((item) => item.id !== id2));
    this.notify();
    return true;
  }
  clear() {
    if (!this.items.length) return false;
    this.items = Object.freeze([]);
    this.notify();
    return true;
  }
  notify() {
    this.listeners.forEach((listener) => listener(this.items));
  }
}
function createNotificationService(options) {
  return new NotificationService(options);
}
const identifierPattern = /^[a-z][a-z0-9_-]*(?:\.[a-z][a-z0-9_-]*)+$/;
const semverPattern$1 = /^\d+\.\d+\.\d+(?:-[0-9A-Za-z.-]+)?(?:\+[0-9A-Za-z.-]+)?$/;
const RESOURCE_REF_TYPES = Object.freeze([
  "file",
  "artifact",
  "task",
  "session",
  "url",
  "business_object"
]);
const COMMAND_RISKS = Object.freeze([
  "read",
  "write",
  "exec",
  "external_effect"
]);
const PLUGIN_CONTEXT_SERVICE_METHODS = Object.freeze({
  permissions: Object.freeze(["has", "require"]),
  commands: Object.freeze(["register", "execute"]),
  dialog: Object.freeze(["confirm", "form", "choice", "open"]),
  notifications: Object.freeze(["show"]),
  resources: Object.freeze(["listRelated", "preview", "open"]),
  tasks: Object.freeze(["list"]),
  artifacts: Object.freeze(["list"]),
  http: Object.freeze(["request"])
});
class WorkbenchContractError extends TypeError {
  constructor(field, message) {
    super(`${field}: ${message}`);
    this.name = "WorkbenchContractError";
    this.field = field;
  }
}
function isPlainObject$1(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) return false;
  const prototype = Object.getPrototypeOf(value);
  return prototype === Object.prototype || prototype === null;
}
function requireObject(value, field) {
  if (!isPlainObject$1(value)) {
    throw new WorkbenchContractError(field, "must be a plain object");
  }
  return value;
}
function requireString(value, field) {
  const normalized = typeof value === "string" ? value.trim() : "";
  if (!normalized) {
    throw new WorkbenchContractError(field, "must be a non-empty string");
  }
  return normalized;
}
function optionalString$1(value, field) {
  if (value === void 0 || value === null || value === "") return void 0;
  return requireString(value, field);
}
function requireIdentifier(value, field) {
  const normalized = requireString(value, field);
  if (!identifierPattern.test(normalized)) {
    throw new WorkbenchContractError(field, "must be a lowercase namespaced identifier");
  }
  return normalized;
}
function stringList(value, field, options = {}) {
  if (value === void 0) return Object.freeze([]);
  if (!Array.isArray(value)) {
    throw new WorkbenchContractError(field, "must be an array of strings");
  }
  const unique = /* @__PURE__ */ new Set();
  value.forEach((item, index2) => {
    const normalized = requireString(item, `${field}[${index2}]`);
    if (options.namespaced && !identifierPattern.test(normalized)) {
      throw new WorkbenchContractError(`${field}[${index2}]`, "must be a lowercase namespaced identifier");
    }
    unique.add(normalized);
  });
  return Object.freeze([...unique]);
}
function cloneJSONValue(value, field) {
  if (value === null || typeof value === "string" || typeof value === "boolean") return value;
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (Array.isArray(value)) {
    return Object.freeze(value.map((item, index2) => cloneJSONValue(item, `${field}[${index2}]`)));
  }
  if (isPlainObject$1(value)) {
    const result = {};
    for (const [key, item] of Object.entries(value)) {
      result[key] = cloneJSONValue(item, `${field}.${key}`);
    }
    return Object.freeze(result);
  }
  throw new WorkbenchContractError(field, "must contain only JSON-compatible values");
}
function optionalSchema(value, field) {
  if (value === void 0) return void 0;
  requireObject(value, field);
  return cloneJSONValue(value, field);
}
function defineResourceRef(input) {
  const value = requireObject(input, "resource");
  const type = requireString(value.type, "resource.type");
  if (!RESOURCE_REF_TYPES.includes(type)) {
    throw new WorkbenchContractError("resource.type", `must be one of ${RESOURCE_REF_TYPES.join(", ")}`);
  }
  if (value.previewable !== void 0 && typeof value.previewable !== "boolean") {
    throw new WorkbenchContractError("resource.previewable", "must be a boolean");
  }
  const resource = {
    id: requireString(value.id, "resource.id"),
    type,
    title: requireString(value.title, "resource.title"),
    source: requireString(value.source, "resource.source"),
    metadata: cloneJSONValue(value.metadata ?? {}, "resource.metadata")
  };
  const mimeType = optionalString$1(value.mimeType, "resource.mimeType");
  if (mimeType !== void 0) resource.mimeType = mimeType;
  if (value.previewable !== void 0) resource.previewable = value.previewable;
  return Object.freeze(resource);
}
function defineCommand(input) {
  const value = requireObject(input, "command");
  const risk = value.risk === void 0 ? "read" : requireString(value.risk, "command.risk");
  if (!COMMAND_RISKS.includes(risk)) {
    throw new WorkbenchContractError("command.risk", `must be one of ${COMMAND_RISKS.join(", ")}`);
  }
  const command = {
    id: requireIdentifier(value.id, "command.id"),
    title: requireString(value.title, "command.title"),
    risk,
    requiredPermissions: stringList(value.requiredPermissions, "command.requiredPermissions", { namespaced: true }),
    contexts: stringList(value.contexts, "command.contexts", { namespaced: true })
  };
  const inputSchema = optionalSchema(value.inputSchema, "command.inputSchema");
  const outputSchema = optionalSchema(value.outputSchema, "command.outputSchema");
  if (inputSchema !== void 0) command.inputSchema = inputSchema;
  if (outputSchema !== void 0) command.outputSchema = outputSchema;
  return Object.freeze(command);
}
function requireService(context, serviceName, methods) {
  const service = context[serviceName];
  if (!service || typeof service !== "object" && typeof service !== "function" || Array.isArray(service)) {
    throw new WorkbenchContractError(`context.${serviceName}`, "must be a service object");
  }
  for (const method of methods) {
    if (typeof service[method] !== "function") {
      throw new WorkbenchContractError(`context.${serviceName}.${method}`, "must be a function");
    }
  }
  return service;
}
function definePluginContext(input) {
  const value = requireObject(input, "context");
  const pluginInput = requireObject(value.plugin, "context.plugin");
  const scopeInput = requireObject(value.scope, "context.scope");
  const version2 = requireString(pluginInput.version, "context.plugin.version");
  if (!semverPattern$1.test(version2)) {
    throw new WorkbenchContractError("context.plugin.version", "must be a semantic version");
  }
  const plugin2 = Object.freeze({
    id: requireIdentifier(pluginInput.id, "context.plugin.id"),
    version: version2
  });
  const scope = {
    workspaceId: requireString(scopeInput.workspaceId, "context.scope.workspaceId"),
    userId: requireString(scopeInput.userId, "context.scope.userId"),
    roles: stringList(scopeInput.roles, "context.scope.roles")
  };
  const organizationId = optionalString$1(scopeInput.organizationId, "context.scope.organizationId");
  if (organizationId !== void 0) scope.organizationId = organizationId;
  const context = {
    plugin: plugin2,
    scope: Object.freeze(scope)
  };
  for (const [serviceName, methods] of Object.entries(PLUGIN_CONTEXT_SERVICE_METHODS)) {
    context[serviceName] = requireService(value, serviceName, methods);
  }
  return Object.freeze(context);
}
class RelatedResourceServiceError extends Error {
  constructor(code2, message, cause) {
    super(message, cause ? { cause } : void 0);
    this.name = "RelatedResourceServiceError";
    this.code = code2;
  }
}
function requireProvider(provider) {
  if (!provider || typeof provider !== "object" || Array.isArray(provider)) {
    throw new RelatedResourceServiceError("invalid_provider", "Resource provider must be an object.");
  }
  const id2 = typeof provider.id === "string" ? provider.id.trim() : "";
  if (!id2) {
    throw new RelatedResourceServiceError("invalid_provider", "Resource provider id is required.");
  }
  if (typeof provider.supports !== "function" && !(typeof provider.sourcePrefix === "string" && provider.sourcePrefix)) {
    throw new RelatedResourceServiceError("invalid_provider", `Resource provider ${id2} must define supports() or sourcePrefix.`);
  }
  return { ...provider, id: id2 };
}
function normalizePreviewDescriptor(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    throw new RelatedResourceServiceError("invalid_preview", "Preview provider returned an invalid descriptor.");
  }
  if (!["image", "text", "download"].includes(value.kind)) {
    throw new RelatedResourceServiceError("invalid_preview", "Preview kind must be image, text, or download.");
  }
  if (value.kind === "image" && !(typeof value.objectUrl === "string" && value.objectUrl)) {
    throw new RelatedResourceServiceError("invalid_preview", "Image previews require objectUrl.");
  }
  if (value.kind === "text" && typeof value.text !== "string") {
    throw new RelatedResourceServiceError("invalid_preview", "Text previews require text.");
  }
  if (value.dispose !== void 0 && typeof value.dispose !== "function") {
    throw new RelatedResourceServiceError("invalid_preview", "Preview dispose must be a function.");
  }
  return Object.freeze({
    kind: value.kind,
    contentType: typeof value.contentType === "string" ? value.contentType : "",
    text: typeof value.text === "string" ? value.text : "",
    objectUrl: typeof value.objectUrl === "string" ? value.objectUrl : "",
    downloadUrl: typeof value.downloadUrl === "string" ? value.downloadUrl : "",
    message: typeof value.message === "string" ? value.message : "",
    dispose: typeof value.dispose === "function" ? value.dispose : () => {
    }
  });
}
function disposePreview(descriptor) {
  if (!descriptor) return;
  try {
    descriptor.dispose();
  } catch {
  }
}
function cancelledError() {
  return new RelatedResourceServiceError("preview_cancelled", "Resource preview was cancelled.");
}
function isPreviewCancelledError(error) {
  return error instanceof RelatedResourceServiceError && error.code === "preview_cancelled";
}
function createRelatedResourceService() {
  const providers = /* @__PURE__ */ new Map();
  let previewSequence = 0;
  let pendingPreview = null;
  let pendingProviderID = "";
  let activePreview = null;
  let activeProviderID = "";
  function providerFor(resource) {
    for (const provider of providers.values()) {
      let supported = false;
      try {
        supported = typeof provider.supports === "function" ? Boolean(provider.supports(resource)) : resource.source.startsWith(provider.sourcePrefix);
      } catch (error) {
        throw new RelatedResourceServiceError("provider_failed", `Resource provider ${provider.id} failed while checking support.`, error);
      }
      if (supported) return provider;
    }
    throw new RelatedResourceServiceError("provider_not_found", `No resource provider supports ${resource.source}.`);
  }
  function releasePreview() {
    previewSequence += 1;
    if (pendingPreview) {
      pendingPreview.abort();
      pendingPreview = null;
    }
    pendingProviderID = "";
    disposePreview(activePreview);
    activePreview = null;
    activeProviderID = "";
  }
  function registerProvider(input) {
    const provider = requireProvider(input);
    if (providers.has(provider.id)) {
      throw new RelatedResourceServiceError("provider_exists", `Resource provider ${provider.id} is already registered.`);
    }
    providers.set(provider.id, provider);
    let registered = true;
    return () => {
      if (!registered) return false;
      registered = false;
      if (pendingProviderID === provider.id || activeProviderID === provider.id) releasePreview();
      providers.delete(provider.id);
      return true;
    };
  }
  async function listRelated(context = {}) {
    const resources = [];
    const seen = /* @__PURE__ */ new Set();
    for (const provider of providers.values()) {
      if (typeof provider.listRelated !== "function") continue;
      let listed;
      try {
        listed = await provider.listRelated(context);
      } catch (error) {
        throw new RelatedResourceServiceError("provider_failed", `Resource provider ${provider.id} failed to list resources.`, error);
      }
      if (!Array.isArray(listed)) {
        throw new RelatedResourceServiceError("invalid_resource_list", `Resource provider ${provider.id} must return an array.`);
      }
      for (const item of listed) {
        const resource = defineResourceRef(item);
        const key = `${resource.source}
${resource.id}`;
        if (seen.has(key)) continue;
        seen.add(key);
        resources.push(resource);
      }
    }
    return Object.freeze(resources);
  }
  async function preview(input, context = {}) {
    var _a2, _b;
    const resource = defineResourceRef(input);
    const provider = providerFor(resource);
    if (typeof provider.preview !== "function") {
      throw new RelatedResourceServiceError("preview_unsupported", `Resource provider ${provider.id} does not support preview.`);
    }
    releasePreview();
    const requestID = previewSequence;
    const controller = new AbortController();
    pendingPreview = controller;
    pendingProviderID = provider.id;
    const externalSignal = context == null ? void 0 : context.signal;
    const abortFromContext = () => controller.abort();
    if (externalSignal == null ? void 0 : externalSignal.aborted) controller.abort();
    else (_a2 = externalSignal == null ? void 0 : externalSignal.addEventListener) == null ? void 0 : _a2.call(externalSignal, "abort", abortFromContext, { once: true });
    let descriptor;
    try {
      const value = await provider.preview(resource, { ...context, signal: controller.signal });
      descriptor = normalizePreviewDescriptor(value);
      if (controller.signal.aborted || requestID !== previewSequence) {
        disposePreview(descriptor);
        descriptor = null;
        throw cancelledError();
      }
      activePreview = descriptor;
      activeProviderID = provider.id;
      return descriptor;
    } catch (error) {
      if (controller.signal.aborted || requestID !== previewSequence || (error == null ? void 0 : error.name) === "AbortError") {
        if (descriptor && descriptor !== activePreview) disposePreview(descriptor);
        throw cancelledError();
      }
      if (error instanceof RelatedResourceServiceError) throw error;
      throw new RelatedResourceServiceError("provider_failed", `Resource provider ${provider.id} failed to preview the resource.`, error);
    } finally {
      (_b = externalSignal == null ? void 0 : externalSignal.removeEventListener) == null ? void 0 : _b.call(externalSignal, "abort", abortFromContext);
      if (pendingPreview === controller) {
        pendingPreview = null;
        pendingProviderID = "";
      }
    }
  }
  async function open(input, context = {}) {
    const resource = defineResourceRef(input);
    const provider = providerFor(resource);
    if (typeof provider.open !== "function") {
      throw new RelatedResourceServiceError("open_unsupported", `Resource provider ${provider.id} does not support open.`);
    }
    try {
      return await provider.open(resource, context);
    } catch (error) {
      if (error instanceof RelatedResourceServiceError) throw error;
      throw new RelatedResourceServiceError("provider_failed", `Resource provider ${provider.id} failed to open the resource.`, error);
    }
  }
  function dispose() {
    releasePreview();
    providers.clear();
  }
  return Object.freeze({ registerProvider, listRelated, preview, open, releasePreview, dispose });
}
const SESSION_ARTIFACT_SOURCE_PREFIX = "tma.session-artifact:";
const MAX_ARTIFACT_PREVIEW_BYTES = 512 * 1024;
const MAX_ARTIFACT_PREVIEW_CHARACTERS = 64e3;
const imageExtensions = /* @__PURE__ */ new Set(["png", "jpg", "jpeg", "gif", "webp", "svg"]);
const textExtensions = /* @__PURE__ */ new Set([
  "txt",
  "md",
  "markdown",
  "json",
  "jsonl",
  "csv",
  "tsv",
  "log",
  "xml",
  "html",
  "htm",
  "css",
  "js",
  "jsx",
  "ts",
  "tsx",
  "go",
  "py",
  "sh",
  "yaml",
  "yml"
]);
function plainMetadata(artifact) {
  const value = artifact == null ? void 0 : artifact.metadata;
  return value && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function resourceExtension(resource) {
  const title = String((resource == null ? void 0 : resource.title) || "").toLowerCase();
  const index2 = title.lastIndexOf(".");
  return index2 >= 0 ? title.slice(index2 + 1) : "";
}
function previewKindForResource(resource, contentType = "") {
  const type = String(contentType || (resource == null ? void 0 : resource.mimeType) || "").toLowerCase();
  const extension2 = resourceExtension(resource);
  if (type.startsWith("image/") || imageExtensions.has(extension2)) return "image";
  if (type.startsWith("text/") || type.includes("json") || type.includes("xml") || textExtensions.has(extension2)) return "text";
  return "download";
}
function isMarkdownResource(resource, contentType = "") {
  const type = String(contentType || (resource == null ? void 0 : resource.mimeType) || "").toLowerCase();
  return type.includes("markdown") || ["md", "markdown"].includes(resourceExtension(resource));
}
function isHTMLResource(resource, contentType = "") {
  const type = String(contentType || (resource == null ? void 0 : resource.mimeType) || "").toLowerCase();
  return type.includes("text/html") || ["html", "htm"].includes(resourceExtension(resource));
}
function htmlPreviewDocument(value = "") {
  const html2 = String(value || "");
  const policy = `<meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src data: blob:; media-src data: blob:; style-src 'unsafe-inline'; font-src data:;">`;
  if (/<head(?:\s[^>]*)?>/i.test(html2)) {
    return html2.replace(/<head(\s[^>]*)?>/i, (match) => `${match}${policy}`);
  }
  if (/<html(?:\s[^>]*)?>/i.test(html2)) {
    return html2.replace(/<html(\s[^>]*)?>/i, (match) => `${match}<head>${policy}</head>`);
  }
  return `<!doctype html><html><head>${policy}</head><body>${html2}</body></html>`;
}
function allowlistedMetadata(artifact) {
  const input = plainMetadata(artifact);
  const output = {};
  const fields = ["size_bytes", "path", "file_path", "workspace_path"];
  for (const field of fields) {
    const value = input[field];
    if (typeof value === "string" && value) output[field] = value;
    if (field === "size_bytes" && typeof value === "number" && Number.isFinite(value) && value >= 0) output[field] = value;
  }
  const turnID = (artifact == null ? void 0 : artifact.turn_id) || input.turn_id;
  if (typeof turnID === "string" && turnID) output.turn_id = turnID;
  const objectRefID = (artifact == null ? void 0 : artifact.object_ref_id) || input.object_ref_id;
  if (typeof objectRefID === "string" && objectRefID) output.object_ref_id = objectRefID;
  return output;
}
function artifactToResourceRef(artifact, { sessionID } = {}) {
  if (!artifact || typeof artifact !== "object" || Array.isArray(artifact)) {
    throw new TypeError("Artifact must be an object.");
  }
  const normalizedSessionID = typeof sessionID === "string" ? sessionID.trim() : "";
  if (!normalizedSessionID) throw new TypeError("Session id is required.");
  const metadata = plainMetadata(artifact);
  const title = artifact.name || artifact.id;
  const resource = {
    id: artifact.id,
    type: artifact.artifact_type === "file" ? "file" : "artifact",
    title,
    source: `${SESSION_ARTIFACT_SOURCE_PREFIX}${normalizedSessionID}`,
    previewable: previewKindForResource({ title, mimeType: metadata.content_type }) !== "download",
    metadata: allowlistedMetadata(artifact)
  };
  if (typeof metadata.content_type === "string" && metadata.content_type.trim()) {
    resource.mimeType = metadata.content_type.trim();
  }
  return defineResourceRef(resource);
}
function sessionIDFromResource(resource) {
  if (!resource.source.startsWith(SESSION_ARTIFACT_SOURCE_PREFIX)) {
    throw new TypeError("Resource is not a Session Artifact.");
  }
  const sessionID = resource.source.slice(SESSION_ARTIFACT_SOURCE_PREFIX.length);
  if (!sessionID) throw new TypeError("Session Artifact source is missing a session id.");
  return sessionID;
}
function throwIfAborted(signal) {
  if (!(signal == null ? void 0 : signal.aborted)) return;
  const error = new Error("Preview aborted.");
  error.name = "AbortError";
  throw error;
}
function createSessionArtifactProvider(options = {}) {
  const downloadArtifact2 = options.downloadArtifact;
  const artifactDownloadPath2 = options.artifactDownloadPath;
  const createObjectURL = options.createObjectURL || ((blob) => URL.createObjectURL(blob));
  const revokeObjectURL = options.revokeObjectURL || ((url) => URL.revokeObjectURL(url));
  const openURL = options.openURL || ((url) => window.open(url, "_blank", "noopener,noreferrer"));
  const maxTextBytes = options.maxTextBytes || MAX_ARTIFACT_PREVIEW_BYTES;
  const maxTextCharacters = options.maxTextCharacters || MAX_ARTIFACT_PREVIEW_CHARACTERS;
  if (typeof downloadArtifact2 !== "function" || typeof artifactDownloadPath2 !== "function") {
    throw new TypeError("Session Artifact provider requires downloadArtifact and artifactDownloadPath.");
  }
  function downloadURL(resource) {
    return artifactDownloadPath2(sessionIDFromResource(resource), resource.id);
  }
  return Object.freeze({
    id: "tma.session-artifact",
    sourcePrefix: SESSION_ARTIFACT_SOURCE_PREFIX,
    listRelated(context = {}) {
      const artifacts2 = Array.isArray(context.artifacts) ? context.artifacts : [];
      return artifacts2.map((artifact) => artifactToResourceRef(artifact, { sessionID: context.sessionID }));
    },
    async preview(resource, context = {}) {
      const url = downloadURL(resource);
      const response = await downloadArtifact2(sessionIDFromResource(resource), resource.id, { signal: context.signal });
      throwIfAborted(context.signal);
      const contentType = response.headers.get("Content-Type") || resource.mimeType || "";
      const contentLength = Number(response.headers.get("Content-Length") || 0);
      const kind = previewKindForResource(resource, contentType);
      if (kind === "image") {
        const blob = await response.blob();
        throwIfAborted(context.signal);
        const objectUrl = createObjectURL(blob);
        let disposed = false;
        return {
          kind,
          contentType,
          objectUrl,
          downloadUrl: url,
          dispose() {
            if (disposed) return;
            disposed = true;
            revokeObjectURL(objectUrl);
          }
        };
      }
      if (kind === "text") {
        if (contentLength > maxTextBytes) {
          return { kind: "download", contentType, downloadUrl: url, message: "预览内容过大，请下载文件后查看。" };
        }
        let text2 = await response.text();
        throwIfAborted(context.signal);
        if (contentType.toLowerCase().includes("json")) {
          try {
            text2 = JSON.stringify(JSON.parse(text2), null, 2);
          } catch {
          }
        }
        const truncated = text2.length > maxTextCharacters;
        return {
          kind,
          contentType,
          text: truncated ? `${text2.slice(0, maxTextCharacters)}

[预览已截断]` : text2,
          downloadUrl: url
        };
      }
      return {
        kind: "download",
        contentType,
        downloadUrl: url,
        message: "暂不支持此文件类型的内联预览，请下载后查看。"
      };
    },
    open(resource) {
      const url = downloadURL(resource);
      openURL(url);
      return url;
    }
  });
}
class PermissionDeniedError extends Error {
  constructor(permissions2) {
    const denied = Object.freeze([...permissions2]);
    super(`Missing required permissions: ${denied.join(", ")}`);
    this.name = "PermissionDeniedError";
    this.code = "permission_denied";
    this.permissions = denied;
  }
}
function permissionList$1(value) {
  if (value === void 0 || value === null) return [];
  const values = Array.isArray(value) ? value : [value];
  return [...new Set(values.map((item) => typeof item === "string" ? item.trim() : "").filter(Boolean))];
}
function createPermissionService(options = {}) {
  const grants = new Set(permissionList$1(options.grants));
  async function has(value) {
    const required = permissionList$1(value);
    return required.every((permission) => grants.has(permission));
  }
  async function requirePermissions(value) {
    const required = permissionList$1(value);
    const denied = required.filter((permission) => !grants.has(permission));
    if (denied.length) throw new PermissionDeniedError(denied);
    return true;
  }
  return Object.freeze({ has, require: requirePermissions });
}
class PluginErrorBoundary extends React.Component {
  constructor(props) {
    super(props);
    this.state = { error: null };
  }
  static getDerivedStateFromError(error) {
    return { error };
  }
  componentDidUpdate(previousProps) {
    if (previousProps.resetKey !== this.props.resetKey && this.state.error) {
      this.setState({ error: null });
    }
  }
  componentDidCatch(error, info) {
    var _a2, _b;
    (_b = (_a2 = this.props).onError) == null ? void 0 : _b.call(_a2, error, info);
  }
  render() {
    if (!this.state.error) return this.props.children;
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "plugin-route-state error", role: "alert", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "插件页面暂时不可用" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: this.state.error.message || String(this.state.error) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: () => this.setState({ error: null }), children: "重试" })
    ] });
  }
}
function PermissionGate({ route, children }) {
  const [state, setState] = reactExports.useState({ status: "checking", error: null });
  reactExports.useEffect(() => {
    let active = true;
    const required = route.requiredPermissions || [];
    route.context.permissions.require(required).then(() => {
      if (active) setState({ status: "allowed", error: null });
    }).catch((error) => {
      if (active) setState({ status: "denied", error });
    });
    return () => {
      active = false;
    };
  }, [route]);
  if (state.status === "checking") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "plugin-route-state", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "正在检查访问权限..." }) });
  }
  if (state.status === "denied") {
    const denied = state.error instanceof PermissionDeniedError ? state.error.permissions.join("、") : "当前页面";
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "plugin-route-state denied", role: "alert", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "无权访问" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
        "缺少权限：",
        denied
      ] })
    ] });
  }
  return children;
}
function PluginRouteHost({ route, path: path2, loading, onBack, onError }) {
  if (loading) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "plugin-route-state", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "正在加载扩展..." }) });
  }
  if (!route) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "plugin-route-state error", role: "alert", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "扩展页面不存在或未启用" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: path2 }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: onBack, children: "返回工作台" })
    ] });
  }
  const Component = route.component;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "plugin-route-shell", "aria-label": route.title || route.id, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "plugin-route-header", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "扩展工作区" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: route.title || route.id })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: onBack, children: "返回工作台" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "plugin-route-body", children: /* @__PURE__ */ jsxRuntimeExports.jsx(PluginErrorBoundary, { resetKey: path2, onError, children: /* @__PURE__ */ jsxRuntimeExports.jsx(PermissionGate, { route, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Component, { context: route.context, route }) }) }) })
  ] });
}
class ExtensionRegistryError extends Error {
  constructor(code2, message, cause) {
    super(message, cause ? { cause } : void 0);
    this.name = "ExtensionRegistryError";
    this.code = code2;
  }
}
function createEmitter(getSnapshot) {
  const listeners = /* @__PURE__ */ new Set();
  return {
    emit() {
      const snapshot = getSnapshot();
      for (const listener of listeners) listener(snapshot);
    },
    subscribe(listener) {
      if (typeof listener !== "function") throw new ExtensionRegistryError("invalid_listener", "Registry listener must be a function.");
      listeners.add(listener);
      listener(getSnapshot());
      return () => listeners.delete(listener);
    }
  };
}
function pluginIDValue(value) {
  const pluginID = typeof value === "string" ? value.trim() : "";
  if (!pluginID) throw new ExtensionRegistryError("invalid_plugin", "Plugin id is required.");
  return pluginID;
}
function createNavigationRegistry(options = {}) {
  const allowedGroups = new Set(options.groups || ["workspace"]);
  const entries = /* @__PURE__ */ new Map();
  const snapshot = () => Object.freeze([...entries.values()].sort((left, right) => (left.order ?? 1e3) - (right.order ?? 1e3) || left.title.localeCompare(right.title)));
  const emitter = createEmitter(snapshot);
  function register(pluginIDInput, input) {
    const pluginID = pluginIDValue(pluginIDInput);
    if (!input || typeof input !== "object" || Array.isArray(input)) {
      throw new ExtensionRegistryError("invalid_navigation", "Navigation contribution must be an object.");
    }
    if (!allowedGroups.has(input.group)) {
      throw new ExtensionRegistryError("navigation_group_denied", `Navigation group ${input.group} is not available.`);
    }
    const key = `${pluginID}:${input.id}`;
    if (entries.has(key)) throw new ExtensionRegistryError("navigation_exists", `Navigation contribution ${key} already exists.`);
    const entry2 = Object.freeze({
      pluginID,
      id: input.id,
      group: input.group,
      title: input.title,
      route: input.route,
      order: input.order
    });
    entries.set(key, entry2);
    emitter.emit();
    let registered = true;
    return () => {
      if (!registered) return false;
      registered = false;
      entries.delete(key);
      emitter.emit();
      return true;
    };
  }
  function list2(filters = {}) {
    const values = snapshot();
    return filters.group ? Object.freeze(values.filter((entry2) => entry2.group === filters.group)) : values;
  }
  return Object.freeze({ register, list: list2, getSnapshot: snapshot, subscribe: emitter.subscribe });
}
function createRouteRegistry() {
  const entries = /* @__PURE__ */ new Map();
  const snapshot = () => Object.freeze([...entries.values()]);
  const emitter = createEmitter(snapshot);
  function register(pluginIDInput, input, component, context = null) {
    const pluginID = pluginIDValue(pluginIDInput);
    if (typeof component !== "function") {
      throw new ExtensionRegistryError("route_component_missing", `Route ${(input == null ? void 0 : input.path) || "unknown"} requires a component export.`);
    }
    if (entries.has(input.path)) throw new ExtensionRegistryError("route_exists", `Route ${input.path} already exists.`);
    const entry2 = Object.freeze({
      pluginID,
      id: input.id,
      title: input.title || input.id,
      path: input.path,
      componentName: input.component,
      component,
      context,
      requiredPermissions: input.required_permissions || Object.freeze([])
    });
    entries.set(input.path, entry2);
    emitter.emit();
    let registered = true;
    return () => {
      if (!registered) return false;
      registered = false;
      entries.delete(input.path);
      emitter.emit();
      return true;
    };
  }
  return Object.freeze({
    register,
    get: (path2) => entries.get(path2) || null,
    list: snapshot,
    getSnapshot: snapshot,
    subscribe: emitter.subscribe
  });
}
function createCommandRegistry(options = {}) {
  const entries = /* @__PURE__ */ new Map();
  const snapshot = () => Object.freeze([...entries.values()].map(({ handler, ...entry2 }) => Object.freeze({ ...entry2, available: typeof handler === "function" })));
  const emitter = createEmitter(snapshot);
  const authorize = options.authorize || (async (command, context) => {
    var _a2;
    if (!command.requiredPermissions.length) return;
    if (typeof ((_a2 = context == null ? void 0 : context.permissions) == null ? void 0 : _a2.require) !== "function") {
      throw new ExtensionRegistryError("permission_service_missing", `Command ${command.id} requires a permission service.`);
    }
    await context.permissions.require(command.requiredPermissions);
  });
  function declare(pluginIDInput, input) {
    const pluginID = pluginIDValue(pluginIDInput);
    const command = defineCommand({
      id: input.id,
      title: input.title,
      risk: input.risk,
      requiredPermissions: input.required_permissions,
      contexts: input.contexts,
      inputSchema: input.input_schema,
      outputSchema: input.output_schema
    });
    if (!command.id.startsWith(`${pluginID}.`)) {
      throw new ExtensionRegistryError("command_namespace_denied", `Command ${command.id} does not belong to ${pluginID}.`);
    }
    if (entries.has(command.id)) throw new ExtensionRegistryError("command_exists", `Command ${command.id} already exists.`);
    entries.set(command.id, {
      pluginID,
      id: command.id,
      title: command.title,
      risk: command.risk,
      requiredPermissions: command.requiredPermissions,
      contexts: command.contexts,
      inputSchema: command.inputSchema,
      outputSchema: command.outputSchema,
      handler: null
    });
    emitter.emit();
    let declared = true;
    return () => {
      if (!declared) return false;
      declared = false;
      entries.delete(command.id);
      emitter.emit();
      return true;
    };
  }
  function registerHandler(pluginIDInput, commandID, handler) {
    const pluginID = pluginIDValue(pluginIDInput);
    const entry2 = entries.get(commandID);
    if (!entry2 || entry2.pluginID !== pluginID) {
      throw new ExtensionRegistryError("command_not_declared", `Command ${commandID} is not declared by ${pluginID}.`);
    }
    if (typeof handler !== "function") throw new ExtensionRegistryError("invalid_handler", `Command ${commandID} handler must be a function.`);
    if (entry2.handler) throw new ExtensionRegistryError("handler_exists", `Command ${commandID} already has a handler.`);
    entry2.handler = handler;
    emitter.emit();
    let registered = true;
    return () => {
      if (!registered) return false;
      registered = false;
      if (entries.get(commandID) === entry2) entry2.handler = null;
      emitter.emit();
      return true;
    };
  }
  async function execute(commandID, input, context = {}) {
    var _a2, _b, _c;
    const entry2 = entries.get(commandID);
    if (!entry2) throw new ExtensionRegistryError("command_not_found", `Command ${commandID} is not registered.`);
    if (typeof entry2.handler !== "function") throw new ExtensionRegistryError("command_unavailable", `Command ${commandID} is not active.`);
    const publicCommand = snapshot().find((command) => command.id === commandID);
    const startedAt = Date.now();
    try {
      await authorize(publicCommand, context);
      await ((_a2 = options.beforeExecute) == null ? void 0 : _a2.call(options, publicCommand, input, context));
      const result = await entry2.handler(input, context);
      await ((_b = options.afterExecute) == null ? void 0 : _b.call(options, publicCommand, { ok: true, result, durationMs: Date.now() - startedAt }, context));
      return result;
    } catch (error) {
      await ((_c = options.afterExecute) == null ? void 0 : _c.call(options, publicCommand, { ok: false, error, durationMs: Date.now() - startedAt }, context));
      throw error;
    }
  }
  return Object.freeze({
    declare,
    registerHandler,
    execute,
    get: (commandID) => snapshot().find((entry2) => entry2.id === commandID) || null,
    hasHandler: (commandID) => {
      var _a2;
      return typeof ((_a2 = entries.get(commandID)) == null ? void 0 : _a2.handler) === "function";
    },
    list: snapshot,
    getSnapshot: snapshot,
    subscribe: emitter.subscribe
  });
}
const WORKBENCH_PLUGIN_PROTOCOL = "tma.workbench_plugin.v1";
const WORKBENCH_PLUGIN_SURFACES = Object.freeze([
  "web_desktop",
  "desktop_shell",
  "web_tablet",
  "web_mobile"
]);
const namespacedIdentifierPattern = /^[a-z][a-z0-9_-]*(?:\.[a-z][a-z0-9_-]*)+$/;
const localIdentifierPattern = /^[a-z][a-z0-9_-]*$/;
const semverPattern = /^(\d+)\.(\d+)\.(\d+)(?:-[0-9A-Za-z.-]+)?(?:\+[0-9A-Za-z.-]+)?$/;
const comparatorPattern = /^(>=|<=|>|<|=)?(\d+\.\d+\.\d+(?:-[0-9A-Za-z.-]+)?(?:\+[0-9A-Za-z.-]+)?)$/;
class PluginManifestError extends TypeError {
  constructor(field, message, code2 = "invalid_manifest") {
    super(`${field}: ${message}`);
    this.name = "PluginManifestError";
    this.field = field;
    this.code = code2;
  }
}
function isPlainObject(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) return false;
  const prototype = Object.getPrototypeOf(value);
  return prototype === Object.prototype || prototype === null;
}
function objectValue$1(value, field) {
  if (!isPlainObject(value)) throw new PluginManifestError(field, "must be a plain object");
  return value;
}
function allowedKeys(value, field, keys2) {
  const allowed = new Set(keys2);
  const unknown = Object.keys(value).find((key) => !allowed.has(key));
  if (unknown) throw new PluginManifestError(`${field}.${unknown}`, "is not supported by the Phase 0A manifest");
}
function stringValue(value, field) {
  const normalized = typeof value === "string" ? value.trim() : "";
  if (!normalized) throw new PluginManifestError(field, "must be a non-empty string");
  return normalized;
}
function optionalString(value, field) {
  if (value === void 0 || value === null || value === "") return void 0;
  return stringValue(value, field);
}
function namespacedIdentifier(value, field) {
  const normalized = stringValue(value, field);
  if (!namespacedIdentifierPattern.test(normalized)) {
    throw new PluginManifestError(field, "must be a lowercase namespaced identifier");
  }
  return normalized;
}
function localIdentifier(value, field) {
  const normalized = stringValue(value, field);
  if (!localIdentifierPattern.test(normalized)) {
    throw new PluginManifestError(field, "must be a lowercase identifier");
  }
  return normalized;
}
function uniqueStrings(value, field, normalize2) {
  if (!Array.isArray(value)) throw new PluginManifestError(field, "must be an array");
  const result = [];
  const seen = /* @__PURE__ */ new Set();
  value.forEach((item, index2) => {
    const normalized = normalize2(item, `${field}[${index2}]`);
    if (seen.has(normalized)) return;
    seen.add(normalized);
    result.push(normalized);
  });
  return Object.freeze(result);
}
function parseSemver(value, field) {
  const normalized = stringValue(value, field);
  const match = normalized.match(semverPattern);
  if (!match) throw new PluginManifestError(field, "must be a semantic version");
  return { raw: normalized, parts: match.slice(1, 4).map(Number) };
}
function compareSemver(left, right) {
  for (let index2 = 0; index2 < 3; index2 += 1) {
    if (left[index2] !== right[index2]) return left[index2] < right[index2] ? -1 : 1;
  }
  return 0;
}
function parseRange(value, field) {
  const normalized = stringValue(value, field);
  if (normalized === "*") return Object.freeze([]);
  const comparators = normalized.split(/\s+/).map((part, index2) => {
    const match = part.match(comparatorPattern);
    if (!match) throw new PluginManifestError(`${field}[${index2}]`, "must use exact or comparator semantic versions");
    return Object.freeze({ operator: match[1] || "=", version: parseSemver(match[2], field).parts });
  });
  return Object.freeze(comparators);
}
function satisfiesSemverRange(version2, range) {
  const current = parseSemver(version2, "version").parts;
  const comparators = parseRange(range, "range");
  return comparators.every(({ operator, version: expected }) => {
    const comparison = compareSemver(current, expected);
    if (operator === ">=") return comparison >= 0;
    if (operator === "<=") return comparison <= 0;
    if (operator === ">") return comparison > 0;
    if (operator === "<") return comparison < 0;
    return comparison === 0;
  });
}
function requiredPermissions(value, field, manifestPermissions) {
  const permissions2 = uniqueStrings(value ?? [], field, namespacedIdentifier);
  permissions2.forEach((permission, index2) => {
    if (!manifestPermissions.has(permission)) {
      throw new PluginManifestError(`${field}[${index2}]`, `permission ${permission} is not declared by the plugin`, "undeclared_permission");
    }
  });
  return permissions2;
}
function normalizeEntry(value, field = "manifest.entry") {
  const entry2 = stringValue(value, field);
  if (!entry2.startsWith("./") || entry2.includes("\\") || entry2.split("/").includes("..") || /^[a-z]+:/i.test(entry2)) {
    throw new PluginManifestError(field, "must be a relative path inside the plugin package");
  }
  return entry2;
}
function normalizeNavigation(value, pluginID) {
  if (value === void 0) return Object.freeze([]);
  if (!Array.isArray(value)) throw new PluginManifestError("manifest.contributes.navigation", "must be an array");
  const ids = /* @__PURE__ */ new Set();
  return Object.freeze(value.map((item, index2) => {
    const field = `manifest.contributes.navigation[${index2}]`;
    const input = objectValue$1(item, field);
    allowedKeys(input, field, ["id", "group", "title", "route", "order"]);
    const id2 = localIdentifier(input.id, `${field}.id`);
    if (ids.has(id2)) throw new PluginManifestError(`${field}.id`, `duplicate navigation id ${id2}`);
    ids.add(id2);
    const route = stringValue(input.route, `${field}.route`);
    if (!route.startsWith(`/plugins/${pluginID}/`)) {
      throw new PluginManifestError(`${field}.route`, `must be under /plugins/${pluginID}/`);
    }
    const navigation2 = {
      id: id2,
      group: localIdentifier(input.group, `${field}.group`),
      title: stringValue(input.title, `${field}.title`),
      route
    };
    if (input.order !== void 0) {
      if (!Number.isInteger(input.order)) throw new PluginManifestError(`${field}.order`, "must be an integer");
      navigation2.order = input.order;
    }
    return Object.freeze(navigation2);
  }));
}
function normalizeRoutes(value, pluginID, manifestPermissions) {
  if (value === void 0) return Object.freeze([]);
  if (!Array.isArray(value)) throw new PluginManifestError("manifest.contributes.routes", "must be an array");
  const ids = /* @__PURE__ */ new Set();
  const paths = /* @__PURE__ */ new Set();
  return Object.freeze(value.map((item, index2) => {
    const field = `manifest.contributes.routes[${index2}]`;
    const input = objectValue$1(item, field);
    allowedKeys(input, field, ["id", "path", "component", "required_permissions"]);
    const id2 = localIdentifier(input.id, `${field}.id`);
    const path2 = stringValue(input.path, `${field}.path`);
    if (ids.has(id2)) throw new PluginManifestError(`${field}.id`, `duplicate route id ${id2}`);
    if (paths.has(path2)) throw new PluginManifestError(`${field}.path`, `duplicate route path ${path2}`);
    if (!path2.startsWith(`/plugins/${pluginID}/`)) {
      throw new PluginManifestError(`${field}.path`, `must be under /plugins/${pluginID}/`);
    }
    ids.add(id2);
    paths.add(path2);
    return Object.freeze({
      id: id2,
      path: path2,
      component: stringValue(input.component, `${field}.component`),
      required_permissions: requiredPermissions(input.required_permissions, `${field}.required_permissions`, manifestPermissions)
    });
  }));
}
function normalizeCommands(value, pluginID, manifestPermissions) {
  if (value === void 0) return Object.freeze([]);
  if (!Array.isArray(value)) throw new PluginManifestError("manifest.contributes.commands", "must be an array");
  const ids = /* @__PURE__ */ new Set();
  return Object.freeze(value.map((item, index2) => {
    const field = `manifest.contributes.commands[${index2}]`;
    const input = objectValue$1(item, field);
    allowedKeys(input, field, ["id", "title", "risk", "required_permissions", "contexts", "input_schema", "output_schema"]);
    const required = requiredPermissions(input.required_permissions, `${field}.required_permissions`, manifestPermissions);
    const command = defineCommand({
      id: input.id,
      title: input.title,
      risk: input.risk,
      requiredPermissions: required,
      contexts: input.contexts,
      inputSchema: input.input_schema,
      outputSchema: input.output_schema
    });
    if (!command.id.startsWith(`${pluginID}.`)) {
      throw new PluginManifestError(`${field}.id`, `must use plugin namespace ${pluginID}`);
    }
    if (ids.has(command.id)) throw new PluginManifestError(`${field}.id`, `duplicate command id ${command.id}`);
    ids.add(command.id);
    const normalized = {
      id: command.id,
      title: command.title,
      risk: command.risk,
      required_permissions: command.requiredPermissions,
      contexts: command.contexts
    };
    if (command.inputSchema) normalized.input_schema = command.inputSchema;
    if (command.outputSchema) normalized.output_schema = command.outputSchema;
    return Object.freeze(normalized);
  }));
}
function normalizeConfiguration(value) {
  if (value === void 0) return void 0;
  const input = objectValue$1(value, "manifest.configuration");
  allowedKeys(input, "manifest.configuration", ["schema", "scope"]);
  const scope = stringValue(input.scope, "manifest.configuration.scope");
  if (!["organization", "workspace", "user"].includes(scope)) {
    throw new PluginManifestError("manifest.configuration.scope", "must be organization, workspace, or user");
  }
  return Object.freeze({ schema: normalizeEntry(input.schema, "manifest.configuration.schema"), scope });
}
function definePluginManifest(input) {
  const value = objectValue$1(input, "manifest");
  allowedKeys(value, "manifest", [
    "protocol_version",
    "id",
    "name",
    "description",
    "version",
    "entry",
    "surfaces",
    "engines",
    "permissions",
    "contributes",
    "configuration"
  ]);
  const protocolVersion = stringValue(value.protocol_version, "manifest.protocol_version");
  if (protocolVersion !== WORKBENCH_PLUGIN_PROTOCOL) {
    throw new PluginManifestError("manifest.protocol_version", `must be ${WORKBENCH_PLUGIN_PROTOCOL}`, "unsupported_protocol");
  }
  const id2 = namespacedIdentifier(value.id, "manifest.id");
  const version2 = parseSemver(value.version, "manifest.version").raw;
  const surfaces2 = uniqueStrings(value.surfaces, "manifest.surfaces", (item, field) => {
    const surface = stringValue(item, field);
    if (!WORKBENCH_PLUGIN_SURFACES.includes(surface)) throw new PluginManifestError(field, "is not a supported surface");
    return surface;
  });
  if (!surfaces2.length) throw new PluginManifestError("manifest.surfaces", "must include at least one surface");
  const permissions2 = uniqueStrings(value.permissions ?? [], "manifest.permissions", namespacedIdentifier);
  const permissionSet = new Set(permissions2);
  const enginesInput = objectValue$1(value.engines, "manifest.engines");
  allowedKeys(enginesInput, "manifest.engines", ["workbench_api", "design_system"]);
  const engines2 = Object.freeze({
    workbench_api: stringValue(enginesInput.workbench_api, "manifest.engines.workbench_api"),
    design_system: stringValue(enginesInput.design_system, "manifest.engines.design_system")
  });
  parseRange(engines2.workbench_api, "manifest.engines.workbench_api");
  parseRange(engines2.design_system, "manifest.engines.design_system");
  const contributesInput = value.contributes === void 0 ? {} : objectValue$1(value.contributes, "manifest.contributes");
  allowedKeys(contributesInput, "manifest.contributes", ["navigation", "routes", "commands"]);
  const routes = normalizeRoutes(contributesInput.routes, id2, permissionSet);
  const navigation2 = normalizeNavigation(contributesInput.navigation, id2);
  const routePaths = new Set(routes.map((route) => route.path));
  navigation2.forEach((item, index2) => {
    if (!routePaths.has(item.route)) {
      throw new PluginManifestError(`manifest.contributes.navigation[${index2}].route`, "must reference a declared route");
    }
  });
  const contributes2 = Object.freeze({
    navigation: navigation2,
    routes,
    commands: normalizeCommands(contributesInput.commands, id2, permissionSet)
  });
  const manifest2 = {
    protocol_version: protocolVersion,
    id: id2,
    name: stringValue(value.name, "manifest.name"),
    version: version2,
    entry: normalizeEntry(value.entry),
    surfaces: surfaces2,
    engines: engines2,
    permissions: permissions2,
    contributes: contributes2
  };
  const description2 = optionalString(value.description, "manifest.description");
  if (description2 !== void 0) manifest2.description = description2;
  const configuration = normalizeConfiguration(value.configuration);
  if (configuration !== void 0) manifest2.configuration = configuration;
  return Object.freeze(manifest2);
}
function checkPluginCompatibility(input, environment) {
  const manifest2 = definePluginManifest(input);
  const value = objectValue$1(environment, "environment");
  const workbenchAPIVersion = parseSemver(value.workbenchAPIVersion, "environment.workbenchAPIVersion").raw;
  const designSystemVersion = parseSemver(value.designSystemVersion, "environment.designSystemVersion").raw;
  const surface = stringValue(value.surface, "environment.surface");
  const reasons = [];
  if (!manifest2.surfaces.includes(surface)) reasons.push(Object.freeze({ code: "surface_unsupported", message: `Surface ${surface} is not declared.` }));
  if (!satisfiesSemverRange(workbenchAPIVersion, manifest2.engines.workbench_api)) {
    reasons.push(Object.freeze({ code: "workbench_api_incompatible", message: `Workbench API ${workbenchAPIVersion} does not satisfy ${manifest2.engines.workbench_api}.` }));
  }
  if (!satisfiesSemverRange(designSystemVersion, manifest2.engines.design_system)) {
    reasons.push(Object.freeze({ code: "design_system_incompatible", message: `Design System ${designSystemVersion} does not satisfy ${manifest2.engines.design_system}.` }));
  }
  return Object.freeze({ compatible: reasons.length === 0, manifest: manifest2, reasons: Object.freeze(reasons) });
}
function normalizedList(value) {
  if (value === void 0 || value === null) return Object.freeze([]);
  if (!Array.isArray(value)) throw new TypeError("Plugin enablement values must be arrays.");
  return Object.freeze([...new Set(value.map((item) => typeof item === "string" ? item.trim() : "").filter(Boolean))]);
}
function definePluginEnablement(input = {}) {
  if (!input || typeof input !== "object" || Array.isArray(input)) throw new TypeError("Plugin enablement must be an object.");
  const unknown = Object.keys(input).find((key) => ![
    "defaultEnabled",
    "organizations",
    "workspaces",
    "roles",
    "excludedOrganizations",
    "excludedWorkspaces",
    "excludedRoles"
  ].includes(key));
  if (unknown) throw new TypeError(`Plugin enablement field ${unknown} is not supported.`);
  if (input.defaultEnabled !== void 0 && typeof input.defaultEnabled !== "boolean") {
    throw new TypeError("Plugin enablement defaultEnabled must be a boolean.");
  }
  return Object.freeze({
    defaultEnabled: input.defaultEnabled !== false,
    organizations: normalizedList(input.organizations),
    workspaces: normalizedList(input.workspaces),
    roles: normalizedList(input.roles),
    excludedOrganizations: normalizedList(input.excludedOrganizations),
    excludedWorkspaces: normalizedList(input.excludedWorkspaces),
    excludedRoles: normalizedList(input.excludedRoles)
  });
}
function evaluatePluginEnablement(input, scope = {}) {
  const policy = definePluginEnablement(input);
  const organizationID = String(scope.organizationId || "").trim();
  const workspaceID = String(scope.workspaceId || "").trim();
  const roles = new Set(normalizedList(scope.roles));
  const reasons = [];
  const hasAllowlist = Boolean(policy.organizations.length || policy.workspaces.length || policy.roles.length);
  if (!policy.defaultEnabled && !hasAllowlist) reasons.push("disabled_by_default");
  if (policy.organizations.length && !policy.organizations.includes(organizationID)) reasons.push("organization_not_enabled");
  if (policy.workspaces.length && !policy.workspaces.includes(workspaceID)) reasons.push("workspace_not_enabled");
  if (policy.roles.length && !policy.roles.some((role) => roles.has(role))) reasons.push("role_not_enabled");
  if (organizationID && policy.excludedOrganizations.includes(organizationID)) reasons.push("organization_excluded");
  if (workspaceID && policy.excludedWorkspaces.includes(workspaceID)) reasons.push("workspace_excluded");
  if (policy.excludedRoles.some((role) => roles.has(role))) reasons.push("role_excluded");
  return Object.freeze({ enabled: reasons.length === 0, policy, reasons: Object.freeze(reasons) });
}
class PluginRuntimeError extends Error {
  constructor(code2, message, cause) {
    super(message, cause ? { cause } : void 0);
    this.name = "PluginRuntimeError";
    this.code = code2;
  }
}
function publicRecord(record) {
  return Object.freeze({
    id: record.manifest.id,
    name: record.manifest.name,
    version: record.manifest.version,
    status: record.status,
    compatible: record.compatibility.compatible,
    compatibilityReasons: record.compatibility.reasons,
    enabled: record.enablement.enabled,
    enablementReasons: record.enablement.reasons,
    error: record.error || "",
    manifest: record.manifest
  });
}
function permissionList(value) {
  if (Array.isArray(value)) return value;
  return value === void 0 || value === null ? [] : [value];
}
function createStaticPluginRegistry(options) {
  if (!options || typeof options !== "object") throw new PluginRuntimeError("invalid_options", "Plugin Runtime options are required.");
  const environment = Object.freeze({
    workbenchAPIVersion: options.workbenchAPIVersion,
    designSystemVersion: options.designSystemVersion,
    surface: options.surface
  });
  const navigation2 = options.navigationRegistry || createNavigationRegistry({ groups: options.navigationGroups });
  const routes = options.routeRegistry || createRouteRegistry();
  const commands = options.commandRegistry || createCommandRegistry(options.commandOptions);
  const records = /* @__PURE__ */ new Map();
  const listeners = /* @__PURE__ */ new Set();
  function list2() {
    return Object.freeze([...records.values()].map(publicRecord));
  }
  function emit() {
    const snapshot = list2();
    for (const listener of listeners) listener(snapshot);
  }
  function get(pluginID) {
    const record = records.get(pluginID);
    return record ? publicRecord(record) : null;
  }
  function registerPackage(pluginPackage) {
    if (!pluginPackage || typeof pluginPackage !== "object" || Array.isArray(pluginPackage)) {
      throw new PluginRuntimeError("invalid_package", "Static plugin package must be an object.");
    }
    const compatibility = checkPluginCompatibility(pluginPackage.manifest, environment);
    const manifest2 = compatibility.manifest;
    const enablement = options.evaluateEnablement ? options.evaluateEnablement(pluginPackage, options.scope) : evaluatePluginEnablement(pluginPackage.enablement, options.scope);
    if (records.has(manifest2.id)) throw new PluginRuntimeError("plugin_exists", `Plugin ${manifest2.id} is already registered.`);
    const plugin2 = pluginPackage.plugin;
    if (!plugin2 || typeof plugin2 !== "object" || typeof plugin2.activate !== "function") {
      throw new PluginRuntimeError("invalid_plugin", `Plugin ${manifest2.id} must export activate().`);
    }
    if (plugin2.id !== manifest2.id) {
      throw new PluginRuntimeError("plugin_id_mismatch", `Plugin export id ${plugin2.id || "missing"} does not match ${manifest2.id}.`);
    }
    const record = {
      manifest: manifest2,
      compatibility,
      enablement,
      plugin: plugin2,
      components: pluginPackage.components || {},
      status: !compatibility.compatible ? "incompatible" : enablement.enabled ? "registered" : "disabled",
      error: "",
      disposers: []
    };
    records.set(manifest2.id, record);
    emit();
    return publicRecord(record);
  }
  function createPermissions(record) {
    var _a2;
    const declared = new Set(record.manifest.permissions);
    const host = (_a2 = options.services) == null ? void 0 : _a2.permissions;
    return Object.freeze({
      async has(value) {
        const permissions2 = permissionList(value);
        if (permissions2.some((permission) => !declared.has(permission))) return false;
        if (typeof (host == null ? void 0 : host.has) !== "function") return permissions2.length === 0;
        return Boolean(await host.has(permissions2));
      },
      async require(value) {
        const permissions2 = permissionList(value);
        const undeclared = permissions2.find((permission) => !declared.has(permission));
        if (undeclared) {
          throw new PluginRuntimeError("permission_not_declared", `Plugin ${record.manifest.id} did not declare ${undeclared}.`);
        }
        if (typeof (host == null ? void 0 : host.require) !== "function") {
          if (permissions2.length) throw new PluginRuntimeError("permission_service_missing", "Host permission service is unavailable.");
          return true;
        }
        return host.require(permissions2);
      }
    });
  }
  function createContext(record, disposers) {
    var _a2, _b, _c, _d, _e, _f;
    const permissions2 = createPermissions(record);
    const declaredCommands = new Set(record.manifest.contributes.commands.map((command) => command.id));
    const commandService = Object.freeze({
      register(commandID, handler) {
        if (!declaredCommands.has(commandID)) {
          throw new PluginRuntimeError("command_not_declared", `Plugin ${record.manifest.id} did not declare ${commandID}.`);
        }
        const dispose = commands.registerHandler(record.manifest.id, commandID, handler);
        disposers.push(dispose);
        return dispose;
      },
      execute(commandID, input, context = {}) {
        return commands.execute(commandID, input, { ...context, permissions: permissions2 });
      }
    });
    return definePluginContext({
      plugin: { id: record.manifest.id, version: record.manifest.version },
      scope: options.scope,
      permissions: permissions2,
      commands: commandService,
      dialog: (_a2 = options.services) == null ? void 0 : _a2.dialog,
      notifications: (_b = options.services) == null ? void 0 : _b.notifications,
      resources: (_c = options.services) == null ? void 0 : _c.resources,
      tasks: (_d = options.services) == null ? void 0 : _d.tasks,
      artifacts: (_e = options.services) == null ? void 0 : _e.artifacts,
      http: (_f = options.services) == null ? void 0 : _f.http
    });
  }
  function rollback(disposers) {
    for (const dispose of [...disposers].reverse()) {
      try {
        dispose();
      } catch {
      }
    }
  }
  async function activate(pluginID) {
    const record = records.get(pluginID);
    if (!record) throw new PluginRuntimeError("plugin_not_found", `Plugin ${pluginID} is not registered.`);
    if (!record.compatibility.compatible) throw new PluginRuntimeError("plugin_incompatible", `Plugin ${pluginID} is incompatible.`);
    if (!record.enablement.enabled) throw new PluginRuntimeError("plugin_disabled", `Plugin ${pluginID} is disabled for the current scope.`);
    if (record.status === "active") return publicRecord(record);
    if (["activating", "deactivating"].includes(record.status)) {
      throw new PluginRuntimeError("plugin_busy", `Plugin ${pluginID} is currently ${record.status}.`);
    }
    const disposers = [];
    record.status = "activating";
    record.error = "";
    emit();
    try {
      for (const contribution of record.manifest.contributes.commands) {
        disposers.push(commands.declare(pluginID, contribution));
      }
      const context = createContext(record, disposers);
      for (const contribution of record.manifest.contributes.routes) {
        const component = record.components[contribution.component];
        const navigationItem = record.manifest.contributes.navigation.find((item) => item.route === contribution.path);
        disposers.push(routes.register(pluginID, { ...contribution, title: (navigationItem == null ? void 0 : navigationItem.title) || contribution.id }, component, context));
      }
      for (const contribution of record.manifest.contributes.navigation) {
        disposers.push(navigation2.register(pluginID, contribution));
      }
      await record.plugin.activate(context);
      const missingHandler = record.manifest.contributes.commands.find((command) => !commands.hasHandler(command.id));
      if (missingHandler) throw new PluginRuntimeError("command_handler_missing", `Command ${missingHandler.id} has no active handler.`);
      record.disposers = disposers;
      record.status = "active";
      emit();
      return publicRecord(record);
    } catch (error) {
      rollback(disposers);
      record.disposers = [];
      record.status = "failed";
      record.error = error.message || String(error);
      emit();
      if (error instanceof PluginRuntimeError) throw error;
      throw new PluginRuntimeError("activation_failed", `Plugin ${pluginID} failed to activate.`, error);
    }
  }
  async function deactivate(pluginID) {
    var _a2, _b;
    const record = records.get(pluginID);
    if (!record) throw new PluginRuntimeError("plugin_not_found", `Plugin ${pluginID} is not registered.`);
    if (record.status !== "active") return publicRecord(record);
    record.status = "deactivating";
    emit();
    let error = null;
    try {
      await ((_b = (_a2 = record.plugin).deactivate) == null ? void 0 : _b.call(_a2));
    } catch (caught) {
      error = caught;
    } finally {
      rollback(record.disposers);
      record.disposers = [];
    }
    if (error) {
      record.status = "failed";
      record.error = error.message || String(error);
      emit();
      throw new PluginRuntimeError("deactivation_failed", `Plugin ${pluginID} failed while deactivating.`, error);
    }
    record.status = "registered";
    record.error = "";
    emit();
    return publicRecord(record);
  }
  async function unregisterPackage(pluginID) {
    const record = records.get(pluginID);
    if (!record) return false;
    let error = null;
    try {
      if (record.status === "active") await deactivate(pluginID);
    } catch (caught) {
      error = caught;
    } finally {
      records.delete(pluginID);
      emit();
    }
    if (error) throw error;
    return true;
  }
  async function load(pluginPackages, loadOptions = {}) {
    const registered = pluginPackages.map(registerPackage);
    if (loadOptions.activate === false) return Object.freeze(registered);
    const results = [];
    for (const plugin2 of registered) {
      if (!plugin2.compatible || !plugin2.enabled) {
        results.push(plugin2);
        continue;
      }
      try {
        results.push(await activate(plugin2.id));
      } catch {
        results.push(get(plugin2.id));
      }
    }
    return Object.freeze(results);
  }
  function subscribe(listener) {
    if (typeof listener !== "function") throw new PluginRuntimeError("invalid_listener", "Plugin listener must be a function.");
    listeners.add(listener);
    listener(list2());
    return () => listeners.delete(listener);
  }
  return Object.freeze({
    registerPackage,
    unregisterPackage,
    activate,
    deactivate,
    load,
    get,
    list: list2,
    subscribe,
    navigation: navigation2,
    routes,
    commands
  });
}
const protocol_version$1 = "tma.workbench_plugin.v1";
const id$1 = "com.tma.workbench-diagnostics";
const name$1 = "扩展诊断";
const description$1 = "查看静态插件运行时与多端声明状态";
const version$1 = "1.0.0";
const entry$1 = "./index.js";
const surfaces$1 = ["web_desktop", "desktop_shell", "web_tablet", "web_mobile"];
const engines$1 = { "workbench_api": ">=1.0.0 <2.0.0", "design_system": ">=1.0.0 <2.0.0" };
const permissions$1 = [];
const contributes$1 = { "navigation": [{ "id": "diagnostics", "group": "workspace", "title": "扩展诊断", "route": "/plugins/com.tma.workbench-diagnostics/status", "order": 900 }], "routes": [{ "id": "status", "path": "/plugins/com.tma.workbench-diagnostics/status", "component": "DiagnosticsPage", "required_permissions": [] }], "commands": [{ "id": "com.tma.workbench-diagnostics.refresh", "title": "刷新诊断状态", "risk": "read", "required_permissions": [] }] };
const manifest$1 = {
  protocol_version: protocol_version$1,
  id: id$1,
  name: name$1,
  description: description$1,
  version: version$1,
  entry: entry$1,
  surfaces: surfaces$1,
  engines: engines$1,
  permissions: permissions$1,
  contributes: contributes$1
};
const plugin$1 = {
  id: "com.tma.workbench-diagnostics",
  activate(context) {
    context.commands.register("com.tma.workbench-diagnostics.refresh", async () => ({
      checkedAt: (/* @__PURE__ */ new Date()).toISOString(),
      pluginID: context.plugin.id,
      workspaceID: context.scope.workspaceId
    }));
  }
};
function metric(label, value, detail) {
  return React.createElement(
    "article",
    { className: "plugin-diagnostic-metric", key: label },
    React.createElement("span", null, label),
    React.createElement("strong", null, value),
    React.createElement("small", null, detail)
  );
}
function DiagnosticsPage({ context }) {
  const [state, setState] = reactExports.useState({ loading: false, checkedAt: "尚未检查", error: "" });
  async function refresh() {
    setState((current) => ({ ...current, loading: true, error: "" }));
    try {
      const result = await context.commands.execute("com.tma.workbench-diagnostics.refresh", {});
      const checkedAt = new Date(result.checkedAt).toLocaleString("zh-CN", { hour12: false });
      setState({ loading: false, checkedAt, error: "" });
      context.notifications.show({
        level: "success",
        title: "扩展状态已刷新",
        message: context.plugin.id,
        dedupeKey: "plugin.diagnostics.refresh"
      });
    } catch (error) {
      setState((current) => ({ ...current, loading: false, error: error.message || String(error) }));
    }
  }
  return React.createElement(
    "div",
    { className: "plugin-diagnostics-page" },
    React.createElement(
      "div",
      { className: "plugin-page-heading" },
      React.createElement(
        "div",
        null,
        React.createElement("span", null, "Workbench Runtime"),
        React.createElement("h1", null, "扩展诊断"),
        React.createElement("p", null, context.plugin.id)
      ),
      React.createElement("button", { type: "button", disabled: state.loading, onClick: refresh }, state.loading ? "检查中..." : "刷新状态")
    ),
    React.createElement("div", { className: "plugin-diagnostic-grid" }, [
      metric("协议", "v1", context.plugin.version),
      metric("工作区", context.scope.workspaceId, context.scope.organizationId || "默认组织"),
      metric("终端", "4", "Desktop · Shell · Tablet · Mobile")
    ]),
    React.createElement(
      "section",
      { className: "plugin-diagnostic-status" },
      React.createElement("h2", null, "运行状态"),
      React.createElement(
        "dl",
        null,
        React.createElement("div", null, React.createElement("dt", null, "Manifest"), React.createElement("dd", null, "已校验")),
        React.createElement("div", null, React.createElement("dt", null, "Route Guard"), React.createElement("dd", null, "已通过")),
        React.createElement("div", null, React.createElement("dt", null, "最近检查"), React.createElement("dd", null, state.checkedAt))
      ),
      state.error ? React.createElement("div", { className: "plugin-inline-error", role: "alert" }, state.error) : null
    )
  );
}
const _package$1 = Object.freeze({
  manifest: manifest$1,
  plugin: plugin$1,
  components: Object.freeze({ DiagnosticsPage }),
  enablement: Object.freeze({ defaultEnabled: true })
});
const __vite_glob_0_0 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: _package$1
}, Symbol.toStringTag, { value: "Module" }));
const protocol_version = "tma.workbench_plugin.v1";
const id = "com.tma.research-projects";
const name = "科研项目";
const description = "组织长期研究项目、已有任务成果与项目摘要";
const version = "0.1.0";
const entry = "./index.jsx";
const surfaces = ["web_desktop", "desktop_shell", "web_tablet", "web_mobile"];
const engines = { "workbench_api": ">=1.0.0 <2.0.0", "design_system": ">=1.0.0 <2.0.0" };
const permissions = [];
const contributes = { "navigation": [{ "id": "projects", "group": "workspace", "title": "科研项目", "route": "/plugins/com.tma.research-projects/projects", "order": 120 }], "routes": [{ "id": "projects", "path": "/plugins/com.tma.research-projects/projects", "component": "ResearchProjectsPage", "required_permissions": [] }], "commands": [{ "id": "com.tma.research-projects.create-project", "title": "创建科研项目", "risk": "write", "required_permissions": [] }, { "id": "com.tma.research-projects.export-brief", "title": "导出项目摘要", "risk": "read", "required_permissions": [] }] };
const manifest = {
  protocol_version,
  id,
  name,
  description,
  version,
  entry,
  surfaces,
  engines,
  permissions,
  contributes
};
const STORAGE_PREFIX = "tma.plugin.com.tma.research-projects.projects.v1";
const PROJECT_STATUSES = /* @__PURE__ */ new Set(["active", "archived"]);
const PROJECT_STAGES = /* @__PURE__ */ new Set(["planning", "collecting", "analyzing", "drafting", "completed"]);
const FINDING_TYPES = /* @__PURE__ */ new Set(["finding", "hypothesis", "question"]);
const RESEARCH_STAGE_OPTIONS = Object.freeze([
  Object.freeze({ id: "planning", label: "研究规划" }),
  Object.freeze({ id: "collecting", label: "资料收集" }),
  Object.freeze({ id: "analyzing", label: "分析归纳" }),
  Object.freeze({ id: "drafting", label: "报告撰写" }),
  Object.freeze({ id: "completed", label: "已完成" })
]);
const RESEARCH_FINDING_TYPE_OPTIONS = Object.freeze([
  Object.freeze({ id: "finding", label: "研究结论" }),
  Object.freeze({ id: "hypothesis", label: "研究假设" }),
  Object.freeze({ id: "question", label: "待验证问题" })
]);
class ResearchProjectRepositoryError extends Error {
  constructor(code2, message) {
    super(message);
    this.name = "ResearchProjectRepositoryError";
    this.code = code2;
  }
}
function requiredText(value, field, maxLength) {
  const normalized = typeof value === "string" ? value.trim() : "";
  if (!normalized) throw new ResearchProjectRepositoryError("invalid_project", `${field} is required`);
  if (normalized.length > maxLength) {
    throw new ResearchProjectRepositoryError("invalid_project", `${field} exceeds ${maxLength} characters`);
  }
  return normalized;
}
function optionalText(value, field, maxLength) {
  const normalized = typeof value === "string" ? value.trim() : "";
  if (normalized.length > maxLength) {
    throw new ResearchProjectRepositoryError("invalid_project", `${field} exceeds ${maxLength} characters`);
  }
  return normalized;
}
function requiredStage(value) {
  if (!PROJECT_STAGES.has(value)) {
    throw new ResearchProjectRepositoryError("invalid_project", "project.stage is invalid");
  }
  return value;
}
function cloneJSON(value) {
  return JSON.parse(JSON.stringify(value));
}
function normalizeResource(input, defaultLinkedAt = "") {
  if (!input || typeof input !== "object" || Array.isArray(input)) {
    throw new ResearchProjectRepositoryError("invalid_resource", "resource must be an object");
  }
  return {
    id: requiredText(input.id, "resource.id", 240),
    type: requiredText(input.type, "resource.type", 64),
    title: requiredText(input.title, "resource.title", 500),
    source: requiredText(input.source, "resource.source", 500),
    linkedAt: optionalText(input.linkedAt || defaultLinkedAt, "resource.linkedAt", 80),
    ...typeof input.mimeType === "string" && input.mimeType.trim() ? { mimeType: input.mimeType.trim() } : {},
    ...typeof input.previewable === "boolean" ? { previewable: input.previewable } : {},
    metadata: input.metadata && typeof input.metadata === "object" && !Array.isArray(input.metadata) ? cloneJSON(input.metadata) : {}
  };
}
function normalizeFinding(input) {
  if (!input || typeof input !== "object" || Array.isArray(input)) {
    throw new ResearchProjectRepositoryError("invalid_finding", "finding must be an object");
  }
  const type = FINDING_TYPES.has(input.type) ? input.type : "finding";
  return {
    id: requiredText(input.id, "finding.id", 240),
    type,
    title: requiredText(input.title, "finding.title", 180),
    content: optionalText(input.content, "finding.content", 4e3),
    createdAt: requiredText(input.createdAt, "finding.createdAt", 80),
    updatedAt: requiredText(input.updatedAt, "finding.updatedAt", 80)
  };
}
function normalizedScope(scope) {
  const workspaceId = requiredText(scope == null ? void 0 : scope.workspaceId, "scope.workspaceId", 240);
  const userId = requiredText(scope == null ? void 0 : scope.userId, "scope.userId", 240);
  return { workspaceId, userId };
}
function projectID(now, randomID2) {
  const generated = typeof randomID2 === "function" ? String(randomID2()).trim() : "";
  if (generated) return `research_${generated}`;
  return `research_${now()}_${Math.random().toString(36).slice(2, 10)}`;
}
function findingID(now, randomID2) {
  const generated = typeof randomID2 === "function" ? String(randomID2()).trim() : "";
  if (generated) return `finding_${generated}`;
  return `finding_${now()}_${Math.random().toString(36).slice(2, 10)}`;
}
function normalizeStoredProject(project) {
  if (!project || typeof project !== "object" || Array.isArray(project)) return null;
  try {
    const status = PROJECT_STATUSES.has(project.status) ? project.status : "active";
    const stage = PROJECT_STAGES.has(project.stage) ? project.stage : "planning";
    const updatedAt = requiredText(project.updatedAt, "project.updatedAt", 80);
    return {
      id: requiredText(project.id, "project.id", 240),
      name: requiredText(project.name, "project.name", 120),
      objective: optionalText(project.objective, "project.objective", 1200),
      description: optionalText(project.description, "project.description", 4e3),
      nextStep: optionalText(project.nextStep, "project.nextStep", 1200),
      status,
      stage,
      resources: Array.isArray(project.resources) ? project.resources.map((resource) => normalizeResource(resource, updatedAt)) : [],
      findings: Array.isArray(project.findings) ? project.findings.map(normalizeFinding) : [],
      createdAt: requiredText(project.createdAt, "project.createdAt", 80),
      updatedAt
    };
  } catch {
    return null;
  }
}
function storageKeyForScope(scope) {
  const normalized = normalizedScope(scope);
  return `${STORAGE_PREFIX}:${encodeURIComponent(normalized.workspaceId)}:${encodeURIComponent(normalized.userId)}`;
}
function createResearchProjectRepository(options = {}) {
  const storage = options.storage;
  if (!storage || typeof storage.getItem !== "function" || typeof storage.setItem !== "function") {
    throw new ResearchProjectRepositoryError("storage_unavailable", "a storage adapter is required");
  }
  const key = storageKeyForScope(options.scope);
  const now = typeof options.now === "function" ? options.now : () => Date.now();
  const randomID2 = options.randomID || (() => {
    var _a2, _b;
    return ((_b = (_a2 = globalThis.crypto) == null ? void 0 : _a2.randomUUID) == null ? void 0 : _b.call(_a2)) || "";
  });
  function read() {
    const raw = storage.getItem(key);
    if (!raw) return [];
    try {
      const parsed = JSON.parse(raw);
      if (!Array.isArray(parsed)) return [];
      return parsed.map(normalizeStoredProject).filter(Boolean);
    } catch {
      return [];
    }
  }
  function write(projects) {
    storage.setItem(key, JSON.stringify(projects));
  }
  function list2(options2 = {}) {
    const includeArchived = Boolean(options2.includeArchived);
    return read().filter((project) => includeArchived || project.status !== "archived").sort((left, right) => right.updatedAt.localeCompare(left.updatedAt)).map(cloneJSON);
  }
  function get(id2) {
    const project = read().find((item) => item.id === id2);
    if (!project) throw new ResearchProjectRepositoryError("project_not_found", `project ${id2} was not found`);
    return cloneJSON(project);
  }
  function create2(input) {
    const timestamp = new Date(now()).toISOString();
    const project = {
      id: projectID(now, randomID2),
      name: requiredText(input == null ? void 0 : input.name, "project.name", 120),
      objective: optionalText(input == null ? void 0 : input.objective, "project.objective", 1200),
      description: optionalText(input == null ? void 0 : input.description, "project.description", 4e3),
      nextStep: optionalText(input == null ? void 0 : input.nextStep, "project.nextStep", 1200),
      status: "active",
      stage: PROJECT_STAGES.has(input == null ? void 0 : input.stage) ? input.stage : "planning",
      resources: [],
      findings: [],
      createdAt: timestamp,
      updatedAt: timestamp
    };
    const projects = read();
    projects.push(project);
    write(projects);
    return cloneJSON(project);
  }
  function update(id2, input) {
    const projects = read();
    const index2 = projects.findIndex((item) => item.id === id2);
    if (index2 < 0) throw new ResearchProjectRepositoryError("project_not_found", `project ${id2} was not found`);
    const current = projects[index2];
    projects[index2] = {
      ...current,
      name: (input == null ? void 0 : input.name) === void 0 ? current.name : requiredText(input.name, "project.name", 120),
      objective: (input == null ? void 0 : input.objective) === void 0 ? current.objective : optionalText(input.objective, "project.objective", 1200),
      description: (input == null ? void 0 : input.description) === void 0 ? current.description : optionalText(input.description, "project.description", 4e3),
      nextStep: (input == null ? void 0 : input.nextStep) === void 0 ? current.nextStep : optionalText(input.nextStep, "project.nextStep", 1200),
      stage: (input == null ? void 0 : input.stage) === void 0 ? current.stage : requiredStage(input.stage),
      updatedAt: new Date(now()).toISOString()
    };
    write(projects);
    return cloneJSON(projects[index2]);
  }
  function archive(id2) {
    const projects = read();
    const index2 = projects.findIndex((item) => item.id === id2);
    if (index2 < 0) throw new ResearchProjectRepositoryError("project_not_found", `project ${id2} was not found`);
    projects[index2] = { ...projects[index2], status: "archived", updatedAt: new Date(now()).toISOString() };
    write(projects);
    return cloneJSON(projects[index2]);
  }
  function restore(id2) {
    const projects = read();
    const index2 = projects.findIndex((item) => item.id === id2);
    if (index2 < 0) throw new ResearchProjectRepositoryError("project_not_found", `project ${id2} was not found`);
    projects[index2] = { ...projects[index2], status: "active", updatedAt: new Date(now()).toISOString() };
    write(projects);
    return cloneJSON(projects[index2]);
  }
  function attachResource(id2, input) {
    const timestamp = new Date(now()).toISOString();
    const resource = normalizeResource({ ...input, linkedAt: (input == null ? void 0 : input.linkedAt) || timestamp }, timestamp);
    const projects = read();
    const index2 = projects.findIndex((item) => item.id === id2);
    if (index2 < 0) throw new ResearchProjectRepositoryError("project_not_found", `project ${id2} was not found`);
    const resources = projects[index2].resources.filter((item) => !(item.id === resource.id && item.source === resource.source));
    projects[index2] = {
      ...projects[index2],
      resources: [...resources, resource],
      updatedAt: timestamp
    };
    write(projects);
    return cloneJSON(projects[index2]);
  }
  function detachResource(id2, resourceID, source) {
    const projects = read();
    const index2 = projects.findIndex((item) => item.id === id2);
    if (index2 < 0) throw new ResearchProjectRepositoryError("project_not_found", `project ${id2} was not found`);
    projects[index2] = {
      ...projects[index2],
      resources: projects[index2].resources.filter((item) => !(item.id === resourceID && item.source === source)),
      updatedAt: new Date(now()).toISOString()
    };
    write(projects);
    return cloneJSON(projects[index2]);
  }
  function addFinding(id2, input) {
    const projects = read();
    const index2 = projects.findIndex((item) => item.id === id2);
    if (index2 < 0) throw new ResearchProjectRepositoryError("project_not_found", `project ${id2} was not found`);
    const timestamp = new Date(now()).toISOString();
    const finding = normalizeFinding({
      id: findingID(now, randomID2),
      type: input == null ? void 0 : input.type,
      title: input == null ? void 0 : input.title,
      content: input == null ? void 0 : input.content,
      createdAt: timestamp,
      updatedAt: timestamp
    });
    projects[index2] = {
      ...projects[index2],
      findings: [...projects[index2].findings, finding],
      updatedAt: timestamp
    };
    write(projects);
    return cloneJSON(finding);
  }
  function updateFinding(id2, findingIDValue, input) {
    const projects = read();
    const index2 = projects.findIndex((item) => item.id === id2);
    if (index2 < 0) throw new ResearchProjectRepositoryError("project_not_found", `project ${id2} was not found`);
    const findingIndex = projects[index2].findings.findIndex((item) => item.id === findingIDValue);
    if (findingIndex < 0) throw new ResearchProjectRepositoryError("finding_not_found", `finding ${findingIDValue} was not found`);
    const timestamp = new Date(now()).toISOString();
    const current = projects[index2].findings[findingIndex];
    const finding = normalizeFinding({
      ...current,
      type: (input == null ? void 0 : input.type) === void 0 ? current.type : input.type,
      title: (input == null ? void 0 : input.title) === void 0 ? current.title : input.title,
      content: (input == null ? void 0 : input.content) === void 0 ? current.content : input.content,
      updatedAt: timestamp
    });
    const findings = [...projects[index2].findings];
    findings[findingIndex] = finding;
    projects[index2] = { ...projects[index2], findings, updatedAt: timestamp };
    write(projects);
    return cloneJSON(finding);
  }
  function removeFinding(id2, findingIDValue) {
    const projects = read();
    const index2 = projects.findIndex((item) => item.id === id2);
    if (index2 < 0) throw new ResearchProjectRepositoryError("project_not_found", `project ${id2} was not found`);
    if (!projects[index2].findings.some((item) => item.id === findingIDValue)) {
      throw new ResearchProjectRepositoryError("finding_not_found", `finding ${findingIDValue} was not found`);
    }
    projects[index2] = {
      ...projects[index2],
      findings: projects[index2].findings.filter((item) => item.id !== findingIDValue),
      updatedAt: new Date(now()).toISOString()
    };
    write(projects);
    return true;
  }
  return Object.freeze({
    key,
    list: list2,
    get,
    create: create2,
    update,
    archive,
    restore,
    attachResource,
    detachResource,
    addFinding,
    updateFinding,
    removeFinding
  });
}
function stageLabel(stage) {
  var _a2;
  return ((_a2 = RESEARCH_STAGE_OPTIONS.find((option) => option.id === stage)) == null ? void 0 : _a2.label) || stage || "研究规划";
}
function findingTypeLabel(type) {
  var _a2;
  return ((_a2 = RESEARCH_FINDING_TYPE_OPTIONS.find((option) => option.id === type)) == null ? void 0 : _a2.label) || type || "研究结论";
}
function projectBriefMarkdown(project) {
  var _a2, _b;
  const lines = [
    `# ${project.name}`,
    "",
    `阶段：${stageLabel(project.stage)}`,
    "",
    "## 研究目标",
    "",
    project.objective || "未填写",
    "",
    "## 项目说明",
    "",
    project.description || "未填写",
    "",
    "## 下一步",
    "",
    project.nextStep || "未填写",
    "",
    "## 研究发现",
    ""
  ];
  if (!((_a2 = project.findings) == null ? void 0 : _a2.length)) lines.push("暂无研究发现");
  else project.findings.forEach((finding) => {
    lines.push(`### ${finding.title}`, "", `类型：${findingTypeLabel(finding.type)}`, "", finding.content || "未填写", "");
  });
  lines.push(
    "## 关联成果",
    ""
  );
  if (!((_b = project.resources) == null ? void 0 : _b.length)) lines.push("暂无关联成果");
  else project.resources.forEach((resource) => lines.push(`- ${resource.title} (${resource.type})`));
  lines.push("", `更新时间：${project.updatedAt}`, "");
  return lines.join("\n");
}
function sessionSelectionOptions(input) {
  if (!Array.isArray(input)) return Object.freeze([]);
  const seen = /* @__PURE__ */ new Set();
  const options = [];
  for (const session2 of input) {
    if (!session2 || typeof session2 !== "object" || Array.isArray(session2)) continue;
    const id2 = typeof session2.id === "string" ? session2.id.trim() : "";
    if (!id2 || seen.has(id2)) continue;
    seen.add(id2);
    const title = typeof session2.title === "string" ? session2.title.trim() : "";
    const status = typeof session2.status === "string" ? session2.status.trim() : "";
    options.push(Object.freeze({
      id: id2,
      label: title || id2,
      description: [title && title !== id2 ? id2 : "", status].filter(Boolean).join(" · "),
      keywords: [title, id2, status].filter(Boolean).join(" ")
    }));
  }
  return Object.freeze(options);
}
const repositories = /* @__PURE__ */ new Map();
const filterOptions = Object.freeze([
  Object.freeze({ id: "active", label: "进行中" }),
  Object.freeze({ id: "archived", label: "已归档" }),
  Object.freeze({ id: "all", label: "全部" })
]);
const viewOptions = Object.freeze([
  Object.freeze({ id: "overview", label: "概览" }),
  Object.freeze({ id: "resources", label: "资料" }),
  Object.freeze({ id: "findings", label: "研究发现" })
]);
function repositoryFor(scope) {
  const key = `${scope.workspaceId}:${scope.userId}`;
  if (!repositories.has(key)) {
    repositories.set(key, createResearchProjectRepository({ storage: window.localStorage, scope }));
  }
  return repositories.get(key);
}
function projectForm(project) {
  return {
    title: project ? "编辑科研项目" : "新建科研项目",
    description: "明确目标和下一步，后续资料与发现都围绕该项目沉淀。",
    schema: {
      type: "object",
      required: ["name", "objective"],
      properties: {
        name: { type: "string", title: "项目名称", description: "例如：新能源材料技术路线调研" },
        objective: { type: "string", format: "textarea", title: "研究目标", description: "说明本项目需要回答的核心问题" },
        nextStep: { type: "string", format: "textarea", title: "下一步", description: "当前最需要推进的具体动作" },
        description: { type: "string", format: "textarea", title: "范围与说明", description: "边界、约束、方法或阶段安排" }
      }
    },
    initialValues: project ? {
      name: project.name,
      objective: project.objective,
      nextStep: project.nextStep,
      description: project.description
    } : {},
    submitLabel: project ? "保存修改" : "创建项目"
  };
}
function findingForm(finding) {
  var _a2;
  const typeLabels = RESEARCH_FINDING_TYPE_OPTIONS.map((option) => option.label);
  const initialType = ((_a2 = RESEARCH_FINDING_TYPE_OPTIONS.find((option) => option.id === (finding == null ? void 0 : finding.type))) == null ? void 0 : _a2.label) || typeLabels[0];
  return {
    title: finding ? "编辑研究发现" : "记录研究发现",
    description: "区分已形成的结论、仍待验证的假设和开放问题。",
    schema: {
      type: "object",
      required: ["type", "title"],
      properties: {
        type: { type: "string", title: "类型", enum: typeLabels },
        title: { type: "string", title: "标题", description: "用一句话概括" },
        content: { type: "string", format: "textarea", title: "内容与依据", description: "记录判断、证据、限制或需要补充的数据" }
      }
    },
    initialValues: {
      type: initialType,
      title: (finding == null ? void 0 : finding.title) || "",
      content: (finding == null ? void 0 : finding.content) || ""
    },
    submitLabel: finding ? "保存修改" : "记录发现"
  };
}
function formatDate(value) {
  try {
    return new Intl.DateTimeFormat("zh-CN", {
      year: "numeric",
      month: "2-digit",
      day: "2-digit",
      hour: "2-digit",
      minute: "2-digit",
      hour12: false
    }).format(new Date(value));
  } catch {
    return value;
  }
}
function safeFilename(value) {
  return String(value || "research-project").replace(/[\\/:*?"<>|]+/g, "-").replace(/\s+/g, "-").slice(0, 80);
}
function downloadMarkdown(project, markdown) {
  const blob = new Blob([markdown], { type: "text/markdown;charset=utf-8" });
  const url = URL.createObjectURL(blob);
  const anchor = document.createElement("a");
  anchor.href = url;
  anchor.download = `${safeFilename(project.name)}-摘要.md`;
  anchor.click();
  window.setTimeout(() => URL.revokeObjectURL(url), 0);
}
function stageOption(stage) {
  return RESEARCH_STAGE_OPTIONS.find((option) => option.id === stage) || RESEARCH_STAGE_OPTIONS[0];
}
function findingTypeOption(type) {
  return RESEARCH_FINDING_TYPE_OPTIONS.find((option) => option.id === type) || RESEARCH_FINDING_TYPE_OPTIONS[0];
}
function sourceSessionID(resource) {
  var _a2;
  const prefix = "tma.session-artifact:";
  return ((_a2 = resource.source) == null ? void 0 : _a2.startsWith(prefix)) ? resource.source.slice(prefix.length) : resource.source;
}
function resourceTypeLabel(type) {
  if (type === "file") return "文件";
  if (type === "artifact") return "任务产物";
  return type || "资源";
}
function projectActivity(project) {
  return [
    ...project.resources.map((resource) => ({
      id: `resource:${resource.source}:${resource.id}`,
      label: "关联资料",
      title: resource.title,
      time: resource.linkedAt || project.updatedAt
    })),
    ...project.findings.map((finding) => ({
      id: `finding:${finding.id}`,
      label: findingTypeOption(finding.type).label,
      title: finding.title,
      time: finding.updatedAt
    }))
  ].sort((left, right) => right.time.localeCompare(left.time)).slice(0, 6);
}
const plugin = {
  id: "com.tma.research-projects",
  activate(context) {
    const repository = repositoryFor(context.scope);
    context.commands.register("com.tma.research-projects.create-project", async (input) => repository.create(input));
    context.commands.register("com.tma.research-projects.export-brief", async ({ projectID: projectID2 }) => projectBriefMarkdown(repository.get(projectID2)));
  }
};
function ResearchProjectsPage({ context }) {
  const repository = reactExports.useMemo(() => repositoryFor(context.scope), [context]);
  const [allProjects, setAllProjects] = reactExports.useState(() => repository.list({ includeArchived: true }));
  const [activeID, setActiveID] = reactExports.useState(() => {
    var _a2, _b;
    return ((_a2 = allProjects.find((project) => project.status === "active")) == null ? void 0 : _a2.id) || ((_b = allProjects[0]) == null ? void 0 : _b.id) || "";
  });
  const [filter, setFilter] = reactExports.useState("active");
  const [query, setQuery] = reactExports.useState("");
  const [view, setView] = reactExports.useState("overview");
  const [busy, setBusy] = reactExports.useState("");
  const projects = reactExports.useMemo(() => {
    const normalized = query.trim().toLocaleLowerCase();
    return allProjects.filter((project) => {
      if (filter !== "all" && project.status !== filter) return false;
      if (!normalized) return true;
      return `${project.name}
${project.objective}
${project.nextStep}`.toLocaleLowerCase().includes(normalized);
    });
  }, [allProjects, filter, query]);
  const activeProject = projects.find((project) => project.id === activeID) || projects[0] || null;
  const counts = reactExports.useMemo(() => ({
    active: allProjects.filter((project) => project.status === "active").length,
    archived: allProjects.filter((project) => project.status === "archived").length
  }), [allProjects]);
  function refresh(preferredID = activeID) {
    const next = repository.list({ includeArchived: true });
    setAllProjects(next);
    if (preferredID) setActiveID(preferredID);
  }
  reactExports.useEffect(() => {
    var _a2;
    if (!projects.some((project) => project.id === activeID)) setActiveID(((_a2 = projects[0]) == null ? void 0 : _a2.id) || "");
  }, [activeID, projects]);
  async function createProject() {
    const values = await context.dialog.form(projectForm());
    if (!values) return;
    setBusy("create");
    try {
      const project = await context.commands.execute("com.tma.research-projects.create-project", values);
      setFilter("active");
      setQuery("");
      setView("overview");
      refresh(project.id);
      context.notifications.show({
        level: "success",
        title: "科研项目已创建",
        message: project.name,
        dedupeKey: "research.projects.created"
      });
    } finally {
      setBusy("");
    }
  }
  async function editProject() {
    if (!activeProject) return;
    const values = await context.dialog.form(projectForm(activeProject));
    if (!values) return;
    repository.update(activeProject.id, values);
    refresh(activeProject.id);
    context.notifications.show({ level: "success", title: "项目已更新", message: values.name });
  }
  async function changeStage() {
    if (!activeProject || activeProject.status === "archived") return;
    const stage = await context.dialog.choice({
      title: "推进研究阶段",
      description: activeProject.name,
      items: RESEARCH_STAGE_OPTIONS.map((option, index2) => ({
        value: option.id,
        label: option.label,
        description: `第 ${index2 + 1} 阶段`
      })),
      initialValue: activeProject.stage,
      searchable: false,
      submitLabel: "更新阶段"
    });
    if (!stage) return;
    repository.update(activeProject.id, { stage });
    refresh(activeProject.id);
    context.notifications.show({ level: "success", title: "研究阶段已更新", message: stageOption(stage).label });
  }
  async function archiveProject() {
    if (!activeProject) return;
    const confirmed = await context.dialog.confirm({
      title: "归档科研项目",
      description: activeProject.name,
      detail: "项目、资料和研究发现都会保留，可以随时恢复。",
      confirmLabel: "归档",
      tone: "warning"
    });
    if (!confirmed) return;
    repository.archive(activeProject.id);
    refresh("");
    context.notifications.show({ level: "success", title: "项目已归档", message: activeProject.name });
  }
  function restoreProject() {
    if (!activeProject) return;
    repository.restore(activeProject.id);
    setFilter("active");
    refresh(activeProject.id);
    context.notifications.show({ level: "success", title: "项目已恢复", message: activeProject.name });
  }
  async function attachArtifact() {
    if (!activeProject) return;
    setBusy("resources");
    try {
      const sessions2 = await context.tasks.list({ workspaceId: context.scope.workspaceId, limit: 50 });
      const sessionOptions = sessionSelectionOptions(sessions2);
      if (!sessionOptions.length) {
        context.notifications.show({ level: "info", title: "当前工作区暂无任务" });
        return;
      }
      const sessionID = await context.dialog.choice({
        title: "选择任务",
        description: "从当前工作区最近任务中选择已有成果来源。",
        items: sessionOptions.map((option) => ({
          value: option.id,
          label: option.label,
          description: option.description,
          keywords: option.keywords
        })),
        initialValue: sessionOptions[0].id,
        searchable: true,
        searchPlaceholder: "搜索任务标题或 Session ID",
        emptyMessage: "没有匹配的任务",
        submitLabel: "查看成果"
      });
      if (!sessionID) return;
      const artifacts2 = await context.artifacts.list(sessionID);
      const resources = await context.resources.listRelated({ sessionID, artifacts: artifacts2 });
      if (!resources.length) {
        context.notifications.show({ level: "info", title: "该任务暂无成果", message: sessionID });
        return;
      }
      const resourceID = await context.dialog.choice({
        title: "选择成果",
        description: sessionID,
        items: resources.map((resource2) => ({
          value: resource2.id,
          label: resource2.title,
          description: `${resourceTypeLabel(resource2.type)} · ${resource2.id}`,
          keywords: `${resource2.title} ${resource2.type} ${resource2.id}`
        })),
        initialValue: resources[0].id,
        searchable: true,
        searchPlaceholder: "搜索成果名称或 Artifact ID",
        emptyMessage: "没有匹配的成果",
        submitLabel: "关联到项目"
      });
      if (!resourceID) return;
      const resource = resources.find((item) => item.id === resourceID);
      if (!resource) throw new Error("选择的任务成果不存在");
      repository.attachResource(activeProject.id, resource);
      refresh(activeProject.id);
      setView("resources");
      context.notifications.show({ level: "success", title: "成果已关联", message: resource.title });
    } catch (error) {
      context.notifications.show({
        level: "error",
        title: "读取任务或成果失败",
        message: error.message || String(error),
        dedupeKey: "research.resources.failed"
      });
    } finally {
      setBusy("");
    }
  }
  async function openResource(resource) {
    try {
      await context.resources.open(resource);
    } catch (error) {
      context.notifications.show({ level: "error", title: "无法打开成果", message: error.message || String(error) });
    }
  }
  async function removeResource(resource) {
    if (!activeProject) return;
    const confirmed = await context.dialog.confirm({
      title: "移除关联成果",
      description: resource.title,
      detail: "只会移除项目关联，不会删除任务中的原始成果。",
      confirmLabel: "移除",
      tone: "warning"
    });
    if (!confirmed) return;
    repository.detachResource(activeProject.id, resource.id, resource.source);
    refresh(activeProject.id);
  }
  async function saveFinding(finding = null) {
    var _a2;
    if (!activeProject || activeProject.status === "archived") return;
    const values = await context.dialog.form(findingForm(finding));
    if (!values) return;
    const type = ((_a2 = RESEARCH_FINDING_TYPE_OPTIONS.find((option) => option.label === values.type)) == null ? void 0 : _a2.id) || "finding";
    if (finding) repository.updateFinding(activeProject.id, finding.id, { ...values, type });
    else repository.addFinding(activeProject.id, { ...values, type });
    refresh(activeProject.id);
    context.notifications.show({ level: "success", title: finding ? "研究发现已更新" : "研究发现已记录", message: values.title });
  }
  async function removeFinding(finding) {
    if (!activeProject) return;
    const confirmed = await context.dialog.confirm({
      title: "删除研究发现",
      description: finding.title,
      detail: "该记录会从项目摘要中移除。",
      confirmLabel: "删除",
      tone: "danger"
    });
    if (!confirmed) return;
    repository.removeFinding(activeProject.id, finding.id);
    refresh(activeProject.id);
  }
  async function exportBrief() {
    if (!activeProject) return;
    const markdown = await context.commands.execute("com.tma.research-projects.export-brief", { projectID: activeProject.id });
    downloadMarkdown(activeProject, markdown);
    context.notifications.show({ level: "success", title: "项目摘要已导出", message: `${activeProject.name}.md` });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "research-projects-page", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "research-projects-heading", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Research Workspace" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { children: "科研项目" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { children: [
          context.scope.workspaceId,
          " · ",
          counts.active,
          " 个进行中项目"
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: createProject, disabled: busy === "create", children: "新建项目" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "research-projects-toolbar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("input", { "aria-label": "搜索科研项目", onChange: (event) => setQuery(event.target.value), placeholder: "搜索项目、目标或下一步", type: "search", value: query }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "aria-label": "项目状态", className: "research-filter-control", role: "group", children: filterOptions.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: filter === option.id ? "active" : "", onClick: () => setFilter(option.id), type: "button", children: [
        option.label,
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: option.id === "active" ? counts.active : option.id === "archived" ? counts.archived : allProjects.length })
      ] }, option.id)) })
    ] }),
    !projects.length ? /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "research-empty-state", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: query ? "没有匹配的科研项目" : filter === "archived" ? "没有已归档项目" : "还没有科研项目" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: query ? "调整搜索条件后重试。" : "创建项目并明确研究目标、下一步和资料来源。" }),
      !query && filter !== "archived" ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: createProject, children: "新建项目" }) : null
    ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "research-projects-layout", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("aside", { className: "research-project-list", "aria-label": "科研项目列表", children: projects.map((project) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          className: project.id === (activeProject == null ? void 0 : activeProject.id) ? "active" : "",
          type: "button",
          onClick: () => {
            setActiveID(project.id);
            setView("overview");
          },
          children: /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "research-project-list-copy", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: project.name }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("em", { children: stageOption(project.stage).label })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: project.nextStep || project.objective || "未填写下一步" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "research-project-list-meta", children: [
              project.resources.length,
              " 份资料 · ",
              project.findings.length,
              " 条发现"
            ] })
          ] })
        },
        project.id
      )) }),
      activeProject ? /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "research-project-detail", "aria-label": activeProject.name, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "research-project-detail-header", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `research-status ${activeProject.status}`, children: activeProject.status === "archived" ? "已归档" : stageOption(activeProject.stage).label }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: activeProject.name }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
              "更新于 ",
              formatDate(activeProject.updatedAt)
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "research-project-actions", children: [
            activeProject.status === "archived" ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: restoreProject, children: "恢复项目" }) : /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: editProject, children: "编辑" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: exportBrief, children: "导出摘要" }),
            activeProject.status !== "archived" ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: archiveProject, children: "归档" }) : null
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "aria-label": "项目视图", className: "research-view-tabs", role: "tablist", children: viewOptions.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            "aria-selected": view === option.id,
            className: view === option.id ? "active" : "",
            onClick: () => setView(option.id),
            role: "tab",
            type: "button",
            children: [
              option.label,
              option.id === "resources" ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: activeProject.resources.length }) : null,
              option.id === "findings" ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: activeProject.findings.length }) : null
            ]
          },
          option.id
        )) }),
        view === "overview" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "research-overview-view", role: "tabpanel", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "research-stage-section", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "研究进度" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: stageOption(activeProject.stage).label })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", disabled: activeProject.status === "archived", onClick: changeStage, type: "button", children: "调整阶段" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "research-stage-track", children: RESEARCH_STAGE_OPTIONS.map((option, index2) => {
              const currentIndex = RESEARCH_STAGE_OPTIONS.findIndex((item) => item.id === activeProject.stage);
              const state = index2 < currentIndex ? "complete" : index2 === currentIndex ? "current" : "pending";
              return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: state, children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: index2 + 1 }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: option.label })
              ] }, option.id);
            }) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "research-summary-grid", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "资料" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: activeProject.resources.length }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "已关联任务成果" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "研究发现" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: activeProject.findings.length }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "结论、假设与问题" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "待验证" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: activeProject.findings.filter((finding) => finding.type === "question").length }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "仍需回答的问题" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "research-overview-copy", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "研究目标" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: activeProject.objective || "未填写" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "research-next-step", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "下一步" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: activeProject.nextStep || "尚未设置下一步" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "范围与说明" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: activeProject.description || "未填写" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "research-activity-section", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("header", { children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "最近沉淀" }) }),
            projectActivity(activeProject).length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "research-activity-list", children: projectActivity(activeProject).map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: item.label }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: item.title }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("time", { children: formatDate(item.time) })
            ] }, item.id)) }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "research-section-empty", children: "还没有资料或研究发现" })
          ] })
        ] }) : null,
        view === "resources" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "research-resource-section", role: "tabpanel", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "项目资料" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                activeProject.resources.length,
                " 项"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: attachArtifact, disabled: busy === "resources" || activeProject.status === "archived", children: busy === "resources" ? "读取中..." : "关联任务成果" })
          ] }),
          !activeProject.resources.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "research-section-empty", children: "暂无资料，先从已有任务中关联成果。" }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "research-resource-list", children: activeProject.resources.map((resource) => /* @__PURE__ */ jsxRuntimeExports.jsxs("article", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "research-resource-kind", children: resourceTypeLabel(resource.type) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: resource.title }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
                sourceSessionID(resource),
                " · 关联于 ",
                formatDate(resource.linkedAt)
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => openResource(resource), children: "打开" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => removeResource(resource), disabled: activeProject.status === "archived", children: "移除" })
            ] })
          ] }, `${resource.source}:${resource.id}`)) })
        ] }) : null,
        view === "findings" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "research-findings-section", role: "tabpanel", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "研究发现" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                activeProject.findings.length,
                " 条"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: () => saveFinding(), disabled: activeProject.status === "archived", children: "记录发现" })
          ] }),
          !activeProject.findings.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "research-section-empty", children: "暂无发现，记录结论、假设或待验证问题。" }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "research-finding-list", children: activeProject.findings.map((finding) => /* @__PURE__ */ jsxRuntimeExports.jsxs("article", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `research-finding-type ${finding.type}`, children: findingTypeOption(finding.type).label }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("time", { children: formatDate(finding.updatedAt) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { children: finding.title }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: finding.content || "未填写内容与依据" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("footer", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => saveFinding(finding), disabled: activeProject.status === "archived", children: "编辑" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => removeFinding(finding), disabled: activeProject.status === "archived", children: "删除" })
            ] })
          ] }, finding.id)) })
        ] }) : null
      ] }) : null
    ] })
  ] });
}
const _package = Object.freeze({
  manifest,
  plugin,
  components: Object.freeze({ ResearchProjectsPage }),
  enablement: Object.freeze({ defaultEnabled: true })
});
const __vite_glob_1_0 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: _package
}, Symbol.toStringTag, { value: "Module" }));
function pluginPackageFromModule(moduleValue, source) {
  var _a2, _b, _c, _d, _e;
  const pluginPackage = moduleValue == null ? void 0 : moduleValue.default;
  if (!pluginPackage || typeof pluginPackage !== "object" || Array.isArray(pluginPackage)) {
    throw new TypeError(`Static plugin module ${source} must export a default package object.`);
  }
  const pluginID = typeof ((_a2 = pluginPackage.manifest) == null ? void 0 : _a2.id) === "string" ? pluginPackage.manifest.id.trim() : "";
  if (!pluginID) throw new TypeError(`Static plugin module ${source} is missing manifest.id.`);
  if (((_b = pluginPackage.plugin) == null ? void 0 : _b.id) !== pluginID || typeof ((_c = pluginPackage.plugin) == null ? void 0 : _c.activate) !== "function") {
    throw new TypeError(`Static plugin module ${source} must export an activate() implementation for ${pluginID}.`);
  }
  const components = pluginPackage.components;
  if (!components || typeof components !== "object" || Array.isArray(components)) {
    throw new TypeError(`Static plugin module ${source} must export a components object.`);
  }
  for (const route of ((_e = (_d = pluginPackage.manifest) == null ? void 0 : _d.contributes) == null ? void 0 : _e.routes) || []) {
    if (typeof components[route.component] !== "function") {
      throw new TypeError(`Static plugin ${pluginID} is missing route component ${route.component}.`);
    }
  }
  return Object.freeze({
    ...pluginPackage,
    components: Object.freeze({ ...components })
  });
}
function buildStaticPluginCatalog(modules) {
  if (!modules || typeof modules !== "object" || Array.isArray(modules)) {
    throw new TypeError("Static plugin modules must be an object.");
  }
  const seen = /* @__PURE__ */ new Set();
  const catalog = Object.entries(modules).map(([source, moduleValue]) => {
    const pluginPackage = pluginPackageFromModule(moduleValue, source);
    const pluginID = pluginPackage.manifest.id;
    if (seen.has(pluginID)) throw new TypeError(`Static plugin id ${pluginID} is duplicated.`);
    seen.add(pluginID);
    return pluginPackage;
  });
  catalog.sort((left, right) => left.manifest.id.localeCompare(right.manifest.id));
  return Object.freeze(catalog);
}
function loadPluginCatalog(runtime, catalog, options = {}) {
  if (!runtime || typeof runtime.load !== "function") {
    throw new TypeError("A Workbench plugin runtime is required.");
  }
  if (!Array.isArray(catalog)) {
    throw new TypeError("A static plugin catalog is required.");
  }
  return runtime.load(catalog, options);
}
const packageModules = {
  .../* @__PURE__ */ Object.assign({ "./workbenchDiagnostics/package.js": __vite_glob_0_0 }),
  .../* @__PURE__ */ Object.assign({ "./researchProjects/package.jsx": __vite_glob_1_0 })
};
const staticPluginCatalog = buildStaticPluginCatalog(packageModules);
function loadStaticPluginCatalog(runtime, options = {}) {
  return loadPluginCatalog(runtime, staticPluginCatalog, options);
}
const activeSessionStorageKey = "tma.workbench.active-session";
const workflowStoragePrefix = "tma.workbench.workflow.";
const workbenchDialogService = createDialogService();
const workbenchNotificationService = createNotificationService();
const workbenchRelatedResourceService = createRelatedResourceService();
workbenchRelatedResourceService.registerProvider(createSessionArtifactProvider({
  downloadArtifact,
  artifactDownloadPath
}));
const workbenchHostPermissionService = createPermissionService({ grants: [] });
const workbenchTaskService = Object.freeze({
  async list(filters = {}) {
    const response = await sessions({
      limit: filters.limit || 50,
      ...filters.workspaceId ? { workspace: filters.workspaceId } : {},
      ...filters.status ? { status: filters.status } : {},
      ...filters.includeArchived ? { includeArchived: true } : {}
    });
    return response.sessions || [];
  }
});
const workbenchArtifactService = Object.freeze({
  async list(sessionID) {
    const response = await artifacts(sessionID);
    return response.artifacts || [];
  }
});
const workbenchScopedHTTPService = Object.freeze({
  async request(path2, options = {}) {
    const target = String(path2 || "").trim();
    if (!target.startsWith("/v2/") || target.startsWith("//")) {
      throw new Error("Plugin HTTP requests must target the scoped /v2 API.");
    }
    const requestOptions2 = { ...options };
    if (requestOptions2.body && typeof requestOptions2.body === "object" && !(requestOptions2.body instanceof FormData)) {
      requestOptions2.headers = { "Content-Type": "application/json", ...requestOptions2.headers || {} };
      requestOptions2.body = JSON.stringify(requestOptions2.body);
    }
    const response = await fetch(target, requestOptions2);
    const contentType = response.headers.get("Content-Type") || "";
    const body = contentType.includes("json") ? await response.json() : await response.text();
    if (!response.ok) throw new Error(typeof body === "string" ? body : (body == null ? void 0 : body.error) || `HTTP ${response.status}`);
    return body;
  }
});
const maxComposerFiles = 10;
const maxComposerFileBytes = 64 * 1024 * 1024;
const modelCapabilityOptions = [
  { value: "text", label: "文本" },
  { value: "text_image", label: "文本 + 图片解析" },
  { value: "image_generation", label: "图片生成" },
  { value: "video_generation", label: "视频生成" },
  { value: "embedding", label: "Embedding" },
  { value: "reranker", label: "Reranker" }
];
const embeddingProtocolOptions = ["openai_embeddings", "tei_embeddings", "ollama_embed"];
const rerankerProtocolOptions = ["jina_rerank", "cohere_rerank", "vllm_score"];
function defaultModelCapabilities(capabilityType, current = {}) {
  if (capabilityType === "embedding") {
    return {
      dimensions: Number(current.dimensions || 1024),
      distance_metric: current.distance_metric || "cosine",
      normalized: current.normalized !== false,
      max_batch_size: Number(current.max_batch_size || 32),
      protocol: current.protocol || "openai_embeddings"
    };
  }
  if (capabilityType === "reranker") {
    return {
      max_candidates: Number(current.max_candidates || 50),
      protocol: current.protocol || "jina_rerank"
    };
  }
  return {};
}
const supportedVisionImageTypes = /* @__PURE__ */ new Set(["image/png", "image/jpeg", "image/gif", "image/webp"]);
function workbenchSurface() {
  if (window.matchMedia("(max-width: 640px)").matches) return "web_mobile";
  if (window.matchMedia("(max-width: 1100px)").matches) return "web_tablet";
  return "web_desktop";
}
function pluginPathFromHash() {
  try {
    const value = decodeURIComponent(String(window.location.hash || "").replace(/^#/, ""));
    return value.startsWith("/plugins/") ? value : "";
  } catch {
    return "";
  }
}
const sessionSyncEventTypes = /* @__PURE__ */ new Set([
  "agent.message",
  "runtime.tool_intervention_required",
  "runtime.tool_intervention_approved",
  "runtime.tool_intervention_rejected",
  "runtime.failed",
  "runtime.completed",
  "session.status_idle",
  "session.status_failed",
  "session.status_terminated",
  "session.config_updated"
]);
function Empty({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "empty", children });
}
function Panel({ title, children, className = "" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: `panel ${className}`.trim(), children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: title }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "content", children })
  ] });
}
function Pill({ value }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: pillClass(value || "unknown"), children: value || "unknown" });
}
function Meta({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "meta", children });
}
function TaskStatusIcon({ status }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": "true", className: `task-status-icon ${status || "unknown"}` });
}
function DeleteIcon() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M5.5 4.5h5m-4-1.5h3m-4 3v6h5v-6m-7-1.5h9", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: "1.4" }) });
}
function RefreshIcon() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { "aria-hidden": "true", viewBox: "0 0 24 24", children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M20 6v5h-5M4 18v-5h5M6.1 9a7 7 0 0 1 11.5-2.6L20 9M4 15l2.4 2.6A7 7 0 0 0 17.9 15", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: "1.8" }) });
}
function PinIcon({ filled = false }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M5 2.5h6l-1 3 2 2v1H8.7V13L8 14l-.7-1V8.5H4v-1l2-2-1-3Z", fill: filled ? "currentColor" : "none", stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: "1.25" }) });
}
function MoreIcon() {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("circle", { cx: "3.5", cy: "8", r: "1", fill: "currentColor" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("circle", { cx: "8", cy: "8", r: "1", fill: "currentColor" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("circle", { cx: "12.5", cy: "8", r: "1", fill: "currentColor" })
  ] });
}
function FolderIcon({ open = false }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: open ? "M2.5 5.5h11l-1.2 7h-8.6l-1.2-7Zm1-2h3l1 1h5v1" : "M2.5 4h4l1 1h6v7.5h-11V4Z", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: "1.3" }) });
}
function FileIcon() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M4 2.5h5l3 3v8H4v-11Zm5 0v3h3", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: "1.3" }) });
}
function CloseIcon() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m4 4 8 8m0-8-8 8", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeWidth: "1.5" }) });
}
function formatFileSize(size) {
  const bytes = Number(size || 0);
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1024 * 1024) return `${Math.ceil(bytes / 1024)} KB`;
  return `${(bytes / (1024 * 1024)).toFixed(bytes >= 10 * 1024 * 1024 ? 0 : 1)} MB`;
}
function composerFileID(file) {
  return `${file.name}-${file.size}-${file.lastModified}-${Math.random().toString(36).slice(2)}`;
}
function clipboardImageFile(file, index2 = 0) {
  const type = String((file == null ? void 0 : file.type) || "").toLowerCase();
  const extensions = {
    "image/gif": "gif",
    "image/jpeg": "jpg",
    "image/png": "png",
    "image/webp": "webp"
  };
  const extension2 = extensions[type] || "png";
  const timestamp = (/* @__PURE__ */ new Date()).toISOString().replace(/[-:]/g, "").replace(/\.\d{3}Z$/, "");
  return new File([file], `clipboard-image-${timestamp}-${index2 + 1}.${extension2}`, {
    type: file.type || `image/${extension2}`,
    lastModified: Date.now() + index2
  });
}
function composerAttachmentValue(item) {
  return (item == null ? void 0 : item.file) || item || {};
}
function isSkillZIPAttachment(item) {
  const value = composerAttachmentValue(item);
  const name2 = String(value.name || "").trim().toLowerCase();
  const contentType = String(value.type || value.content_type || "").trim().toLowerCase();
  return name2.endsWith(".zip") || contentType === "application/zip" || contentType === "application/x-zip-compressed";
}
function defaultComposerTask(text2, attachments) {
  const requested = String(text2 || "").trim();
  if (requested) return requested;
  const files = Array.isArray(attachments) ? attachments : [];
  if (files.length === 1 && isSkillZIPAttachment(files[0])) {
    return "请将我上传的 ZIP 作为离线 Skill 安装。先调用 skills.preview，使用附件的 Session artifact_id，不要使用 workspace_path、主机路径或 URL。仅当 policy.allowed=true 且 install_state=new_install 或 upgrade 时再调用 skills.install；升级时设置 upgrade_existing=true，并原样携带 Preview 返回的 policy pin。安装完成后不要自动启用，先告诉我可以发起 skills.enable。";
  }
  return "请处理我上传的文件。";
}
function modelCapabilityLabel(value) {
  var _a2;
  return ((_a2 = modelCapabilityOptions.find((option) => option.value === value)) == null ? void 0 : _a2.label) || "文本";
}
function payload(event) {
  return (event == null ? void 0 : event.payload) || {};
}
function eventData(event) {
  const data2 = payload(event).data;
  return data2 && typeof data2 === "object" && !Array.isArray(data2) ? data2 : {};
}
function objectValue(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function firstValue(object, keys2) {
  for (const key of keys2) {
    const value = object == null ? void 0 : object[key];
    if (value !== void 0 && value !== null && String(value).trim() !== "") return value;
  }
  return "";
}
function valueText(value) {
  if (Array.isArray(value)) return value.map((item) => String(item)).join(" ");
  if (value && typeof value === "object") return JSON.stringify(value);
  return String(value || "");
}
const builtInToolNamespaces = /* @__PURE__ */ new Set(["default", "artifact", "browser", "agent", "skills", "web", "computer"]);
function humanizeToolName(value = "") {
  return String(value || "").replace(/[._-]+/g, " ").replace(/\s+/g, " ").trim();
}
function toolSource(identifier, source = "", manifestType = "") {
  const normalizedSource = String(source || "").trim().toLowerCase();
  if (normalizedSource) return normalizedSource;
  const normalizedType = String(manifestType || "").trim().toLowerCase();
  if (normalizedType === "mcp_server") return "mcp";
  if (normalizedType === "process_plugin") return "worker_plugin";
  if (builtInToolNamespaces.has(String(identifier || "").trim().toLowerCase())) return "builtin";
  return "tool";
}
function toolSourceLabel(source) {
  switch (String(source || "").trim().toLowerCase()) {
    case "mcp":
      return "MCP";
    case "worker_plugin":
      return "Worker Plugin";
    case "builtin":
      return "Builtin";
    default:
      return "Tool";
  }
}
function toolTitle(identifier, apiName, source = "") {
  const key = [identifier, apiName].filter(Boolean).join(".");
  const titles = {
    "default.run_command": "执行命令",
    "default.execute_code": "执行代码",
    "default.read_file": "读取文件",
    "default.write_file": "写入文件",
    "default.edit_file": "编辑文件",
    "web.search": "搜索网页",
    "web.crawl": "读取网页",
    "browser.open": "打开浏览器",
    "browser.click": "浏览器点击",
    "browser.type": "浏览器输入",
    "browser.takeover": "接管浏览器",
    "computer.get_state": "检查桌面",
    "computer.screenshot": "截取屏幕",
    "computer.click": "桌面点击",
    "computer.type_text": "桌面输入",
    "computer.hotkey": "按下快捷键",
    "computer.launch_app": "启动应用",
    "computer.open_url": "打开网址",
    "computer.search_web": "浏览器内搜索",
    "skills.search": "查找 Skill",
    "skills.inspect": "检查 Skill",
    "skills.discover": "发现 Skill",
    "skills.preview": "安全预览 Skill",
    "skills.read_asset": "读取 Skill 资产",
    "skills.install": "安装 Skill",
    "skills.enable": "启用 Skill",
    "skills.disable": "停用 Skill"
  };
  if (titles[key] || titles[`${identifier}.${apiName}`]) return titles[key] || titles[`${identifier}.${apiName}`];
  if (String(source || "").trim().toLowerCase() === "mcp") return humanizeToolName(apiName) || humanizeToolName(key) || "MCP 工具";
  return key || "调用工具";
}
function toolRisk(identifier, apiName, reason = "") {
  const api2 = String(apiName || "");
  const riskReason = String(reason || "");
  if (identifier === "skills" && ["install", "enable", "disable"].includes(api2)) return "high";
  if (riskReason.includes("network") || ["run_command", "execute_code", "write_file", "edit_file", "click", "type_text", "hotkey", "launch_app", "open_url"].includes(api2)) {
    return "high";
  }
  if (identifier === "web" || identifier === "browser" || identifier === "computer") return "medium";
  return "low";
}
function toolDetail(identifier, apiName, args = {}) {
  const command = firstValue(args, ["command", "cmd"]);
  if (apiName === "run_command" || apiName === "execute_code") {
    const suffix = valueText(firstValue(args, ["args", "arguments"]));
    return shortText([command, suffix].filter(Boolean).join(" "), 220) || "执行代码或命令。";
  }
  if (["read_file", "write_file", "edit_file"].includes(apiName)) {
    return shortText(valueText(firstValue(args, ["path", "file_path", "target_path", "filename"])), 220) || "访问工作区文件。";
  }
  if (apiName === "search") {
    return shortText(valueText(firstValue(args, ["query", "q"])), 220) || "搜索网页。";
  }
  if (apiName === "crawl" || apiName === "open_url") {
    return shortText(valueText(firstValue(args, ["url", "target_url"])), 220) || "打开或读取网页。";
  }
  if (identifier === "computer" || identifier === "browser") {
    return shortText(valueText(firstValue(args, ["app", "name", "url", "text", "selector", "capture_mode", "keys", "key"])), 220) || "与可见应用或浏览器交互。";
  }
  if (identifier === "skills") {
    const source = objectValue(args.source);
    const sourceLabel = source.provider === "artifact" ? `Session Artifact ${source.artifact_id || ""}`.trim() : [source.repository, source.ref, source.path].filter(Boolean).join(" · ");
    if (apiName === "preview") return sourceLabel ? `检查 ${sourceLabel}` : "检查 Skill 来源与安全策略。";
    if (apiName === "install") return [args.identifier || "待推导标识", sourceLabel].filter(Boolean).join(" · ");
    if (apiName === "enable") return `${args.identifier || "Skill"}${args.version ? ` v${args.version}` : ""} · 发布新的 Agent 配置版本`;
    if (apiName === "disable") return `${args.identifier || "Skill"} · 从新的 Agent 配置版本移除`;
    return shortText(valueText(firstValue(args, ["identifier", "query", "path"])), 220) || "访问当前 workspace 的 Skills Registry。";
  }
  return shortText(valueText(args), 220);
}
function normalizeToolParts(identifier = "", apiName = "") {
  const rawIdentifier = String(identifier || "");
  const rawApiName = String(apiName || "");
  if (rawApiName) return { identifier: rawIdentifier, apiName: rawApiName };
  const normalized = rawIdentifier.replace(/-/g, "_");
  const aliases = {
    web_search: { identifier: "web", apiName: "search" },
    web_crawl: { identifier: "web", apiName: "crawl" },
    browser: { identifier: "browser", apiName: "open_url" },
    computer: { identifier: "computer", apiName: "launch_app" },
    run_command: { identifier: "default", apiName: "run_command" },
    execute_code: { identifier: "default", apiName: "execute_code" },
    read_file: { identifier: "default", apiName: "read_file" },
    write_file: { identifier: "default", apiName: "write_file" },
    edit_file: { identifier: "default", apiName: "edit_file" }
  };
  if (aliases[normalized]) return aliases[normalized];
  const dotIndex = rawIdentifier.indexOf(".");
  if (dotIndex > 0) {
    return {
      identifier: rawIdentifier.slice(0, dotIndex),
      apiName: rawIdentifier.slice(dotIndex + 1)
    };
  }
  return { identifier: rawIdentifier, apiName: rawApiName };
}
function toolSummary({ identifier, apiName, args = {}, reason = "", success, source = "", manifestType = "" }) {
  const parts = normalizeToolParts(identifier, apiName);
  const resolvedSource = toolSource(parts.identifier, source, manifestType);
  const risk = toolRisk(parts.identifier, parts.apiName, reason);
  const title = toolTitle(parts.identifier, parts.apiName, resolvedSource);
  const detail = toolDetail(parts.identifier, parts.apiName, args);
  const status = success === true ? "Completed" : success === false ? "Failed" : "";
  return {
    detail,
    label: [parts.identifier, parts.apiName].filter(Boolean).join(".") || "tool",
    source: resolvedSource,
    sourceLabel: toolSourceLabel(resolvedSource),
    risk,
    status,
    title
  };
}
const builtinToolNamespaces = [
  { key: "default", title: "默认工具", description: "文件读写、命令执行和代码运行。" },
  { key: "browser", title: "浏览器工具", description: "打开页面、读取内容、点击输入和截图。" },
  { key: "web", title: "网页检索", description: "搜索公开网页并抓取页面正文。" },
  { key: "agent", title: "子智能体", description: "派生子任务、等待结果、收集输出。" },
  { key: "skills", title: "技能工具", description: "检索技能、查看技能和读取技能资产。" }
];
function parseToolPolicy(raw) {
  if (!raw) return { explicit: false, enabledToolPatterns: [], runtime: "" };
  if (Array.isArray(raw)) {
    return {
      explicit: true,
      enabledToolPatterns: raw.map((value) => String(value || "").trim()).filter(Boolean),
      runtime: ""
    };
  }
  if (typeof raw !== "object") return { explicit: false, enabledToolPatterns: [], runtime: "" };
  const enabledTools = Array.isArray(raw.enabled_tools) ? raw.enabled_tools : [];
  const toolsList = Array.isArray(raw.tools) ? raw.tools : [];
  return {
    explicit: true,
    enabledToolPatterns: [...enabledTools, ...toolsList].map((value) => String(value || "").trim()).filter(Boolean),
    runtime: String(raw.runtime || "").trim()
  };
}
function parseSkillsConfig(raw) {
  if (!raw) return { enabled: [] };
  if (Array.isArray(raw)) {
    return {
      enabled: raw.map((skill) => ({ skill: String(skill || "").trim() })).filter((item) => item.skill)
    };
  }
  if (typeof raw !== "object") return { enabled: [] };
  const enabled = Array.isArray(raw.enabled) ? raw.enabled : [];
  return {
    enabled: enabled.map((item) => {
      if (!item || typeof item !== "object") return null;
      return {
        skill: String(item.skill || "").trim(),
        version: Number(item.version || 0) || 0
      };
    }).filter((item) => item == null ? void 0 : item.skill)
  };
}
function parseMCPServers(raw) {
  if (!raw) return [];
  let source = raw;
  if (Array.isArray(raw)) {
    source = { servers: raw };
  }
  if (typeof source !== "object") return [];
  let servers = [];
  if (Array.isArray(source.servers)) {
    servers = source.servers;
  } else if (Array.isArray(source.mcpServers)) {
    servers = source.mcpServers;
  } else if (source.mcpServers && typeof source.mcpServers === "object") {
    servers = Object.entries(source.mcpServers).map(([identifier, value]) => ({ identifier, ...objectValue(value) }));
  } else if (source.servers && typeof source.servers === "object") {
    servers = Object.entries(source.servers).map(([identifier, value]) => ({ identifier, ...objectValue(value) }));
  }
  return servers.map((server) => {
    var _a2;
    const item = objectValue(server);
    const identifier = String(item.identifier || item.id || item.name || "").trim();
    if (!identifier) return null;
    return {
      command: String(item.command || "").trim(),
      description: String(item.description || "").trim(),
      disabled: Boolean(item.disabled),
      identifier,
      includeTools: Array.isArray(item.include_tools) ? item.include_tools.map((value) => String(value || "").trim()).filter(Boolean) : [],
      loggingLevel: String(((_a2 = item.logging) == null ? void 0 : _a2.level) || "").trim(),
      transport: String(item.transport || "stdio").trim(),
      url: String(item.url || "").trim(),
      title: String(item.title || "").trim()
    };
  }).filter(Boolean);
}
function toolNamespaceEnabled(namespace, policy) {
  if (!policy.explicit) return true;
  return policy.enabledToolPatterns.some((pattern) => pattern === namespace || pattern.startsWith(`${namespace}.`));
}
function runtimeSupportsToolItem(identifier, runtime) {
  const normalizedRuntime = String(runtime).trim() || "cloud_sandbox";
  if (identifier === "browser.takeover" || identifier === "browser.close") {
    return normalizedRuntime === "local_system";
  }
  return true;
}
function toolingGuidanceLabel(item) {
  switch (item.kind) {
    case "tool_namespace":
      return `工具命名空间 ${item.name}`;
    case "tool_api":
      return `工具 API ${item.name}`;
    case "skill":
      return `技能 ${item.title || item.name}`;
    case "mcp":
      return `MCP ${item.title || item.name}`;
    default:
      return item.title || item.name;
  }
}
function toolingHealthCapabilities(result) {
  return Array.isArray(result == null ? void 0 : result.capabilities) ? result.capabilities.map((value) => String(value || "").trim()).filter(Boolean) : [];
}
function toolingHealthCatalogMetric(result, field) {
  const value = Number((result == null ? void 0 : result[field]) || 0);
  return Number.isFinite(value) && value > 0 ? value : 0;
}
function toolingHostMetric(host, field) {
  const value = Number((host == null ? void 0 : host[field]) || 0);
  return Number.isFinite(value) && value >= 0 ? value : 0;
}
function buildToolingCatalog({ config, installedSkills, preferredRuntime }) {
  const toolPolicy = parseToolPolicy(config == null ? void 0 : config.tools);
  const skillsConfig = parseSkillsConfig(config == null ? void 0 : config.skills);
  const mcpServers2 = parseMCPServers(config == null ? void 0 : config.mcp);
  const enabledSkillMap = new Map(skillsConfig.enabled.map((item) => [item.skill, item]));
  const toolItems = [];
  for (const namespace of builtinToolNamespaces) {
    const enabled = toolNamespaceEnabled(namespace.key, toolPolicy);
    const selectable = enabled && runtimeSupportsToolItem(namespace.key, preferredRuntime);
    toolItems.push({
      category: "tools",
      description: enabled ? namespace.description : "当前智能体配置未启用这个工具命名空间。",
      disabledReason: enabled ? "" : "未在当前智能体工具配置中启用",
      key: `tool-namespace:${namespace.key}`,
      kind: "tool_namespace",
      name: namespace.key,
      selectable,
      status: selectable ? "available" : "disabled",
      title: namespace.title
    });
  }
  for (const pattern of toolPolicy.enabledToolPatterns) {
    if (!pattern.includes(".")) continue;
    const selectable = runtimeSupportsToolItem(pattern, preferredRuntime);
    toolItems.push({
      category: "tools",
      description: "当前智能体显式配置的工具 API。",
      disabledReason: selectable ? "" : "当前运行环境下不可用",
      key: `tool-api:${pattern}`,
      kind: "tool_api",
      name: pattern,
      selectable,
      status: selectable ? "available" : "disabled",
      title: pattern
    });
  }
  const knownSkillKeys = /* @__PURE__ */ new Set();
  const skillItems = (installedSkills || []).map((skill) => {
    knownSkillKeys.add(skill.identifier);
    const binding = enabledSkillMap.get(skill.identifier);
    const archived = skill.status === "archived";
    const selectable = Boolean(binding) && !archived;
    return {
      category: "skills",
      description: binding ? skill.description || "已为当前智能体启用。" : "该技能已安装，但当前智能体尚未启用。",
      disabledReason: archived ? "技能已归档" : !binding ? "当前智能体未启用该技能" : "",
      key: `skill:${skill.identifier}`,
      kind: "skill",
      name: skill.identifier,
      selectable,
      status: selectable ? "available" : "disabled",
      title: skill.title || skill.identifier
    };
  });
  for (const binding of skillsConfig.enabled) {
    if (knownSkillKeys.has(binding.skill)) continue;
    skillItems.push({
      category: "skills",
      description: "当前智能体配置中引用了这个技能，但工作区里没有找到对应安装项。",
      disabledReason: "工作区中未找到已安装版本",
      key: `skill:${binding.skill}`,
      kind: "skill",
      name: binding.skill,
      selectable: false,
      status: "disabled",
      title: binding.skill
    });
  }
  const mcpItems = mcpServers2.map((server) => {
    const selectable = !server.disabled && (server.transport === "streamable_http" ? Boolean(server.url) : Boolean(server.command));
    const hintedTools = server.includeTools.length ? `仅包含：${server.includeTools.join(", ")}` : "";
    return {
      category: "mcp",
      description: server.description || hintedTools || (server.transport === "streamable_http" ? `URL：${server.url || "未配置 URL"}` : `命令：${server.command || "未配置命令"}`),
      disabledReason: selectable ? "" : "MCP 服务配置不完整",
      key: `mcp:${server.identifier}`,
      kind: "mcp",
      name: server.identifier,
      selectable,
      status: selectable ? "available" : "disabled",
      title: server.title || server.identifier
    };
  });
  return {
    items: [...toolItems, ...skillItems, ...mcpItems],
    sections: [
      { key: "tools", title: "工具", items: toolItems },
      { key: "skills", title: "Skills", items: skillItems },
      { key: "mcp", title: "MCP", items: mcpItems }
    ]
  };
}
function buildGuidedTaskMessage(task, selectedItems) {
  const text2 = String(task || "").trim();
  if (!selectedItems.length) return text2;
  const lines = selectedItems.map((item) => `- ${toolingGuidanceLabel(item)}`);
  return [
    "请优先考虑使用以下已选择的能力来完成这次任务；如果其中某项并不适合当前问题，就忽略它，按最佳方案继续：",
    ...lines,
    "",
    "用户原始请求：",
    text2
  ].join("\n");
}
function processPreview(identifier, apiName, args = {}, result = {}, source = "") {
  const parts = normalizeToolParts(identifier, apiName);
  const command = shortText([firstValue(args, ["command", "cmd"]), valueText(firstValue(args, ["args", "arguments"]))].filter(Boolean).join(" "), 180);
  const path2 = shortText(valueText(firstValue(args, ["path", "file_path", "target_path", "filename"])), 180);
  const query = shortText(valueText(firstValue(args, ["query", "q"])), 180);
  const url = shortText(valueText(firstValue(args, ["url", "target_url"])), 180);
  const content2 = shortText(String(result.content || ""), 220);
  const error = objectValue(result.error);
  if (result.success === false) {
    return error.message || content2 || toolDetail(parts.identifier, parts.apiName, args) || "工具执行失败。";
  }
  if (parts.identifier === "skills") {
    const state = objectValue(result.state);
    if (parts.apiName === "preview") {
      const policy = objectValue(state.policy);
      const security = objectValue(state.security);
      const findingCount = Array.isArray(security.findings) ? security.findings.length : 0;
      return [
        `${state.title || state.identifier || args.identifier || "Skill"} · ${state.install_state || "previewed"}`,
        `Policy ${policy.allowed === true ? "允许" : policy.allowed === false ? "阻止" : "待确认"}`,
        `${Number(security.scanned_files || 0)} 个文件已扫描`,
        `${findingCount} 个发现`
      ].join(" · ");
    }
    if (parts.apiName === "install") {
      const skill = objectValue(state.skill);
      const version2 = objectValue(state.version);
      return `${skill.title || skill.identifier || args.identifier || "Skill"} v${version2.version || 1} 已${state.upgraded ? "升级" : "安装"}。`;
    }
    if (parts.apiName === "enable") {
      const binding = objectValue(state.binding);
      return `${binding.skill || args.identifier || "Skill"} v${binding.version || args.version || 1} 已写入 Agent 配置${state.requires_session_upgrade ? "；当前 Session 仍固定旧配置，需要升级或新建 Session" : ""}。`;
    }
    if (parts.apiName === "disable") {
      const binding = objectValue(state.binding);
      return state.removed === false ? `${binding.skill || args.identifier || "Skill"} 已处于停用状态。` : `${binding.skill || args.identifier || "Skill"} 已从 Agent 配置移除${state.requires_session_upgrade ? "；当前 Session 仍固定旧配置，需要应用新版本" : ""}。`;
    }
  }
  if (parts.apiName === "run_command") {
    return command ? `命令：${command}` : "命令执行完成。";
  }
  if (parts.apiName === "execute_code") {
    const language = firstValue(args, ["language"]);
    return language ? `代码：${language}` : "代码执行完成。";
  }
  if (parts.apiName === "read_file") {
    return path2 ? `${String(source).trim().toLowerCase() === "mcp" ? "MCP 读取" : "读取"}：${path2}` : "文件读取完成。";
  }
  if (parts.apiName === "write_file") {
    return path2 ? `写入：${path2}` : "文件写入完成。";
  }
  if (parts.apiName === "edit_file") {
    return path2 ? `编辑：${path2}` : "文件编辑完成。";
  }
  if (parts.apiName === "search") {
    return query ? `搜索：${query}` : "搜索完成。";
  }
  if (parts.apiName === "crawl" || parts.apiName === "open_url") {
    return url ? `打开：${url}` : "网页读取完成。";
  }
  return content2 || toolDetail(parts.identifier, parts.apiName, args) || "工具执行完成。";
}
function approvalSummary(intervention) {
  return toolSummary({
    identifier: intervention.tool_identifier,
    apiName: intervention.api_name,
    args: objectValue(intervention.arguments),
    reason: intervention.reason
  });
}
function ApprovalCard({ intervention, onApprove, onReject, busy, active }) {
  const summary = approvalSummary(intervention);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `approval-card risk-${summary.risk}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "approval-card-header", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: summary.title }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Meta, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: summary.label }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Pill, { value: intervention.status || "pending" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: `risk-chip ${summary.risk}`, children: [
            summary.risk,
            " risk"
          ] })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "approval-actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: busy, onClick: () => onApprove(intervention), children: active ? "审批中..." : "批准" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: busy, onClick: () => onReject(intervention), children: "拒绝" })
      ] })
    ] }),
    summary.detail ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "approval-summary", children: summary.detail }) : null,
    intervention.reason ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "approval-reason", children: intervention.reason }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "subtle", children: [
      "调用 ",
      intervention.call_id,
      " · 任务轮次 ",
      intervention.turn_id
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { className: "approval-details", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("summary", { children: "参数" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { children: pretty(intervention.arguments || {}) })
    ] })
  ] });
}
function ToolPickerModal({ loading, error, sections, selectedKeys, onToggle, onClose, onClear }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "tool-picker-backdrop", role: "presentation", onClick: onClose, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "tool-picker-modal", role: "dialog", "aria-modal": "true", "aria-label": "工具与技能", onClick: (event) => event.stopPropagation(), children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "tool-picker-header", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: "工具与技能" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "选择后会在发送时作为提示，优先引导智能体使用这些能力。" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "tool-picker-actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: onClear, disabled: !selectedKeys.length, children: "清空选择" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: onClose, children: "关闭" })
      ] })
    ] }),
    loading ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "empty-state compact", children: "正在加载可用能力..." }) : null,
    !loading && error ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "artifact-preview-error", children: error }) : null,
    !loading && !error ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "tool-picker-sections", children: sections.map((section) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "tool-picker-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "tool-picker-section-title", children: section.title }),
      section.items.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "tool-picker-list", children: section.items.map((item) => {
        const checked = selectedKeys.includes(item.key);
        return /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: `tool-picker-item ${item.selectable ? "" : "disabled"} ${checked ? "selected" : ""}`.trim(), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", checked, disabled: !item.selectable, onChange: () => onToggle(item.key) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "tool-picker-item-body", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "tool-picker-item-head", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: item.title }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `tool-picker-status ${item.status}`, children: item.selectable ? "可用" : "不可用" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: item.description }),
            !item.selectable && item.disabledReason ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "tool-picker-reason", children: item.disabledReason }) : null
          ] })
        ] }, item.key);
      }) }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "empty-state compact", children: "当前没有相关配置。" })
    ] }, section.key)) }) : null
  ] }) });
}
function TaskTemplateModal({ onClose, onSelect, templates }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "tool-picker-backdrop", role: "presentation", onClick: onClose, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "task-template-modal", role: "dialog", "aria-modal": "true", "aria-label": "任务模板", onClick: (event) => event.stopPropagation(), children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "tool-picker-header", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: "任务模板" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "选择常用任务配置，或按顺序工作流逐步执行。" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: onClose, children: "关闭" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "task-template-grid", children: templates.map((template) => /* @__PURE__ */ jsxRuntimeExports.jsxs("article", { className: "task-template-option", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "task-template-option-head", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: template.category }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: template.title })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: template.description }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "task-template-bindings", children: [
        (template.tools || []).map((tool) => /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: tool }, `tool:${tool}`)),
        (template.skills || []).map((skill) => /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "skill", children: skill }, `skill:${skill}`))
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("ol", { className: "task-template-steps", children: (template.workflow_steps || []).map((step) => /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: step.title }, step.id)) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "task-template-actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => onSelect(template, false), children: "填充任务" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: () => onSelect(template, true), children: "使用工作流" })
      ] })
    ] }, template.id)) })
  ] }) });
}
function ComparisonRunColumn({ label, run }) {
  var _a2, _b, _c, _d, _e, _f, _g;
  if (!run) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "comparison-run empty", children: "等待选择运行。" });
  const usage = ((_a2 = run.usage) == null ? void 0 : _a2.summary) || {};
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "comparison-run", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: ((_b = run.session) == null ? void 0 : _b.title) || ((_c = run.session) == null ? void 0 : _c.id) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: (_d = run.session) == null ? void 0 : _d.id })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("dl", { className: "comparison-metrics", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "模型" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("dd", { children: [
          run.llm_provider,
          " / ",
          run.llm_model
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "配置版本" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("dd", { children: [
          "#",
          ((_e = run.session) == null ? void 0 : _e.agent_config_version) || 1
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "耗时" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("dd", { children: [
          Number(run.duration_ms || 0).toLocaleString(),
          " ms"
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "Token" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { children: Number(usage.total_tokens || 0).toLocaleString() })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "输入 / 输出" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("dd", { children: [
          Number(usage.input_tokens || 0).toLocaleString(),
          " / ",
          Number(usage.output_tokens || 0).toLocaleString()
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { children: "结果文件" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { children: ((_f = run.artifacts) == null ? void 0 : _f.length) || 0 })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "comparison-copy", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "最终结果" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { children: run.result || "当前运行还没有最终回复。" })
    ] }),
    ((_g = run.artifacts) == null ? void 0 : _g.length) ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "comparison-files", children: run.artifacts.map((artifact) => /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: artifact.name }, artifact.id)) }) : null
  ] });
}
function SessionComparisonModal({
  sessions: sessions2,
  leftID,
  rightID,
  onLeftChange,
  onRightChange,
  onCompare,
  onClose,
  loading,
  result,
  modelOptions,
  variantModel,
  onVariantModelChange,
  onCreateVariant,
  creatingVariant
}) {
  const selectableSessions = sessions2.filter((session2) => session2.status !== "terminated");
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "tool-picker-backdrop", role: "presentation", onClick: onClose, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "comparison-modal", role: "dialog", "aria-modal": "true", "aria-label": "任务对比", onClick: (event) => event.stopPropagation(), children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "tool-picker-header", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: "任务对比" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "并排查看模型、配置、耗时、Token、文件和最终结果。" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: onClose, children: "关闭" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "comparison-controls", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "左侧运行" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("select", { value: leftID, onChange: (event) => onLeftChange(event.target.value), children: selectableSessions.map((session2) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: session2.id, children: session2.title || session2.id }, session2.id)) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "右侧运行" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("select", { value: rightID, onChange: (event) => onRightChange(event.target.value), children: selectableSessions.map((session2) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: session2.id, children: session2.title || session2.id }, session2.id)) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: !leftID || !rightID || leftID === rightID || loading, onClick: onCompare, children: loading ? "加载中..." : "刷新对比" })
    ] }),
    result ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "comparison-grid", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ComparisonRunColumn, { label: "基准", run: result.left }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ComparisonRunColumn, { label: "变体", run: result.right })
    ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "选择两个不同任务后加载对比。" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "comparison-variant-bar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "创建模型变体" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "复制左侧运行的 prompt、Agent 版本和运行设置，仅替换模型。" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("select", { "aria-label": "变体模型", value: variantModel, onChange: (event) => onVariantModelChange(event.target.value), children: modelOptions.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: `${option.llmProvider}::${option.llmModel}`, children: option.label }, `${option.llmProvider}:${option.llmModel}`)) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: !leftID || !variantModel || creatingVariant, onClick: onCreateVariant, children: creatingVariant ? "创建中..." : "运行变体" })
    ] })
  ] }) });
}
function TaskMetadataModal({ session: session2, tags, onTagsChange, onSave, onClose, saving, onTogglePin }) {
  if (!session2) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "tool-picker-backdrop", role: "presentation", onClick: onClose, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "task-metadata-modal", role: "dialog", "aria-modal": "true", "aria-label": "任务信息", onClick: (event) => event.stopPropagation(), children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "tool-picker-header", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: "任务信息" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: session2.title || session2.id })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: onClose, children: "关闭" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "task-metadata-field", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "标签" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: tags, onChange: (event) => onTagsChange(event.target.value), placeholder: "新闻, 代码, 调研" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "最多 8 个，以逗号分隔。" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "task-summary-detail", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "会话摘要" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: session2.summary_text || "当前会话还没有可显示的结论。" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("footer", { className: "task-metadata-actions", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: saving, onClick: onTogglePin, children: session2.pinned_at ? "取消置顶" : "置顶任务" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: saving, onClick: onSave, children: saving ? "保存中..." : "保存标签" })
    ] })
  ] }) });
}
function WorkflowProgress({ onStop, run }) {
  if (!run) return null;
  const completed = run.steps.filter((step) => step.status === "completed").length;
  const progress = run.steps.length ? Math.round(completed / run.steps.length * 100) : 0;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: `workflow-progress ${run.status}`, "aria-label": "工作流进度", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "多步骤工作流" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: run.templateTitle })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "workflow-progress-actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
          completed,
          "/",
          run.steps.length
        ] }),
        run.status === "running" ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: onStop, children: "停止" }) : null
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "workflow-progress-track", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { width: `${progress}%` } }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("ol", { children: run.steps.map((step, index2) => /* @__PURE__ */ jsxRuntimeExports.jsxs("li", { className: step.status, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: step.status === "completed" ? "✓" : index2 + 1 }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: step.title }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: step.status === "running" ? "执行中" : step.status === "completed" ? "已完成" : step.status === "failed" ? "失败" : step.status === "canceled" ? "已停止" : "等待" })
      ] })
    ] }, step.id)) })
  ] });
}
function workflowStorageKey(sessionID) {
  return `${workflowStoragePrefix}${sessionID}`;
}
function persistWorkflowRun(run) {
  try {
    if (!(run == null ? void 0 : run.sessionID)) return;
    window.localStorage.setItem(workflowStorageKey(run.sessionID), JSON.stringify(run));
  } catch {
  }
}
function readWorkflowRun(sessionID) {
  try {
    const raw = window.localStorage.getItem(workflowStorageKey(sessionID));
    return raw ? JSON.parse(raw) : null;
  } catch {
    return null;
  }
}
function workflowStepMessage(template, baseTask, stepIndex) {
  const steps = template.workflow_steps || [];
  const step = steps[stepIndex];
  return [
    `执行「${template.title}」工作流，第 ${stepIndex + 1}/${steps.length} 步：${step.title}。`,
    `整体目标：${baseTask}`,
    `本步骤任务：${step.instruction}`,
    "只完成本步骤，并清楚说明本步骤的产出、证据和留给下一步的上下文。不要提前跳到后续步骤。"
  ].join("\n\n");
}
function editableMCPServers(raw) {
  if (!raw || typeof raw !== "object") return [];
  const source = raw.servers ?? raw.mcpServers ?? [];
  if (Array.isArray(source)) return source.map((item) => ({ ...objectValue(item) }));
  if (source && typeof source === "object") {
    return Object.entries(source).map(([identifier, value]) => ({ identifier, ...objectValue(value) }));
  }
  return [];
}
function editableMCPBindings(raw) {
  if (!raw || typeof raw !== "object" || !Array.isArray(raw.bindings)) return [];
  return raw.bindings.map((binding) => ({
    server_id: String((binding == null ? void 0 : binding.server_id) || ""),
    version: Number((binding == null ? void 0 : binding.version) || 0),
    ...(binding == null ? void 0 : binding.identifier) ? { identifier: String(binding.identifier) } : {}
  })).filter((binding) => binding.server_id);
}
function agentEditorDraft(agent2) {
  const config = (agent2 == null ? void 0 : agent2.config_version) || {};
  const toolPolicy = parseToolPolicy(config.tools);
  const enabledNamespaces = toolPolicy.explicit ? builtinToolNamespaces.filter((item) => toolNamespaceEnabled(item.key, toolPolicy)).map((item) => item.key) : builtinToolNamespaces.map((item) => item.key);
  const namespaceKeys = new Set(builtinToolNamespaces.map((item) => item.key));
  return {
    llmModel: config.llm_model || "",
    llmProvider: config.llm_provider || "",
    mcpBindings: editableMCPBindings(config.mcp),
    mcpServers: editableMCPServers(config.mcp),
    name: (agent2 == null ? void 0 : agent2.name) || "",
    selectedSkills: parseSkillsConfig(config.skills).enabled.map((item) => item.skill),
    selectedTools: enabledNamespaces,
    system: config.system || "",
    toolPatterns: toolPolicy.enabledToolPatterns.filter((pattern) => !namespaceKeys.has(pattern)),
    toolRuntime: toolPolicy.runtime || ""
  };
}
function agentConfigVersionMetrics(version2) {
  const toolPolicy = parseToolPolicy(version2 == null ? void 0 : version2.tools);
  return {
    mcp: editableMCPServers(version2 == null ? void 0 : version2.mcp).filter((server) => !server.disabled).length + editableMCPBindings(version2 == null ? void 0 : version2.mcp).length,
    skills: parseSkillsConfig(version2 == null ? void 0 : version2.skills).enabled.length,
    tools: toolPolicy.explicit ? toolPolicy.enabledToolPatterns.length : builtinToolNamespaces.length
  };
}
function AgentConfigEditor({ agent: agent2, mcpRegistryServers = [], modelOptions, onRollback, onSave, rollingBackVersion, saving, skills: skills2 }) {
  const [draft, setDraft] = reactExports.useState(() => agentEditorDraft(agent2));
  const [rollbackCandidate, setRollbackCandidate] = reactExports.useState(0);
  const [versionError, setVersionError] = reactExports.useState("");
  const [versionLoading, setVersionLoading] = reactExports.useState(false);
  const [versions, setVersions] = reactExports.useState([]);
  reactExports.useEffect(() => {
    setDraft(agentEditorDraft(agent2));
  }, [agent2 == null ? void 0 : agent2.id, agent2 == null ? void 0 : agent2.current_config_version]);
  reactExports.useEffect(() => {
    let active = true;
    setRollbackCandidate(0);
    setVersionError("");
    setVersions([]);
    if (!(agent2 == null ? void 0 : agent2.id)) return () => {
      active = false;
    };
    setVersionLoading(true);
    agentConfigVersions(agent2.id).then((response) => {
      if (!active) return;
      setVersions([...response.config_versions || []].sort((left, right) => Number(right.version || 0) - Number(left.version || 0)));
    }).catch((error) => {
      if (active) setVersionError(error.message);
    }).finally(() => {
      if (active) setVersionLoading(false);
    });
    return () => {
      active = false;
    };
  }, [agent2 == null ? void 0 : agent2.id, agent2 == null ? void 0 : agent2.current_config_version]);
  if (!agent2) return /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "请先选择一个智能体。" });
  const installedSkillIDs = new Set(skills2.map((skill) => skill.identifier));
  const skillOptions = [
    ...skills2.map((skill) => ({
      description: skill.description || "工作区技能",
      disabled: skill.status === "archived",
      identifier: skill.identifier,
      title: skill.title || skill.identifier
    })),
    ...draft.selectedSkills.filter((identifier) => !installedSkillIDs.has(identifier)).map((identifier) => ({
      description: "当前配置引用了该技能，但工作区中未找到安装项。",
      disabled: true,
      identifier,
      title: identifier
    }))
  ];
  function toggleListValue(key, value) {
    setDraft((current) => ({
      ...current,
      [key]: current[key].includes(value) ? current[key].filter((item) => item !== value) : [...current[key], value]
    }));
  }
  function updateMCP(index2, patch2) {
    setDraft((current) => ({
      ...current,
      mcpServers: current.mcpServers.map((server, serverIndex) => serverIndex === index2 ? { ...server, ...patch2 } : server)
    }));
  }
  function toggleMCPBinding(server) {
    setDraft((current) => {
      const exists = current.mcpBindings.some((binding) => binding.server_id === server.id);
      return {
        ...current,
        mcpBindings: exists ? current.mcpBindings.filter((binding) => binding.server_id !== server.id) : [...current.mcpBindings, { server_id: server.id, version: Number(server.current_version || 1) }]
      };
    });
  }
  function upgradeMCPBinding(server) {
    setDraft((current) => ({
      ...current,
      mcpBindings: current.mcpBindings.map((binding) => binding.server_id === server.id ? { ...binding, version: Number(server.current_version || binding.version || 1) } : binding)
    }));
  }
  const canSave = Boolean(draft.name.trim() && draft.llmProvider && draft.llmModel && !saving);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-editor", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-editor-grid", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "agent-editor-field", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Agent 名称" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: draft.name, onChange: (event) => setDraft((current) => ({ ...current, name: event.target.value })) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "agent-editor-field", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "默认模型" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "select",
          {
            value: `${draft.llmProvider}::${draft.llmModel}`,
            onChange: (event) => {
              const option = modelOptions.find((item) => `${item.llmProvider}::${item.llmModel}` === event.target.value);
              if (option) setDraft((current) => ({ ...current, llmProvider: option.llmProvider, llmModel: option.llmModel }));
            },
            children: [
              !modelOptions.some((item) => item.llmProvider === draft.llmProvider && item.llmModel === draft.llmModel) ? /* @__PURE__ */ jsxRuntimeExports.jsxs("option", { value: `${draft.llmProvider}::${draft.llmModel}`, children: [
                draft.llmProvider,
                " / ",
                draft.llmModel
              ] }) : null,
              modelOptions.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: `${option.llmProvider}::${option.llmModel}`, children: option.label }, `${option.llmProvider}::${option.llmModel}`))
            ]
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "agent-editor-field", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "System prompt" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("textarea", { rows: "7", value: draft.system, onChange: (event) => setDraft((current) => ({ ...current, system: event.target.value })), placeholder: "定义智能体的角色、边界和工作方式" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "agent-editor-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-editor-section-head", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Tools" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "控制该 Agent 默认可调用的工具命名空间。" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "agent-editor-count", children: [
          draft.selectedTools.length,
          "/",
          builtinToolNamespaces.length
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "agent-option-grid", children: builtinToolNamespaces.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "agent-option", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", checked: draft.selectedTools.includes(item.key), onChange: () => toggleListValue("selectedTools", item.key) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: item.title }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: item.description })
        ] })
      ] }, item.key)) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "agent-editor-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-editor-section-head", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Skills" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "选择运行时注入给该 Agent 的工作区技能。" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "agent-editor-count", children: draft.selectedSkills.length })
      ] }),
      skillOptions.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "agent-option-grid", children: skillOptions.map((skill) => /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: `agent-option ${skill.disabled ? "disabled" : ""}`, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", disabled: skill.disabled, checked: draft.selectedSkills.includes(skill.identifier), onChange: () => toggleListValue("selectedSkills", skill.identifier) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: skill.title }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: skill.description })
        ] })
      ] }, skill.identifier)) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "工作区还没有可启用的技能。" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "agent-editor-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-editor-section-head", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "MCP 服务" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "绑定工作区服务，或保留 Agent 专用内嵌配置。" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => setDraft((current) => ({ ...current, mcpServers: [...current.mcpServers, { identifier: "", transport: "stdio", stdio_framing: "json_lines", command: "", disabled: false }] })), children: "添加服务" })
      ] }),
      mcpRegistryServers.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mcp-binding-list", children: mcpRegistryServers.map((server) => {
        var _a2;
        const binding = draft.mcpBindings.find((item) => item.server_id === server.id);
        const behind = binding && Number(binding.version) < Number(server.current_version);
        return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `mcp-binding-row ${server.status !== "active" ? "disabled" : ""}`, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", disabled: server.status !== "active", checked: Boolean(binding), onChange: () => toggleMCPBinding(server) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: server.name }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
                server.identifier,
                " · v",
                (binding == null ? void 0 : binding.version) || server.current_version,
                " · ",
                ((_a2 = server.config) == null ? void 0 : _a2.transport) || "stdio"
              ] })
            ] })
          ] }),
          behind ? /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "secondary", type: "button", onClick: () => upgradeMCPBinding(server), children: [
            "升级到 v",
            server.current_version
          ] }) : null
        ] }, server.id);
      }) }) : null,
      draft.mcpServers.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mcp-editor-list", children: draft.mcpServers.map((server, index2) => {
        var _a2, _b, _c;
        return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mcp-editor-row", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "agent-editor-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "标识" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: server.identifier || server.id || server.name || "", onChange: (event) => updateMCP(index2, { identifier: event.target.value }), placeholder: "例如 filesystem" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "agent-editor-field mcp-transport-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "传输" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: server.transport || "stdio", onChange: (event) => updateMCP(index2, { transport: event.target.value }), children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "stdio", children: "stdio" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "streamable_http", children: "Streamable HTTP" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "agent-editor-field mcp-command-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: (server.transport || "stdio") === "streamable_http" ? "服务 URL" : "启动命令" }),
            (server.transport || "stdio") === "streamable_http" ? /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: server.url || "", onChange: (event) => updateMCP(index2, { url: event.target.value }), placeholder: "https://mcp.example.com/mcp" }) : /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: server.command || "", onChange: (event) => updateMCP(index2, { command: event.target.value }), placeholder: "例如 npx -y @modelcontextprotocol/server-filesystem" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "agent-editor-field mcp-logging-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "日志级别" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: ((_a2 = server.logging) == null ? void 0 : _a2.level) || "", onChange: (event) => updateMCP(index2, { logging: event.target.value ? { level: event.target.value } : void 0 }), children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "不设置" }),
              ["debug", "info", "notice", "warning", "error", "critical", "alert", "emergency"].map((level) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: level, children: level }, level))
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "mcp-enabled-toggle", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", checked: !server.disabled, onChange: (event) => updateMCP(index2, { disabled: !event.target.checked }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "启用" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "mcp-enabled-toggle", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", checked: Boolean((_b = server.expose) == null ? void 0 : _b.resources), onChange: (event) => updateMCP(index2, { expose: { ...objectValue(server.expose), resources: event.target.checked } }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "资源工具" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "mcp-enabled-toggle", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", checked: Boolean((_c = server.expose) == null ? void 0 : _c.prompts), onChange: (event) => updateMCP(index2, { expose: { ...objectValue(server.expose), prompts: event.target.checked } }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Prompt 工具" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "mcp-enabled-toggle", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", disabled: (server.transport || "stdio") !== "streamable_http", checked: Boolean(server.listen), onChange: (event) => updateMCP(index2, { listen: event.target.checked }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "SSE 监听" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "icon-button danger", type: "button", title: "移除 MCP 服务", "aria-label": `移除 MCP 服务 ${server.identifier || index2 + 1}`, onClick: () => setDraft((current) => ({ ...current, mcpServers: current.mcpServers.filter((_, serverIndex) => serverIndex !== index2) })), children: /* @__PURE__ */ jsxRuntimeExports.jsx(DeleteIcon, {}) })
        ] }, `${server.identifier || "new"}-${index2}`);
      }) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "尚未配置 MCP 服务。" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-editor-footer", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "subtle", children: [
        "保存会创建配置版本 #",
        Number(agent2.current_config_version || 0) + 1,
        "，已有会话继续使用原版本。"
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: !canSave, onClick: () => onSave({
        name: draft.name.trim(),
        llm_provider: draft.llmProvider,
        llm_model: draft.llmModel,
        system: draft.system,
        tools: { enabled_tools: [...draft.selectedTools, ...draft.toolPatterns], ...draft.toolRuntime ? { runtime: draft.toolRuntime } : {} },
        skills: { enabled: draft.selectedSkills.map((skill) => ({ skill })) },
        mcp: { bindings: draft.mcpBindings, servers: draft.mcpServers.filter((server) => {
          const identifier = String(server.identifier || server.id || server.name || "").trim();
          return identifier && ((server.transport || "stdio") === "streamable_http" ? String(server.url || "").trim() : String(server.command || "").trim());
        }) }
      }), children: saving ? "保存中..." : "保存配置" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "agent-editor-section agent-version-history", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-editor-section-head", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "版本历史" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "回滚会复制历史配置并生成新的当前版本，已有会话不会改变。" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "agent-editor-count", children: versions.length })
      ] }),
      versionError ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "agent-version-error", children: versionError }) : null,
      versionLoading ? /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "正在加载版本历史..." }) : null,
      !versionLoading && !versions.length && !versionError ? /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "还没有可显示的配置版本。" }) : null,
      versions.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "agent-version-list", children: versions.map((version2) => {
        const current = Number(version2.version) === Number(agent2.current_config_version);
        const metrics = agentConfigVersionMetrics(version2);
        const confirming = rollbackCandidate === Number(version2.version);
        const rollingBack = rollingBackVersion === Number(version2.version);
        return /* @__PURE__ */ jsxRuntimeExports.jsxs("article", { className: `agent-version-row ${current ? "current" : ""}`, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-version-main", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-version-heading", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
                "版本 #",
                version2.version
              ] }),
              current ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "agent-version-current", children: "当前" }) : null
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-version-model", children: [
              version2.llm_provider || "-",
              " / ",
              version2.llm_model || "-"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-version-meta", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: formatTime(version2.created_at) }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                "Tools ",
                metrics.tools
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                "Skills ",
                metrics.skills
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                "MCP ",
                metrics.mcp
              ] })
            ] })
          ] }),
          !current && !confirming ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary agent-version-rollback", type: "button", disabled: Boolean(rollingBackVersion) || saving, onClick: () => setRollbackCandidate(Number(version2.version)), children: "回滚到此版本" }) : null,
          confirming ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-version-confirm", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              "将版本 #",
              version2.version,
              " 复制为版本 #",
              Number(agent2.current_config_version || 0) + 1
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: rollingBack, onClick: () => setRollbackCandidate(0), children: "取消" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: rollingBack, onClick: async () => {
                setVersionError("");
                try {
                  await onRollback(Number(version2.version));
                  setRollbackCandidate(0);
                } catch (error) {
                  setVersionError(error.message);
                }
              }, children: rollingBack ? "回滚中..." : "确认回滚" })
            ] })
          ] }) : null
        ] }, version2.version);
      }) }) : null
    ] })
  ] });
}
function EnvironmentVariablesSettings({ workspaceID }) {
  const [variables, setVariables] = reactExports.useState([]);
  const [name2, setName] = reactExports.useState("");
  const [value, setValue] = reactExports.useState("");
  const [busy, setBusy] = reactExports.useState("");
  const [error, setError] = reactExports.useState("");
  const [message, setMessage] = reactExports.useState("");
  async function load() {
    setError("");
    try {
      const response = await environmentVariables(workspaceID);
      setVariables(response.variables || []);
    } catch (loadError) {
      setError(loadError.message);
    }
  }
  reactExports.useEffect(() => {
    load();
  }, [workspaceID]);
  async function save(event) {
    event.preventDefault();
    const normalizedName = name2.trim();
    if (!normalizedName) return;
    setBusy(`save:${normalizedName}`);
    setError("");
    setMessage("");
    try {
      await putEnvironmentVariable(normalizedName, value, workspaceID);
      setName("");
      setValue("");
      setMessage(`${normalizedName} 已安全保存`);
      await load();
    } catch (saveError) {
      setError(saveError.message);
    } finally {
      setBusy("");
    }
  }
  async function remove(variableName) {
    if (!window.confirm(`删除环境变量 ${variableName}？`)) return;
    setBusy(`delete:${variableName}`);
    setError("");
    setMessage("");
    try {
      await deleteEnvironmentVariable(variableName, workspaceID);
      setMessage(`${variableName} 已删除`);
      await load();
    } catch (deleteError) {
      setError(deleteError.message);
    } finally {
      setBusy("");
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-content-stack environment-settings", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-hero-card environment-security-status", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "运行时环境变量" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "environment-security-badges", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "工作区隔离" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "AES-256-GCM" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "仅调用时注入" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "值不可回读" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { className: "environment-count", children: variables.length })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { className: "settings-card environment-variable-form", onSubmit: save, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "添加或轮换变量" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "environment-form-grid", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Key" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { autoComplete: "off", spellCheck: "false", value: name2, onChange: (event) => setName(event.target.value), placeholder: "SERVICE_API_KEY", pattern: "[A-Za-z_][A-Za-z0-9_]*", maxLength: 128, required: true })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Value" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { autoComplete: "new-password", type: "password", value, onChange: (event) => setValue(event.target.value), placeholder: "••••••••••••", required: true })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", disabled: Boolean(busy) || !name2.trim(), children: busy.startsWith("save:") ? "保存中..." : "保存" })
      ] }),
      error ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "health-error", children: error }) : null,
      message ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "environment-message", children: message }) : null
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card environment-variable-list", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "已配置变量" }),
      variables.length ? variables.map((variable) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "environment-variable-name", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("code", { children: variable.name }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "subtle", children: [
            "更新于 ",
            formatTime(variable.updated_at)
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row-actions", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "environment-configured", children: "已配置" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary danger", type: "button", disabled: Boolean(busy), onClick: () => remove(variable.name), children: busy === `delete:${variable.name}` ? "删除中..." : "删除" })
        ] })
      ] }, variable.name)) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "当前工作区没有环境变量。" })
    ] })
  ] });
}
function llmDiagnosticSummary(result) {
  if (!result) return "";
  const metrics = [`${Number(result.latency_ms || 0)} ms`];
  if (Number(result.dimensions) > 0) metrics.push(`${result.dimensions} 维`);
  if (Number(result.candidate_count) > 0) metrics.push(`${result.candidate_count} 个结果`);
  if (!result.capability_type) metrics.push(result.authenticated ? "认证已通过" : "未使用凭证");
  if (result.status === "succeeded") return `测试成功 · ${metrics.join(" · ")}`;
  return `${result.message || "测试失败"} · ${result.error_type || "unknown"} · ${metrics[0]}`;
}
function ModelCatalogRow({ model, busy, onDelete, onSave, onTest, testBusy, testResult }) {
  const [contextWindowTokens, setContextWindowTokens] = reactExports.useState(String(model.context_window_tokens || 128e3));
  const [capabilityType, setCapabilityType] = reactExports.useState(model.capability_type || "text");
  const [capabilities, setCapabilities] = reactExports.useState(defaultModelCapabilities(model.capability_type || "text", model.capabilities));
  reactExports.useEffect(() => {
    setContextWindowTokens(String(model.context_window_tokens || 128e3));
    setCapabilityType(model.capability_type || "text");
    setCapabilities(defaultModelCapabilities(model.capability_type || "text", model.capabilities));
  }, [model.context_window_tokens, model.capability_type, model.capabilities]);
  const changed = Number(contextWindowTokens) !== Number(model.context_window_tokens) || capabilityType !== (model.capability_type || "text") || JSON.stringify(capabilities) !== JSON.stringify(defaultModelCapabilities(model.capability_type || "text", model.capabilities));
  function changeCapabilityType(nextType) {
    setCapabilityType(nextType);
    setCapabilities(defaultModelCapabilities(nextType));
    if ((nextType === "embedding" || nextType === "reranker") && Number(contextWindowTokens) === 128e3) {
      setContextWindowTokens("8192");
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "form",
    {
      className: "model-catalog-row",
      onSubmit: (event) => {
        event.preventDefault();
        onSave(model.model, Number(contextWindowTokens), capabilityType, capabilities);
      },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-catalog-name", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: model.model }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            "更新于 ",
            formatTime(model.updated_at || model.created_at)
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "上下文窗口" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-token-input", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                type: "number",
                min: "1000",
                step: "1000",
                value: contextWindowTokens,
                onChange: (event) => setContextWindowTokens(event.target.value),
                required: true
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "tokens" })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "模型能力" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("select", { value: capabilityType, onChange: (event) => changeCapabilityType(event.target.value), children: modelCapabilityOptions.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: option.value, children: option.label }, option.value)) })
        ] }),
        capabilityType === "embedding" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-capability-fields", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "向量维度" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "number", min: "1", max: "65535", value: capabilities.dimensions || "", onChange: (event) => setCapabilities((current) => ({ ...current, dimensions: Number(event.target.value) })), required: true })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "距离度量" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: capabilities.distance_metric || "cosine", onChange: (event) => setCapabilities((current) => ({ ...current, distance_metric: event.target.value })), children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "cosine", children: "Cosine" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "l2", children: "L2" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "inner_product", children: "Inner product" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "调用协议" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("select", { value: capabilities.protocol || "openai_embeddings", onChange: (event) => setCapabilities((current) => ({ ...current, protocol: event.target.value })), children: embeddingProtocolOptions.map((protocol) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: protocol, children: protocol }, protocol)) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "批量上限" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "number", min: "1", max: "4096", value: capabilities.max_batch_size || "", onChange: (event) => setCapabilities((current) => ({ ...current, max_batch_size: Number(event.target.value) })), required: true })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "model-capability-toggle", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", checked: capabilities.normalized !== false, onChange: (event) => setCapabilities((current) => ({ ...current, normalized: event.target.checked })) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "输出已归一化" })
          ] })
        ] }) : null,
        capabilityType === "reranker" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-capability-fields reranker", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "调用协议" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("select", { value: capabilities.protocol || "jina_rerank", onChange: (event) => setCapabilities((current) => ({ ...current, protocol: event.target.value })), children: rerankerProtocolOptions.map((protocol) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: protocol, children: protocol }, protocol)) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "最大候选数" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "number", min: "1", max: "1000", value: capabilities.max_candidates || "", onChange: (event) => setCapabilities((current) => ({ ...current, max_candidates: Number(event.target.value) })), required: true })
          ] })
        ] }) : null,
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-catalog-actions", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", title: changed ? "请先保存模型配置" : "测试模型", disabled: busy || testBusy || changed, onClick: () => onTest(model), children: testBusy ? "测试中..." : "测试模型" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "submit", disabled: busy || !changed || Number(contextWindowTokens) <= 0, children: busy ? "处理中..." : "保存" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "icon-button danger", type: "button", title: `删除模型 ${model.model}`, "aria-label": `删除模型 ${model.model}`, disabled: busy, onClick: () => onDelete(model.model), children: /* @__PURE__ */ jsxRuntimeExports.jsx(DeleteIcon, {}) })
        ] }),
        testResult ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `llm-diagnostic-result ${testResult.status === "succeeded" ? "ok" : "failed"}`, children: llmDiagnosticSummary(testResult) }) : null
      ]
    }
  );
}
function ModelManagementSettings({ onCatalogChanged, onOpenEnvironment }) {
  const [providers, setProviders] = reactExports.useState([]);
  const [models, setModels] = reactExports.useState([]);
  const [selectedProviderID, setSelectedProviderID] = reactExports.useState("");
  const [creatingProvider, setCreatingProvider] = reactExports.useState(false);
  const [providerDraft, setProviderDraft] = reactExports.useState({ id: "", providerType: "openai-compatible", baseURL: "", apiKeyEnv: "", enabled: true });
  const [modelDraft, setModelDraft] = reactExports.useState({ model: "", contextWindowTokens: "128000", capabilityType: "text", capabilities: {} });
  const [loading, setLoading] = reactExports.useState(true);
  const [busy, setBusy] = reactExports.useState("");
  const [error, setError] = reactExports.useState("");
  const [message, setMessage] = reactExports.useState("");
  const [providerTestResults, setProviderTestResults] = reactExports.useState({});
  const [modelTestResults, setModelTestResults] = reactExports.useState({});
  async function loadCatalog(preferredProviderID = "") {
    const [providerResponse, modelResponse] = await Promise.all([llmProviders(), llmModels()]);
    const nextProviders = providerResponse.providers || [];
    const nextModels = modelResponse.models || [];
    setProviders(nextProviders);
    setModels(nextModels);
    setSelectedProviderID((current) => {
      var _a2;
      const preferred = preferredProviderID || current;
      return nextProviders.some((provider) => provider.id === preferred) ? preferred : ((_a2 = nextProviders[0]) == null ? void 0 : _a2.id) || "";
    });
    return { providers: nextProviders, models: nextModels };
  }
  reactExports.useEffect(() => {
    let active = true;
    Promise.all([llmProviders(), llmModels()]).then(([providerResponse, modelResponse]) => {
      if (!active) return;
      const nextProviders = providerResponse.providers || [];
      setProviders(nextProviders);
      setModels(modelResponse.models || []);
      setSelectedProviderID((current) => {
        var _a2;
        return current || ((_a2 = nextProviders[0]) == null ? void 0 : _a2.id) || "";
      });
    }).catch((loadError) => {
      if (active) setError(loadError.message);
    }).finally(() => {
      if (active) setLoading(false);
    });
    return () => {
      active = false;
    };
  }, []);
  const selectedProvider = providers.find((provider) => provider.id === selectedProviderID) || null;
  const selectedModels = models.filter((model) => model.provider_id === selectedProviderID);
  const enabledProviderCount = providers.filter((provider) => provider.enabled !== false).length;
  const enabledProviderIDs = new Set(providers.filter((provider) => provider.enabled !== false).map((provider) => provider.id));
  const visionModels = models.filter((model) => enabledProviderIDs.has(model.provider_id) && model.capability_type === "text_image");
  const embeddingModels = models.filter((model) => enabledProviderIDs.has(model.provider_id) && model.capability_type === "embedding");
  const rerankerModels = models.filter((model) => enabledProviderIDs.has(model.provider_id) && model.capability_type === "reranker");
  const defaultVisionModel = models.find((model) => model.is_default_vision) || null;
  const defaultEmbeddingModel = models.find((model) => model.is_default_embedding) || null;
  const defaultRerankerModel = models.find((model) => model.is_default_reranker) || null;
  const providerDraftDirty = Boolean(!creatingProvider && selectedProvider) && (providerDraft.providerType.trim() !== (selectedProvider.provider_type || "openai-compatible") || providerDraft.baseURL.trim() !== (selectedProvider.base_url || "") || providerDraft.apiKeyEnv.trim() !== (selectedProvider.api_key_env || "") || providerDraft.enabled !== (selectedProvider.enabled !== false));
  reactExports.useEffect(() => {
    if (creatingProvider || !selectedProvider) return;
    setProviderDraft({
      id: selectedProvider.id,
      providerType: selectedProvider.provider_type || "openai-compatible",
      baseURL: selectedProvider.base_url || "",
      apiKeyEnv: selectedProvider.api_key_env || "",
      enabled: selectedProvider.enabled !== false
    });
  }, [creatingProvider, selectedProvider]);
  async function refreshCatalog(providerID, successMessage) {
    await loadCatalog(providerID);
    await (onCatalogChanged == null ? void 0 : onCatalogChanged());
    setMessage(successMessage);
  }
  async function saveProvider(event) {
    event.preventDefault();
    const providerID = providerDraft.id.trim();
    if (!providerID || !providerDraft.providerType.trim()) return;
    setBusy("provider-save");
    setError("");
    setMessage("");
    try {
      const body = {
        provider_type: providerDraft.providerType.trim(),
        base_url: providerDraft.baseURL.trim(),
        api_key_env: providerDraft.apiKeyEnv.trim(),
        enabled: providerDraft.enabled
      };
      if (creatingProvider) {
        await createLLMProvider({ id: providerID, ...body });
      } else {
        await updateLLMProvider(providerID, selectedProvider.revision, body);
      }
      setCreatingProvider(false);
      await refreshCatalog(providerID, creatingProvider ? `Provider ${providerID} 已添加。` : `Provider ${providerID} 已更新。`);
    } catch (saveError) {
      setError(saveError.message);
    } finally {
      setBusy("");
    }
  }
  async function toggleProvider() {
    if (!selectedProvider) return;
    const nextEnabled = selectedProvider.enabled === false;
    setBusy("provider-toggle");
    setError("");
    setMessage("");
    try {
      await setLLMProviderEnabled(selectedProvider.id, selectedProvider.revision, nextEnabled);
      await refreshCatalog(selectedProvider.id, `${selectedProvider.id} 已${nextEnabled ? "启用" : "停用"}。`);
    } catch (toggleError) {
      setError(toggleError.message);
    } finally {
      setBusy("");
    }
  }
  async function testProvider() {
    if (!selectedProvider || providerDraftDirty) return;
    setBusy("provider-test");
    setError("");
    setMessage("");
    try {
      const result = await testLLMProvider(selectedProvider.id);
      setProviderTestResults((current) => ({ ...current, [selectedProvider.id]: result }));
    } catch (testError) {
      setError(testError.message);
    } finally {
      setBusy("");
    }
  }
  async function deleteProvider() {
    if (!selectedProvider) return;
    const providerModelCount = selectedModels.length;
    const detail = providerModelCount ? `，并同时删除其下 ${providerModelCount} 个模型` : "";
    if (!window.confirm(`确认删除 Provider ${selectedProvider.id}${detail}？此操作无法撤销。`)) return;
    setBusy("provider-delete");
    setError("");
    setMessage("");
    try {
      const deletedProviderID = selectedProvider.id;
      await deleteLLMProvider(deletedProviderID, selectedProvider.revision);
      await refreshCatalog("", `Provider ${deletedProviderID} 已删除。`);
    } catch (deleteError) {
      setError(deleteError.message);
    } finally {
      setBusy("");
    }
  }
  async function saveModel(modelName, contextWindowTokens, capabilityType = "text", capabilities = {}, isNew = false, isDefaultVision = void 0) {
    if (!selectedProvider || !String(modelName || "").trim() || Number(contextWindowTokens) <= 0) return;
    const normalizedModelName = String(modelName).trim();
    setBusy(`model:${normalizedModelName}`);
    setError("");
    setMessage("");
    try {
      const existingModel = models.find((model) => model.provider_id === selectedProvider.id && model.model === normalizedModelName);
      const effectiveDefaultVision = typeof isDefaultVision === "boolean" ? isDefaultVision : capabilityType === "text_image" ? Boolean(existingModel == null ? void 0 : existingModel.is_default_vision) : false;
      await upsertLLMModel({
        provider_id: selectedProvider.id,
        model: normalizedModelName,
        context_window_tokens: Number(contextWindowTokens),
        capability_type: capabilityType,
        capabilities: defaultModelCapabilities(capabilityType, capabilities),
        is_default_vision: effectiveDefaultVision
      }, isNew ? void 0 : existingModel == null ? void 0 : existingModel.revision);
      if (isNew) setModelDraft({ model: "", contextWindowTokens: "128000", capabilityType: "text", capabilities: {} });
      await refreshCatalog(selectedProvider.id, `模型 ${normalizedModelName} 已保存。`);
    } catch (saveError) {
      setError(saveError.message);
    } finally {
      setBusy("");
    }
  }
  async function saveDefaultVisionModel(value) {
    const [providerID, modelName] = String(value || "").split("::");
    const target = models.find((model2) => model2.provider_id === providerID && model2.model === modelName);
    const current = models.find((model2) => model2.is_default_vision);
    const model = target || current;
    if (!model) return;
    setBusy("vision-default");
    setError("");
    setMessage("");
    try {
      await upsertLLMModel({
        provider_id: model.provider_id,
        model: model.model,
        context_window_tokens: model.context_window_tokens,
        capability_type: model.capability_type || "text",
        capabilities: model.capabilities || {},
        is_default_vision: Boolean(target)
      }, model.revision);
      await refreshCatalog(selectedProviderID, target ? `统一图片视觉模型已设为 ${target.provider_id} / ${target.model}。` : "已清除统一图片视觉模型。");
    } catch (saveError) {
      setError(saveError.message);
    } finally {
      setBusy("");
    }
  }
  async function saveDefaultKnowledgeModel(kind, value) {
    const field = kind === "embedding" ? "is_default_embedding" : "is_default_reranker";
    const current = models.find((model2) => model2[field]);
    const [providerID, modelName] = String(value || "").split("::");
    const target = models.find((model2) => model2.provider_id === providerID && model2.model === modelName);
    const model = target || current;
    if (!model) return;
    setBusy(`${kind}-default`);
    setError("");
    setMessage("");
    try {
      await upsertLLMModel({
        provider_id: model.provider_id,
        model: model.model,
        context_window_tokens: model.context_window_tokens,
        capability_type: model.capability_type,
        capabilities: model.capabilities || {},
        [field]: Boolean(target)
      }, model.revision);
      await refreshCatalog(selectedProviderID, target ? `统一默认 ${kind === "embedding" ? "Embedding" : "Reranker"} 模型已设为 ${target.provider_id} / ${target.model}。` : `已清除统一默认 ${kind === "embedding" ? "Embedding" : "Reranker"} 模型。`);
    } catch (saveError) {
      setError(saveError.message);
    } finally {
      setBusy("");
    }
  }
  async function deleteModel(modelName) {
    if (!selectedProvider || !window.confirm(`确认删除模型 ${selectedProvider.id} / ${modelName}？此操作无法撤销。`)) return;
    setBusy(`model:${modelName}`);
    setError("");
    setMessage("");
    try {
      const model = models.find((candidate) => candidate.provider_id === selectedProvider.id && candidate.model === modelName);
      if (!model) throw new Error("模型目录已变化，请刷新后重试。");
      await deleteLLMModel(selectedProvider.id, modelName, model.revision);
      await refreshCatalog(selectedProvider.id, `模型 ${modelName} 已删除。`);
    } catch (deleteError) {
      setError(deleteError.message);
    } finally {
      setBusy("");
    }
  }
  async function testModel(model) {
    if (!selectedProvider || !model) return;
    const resultKey = `${model.provider_id}::${model.model}`;
    setBusy(`model-test:${model.model}`);
    setError("");
    setMessage("");
    try {
      const result = await testLLMModel(model.provider_id, model.model);
      setModelTestResults((current) => ({ ...current, [resultKey]: result }));
    } catch (testError) {
      setError(testError.message);
    } finally {
      setBusy("");
    }
  }
  function startCreateProvider() {
    setCreatingProvider(true);
    setProviderDraft({ id: "", providerType: "openai-compatible", baseURL: "", apiKeyEnv: "", enabled: true });
    setError("");
    setMessage("");
  }
  if (loading) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "正在加载模型目录..." }) });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-content-stack model-management", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-management-summary", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Provider" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: providers.length }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
          enabledProviderCount,
          " 个已启用"
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "模型" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: models.length }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "已登记到模型目录" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-management-security", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "凭证管理" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "环境变量" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: onOpenEnvironment, children: "管理密钥" })
      ] })
    ] }),
    error ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "health-error", children: error }) : null,
    message ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "environment-message", children: message }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "settings-card model-default-settings", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "默认模型" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-default-grid", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "图片视觉" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { disabled: busy === "vision-default", value: defaultVisionModel ? `${defaultVisionModel.provider_id}::${defaultVisionModel.model}` : "", onChange: (event) => saveDefaultVisionModel(event.target.value), children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "未配置" }),
            visionModels.map((model) => /* @__PURE__ */ jsxRuntimeExports.jsxs("option", { value: `${model.provider_id}::${model.model}`, children: [
              model.provider_id,
              " / ",
              model.model
            ] }, `${model.provider_id}:${model.model}`))
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Embedding" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { disabled: busy === "embedding-default", value: defaultEmbeddingModel ? `${defaultEmbeddingModel.provider_id}::${defaultEmbeddingModel.model}` : "", onChange: (event) => saveDefaultKnowledgeModel("embedding", event.target.value), children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "未配置" }),
            embeddingModels.map((model) => /* @__PURE__ */ jsxRuntimeExports.jsxs("option", { value: `${model.provider_id}::${model.model}`, children: [
              model.provider_id,
              " / ",
              model.model
            ] }, `${model.provider_id}:${model.model}`))
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Reranker" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { disabled: busy === "reranker-default", value: defaultRerankerModel ? `${defaultRerankerModel.provider_id}::${defaultRerankerModel.model}` : "", onChange: (event) => saveDefaultKnowledgeModel("reranker", event.target.value), children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "未配置" }),
            rerankerModels.map((model) => /* @__PURE__ */ jsxRuntimeExports.jsxs("option", { value: `${model.provider_id}::${model.model}`, children: [
              model.provider_id,
              " / ",
              model.model
            ] }, `${model.provider_id}:${model.model}`))
          ] })
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-management-layout", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "settings-card model-provider-list", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-section-heading", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "Providers" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              providers.length,
              " 个连接"
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: startCreateProvider, children: "添加" })
        ] }),
        providers.length ? providers.map((provider) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            className: `model-provider-item ${!creatingProvider && provider.id === selectedProviderID ? "active" : ""}`,
            type: "button",
            onClick: () => {
              setCreatingProvider(false);
              setSelectedProviderID(provider.id);
              setError("");
              setMessage("");
            },
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `model-provider-status ${provider.enabled === false ? "disabled" : ""}`, "aria-hidden": "true" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: provider.id }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
                  provider.provider_type,
                  " · ",
                  models.filter((model) => model.provider_id === provider.id).length,
                  " 个模型"
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("em", { children: provider.enabled === false ? "已停用" : "已启用" })
            ]
          },
          provider.id
        )) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "还没有模型 Provider。" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-management-detail", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { className: "settings-card model-provider-editor", onSubmit: saveProvider, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-section-heading", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: creatingProvider ? "添加 Provider" : "Provider 配置" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: creatingProvider ? "接入一个 OpenAI 兼容模型服务" : (selectedProvider == null ? void 0 : selectedProvider.id) || "未选择" })
            ] }),
            !creatingProvider && selectedProvider ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-section-actions", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", title: providerDraftDirty ? "请先保存 Provider 配置" : "测试连接", disabled: Boolean(busy) || providerDraftDirty, onClick: testProvider, children: busy === "provider-test" ? "测试中..." : "测试连接" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(busy), onClick: toggleProvider, children: busy === "provider-toggle" ? "处理中..." : selectedProvider.enabled === false ? "启用" : "停用" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "icon-button danger", type: "button", title: `删除 Provider ${selectedProvider.id}`, "aria-label": `删除 Provider ${selectedProvider.id}`, disabled: Boolean(busy), onClick: deleteProvider, children: /* @__PURE__ */ jsxRuntimeExports.jsx(DeleteIcon, {}) })
            ] }) : null
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-provider-form-grid", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Provider ID" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: providerDraft.id, disabled: !creatingProvider, onChange: (event) => setProviderDraft((current) => ({ ...current, id: event.target.value })), placeholder: "openai-production", pattern: "[A-Za-z0-9._-]+", required: true })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "协议类型" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: providerDraft.providerType, onChange: (event) => setProviderDraft((current) => ({ ...current, providerType: event.target.value })), children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "openai-compatible", children: "OpenAI Compatible" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "openai", children: "OpenAI" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "fake", children: "Fake（本地测试）" })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "model-provider-wide-field", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Base URL" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: providerDraft.baseURL, onChange: (event) => setProviderDraft((current) => ({ ...current, baseURL: event.target.value })), placeholder: "https://api.openai.com/v1", type: "url" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "model-provider-wide-field", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "API Key 环境变量" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: providerDraft.apiKeyEnv, onChange: (event) => setProviderDraft((current) => ({ ...current, apiKeyEnv: event.target.value })), placeholder: "OPENAI_API_KEY", pattern: "[A-Za-z_][A-Za-z0-9_]*" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: "这里只保存变量名，不保存密钥明文。" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-editor-actions", children: [
            creatingProvider ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(busy), onClick: () => setCreatingProvider(false), children: "取消" }) : null,
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", disabled: Boolean(busy) || !providerDraft.id.trim(), children: busy === "provider-save" ? "保存中..." : creatingProvider ? "添加 Provider" : "保存配置" })
          ] }),
          !creatingProvider && selectedProvider && providerTestResults[selectedProvider.id] ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `llm-diagnostic-result provider ${providerTestResults[selectedProvider.id].status === "succeeded" ? "ok" : "failed"}`, children: llmDiagnosticSummary(providerTestResults[selectedProvider.id]) }) : null
        ] }),
        !creatingProvider && selectedProvider ? /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "settings-card model-catalog", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "model-section-heading", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "模型目录" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              selectedProvider.id,
              " · ",
              selectedModels.length,
              " 个模型"
            ] })
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "form",
            {
              className: "model-create-form",
              onSubmit: (event) => {
                event.preventDefault();
                saveModel(modelDraft.model, modelDraft.contextWindowTokens, modelDraft.capabilityType, modelDraft.capabilities, true);
              },
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "模型 ID" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: modelDraft.model, onChange: (event) => setModelDraft((current) => ({ ...current, model: event.target.value })), placeholder: "gpt-4.1", required: true })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "上下文窗口" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "number", min: "1000", step: "1000", value: modelDraft.contextWindowTokens, onChange: (event) => setModelDraft((current) => ({ ...current, contextWindowTokens: event.target.value })), required: true })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "模型能力" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("select", { value: modelDraft.capabilityType, onChange: (event) => {
                    const capabilityType = event.target.value;
                    setModelDraft((current) => ({
                      ...current,
                      capabilityType,
                      contextWindowTokens: (capabilityType === "embedding" || capabilityType === "reranker") && current.contextWindowTokens === "128000" ? "8192" : current.contextWindowTokens,
                      capabilities: defaultModelCapabilities(capabilityType)
                    }));
                  }, children: modelCapabilityOptions.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: option.value, children: option.label }, option.value)) })
                ] }),
                modelDraft.capabilityType === "embedding" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-create-capabilities", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "向量维度" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "number", min: "1", max: "65535", value: modelDraft.capabilities.dimensions || "", onChange: (event) => setModelDraft((current) => ({ ...current, capabilities: { ...current.capabilities, dimensions: Number(event.target.value) } })), required: true })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "距离" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: modelDraft.capabilities.distance_metric || "cosine", onChange: (event) => setModelDraft((current) => ({ ...current, capabilities: { ...current.capabilities, distance_metric: event.target.value } })), children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "cosine", children: "Cosine" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "l2", children: "L2" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "inner_product", children: "Inner product" })
                    ] })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "协议" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("select", { value: modelDraft.capabilities.protocol || "openai_embeddings", onChange: (event) => setModelDraft((current) => ({ ...current, capabilities: { ...current.capabilities, protocol: event.target.value } })), children: embeddingProtocolOptions.map((protocol) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: protocol, children: protocol }, protocol)) })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "批量上限" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "number", min: "1", max: "4096", value: modelDraft.capabilities.max_batch_size || "", onChange: (event) => setModelDraft((current) => ({ ...current, capabilities: { ...current.capabilities, max_batch_size: Number(event.target.value) } })), required: true })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "model-capability-toggle", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "checkbox", checked: modelDraft.capabilities.normalized !== false, onChange: (event) => setModelDraft((current) => ({ ...current, capabilities: { ...current.capabilities, normalized: event.target.checked } })) }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "输出已归一化" })
                  ] })
                ] }) : null,
                modelDraft.capabilityType === "reranker" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-create-capabilities reranker", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "协议" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("select", { value: modelDraft.capabilities.protocol || "jina_rerank", onChange: (event) => setModelDraft((current) => ({ ...current, capabilities: { ...current.capabilities, protocol: event.target.value } })), children: rerankerProtocolOptions.map((protocol) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: protocol, children: protocol }, protocol)) })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "候选上限" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "number", min: "1", max: "1000", value: modelDraft.capabilities.max_candidates || "", onChange: (event) => setModelDraft((current) => ({ ...current, capabilities: { ...current.capabilities, max_candidates: Number(event.target.value) } })), required: true })
                  ] })
                ] }) : null,
                /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", disabled: Boolean(busy) || !modelDraft.model.trim(), children: busy.startsWith("model:") ? "保存中..." : "添加模型" })
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "model-catalog-list", children: selectedModels.length ? selectedModels.map((model) => /* @__PURE__ */ jsxRuntimeExports.jsx(
            ModelCatalogRow,
            {
              model,
              busy: busy === `model:${model.model}`,
              testBusy: busy === `model-test:${model.model}`,
              testResult: modelTestResults[`${model.provider_id}::${model.model}`],
              onDelete: deleteModel,
              onTest: testModel,
              onSave: (modelName, tokens, capabilityType, capabilities) => saveModel(modelName, tokens, capabilityType, capabilities)
            },
            `${model.provider_id}:${model.model}`
          )) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "这个 Provider 还没有登记模型。" }) })
        ] }) : null
      ] })
    ] })
  ] });
}
function MCPRegistrySettings({ onChanged, onRefreshRuntime, runtimeCheckedAt, runtimeError, runtimeLoading, runtimeStates, servers, workspaceID }) {
  const emptyDraft = { identifier: "", name: "", description: "", config: JSON.stringify({ transport: "streamable_http", url: "https://mcp.example.com/mcp", listen: true }, null, 2) };
  const [selectedID, setSelectedID] = reactExports.useState("");
  const [creating, setCreating] = reactExports.useState(false);
  const [draft, setDraft] = reactExports.useState(emptyDraft);
  const [busy, setBusy] = reactExports.useState("");
  const [error, setError] = reactExports.useState("");
  const [message, setMessage] = reactExports.useState("");
  const [testResults, setTestResults] = reactExports.useState({});
  const [deleteCandidate, setDeleteCandidate] = reactExports.useState("");
  const [versions, setVersions] = reactExports.useState([]);
  const [versionsLoading, setVersionsLoading] = reactExports.useState(false);
  const [versionError, setVersionError] = reactExports.useState("");
  const [restoreCandidate, setRestoreCandidate] = reactExports.useState(0);
  const selected = servers.find((server) => server.id === selectedID) || null;
  const runtimeByServer = groupMCPRuntimeStates(runtimeStates);
  const selectedRuntimeStates = selected ? runtimeByServer[selected.id] || [] : [];
  const selectedRuntimeSummary = summarizeMCPRuntimeStates(selectedRuntimeStates);
  reactExports.useEffect(() => {
    if (creating) return;
    const next = selected || servers[0] || null;
    if (!next) {
      setSelectedID("");
      setDraft(emptyDraft);
      return;
    }
    if (next.id !== selectedID) setSelectedID(next.id);
    setDraft({ identifier: next.identifier || "", name: next.name || "", description: next.description || "", config: JSON.stringify(next.config || {}, null, 2) });
  }, [creating, selectedID, selected == null ? void 0 : selected.current_version, servers]);
  reactExports.useEffect(() => {
    let active = true;
    if (creating || !(selected == null ? void 0 : selected.id)) {
      setVersions([]);
      setVersionsLoading(false);
      setVersionError("");
      return () => {
        active = false;
      };
    }
    setVersions([]);
    setVersionsLoading(true);
    setVersionError("");
    mcpServerVersions(selected.id).then((response) => {
      if (active) {
        setVersions(response.versions || []);
        setVersionsLoading(false);
      }
    }).catch((loadError) => {
      if (active) {
        setVersionError(loadError.message);
        setVersionsLoading(false);
      }
    });
    return () => {
      active = false;
    };
  }, [creating, selected == null ? void 0 : selected.id, selected == null ? void 0 : selected.current_version]);
  function choose(server) {
    setCreating(false);
    setSelectedID(server.id);
    setDraft({ identifier: server.identifier || "", name: server.name || "", description: server.description || "", config: JSON.stringify(server.config || {}, null, 2) });
    setError("");
    setMessage("");
    setDeleteCandidate("");
    setRestoreCandidate(0);
  }
  async function save(event) {
    event.preventDefault();
    setBusy("save");
    setError("");
    setMessage("");
    try {
      const config = JSON.parse(draft.config);
      const saved = creating ? await createMCPServer({ workspace_id: workspaceID, identifier: draft.identifier.trim(), name: draft.name.trim(), description: draft.description.trim(), config }) : await updateMCPServer(selected.id, { name: draft.name.trim(), description: draft.description.trim(), config });
      setCreating(false);
      setSelectedID(saved.id);
      setMessage(`已保存 ${saved.name} v${saved.current_version}`);
      await onChanged(saved.id);
    } catch (saveError) {
      setError(saveError.message);
    } finally {
      setBusy("");
    }
  }
  async function testServer(server) {
    setBusy(`test:${server.id}`);
    setError("");
    try {
      const response = await testMCPServer(server.id);
      setTestResults((current) => ({ ...current, [server.id]: response.result }));
    } catch (testError) {
      setError(testError.message);
    } finally {
      setBusy("");
    }
  }
  async function toggleServer(server) {
    setBusy(`toggle:${server.id}`);
    setError("");
    try {
      await setMCPServerEnabled(server.id, server.status !== "active");
      await onChanged(server.id);
    } catch (toggleError) {
      setError(toggleError.message);
    } finally {
      setBusy("");
    }
  }
  async function archiveServer(server) {
    setBusy(`delete:${server.id}`);
    setError("");
    try {
      await deleteMCPServer(server.id);
      setDeleteCandidate("");
      setSelectedID("");
      await onChanged("");
    } catch (deleteError) {
      setError(deleteError.message);
    } finally {
      setBusy("");
    }
  }
  async function restoreVersion(server, sourceVersion) {
    setBusy(`restore:${sourceVersion}`);
    setError("");
    setVersionError("");
    setMessage("");
    try {
      const result = await restoreMCPServerVersion(server.id, sourceVersion);
      setRestoreCandidate(0);
      setSelectedID(result.server.id);
      setMessage(`已从 v${result.source_version} 恢复为 v${result.new_version}`);
      await onChanged(result.server.id);
    } catch (restoreError) {
      setVersionError(restoreError.message);
    } finally {
      setBusy("");
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mcp-registry-layout", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "settings-card mcp-registry-list", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-section-heading", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "Workspace MCP" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            servers.length,
            " 个注册服务"
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-section-actions", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "icon-button secondary", type: "button", title: "刷新 MCP 运行状态", "aria-label": "刷新 MCP 运行状态", disabled: runtimeLoading, onClick: onRefreshRuntime, children: /* @__PURE__ */ jsxRuntimeExports.jsx(RefreshIcon, {}) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: () => {
            setCreating(true);
            setSelectedID("");
            setDraft(emptyDraft);
            setError("");
            setMessage("");
          }, children: "添加" })
        ] })
      ] }),
      servers.length ? servers.map((server) => {
        const result = testResults[server.id];
        const runtimeSummary = summarizeMCPRuntimeStates(runtimeByServer[server.id] || []);
        return /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: `mcp-registry-item ${!creating && selectedID === server.id ? "active" : ""}`, type: "button", onClick: () => choose(server), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `model-provider-status ${server.status !== "active" ? "disabled" : ""}` }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: server.name }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
              server.identifier,
              " · v",
              server.current_version,
              " · ",
              server.usage_count || 0,
              " 个 Agent"
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mcp-registry-status-stack", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("em", { className: `health-status ${(result == null ? void 0 : result.status) || (server.status === "active" ? "unchecked" : "configuration_error")}`, children: (result == null ? void 0 : result.status) || (server.status === "active" ? "未检查" : "已停用") }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("em", { className: `mcp-runtime-state ${runtimeSummary.state}`, children: runtimeSummary.label })
          ] })
        ] }, server.id);
      }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "Workspace 还没有注册 MCP 服务。" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { className: "settings-card mcp-registry-editor", onSubmit: save, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-section-heading", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: creating ? "添加 MCP 服务" : "服务配置" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: creating ? "创建 Workspace 共享连接" : (selected == null ? void 0 : selected.id) || "未选择" })
        ] }),
        !creating && selected ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-section-actions", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(busy), onClick: () => testServer(selected), children: busy === `test:${selected.id}` ? "测试中..." : "测试" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(busy), onClick: () => toggleServer(selected), children: selected.status === "active" ? "停用" : "启用" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "icon-button danger", type: "button", title: "归档 MCP 服务", "aria-label": "归档 MCP 服务", disabled: Boolean(busy) || selected.usage_count > 0, onClick: () => setDeleteCandidate(selected.id), children: /* @__PURE__ */ jsxRuntimeExports.jsx(DeleteIcon, {}) })
        ] }) : null
      ] }),
      error ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "health-error", children: error }) : null,
      message ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "environment-message", children: message }) : null,
      runtimeError ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "agent-version-error", children: runtimeError }) : null,
      deleteCandidate === (selected == null ? void 0 : selected.id) ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-version-confirm", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "归档后将从 Workspace 目录移除。" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => setDeleteCandidate(""), children: "取消" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: Boolean(busy), onClick: () => archiveServer(selected), children: "确认归档" })
        ] })
      ] }) : null,
      !creating && selected ? /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "mcp-runtime-panel", "aria-label": "MCP 运行保护状态", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "运行保护" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: runtimeCheckedAt ? new Date(runtimeCheckedAt).toLocaleTimeString() : "尚未刷新" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("em", { className: `mcp-runtime-state ${selectedRuntimeSummary.state}`, children: selectedRuntimeSummary.label })
        ] }),
        selectedRuntimeStates.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mcp-runtime-version-list", children: selectedRuntimeStates.map((state) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mcp-runtime-version", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
            "v",
            state.version
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `mcp-runtime-state ${state.state}`, children: mcpRuntimeStateLabel(state.state) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            "并发 ",
            state.in_flight || 0,
            "/",
            state.max_concurrency || 0
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            "连续失败 ",
            state.consecutive_failures || 0,
            "/",
            state.failure_threshold || 0
          ] }),
          state.last_failure_class ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: mcpRuntimeFailureLabel(state.last_failure_class) }) : null,
          state.cooldown_remaining_seconds > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            state.cooldown_remaining_seconds,
            " 秒后探测"
          ] }) : null
        ] }, `${state.server_id}:${state.version}`)) }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mcp-runtime-empty", children: "当前进程还没有该服务的调用记录。" })
      ] }) : null,
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mcp-registry-form-grid", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "标识" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { required: true, disabled: !creating, value: draft.identifier, onChange: (event) => setDraft((current) => ({ ...current, identifier: event.target.value })), placeholder: "team_search" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "名称" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { required: true, value: draft.name, onChange: (event) => setDraft((current) => ({ ...current, name: event.target.value })), placeholder: "团队搜索" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "mcp-registry-wide", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "说明" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: draft.description, onChange: (event) => setDraft((current) => ({ ...current, description: event.target.value })) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "mcp-registry-wide", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Server Config JSON" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("textarea", { rows: "13", required: true, value: draft.config, onChange: (event) => setDraft((current) => ({ ...current, config: event.target.value })), spellCheck: "false" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "model-editor-actions", children: [
        creating ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => {
          setCreating(false);
          if (servers[0]) choose(servers[0]);
        }, children: "取消" }) : null,
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", disabled: Boolean(busy) || !draft.name.trim() || !draft.identifier.trim(), children: busy === "save" ? "保存中..." : creating ? "创建服务" : "发布新版本" })
      ] })
    ] }),
    !creating && selected ? /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "settings-card mcp-registry-history", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "model-section-heading", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "版本历史" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
          versions.length,
          " 个不可变版本"
        ] })
      ] }) }),
      versionError ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "agent-version-error", children: versionError }) : null,
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-version-list", children: [
        versions.map((version2) => {
          const current = Number(version2.version) === Number(selected.current_version);
          const confirming = restoreCandidate === Number(version2.version);
          return /* @__PURE__ */ jsxRuntimeExports.jsxs("article", { className: `agent-version-row ${current ? "current" : ""}`, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-version-main", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-version-heading", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
                  "版本 #",
                  version2.version
                ] }),
                current ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "agent-version-current", children: "当前" }) : null
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-version-meta", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: version2.created_at ? new Date(version2.created_at).toLocaleString() : "" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                  "SHA-256 ",
                  String(version2.checksum_sha256 || "").slice(0, 12)
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { className: "mcp-registry-version-config", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("summary", { children: "查看配置" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { children: JSON.stringify(version2.config || {}, null, 2) })
              ] })
            ] }),
            !current && !confirming ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary agent-version-rollback", type: "button", disabled: Boolean(busy), onClick: () => setRestoreCandidate(Number(version2.version)), children: "恢复此版本" }) : null,
            confirming ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-version-confirm", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                "将版本 #",
                version2.version,
                " 复制为新的当前版本。已有 Agent binding 保持不变。"
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(busy), onClick: () => setRestoreCandidate(0), children: "取消" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: Boolean(busy), onClick: () => restoreVersion(selected, Number(version2.version)), children: busy === `restore:${version2.version}` ? "恢复中..." : "确认恢复" })
              ] })
            ] }) : null
          ] }, version2.id || version2.version);
        }),
        versionsLoading ? /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "正在加载版本..." }) : null,
        !versionsLoading && !versions.length && !versionError ? /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "还没有版本记录。" }) : null
      ] })
    ] }) : null
  ] });
}
function SettingsPage({
  activeSection,
  agents: agents2,
  archivedSessions,
  currentSession,
  onClose,
  onCreateAgent,
  onExportAgent,
  onImportAgent,
  onApplySessionConfig,
  onOpenInspector,
  onOpenSession,
  onRestoreSession,
  onRollbackAgent,
  onSaveAgent,
  onSelectAgent,
  onUpdateAgentPermissions,
  recentSessions,
  runtimeConfig,
  search: search2,
  sections,
  selectedAgent,
  restoringSessionID,
  rollingBackVersion,
  savingAgent,
  setActiveSection,
  setSearch,
  skills: skills2,
  modelOptions,
  toolingCatalog,
  onSkillsChanged,
  onModelCatalogChanged,
  workspaceID
}) {
  var _a2, _b, _c, _d;
  const [archiveQuery, setArchiveQuery] = reactExports.useState("");
  const [archiveRange, setArchiveRange] = reactExports.useState("all");
  const [agentPortabilityBusy, setAgentPortabilityBusy] = reactExports.useState("");
  const [agentPortabilityMessage, setAgentPortabilityMessage] = reactExports.useState("");
  const [agentCreateOpen, setAgentCreateOpen] = reactExports.useState(false);
  const [agentCreateName, setAgentCreateName] = reactExports.useState("");
  const [agentCreateModel, setAgentCreateModel] = reactExports.useState("");
  const [agentCreateBusy, setAgentCreateBusy] = reactExports.useState(false);
  const [agentCreateError, setAgentCreateError] = reactExports.useState("");
  const [agentManagementView, setAgentManagementView] = reactExports.useState("config");
  const [agentPermissionBusy, setAgentPermissionBusy] = reactExports.useState("");
  const [agentPermissionError, setAgentPermissionError] = reactExports.useState("");
  const [healthChecking, setHealthChecking] = reactExports.useState("");
  const [healthError, setHealthError] = reactExports.useState("");
  const [healthReport, setHealthReport] = reactExports.useState({ mcp: [], skills: [] });
  const [mcpRegistryServers, setMCPRegistryServers] = reactExports.useState([]);
  const [mcpRuntimeStatus, setMCPRuntimeStatus] = reactExports.useState({ checked_at: "", states: [] });
  const [mcpRuntimeLoading, setMCPRuntimeLoading] = reactExports.useState(false);
  const [mcpRuntimeError, setMCPRuntimeError] = reactExports.useState("");
  const importAgentInputRef = reactExports.useRef(null);
  const enabledSkills = ((_a2 = toolingCatalog.sections.find((section) => section.key === "skills")) == null ? void 0 : _a2.items.filter((item) => item.selectable)) || [];
  const availableMCP = ((_b = toolingCatalog.sections.find((section) => section.key === "mcp")) == null ? void 0 : _b.items) || [];
  const availableTools = ((_c = toolingCatalog.sections.find((section) => section.key === "tools")) == null ? void 0 : _c.items) || [];
  const sessionStats = {
    active: recentSessions.filter((session2) => session2.status === "running" || session2.status === "interrupting").length,
    idle: recentSessions.filter((session2) => session2.status === "idle").length,
    total: recentSessions.length
  };
  const selectedAgentModel = (selectedAgent == null ? void 0 : selectedAgent.config_version) ? `${selectedAgent.config_version.llm_provider || ""}::${selectedAgent.config_version.llm_model || ""}` : "";
  const defaultCreateModel = modelOptions.some((option) => `${option.llmProvider}::${option.llmModel}` === selectedAgentModel) ? selectedAgentModel : modelOptions[0] ? `${modelOptions[0].llmProvider}::${modelOptions[0].llmModel}` : "";
  const agentCreateModelValue = agentCreateModel || defaultCreateModel;
  const healthItems = [...healthReport.mcp || [], ...healthReport.skills || []];
  const healthyCount = healthItems.filter((item) => item.status === "online").length;
  const unhealthyCount = healthItems.filter((item) => item.status !== "online").length;
  const healthByKey = new Map(healthItems.map((item) => [`${item.kind}:${item.identifier}`, item]));
  const mcpWorkspaceID = workspaceID || (currentSession == null ? void 0 : currentSession.workspace_id) || (selectedAgent == null ? void 0 : selectedAgent.workspace_id) || "";
  async function refreshMCPRegistry() {
    const response = await mcpServers(mcpWorkspaceID);
    setMCPRegistryServers(response.servers || []);
    await refreshMCPRuntimeStatus();
  }
  async function refreshMCPRuntimeStatus() {
    setMCPRuntimeLoading(true);
    setMCPRuntimeError("");
    try {
      const response = await mcpServerRuntimeStatus(mcpWorkspaceID);
      setMCPRuntimeStatus({ checked_at: response.checked_at || "", states: response.states || [] });
    } catch (loadError) {
      setMCPRuntimeError(loadError.message);
    } finally {
      setMCPRuntimeLoading(false);
    }
  }
  reactExports.useEffect(() => {
    if (activeSection !== "mcp" && activeSection !== "agent") return;
    refreshMCPRegistry().catch((loadError) => setHealthError(loadError.message));
  }, [activeSection, mcpWorkspaceID]);
  async function runToolingHealth(kind = "", identifier = "") {
    if (!(selectedAgent == null ? void 0 : selectedAgent.id)) return;
    const key = kind && identifier ? `${kind}:${identifier}` : "all";
    setHealthChecking(key);
    setHealthError("");
    try {
      const report = await agentToolingHealth(selectedAgent.id, { kind, identifier });
      if (!kind) {
        setHealthReport(report);
      } else {
        setHealthReport((current) => {
          const field = kind === "mcp" ? "mcp" : "skills";
          const incoming = report[field] || [];
          const incomingKeys = new Set(incoming.map((item) => item.identifier));
          return {
            ...current,
            ...report.mcp_host ? { mcp_host: report.mcp_host } : {},
            ...report.mcp_http_host ? { mcp_http_host: report.mcp_http_host } : {},
            [field]: [...(current[field] || []).filter((item) => !incomingKeys.has(item.identifier)), ...incoming]
          };
        });
      }
    } catch (error) {
      setHealthError(error.message);
    } finally {
      setHealthChecking("");
    }
  }
  async function updateAgentToolPermission(agent2, namespace, enabled) {
    var _a3;
    const policy = parseToolPolicy((_a3 = agent2.config_version) == null ? void 0 : _a3.tools);
    const namespaceKeys = new Set(builtinToolNamespaces.map((item) => item.key));
    const customPatterns = policy.enabledToolPatterns.filter((pattern) => !namespaceKeys.has(pattern) && !builtinToolNamespaces.some((item) => pattern.startsWith(`${item.key}.`)));
    const enabledNamespaces = builtinToolNamespaces.filter((item) => item.key === namespace ? enabled : toolNamespaceEnabled(item.key, policy)).map((item) => item.key);
    const key = `${agent2.id}:${namespace}`;
    setAgentPermissionBusy(key);
    setAgentPermissionError("");
    try {
      await onUpdateAgentPermissions(agent2.id, {
        enabled_tools: [...enabledNamespaces, ...customPatterns],
        ...policy.runtime ? { runtime: policy.runtime } : {}
      });
    } catch (error) {
      setAgentPermissionError(error.message);
    } finally {
      setAgentPermissionBusy("");
    }
  }
  const filteredArchivedSessions = reactExports.useMemo(() => {
    const query = archiveQuery.trim().toLowerCase();
    const now = Date.now();
    return archivedSessions.filter((session2) => {
      var _a3;
      const archivedTime = new Date(session2.archived_at || 0).getTime();
      const ageDays = archivedTime ? (now - archivedTime) / 864e5 : Number.POSITIVE_INFINITY;
      if (archiveRange === "7" && ageDays > 7) return false;
      if (archiveRange === "30" && ageDays > 30) return false;
      if (archiveRange === "older" && ageDays <= 30) return false;
      if (!query) return true;
      const agentName = ((_a3 = agents2.find((agent2) => agent2.id === session2.agent_id)) == null ? void 0 : _a3.name) || "";
      return [session2.title, session2.id, agentName, session2.summary_text, ...session2.tags || []].some((value) => String(value || "").toLowerCase().includes(query));
    }).sort((left, right) => new Date(right.archived_at || 0).getTime() - new Date(left.archived_at || 0).getTime());
  }, [agents2, archiveQuery, archiveRange, archivedSessions]);
  const content2 = (() => {
    var _a3, _b2;
    switch (activeSection) {
      case "environment":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(EnvironmentVariablesSettings, { workspaceID });
      case "models":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(ModelManagementSettings, { onCatalogChanged: onModelCatalogChanged, onOpenEnvironment: () => setActiveSection("environment") });
      case "skills":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(
          SkillsManagement,
          {
            agent: selectedAgent,
            onApplyAgentConfig: onApplySessionConfig,
            onSkillsChanged,
            runtimeConfig,
            session: currentSession,
            sessionID: (currentSession == null ? void 0 : currentSession.id) || "",
            skills: skills2,
            workspaceID: (currentSession == null ? void 0 : currentSession.workspace_id) || (selectedAgent == null ? void 0 : selectedAgent.workspace_id) || ""
          }
        );
      case "mcp":
        return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-content-stack", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-hero-card", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "health-hero-head", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "MCP / Skills 健康检查" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "真实连接 MCP 服务并解析 Skills 版本，集中定位离线、配置和权限问题。" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: !selectedAgent || Boolean(healthChecking), onClick: () => runToolingHealth(), children: healthChecking === "all" ? "检查中..." : "全面检查" })
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(MCPRegistrySettings, { onChanged: refreshMCPRegistry, onRefreshRuntime: refreshMCPRuntimeStatus, runtimeCheckedAt: mcpRuntimeStatus.checked_at, runtimeError: mcpRuntimeError, runtimeLoading: mcpRuntimeLoading, runtimeStates: mcpRuntimeStatus.states, servers: mcpRegistryServers, workspaceID: mcpWorkspaceID }),
          healthError ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "health-error", children: healthError }) : null,
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-grid three", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-stat-card", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "已检查" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: healthItems.length })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-stat-card", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "正常" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: healthyCount })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-stat-card", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "需处理" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: unhealthyCount })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "MCP 服务" }),
            healthReport.mcp_host ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row health-result-row", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Server Host" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "health-metrics mcp-health-metrics", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    toolingHostMetric(healthReport.mcp_host, "sessions"),
                    " 个会话"
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    toolingHostMetric(healthReport.mcp_host, "in_use_sessions"),
                    " 个使用中"
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "上限 ",
                    toolingHostMetric(healthReport.mcp_host, "max_sessions")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "启动 ",
                    toolingHostMetric(healthReport.mcp_host, "starts_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "回收 ",
                    toolingHostMetric(healthReport.mcp_host, "reaped_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "拒绝 ",
                    toolingHostMetric(healthReport.mcp_host, "rejections_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "目录变更 ",
                    toolingHostMetric(healthReport.mcp_host, "tools_list_changed_total") + toolingHostMetric(healthReport.mcp_host, "resources_list_changed_total") + toolingHostMetric(healthReport.mcp_host, "prompts_list_changed_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "进度 ",
                    toolingHostMetric(healthReport.mcp_host, "progress_notifications_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "日志 ",
                    toolingHostMetric(healthReport.mcp_host, "log_messages_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "非法通知 ",
                    toolingHostMetric(healthReport.mcp_host, "invalid_notifications_total")
                  ] })
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `health-status ${toolingHostMetric(healthReport.mcp_host, "rejections_total") > 0 ? "configuration_error" : "online"}`, children: toolingHostMetric(healthReport.mcp_host, "rejections_total") > 0 ? "容量告警" : "运行中" })
            ] }) : null,
            healthReport.mcp_http_host ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row health-result-row", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Remote HTTP Host" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "health-metrics mcp-health-metrics", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    toolingHostMetric(healthReport.mcp_http_host, "sessions"),
                    " 个会话"
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    toolingHostMetric(healthReport.mcp_http_host, "in_use_sessions"),
                    " 个使用中"
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "上限 ",
                    toolingHostMetric(healthReport.mcp_http_host, "max_sessions")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "启动 ",
                    toolingHostMetric(healthReport.mcp_http_host, "starts_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "回收 ",
                    toolingHostMetric(healthReport.mcp_http_host, "reaped_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "拒绝 ",
                    toolingHostMetric(healthReport.mcp_http_host, "rejections_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "DELETE 失败 ",
                    toolingHostMetric(healthReport.mcp_http_host, "delete_errors_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "目录变更 ",
                    toolingHostMetric(healthReport.mcp_http_host, "tools_list_changed_total") + toolingHostMetric(healthReport.mcp_http_host, "resources_list_changed_total") + toolingHostMetric(healthReport.mcp_http_host, "prompts_list_changed_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "进度 ",
                    toolingHostMetric(healthReport.mcp_http_host, "progress_notifications_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "日志 ",
                    toolingHostMetric(healthReport.mcp_http_host, "log_messages_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "非法通知 ",
                    toolingHostMetric(healthReport.mcp_http_host, "invalid_notifications_total")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: toolingHostMetric(healthReport.mcp_http_host, "egress_allow_http") ? "允许 HTTP" : "仅 HTTPS" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: toolingHostMetric(healthReport.mcp_http_host, "egress_allow_private_networks") ? "允许私网" : "阻止私网" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "Host 白名单 ",
                    toolingHostMetric(healthReport.mcp_http_host, "egress_allowed_host_count")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "CIDR 白名单 ",
                    toolingHostMetric(healthReport.mcp_http_host, "egress_allowed_cidr_count")
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    "出站阻断 ",
                    toolingHostMetric(healthReport.mcp_http_host, "egress_blocked_total")
                  ] })
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `health-status ${toolingHostMetric(healthReport.mcp_http_host, "rejections_total") > 0 || toolingHostMetric(healthReport.mcp_http_host, "delete_errors_total") > 0 || toolingHostMetric(healthReport.mcp_http_host, "egress_blocked_total") > 0 ? "configuration_error" : "online"}`, children: toolingHostMetric(healthReport.mcp_http_host, "rejections_total") > 0 || toolingHostMetric(healthReport.mcp_http_host, "delete_errors_total") > 0 || toolingHostMetric(healthReport.mcp_http_host, "egress_blocked_total") > 0 ? "运行告警" : "运行中" })
            ] }) : null,
            availableMCP.length ? availableMCP.map((item) => {
              const normalizedIdentifier = String(item.name || "").replace(/[^a-zA-Z0-9_]+/g, "_").replace(/^_+|_+$/g, "").toLowerCase();
              const result = healthByKey.get(`mcp:${item.name}`) || healthByKey.get(`mcp:${normalizedIdentifier}`);
              const capabilities = toolingHealthCapabilities(result);
              return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row health-result-row", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: item.title }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: item.description }),
                  (result == null ? void 0 : result.detail) ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `health-detail ${result.status}`, children: result.detail }) : !item.selectable && item.disabledReason ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-row-note", children: item.disabledReason }) : null,
                  result ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "health-metrics mcp-health-metrics", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: result.transport || "stdio" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                      result.latency_ms || 0,
                      " ms"
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                      result.tool_count || 0,
                      " 个工具"
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                      toolingHealthCatalogMetric(result, "resource_count"),
                      " 个资源"
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                      toolingHealthCatalogMetric(result, "resource_template_count"),
                      " 个资源模板"
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                      toolingHealthCatalogMetric(result, "prompt_count"),
                      " 个 Prompt"
                    ] }),
                    capabilities.length ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                      "能力 ",
                      capabilities.join(", ")
                    ] }) : null
                  ] }) : null
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row-actions", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `health-status ${(result == null ? void 0 : result.status) || (item.selectable ? "unchecked" : "configuration_error")}`, children: (result == null ? void 0 : result.status) || (item.selectable ? "未检查" : "配置错误") }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(healthChecking), onClick: () => runToolingHealth("mcp", item.name), children: healthChecking === `mcp:${item.name}` ? "测试中..." : "测试" })
                ] })
              ] }, item.key);
            }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "当前智能体还没有配置 MCP 服务。" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "已启用 Skills" }),
            enabledSkills.length ? enabledSkills.map((item) => {
              const result = healthByKey.get(`skill:${item.name}`);
              return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row health-result-row", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: item.title }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: item.description }),
                  (result == null ? void 0 : result.detail) ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `health-detail ${result.status}`, children: result.detail }) : null,
                  result ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "health-metrics", children: [
                    "版本 ",
                    result.version || "-",
                    " · 约 ",
                    result.estimated_tokens || 0,
                    " tokens"
                  ] }) : null
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row-actions", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `health-status ${(result == null ? void 0 : result.status) || "unchecked"}`, children: (result == null ? void 0 : result.status) || "未检查" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(healthChecking), onClick: () => runToolingHealth("skill", item.name), children: healthChecking === `skill:${item.name}` ? "测试中..." : "测试" })
                ] })
              ] }, item.key);
            }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "当前 Agent 没有启用 Skills。" })
          ] })
        ] });
      case "agent":
        return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-content-stack", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-hero-card agent-management-hero", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Agent 管理" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "可视化维护默认模型、system prompt、工具、Skills 与 MCP 服务。" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-portability-actions", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "input",
                {
                  ref: importAgentInputRef,
                  type: "file",
                  accept: "application/json,.json",
                  hidden: true,
                  onChange: async (event) => {
                    var _a4;
                    const file = (_a4 = event.target.files) == null ? void 0 : _a4[0];
                    event.target.value = "";
                    if (!file) return;
                    setAgentPortabilityBusy("import");
                    setAgentPortabilityMessage("");
                    try {
                      const imported = await onImportAgent(file);
                      setAgentPortabilityMessage(`已导入 ${imported.name || imported.id}`);
                    } catch (error) {
                      setAgentPortabilityMessage(error.message);
                    } finally {
                      setAgentPortabilityBusy("");
                    }
                  }
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: agentCreateBusy, onClick: () => {
                setAgentCreateOpen((current) => !current);
                setAgentCreateName("");
                setAgentCreateModel(defaultCreateModel);
                setAgentCreateError("");
              }, children: agentCreateOpen ? "关闭新建" : "新建 Agent" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(agentPortabilityBusy), onClick: () => {
                var _a4;
                return (_a4 = importAgentInputRef.current) == null ? void 0 : _a4.click();
              }, children: agentPortabilityBusy === "import" ? "导入中..." : "导入 Agent" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: !selectedAgent || Boolean(agentPortabilityBusy), onClick: async () => {
                setAgentPortabilityBusy("export");
                setAgentPortabilityMessage("");
                try {
                  await onExportAgent();
                  setAgentPortabilityMessage(`已导出 ${selectedAgent.name || selectedAgent.id}`);
                } catch (error) {
                  setAgentPortabilityMessage(error.message);
                } finally {
                  setAgentPortabilityBusy("");
                }
              }, children: agentPortabilityBusy === "export" ? "导出中..." : "导出当前 Agent" })
            ] }),
            agentPortabilityMessage ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "agent-portability-message", children: agentPortabilityMessage }) : null
          ] }),
          agentCreateOpen ? /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { className: "settings-card agent-create-panel", onSubmit: async (event) => {
            event.preventDefault();
            const model = modelOptions.find((option) => `${option.llmProvider}::${option.llmModel}` === agentCreateModelValue);
            if (!agentCreateName.trim() || !model) return;
            setAgentCreateBusy(true);
            setAgentCreateError("");
            try {
              await onCreateAgent({
                name: agentCreateName.trim(),
                llm_provider: model.llmProvider,
                llm_model: model.llmModel
              });
              setAgentCreateOpen(false);
              setAgentCreateName("");
              setAgentCreateModel("");
            } catch (error) {
              setAgentCreateError(error.message);
            } finally {
              setAgentCreateBusy(false);
            }
          }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "新建 Agent" }),
            agentCreateError ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "agent-version-error agent-create-error", children: agentCreateError }) : null,
            /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "agent-editor-field", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Agent 名称" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: agentCreateName, onChange: (event) => setAgentCreateName(event.target.value), placeholder: "输入 Agent 名称" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "agent-editor-field", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "默认模型" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: agentCreateModelValue, onChange: (event) => setAgentCreateModel(event.target.value), children: [
                !modelOptions.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "没有可用模型" }) : null,
                modelOptions.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: `${option.llmProvider}::${option.llmModel}`, children: option.label }, `${option.llmProvider}::${option.llmModel}`))
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-create-actions", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: agentCreateBusy, onClick: () => {
                setAgentCreateOpen(false);
                setAgentCreateName("");
                setAgentCreateModel("");
                setAgentCreateError("");
              }, children: "取消" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", disabled: agentCreateBusy || !agentCreateName.trim() || !agentCreateModelValue, children: agentCreateBusy ? "创建中..." : "创建 Agent" })
            ] })
          ] }) : null,
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-management-tabs", role: "tablist", "aria-label": "Agent 管理视图", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: agentManagementView === "config" ? "active" : "", type: "button", role: "tab", "aria-selected": agentManagementView === "config", onClick: () => setAgentManagementView("config"), children: "配置编辑" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: agentManagementView === "permissions" ? "active" : "", type: "button", role: "tab", "aria-selected": agentManagementView === "permissions", onClick: () => setAgentManagementView("permissions"), children: "权限矩阵" })
          ] }),
          agentManagementView === "config" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "agent-management-layout", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card agent-management-list", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card-title", children: [
                "工作区智能体 · ",
                agents2.length
              ] }),
              agents2.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-agent-list", children: agents2.map((agent2) => {
                var _a4, _b3, _c2;
                const isCurrent = agent2.id === (selectedAgent == null ? void 0 : selectedAgent.id);
                return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `settings-row ${isCurrent ? "current" : ""}`, children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: agent2.name || agent2.id }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "subtle", children: [
                      ((_a4 = agent2.config_version) == null ? void 0 : _a4.llm_provider) || "-",
                      " / ",
                      ((_b3 = agent2.config_version) == null ? void 0 : _b3.llm_model) || "-"
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "subtle", children: [
                      "配置版本 #",
                      agent2.current_config_version || ((_c2 = agent2.config_version) == null ? void 0 : _c2.version) || 1
                    ] })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-row-actions", children: isCurrent ? /* @__PURE__ */ jsxRuntimeExports.jsx(Pill, { value: "idle" }) : /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => onSelectAgent(agent2.id), children: "选择" }) })
                ] }, agent2.id);
              }) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "工作区里还没有智能体。" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card agent-management-editor", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "当前 Agent 配置" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(AgentConfigEditor, { agent: selectedAgent, mcpRegistryServers, modelOptions, onRollback: onRollbackAgent, onSave: onSaveAgent, rollingBackVersion, saving: savingAgent, skills: skills2 })
            ] })
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card agent-permissions-card", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card-title", children: [
              "默认工具权限 · ",
              agents2.length,
              " 个 Agent"
            ] }),
            agentPermissionError ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "agent-version-error", children: agentPermissionError }) : null,
            agents2.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "agent-permissions-scroll", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: "agent-permissions-table", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("th", { scope: "col", children: "Agent" }),
                builtinToolNamespaces.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsx("th", { scope: "col", children: item.title }, item.key))
              ] }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("tbody", { children: agents2.map((agent2) => {
                var _a4;
                const policy = parseToolPolicy((_a4 = agent2.config_version) == null ? void 0 : _a4.tools);
                return /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { className: agent2.id === (selectedAgent == null ? void 0 : selectedAgent.id) ? "current" : "", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("th", { scope: "row", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: agent2.name || agent2.id }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                      "版本 #",
                      agent2.current_config_version || 1
                    ] })
                  ] }),
                  builtinToolNamespaces.map((item) => {
                    const checked = toolNamespaceEnabled(item.key, policy);
                    const busy = agentPermissionBusy === `${agent2.id}:${item.key}`;
                    return /* @__PURE__ */ jsxRuntimeExports.jsxs("td", { children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "input",
                        {
                          type: "checkbox",
                          "aria-label": `允许 ${agent2.name || agent2.id} 使用 ${item.title}`,
                          checked,
                          disabled: Boolean(agentPermissionBusy),
                          onChange: (event) => updateAgentToolPermission(agent2, item.key, event.target.checked)
                        }
                      ),
                      busy ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "agent-permission-saving", children: "保存中" }) : null
                    ] }, item.key);
                  })
                ] }, agent2.id);
              }) })
            ] }) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "工作区里还没有智能体。" })
          ] })
        ] });
      case "work":
        return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-content-stack", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-hero-card", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "归档任务中心" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "集中查找已归档任务，查看历史内容或恢复到主任务列表。" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-grid three", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-stat-card", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "总任务" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: sessionStats.total })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-stat-card", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "运行中" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: sessionStats.active })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-stat-card", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "空闲" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: sessionStats.idle })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "当前会话" }),
            currentSession ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: currentSession.title || currentSession.id }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: currentSession.id })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Pill, { value: currentSession.status || "unknown" })
            ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "当前还没有打开的任务。" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "archive-toolbar", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "archive-search-field", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "搜索归档任务" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("input", { value: archiveQuery, onChange: (event) => setArchiveQuery(event.target.value), placeholder: "按标题、任务 ID 或 Agent 搜索" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "archive-range-field", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "归档时间" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { value: archiveRange, onChange: (event) => setArchiveRange(event.target.value), children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "all", children: "全部时间" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "7", children: "最近 7 天" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "30", children: "最近 30 天" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "older", children: "30 天以前" })
                ] })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "archive-result-count", children: [
              filteredArchivedSessions.length,
              " 个归档任务"
            ] }),
            filteredArchivedSessions.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-agent-list", children: filteredArchivedSessions.map((session2) => {
              var _a4, _b3;
              const agentName = ((_a4 = agents2.find((agent2) => agent2.id === session2.agent_id)) == null ? void 0 : _a4.name) || session2.agent_id || "未知 Agent";
              const restoring = restoringSessionID === session2.id;
              return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: session2.title || session2.id }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "subtle", children: [
                    agentName,
                    " · 归档于 ",
                    formatTime(session2.archived_at || session2.created_at)
                  ] }),
                  session2.summary_text ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "archive-session-summary", children: sessionSummaryPreview(session2.summary_text) }) : null,
                  ((_b3 = session2.tags) == null ? void 0 : _b3.length) ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "task-nav-tags archive-tags", children: session2.tags.map((tag) => /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: tag }, tag)) }) : null,
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "archive-session-id", children: session2.id })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row-actions", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => onOpenSession(session2), children: "查看" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: restoring, onClick: () => onRestoreSession(session2), children: restoring ? "恢复中..." : "恢复" })
                ] })
              ] }, session2.id);
            }) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: archivedSessions.length ? "没有符合筛选条件的归档任务。" : "还没有已归档任务。" })
          ] })
        ] });
      default:
        return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-content-stack", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-hero-card", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "工具管理" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "当前智能体和会话可见的工具、Skills、MCP 能力会集中显示在这里。" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-grid two", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "当前工具能力" }),
              availableTools.length ? availableTools.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-row", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: item.title }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: item.description })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Pill, { value: item.selectable ? "idle" : "blocked" })
              ] }, item.key)) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "当前没有可见工具。" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-card", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-card-title", children: "当前智能体" }),
              selectedAgent ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-agent-highlight", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: selectedAgent.name || selectedAgent.id }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "subtle", children: [
                  ((_a3 = selectedAgent.config_version) == null ? void 0 : _a3.llm_provider) || "-",
                  " / ",
                  ((_b2 = selectedAgent.config_version) == null ? void 0 : _b2.llm_model) || "-"
                ] })
              ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "还没有选中智能体。" })
            ] })
          ] })
        ] });
    }
  })();
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "settings-page", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("aside", { className: "settings-sidebar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "settings-back-button", type: "button", onClick: onClose, children: "← 返回应用" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("input", { className: "settings-search", value: search2, onChange: (event) => setSearch(event.target.value), placeholder: "搜索设置..." }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-nav", children: sections.length ? sections.map((section) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          className: `settings-nav-item ${section.key === activeSection ? "active" : ""}`,
          type: "button",
          onClick: () => {
            if (section.key === "inspector") {
              onOpenInspector();
              return;
            }
            setActiveSection(section.key);
          },
          children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: section.title }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: section.description })
          ] })
        },
        section.key
      )) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "没有匹配的设置项。" }) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("main", { className: "settings-main", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("header", { className: "settings-main-header", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "settings-main-label", children: "设置" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { children: ((_d = sections.find((section) => section.key === activeSection)) == null ? void 0 : _d.title) || "设置" })
      ] }) }),
      content2
    ] })
  ] });
}
function eventText(event) {
  const data2 = payload(event);
  const content2 = data2.content;
  if (Array.isArray(content2)) {
    return content2.map((item) => item.text || item.content || "").filter(Boolean).join("\n");
  }
  if (typeof content2 === "string") return content2;
  return data2.message || data2.summary || data2.text || "";
}
function sessionSummaryPreview(value) {
  let text2 = String(value || "").trim();
  const assistantMarker = text2.lastIndexOf("\nassistant:");
  if (assistantMarker >= 0) text2 = text2.slice(assistantMarker + "\nassistant:".length).trim();
  text2 = text2.replace(/\s+/g, " ");
  return text2.length > 180 ? `${text2.slice(0, 180)}...` : text2;
}
function decodeHTML(value) {
  return String(value || "").replace(/&quot;/g, '"').replace(/&apos;/g, "'").replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&amp;/g, "&");
}
function stripTags(value) {
  return decodeHTML(String(value || "").replace(/<[^>]+>/g, "")).trim();
}
function parseSeedToolCall(source) {
  const text2 = String(source || "");
  const functionMatch = text2.match(/<function\s+[^>]*name=["']([^"']+)["'][^>]*>/i);
  const name2 = functionMatch ? decodeHTML(functionMatch[1]) : "tool";
  const args = {};
  const parameterPattern = /<parameter\s+[^>]*name=["']([^"']+)["'][^>]*>([\s\S]*?)<\/parameter>/gi;
  let parameterMatch = parameterPattern.exec(text2);
  while (parameterMatch) {
    args[decodeHTML(parameterMatch[1])] = stripTags(parameterMatch[2]);
    parameterMatch = parameterPattern.exec(text2);
  }
  const parts = normalizeToolParts(name2);
  const summary = toolSummary({
    identifier: parts.identifier || name2,
    apiName: parts.apiName,
    args
  });
  return {
    args,
    rawName: name2,
    summary
  };
}
function messageParts(text2) {
  const source = String(text2 || "");
  const parts = [];
  const pattern = /<seed:tool_call>[\s\S]*?<\/seed:tool_call>/gi;
  let cursor = 0;
  let match = pattern.exec(source);
  while (match) {
    const before = source.slice(cursor, match.index).trim();
    if (before) parts.push({ type: "text", text: before });
    parts.push({ type: "tool", tool: parseSeedToolCall(match[0]) });
    cursor = match.index + match[0].length;
    match = pattern.exec(source);
  }
  const after = source.slice(cursor).trim();
  if (after) parts.push({ type: "text", text: after });
  return parts.length ? parts : [{ type: "text", text: source }];
}
function cleanMessageText(text2) {
  return messageParts(text2).filter((part) => part.type === "text").map((part) => part.text).join("\n").trim();
}
function shortText(value, maxLength = 180) {
  const text2 = String(value || "").replace(/\s+/g, " ").trim();
  if (!text2) return "";
  return text2.length > maxLength ? `${text2.slice(0, maxLength - 1)}...` : text2;
}
function maxSeq(events2) {
  return (events2 || []).reduce((maximum, event) => Math.max(maximum, Number(event.seq || 0)), 0);
}
function isActivityEvent(event) {
  return Boolean(event == null ? void 0 : event.type) && (event.type.startsWith("runtime.") || event.type.startsWith("session.status_"));
}
function activitySummary(event) {
  const summary = activityView(event).detail || eventText(event);
  return shortText(summary || event.type || "");
}
function compactActivityEvents(sourceEvents) {
  const compacted = [];
  const relevantEvents = (sourceEvents || []).filter((event) => isActivityEvent(event) || event.type === "agent.message");
  relevantEvents.forEach((event) => {
    const activity = activityView(event);
    const previous2 = compacted[compacted.length - 1];
    const signature = [activity.title, activity.detail, activity.kind].join("|");
    const isStreamingNoise = event.type === "runtime.llm_delta" || event.type === "runtime.llm_chunk";
    if (previous2 && previous2.signature === signature && (isStreamingNoise || event.type === previous2.type)) {
      previous2.count += 1;
      previous2.event = event;
      previous2.activity = activity;
      return;
    }
    compacted.push({
      activity,
      count: 1,
      event,
      signature,
      type: event.type
    });
  });
  return compacted.slice(-10).reverse();
}
function llmChunkActivity(data2) {
  var _a2;
  switch (data2.type) {
    case "reasoning":
      return { title: "模型推理", detail: shortText(data2.text || "模型正在返回推理内容。", 180), kind: "running" };
    case "tool_call":
      return { title: "模型准备工具", detail: "正在组装工具调用参数。", kind: "running" };
    case "usage":
      return { title: "模型用量已返回", detail: `${Number(((_a2 = data2.usage) == null ? void 0 : _a2.total_tokens) || 0)} tokens`, kind: "ok" };
    case "stop":
      return { title: "模型流已结束", detail: data2.finish_reason || "已完成流式响应。", kind: "ok" };
    case "error": {
      const presentation = providerErrorPresentation(data2.error, "流式响应失败。");
      return { title: "模型流错误", detail: shortText(presentation.detail, 260), kind: "failed" };
    }
    default:
      return { title: "生成回复", detail: "正在流式返回内容。", kind: "running" };
  }
}
function activityView(event) {
  const data2 = eventData(event);
  switch (event == null ? void 0 : event.type) {
    case "agent.message":
      return { title: "智能体已回复", detail: shortText(cleanMessageText(eventText(event)) || "已准备工具动作。", 180), kind: "ok" };
    case "runtime.started":
      return { title: "开始处理", detail: "正在准备本轮任务。", kind: "running" };
    case "runtime.thinking":
      return { title: "正在处理", detail: "正在准备下一步。", kind: "running" };
    case "runtime.llm_request":
      return { title: "请求模型", detail: "正在生成下一步动作或回复。", kind: "running" };
    case "runtime.llm_delta":
      return { title: "生成回复", detail: "正在流式返回内容。", kind: "running" };
    case "runtime.llm_chunk":
      return llmChunkActivity(data2);
    case "runtime.llm_response":
      return { title: "模型已返回", detail: "正在判断是否需要工具。", kind: "running" };
    case "runtime.progress_message":
      return { title: "过程更新", detail: shortText(data2.text || "智能体正在继续处理。", 180), kind: "running" };
    case "runtime.tool_call": {
      const summary = toolSummary({
        identifier: data2.identifier,
        apiName: data2.api_name,
        args: objectValue(data2.arguments),
        reason: data2.reason
      });
      return { title: summary.title, detail: summary.detail || summary.label, kind: summary.risk === "high" ? "warn" : "tool" };
    }
    case "runtime.tool_result": {
      const summary = toolSummary({
        identifier: data2.identifier,
        apiName: data2.api_name,
        args: objectValue(data2.arguments),
        reason: data2.reason,
        success: data2.success
      });
      const artifactCount = Array.isArray(data2.artifacts) ? data2.artifacts.length : 0;
      const detail = data2.success === false ? data2.error || data2.message || "工具返回了错误。" : artifactCount ? `生成了 ${artifactCount} 个结果文件。` : summary.detail;
      return { title: data2.success === false ? `${summary.title} failed` : `${summary.title} finished`, detail: shortText(detail, 180), kind: data2.success === false ? "error" : "ok" };
    }
    case "runtime.tool_intervention_required": {
      const summary = toolSummary({
        identifier: data2.identifier,
        apiName: data2.api_name,
        args: objectValue(data2.arguments),
        reason: data2.reason
      });
      return { title: `需要审批：${summary.title}`, detail: summary.detail || data2.reason || "请先审批再继续。", kind: "warn" };
    }
    case "runtime.tool_intervention_approved":
      return { title: "审批已通过", detail: data2.decision_reason || "正在继续任务。", kind: "ok" };
    case "runtime.tool_intervention_rejected":
      return { title: "审批被拒绝", detail: data2.decision_reason || "该工具调用未被允许。", kind: "error" };
    case "runtime.completed":
    case "session.status_idle":
      return { title: "任务空闲", detail: payload(event).last_turn_status === "failed" ? payload(event).reason || "上一轮执行失败。" : "等待下一条消息。", kind: payload(event).last_turn_status === "failed" ? "error" : "ok" };
    case "runtime.failed":
    case "session.status_failed": {
      const original = payload(event).reason || payload(event).message || "执行过程中出现失败。";
      const providerError = objectValue(data2.provider_error);
      const detail = Object.keys(providerError).length ? providerErrorPresentation(providerError, original).detail : original;
      return { title: "任务失败", detail: shortText(detail, 260), kind: "error" };
    }
    case "session.status_running":
      return { title: "执行中", detail: "智能体正在处理任务。", kind: "running" };
    case "session.status_interrupting":
      return { title: "正在中断", detail: "正在停止当前轮次。", kind: "warn" };
    case "session.status_terminated":
      return { title: "已归档", detail: "该任务已经归档。", kind: "muted" };
    default:
      return { title: (event == null ? void 0 : event.type) || "活动", detail: turnActivityLabel(event) || eventText(event), kind: "muted" };
  }
}
function artifactName(artifact) {
  return (artifact == null ? void 0 : artifact.name) || (artifact == null ? void 0 : artifact.id) || "artifact";
}
function artifactMetadata(artifact) {
  const metadata = artifact == null ? void 0 : artifact.metadata;
  return metadata && typeof metadata === "object" && !Array.isArray(metadata) ? metadata : {};
}
function isUserFileArtifact(artifact) {
  const metadata = artifactMetadata(artifact);
  return metadata.protocol_version === "tma.tool_export.v1" || String((artifact == null ? void 0 : artifact.description) || "").startsWith("Exported file");
}
function finalFileArtifacts(artifacts2) {
  const latestByPath = /* @__PURE__ */ new Map();
  for (const artifact of artifacts2 || []) {
    if (!isUserFileArtifact(artifact)) continue;
    const metadata = artifactMetadata(artifact);
    const key = String(metadata.path || artifactName(artifact)).replace(/\\/g, "/");
    latestByPath.set(key, artifact);
  }
  return [...latestByPath.values()];
}
function artifactReferencedByMessage(artifact, text2) {
  const normalizedText = String(text2 || "").replace(/\\/g, "/");
  const metadata = artifactMetadata(artifact);
  const path2 = String(metadata.path || metadata.file_path || "").replace(/\\/g, "/");
  const name2 = artifactName(artifact);
  return Boolean(path2 && normalizedText.includes(path2) || name2 && normalizedText.includes(name2));
}
function finalAgentMessageArtifacts(event, artifacts2) {
  const turnID = payload(event).turn_id || "";
  const candidates = finalFileArtifacts((artifacts2 || []).filter((artifact) => artifact.turn_id === turnID));
  const referenced = candidates.filter((artifact) => artifactReferencedByMessage(artifact, eventText(event)));
  return referenced.length ? referenced : candidates;
}
function conversationFinalFileArtifacts(artifacts2, events2) {
  const candidates = finalFileArtifacts(artifacts2);
  const finalMessageByTurn = /* @__PURE__ */ new Map();
  for (const event of events2 || []) {
    if (event.type === "agent.message") finalMessageByTurn.set(payload(event).turn_id || "", event);
  }
  const referencedTurns = /* @__PURE__ */ new Set();
  for (const artifact of candidates) {
    const event = finalMessageByTurn.get(artifact.turn_id || "");
    if (event && artifactReferencedByMessage(artifact, eventText(event))) referencedTurns.add(artifact.turn_id || "");
  }
  return candidates.filter((artifact) => {
    const turnID = artifact.turn_id || "";
    if (!referencedTurns.has(turnID)) return true;
    return artifactReferencedByMessage(artifact, eventText(finalMessageByTurn.get(turnID)));
  });
}
function agentMessageWithArtifactLinks(text2, hasArtifacts) {
  if (!hasArtifacts) return text2;
  return String(text2 || "").replace(
    /^\s*(?:\*\*)?文件(?:路径|地址)(?:\*\*)?\s*[：:]\s*`[^`]+`\s*$/gm,
    "文件已保存到任务产物，可从下方预览或下载。"
  ).replace(/点击上方\s*artifact\s*中的/gi, "使用下方相关文件中的");
}
function artifactPathParts(artifact) {
  const metadata = artifactMetadata(artifact);
  const explicitPath = metadata.path || metadata.file_path || "";
  const rawPath = String(explicitPath || artifactName(artifact) || "").replace(/\\/g, "/");
  const parts = rawPath.split("/").filter(Boolean);
  if (explicitPath && parts.length) return parts;
  const turnMatch = String((artifact == null ? void 0 : artifact.turn_id) || "").match(/(\d+)$/);
  const turnFolder = turnMatch ? `第 ${Number(turnMatch[1])} 轮` : "其他结果";
  return [turnFolder, parts.at(-1) || artifactName(artifact)];
}
function buildArtifactTree(artifacts2) {
  const root2 = { folders: /* @__PURE__ */ new Map(), files: [] };
  for (const artifact of artifacts2) {
    const parts = artifactPathParts(artifact);
    let node2 = root2;
    for (const folder of parts.slice(0, -1)) {
      if (!node2.folders.has(folder)) {
        node2.folders.set(folder, { folders: /* @__PURE__ */ new Map(), files: [] });
      }
      node2 = node2.folders.get(folder);
    }
    node2.files.push({ artifact, label: parts.at(-1) || artifactName(artifact) });
  }
  return root2;
}
function ArtifactTreeNode({ name: name2, node: node2, depth, selectedArtifactID, onPreview }) {
  const [open, setOpen] = reactExports.useState(true);
  const folders = Array.from(node2.folders.entries()).sort(([left], [right]) => left.localeCompare(right));
  const files = [...node2.files].sort((left, right) => left.label.localeCompare(right.label));
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "artifact-tree-branch", children: [
    name2 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "artifact-tree-folder", type: "button", style: { "--tree-depth": depth }, onClick: () => setOpen((current) => !current), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `artifact-tree-chevron ${open ? "open" : ""}`, children: "›" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(FolderIcon, { open }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: name2 })
    ] }) : null,
    open ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
      folders.map(([folderName, folderNode]) => /* @__PURE__ */ jsxRuntimeExports.jsx(ArtifactTreeNode, { name: folderName, node: folderNode, depth: depth + 1, selectedArtifactID, onPreview }, `${depth}-${folderName}`)),
      files.map(({ artifact, label }) => {
        return /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            className: `artifact-tree-file ${artifact.id === selectedArtifactID ? "active" : ""}`,
            style: { "--tree-depth": depth + (name2 ? 1 : 0) },
            title: `打开 ${artifactName(artifact)}`,
            type: "button",
            onClick: () => onPreview(artifact),
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(FileIcon, {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label })
            ]
          },
          artifact.id
        );
      })
    ] }) : null
  ] });
}
function ArtifactPreviewContent({ preview, mode = "preview" }) {
  if (!preview) return null;
  if (preview.status === "loading") return /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "正在加载预览..." });
  if (preview.status === "error") return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "artifact-preview-error", children: preview.error });
  if (preview.status === "ready" && preview.kind === "image") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("img", { className: "preview-media", src: preview.objectUrl, alt: preview.resource.title });
  }
  if (preview.status === "ready" && preview.kind === "text") {
    if (mode === "preview") {
      if (isMarkdownResource(preview.resource, preview.contentType)) {
        return /* @__PURE__ */ jsxRuntimeExports.jsx("article", { className: "artifact-preview-markdown", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Markdown, { remarkPlugins: [remarkGfm], children: preview.text || "" }) });
      }
      if (isHTMLResource(preview.resource, preview.contentType)) {
        return /* @__PURE__ */ jsxRuntimeExports.jsx(
          "iframe",
          {
            className: "artifact-preview-html",
            sandbox: "",
            referrerPolicy: "no-referrer",
            srcDoc: htmlPreviewDocument(preview.text || ""),
            title: `${preview.resource.title} HTML 预览`
          }
        );
      }
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { className: "artifact-preview-text", children: preview.text || "" });
  }
  if (preview.status === "ready" && preview.kind === "download") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "artifact-preview-error", children: preview.message || "请下载文件后查看。" });
  }
  return null;
}
function MessageArtifacts({ artifacts: artifacts2, sessionID, onPreview }) {
  if (!artifacts2.length) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "message-artifacts", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "message-artifacts-title", children: "相关文件" }),
    artifacts2.map((artifact) => {
      const href = artifactDownloadPath(sessionID, artifact.id);
      return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "message-artifact", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "message-artifact-open", type: "button", onClick: () => onPreview(artifact), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(FileIcon, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: artifactName(artifact) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("a", { className: "message-artifact-download", href, target: "_blank", rel: "noreferrer", children: "下载" })
      ] }, artifact.id);
    })
  ] });
}
function uploadedMessageArtifacts(event, artifacts2) {
  const attachments = Array.isArray(payload(event).attachments) ? payload(event).attachments : [];
  const byID = new Map((artifacts2 || []).map((artifact) => [artifact.id, artifact]));
  return attachments.map((attachment) => byID.get(attachment.artifact_id) || {
    id: attachment.artifact_id,
    name: attachment.name,
    artifact_type: "file",
    metadata: {
      content_type: attachment.content_type,
      size_bytes: attachment.size_bytes,
      workspace_path: attachment.workspace_path
    }
  }).filter((artifact) => artifact.id);
}
function turnActivityLabel(event) {
  if (!event) return "";
  switch (event.type) {
    case "runtime.started":
      return "本轮已开始，正在处理...";
    case "runtime.thinking":
      return "正在处理当前请求...";
    case "runtime.llm_request":
      return "正在和模型对话...";
    case "runtime.llm_delta":
      return "正在生成回复...";
    case "runtime.llm_chunk": {
      const type = eventData(event).type;
      if (type === "reasoning") return "模型正在推理...";
      if (type === "tool_call") return "模型正在准备工具调用...";
      if (type === "usage") return "模型已返回用量...";
      if (type === "stop") return "模型流已结束，正在整理...";
      if (type === "error") return "模型流式响应失败。";
      return "正在生成回复...";
    }
    case "runtime.llm_response":
      return "模型已返回结果，正在整理...";
    case "runtime.progress_message":
      return "智能体更新了当前进展。";
    case "runtime.tool_call":
      return "正在调用工具...";
    case "runtime.tool_result":
      return "工具已返回结果，正在继续...";
    case "runtime.tool_intervention_required":
      return "工具调用需要确认后才能继续。";
    case "runtime.tool_intervention_approved":
      return "审批已通过，正在继续执行...";
    default:
      return "";
  }
}
function turnSignal(events2, options = {}) {
  const sinceSeq = Number(options.sinceSeq || 0);
  const sessionStatus = options.sessionStatus || "";
  const waitingForReply = Boolean(options.waitingForReply);
  const includeSuccess = Boolean(options.includeSuccess);
  const interventions2 = options.interventions || [];
  const currentEvents = (events2 || []).filter((event) => Number(event.seq || 0) > sinceSeq);
  const latestEvent2 = currentEvents[currentEvents.length - 1] || null;
  const reverseEvents = [...currentEvents].reverse();
  const latestAgentMessage = reverseEvents.find((event) => event.type === "agent.message");
  const failureEvent = reverseEvents.find(
    (event) => event.type === "runtime.failed" || event.type === "session.status_idle" && payload(event).last_turn_status === "failed" || event.type === "session.status_failed"
  );
  const idleEvent = reverseEvents.find((event) => event.type === "session.status_idle");
  const terminatedEvent = reverseEvents.find((event) => event.type === "session.status_terminated");
  const approvalEvent = reverseEvents.find((event) => event.type === "runtime.tool_intervention_required");
  const progressEvent = reverseEvents.find(
    (event) => [
      "runtime.started",
      "runtime.thinking",
      "runtime.llm_request",
      "runtime.llm_chunk",
      "runtime.llm_delta",
      "runtime.llm_response",
      "runtime.tool_call",
      "runtime.tool_result",
      "runtime.tool_intervention_approved"
    ].includes(event.type)
  );
  if (interventions2.length) {
    return {
      kind: "approval",
      title: interventions2.length === 1 ? "等待审批" : `${interventions2.length} 个审批待处理`,
      detail: approvalEvent ? turnActivityLabel(approvalEvent) : "Approve or reject to continue."
    };
  }
  if (failureEvent) {
    const reason = payload(failureEvent).reason || payload(failureEvent).message || eventText(failureEvent) || "Turn failed.";
    return {
      kind: "error",
      title: "本轮失败",
      detail: reason
    };
  }
  if (latestAgentMessage && includeSuccess) {
    return {
      kind: "success",
      title: "已回复",
      detail: shortText(eventText(latestAgentMessage)) || "Agent reply received."
    };
  }
  if (idleEvent || terminatedEvent || sessionStatus === "idle" || sessionStatus === "terminated") {
    return null;
  }
  if (waitingForReply || progressEvent || sessionStatus === "running" || sessionStatus === "interrupting") {
    return {
      kind: "thinking",
      title: "正在处理",
      detail: turnActivityLabel(progressEvent || latestEvent2) || "正在生成回复..."
    };
  }
  return null;
}
function ToolActionCard({ tool }) {
  const summary = tool.summary;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `tool-action-card risk-${summary.risk}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "tool-action-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: summary.title }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Meta, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: summary.label || tool.rawName }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `source-chip ${summary.source}`, children: summary.sourceLabel }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: `risk-chip ${summary.risk}`, children: [
            summary.risk,
            " risk"
          ] })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "tool-action-status", children: "已准备" })
    ] }),
    summary.detail ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "tool-action-detail", children: summary.detail }) : null,
    Object.keys(tool.args || {}).length ? /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { className: "tool-action-details", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("summary", { children: "详情" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { children: pretty(tool.args) })
    ] }) : null
  ] });
}
function ChevronIcon() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m5 6 3 3 3-3", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: "1.5" }) });
}
function ProcessIcon({ type }) {
  if (type === "runtime.failed") {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("circle", { cx: "8", cy: "8", r: "5.5", fill: "none", stroke: "currentColor", strokeWidth: "1.25" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m6 6 4 4m0-4-4 4", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeWidth: "1.4" })
    ] });
  }
  if (type === "runtime.thinking") {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M8 2.25a4.35 4.35 0 0 0-2.72 7.75c.45.36.72.85.72 1.35v.4h4v-.4c0-.5.27-.99.72-1.35A4.35 4.35 0 0 0 8 2.25Z", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: "1.25" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M6.5 13.75h3M8 5v3M6.5 6.5h3", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeWidth: "1.25" })
    ] });
  }
  if (type.includes("intervention")) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M8 2.25 13 4v3.65c0 2.85-1.9 5.2-5 6.1-3.1-.9-5-3.25-5-6.1V4l5-1.75Z", fill: "none", stroke: "currentColor", strokeLinejoin: "round", strokeWidth: "1.25" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M8 5v3.2m0 2.3v.1", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeWidth: "1.4" })
    ] });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m9.5 2.5-6 6h4l-1 5 6-7h-4l1-4Z", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: "1.25" }) });
}
function ProcessStatusIcon({ status }) {
  if (status === "running") return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": "true", className: "process-status-spinner" });
  if (status === "error") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m5 5 6 6m0-6-6 6", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeWidth: "1.5" }) });
  }
  if (status === "warning") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M8 2.5 14 13H2L8 2.5Zm0 3.5v3.2m0 1.8v.1", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: "1.25" }) });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { "aria-hidden": "true", viewBox: "0 0 16 16", children: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m3.5 8 3 3 6-6", fill: "none", stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: "1.5" }) });
}
function formatClockTime(value) {
  if (!value) return "";
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return "";
  return date.toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" });
}
function ProcessEventCard({
  event,
  active = false,
  completedAt = "",
  toolLifecycle = null,
  onApplySessionConfig = () => {
  },
  onRequestSkillEnable = () => {
  },
  onRequestSkillDisable = () => {
  },
  activeSkillKeys = /* @__PURE__ */ new Set(),
  enabledSkillKeys = /* @__PURE__ */ new Set(),
  latestSkillLifecycle = /* @__PURE__ */ new Map(),
  sessionConfigApplyBusy = 0,
  sessionConfigVersion = 0,
  skillEnableBusy = "",
  skillDisableBusy = "",
  skillEnableDisabled = false
}) {
  var _a2, _b;
  const data2 = eventData(event);
  const error = objectValue(data2.error);
  const args = objectValue(data2.arguments);
  const state = objectValue(data2.state);
  const artifacts2 = Array.isArray(data2.artifacts) ? data2.artifacts : [];
  let title = event.type;
  let metaLabel = event.type;
  let preview = "";
  let detailObject = null;
  let tone = "muted";
  let status = "completed";
  let statusLabel = "完成";
  let defaultExpanded = false;
  const lifecycleResult = toolLifecycle == null ? void 0 : toolLifecycle.result;
  const lifecycleResultData = eventData(lifecycleResult);
  const lifecycleRejected = ((_a2 = toolLifecycle == null ? void 0 : toolLifecycle.decision) == null ? void 0 : _a2.type) === "runtime.tool_intervention_rejected";
  const lifecycleApproved = ((_b = toolLifecycle == null ? void 0 : toolLifecycle.decision) == null ? void 0 : _b.type) === "runtime.tool_intervention_approved";
  if (event.type === "runtime.thinking") {
    title = "处理请求";
    metaLabel = "运行状态";
    preview = turnActivityLabel(event) || "正在准备下一步。";
    tone = "tool";
    status = active ? "running" : "completed";
    statusLabel = active ? "进行中" : "完成";
    defaultExpanded = true;
  } else if (isReasoningChunk(event)) {
    title = "推理过程";
    metaLabel = "模型返回";
    preview = String(data2.text || "");
    tone = "tool";
    status = active ? "running" : "completed";
    statusLabel = active ? "生成中" : "完成";
    defaultExpanded = false;
  } else if (event.type === "runtime.tool_call") {
    const summary = toolSummary({
      identifier: data2.identifier,
      apiName: data2.api_name,
      args,
      reason: data2.reason,
      source: data2.tool_source,
      manifestType: data2.manifest_type
    });
    title = summary.title;
    metaLabel = `调用 · ${summary.label}`;
    preview = processPreview(data2.identifier, data2.api_name, args, {}, summary.source);
    detailObject = {
      source: summary.sourceLabel,
      arguments: Object.keys(args).length ? args : void 0
    };
    tone = summary.risk === "high" ? "warn" : "tool";
    status = summary.risk === "high" ? "warning" : active ? "running" : "completed";
    statusLabel = summary.risk === "high" ? "待确认" : active ? "执行中" : "已调用";
    defaultExpanded = active;
    if (lifecycleResult) {
      tone = lifecycleResultData.success === false ? "error" : "ok";
      status = lifecycleResultData.success === false ? "error" : "completed";
      statusLabel = lifecycleResultData.success === false ? "失败" : "完成";
      defaultExpanded = false;
    } else if (lifecycleRejected) {
      tone = "error";
      status = "error";
      statusLabel = "已拒绝";
    } else if (lifecycleApproved) {
      tone = "ok";
      status = "completed";
      statusLabel = "已通过";
    } else if (toolLifecycle == null ? void 0 : toolLifecycle.required) {
      tone = "warn";
      status = "warning";
      statusLabel = "待审批";
    }
  } else if (event.type === "runtime.tool_result") {
    const summary = toolSummary({
      identifier: data2.identifier,
      apiName: data2.api_name,
      args,
      reason: data2.reason,
      success: data2.success,
      source: data2.tool_source,
      manifestType: data2.manifest_type
    });
    title = summary.title;
    metaLabel = `结果 · ${summary.label}`;
    preview = processPreview(data2.identifier, data2.api_name, args, {
      content: data2.content,
      error,
      state,
      success: data2.success
    }, summary.source) || (artifacts2.length ? `${artifacts2.length} artifact${artifacts2.length === 1 ? "" : "s"} generated.` : "Tool finished successfully.");
    detailObject = {
      source: summary.sourceLabel,
      arguments: Object.keys(args).length ? args : void 0,
      content: data2.content || void 0,
      state: data2.state && Object.keys(objectValue(data2.state)).length ? data2.state : void 0,
      artifacts: artifacts2.length ? artifacts2 : void 0,
      error: Object.keys(error).length ? error : void 0
    };
    tone = data2.success === false ? "error" : "ok";
    status = data2.success === false ? "error" : "completed";
    statusLabel = data2.success === false ? "失败" : "完成";
    defaultExpanded = data2.success !== false && data2.identifier === "skills" && ["preview", "install", "enable", "disable"].includes(data2.api_name);
  } else if (event.type === "runtime.tool_intervention_required") {
    const summary = toolSummary({
      identifier: data2.identifier,
      apiName: data2.api_name,
      args,
      reason: data2.reason,
      source: data2.tool_source,
      manifestType: data2.manifest_type
    });
    title = `需要审批：${summary.title}`;
    metaLabel = `审批 · ${summary.label}`;
    preview = summary.detail || data2.reason || "请先审批再继续。";
    detailObject = {
      source: summary.sourceLabel,
      arguments: Object.keys(args).length ? args : void 0
    };
    tone = "warn";
    status = "warning";
    statusLabel = "待审批";
    defaultExpanded = true;
    if (lifecycleResult) {
      tone = lifecycleResultData.success === false ? "error" : "ok";
      status = lifecycleResultData.success === false ? "error" : "completed";
      statusLabel = lifecycleResultData.success === false ? "失败" : "已执行";
      defaultExpanded = false;
    } else if (lifecycleRejected) {
      tone = "error";
      status = "error";
      statusLabel = "已拒绝";
      defaultExpanded = false;
    } else if (lifecycleApproved) {
      tone = "ok";
      status = "completed";
      statusLabel = "已通过";
      defaultExpanded = false;
    }
  } else if (event.type === "runtime.tool_intervention_approved") {
    title = "审批已通过";
    metaLabel = "审批";
    preview = data2.decision_reason || "该工具调用可以继续执行。";
    tone = "ok";
    statusLabel = "已通过";
  } else if (event.type === "runtime.tool_intervention_rejected") {
    title = "审批被拒绝";
    metaLabel = "审批";
    preview = data2.decision_reason || "该工具调用已停止。";
    tone = "error";
    status = "error";
    statusLabel = "已拒绝";
    defaultExpanded = true;
  } else if (event.type === "runtime.failed") {
    title = "任务失败";
    metaLabel = "执行错误";
    const original = payload(event).reason || payload(event).message || eventText(event) || "执行过程中出现错误。";
    const providerError = objectValue(data2.provider_error);
    const presentation = Object.keys(providerError).length ? providerErrorPresentation(providerError, original) : null;
    preview = (presentation == null ? void 0 : presentation.detail) || original;
    detailObject = presentation ? { description: presentation.description, original_error: presentation.original, provider_error: providerError } : Object.keys(error).length ? { error } : null;
    tone = "error";
    status = "error";
    statusLabel = "失败";
    defaultExpanded = true;
  }
  const startedAtMS = new Date(event.created_at || "").getTime();
  const completedAtMS = new Date(completedAt || "").getTime();
  const inferredDurationMS = Number.isFinite(startedAtMS) && Number.isFinite(completedAtMS) && completedAtMS > startedAtMS ? completedAtMS - startedAtMS : 0;
  const durationMS = Number(firstValue(data2, ["duration_ms"]) || firstValue(payload(event), ["duration_ms"]) || inferredDurationMS);
  const eventTime = formatClockTime(event.created_at);
  const installedSkill = objectValue(state.skill);
  const installedVersion = objectValue(state.version);
  const installedSkillKey = `${installedSkill.identifier}:${Number(installedVersion.version || 1)}`;
  const skillInstallAction = event.type === "runtime.tool_result" && data2.success !== false && data2.identifier === "skills" && data2.api_name === "install" && installedSkill.identifier && !enabledSkillKeys.has(installedSkillKey) ? {
    identifier: String(installedSkill.identifier),
    title: String(installedSkill.title || installedSkill.identifier),
    version: Number(installedVersion.version || 1),
    key: `${installedSkill.identifier}:${Number(installedVersion.version || 1)}`
  } : null;
  const enabledBinding = objectValue(state.binding);
  const enabledSkillKey = `${enabledBinding.skill}:${Number(enabledBinding.version || 1)}`;
  const enabledConfigVersion = Number(state.new_config_version || 0);
  const currentSessionConfigVersion = Number(sessionConfigVersion || 0);
  const enabledLifecycle = latestSkillLifecycle.get(String(enabledBinding.skill || ""));
  const enableIsLatest = !enabledLifecycle || Number(enabledLifecycle.seq || 0) === Number(event.seq || 0);
  const skillEnableAction = event.type === "runtime.tool_result" && data2.success !== false && data2.identifier === "skills" && data2.api_name === "enable" && enabledBinding.skill && enabledConfigVersion > 0 ? {
    identifier: String(enabledBinding.skill),
    version: Number(enabledBinding.version || 1),
    targetConfigVersion: enabledConfigVersion,
    operation: "enable",
    isLatest: enableIsLatest,
    status: !enableIsLatest ? "superseded" : activeSkillKeys.has(enabledSkillKey) || currentSessionConfigVersion === enabledConfigVersion ? "applied" : currentSessionConfigVersion > enabledConfigVersion ? "superseded" : "pending"
  } : null;
  const disabledBinding = objectValue(state.binding);
  const disabledConfigVersion = Number(state.new_config_version || 0);
  const disabledLifecycle = latestSkillLifecycle.get(String(disabledBinding.skill || ""));
  const disableIsLatest = !disabledLifecycle || Number(disabledLifecycle.seq || 0) === Number(event.seq || 0);
  const disabledStillActive = [...activeSkillKeys].some((key) => key.startsWith(`${disabledBinding.skill}:`));
  const skillDisableAction = event.type === "runtime.tool_result" && data2.success !== false && data2.identifier === "skills" && data2.api_name === "disable" && disabledBinding.skill && disabledConfigVersion > 0 ? {
    identifier: String(disabledBinding.skill),
    version: Number(disabledBinding.version || 0),
    targetConfigVersion: disabledConfigVersion,
    operation: "disable",
    removed: state.removed !== false,
    isLatest: disableIsLatest,
    status: !disableIsLatest ? "superseded" : currentSessionConfigVersion === disabledConfigVersion || currentSessionConfigVersion > disabledConfigVersion && !disabledStillActive ? "applied" : currentSessionConfigVersion > disabledConfigVersion ? "superseded" : "pending"
  } : null;
  const [expanded, setExpanded] = reactExports.useState(defaultExpanded);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: `process-card ${tone}${expanded ? " expanded" : ""}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "process-card-head", type: "button", "aria-expanded": expanded, onClick: () => setExpanded((value) => !value), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "process-card-chevron", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronIcon, {}) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "process-card-type-icon", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ProcessIcon, { type: event.type }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "process-card-title", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: title }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: metaLabel })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "process-card-status", children: [
        eventTime ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: eventTime }) : null,
        durationMS > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
          "耗时 ",
          formatDuration(durationMS)
        ] }) : null,
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: statusLabel }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `process-card-status-icon ${status}`, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ProcessStatusIcon, { status }) })
      ] })
    ] }),
    expanded ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "process-card-body", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "process-card-summary", children: [
        preview ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "process-card-preview", children: preview }) : null,
        data2.tool_source ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "process-card-source", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "来源" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `source-chip ${toolSource(data2.identifier, data2.tool_source, data2.manifest_type)}`, children: toolSourceLabel(toolSource(data2.identifier, data2.tool_source, data2.manifest_type)) })
        ] }) : null
      ] }),
      skillInstallAction ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "skill-install-action", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
            skillInstallAction.title,
            " v",
            skillInstallAction.version
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "已安装但尚未写入当前 Agent 配置。" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", disabled: skillEnableDisabled || skillEnableBusy === skillInstallAction.key, onClick: () => onRequestSkillEnable(skillInstallAction), children: skillEnableBusy === skillInstallAction.key ? "正在发起" : "请求启用" })
      ] }) : null,
      skillEnableAction ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `skill-install-action session-config-action ${skillEnableAction.status}`, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
            skillEnableAction.identifier,
            " v",
            skillEnableAction.version
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: skillEnableAction.status === "applied" ? `当前 Session 已使用 Agent 配置 v${currentSessionConfigVersion || skillEnableAction.targetConfigVersion}，Skill 已生效。` : skillEnableAction.status === "superseded" ? skillEnableAction.isLatest ? `当前 Session 已使用更高的 Agent 配置 v${currentSessionConfigVersion}，请在 Agent 设置中确认该 Skill 是否仍启用。` : "该启用结果已被后续 Skill 配置操作取代。" : `当前 Session 仍是 Agent 配置 v${currentSessionConfigVersion || "?"}，应用 v${skillEnableAction.targetConfigVersion} 后生效。` })
        ] }),
        skillEnableAction.status === "pending" ? /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            disabled: skillEnableDisabled || sessionConfigApplyBusy === skillEnableAction.targetConfigVersion,
            onClick: () => onApplySessionConfig(skillEnableAction),
            children: sessionConfigApplyBusy === skillEnableAction.targetConfigVersion ? "正在应用" : "应用到当前会话"
          }
        ) : skillEnableAction.status === "applied" && skillEnableAction.isLatest ? /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            className: "secondary danger",
            type: "button",
            disabled: skillEnableDisabled || skillDisableBusy === skillEnableAction.identifier,
            onClick: () => onRequestSkillDisable(skillEnableAction),
            children: skillDisableBusy === skillEnableAction.identifier ? "正在发起" : "请求停用"
          }
        ) : null
      ] }) : null,
      skillDisableAction ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `skill-install-action session-config-action ${skillDisableAction.status}`, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
            skillDisableAction.identifier,
            skillDisableAction.version > 0 ? ` v${skillDisableAction.version}` : ""
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: skillDisableAction.status === "applied" ? `当前 Session 已使用 Agent 配置 v${currentSessionConfigVersion || skillDisableAction.targetConfigVersion}，Skill 已停用但仍保留在 Registry。` : skillDisableAction.status === "superseded" ? skillDisableAction.isLatest ? `当前 Session 已使用更高的 Agent 配置 v${currentSessionConfigVersion}，请确认该 Skill 当前是否启用。` : "该停用结果已被后续 Skill 配置操作取代。" : `当前 Session 仍是 Agent 配置 v${currentSessionConfigVersion || "?"}，应用 v${skillDisableAction.targetConfigVersion} 后停用。` })
        ] }),
        skillDisableAction.status === "pending" ? /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            disabled: skillEnableDisabled || sessionConfigApplyBusy === skillDisableAction.targetConfigVersion,
            onClick: () => onApplySessionConfig(skillDisableAction),
            children: sessionConfigApplyBusy === skillDisableAction.targetConfigVersion ? "正在应用" : "应用到当前会话"
          }
        ) : skillDisableAction.status === "applied" && skillDisableAction.isLatest && skillDisableAction.version > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            className: "secondary",
            type: "button",
            disabled: skillEnableDisabled || skillEnableBusy === `${skillDisableAction.identifier}:${skillDisableAction.version}`,
            onClick: () => onRequestSkillEnable(skillDisableAction),
            children: skillEnableBusy === `${skillDisableAction.identifier}:${skillDisableAction.version}` ? "正在发起" : "重新启用"
          }
        ) : null
      ] }) : null,
      detailObject ? /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { className: "process-card-details", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("summary", { children: "查看执行详情" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { className: "process-card-detail", children: pretty(detailObject) })
      ] }) : null
    ] }) : null
  ] });
}
function MessageBody({ event, artifacts: artifacts2 = [], sessionID = "", onPreview = () => {
} }) {
  const text2 = eventText(event);
  if (event.type === "agent.message") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(AgentMessageBody, { artifacts: artifacts2, onPreview, sessionID, text: text2 });
  }
  const parts = [{ type: "text", text: text2 }];
  if (!parts.length || parts.length === 1 && parts[0].type === "text" && !parts[0].text) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "message-text", children: "（空消息）" });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "message-body", children: [
    parts.map((part, index2) => part.type === "tool" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ToolActionCard, { tool: part.tool }, `tool-${index2}`) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "message-text", children: part.text }, `text-${index2}`)),
    /* @__PURE__ */ jsxRuntimeExports.jsx(MessageArtifacts, { artifacts: artifacts2, sessionID, onPreview })
  ] });
}
function AgentMessageBody({ text: text2, artifacts: artifacts2 = [], sessionID = "", onPreview = () => {
}, streaming = false }) {
  const markdown = streaming ? String(text2 || "") : agentMessageWithArtifactLinks(cleanMessageText(text2), artifacts2.length > 0);
  if (!markdown) {
    return artifacts2.length ? /* @__PURE__ */ jsxRuntimeExports.jsx(MessageArtifacts, { artifacts: artifacts2, sessionID, onPreview }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "message-text", children: "（空消息）" });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "message-body", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(MarkdownMessage, { text: markdown, streaming }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(MessageArtifacts, { artifacts: artifacts2, sessionID, onPreview })
  ] });
}
function MarkdownMessage({ text: text2, streaming = false }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `message-text message-markdown${streaming ? " is-streaming" : ""}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      Markdown,
      {
        remarkPlugins: [remarkGfm],
        skipHtml: true,
        components: {
          a: ({ node: _node, ...props }) => /* @__PURE__ */ jsxRuntimeExports.jsx("a", { ...props, target: "_blank", rel: "noreferrer" })
        },
        children: String(text2 || "")
      }
    ),
    streaming ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": "true", className: "streaming-cursor" }) : null
  ] });
}
function hasVisibleAgentText(event) {
  return Boolean(cleanMessageText(eventText(event)));
}
function streamedAgentReply(events2) {
  var _a2;
  const ordered = [...events2 || []].sort((left, right) => Number(left.seq || 0) - Number(right.seq || 0));
  const lastFinalMessageSeq = ordered.reduce((maximum, event) => event.type === "agent.message" ? Math.max(maximum, Number(event.seq || 0)) : maximum, 0);
  const requestEvent = [...ordered].reverse().find((event) => event.type === "runtime.llm_request" && Number(event.seq || 0) > lastFinalMessageSeq);
  if (!requestEvent) return null;
  const requestSeq = Number(requestEvent.seq || 0);
  const stopped = ordered.some((event) => Number(event.seq || 0) > requestSeq && ["agent.message", "runtime.progress_message", "runtime.tool_call", "runtime.tool_intervention_required", "runtime.failed"].includes(event.type));
  if (stopped) return null;
  const chunkEvents = ordered.filter((event) => event.type === "runtime.llm_chunk" && eventData(event).type === "text" && Number(event.seq || 0) > requestSeq);
  const deltaEvents = chunkEvents.length ? chunkEvents : ordered.filter((event) => event.type === "runtime.llm_delta" && Number(event.seq || 0) > requestSeq);
  const text2 = deltaEvents.map((event) => String(eventData(event).text || "")).join("");
  if (!text2) return null;
  return {
    createdAt: ((_a2 = deltaEvents[0]) == null ? void 0 : _a2.created_at) || requestEvent.created_at,
    turnID: String(payload(requestEvent).turn_id || payload(deltaEvents[0]).turn_id || ""),
    text: text2
  };
}
function mergeEvents(currentEvents, nextEvents) {
  const merged = /* @__PURE__ */ new Map();
  [...currentEvents || [], ...nextEvents || []].forEach((event) => {
    merged.set(`${event.seq}-${event.type}`, event);
  });
  return [...merged.values()].sort((left, right) => Number(left.seq || 0) - Number(right.seq || 0));
}
function sessionStatusRank(status) {
  switch (String(status || "")) {
    case "running":
      return 0;
    case "interrupting":
      return 1;
    case "idle":
      return 2;
    case "failed":
      return 3;
    case "terminated":
      return 4;
    default:
      return 5;
  }
}
function sessionStatusFromEvent(event) {
  switch (event == null ? void 0 : event.type) {
    case "session.status_provisioning":
    case "session.status_idle":
    case "session.status_running":
    case "session.status_interrupting":
    case "session.status_compacting":
    case "session.status_failed":
    case "session.status_terminated":
      return String(payload(event).status || event.type.replace("session.status_", "") || "");
    default:
      return "";
  }
}
function latestSessionStatus(events2, fallback = "") {
  const reverseEvents = [...events2 || []].reverse();
  for (const event of reverseEvents) {
    const status = sessionStatusFromEvent(event);
    if (status) return status;
  }
  return String(fallback || "");
}
function latestIdleTurnStatus(events2) {
  const idleEvent = [...events2 || []].reverse().find((event) => event.type === "session.status_idle");
  return String(payload(idleEvent).last_turn_status || "");
}
function parseSessionRuntimeSettings(raw) {
  if (!raw || typeof raw !== "object") return {};
  return {
    interventionMode: typeof raw.intervention_mode === "string" ? raw.intervention_mode : "",
    llmModel: typeof raw.llm_model === "string" ? raw.llm_model : "",
    llmProvider: typeof raw.llm_provider === "string" ? raw.llm_provider : "",
    toolRuntime: typeof raw.tool_runtime === "string" ? raw.tool_runtime : ""
  };
}
function rememberedSessionID() {
  try {
    return window.localStorage.getItem(activeSessionStorageKey) || "";
  } catch {
    return "";
  }
}
function rememberSession(sessionID) {
  try {
    window.localStorage.setItem(activeSessionStorageKey, sessionID);
  } catch {
  }
}
function forgetSession() {
  try {
    window.localStorage.removeItem(activeSessionStorageKey);
  } catch {
  }
}
function WorkbenchApp() {
  var _a2, _b, _c;
  const [status, setStatus] = reactExports.useState("ready");
  const [principal, setPrincipal] = reactExports.useState(null);
  const [agentID, setAgentID] = reactExports.useState("");
  const [environmentID, setEnvironmentID] = reactExports.useState("");
  const [sessionID, setSessionID] = reactExports.useState("");
  const [task, setTask] = reactExports.useState("");
  const [composerFiles, setComposerFiles] = reactExports.useState([]);
  const [composerDragActive, setComposerDragActive] = reactExports.useState(false);
  const [uploadingFiles, setUploadingFiles] = reactExports.useState(false);
  const [taskSearch, setTaskSearch] = reactExports.useState("");
  const [eventsResponse, setEventsResponse] = reactExports.useState({ events: [] });
  const [interventionResponse, setInterventionResponse] = reactExports.useState({ interventions: [] });
  const [artifactResponse, setArtifactResponse] = reactExports.useState({ artifacts: [] });
  const [sessionMeta, setSessionMeta] = reactExports.useState(null);
  const [waitingForReply, setWaitingForReply] = reactExports.useState(false);
  const [recentSessions, setRecentSessions] = reactExports.useState([]);
  const [decidingApprovalID, setDecidingApprovalID] = reactExports.useState("");
  const [sessionAction, setSessionAction] = reactExports.useState("");
  const [artifactPreview, setArtifactPreview] = reactExports.useState(null);
  const [artifactPreviewMode, setArtifactPreviewMode] = reactExports.useState("preview");
  const [artifactPreviewWidth, setArtifactPreviewWidth] = reactExports.useState(480);
  const [runtimeConfig, setRuntimeConfig] = reactExports.useState(null);
  const [runtimeCapabilities, setRuntimeCapabilities] = reactExports.useState({ default_runtime: "cloud_sandbox", available_runtimes: ["cloud_sandbox"] });
  const [modelOptions, setModelOptions] = reactExports.useState([]);
  const [defaultAgentConfig, setDefaultAgentConfig] = reactExports.useState(null);
  const [availableAgents, setAvailableAgents] = reactExports.useState([]);
  const [taskHoverPreview, setTaskHoverPreview] = reactExports.useState(null);
  const [settingsDraft, setSettingsDraft] = reactExports.useState({
    interventionMode: "request_approval",
    llmModel: "",
    llmProvider: "",
    toolRuntime: "cloud_sandbox"
  });
  const [installedSkills, setInstalledSkills] = reactExports.useState([]);
  const [toolPickerOpen, setToolPickerOpen] = reactExports.useState(false);
  const [toolingLoading, setToolingLoading] = reactExports.useState(false);
  const [toolingError, setToolingError] = reactExports.useState("");
  const [selectedGuidanceKeys, setSelectedGuidanceKeys] = reactExports.useState([]);
  const [settingsOpen, setSettingsOpen] = reactExports.useState(false);
  const [settingsSection, setSettingsSection] = reactExports.useState("skills");
  const [settingsSearch, setSettingsSearch] = reactExports.useState("");
  const [archivedSessions, setArchivedSessions] = reactExports.useState([]);
  const [savingSettings, setSavingSettings] = reactExports.useState(false);
  const [savingAgentConfig, setSavingAgentConfig] = reactExports.useState(false);
  const [rollingBackAgentVersion, setRollingBackAgentVersion] = reactExports.useState(0);
  const [approvalsOpen, setApprovalsOpen] = reactExports.useState(false);
  const [visibleTaskCount, setVisibleTaskCount] = reactExports.useState(10);
  const [rightPanelTab, setRightPanelTab] = reactExports.useState("results");
  const [taskTemplates$1, setTaskTemplates] = reactExports.useState([]);
  const [templatePickerOpen, setTemplatePickerOpen] = reactExports.useState(false);
  const [selectedTaskTemplateID, setSelectedTaskTemplateID] = reactExports.useState("");
  const [workflowMode, setWorkflowMode] = reactExports.useState(false);
  const [workflowRun, setWorkflowRun] = reactExports.useState(null);
  const [comparisonOpen, setComparisonOpen] = reactExports.useState(false);
  const [comparisonLeftID, setComparisonLeftID] = reactExports.useState("");
  const [comparisonRightID, setComparisonRightID] = reactExports.useState("");
  const [comparisonResult, setComparisonResult] = reactExports.useState(null);
  const [comparisonLoading, setComparisonLoading] = reactExports.useState(false);
  const [comparisonVariantModel, setComparisonVariantModel] = reactExports.useState("");
  const [taskMenuSessionID, setTaskMenuSessionID] = reactExports.useState("");
  const [taskMenuPosition, setTaskMenuPosition] = reactExports.useState(null);
  const [metadataSession, setMetadataSession] = reactExports.useState(null);
  const [metadataTagsDraft, setMetadataTagsDraft] = reactExports.useState("");
  const [savingMetadata, setSavingMetadata] = reactExports.useState(false);
  const [requestingSkillEnable, setRequestingSkillEnable] = reactExports.useState("");
  const [requestingSkillDisable, setRequestingSkillDisable] = reactExports.useState("");
  const [applyingSessionConfigVersion, setApplyingSessionConfigVersion] = reactExports.useState(0);
  const [pluginRoutePath, setPluginRoutePath] = reactExports.useState(pluginPathFromHash);
  const [pluginNavigation, setPluginNavigation] = reactExports.useState([]);
  const [pluginRoutes, setPluginRoutes] = reactExports.useState([]);
  const [pluginLoadState, setPluginLoadState] = reactExports.useState("loading");
  const eventStreamCursorRef = reactExports.useRef(0);
  const sessionSyncTimerRef = reactExports.useRef(null);
  const artifactResizeRef = reactExports.useRef(null);
  const threadRef = reactExports.useRef(null);
  const shouldAutoScrollRef = reactExports.useRef(true);
  const pendingApprovalCountRef = reactExports.useRef(0);
  const approvalDecisionRef = reactExports.useRef("");
  const scrollFrameRef = reactExports.useRef(0);
  const workflowAdvancingRef = reactExports.useRef(false);
  const composerFileInputRef = reactExports.useRef(null);
  const taskMenuButtonRefs = reactExports.useRef(/* @__PURE__ */ new Map());
  reactExports.useEffect(() => {
    let active = true;
    currentPrincipal().then((response) => {
      if (active) setPrincipal(response.principal || null);
    }).catch((error) => {
      if (active) setStatus(error.message);
    });
    return () => {
      active = false;
    };
  }, []);
  async function logout() {
    const response = await fetch("/auth/logout", { method: "POST" });
    const payload2 = await response.json().catch(() => ({}));
    if (!response.ok) throw new Error(payload2.error || `HTTP ${response.status}`);
    window.location.assign(payload2.redirect_url || "/app");
  }
  reactExports.useEffect(() => {
    const syncPluginPath = () => setPluginRoutePath(pluginPathFromHash());
    window.addEventListener("hashchange", syncPluginPath);
    window.addEventListener("popstate", syncPluginPath);
    return () => {
      window.removeEventListener("hashchange", syncPluginPath);
      window.removeEventListener("popstate", syncPluginPath);
    };
  }, []);
  reactExports.useEffect(() => {
    if (!taskMenuSessionID) return void 0;
    const closeTaskMenu = (event) => {
      const target = event.target;
      if (target instanceof Element && target.closest(".task-action-menu, .task-menu-button")) return;
      setTaskMenuSessionID("");
      setTaskMenuPosition(null);
    };
    const closeTaskMenuOnViewportChange = () => {
      setTaskMenuSessionID("");
      setTaskMenuPosition(null);
    };
    document.addEventListener("pointerdown", closeTaskMenu);
    window.addEventListener("resize", closeTaskMenuOnViewportChange);
    window.addEventListener("scroll", closeTaskMenuOnViewportChange, true);
    return () => {
      document.removeEventListener("pointerdown", closeTaskMenu);
      window.removeEventListener("resize", closeTaskMenuOnViewportChange);
      window.removeEventListener("scroll", closeTaskMenuOnViewportChange, true);
    };
  }, [taskMenuSessionID]);
  reactExports.useEffect(() => {
    document.title = "TMA Workbench";
    let active = true;
    async function restoreRecentSession() {
      setStatus("loading history");
      const defaultsPromise = loadPreSessionDefaults();
      const response = await sessions({ limit: 30 });
      if (!active) return;
      const sessions$1 = response.sessions || [];
      setRecentSessions(sessions$1);
      const remembered = rememberedSessionID();
      const selected = sessions$1.find((session2) => session2.id === remembered) || sessions$1[0];
      if (!selected) {
        await defaultsPromise;
        setStatus("ready");
        return;
      }
      setSessionID(selected.id);
      setAgentID(selected.agent_id || "");
      setEnvironmentID(selected.environment_id || "");
      rememberSession(selected.id);
      await loadSession(selected.id);
      await defaultsPromise;
      if (active) setStatus("history restored");
    }
    restoreRecentSession().catch((error) => {
      if (active) setStatus(error.message);
    });
    return () => {
      active = false;
    };
  }, []);
  reactExports.useEffect(() => {
    let active = true;
    taskTemplates().then((response) => {
      if (active) setTaskTemplates(response.templates || []);
    }).catch((error) => {
      if (active) setStatus(error.message);
    });
    return () => {
      active = false;
    };
  }, []);
  reactExports.useEffect(() => {
    if (!sessionID) {
      setWorkflowRun(null);
      return;
    }
    const restored = readWorkflowRun(sessionID);
    setWorkflowRun((restored == null ? void 0 : restored.sessionID) === sessionID ? restored : null);
  }, [sessionID]);
  async function loadPreSessionDefaults() {
    const [defaultAgent$1, agentsResponse, providersResponse] = await Promise.all([
      defaultAgent(),
      agents(),
      llmProviders()
    ]);
    const sortedAgents = [...agentsResponse.agents || []].sort((left, right) => {
      if (left.id === defaultAgent$1.id) return -1;
      if (right.id === defaultAgent$1.id) return 1;
      return String(left.name || "").localeCompare(String(right.name || ""));
    });
    const enabledProviders = (providersResponse.providers || []).filter((provider) => provider.enabled !== false);
    const modelResponses = await Promise.all(enabledProviders.map((provider) => llmModels(provider.id).catch(() => ({ models: [] }))));
    const options = enabledProviders.flatMap((provider, index2) => (modelResponses[index2].models || []).map((model) => ({
      label: `${provider.id} / ${model.model}`,
      llmModel: model.model,
      llmProvider: provider.id,
      capabilityType: model.capability_type || "text",
      isDefaultVision: Boolean(model.is_default_vision)
    })));
    setDefaultAgentConfig(defaultAgent$1);
    setAvailableAgents(sortedAgents);
    setAgentID((current) => current || defaultAgent$1.id);
    setModelOptions(options);
    const skillsResponse = await skills({ workspaceId: defaultAgent$1.workspace_id }).catch(() => ({ skills: [] }));
    setInstalledSkills(skillsResponse.skills || []);
    setSettingsDraft((current) => {
      var _a3, _b2;
      return {
        ...current,
        llmModel: current.llmModel || ((_a3 = defaultAgent$1.config_version) == null ? void 0 : _a3.llm_model) || "",
        llmProvider: current.llmProvider || ((_b2 = defaultAgent$1.config_version) == null ? void 0 : _b2.llm_provider) || "",
        toolRuntime: current.toolRuntime || "cloud_sandbox"
      };
    });
  }
  const events$1 = eventsResponse.events || [];
  const toolCallLifecycles = reactExports.useMemo(() => buildToolCallLifecycles(events$1), [events$1]);
  const conversationEvents = reactExports.useMemo(() => events$1.filter((event) => event.type === "user.message" || event.type === "agent.message").sort((left, right) => Number(left.seq || 0) - Number(right.seq || 0)), [events$1]);
  const chatTimelineEvents = reactExports.useMemo(
    () => mergeReasoningChunks([...events$1].sort((left, right) => Number(left.seq || 0) - Number(right.seq || 0))).filter((event) => {
      if (event.type === "user.message") return true;
      if (event.type === "agent.message") return hasVisibleAgentText(event);
      return [
        "runtime.thinking",
        "runtime.llm_chunk",
        "runtime.progress_message",
        "runtime.tool_call",
        "runtime.tool_result",
        "runtime.tool_intervention_required",
        "runtime.tool_intervention_rejected",
        "runtime.failed"
      ].includes(event.type) && (event.type !== "runtime.llm_chunk" || isReasoningChunk(event));
    }),
    [events$1]
  );
  const latestSuccessfulSkillInstallSeq = reactExports.useMemo(() => {
    const event = [...events$1].reverse().find((item) => {
      const data2 = eventData(item);
      return item.type === "runtime.tool_result" && data2.success !== false && data2.identifier === "skills" && data2.api_name === "install";
    });
    return Number((event == null ? void 0 : event.seq) || 0);
  }, [events$1]);
  const enabledSkillKeys = reactExports.useMemo(() => new Set(events$1.flatMap((event) => {
    const data2 = eventData(event);
    const state = objectValue(data2.state);
    const binding = objectValue(state.binding);
    if (event.type !== "runtime.tool_result" || data2.success === false || data2.identifier !== "skills" || data2.api_name !== "enable" || !binding.skill) return [];
    return [`${binding.skill}:${Number(binding.version || 1)}`];
  })), [events$1]);
  const latestSkillLifecycle = reactExports.useMemo(() => {
    const lifecycle = /* @__PURE__ */ new Map();
    for (const event of events$1) {
      const data2 = eventData(event);
      const state = objectValue(data2.state);
      const binding = objectValue(state.binding);
      if (event.type !== "runtime.tool_result" || data2.success === false || data2.identifier !== "skills" || !["enable", "disable"].includes(data2.api_name) || !binding.skill) continue;
      const current = lifecycle.get(String(binding.skill));
      if (!current || Number(event.seq || 0) >= Number(current.seq || 0)) {
        lifecycle.set(String(binding.skill), { apiName: data2.api_name, seq: Number(event.seq || 0) });
      }
    }
    return lifecycle;
  }, [events$1]);
  const activeSkillKeys = reactExports.useMemo(() => new Set(parseSkillsConfig(runtimeConfig == null ? void 0 : runtimeConfig.skills).enabled.map((binding) => `${binding.skill}:${Number(binding.version || 1)}`)), [runtimeConfig == null ? void 0 : runtimeConfig.skills]);
  const streamingReply = reactExports.useMemo(() => streamedAgentReply(events$1), [events$1]);
  const renderedChatTimelineEvents = reactExports.useMemo(() => {
    if (!streamingReply) return chatTimelineEvents;
    return [...chatTimelineEvents, {
      type: "agent.streaming",
      seq: `stream-${streamingReply.turnID || "current"}`,
      created_at: streamingReply.createdAt,
      payload: {
        turn_id: streamingReply.turnID,
        content_format: "markdown",
        content: [{ type: "text", text: streamingReply.text }]
      }
    }];
  }, [chatTimelineEvents, streamingReply]);
  const interventions$1 = interventionResponse.interventions || [];
  const artifacts$1 = artifactResponse.artifacts || [];
  const resultFiles = reactExports.useMemo(() => conversationFinalFileArtifacts(artifacts$1, conversationEvents), [artifacts$1, conversationEvents]);
  const artifactTree = reactExports.useMemo(() => buildArtifactTree(resultFiles), [resultFiles]);
  const lastUserSeq = reactExports.useMemo(() => {
    return events$1.reduce((maximum, event) => event.type === "user.message" ? Math.max(maximum, Number(event.seq || 0)) : maximum, 0);
  }, [events$1]);
  const effectiveSessionStatus = reactExports.useMemo(() => latestSessionStatus(events$1, sessionMeta == null ? void 0 : sessionMeta.status), [events$1, sessionMeta == null ? void 0 : sessionMeta.status]);
  const lastIdleTurnStatus = reactExports.useMemo(() => latestIdleTurnStatus(events$1), [events$1]);
  const liveSignal = reactExports.useMemo(() => turnSignal(events$1, {
    sinceSeq: lastUserSeq,
    sessionStatus: effectiveSessionStatus,
    waitingForReply,
    interventions: interventions$1,
    includeSuccess: waitingForReply
  }), [events$1, interventions$1, lastUserSeq, effectiveSessionStatus, waitingForReply]);
  const runState = reactExports.useMemo(() => {
    if (interventions$1.length) return "waiting approval";
    if (effectiveSessionStatus) return effectiveSessionStatus;
    return sessionID ? "active" : "not started";
  }, [interventions$1.length, effectiveSessionStatus, sessionID]);
  const hasPendingApprovals = interventions$1.length > 0;
  const activityEvents = reactExports.useMemo(() => {
    return compactActivityEvents(events$1);
  }, [events$1]);
  const filteredTaskSessions = reactExports.useMemo(() => {
    const query = taskSearch.trim().toLowerCase();
    const activeAgentID = String(agentID || (defaultAgentConfig == null ? void 0 : defaultAgentConfig.id) || "").trim();
    const matches = recentSessions.filter((session2) => {
      if (activeAgentID && session2.agent_id !== activeAgentID) return false;
      if (!query) return true;
      return [session2.title, session2.id, session2.summary_text, ...session2.tags || []].some((value) => String(value || "").toLowerCase().includes(query));
    });
    return [...matches].sort((left, right) => {
      if (Boolean(left.pinned_at) !== Boolean(right.pinned_at)) return left.pinned_at ? -1 : 1;
      if (left.pinned_at && right.pinned_at) {
        const pinOrder = new Date(right.pinned_at).getTime() - new Date(left.pinned_at).getTime();
        if (pinOrder !== 0) return pinOrder;
      }
      if (left.id === sessionID) return -1;
      if (right.id === sessionID) return 1;
      const statusRank = sessionStatusRank(left.status) - sessionStatusRank(right.status);
      if (statusRank !== 0) return statusRank;
      return new Date(right.created_at || 0).getTime() - new Date(left.created_at || 0).getTime();
    });
  }, [recentSessions, agentID, defaultAgentConfig == null ? void 0 : defaultAgentConfig.id, sessionID, taskSearch]);
  const visibleTaskSessions = reactExports.useMemo(() => {
    return filteredTaskSessions.slice(0, visibleTaskCount);
  }, [filteredTaskSessions, visibleTaskCount]);
  const hasMoreTasks = filteredTaskSessions.length > visibleTaskCount;
  const runtimeOptions = reactExports.useMemo(() => {
    const available = new Set(runtimeCapabilities.available_runtimes || ["cloud_sandbox"]);
    const options = [{ value: "cloud_sandbox", label: "Sandbox" }];
    if (available.has("local_system") || settingsDraft.toolRuntime === "local_system") {
      options.push({ value: "local_system", label: "Local" });
    }
    return options;
  }, [runtimeCapabilities.available_runtimes, settingsDraft.toolRuntime]);
  const selectedModelValue = settingsDraft.llmProvider && settingsDraft.llmModel ? `${settingsDraft.llmProvider}::${settingsDraft.llmModel}` : "";
  const selectedAgentValue = agentID || (defaultAgentConfig == null ? void 0 : defaultAgentConfig.id) || "";
  const selectedAgent = availableAgents.find((agent2) => agent2.id === selectedAgentValue) || defaultAgentConfig;
  const selectedTaskTemplate = taskTemplates$1.find((template) => template.id === selectedTaskTemplateID) || null;
  const toolingConfig = sessionID ? runtimeConfig : (selectedAgent == null ? void 0 : selectedAgent.config_version) || null;
  const toolingWorkspaceID = String((sessionMeta == null ? void 0 : sessionMeta.workspace_id) || (runtimeConfig == null ? void 0 : runtimeConfig.workspace_id) || (selectedAgent == null ? void 0 : selectedAgent.workspace_id) || (defaultAgentConfig == null ? void 0 : defaultAgentConfig.workspace_id) || "").trim();
  const pluginScope = reactExports.useMemo(() => ({
    organizationId: String((principal == null ? void 0 : principal.organization_id) || (sessionMeta == null ? void 0 : sessionMeta.organization_id) || "").trim() || void 0,
    workspaceId: String((principal == null ? void 0 : principal.workspace_id) || toolingWorkspaceID || "wksp_default").trim(),
    userId: String((principal == null ? void 0 : principal.owner_id) || (sessionMeta == null ? void 0 : sessionMeta.owner_id) || "system").trim() || "system",
    roles: Array.isArray(principal == null ? void 0 : principal.roles) && principal.roles.length ? principal.roles : ["member"]
  }), [principal, sessionMeta == null ? void 0 : sessionMeta.organization_id, sessionMeta == null ? void 0 : sessionMeta.owner_id, toolingWorkspaceID]);
  const pluginRuntime = reactExports.useMemo(() => createStaticPluginRegistry({
    workbenchAPIVersion: "1.0.0",
    designSystemVersion: "1.0.0",
    surface: workbenchSurface(),
    navigationGroups: ["workspace"],
    scope: pluginScope,
    services: {
      permissions: workbenchHostPermissionService,
      dialog: workbenchDialogService,
      notifications: workbenchNotificationService,
      resources: workbenchRelatedResourceService,
      tasks: workbenchTaskService,
      artifacts: workbenchArtifactService,
      http: workbenchScopedHTTPService
    }
  }), [pluginScope]);
  reactExports.useEffect(() => {
    let active = true;
    setPluginLoadState("loading");
    setPluginNavigation([]);
    setPluginRoutes([]);
    const unsubscribeNavigation = pluginRuntime.navigation.subscribe((items) => {
      if (active) setPluginNavigation(items);
    });
    const unsubscribeRoutes = pluginRuntime.routes.subscribe((items) => {
      if (active) setPluginRoutes(items);
    });
    loadStaticPluginCatalog(pluginRuntime).then((records) => {
      if (!active) return;
      const failed = records.filter((record) => record.status === "failed");
      setPluginLoadState(failed.length ? "partial" : "ready");
      failed.forEach((record) => workbenchNotificationService.show({
        level: "error",
        title: `${record.name} 加载失败`,
        message: record.error,
        dedupeKey: `plugin.load.${record.id}`
      }));
    }).catch((error) => {
      if (!active) return;
      setPluginLoadState("failed");
      workbenchNotificationService.show({
        level: "error",
        title: "扩展加载失败",
        message: error.message || String(error),
        dedupeKey: "plugin.catalog.load"
      });
    });
    return () => {
      active = false;
      unsubscribeNavigation();
      unsubscribeRoutes();
      pluginRuntime.list().forEach((record) => {
        pluginRuntime.unregisterPackage(record.id).catch(() => {
        });
      });
    };
  }, [pluginRuntime]);
  const activePluginRoute = reactExports.useMemo(() => pluginRoutes.find((route) => route.path === pluginRoutePath) || null, [pluginRoutePath, pluginRoutes]);
  const toolingCatalog = reactExports.useMemo(() => buildToolingCatalog({
    config: toolingConfig,
    installedSkills,
    preferredRuntime: settingsDraft.toolRuntime || "cloud_sandbox"
  }), [toolingConfig, installedSkills, settingsDraft.toolRuntime]);
  const selectableToolingItems = reactExports.useMemo(() => toolingCatalog.items.filter((item) => item.selectable), [toolingCatalog.items]);
  const selectedGuidanceItems = reactExports.useMemo(() => {
    const byKey = new Map(toolingCatalog.items.map((item) => [item.key, item]));
    return selectedGuidanceKeys.map((key) => byKey.get(key)).filter(Boolean);
  }, [selectedGuidanceKeys, toolingCatalog.items]);
  const settingsSections = reactExports.useMemo(() => {
    const sections = [
      { key: "environment", title: "环境变量", description: "工具与 Skills 认证配置", keywords: "environment env secret key token credential 环境变量 密钥 认证" },
      { key: "models", title: "模型", description: "Provider、凭证与模型目录", keywords: "model provider llm api 模型 服务商 上下文" },
      { key: "skills", title: "Skills", description: "技能安装、启用与状态", keywords: "skills 技能 install enable disable 停用" },
      { key: "mcp", title: "MCP", description: "MCP 服务与可用性", keywords: "mcp server tool" },
      { key: "agent", title: "Agent", description: "智能体列表与当前配置", keywords: "agent 智能体 model config" },
      { key: "work", title: "Work", description: "任务与会话状态", keywords: "work task session" },
      { key: "inspector", title: "Inspector", description: "调试与观察入口", keywords: "inspector trace debug" }
    ];
    const query = settingsSearch.trim().toLowerCase();
    if (!query) return sections;
    return sections.filter((section) => section.title.toLowerCase().includes(query) || section.description.toLowerCase().includes(query) || section.keywords.toLowerCase().includes(query));
  }, [settingsSearch]);
  const settingsAgents = reactExports.useMemo(() => {
    const base = [...availableAgents];
    return base.sort((left, right) => {
      if (left.id === (selectedAgent == null ? void 0 : selectedAgent.id)) return -1;
      if (right.id === (selectedAgent == null ? void 0 : selectedAgent.id)) return 1;
      return String(left.name || "").localeCompare(String(right.name || ""));
    });
  }, [availableAgents, selectedAgent == null ? void 0 : selectedAgent.id]);
  async function loadSession(value) {
    const [nextSession, nextEvents, nextInterventions, nextArtifacts] = await Promise.all([
      session(value).catch((error) => ({ error: String(error), id: value })),
      events(value).catch((error) => ({ events: [], error: String(error) })),
      interventions(value, "pending").catch((error) => ({ interventions: [], error: String(error) })),
      artifacts(value).catch((error) => ({ artifacts: [], error: String(error) }))
    ]);
    setSessionMeta(nextSession);
    if (!nextSession.error) {
      setAgentID(nextSession.agent_id || "");
      setEnvironmentID(nextSession.environment_id || "");
    }
    eventStreamCursorRef.current = maxSeq(nextEvents.events || []);
    setEventsResponse(nextEvents);
    setInterventionResponse(nextInterventions);
    setArtifactResponse(nextArtifacts);
    if (nextSession == null ? void 0 : nextSession.id) {
      setRecentSessions((current) => [nextSession, ...current.filter((item) => item.id !== nextSession.id)]);
    }
    return {
      session: nextSession,
      events: nextEvents.events || [],
      interventions: nextInterventions.interventions || []
    };
  }
  async function loadSessionSettings(value, sessionValue = sessionMeta) {
    const nextSessionID = String(value || "").trim();
    if (!nextSessionID) {
      setRuntimeConfig(null);
      setModelOptions([]);
      setRuntimeCapabilities({ default_runtime: "cloud_sandbox", available_runtimes: ["cloud_sandbox"] });
      setSettingsDraft({
        interventionMode: "request_approval",
        llmModel: "",
        llmProvider: "",
        toolRuntime: "cloud_sandbox"
      });
      return;
    }
    const [config, capabilities, providersResponse] = await Promise.all([
      sessionRuntimeConfig(nextSessionID),
      sessionRuntimeCapabilities(nextSessionID),
      llmProviders()
    ]);
    const enabledProviders = (providersResponse.providers || []).filter((provider) => provider.enabled !== false);
    const modelResponses = await Promise.all(enabledProviders.map((provider) => llmModels(provider.id).catch(() => ({ models: [] }))));
    const options = enabledProviders.flatMap((provider, index2) => (modelResponses[index2].models || []).map((model) => ({
      label: `${provider.id} / ${model.model}`,
      llmModel: model.model,
      llmProvider: provider.id,
      capabilityType: model.capability_type || "text",
      isDefaultVision: Boolean(model.is_default_vision)
    })));
    setRuntimeConfig(config);
    setRuntimeCapabilities(capabilities);
    setModelOptions(options);
    const parsedSettings = parseSessionRuntimeSettings((sessionValue == null ? void 0 : sessionValue.runtime_settings) || {});
    const preferredRuntime = parsedSettings.toolRuntime || capabilities.default_runtime || "cloud_sandbox";
    setSettingsDraft({
      interventionMode: parsedSettings.interventionMode || "request_approval",
      llmModel: parsedSettings.llmModel || config.llm_model || "",
      llmProvider: parsedSettings.llmProvider || config.llm_provider || "",
      toolRuntime: preferredRuntime === "auto" ? "cloud_sandbox" : preferredRuntime
    });
  }
  async function refreshModelOptions() {
    const [providersResponse, modelsResponse] = await Promise.all([llmProviders(), llmModels()]);
    const enabledProviderIDs = new Set((providersResponse.providers || []).filter((provider) => provider.enabled !== false).map((provider) => provider.id));
    setModelOptions((modelsResponse.models || []).filter((model) => enabledProviderIDs.has(model.provider_id)).map((model) => ({
      label: `${model.provider_id} / ${model.model}`,
      llmModel: model.model,
      llmProvider: model.provider_id,
      capabilityType: model.capability_type || "text",
      isDefaultVision: Boolean(model.is_default_vision)
    })));
  }
  async function syncSession(value = sessionID) {
    const sessionValue = String(value || "").trim();
    if (!sessionValue) return null;
    const [nextSession, nextInterventions, nextArtifacts] = await Promise.all([
      session(sessionValue).catch((error) => ({ error: String(error), id: sessionValue })),
      interventions(sessionValue, "pending").catch((error) => ({ interventions: [], error: String(error) })),
      artifacts(sessionValue).catch((error) => ({ artifacts: [], error: String(error) }))
    ]);
    setSessionMeta(nextSession);
    if (!nextSession.error) {
      setAgentID(nextSession.agent_id || "");
      setEnvironmentID(nextSession.environment_id || "");
      setRecentSessions((current) => [nextSession, ...current.filter((item) => item.id !== nextSession.id)]);
    }
    setInterventionResponse(nextInterventions);
    setArtifactResponse(nextArtifacts);
    return {
      session: nextSession,
      interventions: nextInterventions.interventions || []
    };
  }
  reactExports.useEffect(() => {
    const currentSessionID = String(sessionID || "").trim();
    if (!currentSessionID || (sessionMeta == null ? void 0 : sessionMeta.id) !== currentSessionID || (sessionMeta == null ? void 0 : sessionMeta.error)) return void 0;
    const afterSeq = Number(eventStreamCursorRef.current || 0);
    const controller = new AbortController();
    let active = true;
    function queueSessionSync() {
      if (sessionSyncTimerRef.current) {
        window.clearTimeout(sessionSyncTimerRef.current);
      }
      sessionSyncTimerRef.current = window.setTimeout(() => {
        sessionSyncTimerRef.current = null;
        syncSession(currentSessionID).catch((error) => {
          if (active) setStatus(error.message);
        });
      }, 150);
    }
    function applyStreamEvent(event) {
      if (!active || !event) return;
      eventStreamCursorRef.current = Math.max(eventStreamCursorRef.current || 0, Number(event.seq || 0));
      setEventsResponse((current) => ({
        ...current,
        events: mergeEvents(current.events, [event])
      }));
      const streamedStatus = sessionStatusFromEvent(event);
      if (streamedStatus) {
        mergeSessionStatus(currentSessionID, streamedStatus);
      }
      if (sessionSyncEventTypes.has(event.type)) {
        queueSessionSync();
      }
    }
    async function consumeEvents() {
      try {
        for await (const event of streamSessionEvents(currentSessionID, {
          afterSeq,
          signal: controller.signal
        })) {
          applyStreamEvent(event);
        }
      } catch (error) {
        if (active && (error == null ? void 0 : error.name) !== "AbortError") setStatus(error.message);
      }
    }
    consumeEvents();
    return () => {
      active = false;
      controller.abort();
      if (sessionSyncTimerRef.current) {
        window.clearTimeout(sessionSyncTimerRef.current);
        sessionSyncTimerRef.current = null;
      }
    };
  }, [sessionID, sessionMeta == null ? void 0 : sessionMeta.id]);
  reactExports.useEffect(() => {
    if (!waitingForReply) return;
    if (effectiveSessionStatus === "interrupting") {
      setStatus("interrupting");
      return;
    }
    if (!liveSignal) {
      if (effectiveSessionStatus === "failed") {
        setStatus("reply failed");
        setWaitingForReply(false);
        return;
      }
      if (effectiveSessionStatus === "terminated") {
        setStatus("archived");
        setWaitingForReply(false);
        return;
      }
      if (effectiveSessionStatus === "idle") {
        if (lastIdleTurnStatus === "failed") {
          setStatus("reply failed");
        } else if (lastIdleTurnStatus === "interrupted") {
          setStatus("interrupted");
        } else {
          setStatus("idle");
        }
        setWaitingForReply(false);
        return;
      }
      setWaitingForReply(false);
      setStatus(effectiveSessionStatus || "ready");
      return;
    }
    if (liveSignal.kind === "thinking") {
      setStatus(effectiveSessionStatus === "interrupting" ? "interrupting" : "thinking");
      return;
    }
    if (liveSignal.kind === "approval") {
      setStatus("waiting approval");
      setWaitingForReply(false);
      return;
    }
    if (liveSignal.kind === "success") {
      setStatus("replied");
      setWaitingForReply(false);
      return;
    }
    if (liveSignal.kind === "error") {
      setStatus(`reply failed: ${liveSignal.detail}`);
      setWaitingForReply(false);
    }
  }, [waitingForReply, liveSignal, effectiveSessionStatus, lastIdleTurnStatus]);
  reactExports.useEffect(() => {
    loadSessionSettings(sessionID, sessionMeta).catch((error) => setStatus(error.message));
  }, [sessionID, sessionMeta]);
  reactExports.useEffect(() => {
    if (!toolPickerOpen && !settingsOpen) return;
    let active = true;
    setToolingLoading(true);
    setToolingError("");
    skills({ workspaceId: toolingWorkspaceID }).then((response) => {
      if (!active) return;
      setInstalledSkills(response.skills || []);
    }).catch((error) => {
      if (!active) return;
      setInstalledSkills([]);
      setToolingError(error.message);
    }).finally(() => {
      if (active) setToolingLoading(false);
    });
    return () => {
      active = false;
    };
  }, [toolPickerOpen, settingsOpen, toolingWorkspaceID]);
  reactExports.useEffect(() => {
    if (!settingsOpen) return;
    let active = true;
    sessions({ limit: 100, includeArchived: true }).then((response) => {
      if (!active) return;
      const items = (response.sessions || []).filter((session2) => Boolean(session2.archived_at));
      setArchivedSessions(items);
    }).catch(() => {
      if (!active) return;
      setArchivedSessions([]);
    });
    return () => {
      active = false;
    };
  }, [settingsOpen]);
  reactExports.useEffect(() => {
    const allowed = new Set(selectableToolingItems.map((item) => item.key));
    setSelectedGuidanceKeys((current) => current.filter((key) => allowed.has(key)));
  }, [selectableToolingItems]);
  reactExports.useEffect(() => {
    if (!comparisonVariantModel && modelOptions.length) {
      setComparisonVariantModel(`${modelOptions[0].llmProvider}::${modelOptions[0].llmModel}`);
    }
  }, [comparisonVariantModel, modelOptions]);
  reactExports.useEffect(() => {
    if (!settingsSections.length) return;
    if (!settingsSections.some((section) => section.key === settingsSection)) {
      setSettingsSection(settingsSections[0].key);
    }
  }, [settingsSections, settingsSection]);
  reactExports.useEffect(() => {
    return () => {
      if (scrollFrameRef.current) {
        window.cancelAnimationFrame(scrollFrameRef.current);
        scrollFrameRef.current = 0;
      }
      workbenchRelatedResourceService.releasePreview();
    };
  }, []);
  reactExports.useEffect(() => {
    shouldAutoScrollRef.current = true;
  }, [sessionID]);
  reactExports.useEffect(() => {
    setVisibleTaskCount(10);
  }, [agentID, defaultAgentConfig == null ? void 0 : defaultAgentConfig.id, taskSearch]);
  reactExports.useEffect(() => {
    if (!threadRef.current) return;
    if (!sessionID && !chatTimelineEvents.length) {
      window.requestAnimationFrame(() => {
        if (threadRef.current) threadRef.current.scrollTop = 0;
      });
      return;
    }
    if (!shouldAutoScrollRef.current) return;
    window.requestAnimationFrame(() => {
      const node2 = threadRef.current;
      if (!node2) return;
      node2.scrollTop = node2.scrollHeight;
    });
  }, [chatTimelineEvents.length, hasPendingApprovals, sessionID, streamingReply == null ? void 0 : streamingReply.text.length, waitingForReply]);
  reactExports.useEffect(() => {
    if (hasPendingApprovals && interventions$1.length > pendingApprovalCountRef.current) {
      setApprovalsOpen(true);
    }
    if (!hasPendingApprovals) {
      setApprovalsOpen(false);
    }
    pendingApprovalCountRef.current = interventions$1.length;
  }, [hasPendingApprovals, interventions$1.length]);
  reactExports.useEffect(() => {
    if (!latestSuccessfulSkillInstallSeq) return;
    const workspaceID = (sessionMeta == null ? void 0 : sessionMeta.workspace_id) || (defaultAgentConfig == null ? void 0 : defaultAgentConfig.workspace_id) || "";
    skills({ workspaceId: workspaceID }).then((response) => {
      setInstalledSkills(response.skills || []);
    }).catch(() => {
    });
  }, [latestSuccessfulSkillInstallSeq, sessionMeta == null ? void 0 : sessionMeta.workspace_id, defaultAgentConfig == null ? void 0 : defaultAgentConfig.workspace_id]);
  async function refresh(nextSessionID = sessionID) {
    const value = String(nextSessionID || "").trim();
    if (!value) {
      setStatus("session required");
      return;
    }
    setStatus("refreshing");
    await loadSession(value);
    await loadSessionSettings(value);
    rememberSession(value);
    setStatus("synced");
  }
  async function startWorkflow(nextSessionID, template, baseTask) {
    const run = {
      baseTask,
      currentStep: 0,
      sessionID: nextSessionID,
      status: "running",
      templateID: template.id,
      templateTitle: template.title,
      steps: template.workflow_steps.map((step, index2) => ({
        ...step,
        sentSeq: 0,
        status: index2 === 0 ? "running" : "pending"
      }))
    };
    persistWorkflowRun(run);
    setWorkflowRun(run);
    try {
      const sent = await sendTask(nextSessionID, {
        text: workflowStepMessage(template, baseTask, 0)
      });
      const startedRun = {
        ...run,
        steps: run.steps.map((step, index2) => {
          var _a3;
          return index2 === 0 ? { ...step, sentSeq: Number(((_a3 = sent == null ? void 0 : sent.userEvent) == null ? void 0 : _a3.seq) || 0) } : step;
        })
      };
      persistWorkflowRun(startedRun);
      setWorkflowRun(startedRun);
    } catch (error) {
      const failedRun = { ...run, status: "failed", steps: run.steps.map((step, index2) => index2 === 0 ? { ...step, status: "failed" } : step) };
      persistWorkflowRun(failedRun);
      setWorkflowRun(failedRun);
      throw error;
    }
  }
  function addComposerFiles(fileList) {
    const incoming = Array.from(fileList || []);
    if (!incoming.length) return;
    setComposerFiles((current) => {
      const known = new Set(current.map((item) => `${item.file.name}:${item.file.size}:${item.file.lastModified}`));
      const next = [...current];
      const rejected = [];
      for (const file of incoming) {
        const fingerprint = `${file.name}:${file.size}:${file.lastModified}`;
        if (known.has(fingerprint)) continue;
        if (next.length >= maxComposerFiles) {
          rejected.push(`最多上传 ${maxComposerFiles} 个文件`);
          break;
        }
        if (file.size > maxComposerFileBytes) {
          rejected.push(`${file.name} 超过 64 MB`);
          continue;
        }
        known.add(fingerprint);
        next.push({ id: composerFileID(file), file, status: "ready", error: "", upload: null });
      }
      if (rejected.length) setStatus(rejected[0]);
      return next;
    });
  }
  function removeComposerFile(id2) {
    if (uploadingFiles) return;
    setComposerFiles((current) => current.filter((item) => item.id !== id2));
  }
  async function uploadComposerFiles(nextSessionID) {
    var _a3, _b2, _c2, _d, _e;
    if (!composerFiles.length) return [];
    setUploadingFiles(true);
    const completed = [];
    try {
      for (const item of composerFiles) {
        let upload = item.upload;
        if (!upload) {
          setComposerFiles((current) => current.map((entry2) => entry2.id === item.id ? { ...entry2, status: "uploading", error: "" } : entry2));
          try {
            upload = await uploadSessionArtifact(nextSessionID, item.file);
          } catch (error) {
            setComposerFiles((current) => current.map((entry2) => entry2.id === item.id ? { ...entry2, status: "error", error: error.message } : entry2));
            throw new Error(`${item.file.name} 上传失败：${error.message}`);
          }
          setComposerFiles((current) => current.map((entry2) => entry2.id === item.id ? { ...entry2, status: "uploaded", error: "", upload } : entry2));
          if (upload.artifact) {
            setArtifactResponse((current) => ({
              ...current,
              artifacts: [...(current.artifacts || []).filter((artifact) => artifact.id !== upload.artifact.id), upload.artifact]
            }));
          }
        }
        completed.push({
          artifact_id: ((_a3 = upload.artifact) == null ? void 0 : _a3.id) || "",
          object_ref_id: ((_b2 = upload.object_ref) == null ? void 0 : _b2.id) || "",
          name: ((_c2 = upload.artifact) == null ? void 0 : _c2.name) || item.file.name,
          content_type: ((_d = upload.object_ref) == null ? void 0 : _d.content_type) || item.file.type || "application/octet-stream",
          size_bytes: ((_e = upload.object_ref) == null ? void 0 : _e.size_bytes) || item.file.size,
          workspace_path: upload.workspace_path || ""
        });
      }
      return completed;
    } finally {
      setUploadingFiles(false);
    }
  }
  function handleComposerDrop(event) {
    var _a3;
    event.preventDefault();
    setComposerDragActive(false);
    addComposerFiles((_a3 = event.dataTransfer) == null ? void 0 : _a3.files);
  }
  function handleComposerPaste(event) {
    const clipboard = event.clipboardData;
    if (!clipboard) return;
    const itemImages = Array.from(clipboard.items || []).filter((item) => item.kind === "file" && String(item.type || "").toLowerCase().startsWith("image/")).map((item) => item.getAsFile()).filter(Boolean);
    const images = itemImages.length ? itemImages : Array.from(clipboard.files || []).filter((file) => String(file.type || "").toLowerCase().startsWith("image/"));
    if (!images.length) return;
    event.preventDefault();
    addComposerFiles(images.map((file, index2) => clipboardImageFile(file, index2)));
    setStatus(images.length === 1 ? "已从剪贴板添加图片" : `已从剪贴板添加 ${images.length} 张图片`);
  }
  function composerVisionError() {
    const imageFiles = composerFiles.filter((item) => String(item.file.type || "").startsWith("image/"));
    if (!imageFiles.length) return "";
    const unsupported = imageFiles.find((item) => !supportedVisionImageTypes.has(String(item.file.type || "").toLowerCase()));
    if (unsupported) return `${unsupported.file.name} 不是支持的视觉图片格式，请使用 PNG、JPEG、GIF 或 WebP。`;
    const current = modelOptions.find((option) => option.llmProvider === settingsDraft.llmProvider && option.llmModel === settingsDraft.llmModel);
    if ((current == null ? void 0 : current.capabilityType) === "text_image") return "";
    if (modelOptions.some((option) => option.isDefaultVision && option.capabilityType === "text_image")) return "";
    return "当前模型不支持图片解析，且尚未配置统一图片视觉模型。请前往设置 > 模型完成配置。";
  }
  async function startSession() {
    var _a3, _b2, _c2, _d;
    const visionError = composerVisionError();
    if (visionError) {
      setStatus(visionError);
      return;
    }
    setStatus("creating session");
    const agent2 = agentID.trim() ? { id: agentID.trim() } : defaultAgentConfig || await defaultAgent();
    const environment = environmentID.trim() ? { id: environmentID.trim() } : await createEnvironment({
      name: "Workbench Environment",
      config: { type: "cloud" }
    });
    const session2 = await createSession({
      agent_id: agent2.id,
      environment_id: environment.id,
      title: task.trim() ? task.trim().slice(0, 80) : ((_a3 = composerFiles[0]) == null ? void 0 : _a3.file.name) || "New workbench task"
    });
    setAgentID(agent2.id);
    setEnvironmentID(environment.id);
    setSessionID(session2.id);
    setSessionMeta(session2);
    rememberSession(session2.id);
    setRecentSessions((current) => [session2, ...current.filter((item) => item.id !== session2.id)]);
    const shouldApplyInitialSettings = Boolean(
      settingsDraft.interventionMode || settingsDraft.toolRuntime || settingsDraft.llmProvider || settingsDraft.llmModel
    );
    if (shouldApplyInitialSettings) {
      const updatedSession = await updateSessionRuntimeSettings(session2.id, {
        intervention_mode: settingsDraft.interventionMode || "request_approval",
        llm_model: settingsDraft.llmModel || ((_b2 = agent2.config_version) == null ? void 0 : _b2.llm_model) || "",
        llm_provider: settingsDraft.llmProvider || ((_c2 = agent2.config_version) == null ? void 0 : _c2.llm_provider) || "",
        tool_runtime: settingsDraft.toolRuntime || "cloud_sandbox"
      });
      setSessionMeta(updatedSession);
      await loadSessionSettings(session2.id, updatedSession);
    }
    if (task.trim() || composerFiles.length) {
      if (workflowMode && ((_d = selectedTaskTemplate == null ? void 0 : selectedTaskTemplate.workflow_steps) == null ? void 0 : _d.length)) {
        await startWorkflow(session2.id, selectedTaskTemplate, task.trim());
      } else {
        await sendTask(session2.id);
      }
    } else {
      await refresh(session2.id);
    }
  }
  async function sendTask(nextSessionID = sessionID, options = {}) {
    var _a3, _b2, _c2;
    const value = String(nextSessionID || "").trim();
    const text2 = String(options.text ?? task).trim();
    const guidanceItems = options.guidanceItems || selectedGuidanceItems;
    const attachmentItems = ((_a3 = options.attachments) == null ? void 0 : _a3.length) ? options.attachments : composerFiles;
    const guidedText = buildGuidedTaskMessage(defaultComposerTask(text2, attachmentItems), guidanceItems);
    if (!value) {
      setStatus("session required");
      return;
    }
    const visionError = composerVisionError();
    if (visionError && !((_b2 = options.attachments) == null ? void 0 : _b2.length)) {
      setStatus(visionError);
      return;
    }
    if (!text2 && !composerFiles.length && !((_c2 = options.attachments) == null ? void 0 : _c2.length)) {
      setStatus("task required");
      return;
    }
    const queued = Boolean(waitingForReply || effectiveSessionStatus === "running" || effectiveSessionStatus === "interrupting");
    setStatus(composerFiles.length ? "uploading files" : queued ? "queued message" : "sending task");
    const attachments = options.attachments || await uploadComposerFiles(value);
    setStatus(queued ? "queued message" : "sending task");
    const response = await sendSessionMessage(value, guidedText, {
      preferLatest: false,
      attachments,
      queued,
      queueIfBusy: true
    });
    const appendedEvents = response.events || [];
    const userEvent = appendedEvents.find((event) => event.type === "user.message");
    setEventsResponse((current) => ({
      ...current,
      events: mergeEvents(current.events, appendedEvents)
    }));
    const appendedStatus = latestSessionStatus(appendedEvents);
    if (appendedStatus) mergeSessionStatus(value, appendedStatus);
    eventStreamCursorRef.current = Math.max(eventStreamCursorRef.current || 0, maxSeq(appendedEvents));
    if (options.clearTask !== false) {
      setTask("");
      setComposerFiles([]);
    }
    setWaitingForReply(true);
    setStatus("waiting for reply");
    if (userEvent == null ? void 0 : userEvent.seq) {
      eventStreamCursorRef.current = Math.max(eventStreamCursorRef.current || 0, Number(userEvent.seq || 0));
    }
    return { response, userEvent };
  }
  function applyTaskTemplate(template, asWorkflow) {
    const toolNames = new Set(template.tools || []);
    const skillNames = new Set(template.skills || []);
    const matchingKeys = toolingCatalog.items.filter((item) => {
      if (!item.selectable) return false;
      if (item.kind === "tool_namespace") return toolNames.has(item.name);
      if (item.kind === "skill") return skillNames.has(item.name);
      return false;
    }).map((item) => item.key);
    setTask(template.prompt || "");
    setSelectedGuidanceKeys(matchingKeys);
    setSelectedTaskTemplateID(template.id);
    setWorkflowMode(Boolean(asWorkflow));
    setTemplatePickerOpen(false);
    setStatus(asWorkflow ? "工作流已就绪" : "模板已填充");
  }
  function clearTaskTemplate() {
    setSelectedTaskTemplateID("");
    setWorkflowMode(false);
  }
  reactExports.useEffect(() => {
    if (!workflowRun || workflowRun.status !== "running" || workflowAdvancingRef.current) return;
    const currentStep = workflowRun.steps[workflowRun.currentStep];
    const sentSeq = Number((currentStep == null ? void 0 : currentStep.sentSeq) || 0);
    if (!currentStep || currentStep.status !== "running" || sentSeq <= 0) return;
    if (waitingForReply || hasPendingApprovals || effectiveSessionStatus !== "idle") return;
    const laterEvents = events$1.filter((event) => Number(event.seq || 0) > sentSeq);
    const failed = lastIdleTurnStatus === "failed" || laterEvents.some((event) => event.type === "runtime.failed");
    const replied = laterEvents.some((event) => event.type === "agent.message" && hasVisibleAgentText(event));
    if (!failed && !replied) return;
    workflowAdvancingRef.current = true;
    const advance = async () => {
      if (failed) {
        const failedRun = {
          ...workflowRun,
          status: "failed",
          steps: workflowRun.steps.map((step, index2) => index2 === workflowRun.currentStep ? { ...step, status: "failed" } : step)
        };
        persistWorkflowRun(failedRun);
        setWorkflowRun(failedRun);
        setStatus("工作流步骤失败");
        return;
      }
      const nextIndex = workflowRun.currentStep + 1;
      if (nextIndex >= workflowRun.steps.length) {
        const completedRun = {
          ...workflowRun,
          status: "completed",
          steps: workflowRun.steps.map((step, index2) => index2 === workflowRun.currentStep ? { ...step, status: "completed" } : step)
        };
        persistWorkflowRun(completedRun);
        setWorkflowRun(completedRun);
        setStatus("工作流已完成");
        return;
      }
      const nextRun = {
        ...workflowRun,
        currentStep: nextIndex,
        steps: workflowRun.steps.map((step, index2) => {
          if (index2 === workflowRun.currentStep) return { ...step, status: "completed" };
          if (index2 === nextIndex) return { ...step, sentSeq: 0, status: "running" };
          return step;
        })
      };
      persistWorkflowRun(nextRun);
      setWorkflowRun(nextRun);
      const template = taskTemplates$1.find((item) => item.id === workflowRun.templateID) || {
        title: workflowRun.templateTitle,
        workflow_steps: workflowRun.steps
      };
      try {
        const sent = await sendTask(workflowRun.sessionID, {
          text: workflowStepMessage(template, workflowRun.baseTask, nextIndex)
        });
        const sentRun = {
          ...nextRun,
          steps: nextRun.steps.map((step, index2) => {
            var _a3;
            return index2 === nextIndex ? { ...step, sentSeq: Number(((_a3 = sent == null ? void 0 : sent.userEvent) == null ? void 0 : _a3.seq) || 0) } : step;
          })
        };
        persistWorkflowRun(sentRun);
        setWorkflowRun(sentRun);
      } catch (error) {
        const failedRun = {
          ...nextRun,
          status: "failed",
          steps: nextRun.steps.map((step, index2) => index2 === nextIndex ? { ...step, status: "failed" } : step)
        };
        persistWorkflowRun(failedRun);
        setWorkflowRun(failedRun);
        setStatus(error.message);
      }
    };
    advance().finally(() => {
      workflowAdvancingRef.current = false;
    });
  }, [effectiveSessionStatus, events$1, hasPendingApprovals, lastIdleTurnStatus, taskTemplates$1, waitingForReply, workflowRun]);
  async function interruptTask(nextSessionID = sessionID) {
    const value = String(nextSessionID || "").trim();
    if (!value) {
      setStatus("session required");
      return;
    }
    setStatus("interrupting task");
    setSessionMeta((current) => current ? { ...current, status: "interrupting" } : current);
    try {
      const response = await interruptSession(value);
      const appendedEvents = response.events || [];
      setEventsResponse((current) => ({
        ...current,
        events: mergeEvents(current.events, appendedEvents)
      }));
      eventStreamCursorRef.current = Math.max(eventStreamCursorRef.current || 0, maxSeq(appendedEvents));
      setWaitingForReply(false);
      await syncSession(value);
      setStatus("interrupt requested");
      workbenchNotificationService.show({
        level: "success",
        title: "已请求中断",
        message: (sessionMeta == null ? void 0 : sessionMeta.title) || value,
        dedupeKey: `session.interrupt.${value}`
      });
    } catch (error) {
      workbenchNotificationService.show({
        level: "error",
        title: "中断任务失败",
        message: error.message || String(error),
        dedupeKey: `session.interrupt.${value}`
      });
      throw error;
    }
  }
  async function confirmInterruptTask() {
    const confirmed = await workbenchDialogService.confirm({
      title: "中断当前任务？",
      description: "智能体将停止本轮执行，已经生成的消息和文件会保留。",
      detail: (sessionMeta == null ? void 0 : sessionMeta.title) || sessionID || "当前任务",
      confirmLabel: "中断任务",
      cancelLabel: "继续运行",
      tone: "warning"
    });
    if (!confirmed) return false;
    await interruptTask();
    return true;
  }
  function deferApprovals() {
    setApprovalsOpen(false);
    workbenchNotificationService.show({
      level: "warning",
      title: "审批已暂时收起",
      message: "任务仍在等待审批，可从顶部提示重新打开或终止任务。",
      dedupeKey: `approval.deferred.${sessionID}`
    });
  }
  async function interruptFromApprovals() {
    const interrupted = await confirmInterruptTask();
    if (interrupted) setApprovalsOpen(false);
  }
  function toggleGuidanceKey(key) {
    setSelectedGuidanceKeys((current) => current.includes(key) ? current.filter((item) => item !== key) : [...current, key]);
  }
  async function archiveTask(targetSessionID) {
    const nextSessionID = String(targetSessionID || "").trim();
    if (!nextSessionID) return;
    setSessionAction(`archive:${nextSessionID}`);
    setStatus("archiving");
    try {
      const archived = await archiveSession(nextSessionID);
      setRecentSessions((current) => current.filter((item) => item.id !== nextSessionID));
      setArchivedSessions((current) => [archived, ...current.filter((item) => item.id !== nextSessionID)]);
      if (sessionID === nextSessionID) {
        setSessionMeta(archived);
        setWaitingForReply(false);
      }
      setStatus("archived");
    } finally {
      setSessionAction("");
    }
  }
  async function restoreTask(session2) {
    const nextSessionID = String((session2 == null ? void 0 : session2.id) || "").trim();
    if (!nextSessionID) return;
    setSessionAction(`restore:${nextSessionID}`);
    setStatus("restoring");
    try {
      const restored = await restoreSession(nextSessionID);
      setArchivedSessions((current) => current.filter((item) => item.id !== nextSessionID));
      setRecentSessions((current) => [restored, ...current.filter((item) => item.id !== nextSessionID)]);
      if (sessionID === nextSessionID) setSessionMeta(restored);
      setStatus("restored");
    } finally {
      setSessionAction("");
    }
  }
  function mergeSessionMetadata(updated) {
    setRecentSessions((current) => current.map((item) => item.id === updated.id ? { ...item, ...updated } : item));
    setArchivedSessions((current) => current.map((item) => item.id === updated.id ? { ...item, ...updated } : item));
    if (sessionID === updated.id) setSessionMeta((current) => ({ ...current || {}, ...updated }));
    setMetadataSession((current) => (current == null ? void 0 : current.id) === updated.id ? { ...current, ...updated } : current);
  }
  function mergeSessionStatus(targetSessionID, nextStatus) {
    if (!targetSessionID || !nextStatus) return;
    setSessionMeta((current) => (current == null ? void 0 : current.id) === targetSessionID ? { ...current, status: nextStatus } : current);
    setRecentSessions((current) => current.map((session2) => session2.id === targetSessionID ? { ...session2, status: nextStatus } : session2));
  }
  async function toggleTaskPin(session2) {
    if (!(session2 == null ? void 0 : session2.id)) return;
    const previousPinnedAt = session2.pinned_at || null;
    const shouldPin = !previousPinnedAt;
    setSessionAction(`pin:${session2.id}`);
    mergeSessionMetadata({
      ...session2,
      pinned_at: shouldPin ? (/* @__PURE__ */ new Date()).toISOString() : null
    });
    try {
      const updated = await updateSessionMetadata(session2.id, { pinned: shouldPin });
      mergeSessionMetadata({
        ...updated,
        pinned_at: shouldPin ? updated.pinned_at || (/* @__PURE__ */ new Date()).toISOString() : null
      });
      setStatus(shouldPin ? "任务已置顶" : "已取消置顶");
    } catch (error) {
      mergeSessionMetadata({ ...session2, pinned_at: previousPinnedAt });
      throw error;
    } finally {
      setSessionAction("");
    }
  }
  function openTaskMetadata(session2) {
    setMetadataSession(session2);
    setMetadataTagsDraft((session2.tags || []).join(", "));
    setTaskMenuSessionID("");
  }
  async function saveTaskMetadata() {
    if (!(metadataSession == null ? void 0 : metadataSession.id)) return;
    const tags = metadataTagsDraft.split(/[,，\n]/).map((item) => item.trim()).filter(Boolean);
    setSavingMetadata(true);
    try {
      const updated = await updateSessionMetadata(metadataSession.id, { tags });
      mergeSessionMetadata(updated);
      setMetadataTagsDraft((updated.tags || []).join(", "));
      setStatus("任务标签已保存");
    } finally {
      setSavingMetadata(false);
    }
  }
  async function rerunTask(sourceSession, overrides = {}) {
    const sourceSessionID2 = String((sourceSession == null ? void 0 : sourceSession.id) || "").trim();
    if (!sourceSessionID2) return null;
    setSessionAction(`rerun:${sourceSessionID2}`);
    setStatus("正在重跑任务");
    try {
      const response = await rerunSession(sourceSessionID2, overrides);
      const rerun = response.session;
      setRecentSessions((current) => [rerun, ...current.filter((item) => item.id !== rerun.id)]);
      await openSession(rerun);
      setStatus("任务已按原参数重跑");
      return response;
    } finally {
      setSessionAction("");
    }
  }
  async function loadTaskComparison(leftID = comparisonLeftID, rightID = comparisonRightID) {
    if (!leftID || !rightID || leftID === rightID) return;
    setComparisonLoading(true);
    try {
      const result = await compareSessions(leftID, rightID);
      setComparisonResult(result);
    } finally {
      setComparisonLoading(false);
    }
  }
  function openTaskComparison(sourceSession) {
    var _a3;
    const sourceID = String((sourceSession == null ? void 0 : sourceSession.id) || "").trim();
    const baseTitle = String((sourceSession == null ? void 0 : sourceSession.title) || "").replace(/ \(rerun\)$/i, "");
    const candidates = recentSessions.filter((item) => item.id !== sourceID && item.status !== "terminated");
    const related = candidates.find((item) => String(item.title || "").replace(/ \(rerun\)$/i, "") === baseTitle);
    const rightID = (related == null ? void 0 : related.id) || ((_a3 = candidates[0]) == null ? void 0 : _a3.id) || "";
    setComparisonLeftID(sourceID);
    setComparisonRightID(rightID);
    setComparisonResult(null);
    setComparisonOpen(true);
    if (sourceID && rightID) {
      loadTaskComparison(sourceID, rightID).catch((error) => setStatus(error.message));
    }
  }
  async function createComparisonVariant() {
    const source = recentSessions.find((item) => item.id === comparisonLeftID);
    const [llmProvider, llmModel] = comparisonVariantModel.split("::");
    if (!source || !llmProvider || !llmModel) return;
    const response = await rerunTask(source, { llm_provider: llmProvider, llm_model: llmModel });
    if (!(response == null ? void 0 : response.session)) return;
    setComparisonRightID(response.session.id);
    setComparisonResult(null);
    setComparisonOpen(true);
    loadTaskComparison(source.id, response.session.id).catch((error) => setStatus(error.message));
  }
  async function deleteTask(targetSessionID) {
    const nextSessionID = String(targetSessionID || "").trim();
    if (!nextSessionID) return;
    const targetSession = recentSessions.find((item) => item.id === nextSessionID);
    const confirmed = await workbenchDialogService.confirm({
      title: "删除任务？",
      description: "该任务、事件记录和关联状态将被永久删除，此操作无法撤销。",
      detail: (targetSession == null ? void 0 : targetSession.title) || nextSessionID,
      confirmLabel: "删除任务",
      cancelLabel: "取消",
      tone: "danger"
    });
    if (!confirmed) {
      window.setTimeout(() => {
        var _a3;
        return (_a3 = taskMenuButtonRefs.current.get(nextSessionID)) == null ? void 0 : _a3.focus();
      }, 0);
      return;
    }
    setSessionAction(`delete:${nextSessionID}`);
    setStatus("deleting");
    try {
      await deleteSession(nextSessionID);
      try {
        window.localStorage.removeItem(workflowStorageKey(nextSessionID));
      } catch {
      }
      setRecentSessions((current) => current.filter((item) => item.id !== nextSessionID));
      if (sessionID === nextSessionID) {
        startNewTask();
      }
      setStatus("deleted");
      workbenchNotificationService.show({
        level: "success",
        title: "任务已删除",
        message: (targetSession == null ? void 0 : targetSession.title) || nextSessionID,
        dedupeKey: `session.delete.${nextSessionID}`
      });
    } catch (error) {
      workbenchNotificationService.show({
        level: "error",
        title: "删除任务失败",
        message: error.message || String(error),
        dedupeKey: `session.delete.${nextSessionID}`
      });
      throw error;
    } finally {
      setSessionAction("");
    }
  }
  function clearArtifactPreview() {
    workbenchRelatedResourceService.releasePreview();
    setArtifactPreview(null);
    setArtifactPreviewMode("preview");
  }
  function startArtifactResize(event) {
    if (!artifactPreview) return;
    event.preventDefault();
    artifactResizeRef.current = {
      pointerX: event.clientX,
      width: artifactPreviewWidth
    };
    document.body.classList.add("resizing-artifact-preview");
  }
  reactExports.useEffect(() => {
    function handlePointerMove(event) {
      const resize = artifactResizeRef.current;
      if (!resize) return;
      const nextWidth = resize.width + resize.pointerX - event.clientX;
      setArtifactPreviewWidth(Math.min(760, Math.max(320, nextWidth)));
    }
    function handlePointerUp() {
      if (!artifactResizeRef.current) return;
      artifactResizeRef.current = null;
      document.body.classList.remove("resizing-artifact-preview");
    }
    window.addEventListener("pointermove", handlePointerMove);
    window.addEventListener("pointerup", handlePointerUp);
    return () => {
      window.removeEventListener("pointermove", handlePointerMove);
      window.removeEventListener("pointerup", handlePointerUp);
      document.body.classList.remove("resizing-artifact-preview");
    };
  }, []);
  async function previewArtifact(artifact) {
    if (!sessionID || !(artifact == null ? void 0 : artifact.id)) return;
    const resource = artifactToResourceRef(artifact, { sessionID });
    setArtifactPreviewMode("preview");
    setArtifactPreview({ resource, status: "loading" });
    try {
      const preview = await workbenchRelatedResourceService.preview(resource);
      setArtifactPreview({ resource, status: "ready", ...preview });
    } catch (error) {
      if (isPreviewCancelledError(error)) return;
      setArtifactPreview({ resource, status: "error", error: error.message });
    }
  }
  async function downloadArtifactResource(resource) {
    try {
      await workbenchRelatedResourceService.open(resource, { target: "download" });
    } catch (error) {
      setStatus(error.message);
      workbenchNotificationService.show({
        level: "error",
        title: "打开文件失败",
        message: error.message || String(error),
        dedupeKey: `resource.open.${resource.source}.${resource.id}`
      });
    }
  }
  async function approve(intervention) {
    const decisionKey = `${sessionID}:${intervention.turn_id}:${intervention.call_id}`;
    if (approvalDecisionRef.current) return;
    approvalDecisionRef.current = decisionKey;
    setDecidingApprovalID(intervention.call_id);
    setWaitingForReply(true);
    setStatus("approving");
    try {
      const response = await approveIntervention(sessionID, intervention.turn_id, intervention.call_id, { reason: "approved from app" });
      setEventsResponse((current) => ({
        ...current,
        events: mergeEvents(current.events, response.events || [])
      }));
      eventStreamCursorRef.current = Math.max(eventStreamCursorRef.current || 0, maxSeq(response.events || []));
      setInterventionResponse((current) => ({
        ...current,
        interventions: (current.interventions || []).filter((item) => item.call_id !== intervention.call_id)
      }));
      await syncSession(sessionID);
      setWaitingForReply(true);
      setStatus("waiting for reply");
      workbenchNotificationService.show({
        level: "success",
        title: "审批已通过",
        message: `${intervention.api_name} · ${intervention.call_id}`,
        dedupeKey: `approval.approved.${decisionKey}`
      });
    } catch (error) {
      setWaitingForReply(false);
      workbenchNotificationService.show({
        level: "error",
        title: "审批失败",
        message: error.message || String(error),
        dedupeKey: `approval.failed.${decisionKey}`
      });
      throw error;
    } finally {
      if (approvalDecisionRef.current === decisionKey) approvalDecisionRef.current = "";
      setDecidingApprovalID("");
    }
  }
  async function requestInstalledSkillEnable(skill) {
    const identifier = String((skill == null ? void 0 : skill.identifier) || "").trim();
    const version2 = Number((skill == null ? void 0 : skill.version) || 0);
    if (!sessionID || !identifier || version2 <= 0) return;
    const key = `${identifier}:${version2}`;
    setRequestingSkillEnable(key);
    setStatus("requesting skill enable");
    try {
      await sendTask(sessionID, {
        text: `请调用 skills.enable，将已安装 Skill ${JSON.stringify(identifier)} 的 version ${version2} 启用到当前 Agent。该操作需要独立审批；完成后明确说明 requires_session_upgrade，以及当前 Session 是否仍固定在旧 Agent 配置。`,
        attachments: [],
        clearTask: false,
        guidanceItems: []
      });
    } catch (error) {
      setStatus(error.message);
    } finally {
      setRequestingSkillEnable("");
    }
  }
  async function requestInstalledSkillDisable(skill) {
    const identifier = String((skill == null ? void 0 : skill.identifier) || "").trim();
    if (!sessionID || !identifier) return;
    setRequestingSkillDisable(identifier);
    setStatus("requesting skill disable");
    try {
      await sendTask(sessionID, {
        text: `请调用 skills.disable，将 Skill ${JSON.stringify(identifier)} 从当前 Agent 的最新配置中停用。只移除这个 binding，不要归档或卸载 Skill。该操作需要独立审批；完成后明确说明 requires_session_upgrade，以及当前 Session 是否仍固定在旧 Agent 配置。`,
        attachments: [],
        clearTask: false,
        guidanceItems: []
      });
    } catch (error) {
      setStatus(error.message);
    } finally {
      setRequestingSkillDisable("");
    }
  }
  async function applySkillConfigToSession(action) {
    var _a3;
    const targetVersion = Number((action == null ? void 0 : action.targetConfigVersion) || 0);
    if (!sessionID || targetVersion <= 0) return;
    if (effectiveSessionStatus !== "idle") {
      const error = new Error("当前任务结束后才能应用 Agent 配置");
      setStatus(error.message);
      if (action == null ? void 0 : action.throwOnError) throw error;
      return null;
    }
    setApplyingSessionConfigVersion(targetVersion);
    setStatus(`正在应用 Agent 配置 v${targetVersion}`);
    try {
      const response = await upgradeSessionConfig(sessionID, {
        to_version: targetVersion,
        updated_by: "workbench"
      });
      const nextSession = response.session || await session(sessionID);
      setSessionMeta(nextSession);
      setRecentSessions((current) => [nextSession, ...current.filter((item) => item.id !== nextSession.id)]);
      if ((_a3 = response.event) == null ? void 0 : _a3.type) {
        setEventsResponse((current) => ({
          ...current,
          events: mergeEvents(current.events, [response.event])
        }));
        eventStreamCursorRef.current = Math.max(eventStreamCursorRef.current || 0, Number(response.event.seq || 0));
      }
      await loadSessionSettings(sessionID, nextSession);
      const operationLabel = (action == null ? void 0 : action.operation) === "disable" ? "Skill 已在当前会话停用" : (action == null ? void 0 : action.operation) === "sync" ? "Agent 配置已应用到当前会话" : "Skill 已在当前会话生效";
      setStatus(response.changed ? `${operationLabel} · config v${targetVersion}` : `当前会话已使用 config v${targetVersion}`);
      return response;
    } catch (error) {
      setStatus(error.message);
      if (action == null ? void 0 : action.throwOnError) throw error;
      return null;
    } finally {
      setApplyingSessionConfigVersion(0);
    }
  }
  async function reject(intervention) {
    var _a3;
    const decisionKey = `${sessionID}:${intervention.turn_id}:${intervention.call_id}`;
    if (approvalDecisionRef.current) return;
    const reason = window.prompt("拒绝原因", "在页面中拒绝");
    if (reason === null) return;
    approvalDecisionRef.current = decisionKey;
    setDecidingApprovalID(intervention.call_id);
    setStatus("rejecting");
    try {
      await rejectIntervention(sessionID, intervention.turn_id, intervention.call_id, { reason });
      const synced = await syncSession(sessionID);
      const resumesTurn = ((_a3 = synced == null ? void 0 : synced.session) == null ? void 0 : _a3.status) === "running";
      setWaitingForReply(resumesTurn);
      setStatus(resumesTurn ? "waiting for reply" : "tool call rejected");
      workbenchNotificationService.show({
        level: "success",
        title: "已拒绝工具调用",
        message: `${intervention.api_name} · ${intervention.call_id}`,
        dedupeKey: `approval.rejected.${decisionKey}`
      });
    } catch (error) {
      workbenchNotificationService.show({
        level: "error",
        title: "拒绝审批失败",
        message: error.message || String(error),
        dedupeKey: `approval.reject-failed.${decisionKey}`
      });
      throw error;
    } finally {
      if (approvalDecisionRef.current === decisionKey) approvalDecisionRef.current = "";
      setDecidingApprovalID("");
    }
  }
  async function openSession(session2) {
    setStatus("loading chat");
    clearArtifactPreview();
    setComposerFiles([]);
    setToolPickerOpen(false);
    setSessionID(session2.id);
    setAgentID(session2.agent_id || "");
    setEnvironmentID(session2.environment_id || "");
    rememberSession(session2.id);
    await loadSession(session2.id);
    await loadSessionSettings(session2.id, session2);
    setStatus("history restored");
  }
  function startNewTask() {
    var _a3, _b2;
    clearArtifactPreview();
    setComposerFiles([]);
    setComposerDragActive(false);
    setToolPickerOpen(false);
    setAgentID((current) => current || (defaultAgentConfig == null ? void 0 : defaultAgentConfig.id) || "");
    setEnvironmentID("");
    setSessionID("");
    setSessionMeta(null);
    setEventsResponse({ events: [] });
    setInterventionResponse({ interventions: [] });
    setArtifactResponse({ artifacts: [] });
    setWaitingForReply(false);
    setRuntimeConfig(null);
    setRuntimeCapabilities({ default_runtime: "cloud_sandbox", available_runtimes: ["cloud_sandbox"] });
    setSettingsDraft({
      interventionMode: "request_approval",
      llmModel: ((_a3 = defaultAgentConfig == null ? void 0 : defaultAgentConfig.config_version) == null ? void 0 : _a3.llm_model) || "",
      llmProvider: ((_b2 = defaultAgentConfig == null ? void 0 : defaultAgentConfig.config_version) == null ? void 0 : _b2.llm_provider) || "",
      toolRuntime: "cloud_sandbox"
    });
    setApprovalsOpen(false);
    setTemplatePickerOpen(false);
    setSelectedTaskTemplateID("");
    setWorkflowMode(false);
    setWorkflowRun(null);
    setTaskHoverPreview(null);
    setStatus("ready");
    eventStreamCursorRef.current = 0;
    forgetSession();
    loadPreSessionDefaults().catch((error) => setStatus(error.message));
  }
  function handleThreadScroll(event) {
    const node2 = event.currentTarget;
    if (scrollFrameRef.current) return;
    scrollFrameRef.current = window.requestAnimationFrame(() => {
      scrollFrameRef.current = 0;
      const distanceFromBottom = node2.scrollHeight - node2.scrollTop - node2.clientHeight;
      const nextNearBottom = distanceFromBottom < 96;
      shouldAutoScrollRef.current = nextNearBottom;
    });
  }
  function showTaskHoverPreview(session2, event) {
    const rect = event.currentTarget.getBoundingClientRect();
    const previewWidth = 280;
    const gap = 16;
    const maxLeft = window.innerWidth - previewWidth - 16;
    const left = Math.min(rect.right + gap, maxLeft);
    setTaskHoverPreview({
      left,
      time: formatTaskTime(session2.created_at),
      title: session2.title || "未命名任务",
      top: rect.top + rect.height / 2
    });
  }
  function hideTaskHoverPreview() {
    setTaskHoverPreview(null);
  }
  function resetSessionViewForAgent(agent2) {
    clearArtifactPreview();
    setComposerFiles([]);
    setToolPickerOpen(false);
    setSessionID("");
    setSessionMeta(null);
    setEnvironmentID("");
    setEventsResponse({ events: [] });
    setInterventionResponse({ interventions: [] });
    setArtifactResponse({ artifacts: [] });
    setWaitingForReply(false);
    setApprovalsOpen(false);
    setTaskHoverPreview(null);
    setStatus("ready");
    eventStreamCursorRef.current = 0;
    forgetSession();
    if (agent2 == null ? void 0 : agent2.config_version) {
      setSettingsDraft((current) => ({
        ...current,
        llmProvider: agent2.config_version.llm_provider || current.llmProvider || "",
        llmModel: agent2.config_version.llm_model || current.llmModel || ""
      }));
    }
  }
  async function applySessionSettings(patch2) {
    const nextDraft = { ...settingsDraft, ...patch2 };
    setSettingsDraft(nextDraft);
    if (!sessionID) return;
    setSavingSettings(true);
    setStatus("saving settings");
    try {
      const updatedSession = await updateSessionRuntimeSettings(sessionID, {
        intervention_mode: nextDraft.interventionMode,
        llm_model: nextDraft.llmModel,
        llm_provider: nextDraft.llmProvider,
        tool_runtime: nextDraft.toolRuntime
      });
      setSessionMeta(updatedSession);
      await loadSessionSettings(sessionID, updatedSession);
      setStatus("settings saved");
    } finally {
      setSavingSettings(false);
    }
  }
  const sessionStatus = String(effectiveSessionStatus || (sessionMeta == null ? void 0 : sessionMeta.status) || "");
  const isSessionInterrupting = sessionStatus === "interrupting";
  const isSessionBusy = Boolean(
    sessionID && (waitingForReply && sessionStatus !== "idle" && sessionStatus !== "failed" && sessionStatus !== "terminated" || hasPendingApprovals || (liveSignal == null ? void 0 : liveSignal.kind) === "thinking" || sessionStatus === "running" || sessionStatus === "interrupting")
  );
  const hasComposerContent = Boolean(task.trim() || composerFiles.length);
  const primaryAction = !sessionID ? {
    className: "composer-primary-button",
    disabled: uploadingFiles || !hasComposerContent,
    label: "开始",
    mode: "start"
  } : isSessionInterrupting ? {
    className: "composer-primary-button interrupting",
    disabled: true,
    label: "中断中...",
    mode: "interrupting"
  } : isSessionBusy ? {
    className: "composer-primary-button interrupt",
    disabled: false,
    label: "运行中",
    mode: "interrupt"
  } : {
    className: "composer-primary-button",
    disabled: uploadingFiles || !hasComposerContent,
    label: "发送",
    mode: "send"
  };
  async function stopWorkflowRun() {
    if (!workflowRun || workflowRun.status !== "running") return;
    const canceledRun = {
      ...workflowRun,
      status: "canceled",
      steps: workflowRun.steps.map((step, index2) => index2 === workflowRun.currentStep && step.status === "running" ? { ...step, status: "canceled" } : step)
    };
    persistWorkflowRun(canceledRun);
    setWorkflowRun(canceledRun);
    setStatus("工作流已停止");
    if (isSessionBusy) await interruptTask(workflowRun.sessionID);
  }
  const hasTaskSearch = Boolean(taskSearch.trim());
  function navigatePluginRoute(path2) {
    clearArtifactPreview();
    setSettingsOpen(false);
    window.location.hash = encodeURI(path2);
  }
  function closePluginRoute() {
    if (!pluginRoutePath && !window.location.hash) return;
    window.history.pushState(null, "", `${window.location.pathname}${window.location.search}`);
    setPluginRoutePath("");
  }
  function openSettingsPage() {
    closePluginRoute();
    setSettingsOpen(true);
  }
  function closeSettingsPage() {
    setSettingsOpen(false);
  }
  function openInspectorPage() {
    window.open("/inspector", "_blank", "noopener,noreferrer");
  }
  function openArchivedSessionFromSettings(session2) {
    closeSettingsPage();
    openSession(session2).catch((error) => setStatus(error.message));
  }
  function handleTaskComposerKeyDown(event) {
    var _a3, _b2;
    if (event.key !== "Enter" || event.shiftKey || ((_a3 = event.nativeEvent) == null ? void 0 : _a3.isComposing)) {
      return;
    }
    if (primaryAction.mode !== "start" && primaryAction.mode !== "send" || primaryAction.disabled) {
      return;
    }
    event.preventDefault();
    if (primaryAction.mode === "start") {
      startSession().catch((error) => setStatus(error.message));
      return;
    }
    if (workflowMode && ((_b2 = selectedTaskTemplate == null ? void 0 : selectedTaskTemplate.workflow_steps) == null ? void 0 : _b2.length) && task.trim()) {
      startWorkflow(sessionID, selectedTaskTemplate, task.trim()).catch((error) => setStatus(error.message));
      return;
    }
    sendTask().catch((error) => setStatus(error.message));
  }
  function selectAgentFromSettings(nextAgentID) {
    const agent2 = availableAgents.find((item) => item.id === nextAgentID);
    if (!agent2) return;
    setAgentID(nextAgentID);
    if ((sessionMeta == null ? void 0 : sessionMeta.agent_id) && sessionMeta.agent_id !== nextAgentID) {
      resetSessionViewForAgent(agent2);
      return;
    }
    if (agent2.config_version) {
      setSettingsDraft((current) => ({
        ...current,
        llmProvider: agent2.config_version.llm_provider || current.llmProvider || "",
        llmModel: agent2.config_version.llm_model || current.llmModel || ""
      }));
    }
  }
  async function createAgentFromSettings(body) {
    setStatus("正在创建 Agent");
    const created = await createAgent(body);
    setAvailableAgents((current) => {
      const nextAgents = [created, ...current.filter((item) => item.id !== created.id)];
      return nextAgents.sort((left, right) => {
        if (left.id === ((defaultAgentConfig == null ? void 0 : defaultAgentConfig.id) || "agt_general")) return -1;
        if (right.id === ((defaultAgentConfig == null ? void 0 : defaultAgentConfig.id) || "agt_general")) return 1;
        return String(left.name || "").localeCompare(String(right.name || ""));
      });
    });
    setAgentID(created.id);
    if ((sessionMeta == null ? void 0 : sessionMeta.agent_id) && sessionMeta.agent_id !== created.id) {
      resetSessionViewForAgent(created);
    } else if (created.config_version) {
      setSettingsDraft((current) => ({
        ...current,
        llmProvider: created.config_version.llm_provider || current.llmProvider || "",
        llmModel: created.config_version.llm_model || current.llmModel || ""
      }));
    }
    setStatus("Agent 已创建");
    return created;
  }
  async function saveAgentFromSettings(body) {
    if (!(selectedAgent == null ? void 0 : selectedAgent.id)) return;
    setSavingAgentConfig(true);
    setStatus("正在保存 Agent 配置");
    try {
      const updated = await updateAgent(selectedAgent.id, body);
      setAvailableAgents((current) => current.map((item) => item.id === updated.id ? updated : item));
      setDefaultAgentConfig((current) => (current == null ? void 0 : current.id) === updated.id ? updated : current);
      if (!sessionID) {
        setSettingsDraft((current) => {
          var _a3, _b2;
          return {
            ...current,
            llmProvider: ((_a3 = updated.config_version) == null ? void 0 : _a3.llm_provider) || current.llmProvider,
            llmModel: ((_b2 = updated.config_version) == null ? void 0 : _b2.llm_model) || current.llmModel
          };
        });
      }
      setStatus("Agent 配置已保存");
    } finally {
      setSavingAgentConfig(false);
    }
  }
  async function rollbackAgentFromSettings(sourceVersion) {
    if (!(selectedAgent == null ? void 0 : selectedAgent.id)) return;
    setRollingBackAgentVersion(sourceVersion);
    setStatus(`正在回滚 Agent 到版本 #${sourceVersion}`);
    try {
      const result = await rollbackAgentConfigVersion(selectedAgent.id, sourceVersion);
      const updated = result.agent;
      setAvailableAgents((current) => current.map((item) => item.id === updated.id ? updated : item));
      setDefaultAgentConfig((current) => (current == null ? void 0 : current.id) === updated.id ? updated : current);
      if (!sessionID) {
        setSettingsDraft((current) => {
          var _a3, _b2;
          return {
            ...current,
            llmProvider: ((_a3 = updated.config_version) == null ? void 0 : _a3.llm_provider) || current.llmProvider,
            llmModel: ((_b2 = updated.config_version) == null ? void 0 : _b2.llm_model) || current.llmModel
          };
        });
      }
      setStatus(`Agent 已回滚：版本 #${sourceVersion} 已复制为 #${result.new_version}`);
      return result;
    } finally {
      setRollingBackAgentVersion(0);
    }
  }
  async function exportAgentFromSettings() {
    var _a3;
    if (!(selectedAgent == null ? void 0 : selectedAgent.id)) return;
    setStatus("正在导出 Agent 配置");
    const response = await exportAgent(selectedAgent.id);
    const blob = await response.blob();
    const disposition = response.headers.get("Content-Disposition") || "";
    const fileName = ((_a3 = disposition.match(/filename="([^"]+)"/)) == null ? void 0 : _a3[1]) || `agent-${selectedAgent.id}.json`;
    const url = window.URL.createObjectURL(blob);
    const link2 = document.createElement("a");
    link2.href = url;
    link2.download = fileName;
    document.body.appendChild(link2);
    link2.click();
    link2.remove();
    window.URL.revokeObjectURL(url);
    setStatus("Agent 配置已导出");
  }
  async function importAgentFromSettings(file) {
    setStatus("正在导入 Agent 配置");
    let importedDocument;
    try {
      importedDocument = JSON.parse(await file.text());
    } catch {
      throw new Error("导入文件不是有效的 JSON");
    }
    const imported = await importAgent(importedDocument);
    setAvailableAgents((current) => [...current.filter((item) => item.id !== imported.id), imported]);
    setAgentID(imported.id);
    clearArtifactPreview();
    setSessionID("");
    setSessionMeta(null);
    setEnvironmentID("");
    setEventsResponse({ events: [] });
    setInterventionResponse({ interventions: [] });
    setArtifactResponse({ artifacts: [] });
    eventStreamCursorRef.current = 0;
    forgetSession();
    setSettingsDraft((current) => {
      var _a3, _b2;
      return {
        ...current,
        llmProvider: ((_a3 = imported.config_version) == null ? void 0 : _a3.llm_provider) || current.llmProvider,
        llmModel: ((_b2 = imported.config_version) == null ? void 0 : _b2.llm_model) || current.llmModel
      };
    });
    setStatus("Agent 配置已导入");
    return imported;
  }
  async function updateAgentPermissionsFromSettings(targetAgentID, tools) {
    setStatus("正在更新 Agent 工具权限");
    const updated = await updateAgent(targetAgentID, { tools });
    setAvailableAgents((current) => current.map((item) => item.id === updated.id ? updated : item));
    setDefaultAgentConfig((current) => (current == null ? void 0 : current.id) === updated.id ? updated : current);
    setStatus(`${updated.name || updated.id} 的工具权限已更新`);
    return updated;
  }
  if (settingsOpen) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SettingsPage,
      {
        activeSection: settingsSection,
        agents: settingsAgents,
        archivedSessions,
        currentSession: sessionMeta,
        onClose: closeSettingsPage,
        onCreateAgent: createAgentFromSettings,
        onExportAgent: exportAgentFromSettings,
        onImportAgent: importAgentFromSettings,
        onApplySessionConfig: (targetConfigVersion) => applySkillConfigToSession({
          operation: "sync",
          targetConfigVersion,
          throwOnError: true
        }),
        onOpenInspector: openInspectorPage,
        onOpenSession: openArchivedSessionFromSettings,
        onRestoreSession: (session2) => restoreTask(session2).catch((error) => setStatus(error.message)),
        onRollbackAgent: rollbackAgentFromSettings,
        onSaveAgent: (body) => saveAgentFromSettings(body).catch((error) => setStatus(error.message)),
        onSelectAgent: selectAgentFromSettings,
        onUpdateAgentPermissions: updateAgentPermissionsFromSettings,
        onModelCatalogChanged: refreshModelOptions,
        recentSessions,
        runtimeConfig,
        search: settingsSearch,
        sections: settingsSections,
        selectedAgent,
        restoringSessionID: sessionAction.startsWith("restore:") ? sessionAction.slice("restore:".length) : "",
        rollingBackVersion: rollingBackAgentVersion,
        savingAgent: savingAgentConfig,
        setActiveSection: setSettingsSection,
        setSearch: setSettingsSearch,
        skills: installedSkills,
        modelOptions,
        toolingCatalog,
        workspaceID: toolingWorkspaceID,
        onSkillsChanged: async () => {
          const targetAgentID = (sessionMeta == null ? void 0 : sessionMeta.agent_id) || (selectedAgent == null ? void 0 : selectedAgent.id) || "";
          const [skillsResponse, refreshedAgent] = await Promise.all([
            skills({ workspaceId: toolingWorkspaceID }),
            targetAgentID ? agent(targetAgentID) : Promise.resolve(null)
          ]);
          setInstalledSkills(skillsResponse.skills || []);
          if (refreshedAgent) {
            setAvailableAgents((current) => current.map((item) => item.id === refreshedAgent.id ? refreshedAgent : item));
            setDefaultAgentConfig((current) => (current == null ? void 0 : current.id) === refreshedAgent.id ? refreshedAgent : current);
          }
          return refreshedAgent;
        }
      }
    );
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "user-app", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "user-topbar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "topbar-brand", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "topbar-label", children: "TMA 工作台" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "topbar-context", children: (activePluginRoute == null ? void 0 : activePluginRoute.title) || (sessionMeta == null ? void 0 : sessionMeta.title) || sessionID || "通用智能体工作区" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "topbar-status", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "status-readout", children: status }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Pill, { value: runState }),
        principal ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "topbar-identity", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { title: principal.subject, children: principal.subject }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: ((_a2 = principal.roles) == null ? void 0 : _a2[principal.roles.length - 1]) || "viewer" })
        ] }) : null,
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary topbar-logout", type: "button", onClick: () => logout().catch((error) => setStatus(error.message)), children: "退出" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: `user-layout ${artifactPreview && !pluginRoutePath ? "has-artifact-preview" : ""} ${pluginRoutePath ? "plugin-route-active" : ""}`.trim(),
        style: { "--artifact-preview-width": `${artifactPreviewWidth}px` },
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("aside", { className: "user-sidebar", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Panel, { title: "工作区", className: "workspace-panel", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "workspace-controls", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "workspace-field", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "智能体" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "select",
                  {
                    value: selectedAgentValue,
                    onChange: (event) => {
                      const nextAgentID = event.target.value;
                      setAgentID(nextAgentID);
                      const selectedAgent2 = availableAgents.find((agent2) => agent2.id === nextAgentID);
                      if ((sessionMeta == null ? void 0 : sessionMeta.agent_id) && sessionMeta.agent_id !== nextAgentID) {
                        resetSessionViewForAgent(selectedAgent2);
                      } else if (selectedAgent2 == null ? void 0 : selectedAgent2.config_version) {
                        setSettingsDraft((current) => ({
                          ...current,
                          llmProvider: selectedAgent2.config_version.llm_provider || current.llmProvider || "",
                          llmModel: selectedAgent2.config_version.llm_model || current.llmModel || ""
                        }));
                      }
                    },
                    children: availableAgents.map((agent2) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: agent2.id, children: agent2.name || agent2.id }, agent2.id))
                  }
                )
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", className: "workspace-primary-action", onClick: () => {
                closePluginRoute();
                startNewTask();
              }, children: "新建任务" })
            ] }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Panel, { title: "任务", className: "tasks-panel", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "stack task-panel-content", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "input",
                {
                  value: taskSearch,
                  onChange: (event) => setTaskSearch(event.target.value),
                  placeholder: "搜索任务..."
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "task-section-scroll", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "task-section-list", children: [
                filteredTaskSessions.length ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "task-section", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "task-section-title", children: "全部任务" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "turn-list", children: visibleTaskSessions.map((session2) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
                    "div",
                    {
                      className: `turn-item task-nav-item ${session2.id === sessionID ? "active" : ""}`,
                      onMouseEnter: (event) => showTaskHoverPreview(session2, event),
                      onMouseLeave: hideTaskHoverPreview,
                      children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(
                          "button",
                          {
                            className: "task-nav-open",
                            type: "button",
                            onClick: () => {
                              closePluginRoute();
                              openSession(session2).catch((error) => setStatus(error.message));
                            },
                            children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "task-nav-row", children: [
                              /* @__PURE__ */ jsxRuntimeExports.jsx(
                                TaskStatusIcon,
                                {
                                  status: session2.id === sessionID && waitingForReply ? effectiveSessionStatus === "interrupting" ? "interrupting" : "running" : session2.status
                                }
                              ),
                              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: session2.title || "Untitled task" })
                            ] })
                          }
                        ),
                        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "task-inline-actions", children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx(
                            "button",
                            {
                              className: `icon-button task-pin-button ${session2.pinned_at ? "active" : ""}`,
                              type: "button",
                              "aria-label": session2.pinned_at ? "取消置顶" : "置顶任务",
                              disabled: sessionAction === `pin:${session2.id}`,
                              onClick: () => toggleTaskPin(session2).catch((error) => setStatus(error.message)),
                              children: /* @__PURE__ */ jsxRuntimeExports.jsx(PinIcon, { filled: Boolean(session2.pinned_at) })
                            }
                          ),
                          /* @__PURE__ */ jsxRuntimeExports.jsx(
                            "button",
                            {
                              className: `icon-button task-menu-button ${taskMenuSessionID === session2.id ? "active" : ""}`,
                              ref: (element2) => {
                                if (element2) taskMenuButtonRefs.current.set(session2.id, element2);
                                else taskMenuButtonRefs.current.delete(session2.id);
                              },
                              type: "button",
                              "aria-label": "更多操作",
                              "aria-expanded": taskMenuSessionID === session2.id,
                              onClick: (event) => {
                                if (taskMenuSessionID === session2.id) {
                                  setTaskMenuSessionID("");
                                  setTaskMenuPosition(null);
                                  return;
                                }
                                const rect = event.currentTarget.getBoundingClientRect();
                                const menuWidth = 180;
                                const menuHeight = 192;
                                const gap = 8;
                                const opensUp = rect.bottom + gap + menuHeight > window.innerHeight;
                                setTaskMenuPosition({
                                  left: Math.max(gap, Math.min(window.innerWidth - menuWidth - gap, rect.right - menuWidth)),
                                  top: opensUp ? Math.max(gap, rect.top - menuHeight - gap) : rect.bottom + gap
                                });
                                setTaskMenuSessionID(session2.id);
                              },
                              children: /* @__PURE__ */ jsxRuntimeExports.jsx(MoreIcon, {})
                            }
                          ),
                          taskMenuSessionID === session2.id && taskMenuPosition ? reactDomExports.createPortal(
                            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "task-action-menu task-action-menu-portal", role: "menu", style: taskMenuPosition, children: [
                              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", role: "menuitem", onClick: () => openTaskMetadata(session2), children: "标签与摘要" }),
                              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", role: "menuitem", onClick: () => {
                                setTaskMenuSessionID("");
                                openTaskComparison(session2);
                              }, children: "任务对比" }),
                              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", role: "menuitem", disabled: sessionAction === `rerun:${session2.id}`, onClick: () => {
                                setTaskMenuSessionID("");
                                rerunTask(session2).catch((error) => setStatus(error.message));
                              }, children: "按原配置重跑" }),
                              session2.status !== "terminated" ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", role: "menuitem", disabled: sessionAction === `archive:${session2.id}`, onClick: () => {
                                setTaskMenuSessionID("");
                                archiveTask(session2.id).catch((error) => setStatus(error.message));
                              }, children: "归档任务" }) : null,
                              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "danger", type: "button", role: "menuitem", disabled: sessionAction === `delete:${session2.id}`, onClick: () => {
                                setTaskMenuSessionID("");
                                deleteTask(session2.id).catch((error) => setStatus(error.message));
                              }, children: "删除任务" })
                            ] }),
                            document.body
                          ) : null
                        ] })
                      ]
                    },
                    session2.id
                  )) }),
                  hasMoreTasks ? /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary task-more-button", type: "button", onClick: () => setVisibleTaskCount((current) => current + 10), children: "显示更多任务" }) : null
                ] }) : null,
                !filteredTaskSessions.length ? /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: hasTaskSearch ? "没有匹配的任务。" : "暂无任务。" }) : null
              ] }) })
            ] }) }),
            pluginNavigation.length ? /* @__PURE__ */ jsxRuntimeExports.jsx(Panel, { title: "扩展", className: "plugin-navigation-panel", children: /* @__PURE__ */ jsxRuntimeExports.jsx("nav", { className: "plugin-navigation", "aria-label": "扩展页面", children: pluginNavigation.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "button",
              {
                className: item.route === pluginRoutePath ? "active" : "",
                type: "button",
                onClick: () => navigatePluginRoute(item.route),
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": "true", className: "plugin-navigation-mark" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: item.title })
                ]
              },
              `${item.pluginID}:${item.id}`
            )) }) }) : null,
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sidebar-footer", children: /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary sidebar-settings-button", type: "button", onClick: openSettingsPage, children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "设置" }) }) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("main", { className: `user-main ${pluginRoutePath ? "plugin-route-main" : ""}`, children: pluginRoutePath ? /* @__PURE__ */ jsxRuntimeExports.jsx(
            PluginRouteHost,
            {
              loading: pluginLoadState === "loading",
              onBack: closePluginRoute,
              onError: (error) => workbenchNotificationService.show({
                level: "error",
                title: "插件页面错误",
                message: error.message || String(error),
                dedupeKey: `plugin.route.${pluginRoutePath}`
              }),
              path: pluginRoutePath,
              route: activePluginRoute
            }
          ) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "user-thread", onScroll: handleThreadScroll, ref: threadRef, children: [
              hasPendingApprovals ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "approval-alert", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { children: [
                    interventions$1.length,
                    " approval",
                    interventions$1.length === 1 ? "" : "s",
                    " waiting"
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: "Review the pending tool call before the agent continues." })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "approval-alert-actions", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => interruptFromApprovals().catch((error) => setStatus(error.message)), children: "终止任务" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: () => setApprovalsOpen(true), children: "查看审批" })
                ] })
              ] }) : null,
              /* @__PURE__ */ jsxRuntimeExports.jsx(WorkflowProgress, { run: workflowRun, onStop: () => stopWorkflowRun().catch((error) => setStatus(error.message)) }),
              renderedChatTimelineEvents.length ? renderedChatTimelineEvents.map((event, eventIndex) => {
                if (event.type === "runtime.progress_message") {
                  const progressText = String(eventData(event).text || "").trim();
                  return progressText ? /* @__PURE__ */ jsxRuntimeExports.jsx("article", { "aria-label": "通用智能体过程更新", className: "agent-progress-message", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: progressText }) }, `${event.seq}-${event.type}`) : null;
                }
                if (event.type === "user.message" || event.type === "agent.message" || event.type === "agent.streaming") {
                  const streaming = event.type === "agent.streaming";
                  const role = event.type === "user.message" ? "user" : "agent";
                  const messageKey = role === "agent" && payload(event).turn_id ? `message-agent-${payload(event).turn_id}` : `${event.seq}-${event.type}`;
                  const messageArtifacts = streaming ? [] : role === "agent" ? finalAgentMessageArtifacts(event, artifacts$1) : uploadedMessageArtifacts(event, artifacts$1);
                  return /* @__PURE__ */ jsxRuntimeExports.jsxs("article", { "aria-live": streaming ? "polite" : void 0, className: `message ${role}${streaming ? " streaming" : ""}`, children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs(Meta, { children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: role === "user" ? "你" : "通用智能体" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: formatTime(event.created_at) }),
                      streaming ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "生成中" }) : null
                    ] }),
                    role === "agent" ? /* @__PURE__ */ jsxRuntimeExports.jsx(
                      AgentMessageBody,
                      {
                        artifacts: messageArtifacts,
                        sessionID,
                        text: eventText(event),
                        streaming,
                        onPreview: (artifact) => previewArtifact(artifact).catch((error) => setStatus(error.message))
                      }
                    ) : /* @__PURE__ */ jsxRuntimeExports.jsx(
                      MessageBody,
                      {
                        event,
                        artifacts: messageArtifacts,
                        sessionID,
                        onPreview: (artifact) => previewArtifact(artifact).catch((error) => setStatus(error.message))
                      }
                    )
                  ] }, messageKey);
                }
                const nextEvent = renderedChatTimelineEvents[eventIndex + 1];
                const toolLifecycle = toolCallLifecycles.get(toolCallID(event));
                const terminalToolEvent = terminalToolLifecycleEvent(toolLifecycle);
                const isLatestActiveEvent = eventIndex === renderedChatTimelineEvents.length - 1 && ["running", "interrupting", "provisioning"].includes(effectiveSessionStatus);
                return /* @__PURE__ */ jsxRuntimeExports.jsx(
                  ProcessEventCard,
                  {
                    active: isLatestActiveEvent,
                    activeSkillKeys,
                    completedAt: (terminalToolEvent == null ? void 0 : terminalToolEvent.created_at) || (nextEvent == null ? void 0 : nextEvent.created_at) || "",
                    enabledSkillKeys,
                    event,
                    latestSkillLifecycle,
                    onApplySessionConfig: applySkillConfigToSession,
                    onRequestSkillEnable: requestInstalledSkillEnable,
                    onRequestSkillDisable: requestInstalledSkillDisable,
                    sessionConfigApplyBusy: applyingSessionConfigVersion,
                    sessionConfigVersion: Number((sessionMeta == null ? void 0 : sessionMeta.agent_config_version) || 0),
                    skillEnableBusy: requestingSkillEnable,
                    skillDisableBusy: requestingSkillDisable,
                    skillEnableDisabled: Boolean(applyingSessionConfigVersion || waitingForReply || hasPendingApprovals || ["running", "interrupting", "provisioning"].includes(effectiveSessionStatus)),
                    toolLifecycle
                  },
                  `${event.seq}-${event.type}`
                );
              }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "welcome-state", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "welcome-hero", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "welcome-eyebrow", children: "托管智能体工作区" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: "开始一个任务，让通用智能体持续推进它。" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: "TMA 可以检查代码、执行校验、编辑文件、调用工具，并持续推进一个会话直到工作完成。 你可以从下面选择一个起点，或者直接输入自己的请求。" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "welcome-tags", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "代码审查" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "文件修改" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "构建测试" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "工具编排" })
                  ] })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "starter-grid task-template-starter-grid", children: taskTemplates$1.length ? taskTemplates$1.map((template) => /* @__PURE__ */ jsxRuntimeExports.jsxs("article", { className: "starter-card task-template-starter", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "task-template-starter-head", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: template.category }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: template.title })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: template.description }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "task-template-starter-actions", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", onClick: () => applyTaskTemplate(template, false), children: "填充" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: () => applyTaskTemplate(template, true), children: "工作流" })
                  ] })
                ] }, template.id)) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "empty-state compact", children: "正在加载任务模板..." }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "welcome-note", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "提示" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "你可以在任务开始前预先选择模型、审批模式和运行环境。" })
                ] })
              ] }),
              !streamingReply && waitingForReply ? /* @__PURE__ */ jsxRuntimeExports.jsxs("article", { className: "message agent pending", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Meta, { children: /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "通用智能体" }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "message-text", children: "正在处理并生成回复…" })
              ] }) : null
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("section", { className: "composer", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "div",
              {
                className: `composer-shell ${composerDragActive ? "drag-active" : ""}`,
                onDragEnter: (event) => {
                  event.preventDefault();
                  setComposerDragActive(true);
                },
                onDragOver: (event) => {
                  event.preventDefault();
                  event.dataTransfer.dropEffect = "copy";
                },
                onDragLeave: (event) => {
                  if (!event.currentTarget.contains(event.relatedTarget)) setComposerDragActive(false);
                },
                onDrop: handleComposerDrop,
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "input",
                    {
                      className: "composer-file-input",
                      ref: composerFileInputRef,
                      type: "file",
                      multiple: true,
                      onChange: (event) => {
                        addComposerFiles(event.target.files);
                        event.target.value = "";
                      }
                    }
                  ),
                  composerFiles.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "composer-attachments", "aria-label": "待上传文件", children: composerFiles.map((item) => {
                    const extension2 = item.file.name.includes(".") ? item.file.name.split(".").pop().slice(0, 5).toUpperCase() : "FILE";
                    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `composer-attachment ${item.status}`, title: item.error || item.file.name, children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "composer-attachment-icon", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(FileIcon, {}),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: extension2 })
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "composer-attachment-copy", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: item.file.name }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: item.status === "uploading" ? "上传中..." : item.status === "uploaded" ? "已上传" : item.status === "error" ? "上传失败，发送时重试" : formatFileSize(item.file.size) })
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "icon-button", type: "button", disabled: uploadingFiles, onClick: () => removeComposerFile(item.id), title: "移除文件", "aria-label": `移除 ${item.file.name}`, children: /* @__PURE__ */ jsxRuntimeExports.jsx(CloseIcon, {}) })
                    ] }, item.id);
                  }) }) : null,
                  composerVisionError() ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "composer-vision-warning", role: "alert", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: composerVisionError() }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", onClick: () => {
                      setSettingsSection("models");
                      setSettingsOpen(true);
                    }, children: "配置模型" })
                  ] }) : null,
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "textarea",
                    {
                      value: task,
                      onChange: (event) => setTask(event.target.value),
                      onKeyDown: handleTaskComposerKeyDown,
                      onPaste: handleComposerPaste,
                      placeholder: "让 TMA 帮你构建、检查、修改或执行某项工作... 回车发送，Shift+Enter 换行。"
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "composer-input-actions", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "button",
                    {
                      className: "icon-button composer-attach-button",
                      type: "button",
                      disabled: uploadingFiles || composerFiles.length >= maxComposerFiles,
                      onClick: () => {
                        var _a3;
                        return (_a3 = composerFileInputRef.current) == null ? void 0 : _a3.click();
                      },
                      "aria-label": "上传文件",
                      children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": "true", children: "+" })
                    }
                  ) }),
                  selectedTaskTemplate ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "composer-template-selection", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: workflowMode ? "工作流" : "任务模板" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: selectedTaskTemplate.title }),
                      workflowMode ? /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
                        ((_b = selectedTaskTemplate.workflow_steps) == null ? void 0 : _b.length) || 0,
                        " 步顺序执行"
                      ] }) : null
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "icon-button", type: "button", title: "清除任务模板", "aria-label": "清除任务模板", onClick: clearTaskTemplate, children: "×" })
                  ] }) : null,
                  selectedGuidanceItems.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "composer-guidance-chips", children: selectedGuidanceItems.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "composer-guidance-chip", type: "button", onClick: () => toggleGuidanceKey(item.key), children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: toolingGuidanceLabel(item) }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "×" })
                  ] }, item.key)) }) : null,
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "composer-toolbar", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "composer-settings-inline", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "composer-setting", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "审批" }),
                        /* @__PURE__ */ jsxRuntimeExports.jsxs(
                          "select",
                          {
                            disabled: savingSettings,
                            value: settingsDraft.interventionMode,
                            onChange: (event) => applySessionSettings({ interventionMode: event.target.value }).catch((error) => setStatus(error.message)),
                            children: [
                              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "request_approval", children: "先审批" }),
                              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "approve_for_me", children: "替我审批" }),
                              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "full_access", children: "完全访问" })
                            ]
                          }
                        )
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "composer-setting", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "运行环境" }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx(
                          "select",
                          {
                            disabled: savingSettings,
                            value: settingsDraft.toolRuntime,
                            onChange: (event) => applySessionSettings({ toolRuntime: event.target.value }).catch((error) => setStatus(error.message)),
                            children: runtimeOptions.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: option.value, children: option.label }, option.value))
                          }
                        )
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "composer-setting composer-setting-model", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "模型" }),
                        /* @__PURE__ */ jsxRuntimeExports.jsxs(
                          "select",
                          {
                            disabled: !modelOptions.length || savingSettings,
                            value: selectedModelValue,
                            onChange: (event) => {
                              const [llmProvider, llmModel] = String(event.target.value || "").split("::");
                              applySessionSettings({ llmModel: llmModel || "", llmProvider: llmProvider || "" }).catch((error) => setStatus(error.message));
                            },
                            children: [
                              !modelOptions.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "No models" }) : null,
                              modelOptions.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsxs("option", { value: `${option.llmProvider}::${option.llmModel}`, children: [
                                option.label,
                                " · ",
                                modelCapabilityLabel(option.capabilityType)
                              ] }, `${option.llmProvider}:${option.llmModel}`))
                            ]
                          }
                        )
                      ] })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "composer-toolbar-end", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "secondary composer-tool-button", type: "button", onClick: () => setToolPickerOpen(true), children: [
                        "工具",
                        selectableToolingItems.length ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                          selectedGuidanceItems.length,
                          "/",
                          selectableToolingItems.length
                        ] }) : null
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "button",
                        {
                          type: "button",
                          className: primaryAction.className,
                          disabled: primaryAction.disabled,
                          onClick: () => {
                            var _a3;
                            if (primaryAction.mode === "start") {
                              startSession().catch((error) => setStatus(error.message));
                              return;
                            }
                            if (primaryAction.mode === "send") {
                              if (workflowMode && ((_a3 = selectedTaskTemplate == null ? void 0 : selectedTaskTemplate.workflow_steps) == null ? void 0 : _a3.length) && task.trim()) {
                                startWorkflow(sessionID, selectedTaskTemplate, task.trim()).catch((error) => setStatus(error.message));
                              } else {
                                sendTask().catch((error) => setStatus(error.message));
                              }
                              return;
                            }
                            if (primaryAction.mode === "interrupt") {
                              confirmInterruptTask().catch((error) => setStatus(error.message));
                            }
                          },
                          children: primaryAction.label
                        }
                      )
                    ] })
                  ] })
                ]
              }
            ) })
          ] }) }),
          !pluginRoutePath && artifactPreview ? /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "artifact-preview-pane", "aria-label": "结果预览", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "artifact-resize-handle", type: "button", "aria-label": "拖动调整预览宽度", onPointerDown: startArtifactResize }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "artifact-preview-pane-header", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "artifact-preview-pane-label", children: "结果预览" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: artifactPreview.resource.title }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: artifactPreview.contentType || artifactPreview.resource.mimeType || artifactPreview.resource.type || "文件" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "artifact-preview-pane-actions", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary icon-button", type: "button", onClick: () => downloadArtifactResource(artifactPreview.resource), title: "下载", "aria-label": "下载", children: "↓" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "icon-button", type: "button", onClick: clearArtifactPreview, title: "关闭预览", "aria-label": "关闭预览", children: /* @__PURE__ */ jsxRuntimeExports.jsx(CloseIcon, {}) })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "artifact-preview-mode-tabs", role: "tablist", "aria-label": "结果查看方式", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    className: artifactPreviewMode === "preview" ? "active" : "",
                    type: "button",
                    role: "tab",
                    "aria-selected": artifactPreviewMode === "preview",
                    onClick: () => setArtifactPreviewMode("preview"),
                    children: "预览"
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    className: artifactPreviewMode === "source" ? "active" : "",
                    type: "button",
                    role: "tab",
                    "aria-selected": artifactPreviewMode === "source",
                    disabled: !isMarkdownResource(artifactPreview.resource, artifactPreview.contentType) && !isHTMLResource(artifactPreview.resource, artifactPreview.contentType),
                    title: isMarkdownResource(artifactPreview.resource, artifactPreview.contentType) || isHTMLResource(artifactPreview.resource, artifactPreview.contentType) ? "查看文件源码" : "当前文件不支持源码切换",
                    onClick: () => setArtifactPreviewMode("source"),
                    children: "源码"
                  }
                )
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "artifact-preview-pane-body", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ArtifactPreviewContent, { preview: artifactPreview, mode: artifactPreviewMode }) })
          ] }) : null,
          !pluginRoutePath ? /* @__PURE__ */ jsxRuntimeExports.jsx("aside", { className: "user-sidebar right", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Panel,
            {
              className: "right-tab-panel",
              title: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "right-panel-tabs", role: "tablist", "aria-label": "右侧面板", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: rightPanelTab === "results" ? "active" : "", type: "button", role: "tab", "aria-selected": rightPanelTab === "results", onClick: () => setRightPanelTab("results"), children: [
                  "结果",
                  resultFiles.length ? ` (${resultFiles.length})` : ""
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: rightPanelTab === "activity" ? "active" : "", type: "button", role: "tab", "aria-selected": rightPanelTab === "activity", onClick: () => setRightPanelTab("activity"), children: "执行" })
              ] }),
              children: rightPanelTab === "results" ? resultFiles.length ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "artifact-tree", role: "tree", "aria-label": "结果文件目录", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ArtifactTreeNode, { node: artifactTree, depth: 0, selectedArtifactID: ((_c = artifactPreview == null ? void 0 : artifactPreview.resource) == null ? void 0 : _c.id) || "", onPreview: (artifact) => previewArtifact(artifact).catch((error) => setStatus(error.message)) }) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "还没有生成结果文件。" }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "list activity-list", role: "tabpanel", children: activityEvents.length ? activityEvents.map((item) => {
                const activity = item.activity;
                const event = item.event;
                return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `list-item activity-item ${activity.kind}`, children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "activity-head", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: activity.title }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: formatTime(event.created_at) })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "subtle", children: [
                    activity.detail || activitySummary(event) || "暂无详情。",
                    item.count > 1 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                      " · ",
                      item.count,
                      " 次更新"
                    ] }) : null
                  ] })
                ] }, `${event.seq}-${event.type}-${item.count}`);
              }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Empty, { children: "还没有执行记录。" }) })
            }
          ) }) : null
        ]
      }
    ),
    toolPickerOpen ? /* @__PURE__ */ jsxRuntimeExports.jsx(
      ToolPickerModal,
      {
        loading: toolingLoading,
        error: toolingError,
        sections: toolingCatalog.sections,
        selectedKeys: selectedGuidanceKeys,
        onToggle: toggleGuidanceKey,
        onClose: () => setToolPickerOpen(false),
        onClear: () => setSelectedGuidanceKeys([])
      }
    ) : null,
    templatePickerOpen ? /* @__PURE__ */ jsxRuntimeExports.jsx(
      TaskTemplateModal,
      {
        templates: taskTemplates$1,
        onClose: () => setTemplatePickerOpen(false),
        onSelect: applyTaskTemplate
      }
    ) : null,
    comparisonOpen ? /* @__PURE__ */ jsxRuntimeExports.jsx(
      SessionComparisonModal,
      {
        sessions: recentSessions,
        leftID: comparisonLeftID,
        rightID: comparisonRightID,
        onLeftChange: (value) => {
          setComparisonLeftID(value);
          setComparisonResult(null);
        },
        onRightChange: (value) => {
          setComparisonRightID(value);
          setComparisonResult(null);
        },
        onCompare: () => loadTaskComparison().catch((error) => setStatus(error.message)),
        onClose: () => setComparisonOpen(false),
        loading: comparisonLoading,
        result: comparisonResult,
        modelOptions,
        variantModel: comparisonVariantModel,
        onVariantModelChange: setComparisonVariantModel,
        onCreateVariant: () => createComparisonVariant().catch((error) => setStatus(error.message)),
        creatingVariant: sessionAction === `rerun:${comparisonLeftID}`
      }
    ) : null,
    metadataSession ? /* @__PURE__ */ jsxRuntimeExports.jsx(
      TaskMetadataModal,
      {
        session: metadataSession,
        tags: metadataTagsDraft,
        onTagsChange: setMetadataTagsDraft,
        onSave: () => saveTaskMetadata().catch((error) => setStatus(error.message)),
        onClose: () => setMetadataSession(null),
        saving: savingMetadata || sessionAction === `pin:${metadataSession.id}`,
        onTogglePin: () => toggleTaskPin(metadataSession).catch((error) => setStatus(error.message))
      }
    ) : null,
    approvalsOpen ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "approval-modal-backdrop", role: "presentation", onClick: deferApprovals, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "approval-modal", role: "dialog", "aria-modal": "true", "aria-label": "Approvals", onClick: (event) => event.stopPropagation(), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "approval-modal-header", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: "Approvals" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "subtle", children: sessionID ? `Session ${sessionID}` : "No session selected" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "approval-modal-actions", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(decidingApprovalID), onClick: () => refresh().catch((error) => setStatus(error.message)), children: "刷新" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(decidingApprovalID), onClick: () => interruptFromApprovals().catch((error) => setStatus(error.message)), children: "终止任务" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "secondary", type: "button", disabled: Boolean(decidingApprovalID), onClick: deferApprovals, children: "稍后处理" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "approval-list-main", children: interventions$1.length ? interventions$1.map((intervention) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        ApprovalCard,
        {
          intervention,
          busy: Boolean(decidingApprovalID),
          active: decidingApprovalID === intervention.call_id,
          onApprove: (item) => approve(item).catch((error) => setStatus(error.message)),
          onReject: (item) => reject(item).catch((error) => setStatus(error.message))
        },
        intervention.call_id
      )) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "empty-state compact", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: "No pending approvals" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "When a tool call needs confirmation, it will appear here with Approve and Reject actions." })
      ] }) })
    ] }) }) : null,
    taskHoverPreview ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "task-hover-floating",
        style: { left: `${taskHoverPreview.left}px`, top: `${taskHoverPreview.top}px` },
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: taskHoverPreview.title }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: taskHoverPreview.time })
        ]
      }
    ) : null
  ] });
}
function WorkbenchRoot() {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WorkbenchApp, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsx(DialogHost, { service: workbenchDialogService }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(NotificationHost, { service: workbenchNotificationService })
  ] });
}
clientExports.createRoot(document.getElementById("root")).render(/* @__PURE__ */ jsxRuntimeExports.jsx(WorkbenchRoot, {}));
